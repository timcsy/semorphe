{
  "componentId": "python:set_append",
  "abstractComponent": null,
  "_abstractComponent_why": "C++ 那側的集合插入回一個「有沒有真的插進去」的旗標，而這一顆什麼都不回。行為不同，不宣告等價。",
  "properties": [],
  "children": {
    "obj": "expression",
    "value": "expression"
  },
  "role": "statement",
  "owner": "(python)",
  "paths": {
    "lift": "./lift-pattern.json",
    "liftStrategy": "./lift-strategy.ts",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json"
  },
  "_why": "🔴 第五十條護欄的通用桶點名的（`.add` ×2，2026-08-23）——集合的加入在同一天才進內建表，而積木要跟上。\n⚠️ **名字是 `set_append` 不是 `container_add`**：做的仍然是 `append`（加一個進去），而**主體是集合**——「同一個操作在任何主體上用同一個字」。"
}
