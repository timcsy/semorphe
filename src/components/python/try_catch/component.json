{
  "componentId": "python:try_catch",
  "abstractComponent": null,
  "_abstractComponent_why": "C++ 那顆抓的是型別（`catch (const std::exception&)`），Python 抓的是例外**類別的名字**而且可以省略。形狀相近而保證不同。",
  "properties": [],
  "children": {
    "body": "statements",
    "handlers": "statements"
  },
  "role": "statement",
  "owner": "(python)",
  "paths": {
    "lift": "./lift-pattern.json",
    "liftStrategy": "./lift-strategy.ts",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json"
  },
  "_children_why": "🔴 **`handlers` 是可變數量的分支（每一個是同族的例外分支元件）**（2026-08-21）。\n第一版只收一個 `except`，理由寫著「收一半會產出少了分支的合法程式」——那是對的，所以多分支整顆降級。而**教學語料的第一段就有兩個**（`except ValueError` ／ `except ZeroDivisionError`），所以邊界往前推。\n⚠️ `else`／`finally` **仍然整顆降級**——同一個理由，還沒有地方放。"
}
