{
  "componentId": "python:import",
  "abstractComponent": null,
  "_abstractComponent_why": "C++ 那側最接近的是前置處理指令，而它是**文字層的貼上**；Python 的這個在執行期綁一個模組物件。形狀不同，指同一個抽象父會讓「可以互換」變成假的。",
  "properties": [
    { "name": "name", "kind": "identifier", "default": "math" }
  ],
  "children": {},
  "role": "statement",
  "owner": "(python)",
  "paths": {
    "lift": "./lift-pattern.json",
    "generate": "./generate.ts",
    "execute": null,
    "_execute_why": "**顯式的空。** 這個直譯器沒有模組系統——模組成員（`math.pi`／`math.sqrt`）由內建表直接提供，所以「引入」這個動作沒有執行期行為。\n⚠️ 而它**不是**「還沒做」：即使將來有模組系統，這一行仍然是宣告性的。",
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json"
  },
  "skipPaths": ["execute"],
  "skipReasons": { "execute": "declarative" }
}
