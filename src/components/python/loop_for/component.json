{
  "componentId": "python:loop_for",
  "abstractComponent": null,
  "_abstractComponent_why": "🔴 **刻意是 null，而這一顆的理由最硬**：Python 的 `for` 是 **for-each**（走訪一個序列），C++ 的 `for` 是**三格式**（初始／條件／遞增）。`for i in range(3)` 與 `for (int i=0;i<3;i++)` 產出的結果一樣，**而它們是兩個不同的概念**。",
  "properties": [
    {
      "name": "obj",
      "kind": "literal",
      "default": "i",
      "_kind_why": "⚠️ `obj` **不是識別字**——它裝的是一個【容器運算式】：`v`、`d2[a]`、`a.b` 都合法。🔴 2026-09-14 拿 218 支學生程式量到 57 筆「不合法的名字」，而每一筆都是 `d2[a]` 這種正常寫法——**宣告寫錯了，不是學生寫錯**。改成 `literal`（只驗存在），而「字串屬性裝了結構」這件事由第七十二條護欄追蹤，不假裝它是名字。"
    }
  ],
  "slots": {
    "iterable": "expression",
    "body": "statements",
    "targets": "param_decl"
  },
  "role": "statement",
  "owner": "(python)",
  "_owner_why": "⚠️ **章要蓋 `(python)` 不是 `(core)`。** 工具箱的來源段落寫著 `{ from: '(python)' }`，查無此章就整批回傳空——**症狀不是報錯，是工具箱裡找不到這顆積木**。",
  "annotations": {
    "control_flow": "loop",
    "debug_step": true
  },
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "lift": "./lift-pattern.json",
    "execute": "./execute.ts",
    "liftStrategy": "./lift-strategy.ts"
  },
  "traits": {
    "declaresVariable": [
      "PARAM_{i}"
    ]
  },
  "_traits_why": "**這顆積木會生出變數**——迴圈的每一個被綁定的名字都要進變數下拉。\n⚠️ 欄位名跟著形態走：2026-08-21 從單一的 `NAME` 換成 paramList 的 `PARAM_{i}`（多目標的 `for k, v in …`），而**宣告不跟著改的話下拉會少一整族名字**——護欄 `audit-program-root` 當場擋下。",
  "_children_why": "**多目標（`for k, v in …`）走 `targets` 子節點**，與同族函式定義的參數同一個做法；單一目標仍然走 `obj` 屬性——**舊存檔照樣打得開**，而積木上那個名字欄位也還在用它。\n⚠️ 兩者不是兩份真相：`targets` 有東西時它是唯一的真實，`obj` 是它的第一格。"
}
