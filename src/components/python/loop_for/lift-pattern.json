[
  {
    "id": "python_for",
    "grammar": "tree-sitter-python",
    "astNodeType": "for_statement",
    "component": {
      "componentId": "python:loop_for"
    },
    "fieldMappings": [],
    "priority": 10,
    "_why": "🟢 **2026-08-21 從純資料換成具名策略**：原本 `left` 走 `extract: \"text\"`，而 `for a, b in …` 的 `left` 是 `pattern_list`，`text` 拿到的是字串 `\"a, b\"`——**字串裡藏著結構**。舊的 `_why` 自己寫著那是「已知的邊界」，而它的代價在執行期才付：迴圈宣告了一個名字叫 `k, v` 的變數。\n見 `lift-strategy.ts` 的檔頭。",
    "liftStrategy": "python:liftLoopFor"
  }
]
