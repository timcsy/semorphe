[
  {
    "id": "python_augmented_assignment",
    "grammar": "tree-sitter-python",
    "astNodeType": "augmented_assignment",
    "component": {
      "componentId": "python:var_assign_compound"
    },
    "fieldMappings": [
      {
        "semantic": "name",
        "ast": "left",
        "extract": "text"
      },
      {
        "semantic": "operator",
        "ast": "operator",
        "extract": "text"
      },
      {
        "semantic": "value",
        "ast": "right",
        "extract": "lift"
      }
    ],
    "_why": "🟢 **左邊可以是變數名，也可以是 `self.n` 這種取成員**（2026-08-21）。\n第一版用 `constraints` 把左邊限定成單純的變數名，理由是「認一半不如誠實降級」——而類別做出來之後 `self.n += k` 是最常見的一行，那個限制就從誠實變成擋路。\n⚠️ `nums[0] += 1`（左邊是 `subscript`）**仍然降級**——那需要一個索引接點，而這顆的積木上沒有那一格。"
  }
]
