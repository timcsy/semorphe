[
  {
    "id": "python_assignment",
    "grammar": "tree-sitter-python",
    "astNodeType": "assignment",
    "component": {
      "componentId": "python:var_assign"
    },
    "fieldMappings": [
      {
        "semantic": "obj",
        "ast": "left",
        "extract": "text"
      },
      {
        "semantic": "value",
        "ast": "right",
        "extract": "lift"
      }
    ],
    "priority": 10,
    "_why": "⚠️ **`left` 用 `text` 而不是 `lift`**：左邊是**被指派的名字**，不是一個要被求值的運算式。用 `lift` 會把它變成一顆 `var_ref` 積木掛在插槽上——而那讀起來像「把 x 的值設成…」。\n\n🟢 **而「左邊不是單純名字」的那些已經被收走了**（2026-08-22）：`a[0] = 5` 與 `a.b = 5` 各有自己的元件，priority 14 比這一筆高。所以這裡多一個限制**明說**它只收裸名字——不加的話兩筆會互搶，而搶輸的那一種會**安靜地**回到「把左邊當字串」那條老路。",
    "constraints": [
      {
        "field": "left",
        "nodeType": "identifier"
      }
    ]
  }
]
