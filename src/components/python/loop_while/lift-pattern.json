[
  {
    "id": "python_while",
    "grammar": "tree-sitter-python",
    "astNodeType": "while_statement",
    "component": {
      "componentId": "python:loop_while"
    },
    "fieldMappings": [
      {
        "semantic": "condition",
        "ast": "condition",
        "extract": "lift"
      },
      {
        "semantic": "body",
        "ast": "body",
        "extract": "liftBody"
      }
    ],
    "priority": 10,
    "_why": "⚠️ **`while ... else` 沒有收**：帶 `alternative` 的 `while_statement` 仍然會匹配這一筆，而那個 else 分支**會被靜靜丟掉**。🔴 那是一個已知的邊界，記在這裡以免它被當成「已支援」。\n🔴 **`while … else:` 不由這一筆接**（2026-08-23）：這顆元件沒有地方放那一段，而在此之前它被**安靜地丟掉**——`else` 底下的程式碼在來回一趟之後不見了，產出的碼仍然合法。`absent` 讓它整顆走誠實降級（原文逐字留著）。",
    "constraints": [
      {
        "field": "alternative",
        "absent": true
      }
    ]
  }
]
