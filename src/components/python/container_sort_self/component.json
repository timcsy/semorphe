{
  "componentId": "python:container_sort_self",
  "abstractComponent": null,
  "_abstractComponent_why": "C++ 那側的排序**一律就地**（沒有「產一份新的」那種），所以那邊沒有這個對立。不宣告等價。",
  "properties": [],
  "children": {
    "obj": "expression",
    "key": "expression",
    "reverse": "expression"
  },
  "role": "statement",
  "owner": "(python)",
  "paths": {
    "lift": "./lift-pattern.json",
    "liftStrategy": "./lift-strategy.ts",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json"
  },
  "_why": "🔴 第五十條護欄的通用桶點名的（`.sort` ×2）。\n⚠️ **名字裡的 `self` 是種差**：做的仍然是 `sort`，差別在**改的是自己**（而且回 `None`）——同族那顆 `sorted(...)` 產一份新的。\n> 一個回 `None` 的東西被當成運算式用，是初學者最常見的錯之一（`xs = xs.sort()`）——**兩顆積木長得不一樣，那個錯就做不出來**。"
}
