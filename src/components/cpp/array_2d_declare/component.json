{
  "componentId": "cpp:array_2d_declare",
  "abstractComponent": "cpp:var_declare",
  "properties": [
    {
      "name": "type",
      "kind": "enum",
      "default": "int"
    },
    {
      "name": "name",
      "kind": "identifier",
      "default": "arr"
    }
  ],
  "slots": {
    "rows": {
      "allowed": [
        "expression"
      ],
      "min": 1,
      "max": 1
    },
    "cols": {
      "allowed": [
        "expression"
      ],
      "min": 1,
      "max": 1
    },
    "values": {
      "allowed": [
        "expression"
      ],
      "min": 0
    }
  },
  "role": "statement",
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "_children_why": "`values` 是初始值列（`int a[2][3] = {{1,2,3},{4,5,6}}`），每一項是一層 `{…}`。\n⚠️ 它原本不存在，因為**帶初始值的多維陣列根本走不到這顆元件**——判別只掛在「沒有初始值」那條路上。\n🔴 **維度是【接點】不是屬性**（2026-09-19）。在此之前 `rows`／`cols` 是兩個屬性，裝著原始碼的片段，而執行期 `Number(properties.rows)` 讀它\n——於是 `int d2[n][5]` 與 `int d2[x*2][7]` 算出 `NaN`，**零列**。\n⚠️ 而症狀出現在**下一行**：`cin >> d2[i][j]` 說「讀進來的東西不知道要放哪裡」。\n> **一個錯誤訊息指著最後一個碰到它的人，而不是造成它的人。**\n🔴 **而它是一個不對稱**：同族那顆一維陣列的 `size` **早就是接點**，只有這一顆還是屬性。\n⚠️ 第七十二條護欄（字串屬性不得裝結構）**看不到它**——測試語料裡的維度永遠是數字字面值。\n> **語料乾淨不代表模型對：它代表語料是照著模型長的。**\n🔴 屬性整個退場，不留欄位；舊存檔由 `SHAPE_CHANGES_V22` 丟快取、從程式碼重 lift。",
  "traits": {
    "declarator": true,
    "forLoopPart": true
  },
  "_traits_why": "`declarator: true`＝**我可以當一次宣告裡的一個宣告子**（`int a, b;` 的 `a` 與 `b`）。2026-09-14 補：多宣告子那一格原本只收單一身分，而實測 `string s, t;` 的宣告子是**字串宣告**那一顆——**宣告子的身分跟著型別走**，所以那一格要的是一個【族】不是一個身分。\n🔴 **`forLoopPart` ＝「我可以當 for 的初始化子」**（2026-09-18，這個病的**第三次**）。\n同族那顆一般的變數宣告早就宣告了它，`auto` 那顆 2026-09-17 才補，而這一顆沒有——於是 `for (int* p = begin(a); p != end(a); p++)` 整個掉進 raw code，而 `for (int i = 0; …)` 好好的。\n⚠️ 症狀指著**那一整段**，沒有指著少掉的那個宣告——學生會以為是 `begin()` 不支援。\n> **同一族的 27 顆元件，兩顆宣告了某個性質而另外 25 顆沒有——那個差別不會有人發現，直到有人寫出只有前者能表達的程式。**\n🟢 這一次**整族一起宣告**，並且加一條護欄（宣告式的元件必須答得出這一題），因為它已經一顆一顆修過兩次了。\n⚠️ 判準是 **C++ 的文法事實**（一個宣告放得進 for 的第一格），不是「這樣寫好不好看」。"
}
