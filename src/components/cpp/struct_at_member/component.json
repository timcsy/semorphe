{
  "conceptId": "cpp:struct_at_member",
  "layer": "lang-core",
  "abstractConcept": null,
  "properties": [
    {
      "name": "obj",
      "kind": "identifier",
      "default": "obj"
    },
    {
      "name": "member",
      "kind": "literal",
      "default": "field"
    }
  ],
  "role": "expression",
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "_children_why": "🔴 **移除了 `obj` 接點宣告**（2026-08-14，spec `123`）——它與現實不符。`generate.ts:6` 用的是 `node.properties.obj`，**它是【參數】不是接點**。而符合性量測照宣告合成一棵樹，於是造出 `5.field`（真實世界不存在）再回報它走不通。⚠️ 這【不是】用宣告刷數字——差別在有出處：產生端明擺著讀的是 properties。"
}
