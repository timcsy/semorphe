{
  "componentId": "cpp:typedef",
  "abstractComponent": null,
  "properties": [
    {
      "name": "orig_type",
      "kind": "type_expr",
      "default": "int"
    },
    {
      "name": "alias",
      "kind": "literal",
      "default": "myint"
    }
  ],
  "slots": {},
  "role": "statement",
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift-pattern.json",
    "liftStrategy": "./lift-strategy.ts"
  },
  "_execute_why": "🔴 **它一度宣告 `skipPaths: [\"execute\"]`，理由是「宣告性」**——而 2026-09-18 之後它**真的有行為**：把型別別名登記進執行期的別名表，讓 `typedef map<int,set<int>> Graph; Graph g;` 的 `g` 建得出一個對照表。\n⚠️ 那個宣告因此撤掉了。**顯式的空與遺漏的空要分得出來，而「已經不空了」要立刻改口。**"
}
