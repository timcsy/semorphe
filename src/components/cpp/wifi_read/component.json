{
  "conceptId": "cpp:wifi_read",
  "layer": "lang-library",
  "abstractConcept": null,
  "properties": [
    {
      "name": "obj",
      "kind": "identifier",
      "default": "WiFi"
    },
    {
      "name": "quantity",
      "kind": "enum",
      "default": "status"
    }
  ],
  "children": {},
  "role": "expression",
  "owner": "(arduino)",
  "_owner_why": "**它屬於 Arduino 的套件生態**——`#include` 那一行就是它的申請書。",
  "_lift_why": "🔴 **「連線狀態」與「本機位址」是同一顆概念的兩個參數**——兩者都是「讀無線網路的某個屬性」，零引數、形狀相同。與溫濕度那顆同一條：**讀什麼不是身分，是參數。**\n\n⚠️ 而綁 `obj === 'WiFi'`：`status` 是一個非常普通的方法名，一張只放得下方法名的表會讓任何物件的 `.status()` 都變成這一顆。",
  "requires": [
    "<WiFi.h>"
  ],
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "traits": {
    "needsCapability": "wifi",
    "_needsCapability_why": "🔴 **AVR 的核心裡沒有 WiFi 函式庫**——Uno／Nano 上這顆積木拿得到也跑不了（spec 150 查證：`arduino/ArduinoCore-avr` 的 `libraries/` 只有 EEPROM／HID／SPI／SoftwareSerial／Wire）。\\n\\n⚠️ 而**標頭在有 WiFi 的板子之間還會不一樣**：ESP32 是 `<WiFi.h>`，ESP8266 是 `<ESP8266WiFi.h>`——那一半由目標的 `headerAliases` 處理，不在這裡。"
  }
}
