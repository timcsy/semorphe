{
  "componentId": "cpp:array_2d_at",
  "abstractComponent": "cpp:array_at",
  "properties": [],
  "children": {
    "obj": "expression",
    "row": "expression",
    "col": "expression"
  },
  "role": "expression",
  "traits": {
    "addressable": true
  },
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "_children_why": "🟢 **容器是一個接點**（2026-08-26）——在此之前是一個字串屬性，而 `obj.arr[i]` 的容器是一個成員存取：它被 `.text` 抄進去之後，執行期 `ctx.scope.get(\"obj.arr\")` **查不到**（連讀都是壞的，不只寫）。\n🟢 **另一個語言的同族元件早就是這個形狀**（兩個接點）——C++ 這一顆才是例外。（刻意不寫它的身分：寫下來就是一次跨資料夾耦合，就近性護欄會擋——**這是我第三次踩它**。）",
  "_addressable_why": "🟢 **`&` 取得到位址的那些**（2026-08-26）——`scanf(\"%d\", &a[i])` 的產生器要問這個。\n⚠️ 它與「宣告了左值解法的那些」**是同一組**（`declareLvalue`）：\n**一個取得到位址的東西，就是一個寫得回去的位置。**\n🔴 而在此之前產生器問的是「它是不是 `variableRef`」，於是 `&a[i]` 的 `&` 掉了\n——`scanf(\"%d\", &a[i])` 來回轉換變成 `scanf(\"%d\", a[i])`，**編不過**。"
}
