{
  "componentId": "cpp:container_append",
  "abstractComponent": null,
  "properties": [],
  "slots": {
    "obj": "expression",
    "value": "expression"
  },
  "role": "statement",
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "_slots_why": "🔴 **接收者是一個【接點】，不是一串文字**（2026-09-18）。\n在此之前它是 `properties.obj`，而解開那串文字的地方只認得「名字、數字、以及它們的加減」——於是 `m[name]`（字串鍵）、`buckets[keys[i % 2]]`（巢狀下標）、`m[1]`（巢狀容器）在執行期就斷了。\n⚠️ **Python 那一側早就是接點**——同一個概念、同一個 `OBJ` 引數名，兩種做法。"
}
