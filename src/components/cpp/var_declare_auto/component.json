{
  "componentId": "cpp:var_declare_auto",
  "abstractComponent": "cpp:var_declare",
  "properties": [
    {
      "name": "name",
      "kind": "literal",
      "default": "x",
      "_kind_why": "⚠️ `name` 在這顆上**不保證是單一識別字**：`auto [u,v,w] = f();` 的結構化繫結把一串名字放在這裡（實測學生語料 5 筆）。改成 `literal`。"
    }
  ],
  "slots": {
    "initializer": "expression"
  },
  "role": "statement",
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts",
    "_lift_why": "⚠️ `lift.ts` 匯出的是**建構子**（`建自動宣告`）——「型別修飾詞是 auto」的判別是 C++ 語法的知識，留在共用檔。"
  }
}
