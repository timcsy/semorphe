{
  "componentId": "cpp:string_find",
  "properties": [
    {
      "name": "obj",
      "kind": "identifier",
      "default": "str"
    }
  ],
  "children": {
    "arg": {
      "allowed": [
        "expression"
      ],
      "min": 1,
      "max": 1
    },
    "from": "expression"
  },
  "role": "expression",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "requires": [
    "<string>"
  ],
  "_children_why": "⚠️ `from` 是 `str.find(x, pos)` 的第二個引數（起始位置），由 lift 產出——`registerMethodConcept('find', …, [\"arg\", \"from\"])`。\n\n🔴 而**產生器沒有讀它**：`str.find(x, 5)` 會被產成 `str.find(x)`——**起始位置安靜地不見**。\n\n2026-08-18 我一度把它當成死接點刪掉（只查了產生器），而宣告完整性護欄糾正了。"
}
