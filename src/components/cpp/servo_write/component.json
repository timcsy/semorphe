{
  "conceptId": "cpp:servo_write",
  "abstractConcept": null,
  "properties": [
    {
      "name": "obj",
      "kind": "identifier",
      "default": "myServo"
    }
  ],
  "children": {
    "angle": "expression"
  },
  "role": "statement",
  "owner": "(arduino)",
  "_owner_why": "**它屬於 Arduino 的套件生態**——`#include` 那一行就是它的申請書。",
  "_lift_why": "🔴 **走分支而不是「型別 → 方法」那張純資料表**，理由是【引數會被丟掉】：那張表的消費者用一組共用的 `METHODS_WITH_ARG` 決定要不要接引數，而這顆的引數不在那組裡——⚠️ 症狀是**引數安靜地不見**，而程式碼照樣產得出來。\n\n🟢 分支拿得到 `ctx`，所以它自己查接收者的宣告型別。**型別是宣告出來的，不是猜的。**\n\n⚠️ **型別查不到時不認**——留在通用的方法呼叫。猜一個錯的專屬身分比誠實降級更糟。",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  }
}
