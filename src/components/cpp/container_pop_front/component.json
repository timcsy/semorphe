{
  "componentId": "cpp:container_pop_front",
  "abstractComponent": null,
  "_componentId_why": "⚠️ **刻意叫 `container_` 而不是 `deque_`**：`pop_front` 對 `deque` 與 `list` 都成立，而那張「方法名 → 元件」的登錄表今天的教訓正是**名字寫了某一種容器、行為卻涵蓋全部**——負責 `back` 的那顆名字裡有 vector、負責 `insert` 的那顆名字裡有 set，而它們都是所有容器的主人。見 `specs/component-discovery/cpp-container-ends.md`。",
  "properties": [],
  "slots": {
    "obj": "expression"
  },
  "role": "statement",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "requires": [
    "<deque>"
  ],
  "_requires_why": "`pop_front` 最常見的宿主是 `deque`（語料裡 22 支宣告 deque、18 支用 pop_front）。`list` 也有它，而補丁器只需要一個能讓程式編得過的標頭。",
  "_slots_why": "🔴 **接收者是一個【接點】，不是一串文字**（2026-09-18）。\n解開那串文字的地方（`interpreter/receiver.ts`）只認得「名字、數字、以及它們的加減」——於是 `m[name]`（字串鍵）、`buckets[keys[i%2]]`（巢狀下標）、`m[1]`（巢狀容器）在執行期就斷了。十支資訊隔離的盲測有 7 支死在上面。\n⚠️ Python 那一側**早就是接點**——同一顆元件、同一個引數名，兩種做法。"
}
