{
  "componentId": "cpp:container_iter",
  "abstractComponent": null,
  "properties": [
    {
      "name": "which",
      "kind": "enum",
      "default": "begin"
    }
  ],
  "slots": {
    "obj": "expression"
  },
  "role": "expression",
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "_owner_why": "⚠️ **沒有 `requires`**——`begin()`／`end()` 是容器自己的成員，不需要任何標頭。\n工具箱歸在核心的容器那一格，與其他容器方法同段。",
  "_default_why": "`which` 的預設是 `begin`——`end()` 幾乎只在成對出現時使用，而單獨拖一顆積木時「開頭」是唯一合理的起手。",
  "_slots_why": "🔴 **接收者是一個【接點】，不是一串文字**（2026-09-18）。\n解開那串文字的地方（`interpreter/receiver.ts`）只認得「名字、數字、以及它們的加減」——於是 `m[name]`（字串鍵）、`buckets[keys[i%2]]`（巢狀下標）、`m[1]`（巢狀容器）在執行期就斷了。十支資訊隔離的盲測有 7 支死在上面。\n⚠️ Python 那一側**早就是接點**——同一顆元件、同一個引數名，兩種做法。"
}
