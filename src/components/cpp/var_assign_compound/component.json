{
  "componentId": "cpp:var_assign_compound",
  "abstractComponent": "cpp:var_assign",
  "properties": [
    {
      "name": "operator",
      "kind": "enum",
      "default": "+="
    }
  ],
  "children": {
    "target": "expression",
    "value": "expression"
  },
  "role": "statement",
  "owner": "(core)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "traits": {
    "forLoopPart": true,
    "parenInCout": true,
    "writesTo": "target"
  },
  "_traits_why": "**優先級、能不能放進 for 的三格、放進 cout 要不要括號——都是我自己的性質**，不是共用表該列的身分。",
  "_writesTo_why": "🟢 **已還**——左值是 `target` 接點。判準見 `knowledge/concepts/左值.md`。",
  "_children_why": "🟢 **左邊是一個接點**（2026-08-25，路線圖「左值是接點，不是字串」）。\n🪦 在此之前是 `name: literal` ＋ 一個可有可無的 `index` 接點——**那是左值形狀的列舉，而它列了兩種**。`o.x`／`p->x`／`*q`／`a[i][j]`／`a.b.c` 全部被 `left.text` 壓進 `name`，執行期於是去查一個叫 `p->x` 的變數。\n⚠️ `index` 那一格是 2026-08-25 早上補的 `altLayout` 在服務的東西——**那是這個病的症狀**，而它隨這一刀一起退場。"
}
