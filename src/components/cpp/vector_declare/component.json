{
  "componentId": "cpp:vector_declare",
  "abstractComponent": "cpp:var_declare",
  "properties": [
    {
      "name": "type",
      "kind": "enum",
      "default": "int"
    },
    {
      "name": "name",
      "kind": "identifier",
      "default": "vec"
    }
  ],
  "slots": {
    "values": {
      "allowed": [
        "expression"
      ],
      "min": 0
    },
    "source": {
      "allowed": [
        "expression"
      ],
      "min": 0,
      "max": 1
    },
    "size": {
      "allowed": [
        "expression"
      ],
      "min": 0,
      "max": 1
    },
    "fill": {
      "allowed": [
        "expression"
      ],
      "min": 0,
      "max": 1
    }
  },
  "role": "statement",
  "requires": [
    "<vector>"
  ],
  "paths": {
    "lift": "./lift.ts",
    "generate": "./generate.ts",
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "execute": "./execute.ts"
  },
  "_children_why": "`fill` 是 `vector<int> v(5, 7)` 的第二個建構引數（「5 個 7」）。\n⚠️ 它原本不存在，而 lift 的註解寫著「兩個引數的形式必須被丟到 raw_code」——**實際上它 fall through 成一個空向量**。註解與行為不符，而空向量不會出聲。",
  "traits": {
    "declarator": true
  },
  "_traits_why": "`declarator: true`＝**我可以當一次宣告裡的一個宣告子**（`int a, b;` 的 `a` 與 `b`）。2026-09-14 補：多宣告子那一格原本只收單一身分，而實測 `string s, t;` 的宣告子是**字串宣告**那一顆——**宣告子的身分跟著型別走**，所以那一格要的是一個【族】不是一個身分。"
}
