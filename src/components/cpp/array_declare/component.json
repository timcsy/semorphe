{
  "conceptId": "cpp:array_declare",
  "abstractConcept": null,
  "properties": [
    {
      "name": "type",
      "kind": "enum",
      "default": "int"
    },
    {
      "name": "name",
      "kind": "identifier",
      "default": "arr"
    }
  ],
  "children": {
    "size": "expression",
    "values": "expressions"
  },
  "role": "statement",
  "annotations": {
    "debug_step": true
  },
  "owner": "(universal)",
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts",
    "_lift_why": "⚠️ `lift.ts` 匯出的是**建構子**（`建陣列宣告`）而不是判別——共用檔有四個位置會造這顆元件（一般／指標陣列兩種寫法／帶初始值），節點形狀四處相同。**同一顆元件在同一個檔裡被建立四次，該收的不是分支，是建構子。**"
  },
  "_owner_why": "⚠️ **這顆是 universal 層，章要蓋 `(universal)` 不是 `(core)`。**\n批次工具原本依「概念宣告在哪個檔」蓋章，而 `universal-concepts.json` 的路徑\n含 `/blocks/`，於是被誤判成核心。症狀不是報錯，是**工具箱的 operators 段落\n變成零筆**——那個段落寫著 `{ from: '(universal)' }`，查無此章就整批回傳空。\n（可拿性護欄的「來源段落對不到積木」抓到的正是這個。）"
}
