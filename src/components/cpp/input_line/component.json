{
  "componentId": "cpp:input_line",
  "properties": [],
  "slots": {
    "target": "expression"
  },
  "role": "statement",
  "positions": [
    "statement",
    "expression"
  ],
  "_positions_why": "getline(cin, s) —— 回傳 istream&，同上。⚠️ 而它的 `role` 是 `statement`，那是**產生器契約**（「我自己印分號」），不是文法——兩條軸在這顆身上分岔。見 `core/component/traits.ts` 的 `positionsOf`。",
  "traits": {
    "writesTo": "target"
  },
  "paths": {
    "render": "./forms/blocks.json",
    "extract": "./forms/blocks.json",
    "generate": "./generate.ts",
    "execute": "./execute.ts",
    "lift": "./lift.ts"
  },
  "requires": [
    "<string>"
  ],
  "_writesTo_why": "🟢 **已還**——左值是 `target` 接點。判準見 `knowledge/concepts/左值.md`。",
  "_children_why": "🟢 **讀進去的那一格是一個接點**（2026-08-25，路線圖「左值是接點，不是字串」）。\n`getline(cin, o.name)`／`getline(cin, v[i])` 在 C++ 都合法——在此之前它們被 `.text` 抄成字串，執行期於是 `ctx.scope.declare(\"o.name\", …)`：**在作用域裡長出一個叫 `o.name` 的變數**，而原本那個欄位一個字都沒動。"
}
