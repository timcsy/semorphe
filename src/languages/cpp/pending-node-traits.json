{
  "_why": "**還沒膠囊化的元件的節點性狀。只減不增。**\n一顆搬進膠囊時，這裡刪一列、`component.json` 的 `traits` 加一筆。\n\n⚠️ **2026-08-11：F 完成，這張表空了。**\n最後被刪掉的三筆是 `boolean`／`number`／`string`——**不帶 `cpp:` 前綴的舊通用層身分**。\n它們是從 `FOR_LOOP_CONCEPTS` 那個集合照抄過來的（那個集合同時列著兩套詞彙），\n而實查**沒有任何地方產出它們**：`grep` 全 src 找不到一個 `createNode('number')`，\n`id-migrations` 也沒有把它們映射到誰。\n\n> **一個集合裡的成員不必是活的——照抄一份集合，等於把它的幽靈也抄過來。**\n\n檔案留著是因為**過渡表這個形狀還會再用**（靜態 import 的 JSON、glob 直讀、\n沒有登錄呼叫）——下一次是 Python 或硬體域。",
  "traits": {}
}