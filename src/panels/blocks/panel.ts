/**
 * **積木**——理解的「空間」那一層，而它是這個產品的主體。
 */
import type { PanelSpec } from '../../core/host/panel-spec'

const spec: PanelSpec = {
  id: 'blocks',
  layer: 'space',
  nameKey: 'LAYER_SPACE',
  mountedByShell: true,
}
export default spec
