# 驗收指引：讓「刻意不執行」說得出話

```bash
npx vitest run tests/integration/noop-classification.test.ts \
              tests/integration/audit-completeness.test.ts \
              tests/integration/audit-neutrality.test.ts
npm test   # 全套，確認六項量測
```

---

## 情境 0（先跑）：把 34 個全部跑一遍，不要先分類

**這是第一個任務，不是最後一個。**

research F2b 已經現形過一次：靜態掃描給出的分類是**反的**（把壞掉的 `static_cast` 判成好的，把好的 `cpp_case` 判成壞的）。抓到它的是先做的實測與它矛盾。

所以：**先跑，再分類**。每個概念一支最小程式、一個人工寫定的期望輸出。

驗收：一份 34 列的表，每列有程式碼、期望、實得、判定。

> **若 34 個全部通過**——不是好消息，是判準太鬆的證據。回頭看那些最小程式是不是太簡單。

---

## 情境 1：四個轉型恢復正確

| 程式 | 修好前 | 修好後 |
|---|---|---|
| `static_cast<int>(3.9)` | **0** | 3 |
| `const_cast` / `dynamic_cast` / `reinterpret_cast` | 同樣被關掉 | 走各自的實作 |

**修法是刪掉清單裡那四行**，不是新寫實作——實作一直都在（`functions.ts:130` 一帶）。

> research F8 的推理（`Map.set` 覆蓋 + 清單在後）要先**跑出來**再改。跑不出來代表還有第三處在覆蓋。

## 情境 2：分類的結果進得了、也擋得住

- 判為「真的不執行」或「由父概念消費」→ 拿到 `skipPaths` **與理由**
- 判為「還沒實作」→ **拿不到**宣告，仍被算成殼，且進缺陷帳
- 寫了 `skipPaths` 卻沒寫理由 → **測試失敗**

## 情境 3：清單消失，行為不變

- 核心層搜不到那 34 個概念名
- 每一個被宣告的概念，執行行為與修改前**逐一比對相同**
- 沒有宣告也沒有執行器的概念 → 仍回報未知概念（現況）

## 情境 4：除錯停的位置不變

把 12 個概念名的清單換成標註後，除錯逐步執行**停在完全相同的位置**。

## 情境 5：重複註冊變得看得見

- 量出目前執行器註冊表有幾處重複註冊
- 進基線與棘輪
- **本功能不在註冊時報錯**——只讓它可見（`history/017`：加嚴之前先回答「被拒絕的東西去哪了」）

## 情境 6：報表分得出兩種下降

完備性報表要能回答：「這次殼少了 N 個，其中幾個是因為實作、幾個是因為宣告？」

**目前完全分不出。** 分不出的話，下一個人會用宣告刷數字。

---

## 情境 7：既有行為零回歸

- `npm test` 全綠
- 中立性與完備性**下降**；其餘四項量測**不上升**
- 使用者現在跑得起來的程式仍然跑得起來——`static_cast` 那四個是**變好**，不是變嚴
