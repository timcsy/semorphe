# Feature Specification: 兩個 bug 疊起來的無限迴圈

**Feature Branch**: `092-string-find-scan` ｜ **Created**: 2026-08-06

## 為什麼做這個

三支阻斷者不同的 todo（`var_assign` 的掃描迴圈、`print` 的括號布林運算式、
逗號運算子）。實測：

| | 結果 |
|---|---|
| 括號布林運算式 | **早就正確**（091 修好布林輸出之後） |
| 逗號運算子 | **早就正確** |
| 掃描迴圈 | **真的壞**——兩個 bug 疊在一起 |

## 兩個 bug

| 缺陷 | 後果 |
|---|---|
| 搜尋的**起點參數被丟掉** | `s.find("X", pos)` 永遠從頭找 → **無限迴圈** |
| 找不到時回 `4294967295` | 使用者寫 `!= -1` 的話，那個比較**永遠成立** |

兩個湊在一起的症狀是**爆步數上限**——離現場很遠，而且看起來像「程式太複雜」，
不像一個具體的缺陷。

## 為什麼回 -1 而不是 npos

C++ 的 `string::npos` 是 `size_t(-1)`，而**使用者常寫 `!= -1` 來比**。
這個直譯器沒有把 npos 表示成一個常數，所以回 -1 讓兩種寫法都對。

## Requirements

- **FR-001**: 搜尋 MUST 用起點參數
- **FR-002**: 沒給起點時 MUST 從頭找——**只驗有起點的話，「一律用 0」也會過**
- **FR-003**: 找不到 MUST 回 -1
- **FR-004**: 期望值 MUST 由**真的編譯器**決定

## Success Criteria

- **SC-001**: 只有名字的測試 **21 → 15**
- **SC-002**: 掃描迴圈的執行結果與 g++ 一致
- **SC-003**: 既有測試全數通過

## 順帶：第四個空的 describe

移掉 todo 之後 `known lifter limitations (not cstdlib-specific)` 變空。
**一個空的 describe 是殼**——填成真的回歸測試（陣列初始化列表 + 函式呼叫鏈，
期望值 137 由 g++ 決定）。
