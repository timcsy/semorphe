# 動工前基線（T001）

> **這不是儀式**：驗收要求「護欄不得上升」，而那需要一個比較基準。
> 全套跑一次就會有人手癢去改基線——所以先把數字釘在這裡。
>
> 量測時間：2026-08-07，commit `d22659b`（tasks.md 提交後、任何實作之前）

## 全套

```
Test Files  212 passed | 1 skipped (213)
```

## 護欄

| 護欄 | 基線 |
|---|---|
| 中立性 | 違規檔案 **0** 個｜違規項目 **0** 筆 |
| 語法耦合 | 確定 **0** 處｜無法確定 **60** 處 |
| 執行器重複註冊 | 重複概念 **0** 個｜多出的註冊 **0** 次 |
| 就近性 | 平均擴散 **7.9** 檔（181 元件） |
| 元件身分健檢 | 181 顆｜可疑 **47** 顆（確定 9 ／ 要看 51） |
| 雙重真相 | 見 `tests/baselines/dual-truth.json` |

## 驗收時要比對的

- **中立性 MUST 維持 0** —— 契約 C-2（選擇規則不得以元件身分分支）**唯一的機械檢查就是它**
- **執行器重複註冊 MUST 維持 0** —— SC-004（多形態不得複製行為）
- 其餘 MUST NOT 上升

## 預期會動的

| | 預期 | 為什麼不算退步 |
|---|---|---|
| 就近性 | 容器那兩顆的擴散度可能 +1（多了形態宣告） | 形態宣告住在該元件的 `blocks.json` 裡，是**它自己的檔** |
| 元件身分健檢 | 不變 | 本功能**不執行整併**，只提供機制 |
| 全套檔數 | +2（新增兩支測試檔） | 測試只准增加 |
