# 願景

## 問題陳述

程式教育有一個根本鴻溝：**理解程式邏輯**和**掌握語法細節**之間的斷層。

傳統工具走兩個極端——Scratch/Blockly 讓學生拼積木但永遠碰不到真正的程式碼；文字教學讓初學者同時面對邏輯和語法的雙重認知負荷。兩者之間缺少一座橋。

**目標使用者：**

- **學生**（主要）：從零基礎到進階，涵蓋 APCS 考試準備、競程訓練、學校課程
- **教師**：設計教學路徑（Topic）、指定練習、追蹤學習軌跡
- **語言開發者**：透過模組化語言套件擴充新程式語言
- **課程設計者**：透過 Topic 系統定制不同教學情境的概念可見性和積木形態

## 核心想法

**程式碼和積木都不是本質——它們是同一個語義結構的不同投影。** 學生可以用積木理解邏輯，同時看到對應的真實程式碼，在兩者之間自由切換，最終過渡到純文字編程。

關鍵不是在程式碼上面疊一層積木——**只有語義層存在**，程式碼和積木都是參數化的投影。理論基礎見 [原則](principles.md)。

**和現有工具的根本差異：**

| 工具 | 做法 | Semorphe 的不同 |
|------|------|----------------|
| Blockly/Scratch | 積木 → 程式碼（單向，不可逆） | 程式碼 ↔ 積木（雙向，完美 round-trip） |
| Hedy | 每個等級有不同語法 | 同一語義樹，不同投影解析度 |
| 傳統 IDE | 以文字為中心，視覺化為附加 | 以語義為中心，文字和視覺都是投影 |
| Unison | AST hash 內容定址 | hash 語法層 vs hash 語義層 |
| LSP/LSIF | 跨檔案符號索引 | 位置導向 vs 語義導向 |

## 專案身份

| | |
|---|---|
| **名稱** | Semorphe（散模費 σημορφή）— σῆμα（語義）+ μορφή（形態） |
| **標語** | 唯一真實，各式投影 |
| **CLI** | `smorph` |
| **Logo** | `<Σ>` |
| **npm** | `semorphe` |
| **VSCode** | `semorphe-vscode` |
| **技術棧** | TypeScript 5.x, Blockly 12.4.1, web-tree-sitter 0.26.6, Monaco Editor, Vite |

## 現狀

C++ 單語言可用，程式碼 ↔ 積木雙向 round-trip 成立，語義直譯器可執行。三層解耦（Core / Bus / View）已落地。Topic 系統已完成。

> **語言無關性仍然是破的，而破的位置換了**（2026-08-12 重寫）。
>
> | P9 的四條 | 2026-08-06 的說法 | 今天 |
> |---|---|---|
> | 核心層硬編語言專屬身分 | **主要症狀** | ✅ **歸零**（中立性護欄 `total: 0`） |
> | 碎裂（一個元件散在太多檔） | 平均擴散 8.5 檔 | ✅ **歸零**（177/177 膠囊化） |
> | 殼（路徑存在但空的） | 🈳92 ／ ❌5 | ✅ **歸零** |
> | **視圖層 import 語言套件** | 沒有人在看 | ❌ **現在在這裡**（5 筆） |
> | **執行器直接持有面板** | 沒有人在看 | ❌ **現在在這裡**（73 處） |
>
> **前三條是階段 6.5 清掉的；後兩條不是新長出來的，是一直都在而沒有人量。**
> 它們被看見的原因是[第三十九條護欄](../tests/integration/audit-four-independences.test.ts)
> ——P9 自稱「每個 Phase 完成後必須通過」，而在那之前 **31 條護欄裡零檢查**。
>
> > **一個病從「主要症狀」變成「已歸零」時，要問的不是「好了嗎」，
> > 是「它搬到哪裡去了」。**
>
> **當下的實測值一律以 `tests/baselines/*.json` 為準，不寫在這裡**——寫死的數字每交付一次就過期一次（這已經是第三次了）。這裡只說**問題的形狀**，數字歸護欄。
> 這句話從「尚未驗證」改成「已知是破的」的轉折，以及階段 7 角色隨之改變的理由，見 [history/015](history/015-語言無關性從尚未驗證到已知是破的.md)。
> 而「破的位置換了」這一次改寫的依據，見 [history/051](history/051-P9從沒有執行機構到病換了位置.md)。

**已完成元件：**

- **Core**：SemanticNode 型別、ConceptRegistry、BlockSpecRegistry、PatternLifter、PatternRenderer/Extractor、CodeGenerator、TemplateGenerator、BlockRenderer、Interpreter、Topic 系統、Storage、DependencyResolver（語言無關介面）、ProgramScaffold、CodeMapping + BlockMapping（nodeId-based 跨投影查詢）
- **C++ 語言套件**：core concepts/blocks/generators/lifters、std modules（iostream/cstdio/vector/algorithm/string/map/stack/queue/set/cstring/cmath）、ModuleRegistry、auto-include、lift-patterns、render strategies、style presets（apcs/competitive/google）、style-exceptions、manifest
- **UI**：Blockly/Monaco/Console/Variable 面板、Debug 工具列、SyncController、ToolboxBuilder、BlockRegistrar、AppShell、ExecutionController、Ghost Line

**已知差距：**

| 缺失項目 | 優先級 | 說明 |
|----------|--------|------|
| SemanticDiff 增量更新 | P2 | 全量替換 → 增量（Phase 9.2，nodeId 穩定性已部分完成） |
| 硬體描述層 | P3 | 等 Arduino 需求（Phase 8） |
| DataFlow 視圖 | P3 | 需 annotations 機制（Phase 9.1） |

## 架構

```
宿主環境（瀏覽器 / VSCode / Electron）
  ↕ ViewHost 介面
多個獨立視圖（積木 / 程式碼 / 主控台 / 變數 / 資料流 / 接線 / 模擬）
  ↕ SemanticBus（瀏覽器=EventEmitter / VSCode=postMessage）
SemanticCore（語義樹 + 概念註冊 + 投影引擎 + 直譯器，零 DOM 依賴）
  ↕ 語言套件介面
多個語言套件（C++ / Python / Arduino）+ 外部擴充套件
```

### 三層契約

- **Layer 1（語義宣告）**：concepts.json + lift-patterns + lifters — 所有消費者可用
- **Layer 2（投影提示）**：blockDef / renderMapping / codeTemplate / generator — 僅對應視圖消費
- **Layer 3（視圖策略）**：視圖根據 annotations 決定呈現 — 新增唯讀視圖不需改語言套件

### 語言套件目錄結構

```
languages/{lang}/
  ├─ manifest.json          ← 套件中繼資料
  ├─ core/                  ← 語言核心概念（concepts.json / blocks.json / generators/ / lifters/）
  ├─ std/                   ← 標準函式庫（目錄名即 header 名）
  ├─ lift-patterns.json     ← AST → 語義 pattern 規則
  └─ auto-include.ts / style-exceptions.ts / renderers/ / styles/
```

### 尚未實作但已有設計的規格

- **語義標註（Annotations）**：語言套件與視圖套件之間的開放契約（`control_flow`、`body_execution`、`introduces_scope`、`side_effects`、`hardware_binding` 等）。缺少 annotation 時用 generic fallback，不報錯——任何語言 × 視圖組合都能工作。
- **視圖套件規格**：按與語義樹的互動方式分類（可編輯 / 唯讀-執行 / 唯讀-分析 / 唯讀-硬體），結構為 `views/{name}/manifest.json` + `src/view.ts`⟨規劃⟩（實作 ViewHost）。
- **WebView 隔離模型**：瀏覽器每視圖 = `<div>`/`<iframe>` + EventEmitter；VSCode 中 blocks = WebviewPanel、code = 原生 TextEditor、console = Terminal + postMessage。**Core 跑在 Extension Host，不跑在 WebView。**
  > 🔴 **而這一條有兩個 2026-08-16 才被量出來的前提**（[history/069](history/069-vscode原型退休而它的兩個教訓被撈出來.md)）：
  > ① 膠囊登錄表用 `import.meta.glob`（**Vite 的轉換**），所以**那個宿主也必須用 Vite 建置**（已實測可行）；
  > ② `src/ui/app.ts` 有**六個單例**寫死了「只有一個文件」，而編輯器有 N 個
  > ——**那是這條路真正的成本，而它不在任何介面上**。
  > ⚠️ 而 `code = 原生 TextEditor` 有一個順帶的好處：**宿主的診斷本來就在**
  > （見 [draft/語義診斷系統](draft/2026-08-05-語義診斷系統.md) 的委派宿主分工）。
- **外部套件與硬體擴充**：`packages/{name}/` 含 semantics / projections / hardware。依賴規則：擴充可引用基礎套件概念，**不可引用其他擴充**（避免菱形依賴）。

### 長期方向

1. **多語言**：C++ → Python → Java → Go → Rust……同一語義結構，切換語言即切換投影
2. **語義套件市場**：語義契約 + 投影定義 + 多後端執行體。同一概念可有多個執行體（wasm / js / webgpu / remote），自動基準測試 + 自動後端選擇。教學價值：讓學習者直觀感受演算法複雜度
3. **可插拔執行與時空旅行除錯**：執行即投影；直譯器每步產生語義狀態快照，回溯 = 用歷史快照重新投影（viewParams 多一個 `timeStep`）
4. **硬體教育**：Arduino 接線視圖 + 模擬視圖，從軟體延伸到嵌入式
5. **AI 輔助**：LLM 作為語用分析師，在確定性 guardrails 內提供提示投影——建議下一步，但不自動修改語義結構
6. **自舉**：教學路徑本身成為可分享的語義套件；學習數據回饋優化 Topic 層級樹，目標是**結構還原度最大化**而非效能最大化

### 🟡 符合性的清償（宣告了的接點在積木上表達不出來）

- [ ] **進行中**（2026-08-14）——**12 → 11**（`122` 實作了一筆），
  而剩下 11 筆的逐筆理由寫在 `tests/baselines/conformance.json` 的 `_meta.note`

> 🔴 **而 `123` 想再清兩筆「假違規」，判定錯了並已回退**
> （[history/065](history/065-兩條護欄從相反方向量同一件事.md)）：
> `audit-declared-slots` 證明 lift **真的會產生**那兩個接點。
>
> ⚠️ **所以再判「假違規」之前，兩條護欄都要跑**——它們從相反方向量同一件事，
> 而那對關係就是「用宣告刷數字」的機械偵測器。
> **在此之前，11 筆一律當成真的。**

> ⚠️ 使用者撞過這個病一次（`int a[3] = {1,2,3}` 的初始值消失）。
> **這些是同一個機制的其他出口。**
>
> **子機制有三種，修法完全不同**：
> ① JSON 就是唯一真相（便宜，已做過一次 → 有模板）
> ② 有動態插槽而沒宣告對映
> ③ 🔴 積木在 `block-registrar` 命令式產生（**雙重真相**，兩邊都要改）
>
> 🔴 **而根因是同一個**：宣告與形態**分開寫**，而**沒有任何東西強制它們對齊**
> ——`audit-conformance` **偵測而不預防**。
> ⚠️ **預防機制是一個獨立的設計決定**（它會影響每一顆元件的加法），
> 而它今天**還沒有家**——不在任何階段裡。

### 離線可用

- [x] **已完成**（2026-08-16，spec `128`）——執行期外部請求 **4 → 0**，
  第四十五條護欄（`e2e/offline.spec.ts`）守著。
  🔴 而它是委派探針**順手掀出來的**，不是計畫裡的
  ——轉變見 [history/070](history/070-離線可用一直是半真的而它壞得不夠大聲.md)。

## 開發約束（跨階段，不隨階段退場）

> ⚠️ 這些**不是路線圖項目**——沒有完成的一天，所以它們住在這裡而不是某個階段裡。
> （2026-08-14 從階段 6.5 提出來：那條「不准紅著過夜」原本埋在一個**已完成的階段**裡，
> 而 [experience](experience.md)`:1293` 引用它時，它的定義正要隨那個階段被收斂掉。
> **一條被別處引用的規則，不能住在一個會退場的地方。**）

- **既有測試全程綠，一次都不准紅著過夜。** 測試數只准增加。
  ⚠️ **當下的值以 `npm test` 為準，不寫在這裡**——寫死的數字每交付一次就過期一次。
  而它與「先讓測試紅」不衝突：那條講的是不要**放著**，不是不要紅
  （見 `experience` 的「一個十秒能修的紅」）。
- **護欄的基線只准下降。** 上升時要**指名是哪一筆**，不得只改數字。
- **現況數字的唯一來源是 `tests/baselines/`**，不是這份文件。

## 路線圖

每個 Phase 用 SpecKit 展開（`/speckit-specify` → `/speckit-plan` → `/speckit-tasks` → `/speckit-implement`）。

### 階段 0-5b：地基與解耦

- [x] 完成（2026-03-10）

<!--
  交付：ViewHost + SemanticBus + Annotations (014) → SyncController 解耦 (015)
       → app.ts 拆分為 ToolboxBuilder/BlockRegistrar/AppShell (016)
       → concept/blockDef 分離、std 按 header 重組 (019)
       → VSCode Extension 原型 (018)  ⚠️ 2026-08-16 退休，見 history/069
       → DependencyResolver + ProgramScaffold + Ghost Line (020)
       → Semantic Node Identity：CodeMapping + BlockMapping 雙表 (021)
  前置條件：無
-->

**成功標準：**

- [x] Core 零 DOM 依賴，Node.js 環境可執行
- [x] 面板間零 import，跨層通訊只走 Bus
- [x] nodeId-based 跨投影查詢可用
- [ ] SemanticDiff 算法原型 → 延後至階段 9.2

### 階段 6：Topic 系統

- [x] 完成（2026-03-11）

<!--
  交付：Topic 核心型別與註冊表、層級樹引擎、BlockSpec Topic 覆蓋機制、
       C++ 內建 Topic 定義、Toolbox 整合、Topic 持久化與 User Context (022)
  前置條件：階段 0-5b
  設計原則：Topic 是純投影層概念，SemanticNode 不知道 Topic；
           Lifter 保持 Topic-agnostic；base + override 避免組合爆炸
-->

**成功標準：**

- [x] 同一語言在不同 Topic 下有不同層級樹與積木覆蓋
- [x] 所有測試通過
- [ ] manifest.json 加入 `topics` 欄位（等階段 7 統一做 manifest-driven plugin system）

### 執行機構自己接上（2026-08-08，不在 A–F 裡）

> ⚠️ **這一段 67 行沒有收斂，而那是刻意的**（2026-08-16 judge）：
> 它**沒有對應的 `history/` 轉變**，收成指標會讓那個轉變無處可指。
> **要收之前先補 history** —— 而寫它的人要有第一手記憶，我沒有。

- [x] **CI 跑測試** —— 在此之前 CI 一行測試都沒跑：22 條護欄、226 檔、3633 個測試（**當時的數字，現況看 `tests/baselines/`**），
      而自動執行它們的機制是零。**驗收用一個故意紅的 commit 做過**（`test: failure`／
      `build: skipped`／`deploy: skipped`），不是設定完就宣告完成。
      接上第一次就抓到一支**只在本機通得過**的測試。
      見 [執行機構](concepts/執行機構.md)「機制有了，沒人接上」第七個實例。

- [x] **P9「四項獨立性」第一次真的會關**（2026-08-11）—— `principles.md:117` 自稱
      「每個 Phase 完成後**必須通過**」，而在此之前 **31 條護欄裡零檢查**。
      第三十九條護欄（`tests/integration/audit-four-independences.test.ts`）把四條
      各對到一個數字：視圖 import 語言 **3**／其餘 UI 檔 **4 → 2**／視圖間 import **0**（硬性零）／
      跨層直接呼叫 **125 → 95**。
      兩個下降都是「**實作了**」不是「宣告了」：32 處狀態與輸出改成廣播、
      兩個中立的性狀讀取器上移到核心。
      > **一個從來沒有被執行過的品質閘門，比沒有閘門更糟——每個 Phase 都以為自己通過了。**

      剩下的耦合與它的三分類（廣播已清／命令 ~55 要先想清 `execution:at-node`／
      詢問 ~6 本來就該是依賴）見
      [draft/執行器直接持有五個面板](draft/2026-08-11-執行器直接持有五個面板.md)
      ——**硬體域的 2D 面板卡在命令那一堆上**。

- [ ] **`execution:at-node`：拆掉那張中央對映表**（2026-08-12 升格，設計見
      [draft/執行器直接持有五個面板 §二之二](draft/2026-08-11-執行器直接持有五個面板.md)）

      P9 的清償剩下的那一半。**要拆的不只是 55 處呼叫**——
      `sync-controller.getMappingForNode` 回傳 `{ blockId, startLine, endLine }`，
      **一張中央表回傳兩個視圖的座標**，加第三個視圖要改它的型別。

      > **一張「什麼都查得到」的中央對映表，就是視圖清單的另一種寫法。**

      **已決（2026-08-12）**：`autoScroll` 走 A（事件帶 `follow`）。
      理由是**開關在哪裡就說明了它屬於誰**——使用者在除錯工具列上打開它，
      那是對「執行」的指令；而 B（視圖自己訂閱檢視偏好）要為一個布林
      建一條新機制，那正是「機制有了沒人接上」的溫床。

      **驗收**：
      - [ ] `execution-controller.ts` 對 `blocklyPanel`／`monacoPanel` 的呼叫 → **0**
      - [ ] `getMappingForNode` 不再回傳兩個視圖的座標
      - [ ] 斷點命中的判斷裡**沒有行號**
      - [ ] ⚠️ **瀏覽器實測**四件事：積木高亮＋捲動、程式碼行高亮＋捲動、斷點會停
      - [ ] `npm test` 全綠

      **不在範圍**：`promptInput()`（要回覆，匯流排單向）、`getWorkspace()`／
      `getBreakpoints()` 本身（那是依賴，該依賴介面）、2D 視圖本身。

- [x] **端對端測試接上**（2026-08-12）—— 上面那條「沒有任何自動化在看渲染出來的
      結果」已兌現。6 支 Playwright（27 秒）進 CI，涵蓋**手動驗不到的那兩件事**：
      斷點命中會停、加速跳過整層。

      > 手動失敗的機制值得記著：斷點 gutter 靠座標點，**同一個座標時中時不中**，
      > 而 toggle 兩次等於沒設、**畫面上看不出差別**——於是「跑了但沒停」
      > 有兩種解釋而截圖分不出來。

      ⚠️ **接上第一天就抓到一個錯誤假設**：我以為「停住 ＝ 還沒印出 6」，
      而除錯模式是**先跑完再回放**——輸出與執行位置是兩個時間軸。
      那個假設在單元測試裡永遠不會被否證（那裡沒有「主控台」）。
      → [experience：「執行到哪裡」不能從輸出推](experience.md)

      判準寫在 `playwright.config.ts` 檔頭：**這件事在 DOM 之外驗得到嗎？**
      驗得到就不要寫進 e2e——3956 個 vitest 仍是主力。

- [x] **識別字一律英文**（2026-08-12）—— 618 個中文識別字 → **0**，
      收[第四十條護欄](../tests/integration/audit-identifier-language.test.ts)（硬性零）。
      轉變與五個「型別檢查看不到」的坑見
      [history/050](history/050-識別字不再是中文-一條沒有機制的慣例漂了兩個月.md)。

### 目錄結構的四件小整理（2026-08-12，`specs/117`）

- [x] **完成，而交付的是 2/4** —— `src/blocks/` 與 `src/views/` 消失，五個檔各歸其位。
      **另外兩項（合併遷移表、`ui/` 分層）被 Phase 0 查證否決**：
      那兩對「分裂」是刻意的，而 `toolbox-builder` 碰 Blockly 0 次。

> **同名不是重複的證據，它只是重複的一種可能長相。**
>
> 四個假設倒了三個在查證、一個在實作（護欄抓的）。轉變與機制見
> [history/052](history/052-同名不是重複的證據.md)。

### 階段 6.5：元件膠囊重構（碎裂與殼的清償）

> ⚠️ **這一段 57 行沒有收斂，理由同上**（2026-08-16 judge）：
> `history/027`／`047` 記的是它的**兩個片段**，不是這個階段的轉變本身。

- [x] **完成**（2026-08-12 判定）——**結果：加一顆元件 ＝ 新增一個資料夾，零編輯**

> **判定的內容，逐條**：A–F 六步全數交付（F 是 167 → 0），
> 三個病裡**碎裂**與**殼**都收到零（就近性 `notEncapsulated: 0`、
> 完備性 `shell: 0 / missing: 0`），而**條件性正確**沒有收零、也不該由這一階段收。
>
> **三項標為不阻斷，而它們的理由各不相同**：
> - 停用測試（85 → 36，目標 1）：它是**持續債**，不是這階段的產物
>   ——把持續指標當出口，階段永遠不會結束。
> - lift 平行機制（5 套 → 1）：**這條無法判定**，「原本那 5 套」沒有定義在任何地方。
> - C2 資訊軸：**延後不是取消**——見下。
>
> ⚠️ **勾這個 checkbox 不表示那三項消失了**——它們各自需要一個新項目，
> 而留在這裡只會讓一個已交付的階段看起來還開著。

> | | 項目 | 結果 | 轉變記錄 |
> |---|---|---|---|
> | ~~**A**~~ | ✅ 多形態機制 | 一個身分可有多個積木形態；元件 181 → 181、積木 77 → 81 | [027](history/027-A項多形態機制從阻斷變成解除.md) |
> | ~~**B**~~ | ✅ 身分整併 | 元件 182 → 175，確定桶 9 → 0；**位置不是身分，是形態** | [028](history/028-B項身分整併-位置不是身分.md) |
> | ~~**E**~~ | ✅ 登錄表導出 | 工具箱手寫 80 筆 → 45 段有序來源；使用者拿不到的積木 7 → 0 | [029](history/029-E項登錄表導出-清單不是碎裂是量測算錯了.md) |
> | ~~**C3**~~ | ✅ 引用完備性 | 幽靈身分 → 0；元件 175 → 177 | `specs/101` |
> | ~~**C1**~~ | ✅ 參數規格化 | 124 顆全部 `ParamSpec`；抓出 `cpp_malloc` 產出不合法 C++ | [030](history/030-C1參數規格化-證據優先於名字.md) |
> | ~~**D**~~ | ✅ 命名空間遷移 | 177/177 顆 `<scope>:<name>`；舊格式引用 3251 → **0**（硬性零） | [031](history/031-D命名空間遷移-身分不只以字串出現.md) |
> | **C2** | 🟡 **資訊軸（邊）——延後的理由已失效（2026-08-12）** | 原本的理由是「**今天沒有消費者**」。而[語義診斷系統](draft/2026-08-05-語義診斷系統.md)`:11` 推翻了它：那句話只對**軟體**診斷成立，**硬體診斷大多掛在邊上**——本設計因此是 C2 的**第一個真消費者**。⚠️ 所以它現在等的不是消費者，是**那個消費者被排進來** | 隨[階段 6.6](#階段-66語義診斷系統錯誤回饋)重看——⚠️ 而**軟體診斷不需要它**，等的是硬體診斷那一半 |
> | ~~**G**~~ | ✅ **命名整理**（2026-08-09） | 六項驗收全部歸零，已收**硬性零**；存檔 v3→v9 | [032](history/032-G命名整理-詞彙表就是護欄的輸入.md) |
> | ~~**F1**~~ | ✅ **積木型別導出**（2026-08-11） | 153 → **0**（硬性零）；存檔 v9→v10，**專案第一次積木狀態遷移** | [048](history/048-積木型別導出與四類描述過去的東西.md) |
> | ~~**F**~~ | ✅ **膠囊搬家**（2026-08-11） | 未膠囊化 **167 → 0**；四份共用宣告檔全部歸零 | [049](history/049-F完成-把身分換成性狀.md) |

#### 🟡 這一階段唯一還開著的：C2 資訊軸

- [ ] **C2 資訊軸（邊）**——⚠️ **延後的理由（「今天沒有消費者」）已經失效兩次**：
  ① 2026-08-12 [語義診斷系統](draft/2026-08-05-語義診斷系統.md)`:11`
  ——硬體診斷大多掛在**邊**上；
  ② 2026-08-14 [`Ln` 的 n 只是排序](draft/2026-08-13-Ln的n只是排序.md) §五之九
  ——傳遞閉包長出了第二個消費者。
  **而它今天仍不排入**：軟體診斷不需要它（診斷掛在節點上），
  等的是[階段 6.6](#階段-66語義診斷系統錯誤回饋) 之後的硬體診斷那一半。

<!--
  ⚠️ 2026-08-14 收斂：本節原本 212 行，其中大半是【已交付的細節與過期的起始快照】
  （三個病的起始基線、P0 四條護欄的首次數字、九項成功標準的逐條交付紀錄）。
  按 knowie-judge §5「已完成項目收斂成一行 ＋ 指標」拿掉，而【沒有一個數字消失】：

    現況數字   tests/baselines/          ← 唯一不會過期的來源
    起始數字   平均擴散 8.5 檔／中立性 20 檔 216 筆／殼 92 個空路徑 5 個缺／停用測試 85 筆
    每一步     上表的「轉變記錄」欄，history/027-032、048-050
    設計理由   draft/retired/2026-08-05-元件膠囊重構.md
    詞彙地基   concepts/元件.md
    改名工具   history/050（618 識別字／3918 次／223 檔，掀出五個「型別檢查看不到」的坑）
    命名翻轉   episodes/2026-08-09-命名的兩次翻轉.md

  為何插在階段 7 之前：Python 會繼承碎裂與殼；它帶來新內容不是新槽，
  而硬體域帶來新槽，兩者都該等格式收斂。見 draft/2026-08-05-硬體域併入計畫.md
-->

### 階段 6.6：語義診斷系統（錯誤回饋）

- [x] **已完成**（2026-08-17）——**五條驗收全數達成**。
  ⑤ 的兩個數字由 `tools/clangd-oracle/` 量出（65 個樣本）：

  ```
  涵蓋率  7/9  = 78%     缺口正好是【已知的那兩筆】：Cout 大小寫、<< 寫成 <
  假警報  0/45 = 0%      🟢 我們沒有擋下任何 clang 說合法的程式
  無法確定 11            Arduino 10（核心標頭不在 wasi sysroot）＋ __gcd 1
  ```

  🔴 **而排除「無法確定」是必須的**：不排的話涵蓋率是 **35%**
  ——⚠️ 而那個數字**看起來像一個發現**（`build-guardrail` 6.5）。
  轉變見 [history/071](history/071-裁判有了而它看不懂的方言要先排掉.md)。

> 🔴 **而 ⑤ 卡了三天的不是實作，是「分母怎麼定義」**——
> `build-guardrail` 6.8 記過參照編譯器**環境相依**（macOS clang vs CI gcc）。
> **跑在瀏覽器 wasm 裡的 clangd 每台機器都是同一個二進位**，那才是分母成立的原因。

> **為什麼是下一個**：使用者逐字——「我想要先打磨的是**錯誤回饋**，
> 因為這對教學有直接幫助」。而 C++ 的三情境覆蓋剛歸零
> （[history/057](history/057-缺陷清單只包含被記下來的缺陷.md)），
> 前面沒有東西擋著。
>
> **設計脈絡**：[draft/語義診斷系統](draft/2026-08-05-語義診斷系統.md)
> ——**in-flight rationale，做完才退休**（judge §4）。
>
> ### 驗收
>
> | | 條件 | 為什麼是它 |
> |---|---|---|
> | ~~1~~ | ✅ 診斷錨在 **`nodeId`** 而不是 `blockId` | 錨點決定**哪些視圖看得到**。處方抄 `execution:at-node`（`history/051`）——`onDiagnostics?()` 是可選方法，**沒有它 ＝ 明確地不接** |
> | ~~2~~ | ✅ **Monaco 收得到診斷**（程式碼面板有波浪） | 它是①的可否證版本。**在瀏覽器裡驗過**（積木圖示 1、波浪 1），並收成 e2e `diagnostics.spec.ts`——⚠️ 而那支**注入驗過會紅**：把錨點退回 `blockId`，波浪就消失 |
> | ~~3~~ | ✅ `degradationCause` **上得了畫面，而且不長得像錯誤** | 用 `MarkerSeverity.Info` ＋ **另一個 marker owner**，與診斷分開。⚠️ **而這一步先修掉一個更底層的缺陷**：語法錯誤原本被 lift 成一棵乾淨的樹（信心 `high`）——資料根本不在節點上 |
> | ~~4~~ | ✅ 訊息是 **`rule` ＋ `params`**，在**投影時**才組出來，不是一個字串 | 積木側好讀、程式碼側像編譯器——那是**兩個投影**，而它決定資料形狀，愈晚改愈貴。⚠️ **不是 `(level, panel)`**（2026-08-14 更正）：`level` 想指的是「讀的人碰過哪些概念」，那是 `levelTree` 上的一個**前緣（集合）**。⚠️ 節點的 level 定義得很好（[`Ln` 的 n](draft/2026-08-13-Ln的n只是排序.md)`:256`「n 是步數，字母是方向」），**而讀者不在任何一個座標上**——走完 `L2a` 沒碰 `L1b` 的人沒有等級。今天那個集合恆等於「全部」，所以④**不必發明等級就做得成**。🔴 **而做的時候撞出兩個既有缺陷**：程式碼面板一直把規則代號當訊息顯示給使用者（查 `window.Blockly?.Msg`，那個全域不存在，而 `?? key` 讓失敗看起來像成功）；`int , , ;` 產出三則無法區分的診斷，而積木側後蓋前——**三個問題只看得到一個**。見 [history/061](history/061-兩條軸的第二條一直沒接上.md) |
> | ~~4.5~~ | ✅ **`syntax_error` 從殘差通道搬回診斷通道**（2026-08-14 同日完成） | 少一個分號今天是 `MarkerSeverity.Info` ＋ `semorphe-residual`，**不走 `onDiagnostics`**——驗收④ 的整套一點都沒套到**第一週最常見的那個錯**上。⚠️ 而它也是⑤的前置：今天分號不在分母裡，「涵蓋率」量的是一個**不含最常見錯誤**的集合。見 [history/062](history/062-把使用者的錯歸進了我們的殘差.md)。🔴 **而外部證據站在這一邊**（2026-08-14）：結構化編輯器讓語法問題少 4.6x，**而學習者轉到文字編輯器後語法錯誤多 4x**（arXiv 2302.05708）——**擋掉而沒被看見的錯誤，鷹架撤掉時會原封不動回來**。見 [concepts/認知鷹架](concepts/認知鷹架.md)。**✅ 2026-08-14 完成**（spec `119`）——通道接好了：Error 級、走 `onDiagnostics`、帶 `source: 'parser'`，而 `unsupported`／`nonstandard_but_valid` 一行沒動（不變式由測試釘住）。🔴 **而做完之後才現形兩個辨識層的缺口**：「少分號」只有**某些形狀**會被標記、抄回來的原文是 ERROR 節點的文字（只有「1」）——見 [history/063](history/063-通道接好了而辨識那一層只認得某些形狀.md)。⚠️ **在那之前不得宣稱「少分號已經處理好了」**。**同批做 `source` 欄位**（`component` ｜ `parser`）——搬進診斷的東西當下就要說得出「這是誰的問題」，分兩次做等於把同一個型別改兩遍（設計見 [draft §文獻之後的設計](draft/2026-08-05-語義診斷系統.md)）。⚠️ **不動任何基線**：`syntax_error` 標在被 lift 出來的節點上而不是 `raw_code`，所以它**不在 `projection-residual` 的分母裡**（`residual2` 全零），也不在診斷的分母裡——**它有畫面而沒有任何護欄在數它** |
> | 5 | 涵蓋率與假警報**兩個數字都有** | 「我們不會檢查這種錯」是危險的那一格，而它只有量得出來才存在。⚠️ **這個寫法有外部支持**：加強錯誤訊息這件事**做了六十年而沒有共識**（Becker 等 2019 回顧 107 篇；2024 年連 LLM 版在 6 個任務只贏 1 個）——所以驗收不能寫成「訊息變好了」，**那正是證不出來的那個宣稱**。見 [draft §文獻怎麼說](draft/2026-08-05-語義診斷系統.md) |
>
> ⚠️ **不在這個階段的**：L2 委派編譯器（🔴 **2026-08-16 更正：
> 「要先回答離線／體積」那個理由已經失效**——兩者連同 COOP/COEP 標頭
> 都在 2026-08-15 實測過了，見 [draft §委派](draft/2026-08-05-語義診斷系統.md)。
> **今天等的是「clangd 在我們的頁面上真的吐得出一則診斷」**）、
> `target` 當第二個輸入（它會讓 [C 和 C++](draft/2026-08-13-C和C++難分難捨.md)§三 的設計首次被使用，
> 而那是一個獨立的承諾）。兩者都留在 draft。
>
> ⚠️ 而 **C2 資訊軸隨這個階段一起重看**——本設計是它的第一個真消費者，
> 但**軟體診斷不需要它**（診斷掛在節點上），所以 C2 等的是**硬體診斷**那一半。

### 階段 6.7：語法錯誤的辨識層（🟡 進行中——閘門已做，而三個缺口是主體）

- [ ] **進行中**（2026-08-14）——**執行閘門**（`120`）＋ **兩個缺口已關**（`121`），
  剩**第三個**（那個標記分不出是誰的錯）

> 🔴 **而 `120` 量出一件改變這個階段份量的事**（[history/064](history/064-量出來的東西不是我們要找的.md)）：
> 閘門只擋得住**有標記的**，而實測四種寫錯的程式**只有兩種有標記**。
> **瓶頸在辨識層，不在閘門**。
>
> ✅ **而 `121` 證實了那個判斷**：補上解析器的**傳播旗標**之後，
> 三種漏分號 ＋ 少右大括號全部擋得住，而**閘門一行沒改**。
> ⚠️ 根因是**表示方式**（實體節點 vs 旗標），不是「形狀」
> ——`063` 原本按症狀分類，於是同一個根因看起來像好幾個缺陷。
>
> ⚠️ **而限定換了範圍沒有消失**：認得的是**解析器認得的**。
>
> ### ✅ 而量出來的兩筆缺口，關掉了一半（2026-08-17）
>
> 階段 6.6 ⑤ 量出的缺口只有兩筆，而 clang 對第一筆給的代號是
> `undeclared_var_use_suggest`——**`suggest`：它會說「你是不是要打 cout」**。
>
> ```
> 之前   變數 'Score' 尚未宣告
> 之後   變數 'Score' 尚未宣告——你是不是要打 'score'？
> ```
>
> ⚠️ **這不是「改措辭」，是「加一個事實」**——`spec 119` 記過錯誤訊息的措辭
> **六十年沒有共識**，而這一筆告訴使用者**可見範圍裡有一個長得很像的名字**。
> **那是資訊，不是修辭。**
>
> 🔴 **而「不亂猜」比「會猜」重要**：找不到近似名時，訊息與之前**逐字相同**。
> 判準（大小寫／Damerau-Levenshtein 距離 1 且兩邊 ≥4 字元）與**它不猜什麼**
> 都由 `tests/unit/near-miss-suggestion.test.ts` 釘住。
>
> ### ✅ 而第二筆也關掉了——**它不需要型別資訊**（2026-08-17）
>
> `cout < "x"` 被 tree-sitter 讀成 `(cout) < ("x" << endl)`，於是 `cout`
> 成了裸識別字 → 🔴 **「變數 'cout' 尚未宣告」，而那是【事實錯誤】**
> （`cout` 在真的 C++ 裡是宣告過的）。
>
> ```
> 現在   'cout' 不是一個變數——它是 C++ 的輸入／輸出串流，要用 '<<' 才接得上東西
> ```
>
> 🔴 **而這一格我們說得比 clang 好**：同一段程式 clang 說
> 「reference to overloaded function could not be resolved」而且**指著 `endl`**。
> **委派解決的是判斷，不是說法。**
>
> ⚠️ **修在 cpp 膠囊裡**（`components/cpp/var_ref/`），不在 `Scope`
> ——中立性護欄：`src/interpreter` 不得硬編特定語言的名字。
>
> ### 🔴 而重量之後，那個 78% 有一半是量測的問題
>
> ```
> 偵測率      9/9 = 100%   擋下 or 跑到那一行才停
> 執行前擋下  7/9 =  78%   只有語法錯誤——【那是刻意的】（spec 120）
> 真的漏掉    0 筆
> ```
>
> **「我們發現了嗎」與「我們在什麼時候發現」是兩個問題**，
> 而第一版把它們壓成一個數字，於是一個**刻意的設計位置**看起來像缺陷。
>
> 🔴 **剩下的第三個缺口**：那個標記**分不出是「你寫錯了」還是
> 「我們的解析器讀不懂」**——由第四十三條護欄守著（合法語料誤標 0）。
> ⚠️ 而少分號時系統仍會說「**變數 x 未宣告**」
> （`experience`「一個指錯地方的錯誤訊息，比沒有訊息更糟」）
> ——**閘門擋住了它，於是那則誤導的訊息被蓋住而沒有被治**。

> **為什麼要有這個項目**：[history/063](history/063-通道接好了而辨識那一層只認得某些形狀.md)
> 記了三個缺口，而它們今天**沒有任何家**。
> ⚠️ 而 [history/017](history/017-加嚴一個檢查可能比不檢查更糟.md)
> 在被 judge 掃出來之前是**一份零引用的孤兒**——
> **一份沒有人引用的教訓等於沒有寫過**，一個沒有排期的缺口也一樣。
>
> ### 三個缺口
>
> | | 缺口 | 今天 |
> |---|---|---|
> | ① | 「少分號」只有**某些形狀**會被標記 | `int x = 1 ⏎ cout << x;` ✅／`⏎ return 0;` ❌／`⏎ int y = 2;` ❌ |
> | ② | 抄回來的原文是 **ERROR 節點**的文字 | 積木上顯示「照抄原文的：**1**」——技術正確，教學沒用 |
> | ③ | 🔴 那個標記**分不出是誰的錯** | 唯一訊號是「tree-sitter 產生 ERROR」，而合法但文法不涵蓋也會 |
>
> ### 而 ③ 已經有安全網，所以不急
>
> **第四十三條護欄**（`audit-false-syntax-error`，棘輪基線 0）：
> 合法語料 55 段被誤標 0 段。⚠️ **它不修 ③，它只保證 ③ 變壞時有人知道。**
>
> ### ✅ 第四個缺口（2026-08-15 加入，**同日完成**，spec `127`）
>
> ```
> score = 90;      ← 沒有 int
> cout << score;   → ✅ 跑完，輸出 90     而 C++ 拒絕它
> ```
>
> `src/interpreter/scope.ts:52` 的 `this.variables.set(name, value)`
> ——**找不到這個名字就默默創出來**。而同一個類別的 `get()`（`:33`）
> 為同一件事拋 `UNDECLARED_VAR`。**讀會拋，寫不會。**
>
> 🔴 **爆炸半徑實測 = 1 支測試**（4167/4168 綠，含 55 段情境語料與 APCS），
> **而那支測試斷言的正是這個缺陷**（`tests/unit/scope.test.ts:64`，
> 來自最早的 TDD 地基階段，**沒有寫任何理由**）。
>
> > **一支沒有寫理由的測試，記的是「當時的實作」而不是「當時的意圖」
> > ——而後來的人分不出這兩者，於是它變成一道看起來像決定的擋牆。**
>
> ⚠️ **它有一個教學上的受害者**：第二課花兩段講「`int` 不能省」，
> 而系統本身允許省，所以課文**繞了路**（`specs/126-.../findings.md` 坑三）。
>
> #### 驗收
>
> | | 條件 | 為什麼是它 |
> |---|---|---|
> | 1 | `score = 90;` 跑到那一行會停，訊息說得出**是哪個名字** | 訊息與 `get()` 那條同一則，不另立文案 |
> | 2 | 那支測試改成斷言**拒絕**，而**理由寫在測試裡** | 🔴 不寫的話下一個人又只能猜——這一次的根因就是它 |
> | 3 | 55 段情境語料 ＋ APCS 全綠、**護欄基線一個數字不動** | 已實測會過，而**預期要驗**。⚠️ 條數不寫在這裡（見開發約束） |
> | 4 | 第二課的繞路拿掉，正面教它並用錯誤訊息證明 | 沒有這一條，修了也沒有人接上 |
> | 5 | 🔴 **限定寫進程式碼與 spec**：這是**執行期**檢查 | C++ 在編譯期拒絕，我們在**跑到那一行時**才停——**不得宣稱兩者等價** |
>
> ⚠️ **而它不牴觸 P1**：`unsupported` 是我們沒長到（該跑），
> 這一筆是使用者寫壞了（[history/062](history/062-把使用者的錯歸進了我們的殘差.md) 的三分法）。
>
> #### ✅ 已完成（2026-08-15，spec `127`）——五條驗收全數兌現
>
> 轉變記在 [history/067](history/067-讀會拋寫不會而擋著的是一支沒寫理由的測試.md)。
> **淨減少程式碼**（拿掉一個 try/catch，換成遞迴）。
>
> 🔴 **而查證推翻了兩個設想**：
> ```
> 只改最後一行     行為【完全不會變】——catch 會吃掉新的拋錯，而它看起來像修好了
> 課文只是繞路     🔴 它還【寫了一句系統做不到的話】（「不加 int 就會看到那句話」）
> ```
>
> 🟢 **而繞路修好之後變成更好的東西**：第二課多了**第五步**
> （讓學生真的試一次忘記 `int`），錯誤訊息成了教學主軸。
>
> ⚠️ **仍然開著的**（本輪掀出、明確不修）：
> **一個「只有敘述沒有演練」的知識點**——課文寫了「那一行永遠跑不到就不會提醒」，
> 而**沒有讓學生試一次**。第三課的候選。
>
> ### 觸發條件（沒有這個，它會變成下一個孤兒）
>
> ```
> ① 第四十三條護欄的數字變成非零     → ③ 真的發生了，那時必須處理
> ② 教學現場回報「它說我語法錯但沒錯」 → 同上，而且更急
> ③ 委派編譯器（⑤ 的前置）做了       → 那時 ③ 有現成的仲裁者，順手解掉
> ```
>
> ⚠️ **①與③的關係要說清楚**：委派編譯器一旦做了，「這段程式合不合法」
> 就有權威答案——**③ 從一個判斷題變成一個查詢**。所以它可能**根本不必單獨做**。

### 階段 6.8：第一課（教材的第一個實例）

- [x] **已完成**（2026-08-14，specs `124`／`125`）——四條驗收全數兌現。
  坑清單在 `specs/124-first-lesson/findings.md`，走查在 `e2e/lesson-01.spec.ts`。

> 🔴 **它最有價值的產出是一份「不要做」的清單**：走完之後
> **一次都沒有暴露面板需求**（`findings.md:104`：「走一次只暴露了**說明的缺陷**」）。
> ⚠️ 而使用者的兩個回饋（圖片／用指的）**都是「更好」不是「走不下去」**。

### 階段 6.9：第二課（訊息不是代號）

- [x] **已完成**（2026-08-15，spec `126`）——第四十四條護欄
  （推給使用者看的字串不得長得像代號，硬性零）＋ 三種缺陷 ＋ 第二課 ＋ 走查。
  轉變見 [history/066](history/066-護欄錨在供給端所以量不到消費端.md)，
  坑清單在 `specs/126-runtime-message-and-lesson-2/findings.md`。

> 🔴 **兩份「不要做」的證據留在這裡**（它們影響後續判斷）：
> ```
> 教材面板    兩課、同一個人走過、【零次】浮出需求  → 仍然不做
> 走查框架    兩支的斷言【毫無交集】               → 不導出，重看條件是第四課
> ```
> ⚠️ 設計脈絡 [draft/教材要如何呈現](draft/2026-08-14-教材要如何呈現.md)
> **仍是 in-flight**（面板那一格還沒有答案），不退休。

### 階段 6.10：目標（`target`）——把三個既有機制綁成一個具名組合

- [x] **第一刀完成**（2026-08-17，spec `134`）——`Target` 型別 ＋ 登錄表 ＋
  兩筆資料，而 **C 的產出 6/10 → 10/10**（`c-style-parity`）。
  坑清單在 `specs/134-target-named-combination/findings.md`，
  轉變見 [history/072](history/072-target的兩個假設都倒了而它反而證明了target.md)。
  ⚠️ 設計脈絡 [draft/C 和 C++ 難分難捨](draft/2026-08-13-C和C++難分難捨.md)
  **仍是 in-flight**（`provides`／`reference` 兩格沒做），不退休。

> 🔴 **而實作時兩個假設被推翻，兩個都值得記**：
> ```
> plan 說「修 6/10 不需要 target」   ❌ 沒有任何既有欄位標得出「這是 C」
>                                    printf 競賽也是、explicit google 也是
>                                    → 而那【反而證明了 target 的必要性】
> spec 說「對映表今天不存在」        ❌ 它是 19 筆，而且【反向表也已經建好】
>                                    → 本功能新增的資料 = 0
> ```
>
> 🔴 **而最貴的坑是「兩條產出路徑」**：`c-style-parity` 10/10 全綠，
> **而瀏覽器上仍然產出 `<iostream>` ＋ `using namespace std;`**
> ——`cpp:program` 的鷹架路徑（UI 走）與 legacy 路徑（測試走）是分開的。
> 已收成 `e2e/c-target.spec.ts`。
>
> ⚠️ **而 `c.json` 從來沒進過選單**——本輪修好的 10/10，
> **在接上選單之前沒有任何人拿得到**。那是「機制有了沒人接上」的第六次，
> 而它差一點發生。
>
> ### ✅ 而「兩種投影跑起來一樣嗎」問了（2026-08-17）
>
> ```
> 兩邊都跑得動  9 / 10      🔴 輸出不一致 0      ⚠️ 不可判定 1
> ```
> 轉變見 [history/073](history/073-投影之間的一致性第一次被驗.md)（spec `135`）。
>
> 🔴 **那是根公理「唯一真實，各式投影」沒有被驗過的那一半**——
> 之前只驗「兩種投影**各自**合法」，而**投影之間的一致性**沒有任何東西在看。
> ⚠️ 而它等了一輪：`draft`§六 說「分母不夠時，任何『差異為 0』都不算數」，
> **先有 10/10 才問得了這一題**。
>
> ### 🔴 而 SC-001（選 3 次 → 1 次）**沒有真的兌現**
>
> ```
> cpp.json   topic: cpp-beginner   style: apcs
> c.json     topic: cpp-beginner   style: c     ← 🔴 【同一個課程清單】
> ```
>
> **兩個 target 的 `topic` 一樣**——所以今天只省了一次（選 style），
> 而「選一次而不是三次」**只兌現了三分之一**。
>
> 要它真的兌現，需要**一個 C 專屬的課程清單**——`<vector>`／`<iostream>`
> 那 **13 個在 C 裡不存在的概念**不該可見（research Q2 數過）。
> ⚠️ **而那是 `visible` 那一格真正的工作，第一刀沒做。**
>
> > **一個「綁定」如果兩邊綁的是同一個值，它在資料上成立，而在效果上是零。**
>
> ### ~~還沒做的~~ → 第二刀做掉了兩格中的一格
>
> ```
> ~~Target 的 UI~~       ✅ spec 136——選單選的是【目標】，選一次而不是三次
> provides／reference    完整設計的另外兩格　🔴 消費者是 Arduino
> 🔴 requires 缺一維     `cpp:print` 宣告 <iostream>，而 printf 產出要 <stdio.h>
>                        —— 那正是 `provides` 要解的
> ```

- [x] **第二刀完成**（2026-08-17，spec `136`）——**目標有了第一個真消費者**。
  轉變見 [history/074](history/074-機制有了沒人接上的第十一個而這次它有了執行機構.md)。

> 🔴 **出發點是一個查到的事實**：`grep targetRegistry src` → **0 筆**。
> 上一刀交付的登錄表**沒有任何產品程式碼呼叫它**
> ——那是「機制有了，沒人接上」的**第十一個實例，而它是我上一輪的交付**。
>
> ```
> 🟢 沒有產品消費者的登錄表   1 → 0     ← 第四十七條護欄（硬性零）
> 🟢 c-beginner 概念數        179 → 92  ← SC-001 這次真的兌現
> 🟢 使用者的選擇次數         3 → 1
> 🟢 選擇器數量               4 → 4     ← 反目標：收攏，不是多一個
> ```
>
> 🔴 **而那條護欄是 `concepts/執行機構.md:539` 早就指名的缺口**
> （「沒有一條護欄問過『機制有沒有人用』」）——**那句話寫下之後又被撞了三次。**
>
> ⚠️ **而本輪最貴的產出是一次誤診**：開瀏覽器看到 `<iostream>` → 判定是
> 顯式 include 節點 → 為它寫了程式碼 → **而 build 上重現不出來**。
> 那個 dev server 是好幾個編輯之前啟動的。
>
> > **一個開著沒關的 dev server，會讓「開瀏覽器實測」測到一個不存在的世界。**
>
> ⚠️ **而判準只涵蓋一半**：`requires` 推得出 59 顆函式庫層的，
> **而 `class`／`virtual`／`try` 一顆都推不出**——它們不需要任何標頭。
> 那 35 顆用具名清單 ＋ 兩道交叉驗證，**而其中 9 顆是漏宣告 `requires` 的**。
>
> **下一步是 Arduino**（第三個目標），而 `provides`／`reference` 是它的前置。

> **為什麼是現在**：那份 draft 的出口條件逐字寫著
> 「**`target` 有第一個真實需求** → §三的最後一段升格成 roadmap」，
> 而 2026-08-17 使用者提出：「**Arduino 我想要先把它至少視為不同風格或是
> 不同語言，而不是純粹 C++**」。
>
> 🔴 **而它比第一個消費者（語義診斷）硬**：
> ```
> 診斷      「先不做也不會怎樣」——只是分不出 C++03/11
> Arduino   🔴 無路可走——setup／loop 不是 C++ 的合法程式進入點
> ```
>
> ### 它是什麼（而它不是新的抽象層）
>
> ```
> target: c99-teaching
>   visible    → levelTree      哪些積木看得到
>   io         → cstdio         產出 printf 還是 cout
>   provides   → ["c99", …]     requires 對它求解
>   reference  → gcc -std=c99   驗證
> ```
>
> **把三個既有欄位綁成一個具名的組合**——就像 `style` 已經把一堆排版選項
> 綁成 `apcs`／`google`／`competitive`。
>
> ⚠️ **而缺的東西是「一致性」不是「能力」**：三個機制今天各存一半，
> 而使用者要選三次且必須一致。
>
> ### 🔴 第一刀用 **C**，不是 Arduino
>
> ```
> C        ✅ style 已存在（c.json）、對照實驗量過（6/10，缺口正好 2 種）
>          → 【今天就驗得出來】target 有沒有生效
> Arduino  🔴 要先有執行模型 ＋ 腳位 ＋ Serial，才看得到任何東西
> ```
>
> > **一個機制的第一個消費者，該選【已經量過差異】的那個，
> > 而不是【最想要】的那個。**
>
> ### 驗收
>
> | | 條件 | 為什麼是它 |
> |---|---|---|
> | 1 | `target` 是一個**具名組合**，而它綁的是**既有欄位** | ⚠️ 新增任何一個機制就違反了 draft 的核心主張 |
> | 2 | 至少兩個 target（今天的 C++ ＋ C），而**切換不改語義樹** | 「唯一真實，各式投影」——target 是投影參數不是真相 |
> | 3 | 🔴 `c-style-parity` 從 **6/10 → 10/10** | 缺口是**已知的兩種**（`bool` 要 `<stdbool.h>`／`struct` 標籤），而它們是 draft §六 的 ①② |
> | 4 | ⚠️ **中立性護欄 `total` 仍是 0** | target 是語言套件的東西，**核心不得認識它** |
> | 5 | 既有測試全綠、既有基線**一個數字不動** | |
>
> 🔴 **明確不含**：Arduino（那是下一個 target）、`reference` 欄位真的去跑編譯器、
> `provides`／`requires` 的完整求解。⚠️ **第一刀只做 `visible` ＋ `io`**。
>
> ### ⚠️ 而沒被採用的那一邊（tombstone）
>
> **先做 Python（階段 7）的理由是真的**：`vision` 記著它驗的是
> **語言無關性**（P9，四項獨立性之一），而 **Arduino／C 都驗不了**——
> 它們共用同一個解析器與同一批膠囊。
>
> **而先做 target 的理由**：
> ```
> Python 的第一步   🔴 收錄門檻在哪（PyPI 50 萬個）——draft 自己說【還沒答】
> target 的第一步   設計已寫好（§三），而第一個消費者是【不做就繞不過去】的
> ```
> ⚠️ **代價**：語言無關性晚一輪被驗。而 2026-08-17 查證讓那個代價變小——
> `universal` scope 是**空的**（189 顆全是 `cpp`），所以「universal 是一顆還是
> 每語言一顆」**今天沒有任何實例會被做錯**。
> 完整比較見 [draft/retired/Arduino 先還是 Python 先](draft/retired/2026-08-17-Arduino先還是Python先.md)⟨曾經⟩
> ——🟢 **那個問題 2026-08-17 有答案了**（「Arduino 先」），見
> [history/075](history/075-Arduino先於Python而被否決的那一邊的代價是真的.md)。

### 階段 6.11：Arduino——第一個「不是純粹 C++」的目標

- [ ] **已排序，未開工**（2026-08-17）——使用者逐字：「**Arduino 先**」。
  設計脈絡 [draft/支援 Arduino 要加什麼](draft/2026-08-13-支援Arduino要加什麼.md)
  **in-flight**，排序理由與**被否決那一邊的代價**見
  [history/075](history/075-Arduino先於Python而被否決的那一邊的代價是真的.md)。

> **為什麼是它**：第一步**沒有未決**（執行模型，純軟體），
> 而 Python 的第一步是一個**還沒有答案**的門檻問題（PyPI 50 萬個套件）。
> 🟢 而語言那一半是**量過的**：十段 Arduino 殘差節點 **0/10**，177 顆膠囊直接可用。
>
> 🔴 **而付出的代價要看得見**：Python 驗的是**語言無關性**（四項獨立性之一），
> 先做 Arduino ⟹ **膠囊格式的語言無關性晚一輪被驗**。
> ⚠️ 已查證它**沒有**擴大成「膠囊要重做」（`src/components/universal/` 不存在，
> 189 顆全是 `cpp` scope）——**晚被驗的是格式，不是這些膠囊。**

**順序（🔴 與直覺相反：不從板子或接線開始）**

```
1  執行模型：持續執行 ＋ 模擬時鐘         ❌ 不需硬體  🟡 中——`maxSteps` 語義要改
2  腳位狀態機 ＋ pinMode／digitalWrite…   ❌           🟢 小——每顆就是一般膠囊
3  Serial——接的是【現有的主控台】         ❌           🟢 小
4  板子視圖（唯讀，顯示腳位狀態）          🟡 一張圖    🟢 小——不需要求解
───────────────────────────────────────────────────────────────
5  接線（2D）＝ ArduinoCAD 併入           ✅           🔴 大——守恆網／全網求解／C2
6  真板子：arduino-cli ＋ 序列埠回讀       ✅           🔴 大——它是 adapter
```

> **1–4 全部不需要電氣求解，而那正是最貴的那一塊。
> 1–4 做完就有一個能教 Arduino 的工具了，而一塊板子都還沒接。**

**驗收（1–4）**

- [x] **第 1–3 項完成**（2026-08-17，spec `137`）——轉變見
  [history/076](history/076-Arduino跑得動了而我的驗收本身寫錯了兩條.md)。

> ```
> 🟢 十段語料   安靜地什麼都不做 → 10/10 跑得完、0 拋錯、9/10 有可觀察效果
> 🟢 新增元件   189 → 200（腳位 3 ＋ 常數 1 ＋ 時間 2 ＋ 類比 3 ＋ Serial 2）
> 🟢 npm test   4211 → 4275｜e2e 28｜中立性 0｜io_style 一字未改
> 🟢 瀏覽器     主控台印出 blink ×5
> ```
>
> 🔴 **而三個推翻比交付更值得記**：
> ```
> 規劃指認的「閘門」    不存在——修法六行、核心一字未動
>                       進入點由「樹裡有哪些函式」決定，而那顆的檔頭早就寫著
> 靠名字認人的樣式      把使用者宣告的 `enum Level { LOW = -1 }` 搶成 0
>                       ⚠️ 而既有的 builtin_constant 用【同一個做法】
> 🔴 兩條驗收本身       SC-001 用了與被修缺陷【同一個錯誤觀察量，方向相反】
> ```
>
> 🔴 **而 2026-08-17 使用者把順序改了**：第 4–6 項（虛擬硬體）**往後推**。
> 逐字：「**因為時間的緣故，我希望虛擬硬體元件等等可以晚點再做……
> 至少讓學生在寫 Arduino 程式的時候可以雙向轉換，然後甚至 AI 給的 Code
> 他們貼上來也是可以順利雙向轉換的。**」
> 轉變見 [history/077](history/077-虛擬硬體往後推而先做寬度.md)。
>
> ### ✅ 而盲測補上了（2026-08-17，`component-fuzz` 20 段）
>
> ```
> Round-trip PASS 7｜🔴 修掉的真缺陷 3｜待修 13（各有分類標記與根因）
> ```
>
> 🔴 **而兩個缺陷就在那 11 顆【剛寫完並自己測過】的膠囊裡**
> （數字字面的後綴 → `NaN` 不出聲；`Serial.print(v, 3)` 的第二引數被靜默丟掉）。
>
> > **一個知道實作的人寫的測試，會沿著實作的形狀長
> > ——而實作的形狀正是缺口的形狀。**
>
> ⚠️ **而缺陷帳因此退步**：停用測試 32 → 45。那 13 筆**不是新造的，是新發現的**
> ——報表要承受得住「發現＝退步」。報告在
> `tests/reports/fuzz-cpp-hardware-2026-08-17.md`。

### 階段 6.12：寬度——讓貼進來的 Arduino 程式碼轉得動

- [ ] **已排序，未開工**（2026-08-17）——排在虛擬硬體**之前**，見
  [history/077](history/077-虛擬硬體往後推而先做寬度.md)。

> **目標**：學生（或 AI）寫的 Arduino 程式**貼進來就能雙向轉換**。
> 🔴 而它是**可量的**：`lift 殘差` ＋ `round-trip 保真`。
>
> ⚠️ **而 2026-08-17 的盲測報表答的是另一個軸**：
> ```
> lift 殘差    20 段裡 14 段是 0     ← 辨識得出來
> round-trip   20 段裡 3 段漂移      ← 🔴 這兩個才是「雙向轉換」
> 執行不拋錯   20 段裡 7 段          ← 而報表的標題數的是這個
> ```
> > **同一份量測，換一個目標就要換一個軸——而舊的軸不會自己失效，
> > 它會繼續給出一個看起來像答案的數字。**

**順序**

- [x] **第 1 步（量）完成**（2026-08-17）——🔴 **而它推翻了第 2 步與第 4 步的理由**。
  轉變見 [history/078](history/078-量完之後階段6-12的前提垮了一半.md)。

> ```
> 語料       20 段典型的 AI／學生 Arduino 程式｜語法完整 20／片段 0
>            Uno 14｜Nano 3｜ESP32 3　套件 11 種
> 🟢 殘差    1 / 1516 節點 = 0.07%
> 🟢 漂移    樹 0/20　文字 0/20
> ```
>
> **貼進來幾乎完全轉得動。**
>
> 🔴 **而 `history/077` 寫的優先序是從錯的母體推出來的**：
> 「`const byte PINS[3]` 是招牌寫法」——**實測 20 段裡出現 0 次**。
> > **一個從「刻意找邊界」的語料得出的優先序，會把邊界誤認成中心。**
>
> 🔴 **而 11 種套件的呼叫全部辨識得出來**——套件的**語法**不特別，
> 特別的是**語義**（`Servo::write` 會轉動馬達）。**而雙向轉換不需要語義。**

```
~~2  修 const／static ＋ 陣列~~   ⚠️ 仍是真缺口，而**不該排最前面**（真實語料 0 次）
 3  寬 provides → 板子型號（Uno／Nano／ESP32）　⚠️ 不需要硬體
 ~~4  收套件~~                    🔴 **這一步的內容要重新定義**——
                                  它不是「讓程式碼轉得動」（已經動了），
                                  而是「讓積木好懂」＋「執行時有意義」，
                                  **而後者正是本輪往後推的虛擬硬體**
```

**驗收**

- [x] 一批 AI 寫的真實 Arduino 程式，**殘差與 round-trip 漂移各說得出數字**
      → `tests/probes/arduino-realistic.test.ts`（探測，**不是護欄**——今天不知道目標值）
- [ ] `arduino-uno`／`arduino-nano`／`esp32` 三個目標選得到，而它們**提供不同的常數與函式**
- [ ] C／C++ 三個既有目標不得退步

🔴 **而下一步變成「板子型號」，不是「修 const 陣列」** ——因為量出來的分佈是這樣說的。

**⚠️ 而它翻轉了一個排期假設**：`history/076` 與 draft 都說 `provides`／`reference`
的消費者是第 5–6 項（接線／真板子）。**而板子型號比接線先需要它，且不需要任何硬體。**

⚠️ **未決**：套件積木的粒度與工具箱預組 →
[draft](draft/2026-08-17-套件積木的粒度與預組.md)（**內容刻意留空，等第 1 步的資料**）。

- [x] 十段語料**有可觀察的效果**——⚠️ **而這條驗收本身改過一次**
      （原文是「真的跑得出輸出」，見 [history/076](history/076-Arduino跑得動了而我的驗收本身寫錯了兩條.md)§三）：
      四段只有 `pinMode`，**本來就不印東西**。正確的量是**輸出【或】腳位狀態**。
      ```
      🟢 10/10 跑得完、0 拋錯　🟢 9/10 有輸出或腳位變化　⚠️ 1/10 只改變數（而那是它該做的）
      ```
      🔴 原本的錨仍然成立且重要：「**沒有失敗**」與「**成功**」在報表上長得一模一樣
      ——本功能的起因正是那個，⚠️ **而我第一版的驗收犯了它的反面**。
- [x] `arduino` 目標**選得到**，而選它之後 `main` 不再是唯一進入點
- [ ] 板子視圖顯示腳位狀態，且**它逼出了佈局那一格的需求**
      （今天 `split-pane.ts` 只有左右兩個位置、`mobile-tab-bar.ts` 的 `TabId` 型別寫死）
- [x] C++／C 兩個目標**不得退步**——全套 4275 綠
      ⚠️ 而**六條基線刻意上調**（系統多會做一件事，不是判準放寬）

**兩個已拍板的決定（2026-08-17）**

```
✅ 時鐘      模擬為主，而【可以切成真實時間】
             ⚠️ 硬性要求：兩條路都要有測試，或【明說】哪一條沒被測到
             ——一條沒有人跑的路與一條不存在的路長得一樣，而學生會跑到它
✅ Serial    語言套件的 `ioStyle` trait（node-traits.ts:195 加第三個成員）
             🔴 【不是】核心的 io_style（core/types.ts:596）——P9：核心不認識 Arduino
```

⚠️ **仍然開著**：`arduino:` 該不該是自己的 scope（draft §六 未決②）
——而 draft 說**第 5 項才會逼出答案**。

**前置**

```
🟢 target 機制        spec 134／136 做完——而 Arduino 是它的第三個目標
🟢 provides           【不是前置】——Arduino sketch 一行 #include 都沒有（十段語料逐段確認）
🔴 執行路不知道目標    `ExecutionContext`（executor-registry.ts:9-48）沒有 style／target
                       ——那是第 1 項真正的閘門
🔴 provides／reference  完整設計的另外兩格【還沒做】，而 Arduino 是它們的消費者
                        provides  → pinMode／digitalWrite 的 requires 對它求解
                        reference → arduino-cli，而不是 clangd
                        ⚠️ 見 history/071：裁判的能力邊界是【目標】的函數
```

### 階段 7：Python 語言套件

- [ ] 完成

> **前置改為階段 6.5。** 角色也隨之改變：Python 不再是「發現架構是不是語言無關」的探針（答案已知：不是），而是**驗證階段 6.5 修好的架構**，並回答膠囊格式唯一需要第二語言的問題——universal 元件是一顆膠囊還是每語言一顆（見 draft/retired/2026-08-05-元件膠囊重構.md）。

> ⚠️ **2026-08-17 排到 Arduino（階段 6.11）之後**，使用者拍板。
> 🔴 **而那個代價要記住**：這一階段驗的是**語言無關性**（四項獨立性之一），
> 所以它晚一輪 ⟹ **在那之前加的每一顆 Arduino 膠囊，
> 都是在一個沒被驗過的格式上加的**。
> 理由與已查證的限縮見
> [history/075](history/075-Arduino先於Python而被否決的那一邊的代價是真的.md)。

<!--
  交付：7.1 語言骨架（manifest + core + stdlib + tree-sitter-python）
       7.2 Python DependencyResolver（concept → import 映射）
       7.3 Python ProgramScaffold（imports + if __name__ == "__main__"）
       7.4 Python 積木投射（核心積木達 C++ L0 等效）
  前置條件：階段 5 + 階段 6
  目的：用第二個語言驗證架構的語言無關性——這是四項獨立性中「語言獨立性」的真正檢驗
-->

**成功標準：**

- [ ] Python code ↔ blocks roundtrip 成功
- [ ] C++ 既有測試不受影響
- [ ] 語言切換時 toolbox 自動更新

### 階段 8：外部套件生態

- [ ] 完成

<!--
  交付：8.1 外部套件載入（manifest 依賴鏈 + 概念生命週期降級）
       8.2 套件安裝狀態 UI（ghost line 安裝指示 + 一鍵安裝）
       8.3 Arduino 語言套件（基於 C++ 擴充 + 硬體描述層 + Servo/NeoPixel）
  前置條件：階段 7
-->

**成功標準：**

- [ ] 安裝/移除套件後概念自動出現/降級
- [ ] 依賴鏈正確解析

### 階段 9+：進階擴充

- [ ] 完成

<!--
  交付：9.1 DataFlow 視圖（消費 control_flow annotations）
       9.2 SemanticDiff 增量更新（前置：階段 5b nodeId 穩定性）
       9.3 接線視圖 + 模擬視圖（硬體教育）
       9.4 跨語言映射視圖（abstractConcept 驅動）
       9.5 語義套件市場
  前置條件：階段 8
-->

**成功標準：**

- [ ] 唯讀視圖可從 annotations 自動生成，不需改語言套件
- [ ] style 變更的語義 diff 為零

## 關鍵延伸（主題觸發必讀）

| 觸發關鍵字 | MUST 讀 |
|---|---|
| 新增語言、語言套件、manifest、依賴解析 | `concepts/開放擴充.md` |
| Topic、認知層級、toolbox、scaffold | `concepts/漸進揭露.md` |
| 新視圖、viewType、可逆性、唯讀視圖 | `concepts/投影.md` |
| 元件、接點、埠、關係律、命名、跨域 | `concepts/元件.md` |
| 殼、規範沒有檢查、護欄、量測工具 | `concepts/執行機構.md` |
| 語義診斷、錯誤訊息投影 | `draft/2026-08-05-語義診斷系統.md` |
| 圖解、記憶體視覺化、資料結構圖鑑 | `draft/2026-08-05-圖解形態與資料結構圖鑑.md` |
| 硬體、Arduino、接線視圖、2D/3D 組裝 | `draft/2026-08-05-硬體域併入計畫.md` |
| 待解工程問題、效能、版本控制、沙箱 | `draft/2026-03-11-已知工程待解問題.md` |
