# 願景

## 問題陳述

程式教育有一個根本鴻溝：**理解程式邏輯**和**掌握語法細節**之間的斷層。

傳統工具走兩個極端——Scratch/Blockly 讓學生拼積木但永遠碰不到真正的程式碼；文字教學讓初學者同時面對邏輯和語法的雙重認知負荷。兩者之間缺少一座橋。

**目標使用者：**

- **學生**（主要）：從零基礎到進階，涵蓋 APCS 考試準備、競程訓練、學校課程
- **教師**：設計教學路徑（Topic）、指定練習、追蹤學習軌跡
- **語言開發者**：透過模組化語言套件擴充新程式語言
- **課程設計者**：透過 Topic 系統定制不同教學情境的概念可見性和積木形態

## 核心想法

**程式碼和積木都不是本質——它們是同一個語義結構的不同投影。** 學生可以用積木理解邏輯，同時看到對應的真實程式碼，在兩者之間自由切換，最終過渡到純文字編程。

關鍵不是在程式碼上面疊一層積木——**只有語義層存在**，程式碼和積木都是參數化的投影。理論基礎見 [原則](principles.md)。

**和現有工具的根本差異：**

| 工具 | 做法 | Semorphe 的不同 |
|------|------|----------------|
| Blockly/Scratch | 積木 → 程式碼（單向，不可逆） | 程式碼 ↔ 積木（雙向，完美 round-trip） |
| Hedy | 每個等級有不同語法 | 同一語義樹，不同投影解析度 |
| 傳統 IDE | 以文字為中心，視覺化為附加 | 以語義為中心，文字和視覺都是投影 |
| Unison | AST hash 內容定址 | hash 語法層 vs hash 語義層 |
| LSP/LSIF | 跨檔案符號索引 | 位置導向 vs 語義導向 |

## 專案身份

| | |
|---|---|
| **名稱** | Semorphe（散模費 σημορφή）— σῆμα（語義）+ μορφή（形態） |
| **標語** | 唯一真實，各式投影 |
| **CLI** | `smorph` |
| **Logo** | `<Σ>` |
| **npm** | `semorphe` |
| **VSCode** | `semorphe-vscode` |
| **技術棧** | TypeScript 5.x, Blockly 12.4.1, web-tree-sitter 0.26.6, Monaco Editor, Vite |

## 現狀

C++ 單語言可用，程式碼 ↔ 積木雙向 round-trip 成立，語義直譯器可執行。三層解耦（Core / Bus / View）已落地。Topic 系統已完成。

> **語言無關性目前是破的**——核心層仍有檔案硬編語言專屬元件身分，違反 P9；同時有碎裂（一個元件散在太多檔）與殼（路徑存在但空的）。清償排在階段 6.5，**數字已開始下降**。
> **當下的實測值一律以 `tests/baselines/*.json` 為準，不寫在這裡**——寫死的數字每交付一次就過期一次（這已經是第三次了）。這裡只說**問題的形狀**，數字歸護欄。
> 這句話從「尚未驗證」改成「已知是破的」的轉折，以及階段 7 角色隨之改變的理由，見 [history/015](history/015-語言無關性從尚未驗證到已知是破的.md)。

**已完成元件：**

- **Core**：SemanticNode 型別、ConceptRegistry、BlockSpecRegistry、PatternLifter、PatternRenderer/Extractor、CodeGenerator、TemplateGenerator、BlockRenderer、Interpreter、Topic 系統、Storage、DependencyResolver（語言無關介面）、ProgramScaffold、CodeMapping + BlockMapping（nodeId-based 跨投影查詢）
- **C++ 語言套件**：core concepts/blocks/generators/lifters、std modules（iostream/cstdio/vector/algorithm/string/map/stack/queue/set/cstring/cmath）、ModuleRegistry、auto-include、lift-patterns、render strategies、style presets（apcs/competitive/google）、style-exceptions、manifest
- **UI**：Blockly/Monaco/Console/Variable 面板、Debug 工具列、SyncController、ToolboxBuilder、BlockRegistrar、AppShell、ExecutionController、Ghost Line

**已知差距：**

| 缺失項目 | 優先級 | 說明 |
|----------|--------|------|
| SemanticDiff 增量更新 | P2 | 全量替換 → 增量（Phase 9.2，nodeId 穩定性已部分完成） |
| 硬體描述層 | P3 | 等 Arduino 需求（Phase 8） |
| DataFlow 視圖 | P3 | 需 annotations 機制（Phase 9.1） |

## 架構

```
宿主環境（瀏覽器 / VSCode / Electron）
  ↕ ViewHost 介面
多個獨立視圖（積木 / 程式碼 / 主控台 / 變數 / 資料流 / 接線 / 模擬）
  ↕ SemanticBus（瀏覽器=EventEmitter / VSCode=postMessage）
SemanticCore（語義樹 + 概念註冊 + 投影引擎 + 直譯器，零 DOM 依賴）
  ↕ 語言套件介面
多個語言套件（C++ / Python / Arduino）+ 外部擴充套件
```

### 三層契約

- **Layer 1（語義宣告）**：concepts.json + lift-patterns + lifters — 所有消費者可用
- **Layer 2（投影提示）**：blockDef / renderMapping / codeTemplate / generator — 僅對應視圖消費
- **Layer 3（視圖策略）**：視圖根據 annotations 決定呈現 — 新增唯讀視圖不需改語言套件

### 語言套件目錄結構

```
languages/{lang}/
  ├─ manifest.json          ← 套件中繼資料
  ├─ core/                  ← 語言核心概念（concepts.json / blocks.json / generators/ / lifters/）
  ├─ std/                   ← 標準函式庫（目錄名即 header 名）
  ├─ lift-patterns.json     ← AST → 語義 pattern 規則
  └─ auto-include.ts / style-exceptions.ts / renderers/ / styles/
```

### 尚未實作但已有設計的規格

- **語義標註（Annotations）**：語言套件與視圖套件之間的開放契約（`control_flow`、`body_execution`、`introduces_scope`、`side_effects`、`hardware_binding` 等）。缺少 annotation 時用 generic fallback，不報錯——任何語言 × 視圖組合都能工作。
- **視圖套件規格**：按與語義樹的互動方式分類（可編輯 / 唯讀-執行 / 唯讀-分析 / 唯讀-硬體），結構為 `views/{name}/manifest.json` + `src/view.ts`（實作 ViewHost）。
- **WebView 隔離模型**：瀏覽器每視圖 = `<div>`/`<iframe>` + EventEmitter；VSCode 中 blocks = WebviewPanel、code = 原生 TextEditor、console = Terminal + postMessage。**Core 跑在 Extension Host，不跑在 WebView。**
- **外部套件與硬體擴充**：`packages/{name}/` 含 semantics / projections / hardware。依賴規則：擴充可引用基礎套件概念，**不可引用其他擴充**（避免菱形依賴）。

### 長期方向

1. **多語言**：C++ → Python → Java → Go → Rust……同一語義結構，切換語言即切換投影
2. **語義套件市場**：語義契約 + 投影定義 + 多後端執行體。同一概念可有多個執行體（wasm / js / webgpu / remote），自動基準測試 + 自動後端選擇。教學價值：讓學習者直觀感受演算法複雜度
3. **可插拔執行與時空旅行除錯**：執行即投影；直譯器每步產生語義狀態快照，回溯 = 用歷史快照重新投影（viewParams 多一個 `timeStep`）
4. **硬體教育**：Arduino 接線視圖 + 模擬視圖，從軟體延伸到嵌入式
5. **AI 輔助**：LLM 作為語用分析師，在確定性 guardrails 內提供提示投影——建議下一步，但不自動修改語義結構
6. **自舉**：教學路徑本身成為可分享的語義套件；學習數據回饋優化 Topic 層級樹，目標是**結構還原度最大化**而非效能最大化

## 路線圖

每個 Phase 用 SpecKit 展開（`/speckit-specify` → `/speckit-plan` → `/speckit-tasks` → `/speckit-implement`）。

### 階段 0-5b：地基與解耦

- [x] 完成（2026-03-10）

<!--
  交付：ViewHost + SemanticBus + Annotations (014) → SyncController 解耦 (015)
       → app.ts 拆分為 ToolboxBuilder/BlockRegistrar/AppShell (016)
       → concept/blockDef 分離、std 按 header 重組 (019)
       → VSCode Extension 原型 (018)
       → DependencyResolver + ProgramScaffold + Ghost Line (020)
       → Semantic Node Identity：CodeMapping + BlockMapping 雙表 (021)
  前置條件：無
-->

**成功標準：**

- [x] Core 零 DOM 依賴，Node.js 環境可執行
- [x] 面板間零 import，跨層通訊只走 Bus
- [x] nodeId-based 跨投影查詢可用
- [ ] SemanticDiff 算法原型 → 延後至階段 9.2

### 階段 6：Topic 系統

- [x] 完成（2026-03-11）

<!--
  交付：Topic 核心型別與註冊表、層級樹引擎、BlockSpec Topic 覆蓋機制、
       C++ 內建 Topic 定義、Toolbox 整合、Topic 持久化與 User Context (022)
  前置條件：階段 0-5b
  設計原則：Topic 是純投影層概念，SemanticNode 不知道 Topic；
           Lifter 保持 Topic-agnostic；base + override 避免組合爆炸
-->

**成功標準：**

- [x] 同一語言在不同 Topic 下有不同層級樹與積木覆蓋
- [x] 所有測試通過
- [ ] manifest.json 加入 `topics` 欄位（等階段 7 統一做 manifest-driven plugin system）

### 執行機構自己接上（2026-08-08，不在 A–F 裡）

- [x] **CI 跑測試** —— 在此之前 CI 一行測試都沒跑：22 條護欄、226 檔、3633 個測試，
      而自動執行它們的機制是零。**驗收用一個故意紅的 commit 做過**（`test: failure`／
      `build: skipped`／`deploy: skipped`），不是設定完就宣告完成。
      接上第一次就抓到一支**只在本機通得過**的測試。
      見 [執行機構](concepts/執行機構.md)「機制有了，沒人接上」第七個實例。

> **下一格（未承諾）：沒有任何自動化在看渲染出來的結果。** 五次使用者回報裡有四次
> 現在有機械檢查，而它們全部停在瀏覽器之前。`happy-dom` 走不通（Blockly 12 的
> FocusManager 會炸），**真瀏覽器可行**——這一輪用它找到了根因。
> 要補得付 Playwright ＋ CI 的成本，需要人拍板。

### 階段 6.5：元件膠囊重構（碎裂與殼的清償）

- [ ] 完成

<!--
  交付：P0 四條護欄（就近性/中立性/完備性/缺陷帳）→ P1 認領協定 → P1.5 目錄軸定案
       → P2 lift+execute 搬回模組（17 次）→ P3 io.ts 塌成路由器 → P4 殺雙重真相
       → P5 核心型別（原子）→ P6 膠囊格式定案+遷移 → P7 kernel 約束脊椎
  前置條件：階段 6
  設計理由（在建，做完才反流退場）：draft/2026-08-05-元件膠囊重構.md
  詞彙地基：concepts/元件.md
  為何插在 Phase 7 之前：Python 會繼承碎裂與殼；且它帶來的是新內容不是新槽，
       而硬體域（未承諾）帶來新槽，兩者都該等格式收斂。見 draft/2026-08-05-硬體域併入計畫.md
-->

**這一階段治的是三個病**（難度遞增，偵測工具不同）：

| 病 | 症狀 | 誰抓得到 | 基線 |
|---|---|---|---|
| **碎裂** | 東西散在太多地方 | 靜態計數 | 起始：平均擴散 8.5 檔；中立性 20 檔／216 筆 |
| **殼** | 東西在，但是空的 | 執行式單元審計 | 起始：🈳92 個路徑 ／ ❌5 個｜**2026-08-06：🈳8 ／ ❌0** |
| **條件性正確** | 單獨測都過，組合才壞 | 組合式測試 | 起始：85 筆停用測試 |

> **表中是「起始」值，不是現況。** 現況看 `tests/baselines/`——那是唯一不會過期的來源。

**成功標準：**

- [x] **P0 四條護欄進 CI，各自輸出非零基線數字**（後續增至七條：P1 辨識歧義、052 存檔完整性、053 執行器重複註冊），且全部是單調不增的棘輪（2026-08-06，`specs/049-audit-guardrails`）

> **P0 實測基線**（`tests/baselines/`）：
>
> | 護欄 | 抓哪個病 | 基線 |
> |---|---|---|
> | 中立性 | 跨域碎裂 | **20 個檔** 硬編語言專屬元件身分（216 筆） |
> | 完備性 | 殼 | 175 元件 × 5 路：✅778 ／ **🈳92** ／ **❌5**；殼分佈 execute 54、lift 34、generate 4 |
> | 缺陷帳 | 條件性正確 | **85 筆**停用測試；`print` 擋住 21 個、`array_declare` 擋住 19 個 |
> | 就近性 | 碎裂 | 175 元件全數有足跡，**平均擴散 8.5 檔**；最擴散 `input` 20 檔／16 目錄 |
>
> **清償優先序由基線直接落出**：
>
> ~~修 `print` 與 `array_declare` 解鎖 40 個測試（47%）~~ ⚠️ **這句在 2026-08-06 被 050 推翻**——那 40 筆裡有 64/86 是**只有名字的測試**（`it.todo`，沒有本體），修好阻斷者不會解鎖任何東西，它們需要的是**重新產生測試**。實際數字：`print` 解鎖 **5** 個、`array_declare` **2** 個。**缺陷帳的優先序是平的**：22 筆有本體，散在 11 個阻斷者上，最大的一個只有 5。
>
> **真正集中的是中立性**：違規的 **77% 在 `src/interpreter/`**（起始 176/216，053 後 134/174），而完備性的殼裡 `execute` 也是最大宗——**同一個子系統，兩個病都在那裡**。清償它需要把執行器搬進語言套件，那是 P2。
- [ ] 新增一個元件觸碰的檔案數 = **1**（就近性；基線平均 8.5、最高 20）

> **2026-08-07：這一項被拆成有依賴關係的六步 A–F。** 起始 8.5、現在 **7.9**——
> 整個階段打下來幾乎沒動，因為它不能逐筆清：**專屬檔只有 24 個（2%）**，
> 膠囊今天幾乎不存在。而 71% 的檔次集中在 20 個共用檔。
>
> | | 項目 | 結果 | 轉變記錄 |
> |---|---|---|---|
> | ~~**A**~~ | ✅ 多形態機制 | 一個身分可有多個積木形態；元件 181 → 181、積木 77 → 81 | [027](history/027-A項多形態機制從阻斷變成解除.md) |
> | ~~**B**~~ | ✅ 身分整併 | 元件 182 → 175，確定桶 9 → 0；**位置不是身分，是形態** | [028](history/028-B項身分整併-位置不是身分.md) |
> | ~~**E**~~ | ✅ 登錄表導出 | 工具箱手寫 80 筆 → 45 段有序來源；使用者拿不到的積木 7 → 0 | [029](history/029-E項登錄表導出-清單不是碎裂是量測算錯了.md) |
> | ~~**C3**~~ | ✅ 引用完備性 | 幽靈身分 → 0；元件 175 → 177 | `specs/101` |
> | ~~**C1**~~ | ✅ 參數規格化 | 124 顆全部 `ParamSpec`；抓出 `cpp_malloc` 產出不合法 C++ | [030](history/030-C1參數規格化-證據優先於名字.md) |
> | ~~**D**~~ | ✅ 命名空間遷移 | 177/177 顆 `<scope>:<name>`；舊格式引用 3251 → **0**（硬性零） | [031](history/031-D命名空間遷移-身分不只以字串出現.md) |
> | ~~**C2**~~ | ⏸ **資訊軸（邊）——延後，不是取消** | **今天沒有消費者**：`concepts/元件.md:343` 說它「尚不存在」，而 semorphe 這側四個 draft 沒有一個需要它。而「機制有了沒人接上」已有**八個實例**——現在加一整條沒人讀的軸就是預約第九個 | 等硬體域併入，或等它長出消費者 |
> | **G** | **命名整理** ⬅ **新增，且必須在 F 之前** | 依賴 ~~D~~ ✅ | 見下方驗收數字 |
> | **F** | **膠囊搬家** | 依賴 A–E ✅、C1 ✅、C3 ✅、D ✅、**G** | 驗收：專屬檔 2% → **100%**；就近性 → 1 |
>
> ### G 命名整理（2026-08-09 新增）
>
> **為什麼在 F 之前**：F 讓每顆元件搬進自己的目錄，而**目錄名從元件名來**。
> 名字晚一步改，F 就要做兩次。
>
> **方向已定**（人拍板，2026-08-09）：**各語言自理 ＋ 轉換規範分開**——
> 每個語言擁有自己的元件、用自己的方式命名，通用性住在轉換規範裡而不在名字裡。
> 於是**這不是把 145 顆 `cpp:` 搬去 `lang:`**（那是被推翻的方向），
> 而是讓 `cpp:` 這一套內部自洽。
>
> **驗收數字**（已量，四項現在都是紅的）：
>
> | 量 | 現在 | 目標 |
> |---|---|---|
> | 同一角色的參數名種類 | 2（`obj` 30 顆／`vector` 3 顆） | **1** |
> | 同義操作詞群 | ≥4（大小／加入／取值／看某一端） | **0** |
> | 裸的函式庫名（`strlen`／`stoi`／`iota`…） | ~35 | **0** |
> | 修飾詞站在主體位置（`static_*`／`const_*`） | 2 族 | **0** |
>
> ⚠️ **驗收必須是可機械檢查的數字**，不能是「看起來整齊」——
> E 項踩過一次「一個假的驗收標準活了兩天，因為它是一個數字」（[history/029](history/029-E項登錄表導出-清單不是碎裂是量測算錯了.md)）。
> 這四項的反面是：如果只寫「命名一致」，下一個維護者會用自己的品味再改一次，
> 而每次改都是一次存檔遷移。
>
> **範圍不只是改名**：要一併把**封閉詞彙表**定下來、接上護欄。
> 不然半年後有人加元件時又長出 `length`、又長出 `count`——
> **而那時已經有版本了，改起來就貴**（要發版、遷移、立墓碑）。
>
> > **趁還沒有版本的時候，把會反覆發生的錯誤先擋住。**
> > 「乾淨的開始」不只是名字乾淨，是**讓它保持乾淨的機制先就位**。
>
> **順序**（2026-08-09 定）：
>
> ```
> 四條驗收護欄（先紅） → D1 拍板 → G（改名 ＋ 定詞彙表 ＋ 收綠）
>       → 宣告 cpp@1  ← 版本紀律從這裡開始
> ```
>
> **版本 1 是 G 的產物，不是 G 的前提**——先版本化的話，G 的每一個舊名字
> 都會變成永久墓碑（約 100 個你正要丟掉的名字，永遠不得重用）。
> 見 [draft/元件套件管理](draft/2026-08-09-元件套件管理.md)。
>
> **已定**：分隔符 `_`；scope ≠ 套件（一個 scope 可有多個套件）；
> scope 是**套件擁有者**不是語言；積木各語言各自（不共用）。
> **尚未決定**：D1（那 32 顆 `lang:` → `cpp:`）要不要現在做。
>
> **設計理由（在建）**：[draft/2026-08-08-元件命名重新設計](draft/2026-08-08-元件命名重新設計.md)
> ／[draft/2026-08-08-等價的觀察集與回饋](draft/2026-08-08-等價的觀察集與回饋.md)
>
> **F 之前要先 skill 化**——「搬一顆元件進膠囊」會重複 177 次，而既有的
> `component-refactor` 做的是四路稽核與醫治，不是搬家。
> ⚠️ **大規模身分改名已經重複兩次**（089 型別改名 560 處、103 身分改名 3251 處），
> F 是第三次——`knowie-judge` 的重複偵測把它標為 skill 候選。
>
> **設計理由（在建，F 做完才反流退場）**：[draft/2026-08-07-元件目錄與膠囊契約](draft/2026-08-07-元件目錄與膠囊契約.md)
>
> ⚠️ **這一項原本寫著「＝ P9 語言獨立性第一次真的成立」，而那個等號是錯的。**
>
> 中立性只量得到耦合的**一種形式**——元件身分字串。清償過程中發現核心層還有
> 六處寫死 C 家族的註解語法，而這條護欄**一筆都數不到**（它找身分，不找語法）。
> 那六處同樣違反 P9。
>
> 歸零代表「**身分那一維乾淨了**」，不代表 P9 成立。其他維度還沒有護欄：
> 語法符號（目前只有註解那一類有專門檢查）、執行期才成立的耦合。
> 見 [history/021](history/021-中立性的數字只量身分那一維.md)。
>
> **下一步是補其他維度的護欄，不是宣告勝利。**
>
> ✅ **語法那一維已補**（2026-08-06，065，第九條護欄）：確定 0 處、無法確定
> 48 處。它一蓋起來就抓到中立性看不見的東西，所以上面那句不是猜的。
> 還沒補的維度：**執行期才拼出來的語法**（從設定組合的字串）。
>
> ✅ **型別名那一維也補了**（2026-08-07，095）：核心層寫死 `'istringstream'`
> 判斷 `>>` 是讀值還是位移——中立性與語法耦合**兩條都數不到**，叫的是就近性，
> 而它量的根本不是語言耦合。**「有護欄叫了」不代表那條護欄涵蓋了這個問題。**
> 見 [history/024](history/024-耦合的第四種形式是型別名.md)。

> **收盤與雙重真相基線見 [episodes/…五次下降](episodes/2026-08-06-中立性從216到0的五次下降.md)。**
- [ ] 完備性報表的 🈳殼 與 ❌缺 **歸零**（＝ P2「0 容忍」第一次真的被檢查）

> **完備性 🈳 92 → 27、❌ 5 → 1**（換線打完備性那兩輪）。修的過程挖到三個更根本的
> 缺口（陣列初始值沒進到執行期、字元陣列印成 `[array]`、`char s[4]="ab"` 沒拆成字元）。
> 現場見 [episodes/…五次下降](episodes/2026-08-06-中立性從216到0的五次下降.md)。

> **五次下降，中立性 216 → 0**（053／054／055／056／059–064）。**每一次的性質都不同**
> ——說清楚／純搬位置／修抽象層／修量測誤報，混在同一個數字裡的話，用宣告刷數字
> 看起來會像進步。交付現場與逐次歸因見
> [episodes/2026-08-06-中立性從216到0的五次下降.md](episodes/2026-08-06-中立性從216到0的五次下降.md)。

- [ ] 停用測試 **85 → 1**（只剩那個連到墓碑的 `#define`）

> **2026-08-07：31 筆**（有測試本體 18、只有名字 13）。第八批查證三支，**全部
> 不可信**——兩支標籤過期、一支方向錯。八批累計 ~50/64 的標籤是過期或指錯方向的。
> 判準見 [experience.md](experience.md)「停用測試的標籤有四層可信度」。
>
> **同一輪修好了測試套件自己的條件性正確**：呼叫編譯器的測試吃 vitest 預設的
> 5 秒逾時，於是全套隨機紅 2–4 個檔。成因四天前就診斷對了，寫在 draft 裡然後
> 沒有機制把它帶回來。修好之後全套連兩次 **211 檔全綠、零 FAIL**——這一輪第一次
> 真正的全綠。見 [history/025](history/025-那個-flake-從真實變數改為診斷過但沒人接上.md)。
- [x] **P1 認領協定：P3「歧義在註冊時仲裁」第一次有執行機構**（2026-08-06，`specs/051-lift-claim-arbitration`）

> **P1 實測基線**（`tests/baselines/lift-ambiguity.json`）：8 個同優先權群組、**10 對確定會撞**、18 對無法確定、**11 處重複登記**。
>
> `declaration` 上有 **5 條規則的限定條件完全相同**（都是 `type: template_type`）——第一條贏走全部，**另外 4 條永遠不會被試到**。
>
> ⚠️ **2026-08-06 當日修正**：原文寫「它們是死規則」並列為「最嚴重的發現」，**高估了**。實測 `vector`／`map`／`stack`／`queue`／`set` 宣告全部辨識正確——因為 5 條指向**同一個 strategy**（`strategies.ts:587` 依模板名分派 7 種容器）。所以它們不是 5 條規則，是**同一條規則被登記 5 次**。行為沒壞，壞的是護欄的分類：`duplicates` 只抓「同一 conceptId 登記多次」，抓不到「**不同 conceptId 但同一判別式 + 同一 strategy**」。
>
> 這條護欄量的是「有多少東西**靠運氣**」，不是「有多少東西壞了」——兩者為何要分開講，見 [concepts/執行機構.md](concepts/執行機構.md)。
>
> 順帶推翻了 `history/005` 的狀態欄：偏序仲裁標著「✅ 已採用」，實測**只有三分之一落地**。見 [history/016](history/016-偏序仲裁的情況二從未實作.md)。

- [ ] lift 平行機制 **5 套 → 1 條脊椎**（P2–P3；P1 已讓歧義可見）
- [ ] 既有測試全程綠，**一次都不准紅著過夜**（測試數只准增加；當下的值以 `npm test` 為準，不寫在這裡——寫死的數字每交付一次就過期一次）

**阻斷前置（不做會壞事，不是順手做）：**

- [x] **P5 之前**：補存檔版本閘門（2026-08-06，`specs/052-storage-integrity-gate`）

> **實測到的比原本以為的多三個**：原本記的是「`version` 只檢查存在不檢查值」——那講的是**匯入檔案**那條路徑。**自動載入那條（每次開頁面都跑）連存在都不檢查**，`{hello:'world'}` 會被原樣當成存檔回傳。
>
> 而且挖出一個**現在就在傷害使用者**的 bug：`blockStyleId` 與 `locale` 每次存檔都被無聲丟掉——積木外觀與介面語言**從來沒有被保存過**。
>
> 最危險的一環是推理出來、再實測證實的：`load()` 一旦回 `null`，`save()` 拿不到 existing、全部落到預設值、寫回去。**「拒絕載入」在四步之內變成「永久刪除」**——所以加嚴閘門必須連同「先備份再拒絕」一起做，否則這個修復會反過來害人。
>
> 第六條護欄基線 **0／0／0**（欄位不守恆／兩條路徑判定不一致／缺升級路徑）。三個數字都是 0 的護欄，健康與全瞎長得一樣，所以它的健康檢查是**注入已知違規**而不是斷言數字——見 [concepts/執行機構.md](concepts/執行機構.md)。
- [x] **P2 動 `strategies.ts` 之前**：先消掉它與 `core/lifters/expressions.ts` 的 `any`（2026-08-06，12 → 0）
- [x] **P5 之前（一）**：`BlockSpec.concept` → `conceptMapping`（2026-08-06）。`concept` 原本同時長在三個型別上，腳本分不出來——那是第一次改名翻車的直接原因。現在剩兩個，且**剩下的 `LiftPattern.concept` 是物件值**，與 `SemanticNode.concept`（字串）可用機械判準區分。
- [ ] **P5 之前（二）**：改名要用**型別層級的 codemod**（ts-morph 之類），不是正規表示式。實測「值是物件才不改」這條判準**不夠**——`LiftPattern` 與 `BlockSpec` 都是物件值，兩次都誤改。真正夠用的判準是「這個 `.concept` 的接收者是什麼型別」，而那只有 AST 分析答得出來。
  > 2026-08-06 試過正規表示式，翻車、已回退。除了改錯欄位，腳本還**刪掉一整段執行器**、把一個**刻意擺在建構式最後**的區塊移到中間。見 experience「同一個欄位名長在三個不同型別上時」

> 換掉的過程發現：**那些 `any` 藏住的是「`AstNode` 這個抽象不完整」**——lifters 需要 `parent` 與 `startIndex`，介面裡沒有。補進去之後 12 個全部消失。**`any` 常常不是「型別難寫」，是「抽象漏了東西」。**

> **每一項加嚴之前先回答一個問題**（052 學到的，見 [history/017](history/017-加嚴一個檢查可能比不檢查更糟.md)）：
> **「這道檢查拒絕的東西，去哪了？」** 答不出來的話，加嚴會把「不知道有問題」變成「資料被刪掉」。
>
> | 項目 | 拒絕誰 | 去哪了 |
> |---|---|---|
> | P2–P3 消歧義 | 搶匹配的辨識規則 | ❓ 那段語法降級成 `raw_code`，還是整個檔案打不開 |
> | P4 殺雙重真相 | 與註冊表不一致的積木定義 | ❓ 使用者存檔裡的那顆積木 |
> | P5 核心型別改名 | 舊格式的存檔 | ✅ 已由 052 的備份機制接住 |
> | P7 kernel 約束脊椎 | 違反約束的語義樹 | ❓ 使用者正在編輯的程式 |

**Out of scope：** 硬體域併入、Python、圖解形態、語義診斷系統——全部等這階段收斂。

> **硬體域併入（ArduinoCAD）的六步全部 gate 在 F。** 併入計畫逐字：
> 「**膠囊重構的『就近性』必須在硬體元件進來之前做完**」——理由是「AI 元件管線把碎裂
> 模板一起規模化」，直接灌 23 顆零件進來會繼承碎裂模板，之後要清兩次。
> 形狀與順序見 [draft/2026-08-05-硬體域併入計畫](draft/2026-08-05-硬體域併入計畫.md)（未承諾排期）。

### 階段 7：Python 語言套件

- [ ] 完成

> **前置改為階段 6.5。** 角色也隨之改變：Python 不再是「發現架構是不是語言無關」的探針（答案已知：不是），而是**驗證階段 6.5 修好的架構**，並回答膠囊格式唯一需要第二語言的問題——universal 元件是一顆膠囊還是每語言一顆（見 draft/2026-08-05-元件膠囊重構.md）。

<!--
  交付：7.1 語言骨架（manifest + core + stdlib + tree-sitter-python）
       7.2 Python DependencyResolver（concept → import 映射）
       7.3 Python ProgramScaffold（imports + if __name__ == "__main__"）
       7.4 Python 積木投射（核心積木達 C++ L0 等效）
  前置條件：階段 5 + 階段 6
  目的：用第二個語言驗證架構的語言無關性——這是四項獨立性中「語言獨立性」的真正檢驗
-->

**成功標準：**

- [ ] Python code ↔ blocks roundtrip 成功
- [ ] C++ 既有測試不受影響
- [ ] 語言切換時 toolbox 自動更新

### 階段 8：外部套件生態

- [ ] 完成

<!--
  交付：8.1 外部套件載入（manifest 依賴鏈 + 概念生命週期降級）
       8.2 套件安裝狀態 UI（ghost line 安裝指示 + 一鍵安裝）
       8.3 Arduino 語言套件（基於 C++ 擴充 + 硬體描述層 + Servo/NeoPixel）
  前置條件：階段 7
-->

**成功標準：**

- [ ] 安裝/移除套件後概念自動出現/降級
- [ ] 依賴鏈正確解析

### 階段 9+：進階擴充

- [ ] 完成

<!--
  交付：9.1 DataFlow 視圖（消費 control_flow annotations）
       9.2 SemanticDiff 增量更新（前置：階段 5b nodeId 穩定性）
       9.3 接線視圖 + 模擬視圖（硬體教育）
       9.4 跨語言映射視圖（abstractConcept 驅動）
       9.5 語義套件市場
  前置條件：階段 8
-->

**成功標準：**

- [ ] 唯讀視圖可從 annotations 自動生成，不需改語言套件
- [ ] style 變更的語義 diff 為零

## 關鍵延伸（主題觸發必讀）

| 觸發關鍵字 | MUST 讀 |
|---|---|
| 新增語言、語言套件、manifest、依賴解析 | `concepts/開放擴充.md` |
| Topic、認知層級、toolbox、scaffold | `concepts/漸進揭露.md` |
| 新視圖、viewType、可逆性、唯讀視圖 | `concepts/投影.md` |
| 元件、接點、埠、關係律、命名、跨域 | `concepts/元件.md` |
| 殼、規範沒有檢查、護欄、量測工具 | `concepts/執行機構.md` |
| 語義診斷、錯誤訊息投影 | `draft/2026-08-05-語義診斷系統.md` |
| 圖解、記憶體視覺化、資料結構圖鑑 | `draft/2026-08-05-圖解形態與資料結構圖鑑.md` |
| 硬體、Arduino、接線視圖、2D/3D 組裝 | `draft/2026-08-05-硬體域併入計畫.md` |
| 待解工程問題、效能、版本控制、沙箱 | `draft/2026-03-11-已知工程待解問題.md` |
