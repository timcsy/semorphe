# 願景

## 問題陳述

程式教育有一個根本鴻溝：**理解程式邏輯**和**掌握語法細節**之間的斷層。

傳統工具走兩個極端——Scratch/Blockly 讓學生拼積木但永遠碰不到真正的程式碼；文字教學讓初學者同時面對邏輯和語法的雙重認知負荷。兩者之間缺少一座橋。

**目標使用者：**

- **學生**（主要）：從零基礎到進階，涵蓋 APCS 考試準備、競程訓練、學校課程
- **教師**：設計教學路徑（Topic）、指定練習、追蹤學習軌跡
- **語言開發者**：透過模組化語言套件擴充新程式語言
- **課程設計者**：透過 Topic 系統定制不同教學情境的元件可見性和積木形態

## 核心想法

**程式碼和積木都不是本質——它們是同一個語義結構的不同投影。** 學生可以用積木理解邏輯，同時看到對應的真實程式碼，在兩者之間自由切換，最終過渡到純文字編程。

關鍵不是在程式碼上面疊一層積木——**只有語義層存在**，程式碼和積木都是參數化的投影。理論基礎見 [原則](principles.md)。

**和現有工具的根本差異：**

| 工具 | 做法 | Semorphe 的不同 |
|------|------|----------------|
| Blockly/Scratch | 積木 → 程式碼（單向，不可逆） | 程式碼 ↔ 積木（雙向，完美 round-trip） |
| Hedy | 每個等級有不同語法 | 同一語義樹，不同投影解析度 |
| 傳統 IDE | 以文字為中心，視覺化為附加 | 以語義為中心，文字和視覺都是投影 |
| Unison | AST hash 內容定址 | hash 語法層 vs hash 語義層 |
| LSP/LSIF | 跨檔案符號索引 | 位置導向 vs 語義導向 |

## 專案身份

| | |
|---|---|
| **名稱** | Semorphe（散模費 σημορφή）— σῆμα（語義）+ μορφή（形態） |
| **標語** | 唯一真實，各式投影 |
| **CLI** | `smorph` |
| **Logo** | `<Σ>` |
| **npm** | `semorphe` |
| **VSCode** | `semorphe-vscode` |　🔴 **而它同時是 Arduino IDE**（Theia，已驗證）→ [history/080](history/080-ArduinoIDE吃VSCode擴充而那不是一個平台是一個位置.md) |
| **技術棧** | TypeScript 5.x, Blockly 12.4.1, web-tree-sitter 0.26.6, Monaco Editor, Vite |

## 現狀

程式碼 ↔ 積木雙向 round-trip 成立，語義直譯器可執行。三層解耦（Core / Bus / View）已落地。Topic 系統已完成。

🔴 **不再是單語言**（2026-08-21 更正）。Python 是第二個**能跑**的語言——
自己的文法、元件、課程、執行器、互動輸入，瀏覽器實測過（specs 167–173，
[118](history/118-文法不是語言而歸屬要用宣告的.md)–[123](history/123-一筆判定把半個故事當成全部.md)）。

> ⚠️ 這一句在 Python 落地之後**還說了兩天「C++ 單語言可用」**，
> 而整段「現狀」一次都沒提到 Python。判官 2026-08-21 抓到。
>
> **一份說明現狀的文字，是這個專案裡唯一沒有任何測試會紅的東西。**

而**第二個語言的價值不在多一個語言**：它是第一件能證偽「語言無關」那句話的東西。
在只有一個語言的時候，一份宣稱中立的程式碼與一份寫死的程式碼**行為完全相同**
——差別要等第二個實例進來才存在。這也是下一步（流程圖面板逼出 per-panel）
對面板層要做的同一件事。

> **語言無關性仍然是破的，而破的位置換了**（2026-08-12 重寫）。
>
> | P9 的四條 | 2026-08-06 的說法 | 今天 |
> |---|---|---|
> | 核心層硬編語言專屬身分 | **主要症狀** | ✅ **歸零**——而**證據要三個數字**，見下 |
> | 碎裂（一個元件散在太多檔） | 平均擴散 8.5 檔 | ✅ **歸零**（177/177 膠囊化） |
> | 殼（路徑存在但空的） | 🈳92 ／ ❌5 | ✅ **歸零** |
> | **視圖層 import 語言套件** | 沒有人在看 | ✅ **歸零**（spec `153`）→ [104](history/104-語言耦合先變成看得見的再清它.md) |
> | **執行器直接持有面板** | 沒有人在看 | ❌ **現在在這裡**（未清的債；數字看基線） |
>
> 🔴 **而「核心層歸零」這一列，2026-08-20 之前是【對的結論配錯的證據】。**
>
> 它原本寫著「中立性護欄 `total: 0`」——**一個數字**。而那條護欄後來被量出**三個維度**：
>
> ```
> ① 元件身分字面（cpp:print）   一直都有         0
> ② 積木型別字面（cpp_print）   spec 153 才加     核心當時有 11 筆 → spec 154 歸零
> ③ import                     spec 155 才加     核心當時有 1 筆  → spec 155 歸零
> ```
>
> > **這一列自己就是「一條護欄的維度是被發現的」那條教訓的受害者
> > ——它拿一個數字，去證明一個三維的性質。**
>
> 轉折見 [104](history/104-語言耦合先變成看得見的再清它.md)／[105](history/105-核心層的積木型別歸零而佔位值差點變成新的靜默降級.md)／[106](history/106-護欄的維度是被發現的不是被設計的.md)。
>
> **前三條是階段 6.5 清掉的；第四條是階段 7 的第 0 刀清掉的；
> 第五條不是新長出來的，是一直都在而沒有人量。**
> 它們被看見的原因是[第三十九條護欄](../tests/integration/audit-four-independences.test.ts)
> ——P9 自稱「每個 Phase 完成後必須通過」，而在那之前 **31 條護欄裡零檢查**。
>
> > **一個病從「主要症狀」變成「已歸零」時，要問的不是「好了嗎」，
> > 是「它搬到哪裡去了」。**
>
> **當下的實測值一律以 `tests/baselines/four-independences.json` 為準，不寫在這裡**——寫死的數字每交付一次就過期一次（這已經是第三次了）。
> ⚠️ 而 2026-08-18 的健檢抓到**這一段自己犯過同一個錯**：它寫著「不寫在這裡」，
> 卻在上表填了 `5 筆`／`73 處`，而基線當時已經是 67。**已拿掉。**這裡只說**問題的形狀**，數字歸護欄。
> 🔴 **2026-08-21 又犯了一次（第四次），而這一次是判官在修前一次的殘留時當場犯的**：
> 判官報「表上寫 67、基線是 68」，於是我把它改成「2026-08-21 是 68」——
> **把一個過期的數字換成一個還沒過期的數字**。
>
> > **一個「不要寫數字」的規定，最常見的違反方式是【去更新那個數字】
> > ——因為那看起來像在修它。**
> 這句話從「尚未驗證」改成「已知是破的」的轉折，以及階段 7 角色隨之改變的理由，見 [history/015](history/015-語言無關性從尚未驗證到已知是破的.md)。
> 而「破的位置換了」這一次改寫的依據，見 [history/051](history/051-P9從沒有執行機構到病換了位置.md)。

**已完成元件：**

- **Core**：SemanticNode 型別、ComponentRegistry、BlockSpecRegistry、PatternLifter、PatternRenderer/Extractor、CodeGenerator、TemplateGenerator、BlockRenderer、Interpreter、Topic 系統、Storage、DependencyResolver（語言無關介面）、ProgramScaffold、CodeMapping + BlockMapping（nodeId-based 跨投影查詢）
- **C++ 語言套件**：core components/blocks/generators/lifters、std modules（iostream/cstdio/vector/algorithm/string/map/stack/queue/set/cstring/cmath）、ModuleRegistry、auto-include、lift-patterns、render strategies、style presets（apcs/competitive/google）、style-exceptions、manifest
- **UI**：Blockly/Monaco/Console/Variable 面板、Debug 工具列、SyncController、ToolboxBuilder、BlockRegistrar、AppShell、ExecutionController、Ghost Line
- **宿主層**：`CodeView`／`HostProfile` 兩個角色（網頁版注入編輯器面板，擴充注入交給 IDE 的空殼）、
  `src/vscode/`（VSCode ／ Arduino IDE 擴充：面板、範圍編輯、回音守衛、鏡像對帳、視圖狀態）

**已知差距：**

| 缺失項目 | 優先級 | 說明 |
|----------|--------|------|
| SemanticDiff 增量更新 | P2 | 全量替換 → 增量（Phase 9.2，nodeId 穩定性已部分完成） |
| 硬體描述層 | P3 | 等 Arduino 需求（Phase 8） |
| DataFlow 視圖 | P3 | 需 annotations 機制（Phase 9.1） |

## 架構

```
宿主環境（瀏覽器 / VSCode / Electron）
  ↕ ViewHost 介面
多個獨立視圖（積木 / 程式碼 / 主控台 / 變數 / 資料流 / 接線 / 模擬）
  ↕ SemanticBus（瀏覽器=EventEmitter / VSCode=postMessage）
SemanticCore（語義樹 + 元件註冊 + 投影引擎 + 直譯器，零 DOM 依賴）
  ↕ 語言套件介面
多個語言套件（C++ / Python / Arduino）+ 外部擴充套件
```

### 三層契約

- **Layer 1（語義宣告）**：components.json + lift-patterns + lifters — 所有消費者可用
- **Layer 2（投影提示）**：blockDef / renderMapping / codeTemplate / generator — 僅對應視圖消費
- **Layer 3（視圖策略）**：視圖根據 annotations 決定呈現 — 新增唯讀視圖不需改語言套件

### 語言套件目錄結構

```
languages/{lang}/
  ├─ manifest.json          ← 套件中繼資料
  ├─ core/                  ← 語言核心元件（components.json / blocks.json / generators/ / lifters/）
  ├─ std/                   ← 標準函式庫（目錄名即 header 名）
  ├─ lift-patterns.json     ← AST → 語義 pattern 規則
  └─ auto-include.ts / style-exceptions.ts / renderers/ / styles/
```

### 尚未實作但已有設計的規格

- **語義標註（Annotations）**：語言套件與視圖套件之間的開放契約（`control_flow`、`body_execution`、`introduces_scope`、`side_effects`、`hardware_binding` 等）。缺少 annotation 時用 generic fallback，不報錯——任何語言 × 視圖組合都能工作。
- **視圖套件規格**：按與語義樹的互動方式分類（可編輯 / 唯讀-執行 / 唯讀-分析 / 唯讀-硬體），結構為 `views/{name}/manifest.json` + `src/view.ts`⟨規劃⟩（實作 ViewHost）。
- **WebView 隔離模型**：瀏覽器每視圖 = `<div>`/`<iframe>` + EventEmitter；VSCode 中 blocks = WebviewPanel、code = 原生 TextEditor、console = Terminal + postMessage。**Core 跑在 Extension Host，不跑在 WebView。**
  > 🔴 **而這一條有兩個 2026-08-16 才被量出來的前提**（[history/069](history/069-vscode原型退休而它的兩個教訓被撈出來.md)）：
  > ① 膠囊登錄表用 `import.meta.glob`（**Vite 的轉換**），所以**那個宿主也必須用 Vite 建置**（已實測可行）；
  > ② `src/ui/app.ts` 有**六個單例**寫死了「只有一個文件」，而編輯器有 N 個
  > ——**那是這條路真正的成本，而它不在任何介面上**。
  > ⚠️ 而 `code = 原生 TextEditor` 有一個順帶的好處：**宿主的診斷本來就在**
  > （見 [draft/語義診斷系統](draft/2026-08-05-語義診斷系統.md) 的委派宿主分工）。
- **外部套件與硬體擴充**：`packages/{name}/` 含 semantics / projections / hardware。依賴規則：擴充可引用基礎套件元件，**不可引用其他擴充**（避免菱形依賴）。

### 長期方向

1. **多語言**：C++ → Python → Java → Go → Rust……同一語義結構，切換語言即切換投影
   🟢 **而「加一個語言」現在有一支 skill**（`add-language`，2026-08-21）——
   Python 那次走了六支 spec，而那條路的清單當時只存在於 `history/118`–`120` 裡。
2. **語義套件市場**：語義契約 + 投影定義 + 多後端執行體。同一元件可有多個執行體（wasm / js / webgpu / remote），自動基準測試 + 自動後端選擇。教學價值：讓學習者直觀感受演算法複雜度
3. **可插拔執行與時空旅行除錯**：執行即投影；直譯器每步產生語義狀態快照，回溯 = 用歷史快照重新投影（viewParams 多一個 `timeStep`）
4. **硬體教育**：Arduino 接線視圖 + 模擬視圖，從軟體延伸到嵌入式
5. **AI 輔助**：LLM 作為語用分析師，在確定性 guardrails 內提供提示投影——建議下一步，但不自動修改語義結構
6. **自舉**：教學路徑本身成為可分享的語義套件；學習數據回饋優化 Topic 層級樹，目標是**結構還原度最大化**而非效能最大化

### 🟡 符合性的清償（宣告了的接點在積木上表達不出來）

- [x] 🟢 **結清（2026-08-19）：12 → 11 → 1 → 0。**

> 🟢 **11 → 1 不是清償來的，是【第四十六條護欄】順帶的**：
> 「投影遺失」逼出八個補插槽的修法，而符合性從相反方向量同一件事，於是一起降。
>
> 🔴 **而最後那 1 筆是【護欄在說謊】，不是系統有缺陷**（2026-08-19 查證）。
> 根因是一個說謊的宣告：`cpp:var_declare` 的 `declarators` 寫著 `'expressions'`，
> **而 lift 產出的是 `cpp:var_declare` 自己**。那一個謊讓**三個消費者各自錯一次**：
>
> ```
> 符合性       合成一顆 literal_number → 策略讀不到 .name → 報「接點不見了」
> 投影遺失      同一個原因 → 歸進「判不出來」
> US2 那支測試  自己餵 cpp:var_ref → 型別也錯，只是【碰巧有 .name】而通過
> ```
>
> 🟢 **而真實路徑一直是好的**：`int a, b, c;` 走完 lift → render → extract，
> 三個名字全部回得來。轉變見 [history/086](history/086-一個說謊的宣告讓三個消費者各自錯一次.md)。

> 🔴 **而 `123` 想再清兩筆「假違規」，判定錯了並已回退**
> （[history/065](history/065-兩條護欄從相反方向量同一件事.md)）：
> `audit-declared-slots` 證明 lift **真的會產生**那兩個接點。
>
> ⚠️ **所以再判「假違規」之前，兩條護欄都要跑**——它們從相反方向量同一件事，
> 而那對關係就是「用宣告刷數字」的機械偵測器。
> **在此之前，11 筆一律當成真的。**

> ⚠️ 使用者撞過這個病一次（`int a[3] = {1,2,3}` 的初始值消失）。
> **這些是同一個機制的其他出口。**
>
> **子機制有三種，修法完全不同**：
> ① JSON 就是唯一真相（便宜，已做過一次 → 有模板）
> ② 有動態插槽而沒宣告對映
> ③ 🔴 積木在 `block-registrar` 命令式產生（**雙重真相**，兩邊都要改）
>
> 🔴 **而根因是同一個**：宣告與形態**分開寫**，而**沒有任何東西強制它們對齊**
> ——`audit-conformance` **偵測而不預防**。
> ⚠️ **預防機制是一個獨立的設計決定**（它會影響每一顆元件的加法），
> 而它今天**還沒有家**——不在任何階段裡。

### 離線可用

- [x] **已完成**（2026-08-16，spec `128`）——執行期外部請求 **4 → 0**，
  第四十五條護欄（`e2e/offline.spec.ts`）守著。
  🔴 而它是委派探針**順手掀出來的**，不是計畫裡的
  ——轉變見 [history/070](history/070-離線可用一直是半真的而它壞得不夠大聲.md)。

## 開發約束（跨階段，不隨階段退場）

> ⚠️ 這些**不是路線圖項目**——沒有完成的一天，所以它們住在這裡而不是某個階段裡。
> （2026-08-14 從階段 6.5 提出來：那條「不准紅著過夜」原本埋在一個**已完成的階段**裡，
> 而 [experience](experience.md)`:1293` 引用它時，它的定義正要隨那個階段被收斂掉。
> **一條被別處引用的規則，不能住在一個會退場的地方。**）

- **既有測試全程綠，一次都不准紅著過夜。** 測試數只准增加。
  ⚠️ **當下的值以 `npm test` 為準，不寫在這裡**——寫死的數字每交付一次就過期一次。
  而它與「先讓測試紅」不衝突：那條講的是不要**放著**，不是不要紅
  （見 `experience` 的「一個十秒能修的紅」）。
- **護欄的基線只准下降。** 上升時要**指名是哪一筆**，不得只改數字。
- **現況數字的唯一來源是 `tests/baselines/`**，不是這份文件。

## 路線圖

> **這一節只回答「下一步在哪」。**
> 🔴 已完成的交付細節**不留在這裡**——它們收斂成一行 ＋ 指向 `history/` 的指標
> （2026-08-18 收斂：路線圖 1032 → 約 200 行）。
> ⚠️ 判準：**這一行說得出下一步嗎？** 說的是「我們完成了什麼、當時長什麼樣」→ 收成指標。

### 🟡 進行中

**階段 7：Python 語言套件**——十八筆已交付，**兩筆還開著**（見下面那一節的
階段 7）。⚠️ 它原本整段坐在「🔜 下一步（**已排序、未開工**）」裡，
而它已經開工兩個星期。

> **一個區段的標題是一句宣稱，而它不會自己去看底下裝了什麼。**

🔴 而「進行中」**空的時候不是壞事**：下一刀從「下一步」那一節挑，
而挑之前先跑 `/knowie-next`——它會對照程式碼驗前置，而那**每一次都改掉了計畫**。

### 🔜 下一步（已排序）

⚠️ 這一節原本叫「已排序，**未開工**」，而階段 7 開工之後那三個字就不對了
——**開工的那一刀留在原位，由上面的「進行中」指過來**，
比整段搬家便宜，也不會讓它的上下文（目的、前置、驗收）跟著漂。

#### 階段 6.11：Arduino 的板子與腳位（第 4 項起）

> 🧾 **已完成的部分收成一行 ＋ 指標**（judge 判準：這一行說得出下一步嗎）。
>
> | 交付 | 結果 | 轉變 |
> |---|---|---|
> | 三個目標提供不同的**函式**（spec `142`） | ESP32 219 顆／Uno・Nano 214 顆 | [091](history/091-能力是一個性狀而不是一份清單.md) |
> | 三個目標提供不同的**常數**（spec `145`） | 腳位上界與常數表由目標提供 | [095](history/095-腳位的界由板子決定而路線圖指著一個不存在的下拉.md) |
> | **八塊板子**，每個值附上游來源（spec `147`） | `maxPin` 升成腳位集合 | [097](history/097-八塊板子而三個錯都是憑印象填的.md) |
> | 消掉 `properties.values` 的雙重真相（spec `144`） | 40 → 0 | [094](history/094-一份宣告是另一份的投影就沒有資格當真相.md) |
> | 下拉跟著板子走（spec `148`） | 八個目標八份名單 | [099](history/099-下拉跟著板子走而瀏覽器照出測試看不到的那一半.md) |
> | 名字的範圍（spec `149`） | 貼上的 `D1` 選得到了 | [100](history/100-名字的範圍在積木上只畫得出一段而錨點掀出第二個缺口.md) |
> | 函式庫標頭與能力跟著板子（spec `150`） | Uno 看不到 WiFi；D1 mini 產出 `ESP8266WiFi.h` | [101](history/101-標頭跟著板子走而產品走的是另一條路徑.md) |
>
> ⚠️ **第 1–3 項**（spec `137`）與**虛擬硬體被推遲**的理由：[077](history/077-虛擬硬體往後推而先做寬度.md)。

- [ ] **板子視圖顯示腳位狀態**，且**它逼出了佈局那一格的需求**
      —— ⚠️ 它會拉進被 [077](history/077-虛擬硬體往後推而先做寬度.md) 推遲的虛擬硬體設計
- [ ] **C／C++ 三個既有目標不得退步**
- [ ] ~~三塊板子的腳位下拉不同~~ 🔴 **實測否決：沒有腳位下拉**
      （`pinMode` 吃 input_value，學生插數字積木）。重開條件是
      「真的做出一個由目標提供選項的下拉」，不是「又想到它了」。
- [ ] **Servo**——⚠️ **量過了而它是政策決定**：三個核心都沒有 `Servo.h`，
      IDE 附的那份支援 AVR／ESP8266，ESP32 要裝第三方 `ESP32Servo`。
      **「核心沒有」不等於「不能用」**——重開條件是**有人決定那條政策**。
- [ ] **標頭的反向不成立**：ESP8266 的產出 round-trip 之後再切回 ESP32，
      `<ESP8266WiFi.h>` 不會換回去。🔴 補「每塊板子列出所有別塊的標頭」不 scale
      ——正解是**一個能力有一個正典標頭**，而那是更大的設計。
- [ ] ⚠️ **已知不一致**：`cpp:pin_constant` 只在**工具箱**那條路上；
      學生**貼上** `pinMode(D1, …)` 得到的是 `cpp:var_ref`。
      剩下那一半是**身分**的決定，不是下拉的決定。

#### 量測機制的洞（2026-08-20，使用者選「A 先做」）

- [x] 🟢 **兩條產出路徑的一致性**（spec `151`）
      → [102](history/102-兩條路徑的護欄第一次跑就抓到一個而我對哪條是死路的判斷是錯的.md)
      三個共用轉換 × 兩條路 ＝ 十支斷言；七種注入逐一驗過會紅。
      🔴 **第一次跑就抓到一個真的缺陷**——而 spec 146 的護欄**有跑 `gcc`** 也沒抓到。
      ⚠️ **沒有**遷移那 946 支測試，也**沒有**刪 legacy 路徑：**兩條路產品都會走**。

#### 🟡 階段 7 的第 0 刀：語言耦合（清償先於加語言）

> 🧾 **三刀已完成**——中立性護欄從**一個數字**長成**三個維度**：
>
> ```
> ① 元件身分字面   0        一直都有
> ② 積木型別字面   33       spec 153 加（44）→ spec 154 清掉核心那 11 筆
> ③ import        0        spec 155 加（1）→ 同刀清掉
> ```
>
> | 交付 | 轉變 |
> |---|---|
> | 視圖／UI 的 5 個 import → 0，而第二維被發現（spec `153`） | [104](history/104-語言耦合先變成看得見的再清它.md) |
> | 核心層那 11 筆歸零（spec `154`） | [105](history/105-核心層的積木型別歸零而佔位值差點變成新的靜默降級.md) |
> | 第三維（import）1 → 0（spec `155`） | [106](history/106-護欄的維度是被發現的不是被設計的.md) |
>
> 🔴 **而三次都是「前一維看不見另一種拼法」**——那條教訓在 `experience`。

- [ ] 🟡 **剩下的 32 筆**：`block-registrar` 裡的命令式 cpp 積木定義
      —— 🎯 **33 → 32：這個數字第一次下降**（spec `162`，2026-08-20）
      → [113](history/113-中立性第二維第一次下降.md)
      ```
      🟢 已退場   cpp_print、python_print（162）
                 cpp_array_at、cpp_continue、cpp_endl、cpp_literal_string（163）
                 cpp_break、cpp_return、cpp_var_ref（164）
                 array_assign、block_comment、comment、loop_count、var_assign、loop_while（165）
                 var_assign_compound（166）
      🔴 還有 10 顆【宣告與命令式說得不一樣】——逐筆印在 audit-block-def-parity 的報表上
      🟢 **而護欄現在有五維**：spec `166` 補上「載入時的狀態」
      （→ [117](history/117-一次靠運氣攔下的迴歸.md)），
      `cpp_raw_code` 因此從「可刪」變成「有差異」——**它原本是靠 tsc 抱怨未用的 import
      才沒被誤刪的**。
      —— 🔴 剩下的四顆（`doc_comment`／`if`／`if_else`／`raw_code`）需要一個
      **「依 extraState 換視覺」的宣告機制**，而它還沒被設計。
      ```
      —— 🛠 **這個操作已 skill 化**：`retire-imperative-block`
      （先比對、判哪一邊對、補齊對的那份、才刪、然後開瀏覽器看）
      —— 🟢 **還法有三種**（spec `164` → [115](history/115-還債的方式是把對的那份補齊.md)）：
      修宣告 ／ 補機制 ／ 兩邊都錯就都修。
      > **不是「刪掉就降」——是「把對的那一份補齊，另一份才變成多餘的」。**
      —— 🎯 **落差第一次被量出來**（spec `163`）→ [114](history/114-兩份定義的落差第一次被量出來.md)
      ⚠️ 那 19 筆**要逐筆判**「哪一邊才是對的」，
      **不准用「把宣告改成跟命令式一樣」刷數字**——每一筆要分開判，兩個方向都有實例。
      —— ⚠️ **一次只還一種形狀**：判準是「這一顆的形狀，宣告表達得完嗎」。
      `cpp_vector_declare` 也宣告了 `dynamicRules`，而它另有型別下拉與名稱欄位
      ——**符合這個形狀不等於只有這個形狀**。
      —— 🔴 **每選入一顆都要開瀏覽器看標籤與欄位**：工具箱快照只比 id 與順序，
      **沒有任何測試在看標籤**。
- [ ] 🟡 **`app.ts` 的語言管線 12 處**（產生器／lifters／鷹架／診斷）
      —— 🟢 **不再是「可見而不入棘輪」**：spec `161` 給了它基線與棘輪
      （`tests/baselines/language-plugin.json`）。
      —— ⚠️ 要它歸零得把整個 bootstrap 搬進**安裝鉤**，而那些呼叫與 `app.ts`
      自己的物件（`this.scaffold`／`this.syncController`／閉包）**深度交織**。

#### 狀態層走 IDE 的管道（2026-08-25，人拍板「把前四刀做完」）

> **判準**：「跟 IDE 結合」不是把我們的面板搬進它的 panel 區
> ——**搬面板只是換了個位置；走管道才拿得到 F8、紅色波浪線、中斷點，
> 以及使用者已經會的每一個快捷鍵。**
>
> 設計：[draft/版面與檔案](draft/2026-08-24-版面與檔案.md) §六之五～六之八

🟢 **①–④ 已交付**（0.10.4）——診斷 → Problems · 流程進 editor 區 ·
行動版四分頁 ＋ QuickPick 設定 · 主控台 → Terminal（退路：編輯器分頁）。
轉變見 [154](history/154-宿主的能力要用做得到嗎問不是用你是誰猜.md)。
預檢實測：面板內控制項 0 · 主控台／變數分頁不存在 · 畫布佔比 100%。

- [ ] ⑤ **變數／逐步 → Debug Adapter**——⚠️ 它是**另一個宿主介面**，
      不是另一個執行器（[[執行器是可插拔的]]）；混掉會長出
      「VSCode 專用的執行器」，違反 P9。
      🟡 在那之前變數住在 `panel` 區、與終端機同一排——**那不是將就**，
      `panel` 本來就是 VSCode 放「執行時看的東西」的地方。

⏳ **等人工**：Theia 的 DAP 支援——自動化驗不到（`ship-extension`：
「更驗不到 Theia 與 Chromium 的差異」）。
🔴 而 Theia 的 `Pseudoterminal` **已經實測過：不支援**，退路是編輯器分頁。


#### 階段 7：Python 語言套件

> 🔄 **2026-08-20 目的改寫**（人拍板）。設計脈絡：
> [draft/通解與特解和小世界模型](draft/2026-08-20-通解與特解和小世界模型.md)（**in-flight**）。
>
> **舊的說法**：「`layer: universal` 是一份還沒被驗證的外延主張
> ——跨語言等價要等第二個語言進來才驗得了。」
>
> 🔴 **而那個問法本身不成立**：通解是**算子**的性質，不是**解**的性質，
> 「某個東西是不是通用的」在固定觀察集之前是**不良定義的**。
> （`layer` 那一格已於 spec `152` 退場。）
>
> **新的目的**：**拿第二個語言去【產生等價的證據】**，
> 順便量出**鄰域半徑在哪裡失效**。

**為什麼是它**：階段 7 開始時，233 顆元件**全是 `cpp:` scope**，
一個等價類的證據都還沒有。
🟢 **2026-08-23：Python 98 顆，而 108 段 AI 會寫的語料【與真的 `python3` 同一個答案】**
——答案不同／執行失敗／來回不同**三軸都是 0**，降級節點 **0**，而**通用桶也歸零了**
（36 → 0：`.format`／`map`／`isinstance`／`.sort`／`.index` 那一批都有了自己的積木）。
🔴 **而把它做完的那一刻才是重點**：拿資訊隔離的出題者寫的十段程式重對一次答案，
照出**九個執行期的錯**（補零的負號、`round` 的型別、多串的 `map`、`filter(None, …)`、
裸的產生器、字典的 `.pop`…）——見 [history/135](history/135-通用桶歸零而它一路照出九個執行期的錯.md)。
🔴 **而那 105 段不是想出來的**：第五十三條護欄拿**文法自己宣告的 122 個形狀**當全集
比對，沒碰到的每一格都要一筆具名判定——它點名的 20 格當天補了六個形狀
（相鄰字串／型別註記／`from import` 與別名／集合與集合推導式／`*` 展開／海象運算子），
剩下 7 格是帶理由的待補（`with`、相對匯入、行接續）。見
[history/134](history/134-投影裝不下的那一格.md)。
通用桶（跑得動而拖不到）43 → 36（剩下的是 `.format`／`map`／`filter`／`set`
與一批單次的字串方法——它們**跑得動**，只是還沒有專屬的積木）。
⚠️ **而「拖得到」與「跑得動」是兩個問題**：內建函式表管跑得動，膠囊管拖得到。

**已交付**——**一行一筆的細節在各自的 `history/`，不在這裡**。三十三筆轉變：

[107](history/107-第一條跨語言的等價邊而契約沒有語言建構中這個狀態.md)·[108](history/108-讀了而不驗而等價升級成行為時抓到三個假綠.md)·[111](history/111-Python的第一顆積木撞出九筆假設.md)·[112](history/112-加一個語言的代價第一次有數字.md)·[118](history/118-文法不是語言而歸屬要用宣告的.md)·[119](history/119-全域單槽是在宣告這個系統只有一個語言.md)·[120](history/120-一份路線圖寫著缺什麼而那個東西在別的目的下被做出來了.md)·[121](history/121-Python的執行期而十七顆執行器第一次被跑過.md)·[122](history/122-切語言之後才發現的三處而它們都是同一句話.md)·[123](history/123-一筆判定把半個故事當成全部.md)·[124](history/124-護欄有了而量測機構自己沒有人量.md)·[125](history/125-跑測試的瓶頸不在直覺的地方.md)·[126](history/126-還沒支援的邊界會在別的東西做出來的那天變成擋路的.md)·[127](history/127-驗收改成跟真的直譯器對答案.md)·[128](history/128-渲得出積木而渲的是一份JSON.md)·[129](history/129-一個插槽比一個最多兩層的欄位多表達了無限層.md)·[130](history/130-一段程式的積木擺得上去嗎.md)·[131](history/131-一個分類裝了一半的積木它就是其餘.md)·[133](history/133-第三層樣本夠不夠.md)·[134](history/134-投影裝不下的那一格.md)·[135](history/135-通用桶歸零而它一路照出九個執行期的錯.md)·[136](history/136-補課的第一輪就抓到三個缺陷.md)·[137](history/137-註解不見了而它底下有四個洞.md)·[138](history/138-註解有了一個看得到的家.md)·[139](history/139-註解變成一顆看得到的積木.md)·[140](history/140-第五十五條而它抓到的第一個是護欄自己.md)·[141](history/141-反白對不上而它是一條靠記得呼叫的契約.md)·[142](history/142-開機不准喊系統錯誤.md)·[143](history/143-程式碼到積木丟失註解而根因是AST的形狀.md)·[144](history/144-cpp_if退場而擋路的是比對器自己.md)·[145](history/145-呼叫積木退場而括號藏在另一個時機裡.md)·[146](history/146-網頁版的程式碼面板也是投影.md)·[147](history/147-函式定義退場而護欄漏掉的那一維是排版.md)

> ⚠️ 這裡原本是二十列交付表 ＋ 一整批打勾的驗收（占「下一步」的 44%）。
> **判準：這一行說的是「往哪裡走」，還是「我們做完了什麼」？** 後者收斂成指標。

🎯 **而「加一個語言的代價」那個數字仍然活著**，因為下面那一筆待辦靠它驗收：

```
選單接線（topic/target/style/分類/解析器）   🟢 硬性零
語言管線（產生器/lifters/鷹架/診斷）          🟡 12，棘輪 → 下面那一格
```

- [ ] 🎯 **那 6 筆命令式積木——而剩下的每一顆都缺一個【還不存在的機制】**

      🟢 **`cpp_if`（10 → 9）· `cpp_func_call` ×2（9 → 7）· `cpp_func_def`（7 → 6）已退場**（2026-08-24）
      🟢 **`cpp_doc_comment`（6 → 5）已退場**（2026-08-25）——而**那一行「要齒輪驅動的欄位列」是對的**：
      我一開始判成「只是改宣告」，比對報表當場否證。補了兩格機制
      （`plusMinus: false` 只有齒輪、`blockOptions[].fields` 用完才建），
      🔴 而**「看不見」與「不存在」在使用者眼裡一樣，在比對器眼裡不一樣**。
      ⚠️ 順帶抓到兩個：那四個標籤鍵**從來不存在**（英文模式下說中文），
      以及刪掉一個「沒有被使用」的 import **把 `field_multilinetext` 取消註冊了**
      ——積木上少一個欄位而測試全綠（`experience` 有這條）。
      🟢 **`cpp_increment`（5 → 4）已退場**（2026-08-25）——機制是新增的 `altLayout`
      （**依 `extraState` 換一整份佈局**；不是「可有可無的一格」，因為欄位會換到別的插槽上）。
      🔴 **而做它時發現 `cpp_var_assign_compound` 在 spec 166 退場時就壞了**：
      那顆的宣告裡沒有 `INDEX`，`a[i] += 2` 的索引安靜地掉了五天。
      一併修好，並蓋第七十一條護欄 → [156](history/156-第五維證明的是鍵對得上不是那一格會出現.md)
      ⚠️ **剩下四顆各自缺一個機制**（2026-08-24 逐顆量過）：
      `cpp_input` 是「多模式的插槽」· 兩顆格式化 I/O 與 `cpp_raw_code` 見下方原註。
      → [147](history/147-函式定義退場而護欄漏掉的那一維是排版.md)→ [144](history/144-cpp_if退場而擋路的是比對器自己.md)
      ——而那一刀真正的收穫是**比對器補上了三個建構子**：在那之前它把
      每一顆用建構子的積木都報成「宣告表達不出」。

      🔴 **2026-08-21 的兩次修正**（同一天寫、同一天過期，見 [120](history/120-一份路線圖寫著缺什麼而那個東西在別的目的下被做出來了.md)）：

      ```
      第一版說   「需要可變參數 ＋ 前置欄位」            → 不夠精確
      第二版說   「真正缺的是【前置的啞輸入】」          → 隔天就被做出來了
      今天       🟢 三個宣告式建構子都在，而它們是【為了 Python 的 mutation】做的
      ```

      | 建構子 | 加減的是 | 它解掉誰 |
      |---|---|---|
      | `variadic`（＋`HEAD`／`minCount`） | 值插槽 | `cpp_func_call` ×2 · `cpp_input` |
      | `paramList` | 欄位組 | `cpp_func_def` · `cpp_doc_comment` |
      | `branchList` | 成對插槽 ＋ 可有可無的尾巴 | `cpp_if` |

      > **一份路線圖項目寫著「缺的是 X」，而 X 在別的目的下被做出來了
      > ——那條就不再是待辦，是誤導。**

      **所以這一刀現在是「改宣告」而不是「補機制」**，而它有兩個已知的細節：
      ① 啞輸入的名字要對得上（`HEAD` vs 命令式的 `LABEL`／`HEADER`）——建構子要能指定
      ② `cpp_raw_code` 與兩顆格式化 I/O 仍在「宣告表達不出」那一族（各自的理由不同）

      驗收：`block-def-parity` 的 `differ` 下降，而**判準是「比對護欄說它們一模一樣」**，
      不是「看起來很像」。⚠️ 反向：那批退場之後 `dual-truth` 與
      `neutrality.blockTypes` 要一起降。
      ⚠️ 而**每退一顆都要開瀏覽器截圖**——`length` 答得出「有幾個」，答不出「長對了沒」。

- [x] 🟢 **網頁版的空行**（2026-08-24 完成）→ [146](history/146-網頁版的程式碼面板也是投影.md)
      使用者拍板逐字：「**網頁版的程式碼面板也是投影的產物，與 VSCode 的面板應該行為一致**」
      ——**兩個判斷合起來才是答案**：是投影決定它屬於誰，行為一致決定它怎麼做。
      ⚠️ 推論：空行**不進積木**，所以使用者在積木那側加不出空行——那是決定的後果不是缺陷。
      🪦 原本的判準句留在 [146](history/146-網頁版的程式碼面板也是投影.md)：
      （同一個問題的另一個實例：[面板協定](draft/2026-08-20-面板協定的三個頻道.md) §二的接線圖擺放位置。）

#### 🎯 左值是接點，不是字串（2026-08-25 升格，使用者拍板「做完關於 lvalue 的部分」）

> 使用者 2026-08-24 逐字：「**我的意思是 lvalue 的型態應該百百種吧，這樣不就寫死了？**」
> 隔天 8/25：「我希望用『**的**』來做到 `.` 的功能……有時候我們要串好幾個 `.`」
> ——**兩句是同一件事**：`.` 是二元運算子，左值是運算式；用字串就永遠要列舉。
>
> 設計脈絡：[draft/lvalue的形狀被列舉了](draft/2026-08-25-lvalue的形狀被列舉了.md)（**in-flight**）

**範圍是扣除式的，不是列舉式的**（`experience.md:603`「這個分派是列舉式的還是扣除式的？」）：

> **一顆會【寫入】的元件，它的左值必須是接點。**
> 「其餘」＝ 沒有左值的元件——那是定義得出來的集合。

實測的全集（2026-08-25，**15 顆**——第一版寫 13，漏掉 `cin >> x` 那兩顆）：

```
接點 ✅          3   python: container_assign · member_assign · var_assign_sequence
字串 ＋ 下拉 🔴   5   cpp: array_assign · increment · var_assign · var_assign_compound
                    python: var_assign_compound
                    ↑ 下拉列的是變數清單，點一下就毀掉成員存取
字串 ＋ 文字框 🟡  7   cpp: array_2d_assign · map_assign · pointer_assign · input · input_line
                    python: var_assign · var_assign_expr
```

⚠️ **`cin >> x` 的 `x` 也是左值**（`cin >> a[i]`、`cin >> o.f` 都合法）
——第一版用「元件名字含 assign」去想，就漏掉了它們。**那正是列舉式的症狀。**

🔴 **而執行面今天是列舉式的**：`member_assign` 與 `container_assign` **各自手拆**
target 節點的形狀。每多一種左值形狀就要多一個執行器分支。
→ 要一個**左值解析器**（給一顆 target 節點，回傳「可讀可寫的那一格」），
按 `componentId` 分派、可登記——那才是 P3 說的「不修改既有程式碼」。

- [x] 🟢 **第七十三條護欄已蓋**（`audit-lvalue-slot`，2026-08-25）——棘輪 **12 → 0**
      機制是 `traits.writesTo`（宣告驅動，不是用名字認）；第一次跑**紅的**，逐項指名 12 顆。
- [ ] 🔴 **驗收①：那 12 筆還到 0**——🎯 **今天 12 → 10**
      🟢 `python:var_assign_compound`（`a.b += 1`／`nums[i+1] += 1`／`grid[1][2] += 1`）
      🟢 `cpp:var_assign_compound`（`a[i]`／`o.x`／`p->x`／`*q`／`a[i][j]`／`s[i]` 六種形狀）
      🪦 而 `altLayout` **隨第二筆退場**——它是這個病的症狀（驗收④先到了）
      ⚠️ 剩下 10 筆裡，`python:var_assign`／`var_assign_expr` 的左邊被 `constraints`
      限成 `identifier`，**依建構就是原子**——換它們是齊一性不是修缺陷，
      而真正的解是**把三顆合併成一顆**（`member_assign`／`container_assign` 的
      存在理由就是左值的形狀）。🔴 **那是元件身分的決定，要人拍板。**
      ⚠️ 走**棘輪不走硬性零**——build-guardrail §6.8 的第二問「修一筆要付多少？」：
      每一筆跨 `lift`／`generate`／`execute`／`render` 四條路徑，**規範成立 ＋ 每一筆都要驗行為 → 棘輪**
- [x] 🟢 **驗收②：左值解析器是扣除式的**——`core/component/lvalue-nodes.ts` 的登記
      從三個寫死的 `kind` 換成**一個解析函式**，每顆左值節點在自己的膠囊裡宣告。
      🔴 **證據**：`cpp-lvalue-slot.test.ts` 第一次跑時 `a[i][j]` 與 `s[i]` 都紅
      （「這個東西不能被指定值」），而修法是各加一個 `declareLvalue`
      ——**沒有動任何一支賦值執行器**。
- [ ] 🔴 **驗收③：一次性轉換**（`principles.md:158` 逐字：「這種變更 MUST 附**一次性的轉換**」）
      舊存檔的 `name: "o.x"` 載入後變成巢狀節點，而**同一份檔案載入前後產出的程式碼一字不變**
      —— 🛠 **用 `migrate-storage`**，不要現場想
      🟢 **做法定了（2026-08-25）**：`blocklyState` 是**帶失效條件的快取**（v11），
      所以形狀變了就**丟掉快取**讓它從程式碼重 lift——不必在遷移裡寫一個 parser。
      `migrations/block-shape-changes.ts`；v12（Python）· v13（C++）。
      🔴 **開瀏覽器用真的 v11 存檔驗過**，並證實了它是承重的（不丟的話左值變成空插槽）。
- [x] 🟢 **驗收④：`altLayout` 已退場**（2026-08-25，隨 `cpp:var_assign_compound` 那一刀）
- [ ] ~~驗收④~~ 原文留存：**`altLayout` 退場**——它是這個病的症狀（draft §四），
      左值變接點之後 `cpp_increment`／`cpp_var_assign_compound` 不再需要「依 extraState 換佈局」
- [ ] ⚠️ **不在本項**：型別那 15 個字串屬性（乙族）——draft §八逐字
      「**同樣是『字串裝著文法』，會出事的是那個【被投影成選單】的**」，
      而型別那格今天沒有這個問題（`allowCustom` 已解掉可寫性）
- [ ] ⚠️ **不在本項**：`range_*` 那 13 個原子 0 的屬性、`cpp:var_declare.name = "f()"`（未查）

🔴 **而護欄看不到起點那一顆**：`cpp:var_assign_compound.name` 與
`python:var_assign_compound.name` **不在第七十二條的 35 筆裡**——語料裡沒有 `o.x += 1`。
那正是它自己的基線寫著的：「**語料乾淨不代表模型對：它代表語料是照著模型長的**」。

#### 階段 8：外部套件生態

- [ ] 安裝／移除套件後元件自動出現／降級
- [ ] 依賴鏈正確解析

設計脈絡：[draft/套件積木的粒度與預組](draft/2026-08-17-套件積木的粒度與預組.md)、
[draft/套件的物件在執行期是什麼](draft/2026-08-17-套件的物件在執行期是什麼.md)。

- [ ] **核心可獨立出貨**（2026-08-24 升格，使用者拍板）
      —— ⚠️ 這是階段 8 的**反方向**：8 是「別人的套件裝進我們」，這一項是「**我們被別人裝**」。
      使用者的四個情境裡最重要的是第四個：
      「本身就有 Blockly 或是 Flow 了，**但是想要加入程式碼即時互轉功能**」
      ——**那個情境一個面板都不需要**，它要的是引擎 ＋ 自己實作 `ViewHost`。
      設計見 [draft/可嵌入與可插拔的四個軸](draft/2026-08-24-可嵌入與可插拔的四個軸.md)。
      - [x] 🔴 **驗收：`examples/bring-your-own-view/` 在非 Vite 專案編譯並跑通即時互轉**
            —— **2026-08-24 交付**（第五十八條護欄 `audit-portable-core`，硬性零）
            🔴 **而需求被推導錯了一格**：不是「核心要戒掉 `import.meta.glob`」，
            是「出貨打包好的產物」——`vite build --lib` → `dist-sdk/semorphe.mjs`，
            **核心一行沒改**。見 [history/149](history/149-核心第一次在別人的建置工具下跑起來.md)。
            🟢 順手修掉的架構債：**語言套件不再 import 視圖層**
            （`languages/<lang>/pack.ts` → `ui/dynamic-dropdown-field` → `blockly` → `jsdom`）
            ——拆成 `core/dropdown-sources.ts`（登記處）＋ `core/messages.ts`（訊息埠）
      - [ ] **組裝要收成一個入口，或者漏了要能出聲**（例子照出來的 ③④）
            膠囊的 lift 策略、產生器（`pack.install()`）今天都要消費者自己登記，
            而**漏掉的症狀是降級不是錯誤**：`if` 變 `unresolved`、產出變 `⟨unknown component⟩`
      - [ ] **登錄表實例化**（`view-registry.ts:52`／`skip-declarations.ts:22,73`／
            `component/registry.ts:63` 四處模組全域）——教學網站**一頁多題**才逼得出來
      - [ ] 面板只 import 協定——`ViewConfig` 長出 `t` / `appearance` / `schema` 三個 port，
            **`flow-panel` 是第一個實例**（今天它 import 了 Blockly、`BlockSpecRegistry`、`core/traits`）
      - [ ] 版面登錄表（`viewId → slot`）：面板宣告**偏好**不宣告位置；宿主各自渲染區域樹
      - [ ] topic 子集變成公開 API（🟢 機制已存在，缺的是 API 與文件）
      - [ ] 🔴 **P8「不做向後相容」的範圍要重畫**：`ViewHost` 協定與 `componentId` 詞彙
            一旦對外就是公開介面（[history/026](history/026-P8-的範圍不含語義詞彙本身.md) 釐清過內部範圍）
      - [x] 🟢 **「邊要不要進真相」明確留給 v2**（2026-08-24 決定）
            理由：**導得出來的邊回答不了那個問題**，而接線圖還沒有——出貨前答等於
            在沒有消費者的情況下決定一個很貴的型別。
            配套兩件（缺一不可）：**`dist-sdk` 從 v0 開始且 README 寫明會有破壞性改版** ·
            **接線圖進來時必須回答 A／B，不得用 `metadata` 繞過去**。
            見 [draft/語義樹只有樹沒有邊 §六](draft/2026-08-20-語義樹只有樹沒有邊.md)

- [ ] **執行器可插拔**（2026-08-24 升格，使用者拍板）
      —— 使用者：「執行的模擬或許也是重要的一塊，這如果能和**外部 Debugger 或是真實環境**
      也有串接的機會就更好了」。設計見 [draft/執行器是可插拔的](draft/2026-08-24-執行器是可插拔的.md)。
      🟢 **契約已經寫好了，只是沒有人替它命名**——匯流排上的 `execution:*` 四個事件就是它，
      而 `concepts/模擬的誠實.md` 早就寫著「執行由**真板子**提供」。
      - [ ] 🔴 **驗收：第二個 runner（Pyodide 或 DAP）接上去，`execution:*` 事件一個都沒改**
            ——提議先做這兩個之一，**不要先做硬體**：它們不需要新的執行模型
      - [ ] Runner 收的是**樹 ＋ 對映**（nodeId 是共同語言），不是程式碼字串
            🟢 前置已備：第五十六條護欄（對應表指到自己那一行，2026-08-24）
      - [ ] `capabilities`：`steppable` / `resettable` / `variables: push|pull` / `timeModel`
            ⚠️ 那是**能力宣告**，不是新的 `ExecutionStatus` 值（`concepts/執行機構.md:279` 的門檻）
      - [ ] `declareSkips` 改成 **per-runner**（「`with` 不執行」是針對**我們這顆**直譯器說的）
      - [ ] 護欄：**同一組語料上兩個執行器的分歧數**，棘輪只准下降
            （沒被量的差異，會以「學生的程式在板子上不動」的形式被發現）
      - [ ] ⚠️ **時間軸（硬體模擬）不在本項**——那是換執行模型，自己一輪設計

- [ ] **執行的誠實：沒看過就不繼續**（2026-08-24 升格，使用者拍板方向）
      —— 使用者逐字：「**如果沒看過的東西就不要執行下去了，要誠實的說沒看過**」，
      三條出口：① 使用者親手改 runtime 狀態 · ② 連線外部 runtime · ③ 停止執行。
      設計見 [draft/執行遇到沒看過的東西](draft/2026-08-24-執行遇到沒看過的東西.md)。
      🔴 **今天的兩個答案裡有一個正是要拿掉的**：`unknownComponentHandler` 回
      `'skip' | 'abort'`，而 `'skip'` 不是「跳過一行」，是**帶著錯的狀態繼續跑**。
      - [ ] 🔴 **驗收：`'skip'` 這個答案從程式碼裡消失**（硬性零，護欄可測）
      - [ ] 驗收：`isSkipped()` 那條**靜默 return** 也要出聲——
            而它出的聲就是那份宣告的理由（`declareSkips` 的 reason 第一次有消費者）
      - [ ] 驗收：這個決定走**匯流排**，`confirm()` 從 `execution-controller` 消失
            （VSCode 與 Node 宿主答不了一個瀏覽器對話框）
      - [ ] 驗收：① 手填的值進「這次執行的輸入」，**同一份輸入重播得到同一個答案**
            （`concepts/模擬的誠實.md` 第一條：可重現 > 擬真）；而它要**看得見**
      - [ ] **執行前先說**：靜態算得出來的那一半（`declareSkips` ＋ 執行器登錄表），
            在按下執行之前就列出「這段我不會跑」
      - [ ] ⚠️ **②b 整段委派不在本項**——那是「執行器可插拔」；本項只到 ②a（單一呼叫）

- [ ] **開放值域的欄位要寫得出來**（2026-08-24 升格，使用者回報 `int**` 選不到）
      —— 設計見 [draft/開放值域的下拉](draft/2026-08-24-開放值域的下拉.md)。
      實測：靜態 `field_dropdown` **62 個**，其中值域**在現實中開放的 29 個**
      （涵蓋 26 顆元件）——`include` 20 選項、`math_unary` 17、`pin_attach` 10…
      🔴 而它**不是** R 分級的問題：可寫性是獨立的第二條軸
      （見 [concepts/投影.md](concepts/投影.md)「可逆性量不到使用者造不造得出來」）。
      - [ ] 🔴 **驗收：清單外的值不得被改掉**（正確性）
            ⚠️ **更正（2026-08-24）**：這裡本來寫「`audit-field-writes` 已經在數」——**它沒有**。
            那條護欄自己逐字說：「⚠️ **這裡的 0 不代表『使用者不會遇到』，只代表『測試沒跑到』**」
            ——又一次「**護欄的零比它名字宣稱的性質窄**」。要新的量測。
      - [x] 🟢 **機制到位 ＋ 使用者回報的那一顆解決了**（2026-08-24）：
            `field_dynamic_dropdown` 收 `options`（靜態）或 `source`（動態）＋ `allowCustom`，
            四個型別下拉打開；**瀏覽器實測打得出 `int**`**，產出 `int** add(int a, int b)`。
            🔴 順帶修掉一個 P9 破口：Blockly 的 `dialog.prompt` 落回 `window.prompt`，
            而**那在 VSCode 的 webview 裡是停用的**（點了沒反應）→ `src/ui/prompt-dialog.ts`。
      - [ ] 其餘開放欄位逐一開放（**第五十九條護欄的棘輪，今天 3**）
            ⚠️ 而那 3 筆都是**命令式定義蓋過宣告**——它會在那三顆積木退場的那天下降
      - [ ] 驗收：選項跟著程式長——用過的型別會出現在其他積木的下拉裡（導出的，不是宣告的）
      - [ ] ⚠️ **型別積木（`int` ＋ `*` ＋ `*`）不在本項**——那屬於「指標與參考 UX」
      - [x] 🟢 **順帶修掉的兩個資料遺失**（2026-08-24）：巢狀 `pointer_declarator`
            只剝一層，於是 `int** p;` 的名字退成預設 `"ptr"`、參數 `int** a` 掉一顆星
            ＋名字空的。三處同根因，一個 `unwrapPointers` 解掉
            （`tests/integration/cpp-multi-pointer.test.ts`，9 條）

#### 階段 9+：進階擴充

```
9.1 DataFlow 視圖（消費 control_flow annotations）  ← 🟢 消費者 2026-08-24 到場，而**來的是流程面板**
9.2 SemanticDiff 增量更新（前置：階段 5b nodeId 穩定性）
9.3 接線視圖 ＋ 模擬視圖（硬體教育）
9.4 跨語言映射視圖（abstractComponent 驅動）
9.5 語義套件市場
```

- [ ] 唯讀視圖可從 annotations 自動生成，不需改語言套件
- [ ] style 變更的語義 diff 為零

> 🔴 **這五行不是裝飾——它們是被引用的**。
> `tests/baselines/annotation-adoption.json` 用 `pending-consumer:9.1 DataFlow 視圖`
> 替一個零讀取點的標註背書，而第 X 條護欄會**逐字**在本檔搜那個字串。
>
> ⚠️ 2026-08-18 的路線圖收斂把這一段刪成三行，護欄當場變紅：
>
> > **一個路線圖項目不只是計畫，它還是別人引用的【指涉】——
> > 收斂它的時候，被引用的那個名字要留下來。**

### 🧾 未清的債（跨階段，不屬於任何一刀）

🔴 **這些不會因為某一刀做完而消失——每一條都要有人領走。**

- [ ] 🆕 **名詞表的其餘各列**（2026-08-20）
      🟢 **命名本身已經掃乾淨**：spec `158` 做了身分鍵那一列，spec `159` 把
      `concept` 家族的**周邊命名**整族退場（3496 處／656 檔 → 0，含 14 個檔名與 39 個匯出符號），
      轉變見 [history/110](history/110-concept家族整族退場.md)。
      🔴 **而 `concepts/元件.md`「現況落差」表的下面五列一列都沒動**
      ——它們不是改名，是**改結構**：
      ```
      三個登錄表的【合併】   ComponentRegistry ／ BlockSpecRegistry ／ component/registry
      Instance             取代 SemanticNode（動的是型別的意義，不只名字）
      params / ParamSpec   型別側只有名字清單，沒有型別、範圍、預設值
      attachments + relation   資訊軸【尚不存在】——那是要加的，不是要改的
      forms                blockDef／codeTemplate／astPattern 分屬三個機制
      ```
      🔴 **每一列都是一刀**，而 `params` 那列**不只是改名**——名詞表自己寫著
      「改名時應連結構一起借過來」。

- [x] 🟢 **`cpp:var_ref` 的下拉只投影了「範圍」的一半**（spec `149`，2026-08-19）
      → [100](history/100-名字的範圍在積木上只畫得出一段而錨點掀出第二個缺口.md)
      貼上 `pinMode(D1, OUTPUT)` → 下拉從「只有一個 `x`」變成
      「`speed`（學生自己的）＋ 板子的 `D0 D1 D2…`」。
      🔄 **這條原本寫成身分問題——查了執行期之後改寫成投影問題**，於是從一場規格辯論
      變成一次小改動。
      🔴 **而 US2 的錨點掀出第二個缺口**：`abstractComponentOf(block.type)` 傳錯了命名空間
      （積木型別 vs 元件身分），**24 顆宣告元件一顆都沒進下拉**，而它不會拋錯。
- [ ] **元件套件管理：契約是「名字 ＋ 版本」**（2026-08-24 升格）
      —— 設計見 [draft/元件套件管理](draft/2026-08-09-元件套件管理.md)。
      🔴 **它連十二輪標熟而未升格**（`draft/目錄結構對硬體的適配 §五` 點名過），
      而 2026-08-24 `dist-sdk` 出貨讓它從「未來式」變成**現在式**：
      一份對外的產物，它的 `componentId` 詞彙與 `ViewHost` 協定就是公開介面。
      - [ ] 驗收：`dist-sdk` 有版本號，而**破壞性改版說得出破壞了什麼**
      - [ ] 驗收：第三方 scope（`@someone:*`）的元件裝得進來、拿得到、認得出
            🟢 命名那一半**已經設計完了**（`concepts/元件.md:138`：scope 是套件擁有者，
            而 `@someone:boost_vector` 宣告一條轉換邊就自動進同一個抽象元件）
      - [ ] ⚠️ **P8 範圍要一起重畫**——與「核心可獨立出貨」那一項的最後一條是同一件事

- [ ] **`execution:at-node`：拆掉那張中央對映表**（2026-08-12 升格）
      —— 設計見 [draft/執行器直接持有五個面板](draft/2026-08-11-執行器直接持有五個面板.md)
- [ ] **網頁版有檔案：主體、side-car、檔案系統埠**（2026-08-24 升格，使用者拍板七題）
      —— 設計見 [draft/版面與檔案](draft/2026-08-24-版面與檔案.md)。
      🔴 **它讓兩個宿主的真相模型統一**：`src/vscode/sync/view-state.ts:5` 逐字
      「網頁版**沒有檔案** ⟹ 積木的擺放【就是】真相」——**那句話的左半邊會作廢**。
      🟢 連帶結清 `面板協定 §八` 的三個未決：主體＝檔案 · ③ 的 key ＝ `檔案 × 面板種類` ·
      B「兩個焦點」＝開兩個檔案。
      - [x] 🟢 **第一刀交付（2026-08-24）：一份存檔拆成四種歸屬，而樹停止儲存**
            `FIELD_OWNERSHIP` 宣告 document／sideCar／user／context／meta（值域封閉）·
            v10 → v11 拿掉 `tree` · `blocklyState` 降格成**帶失效條件的快取**（`codeHash`）·
            **第六十一條護欄**（每個欄位要說得出屬於誰、而且要有讀取點）
            🔴 而它照出兩件事，見 [history/151](history/151-存檔裡的樹沒有人讀而還原路徑會把真相抹掉.md)：
            `tree` 存了 10 個世代**沒有人讀**（8 個升級步驟在改寫它）·
            **「不用快取」差點變成「刪掉使用者的程式」**（還原路徑是從積木產生程式碼）
            🔔 順手把 `migrate-storage` skill 化——**在第 10 次改版之前**
      - [ ] 🔴 **驗收：`FileStore` 埠 ＋ 兩個實作**（記憶體／原生），
            **而 VSCode 那側不實作**（`vscode.workspace.fs` 就是）——宿主獨立性的第三個實例
      - [ ] 驗收：**檔案存原始碼**，佈局存 side-car；**side-car 刪掉 ＝ 自動排版**（不是壞掉）
      - [ ] 驗收：**新檔案可以選語言**（還沒存檔所以沒有副檔名）；存檔時**副檔名跟著語言走**
            ⚠️ 反過來是錯的——`traits.ts:60`「從產出的形狀反推它是什麼的判準，會安靜地做錯事」
      - [ ] 驗收：匯出 zip（原始碼＋side-car）／匯入解壓；**離線可用**（`e2e/offline.spec.ts` 是硬條件）
      - [ ] 驗收：`semorphe-state` 的舊資料**有一次性遷移**（`storage-integrity` 護欄盯著）
      - [ ] ⚠️ **多檔之間的引用**（`#include "my.h"`／`import mymodule`）**不在本項**——
            有了多檔它才變成真問題，而它會碰到 `dependency-resolver`

- [ ] **版面：一套 slot 詞彙，兩個宿主**（2026-08-24 升格）
      —— 設計見 [draft/版面與檔案 §六](draft/2026-08-24-版面與檔案.md)、
      [concepts/理解的層次](concepts/理解的層次.md)。
      🎯 **版面的分界不是「這是哪一類」，是「這東西怎麼被用」**：
      取用要**相鄰**、認識要**面積**、狀態在**面板區**。
      - [ ] 驗收：slot 詞彙落地（`activityBar`／`sideBar`／`editor`／`panel`／`statusBar`），
            面板宣告**偏好**不宣告位置
      - [ ] 驗收：桌機**佈局預設**（專注／對照／三欄）＋ 可拖分隔線；**不做自由 docking**
      - [ ] 驗收：手機四分頁 **程式碼 · 流程 · 積木 · 主控台**（＝理解的四層）＋ **虛擬鍵盤保留**
      - [ ] 🔴 **驗收：同步是【三態】，而入口在狀態列不在面板**（2026-08-25 使用者追問後修正）
            ```
            同步中   來源是導出的（誰最後編輯）
            已暫停   沒有來源——兩邊各自為政
            分岔了   🔴 系統【停下來問】，不自己挑一邊    ← 原本漏掉這一態
            ```
            理由：3 個可編輯視圖 → 6 個方向，方向模型不可行；而**方向是 N²，來源只有 N**。
            🔴 **而暫停暴露了「導出」的洞**：暫停後兩邊都改過，「誰最後編輯」是任意的
            ——**有暫停就一定要有手動來源，而分岔之後系統該問，不該推**。
            - [x] 🟢 **交付（2026-08-25）**：三顆按鈕 → 一顆「⇄ 同步」＋ 狀態列三態；
                  機制住 `core/sync-coordinator.ts`，**兩個宿主各接一個入口**
                  （網頁版自己的狀態列 · VSCode／Theia 的 `StatusBarItem` ＋ 命令面板）。
                  **第六十二條護欄**：方向按鈕硬性零 · 來源清單必須從 `viewsWith('editable')` 導出。
                  🔴 而瀏覽器實測抓到兩件測試看不到的事，見
                  [history/152](history/152-同步從方向變成來源而暫停暴露了第三態.md)
            - [ ] ⏳ **在 Arduino IDE（Theia）裡真的按一次**——已裝 0.9.5，等人工驗
            - [ ] 🔴 **兩個宿主都要**（2026-08-25 使用者反問後更正）：機制住 `core/`，
                  兩邊各接一個入口。⚠️ 我一度以為「VSCode 真相是文件所以不必做」
                  ——**那只在「來源是誰」那一格成立**；暫停（別把排版弄掉）與分岔
                  在那側**更常見**（文件會被 git／別人改）
            - [ ] 入口：**狀態列 ＋ QuickPick ＋ 命令面板**（使用者：全域、**不放在面板裡**）
                  ⚠️ **不是側邊欄 ListView**：它收起來就看不見，而「暫停中必須看得見」是驗收
            - [ ] QuickPick 的來源清單由 **`viewsWith('editable')` 導出**——硬編視圖名 ＝ 0
                  🟢 而那支函式今天**零消費者**（與 `control_flow` 同形：宣告了而沒有人讀）
            - [ ] ⚠️ **「暫停」今天真正在做的是「別把我的排版弄掉」**——那是 5b 缺席的代償。
                  nodeId 穩定之後回來重看：它會**縮小但不會消失**（分岔那一個永遠在）
            - [ ] 🔴 **跨宿主判準**：挑最舊、最多實作的宿主 API（狀態列／命令面板／編輯器分頁）
                  ——這一刀是**把入口從我們自己畫的 chrome 換成宿主都有的 chrome**
            - [ ] 🔴 **收工條件含「在 Arduino IDE（Theia）裡真的按一次」**
                  ——`ship-extension` 逐字：「驗得到我送了什麼，驗不到對面怎麼處理，
                  **更驗不到 Theia 與 Chromium 的差異**」。把入口交給宿主的狀態列，
                  等於交給一個**我們的測試看不到的地方**
      - [ ] 🔴 **驗收：工具箱仍然附著在它服務的視圖上**（不得拆成獨立面板）
            ——使用者實測過：「獨立面板的拖曳效果超爛，後來就放棄獨立面板了」
      - [ ] 驗收：圖鑑在**編輯區**（要面積），而它**不靠拖曳**取用
      - [ ] ⚠️ **活動列＋檔案總管要由 `profile.features` 擋**（P4 漸進揭露）——
            版面由 profile／topic 決定，不是由使用者拖

- [ ] **流程可編輯**（2026-08-24 升格，使用者：「流程圖的部分，我希望**不只是唯讀**」）
      —— 設計見 [draft/版面與檔案 §四](draft/2026-08-24-版面與檔案.md)。
      🎯 **它是 2D 接線視圖的預演**：同一種互動（拉一條線），
      而在流程圖上那條線**還是導得出來的**，所以不必先付「邊要不要進真相」那筆錢。
      - [ ] 驗收：**(b) 改欄位 ·(c) 改接線 ·(d) palette 拉新節點 ·(e) 語句重排** 都做得到，
            而每一種都**來回一趟不走樣**
      - [ ] 🔴 **驗收：(f) 畫一條不是父子關係的線 → 誠實拒絕並說得出理由**，
            **不得塞進 `metadata`**（`語義樹只有樹沒有邊 §六` 的「不得繞過去」）
      - [ ] 驗收：`EditableView` 契約長出來，而**流程面板是它的第二個實作**
            （今天 `extractSemanticTree()` 是 `blockly-panel` 的方法，不是契約）
      - [ ] 驗收：流程的 palette 與積木的工具箱**讀同一份登錄表**（不得各寫一份）

- [ ] **面板協定：第三個頻道（per-panel 狀態）**（2026-08-20 升格）
      —— 設計見 [draft/面板協定的三個頻道](draft/2026-08-20-面板協定的三個頻道.md)
      —— 🟢 **2026-08-24：它的兩個閘門被「網頁版有檔案」那一項結清了**
      （主體＝檔案 · key ＝ `檔案 × 面板種類` · 兩個焦點＝開兩個檔案），
      而 **side-car 就是 ③ 的家**。於是這一項的前置從「先答一個問題」變成「等那一項做完」。
      —— 🟢 **動工前的閘門 2026-08-24 換了問句**（同一份 draft §八，使用者）：
      不是「網頁版的共同真相**住哪**」，而是「**大家現在關注的那個主體是誰**」
      ——檔案只是它在 VSCode 的實例，網頁版有一個、只是沒有名字。
      ⚠️ 它換出**兩個新的未決**，而它們擋的是 ③ 的 key 設計（不擋 ①②）：
      **A** per-panel 狀態跟著主體還是跟著使用者 · **B** 能不能同時有兩個焦點。
      🎯 **順序（使用者拍板 2026-08-20）**：Python 功能寫完 → 流程圖面板逼出 per-panel
      → ArduinoCAD 合流。**每一步剛好買一個貴的決定，而消費者都已到場。**
      ⚠️ ③ **不是從零開始**：`src/vscode/sync/view-state.ts` 已經是它（spec 140 交付），
      而它是 **per-uri，不是 per-panel**，且網頁版沒有對應物。
      —— 🎨 **形式已定調**（2026-08-24，使用者拍板三件事 ＋ 動工當天的第四件）：
      設計見 [draft/流程面板的形式](draft/2026-08-24-流程圖面板的形式.md)
      （**沿用積木的顏色語彙** · **手拖 ＋ 記住 ＋ 一鍵自動排版** · **粒度可選**）。
      🔴 **第四件（同日，使用者更正兩次）**：它是 **flow-based 的節點圖**，
      **不是 flow chart**——所以 tab 叫「流程」、檔案叫 `flow-panel.ts`。
      「`>` 自己是一顆節點」而不是「格子裡的一串字」。見 [history/148](history/148-流程面板是節點圖而不是流程圖.md)。
      ⚠️ 那份 draft 先查了 SOTA：這個領域有**兩套互不相容的視覺語言**，
      而它**沒有寫下選哪一欄**——於是第一版做成了另一欄（experience 有這一條）。
      - [x] 流程面板可獨立開關（底部分頁），佈局由宿主排 —— **2026-08-24 交付**
            🟢 `app.ts` 一行沒改：面板只實作 `ViewHost`，由 `registerViewsIn` 掃到
            🟢 `control_flow` 因此有了**第一個真正的讀取點**（35 顆宣告者）
      - [ ] 使用者拉開的節點佈局，重開後在原位
            ⚠️ **今天是記憶體裡的**（`offsets`，關掉就沒了）——持久化是下一刀
            🔴 **它買到的硬問題**：佈局記在 `nodeId` 上，而**nodeId 穩不穩定**
            （9.2 的前置「階段 5b nodeId 穩定性」）——不穩就對不回去
      - [x] 🔴 **改面板私有狀態之後，語義樹的雜湊不變** —— **第五十七條護欄，2026-08-24**
            （`audit-view-state-isolated`；「先紅」靠**合成注入**——那天還沒有面板私有狀態可以弄髒）
      - [ ] `ViewState` 的 key 從 uri 變成 uri × 面板種類，**且舊 key 有遷移**
      - [ ] 網頁版與擴充兩側都成立
            （⚠️ 網頁版沒有文件，**共同真相住哪要先答**——VSCode 那側是文件，白撿的）
- [ ] **C2 資訊軸（邊）**——⚠️ **延後的理由（「今天沒有消費者」）已經失效三次**。
      第一個真消費者是**語義診斷系統**，不是原本寫的 2D 面板。
      🔴 **第三個消費者性質不同**（2026-08-20）：
      [小世界模型](draft/2026-08-20-通解與特解和小世界模型.md) 要用邊**定義鄰域半徑**——
      ```
      前兩個   要把邊【畫出來】或【讀出來】
      第三個   要用邊算【距離】
      ```
      而它會逼出邊的**型別**：有沒有方向、能不能疊、距離怎麼算
      ——**那三個問題今天一個都沒有答案**，而前兩個消費者不需要回答它們。
      🔴 **而「邊進不進真相」（A／B）要由【第四個】消費者逼**（2026-08-20）：
      ```
      前三個   診斷讀邊 · 小世界算距離 · 流程圖畫邊     全是【導出的】邊
      接線圖   D13 → LED+ 導不出來，只能存             🔴 第一條無法導出的邊
      ```
      > **導得出來的邊，回答不了「邊要不要進真相」。**

      而 break／continue 要靠 `BreakSignal` **丟訊號**才實作得出來
      （`src/interpreter/executors/control-flow.ts:3`），這證明控制流的邊本來就
      **不是 parent-child**——⚠️ 而它們**算得出來，所以不必存**。
      🔄 **這條原本把 2D 面板寫成「原本寫的」**（讀起來像被取代）——查證後更正：
      **2D 面板沒有被取代，它排在最後，負責回答最貴的那個問題。**
- [ ] **`reference` 那一格**（目標的完整設計還缺的最後一格）
      - [x] 🟢 `provides`（spec `142`）· [x] 🟢 `board`（spec `145`）
      - [x] 🟢 **`requires` 缺的那一維**（spec `146`）：I/O 標頭跟著 `io_style` 走
        → [096](history/096-標頭跟著風格走而兩個問題差點被合成一個.md)
        ⚠️ C 目標的產出從**編不過**變成 `gcc -x c` **編得過**
      設計脈絡 [draft/C 和 C++ 難分難捨](draft/2026-08-13-C和C++難分難捨.md)
      （**in-flight**——⚠️ 它的舊標記指著已完成的階段 6.10，**新的消費者是這一格**）。
- [x] 🟡 **`TypeError: … reading 'indexOf'`**（Blockly 載入積木時，只在 Theia 出現）
      —— **修好範圍翻譯之後就不再復現**（2026-08-18 使用者回報）。
      🔴 **而「不再復現」不等於「證明修好」**：它從來沒有在 Chromium 重現過，
      所以沒有一個會紅的檢查在守它。⚠️ 結案的是**追查**，不是**保證**。
      🟢 留著的防線：`isolateFailingBlock`（再發生時直接指出是哪一顆積木 ＋ extraState）、
      `isStateStale`（殘的工作區不得覆蓋程式碼）。

### ✅ 已完成的里程碑（一行一筆 ＋ 指標）

| 階段 | 結果 | 轉變 |
|---|---|---|
| 0–5b 地基與解耦 | Core 零 DOM、面板零 import、跨投影查詢可用 | [069](history/069-vscode原型退休而它的兩個教訓被撈出來.md) |
| 6.7 語法錯誤的辨識層 | 閘門 ＋ 訊息投影 ＋ 修復建議全數關閉；**而 ① 從「待實作」重判為「待論證」**（它拿不到 P6 的背書，論證移入 `draft/語義診斷系統`） | [092](history/092-把構想翻過來的是一次量測.md) |
| 8·控制項離開積木面板 | `HostFeatures` 布林 **5 → 4**；面板內控制項 **→ 0**（工具箱與畫布不變）；三個宿主讀**同一份 `ControlState`** | [153](history/153-控制項離開積木面板而做法是登錄表不是第六格布林.md) |
| 6.13 IDE 擴充（spec 140） | 驗收**七條逐條按過、一條刪除**；同步那一族結清（擴充 0.9.4） | [087](history/087-一段活過重畫的復原歷史.md)·[088](history/088-空行是排版不是真實.md)·[089](history/089-spec-140的驗收清單.md) |
| 6 Topic 系統 | 同一語言在不同 Topic 下有不同層級樹與積木覆蓋（2026-03-11） | — |
| 執行機構自己接上 | CI 真的跑測試；四項獨立性第一次會關；e2e 接上；識別字 618 → 0 | [050](history/050-識別字不再是中文-一條沒有機制的慣例漂了兩個月.md) |
| 目錄結構四件小整理 | `src/blocks/`／`src/views/` 消失，五個檔各歸其位（交付 2/4） | [052](history/052-同名不是重複的證據.md) |
| 6.5 元件膠囊重構 | **加一顆元件 ＝ 新增一個資料夾，零編輯**（177 顆全膠囊化） | [030](history/030-C1參數規格化-證據優先於名字.md)·[031](history/031-D命名空間遷移-身分不只以字串出現.md)·[049](history/049-F完成-把身分換成性狀.md) |
| 6.6 語義診斷系統 | 五條驗收全數達成 | [071](history/071-裁判有了而它看不懂的方言要先排掉.md) |
| 6.8 第一課 | 教材的第一個實例，四條驗收全數兌現（specs `124`／`125`） | [098](history/098-第一課補一筆轉變而它當初撞到的是產品的核心宣稱.md)·[過程](episodes/2026-08-19-第一課走過三遍.md) |
| 6.9 第二課 | 訊息不是代號；第四十四條護欄 | [066](history/066-護欄錨在供給端所以量不到消費端.md) |
| 6.10 目標（`target`） | 選一次而不是三次；C 的產出 6/10 → 10/10 | [072](history/072-target的兩個假設都倒了而它反而證明了target.md)·[074](history/074-機制有了沒人接上的第十一個而這次它有了執行機構.md) |
| 6.12 寬度 | 貼進來的 Arduino 程式碼轉得動；殘差與漂移各說得出數字 | [078](history/078-量完之後階段6-12的前提垮了一半.md)·[079](history/079-補量執行之後下一步又換了一次.md) |
| 6.13 擴充第一刀 | `.vsix` 產得出來，畫布在 VSCode 裡跑得順（16.7 ms） | [080](history/080-ArduinoIDE吃VSCode擴充而那不是一個平台是一個位置.md)·[081](history/081-第一刀畫得出積木而三個坑都不會拋錯.md) |
| 6.14 擴充長成能用的 | 雙向同步／高亮／執行／設定 | [082](history/082-擴充長成能用的而一個缺口只在大家都不那樣寫的地方.md) |
| 6.15 面板就是網頁版本身 | 程式碼那一格交給 IDE；`src/vscode/` 2170 → 1624 | [083](history/083-面板就是網頁版本身而四次修好都是我自己上一次修出來的.md) |
| 6.16 零件積木 | 四批 33 顆；貼上的 LLM 程式碼轉得成專屬積木**而形式一字不差**；四份盲測殘差 0.00% | [084](history/084-零件不是身分是參數而那份draft擋的是執行不是積木.md)·[過程](episodes/2026-08-18-三批零件積木.md) |

⚠️ **階段 6 沒有 `history/` 轉變**（做的時候還沒有這個習慣）
——那一行因此寫得比其他行完整一點，因為沒有指標可以贖回它。
🟢 **6.8 的那一筆已於 2026-08-19 補上**——它是 `draft/教材要如何呈現` 反流時
順帶還的債。**一個階段的 draft 退場，正是補回它缺的轉變的時機。**

## 關鍵延伸（主題觸發必讀）

| 觸發關鍵字 | MUST 讀 |
|---|---|
| 新增語言、語言套件、manifest、依賴解析 | `concepts/開放擴充.md` |
| Topic、認知層級、toolbox、scaffold | `concepts/漸進揭露.md` |
| 新視圖、viewType、可逆性、唯讀視圖 | `concepts/投影.md` |
| 元件、接點、埠、關係律、命名、跨域 | `concepts/元件.md` |
| 殼、規範沒有檢查、護欄、量測工具 | `concepts/執行機構.md` |
| 語義診斷、錯誤訊息投影 | `draft/2026-08-05-語義診斷系統.md` |
| 圖解、記憶體視覺化、資料結構圖鑑 | `draft/2026-08-05-圖解形態與資料結構圖鑑.md` |
| 硬體、Arduino、接線視圖、2D/3D 組裝 | `draft/2026-08-05-硬體域併入計畫.md` |
| 待解工程問題、效能、版本控制、沙箱 | `draft/2026-03-11-已知工程待解問題.md` |
