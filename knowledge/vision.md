# 願景

## 問題陳述

程式教育有一個根本鴻溝：**理解程式邏輯**和**掌握語法細節**之間的斷層。

傳統工具走兩個極端——Scratch/Blockly 讓學生拼積木但永遠碰不到真正的程式碼；文字教學讓初學者同時面對邏輯和語法的雙重認知負荷。兩者之間缺少一座橋。

**目標使用者：**

- **學生**（主要）：從零基礎到進階，涵蓋 APCS 考試準備、競程訓練、學校課程
- **教師**：設計教學路徑（Topic）、指定練習、追蹤學習軌跡
- **語言開發者**：透過模組化語言套件擴充新程式語言
- **課程設計者**：透過 Topic 系統定制不同教學情境的元件可見性和積木形態

## 核心想法

**程式碼和積木都不是本質——它們是同一個語義結構的不同投影。** 學生可以用積木理解邏輯，同時看到對應的真實程式碼，在兩者之間自由切換，最終過渡到純文字編程。

關鍵不是在程式碼上面疊一層積木——**只有語義層存在**，程式碼和積木都是參數化的投影。

🔴 **而「投影」是三族，不是兩族**（2026-09-25 從兩族改成三族）：

```
形態   長什麼樣        程式碼 · 積木 · 流程 · 3D
行為   做什麼          執行 · 電氣傳播 · 物理模擬
形式   什麼時候成立      🆕 形式核（`contract.cella`）
```

理論基礎見 [原則](principles.md)；協定見 [元件](concepts/元件.md) ·
[連接](concepts/連接.md) · [組裝](concepts/組裝.md) · [契約](concepts/契約.md)。

**和現有工具的根本差異：**

| 工具 | 做法 | Semorphe 的不同 |
|------|------|----------------|
| Blockly/Scratch | 積木 → 程式碼（單向，不可逆） | 程式碼 ↔ 積木（雙向，完美 round-trip） |
| Hedy | 每個等級有不同語法 | 同一語義樹，不同投影解析度 |
| 傳統 IDE | 以文字為中心，視覺化為附加 | 以語義為中心，文字和視覺都是投影 |
| Unison | AST hash 內容定址 | hash 語法層 vs hash 語義層 |
| LSP/LSIF | 跨檔案符號索引 | 位置導向 vs 語義導向 |

## 專案身份

| | |
|---|---|
| **名稱** | Semorphe（散模費 σημορφή）— σῆμα（語義）+ μορφή（形態） |
| **標語** | 唯一真實，各式投影 |
| **CLI** | `smorph` |
| **Logo** | `<Σ>` |
| **npm** | `semorphe` |
| **VSCode** | `semorphe-vscode` |　🔴 **而它同時是 Arduino IDE**（Theia，已驗證）→ [history/080](history/080-ArduinoIDE吃VSCode擴充而那不是一個平台是一個位置.md) |
| **技術棧** | TypeScript 5.x, Blockly 12.4.1, web-tree-sitter 0.26.6, Monaco Editor, Vite |

## 現狀

程式碼 ↔ 積木雙向 round-trip 成立，語義直譯器可執行。三層解耦（Core / Bus / View）已落地。Topic 系統已完成。

🔴 **2026-09-25／26：協定從一個主體變成三個**（元件 · 連接 · 組裝）＋ 橫跨的契約。
而**連接與組裝今天在程式碼裡是 0**——那不是漏做，它們的動工條件寫在〈未清的債〉。
舊協定到新協定的逐格順序見 [draft/舊協定到新協定的遷移](draft/2026-09-26-舊協定到新協定的遷移.md)。

🔴 **不再是單語言**（2026-08-21 更正）。Python 是第二個**能跑**的語言——自己的文法、元件、
課程、執行器、互動輸入，瀏覽器實測過（specs 167–173，
[118](history/118-文法不是語言而歸屬要用宣告的.md)–[123](history/123-一筆判定把半個故事當成全部.md)）。

而**第二個語言的價值不在多一個語言**：它是第一件能證偽「語言無關」那句話的東西。
在只有一個語言的時候，一份宣稱中立的程式碼與一份寫死的程式碼**行為完全相同**。

> **語言無關性（P9）仍然是破的，而破的位置換了。** 前四條（核心層硬編身分／碎裂／
> 殼／視圖層 import 語言套件）**都已歸零**；今天在的是**執行器直接持有面板**。
>
> 🔴 **當下的數字一律以 `tests/baselines/four-independences.json` 為準，不寫在這裡**
> ——寫死的數字每交付一次就過期一次（**已經發生四次**，教訓見 `experience.md`）。
> 這裡只說**問題的形狀**。
>
> 轉折：[015](history/015-語言無關性從尚未驗證到已知是破的.md) ·
> [051](history/051-P9從沒有執行機構到病換了位置.md) ·
> [104](history/104-語言耦合先變成看得見的再清它.md) ·
> [106](history/106-護欄的維度是被發現的不是被設計的.md)

🔴 **「已完成元件」那三段清單 2026-09-26 退場**——它回答「我們蓋了什麼」，而那是程式碼自己說得出來的，且**每交付一次就過期一次**。
構成見〈架構〉；做過什麼見〈已完成的里程碑〉。

**已知差距：**

🔴 **這一格 2026-09-26 收斂**：原本是一張帶 `Phase 9.1／9.2` 編號的表，
而那套編號在路線圖裡已經不用了——**一份用過期座標指路的表，指不到任何地方。**

| 缺失 | 它在等什麼 |
|---|---|
| SemanticDiff 增量更新 | 全量替換今天夠用；它等的是**大檔案變慢**這個可觀察的症狀 |
| 硬體描述層 | 等 `hw:` scope 落地（見〈階段 6.11〉與 [組裝](concepts/組裝.md) 的四個零） |
| 資料流視圖 | 🔴 阻斷是**邊**不是 annotations（見 [連接](concepts/連接.md)）；最便宜的第一步是「定義 → 使用」那條線 |

⚠️ **其餘的「還沒做」一律在〈🧾 未清的債〉**，不在這裡——兩個地方各記一份會漂。

## 架構

### 協定：三個主體

```
元件   身分 · 埠 · 參數 · 形態 · 行為 · 形式  ＋ preconditions／postconditions／invariants
連接   結構（樹 · 有向邊 · 網）× 關係（組合 · 資訊 · 守恆）—— 🔴 兩軸【正交】
組裝   整張網才成立的：全網不變量 · 預算 · blame        —— 🔴 今天程式碼裡是 0
```

[元件](concepts/元件.md) · [連接](concepts/連接.md) · [組裝](concepts/組裝.md) · [契約](concepts/契約.md) · [面板](concepts/面板.md)

🔴 **契約不是第六個槽，是加在槽上的精化**（`{x : T // P x}`）：槽的產物是**剪枝力**
（填不出就不是元件），契約的產物是**棘輪**（填了有沒有人判）。
而**階梯是判定者算出來的，不宣告**——`judge == null` 的筆數是契約的誠實度度量。

### 投影：三族，而它們有機械的簽名

```
形態   成對、可逆    lift ↔ generate · render ↔ extract
行為   單向         execute   → 一條軌跡
形式   單向         formalize → 一個命題
```

**形態成對是因為它保存語義**；另兩族單向，因為它們產出的是**關於**語義的東西。

### 執行期的堆疊

```
宿主（瀏覽器 / VSCode / Theia）
  ↕ ViewHost
面板（積木 · 程式碼 · 流程 · 主控台 · 變數）        src/panels/<name>/panel.ts
  ↕ SemanticBus
核心（語義樹 · 元件登錄 · 投影引擎 · 直譯器）        零 DOM，Node 跑得起來
  ↕ 語言套件介面
語言套件（cpp · python）＋ 膠囊 src/components/<scope>/<name>/
```

⚠️ **Arduino 不是語言套件，是【目標】**（board target）——域／語言／layer 三軸正交，
把它排成第三個語言是本檔〈元件住在哪〉禁止的類別錯誤。

### 出貨：五層（階段 8）

```
① 核心      協定 ＋ 匯流排 ＋ 登錄表        不認得任何 componentId
② 詞彙套件   🆕 關係律 · 組裝約束 · 單位     可被 ③④ 依賴，獨立版本
③ 元件套件   lang-cpp · hardware-arduino   貢獻到工具箱
④ 視圖套件   panel-*                      只依賴 ①
⑤ 應用      組裝 ②③④
```

🔴 **② 是 2026-09-26 加的**，而推翻原本四層的是 ROS 的 `.msg` 與 Maven 的 BOM
**各自獨立地**說同一件事：詞彙／約束要是一個**可以被依賴的單位**。
設計見 [draft/套件協定](draft/2026-09-26-套件協定.md)。

### 三層宣告

⚠️ **這一節 2026-09-26 從「三層契約」改名**——「契約」現在是
`preconditions`／`postconditions`／`invariants`（見 [契約](concepts/契約.md)），
而這三層講的是**宣告給誰看**，不是「什麼時候成立」。同一個詞兩個意思。

- **第一層（語義宣告）**：`component.json` ＋ lift pattern ＋ lifter——**所有消費者可用**
- **第二層（投影提示）**：`forms/blocks.json` ／ `codeTemplate` ／ generator——**僅對應投影消費**
- **第三層（視圖策略）**：視圖讀 annotations 自己決定呈現——加一個唯讀視圖不必改語言套件

### 語言套件目錄結構

```
src/components/<scope>/<name>/     膠囊：component.json · lift.ts · generate.ts
                                   execute.ts · forms/ · labels/ ·（contract.cella）
src/languages/<lang>/              文法、產生器、抽取器、骨架、診斷、標頭別名
  ├─ manifest.json                 套件中繼資料
  ├─ lift-patterns.json            AST → 語義 pattern 規則
  └─ std/                          標準函式庫（目錄名即 header 名）
```

⚠️ **2026-09-26 更正**：原本這張圖畫著 `languages/{lang}/core/components.json`
——那個集中檔**在膠囊化之後就不存在了**（元件住在 `src/components/`）。

### 尚未實作但已有設計的規格

- **語義標註（Annotations）**：語言套件與視圖套件之間的開放介面（`control_flow`、
  `body_execution`、`introduces_scope`、`side_effects`、`hardware_binding`）。缺少標註時用
  generic fallback，不報錯——任何語言 × 視圖組合都能工作。
- **視圖套件**：⚠️ **部分已實作**——今天是 `src/panels/<name>/panel.ts`（五份），
  而**不是**原本設計的 `views/{name}/manifest.json`（那個目錄不存在）。
  缺的是把它們真的拆成獨立套件（階段 8 的 ④）。
- **WebView 隔離模型**：瀏覽器每視圖 ＝ `<div>` ＋ EventEmitter；VSCode 中積木 ＝ WebviewPanel、
  程式碼 ＝ 原生 TextEditor。**核心跑在 Extension Host，不跑在 WebView。**
  > 🔴 兩個 2026-08-16 量出來的前提（[069](history/069-vscode原型退休而它的兩個教訓被撈出來.md)）：
  > ① 膠囊登錄表用 `import.meta.glob`（Vite 的轉換）⟹ 那個宿主也必須用 Vite 建置；
  > ② `src/ui/app.ts` 有**六個單例**寫死了「只有一個文件」，而編輯器有 N 個
  > ——**那是這條路真正的成本，而它不在任何介面上**。

### 長期方向

1. **多語言**：C++ → Python → Java → Go → Rust……同一語義結構，切換語言即切換投影
   🟢 **而「加一個語言」現在有一支 skill**（`add-language`，2026-08-21）——
   Python 那次走了六支 spec，而那條路的清單當時只存在於 `history/118`–`120` 裡。
2. **語義套件市場**：語義契約 + 投影定義 + 多後端執行體。同一元件可有多個執行體（wasm / js / webgpu / remote），自動基準測試 + 自動後端選擇。教學價值：讓學習者直觀感受演算法複雜度
3. **可插拔執行與時空旅行除錯**：執行即投影；直譯器每步產生語義狀態快照，回溯 = 用歷史快照重新投影（viewParams 多一個 `timeStep`）
4. **硬體教育**：Arduino 接線視圖 + 模擬視圖，從軟體延伸到嵌入式
5. **AI 輔助**：LLM 作為語用分析師，在確定性 guardrails 內提供提示投影——建議下一步，但不自動修改語義結構
6. **自舉**：教學路徑本身成為可分享的語義套件；學習數據回饋優化 Topic 層級樹，目標是**結構還原度最大化**而非效能最大化

### 離線可用

- [x] **已完成**（2026-08-16，spec `128`）——執行期外部請求 **4 → 0**， → [history/070](history/070-離線可用一直是半真的而它壞得不夠大聲.md)
## 開發約束（跨階段，不隨階段退場）

> ⚠️ 這些**不是路線圖項目**——沒有完成的一天，所以它們住在這裡而不是某個階段裡。
> （2026-08-14 從階段 6.5 提出來：那條「不准紅著過夜」原本埋在一個**已完成的階段**裡，
> 而 [experience](experience.md)`:1293` 引用它時，它的定義正要隨那個階段被收斂掉。
> **一條被別處引用的規則，不能住在一個會退場的地方。**）

- **既有測試全程綠，一次都不准紅著過夜。** 測試數只准增加。
  ⚠️ **當下的值以 `npm test` 為準，不寫在這裡**——寫死的數字每交付一次就過期一次。
  而它與「先讓測試紅」不衝突：那條講的是不要**放著**，不是不要紅
  （見 `experience` 的「一個十秒能修的紅」）。
- **護欄的基線只准下降。** 上升時要**指名是哪一筆**，不得只改數字。
- **現況數字的唯一來源是 `tests/baselines/`**，不是這份文件。

## 路線圖

> **這一節只回答「下一步在哪」。**
> 🔴 已完成的交付細節**不留在這裡**——它們收斂成一行 ＋ 指向 `history/` 的指標
> （2026-08-18 收斂：路線圖 1032 → 約 200 行）。
> ⚠️ 判準：**這一行說得出下一步嗎？** 說的是「我們完成了什麼、當時長什麼樣」→ 收成指標。

### 🟡 進行中

⚠️ **這一節的每一項都必須至少有一個沒打勾的框**（`audit-vision-wip` 守著）。
🪦 2026-09-07 追認過一次「十二刀從 `draft/` 直接做出來、沒走升格閘門」——
那條教訓在 [experience](experience.md)，而它留下的規矩是：**把在做的事寫進來**。

#### 🟡 課程的第六刀之二：拆輪子的曲線

**目的**：讓積木成為學生的**輔助輪**，最終看得懂程式碼（使用者原話）。
🔴 而研究對這一格說過一句重話（blocks→text 系統性回顧）：直接翻譯的工具往往
**沒有處理**從拖拉到**打字與除錯**的概念轉變 ⟹ 交付不能只是「把哪一格放大」。

**設計脈絡**（做完才退場）：[draft/拆輪子的曲線](draft/2026-09-03-拆輪子的曲線.md)

- [x] **軌道宣告一條曲線**——軌道給預設、課在轉折點覆寫 → [217](history/217-一條曲線的宣告寫的是轉折點不是每一格的值.md)
- [x] **除錯的練習**（`kind: 'debug'` ＋ `starters/`）——e2e 驗**相反的方向**（起點必須跑不出答案）→ [218](history/218-一個資料夾同時裝著該對的與該壞的.md)
- [ ] 🔴 **打字的練習**——⏳ **動工條件**：要一個 **per-task 的編輯來源**，而今天**一個都沒有**
      （`core/edit-tally.ts` 2026-09-08 零消費者退場）。
      ⚠️ 在那之前加一個 `kind: 'type'` 只會是版面建議的別名。
- [x] 🪦 **指標顯示 ＋ 畢業**（2026-09-08 交付，**同日退場**）——使用者換了更直接的答案：
      在**設計課程**的時候提醒可以開始用程式碼寫。留下的是一條護欄 → [222](history/222-一面鏡子不說話.md)
- [ ] 🪦 ~~一層薄的「學習訊號」~~ **升格當天被推翻**——它與 `progress.ts:23` 已有的決定衝突
      （三件事一次抽象會長出假的父概念）。改成：編輯來源那一格自己存


#### 🟡 UI 也是一個域

—— 設計見 [draft/UI 也是一個域而框架是投影](draft/2026-09-03-UI也是一個域而框架是投影.md)
（2026-09-07 升格）。⚠️ **前提早就定了**：`concepts/元件.md` 的軸表裡
域的值是 **`software / hardware / UI`**——UI 一開始就是三個域之一，不是新東西。

- [ ] 驗收：一顆 UI 元件走**同一套**五槽宣告，而框架（React／Lit）是它的**投影**
- [ ] ⏳ **動工條件**：分子那一刀的**路徑語法**先落地
      （已於 2026-09-06 交付判別器 → [216](history/216-身分的階層與實例的路徑在同一個字串上分得出來.md)），
      而**組合階層**還沒有——UI 元件天生是巢狀的
- [ ] ⚠️ **不在本項**：挑框架。🔴 那是最後一步，不是第一步

🟢 護欄 `audit-vision-wip`：**這一節裡的每一項都必須至少有一個沒打勾的框**
（2026-09-06 補上「`####` 標題也算一項」——在那之前一整刀做完了它一聲不吭）。

> **一個區段的標題是一句宣稱，而它不會自己去看底下裝了什麼。**

### 🔜 下一步（已排序）

⚠️ 這一節原本叫「已排序，**未開工**」，而階段 7 開工之後那三個字就不對了
——**開工的那一刀留在原位，由上面的「進行中」指過來**，
比整段搬家便宜，也不會讓它的上下文（目的、前置、驗收）跟著漂。

#### 🆕 分子：組合階層與粒度切換（2026-09-03 升格）

**目的**：今天的元件都是「原子」，而使用者說的基本要素多半是**分子**（車子裡有輪子）。
使用者：「模組之間的訊息傳遞…模組之間的組合包含關係…**如何在這些粒度切換**？」

🔴 **它是 UI 域與硬體接線兩格的共同前置**——那兩格都需要「一個模組對外長什麼樣」。

**拍板**（2026-09-26）：封裝語義做**純整理**，而終點是**可重用**（完整形式在〈階段 8〉）。
**設計脈絡**（做完才退場）：[draft/分子與粒度切換](draft/2026-09-03-分子與粒度切換.md)
——三種封裝語義的層級與代價 · 為什麼鍵不能是 `nodeId` · 三個視圖的投影為何結構不同 · 收合為什麼不是「藏起來」。

- [x] **跨層路徑語法**——判別器回三種答案（身分／實例路徑／歧義）→ [216](history/216-身分的階層與實例的路徑在同一個字串上分得出來.md)
- [x] **切口計算**（`core/molecule.ts`）——`crossingEdges` ＝ `boundaryOf` 的埠數，而**兩邊各自算一次**
- [x] **粒度狀態**（`core/granularity.ts`）——每個 `nodeId` 都換掉之後還配得回去
- [ ] **流程面板的投影**——收成一個**有埠的框**（e2e。⚠️ 今天 e2e 測過收合的條數是 0）
- [ ] **積木／程式碼的投影**——訂閱同一個狀態（積木原生就會；程式碼要 `nodeId` → 行區間）
- [ ] **鑽入 ＋ 麵包屑 ＋ 裝配樹**——e2e：三層深進出後回到原位；樹上選 ↔ 圖上亮雙向連動
- [ ] **階層下的來回一致**——fuzz：`分子 → 展開 → 收合 → 分子` 逐字相同

⚠️ **不做**：多選（要 Blockly 插件，會撞 `middle-drag-pan` 的手勢）· 語意縮放 ·
UI 域與硬體域本身（它們是這一格的**消費者**）。


#### 🆕 執行器 B：把執行位置變成一個【值】（2026-08-31 升格）

**目的**：讓「執行中改結構」從**未定義行為**變成有規則的能力，並順手拿到單步、斷點、回放、時間軸
——那些全都是「位置是值」的推論。

🔴 **不是改造現有直譯器，是加第二個**（量出來的）：`interpreter.ts` 693 行 `yield` 0 次
（位置住在 JS 呼叫堆疊，不可遷移）· 232 / 309 個 `execute.ts` 直接讀 `children`。
改造 ＝ 309 個 executor 改簽名，而改完只換到「可暫停」。**現有那個一行都不動。**

**設計脈絡**（做完才退場）：[draft/執行器是可插拔的 §九](draft/2026-08-24-執行器是可插拔的.md) ·
[draft/過程是一等公民](draft/2026-08-31-過程是一等公民.md)

- [ ] **甲：把現有執行器釘成「對快照的觀測」**——一條護欄：執行中改積木，這次執行的輸出不變（第一次跑會紅）
- [ ] **驗 `SemanticNode.id` 在 lift→generate→lift 之後穩不穩**——不過就先做「id 可追溯」，後面全部擋住
- [ ] **執行器 B 的最小子集**（賦值／if／while／印出）——護欄：同一份程式 A 與 B 的輸出必須一致
- [ ] **換版點 ＋ PC 遷移**——執行中改結構 → 用 `nodeId` 比對 → 對得上續跑、對不上**停下來說出來**
      （形狀同 [狀態遷移](concepts/狀態遷移.md)，Erlang 的 `code_change/3`）

⚠️ **不承諾**：B 追上 A 的元件覆蓋率，也不承諾 B 變快。B 的價值在「位置是資料」，不在效能。


#### 階段 6.11：Arduino 的板子與腳位（第 4 項起）

🧾 **已完成的七項收成一行**：三個目標各自提供函式與常數 · 八塊板子（每個值附上游來源）·
消掉 `properties.values` 的雙重真相（40 → 0）· 下拉跟著板子走 · 名字的範圍 ·
函式庫標頭與能力跟著板子。→ [091](history/091-能力是一個性狀而不是一份清單.md) ·
[094](history/094-一份宣告是另一份的投影就沒有資格當真相.md) ·
[095](history/095-腳位的界由板子決定而路線圖指著一個不存在的下拉.md) ·
[097](history/097-八塊板子而三個錯都是憑印象填的.md) ·
[099](history/099-下拉跟著板子走而瀏覽器照出測試看不到的那一半.md) ·
[100](history/100-名字的範圍在積木上只畫得出一段而錨點掀出第二個缺口.md) ·
[101](history/101-標頭跟著板子走而產品走的是另一條路徑.md)

⚠️ 第 1–3 項與虛擬硬體被推遲的理由：[077](history/077-虛擬硬體往後推而先做寬度.md)。
＋ `cpp:pin_constant` 的 lift 路 2026-09-06 補回來了 → [212](history/212-一個因為沒有人會這樣做所以安全的理由.md)
（⏳ 已釘住的界線：宣告寫在使用**之後**仍然會被搶，解掉它要兩趟掃描）。

- [ ] **板子視圖顯示腳位狀態**——⚠️ 它會拉進被 [077](history/077-虛擬硬體往後推而先做寬度.md) 推遲的虛擬硬體設計
- [ ] **C／C++ 三個既有目標不得退步**
- [ ] **標頭的反向不成立**——ESP8266 的產出 round-trip 再切回 ESP32，`<ESP8266WiFi.h>` 不會換回去。
      🔴 補「每塊板子列出所有別塊的標頭」不 scale，正解是**一個能力有一個正典標頭**（更大的設計）
- [ ] 🪦 ~~三塊板子的腳位下拉不同~~ **實測否決：沒有腳位下拉**（`pinMode` 吃 input_value）。
      重開條件是「真的做出一個由目標提供選項的下拉」
- [ ] 🪦 ~~Servo~~ **量過了，而它是政策決定**：三個核心都沒有 `Servo.h`。
      重開條件是**有人決定那條政策**


#### 量測機制的洞（2026-08-20，使用者選「A 先做」）

- [x] 🟢 **兩條產出路徑的一致性**（spec `151`） → [102](history/102-兩條路徑的護欄第一次跑就抓到一個而我對哪條是死路的判斷是錯的.md)
#### 狀態層走 IDE 的管道（2026-08-25，人拍板「把前四刀做完」）

**判準**：「跟 IDE 結合」不是把面板搬進它的 panel 區——**搬面板只是換了個位置；
走管道才拿得到 F8、紅色波浪線、中斷點，以及使用者已經會的每一個快捷鍵。**
⚠️ 這條管的是**狀態層走管道**，不管主控台住在哪（主控台是**執行的輸出**不是投影）。

**設計脈絡**：[draft/版面與檔案 §六之五～六之八](draft/2026-08-24-版面與檔案.md)

🟢 **①–④ 已交付**（0.10.4）：診斷 → Problems · 流程進 editor 區 · 行動版四分頁 ＋ QuickPick ·
主控台 → 宿主的 panel 區 → [154](history/154-宿主的能力要用做得到嗎問不是用你是誰猜.md) ·
[202](history/202-十字退場而主控台回到它本來的位置.md)

- [ ] ⑤ **變數／逐步 → Debug Adapter**——⚠️ 它是**另一個宿主介面**，不是另一個執行器
      （[[執行器是可插拔的]]）；混掉會長出「VSCode 專用的執行器」，違反 P9。
      🟡 在那之前變數住在 `panel` 區——**那不是將就**，`panel` 本來就是放「執行時看的東西」的地方。
- [ ] ⏳ **等人工**：Theia 的 DAP 支援——自動化驗不到。
      🔴 而 Theia 的 `Pseudoterminal` 已實測**不支援**，退路是編輯器分頁


#### 階段 8：外部套件生態

**設計脈絡**：[draft/套件協定](draft/2026-09-26-套件協定.md)（十三個系統，收斂出五層）·
[draft/套件積木的粒度與預組](draft/2026-08-17-套件積木的粒度與預組.md) ·
[draft/套件的物件在執行期是什麼](draft/2026-08-17-套件的物件在執行期是什麼.md)

- [ ] **安裝／移除套件後元件自動出現／降級**
- [ ] **依賴鏈正確解析**
- [ ] 🔴 **詞彙套件要獨立成一層**——`lang-cpp` 與 `hardware-arduino` 都要用同一套關係律，
      而 ROS 的 `.msg` 與 Maven 的 BOM 各自獨立說了同一件事（見設計脈絡）
- [ ] **使用者自製元件能重用／發佈**——⏳ 三個阻斷：登錄表是編譯期的 glob
      （`component/registry.ts:77`）· 存檔沒有「自製元件」那一格 · 發佈那一段解法未定
      （[draft/膠囊要怎麼離開這個 repo](draft/2026-08-20-膠囊要怎麼離開這個repo.md)）

- [ ] **核心可獨立出貨**（2026-08-24 升格）——⚠️ 這是階段 8 的**反方向**：8 是「別人的套件裝進我們」，
      這一項是「**我們被別人裝**」。使用者四個情境裡最重要的是「本身就有 Blockly 或 Flow 了，
      但想加入程式碼即時互轉」——**那個情境一個面板都不需要**。
      設計見 [draft/可嵌入與可插拔的四個軸](draft/2026-08-24-可嵌入與可插拔的四個軸.md)。
      🟢 七條驗收已交付（2026-08-26）→ [149](history/149-核心第一次在別人的建置工具下跑起來.md) ·
      [167](history/167-一個都不認得不是降級是沒接上.md) · [168](history/168-那層轉接就是介面的一部分.md) ·
      [169](history/169-宣告的是層不是位置.md) · [171](history/171-一條規範落在兩條護欄之間.md) ·
      [026](history/026-P8-的範圍不含語義詞彙本身.md)
      - [ ] **登錄表實例化**——四處模組全域（`view-registry.ts:52`／`skip-declarations.ts:22,73`／
            `component/registry.ts:63`）。教學網站**一頁多題**才逼得出來
      - [ ] **面板只 import 協定**——`ViewConfig` 的 `appearance`（顏色，投影）與
            `schema`（本體插槽，**真實**）兩個埠。⚠️ `schema` 那一刀要先答
            「面板該不該知道本體插槽」，不是直接包一個埠
      - [ ] **topic 子集變成公開 API**（🟢 機制已存在，缺的是 API 與文件）

- [ ] **執行器可插拔**（2026-08-24 升格）——使用者：「執行的模擬…能和**外部 Debugger 或是真實環境**
      串接」。🟢 契約已經寫好了只是沒有人替它命名：匯流排上的 `execution:*` 四個事件就是它。
      設計見 [draft/執行器是可插拔的](draft/2026-08-24-執行器是可插拔的.md)。
      - [ ] 🔴 **第二個 runner（Pyodide 或 DAP）接上去，`execution:*` 一個都沒改**
            ——先做這兩個之一，**不要先做硬體**：它們不需要新的執行模型
      - [ ] Runner 收**樹 ＋ 對映**（`nodeId` 是共同語言），不是程式碼字串
      - [ ] `capabilities`（`steppable`／`resettable`／`variables: push|pull`／`timeModel`）
            ——⚠️ 那是**能力宣告**，不是新的 `ExecutionStatus` 值
      - [ ] `declareSkips` 改成 **per-runner**（「`with` 不執行」是針對**我們這顆**直譯器說的）
      - [ ] 護欄：**同一組語料上兩個執行器的分歧數**，棘輪只准下降
      - [ ] ⚠️ **時間軸（硬體模擬）不在本項**——那是換執行模型，自己一輪設計

- [ ] **執行的誠實：沒看過就不繼續**（2026-08-24 升格）——使用者逐字：「**如果沒看過的東西
      就不要執行下去了，要誠實的說沒看過**」。設計見
      [draft/執行遇到沒看過的東西](draft/2026-08-24-執行遇到沒看過的東西.md)。
      🟢 四條驗收已交付（2026-08-26）→ [173](history/173-拿掉錯的那個答案問題也跟著消失.md) ·
      [174](history/174-規範立錯了半天而使用者一句話把它翻過來.md) ·
      [175](history/175-一份沒有被記下來的介入讓執行變成不可重現.md) ·
      [176](history/176-一個會凍住自動化的東西不會有一支紅的測試.md)
      🪦 兩條被量測否決（`isSkipped()` 要出聲 · 執行前先說）——理由在 draft
      - [ ] 🆕 **一顆「重播上一次」的按鈕**——引擎那一半已經有了（`setReplayInputs`），
            而觸發它的動作是新的 UI。🔴 **它不該是預設行為**：學生按第二次執行，期待的是重新打輸入
      - [ ] ⚠️ **整段委派不在本項**——那是「執行器可插拔」

- [ ] ⚠️ **型別積木（`int` ＋ `*` ＋ `*`）**——屬於「指標與參考 UX」，不屬於本階段


#### 階段 9+：進階擴充

```
9.1 DataFlow 視圖（消費 control_flow annotations）  ← 🟢 消費者 2026-08-24 到場，而**來的是流程面板**
9.2 SemanticDiff 增量更新（前置：階段 5b nodeId 穩定性）
9.3 接線視圖 ＋ 模擬視圖（硬體教育）
9.4 跨語言映射視圖（abstractComponent 驅動）
9.5 語義套件市場
```

🔴 **上面那五行不得改字**——`tests/baselines/annotation-adoption.json` 用
`pending-consumer:9.1 DataFlow 視圖` 替一個零讀取點的標註背書，而護欄會**逐字**在本檔搜它。
（2026-08-18 那次收斂把它刪成三行，護欄當場紅。）

- [ ] 唯讀視圖可從 annotations 自動生成，不需改語言套件
- [ ] style 變更的語義 diff 為零


### 🧾 未清的債（跨階段，不屬於任何一刀）

🔴 **這些不會因為某一刀做完而消失——每一條都要有人領走。**

🆕 **而「舊協定 → 新協定」的逐格順序在
[draft/舊協定到新協定的遷移](draft/2026-09-26-舊協定到新協定的遷移.md)**（2026-09-26）
——它把 `ports`／`relation`／`invariants`／`preconditions` 這幾格分成三批，
判準是**消費者在不在**，而第一句是：**不要遷移 349 顆，要讓預設涵蓋它們。**

- [ ] 🆕 **學生真的上完之後留下的那幾筆**（2026-09-21 升格）
      現場在 [episodes/一班學生上完前幾課](episodes/2026-09-21-一班學生上完前幾課.md)，
      追下去的結果在 [274](history/274-學生上完之後那四條回饋各自指向什麼.md) ·
      [275](history/275-那句含糊的回饋指到的是一條真的斷掉的連結.md) ·
      [277](history/277-感覺太擠量得出來的那一面.md)
      🔴 它是這個專案第一次拿到「**真實學生**」這個母體的讀數。
      🟢 六條裡五條已處理（`+=` 量過是對的 · 排一排那兩題拿掉 · 答案的空格 · 站內連結 · 過程圖）。
      ⚠️ **驗收不是「六條都做完」**，是「**下一班學生上完之後，回饋落在別的地方**」。
      - [ ] 🟠 **印出的積木沒顯示**——使用者決定維持現狀（36 px vs 吸附半徑 28 px）。
            要改就得引入**接點型別**（全庫 293 處只有一種 `Statement`）
      - [ ] 🪦 **指路橫條的前七個字被工具箱蓋住**——⏳ 量清楚了而修不成（試三版全退）。
            **下一個人不必重走那三步** → [277](history/277-感覺太擠量得出來的那一面.md)
      - [ ] ⏳ 把**函式指標做對**（變成一顆概念：有型別、能當參數、能呼叫）——今天它誠實地不做

- [ ] 🆕 **名詞表的其餘各列**（2026-08-20）
      🟢 命名本身已掃乾淨（`concept` 家族整族退場，3496 處／656 檔 → 0）→ [110](history/110-concept家族整族退場.md)
      🟢 **2026-09-06 逐列查證**：五列裡只有兩列還要做事，而「每一列都是一刀」那句判斷不成立。
      - [ ] **三個登錄表的合併**——🟡 它與 `experience` 的一條教訓打架。
            目標改成「**名字說得出職責**」，待人拍板
      - [ ] **`forms` 仍分屬三個機制**（`blockDef`／`codeTemplate`／`astPattern`）
            ——先問誰因為它受傷過
      - [ ] ⚠️ **`Instance` 取代 `SemanticNode`** 動的是**型別的意義**——要有消費者才動

- [ ] **網頁版有檔案：主體、side-car、檔案系統埠**（2026-08-24 升格）
      —— 設計見 [draft/版面與檔案](draft/2026-08-24-版面與檔案.md)。
      🔴 它讓兩個宿主的真相模型統一：`view-state.ts:5`「網頁版**沒有檔案** ⟹
      積木的擺放【就是】真相」——**那句話的左半邊會作廢**。
      🟢 四條已交付：一份存檔拆成四種歸屬 · 存放的埠 ＋ 兩個實作（核心直接叫宿主儲存 7 → 0）·
      side-car 刪掉 ＝ 自動排版 · 匯出／匯入 zip → [151](history/151-存檔裡的樹沒有人讀而還原路徑會把真相抹掉.md) ·
      [210](history/210-把例外換成回傳值之後接例外的那一段安靜地變成永遠成功.md) ·
      [211](history/211-兩份宣告對同一個欄位說了相反的話.md) ·
      [214](history/214-一份匯出如果程式碼不是第一級的檔案.md)
      - [ ] **新檔案可以選語言**——⏳ 等「檔案」這個概念（今天沒有「新檔案」這個動作）。
            🟢 後半「副檔名跟著語言走」已兌現（第一百一十二條護欄擋住反推）
      - [ ] **`semorphe-state` 的舊資料有一次性遷移**（`storage-integrity` 護欄盯著）
      - [ ] ⚠️ **多檔之間的引用**（`#include "my.h"`）**不在本項**——有了多檔它才變成真問題
      ⏳ **剩下兩條共用的前置**：File System Access API **驗不到**（需要使用者手勢，
      headless 叫不起 picker）。**動工條件是有一個能驗 FSA 的辦法**，不是「等有空」。
      > **一個驗不到的實作與一個不存在的實作，在 CI 上長得一樣。**

- [ ] **版面：一套 slot 詞彙，兩個宿主**（2026-08-24 升格）
      —— 設計見 [draft/版面與檔案 §六](draft/2026-08-24-版面與檔案.md) ·
      [concepts/理解的層次](concepts/理解的層次.md)。
      🎯 **分界不是「這是哪一類」，是「這東西怎麼被用」**：取用要**相鄰**、認識要**面積**、
      狀態在**面板區**。
      🟢 五條已交付：slot 詞彙 · 桌機三種佈局 ＋ 可拖分隔線（不做自由 docking）· 手機四分頁 ·
      同步三態的機制與兩個宿主的入口 · 取用面附著在它服務的視圖上
      → [152](history/152-同步從方向變成來源而暫停暴露了第三態.md) ·
      [169](history/169-宣告的是層不是位置.md) · [180](history/180-四個面板的版面預設而護欄擋了一顆正當的按鈕.md)
      - [ ] ⏳ **在 Arduino IDE（Theia）裡真的按一次同步三態**——⚠️ **只有人做得到**
            （`preflight:vscode` 跑的是 Chromium）。🟢 必要條件已機械釘住
      - [ ] ⏳ **圖鑑在編輯區**——⚠️ **那個視圖還不存在**。
            🔴 這不是版面的債，是那個視圖的債（版面今天就排得出來）
      - [ ] ⚠️ 「暫停」今天真正在做的是「別把我的排版弄掉」——那是 nodeId 不穩的代償。
            穩定之後回來重看：它會**縮小但不會消失**（分岔那一態永遠在）

- [ ] **面板協定：第三個頻道（per-panel 狀態）**（2026-08-20 升格）
      —— 設計見 [draft/面板協定的三個頻道](draft/2026-08-20-面板協定的三個頻道.md) ·
      [draft/流程面板的形式](draft/2026-08-24-流程圖面板的形式.md)
      🟢 兩個未決 2026-09-06 決掉了（per-panel 狀態**跟著主體** · 兩個焦點 ＝ 開兩個檔案）
      → [211](history/211-兩份宣告對同一個欄位說了相反的話.md)；
      而 side-car 就是它的家，所以前置從「先答一個問題」變成「等多檔案落地」。
      🟢 四條已交付：流程面板可獨立開關 · 手拖的佈局活得過一次編輯 · 重開後在原位（**只在網頁版**）·
      第五十七條護欄（改面板私有狀態之後語義樹的雜湊不變）
      🔴 **而查證翻出一個真的不一致**：`flowLayout` 在 `src/vscode/` 裡**零筆**
      ——第一百零九條護欄（棘輪）盯著它。
      - [ ] **`ViewState` 的 key 從 uri 變成 uri × 面板種類**，且舊 key 有遷移
      - [ ] **網頁版與擴充兩側都成立**
      ⏳ **動工條件**：**多檔案那一刀開始的時候**。在那之前改 key 會是「機制有了沒人接」。

- [ ] ⏳ **C2 資訊軸（邊）——正確地在等**（2026-09-06 逐個查證消費者）
      🔴 「延後的理由已經失效三次」那句話**本身是錯的**——它把「有人提到過」
      誤認成「**有人在問**」。診斷系統在跑而一條邊都沒問過；流程圖的 `wires` 是**渲染時導出的**。
      > **「這個機制已經有三個消費者了」與「這三個消費者裡有人在問它」
      > 是兩句不同的話——而前者讀起來像後者。**
      ⏳ **動工條件**：出現**第一條導不出來的邊**——接線圖的 `D13 → LED+`。
      而它會逼出邊的**型別**（有沒有方向、能不能疊、距離怎麼算），而那三個問題今天
      一個都沒有答案，也**沒有人需要它們有答案**。

- [ ] ⏳ **`reference` 那一格——等它的消費者**（2026-09-06 重新判斷）
      🔴 **今天不做，理由是查證出來的**：`reference` 的消費者是**外部編譯器裁判**，
      而 `src/` 裡零筆 `clangd`／`arduino-cli`——**那個裁判沒有在跑**。
      > **一個「為了完整」而加的欄位，與一個「有人要用」而加的欄位，
      > 在型別定義上長得一模一樣——差別要等到兩年後才看得出來。**
      ⏳ **動工條件**：有一個真的會跑外部編譯器的裁判進來。
      設計脈絡 [draft/C 和 C++ 難分難捨](draft/2026-08-13-C和C++難分難捨.md)（in-flight）。


### ✅ 已完成的里程碑（一行一筆 ＋ 指標）

| 階段 | 結果 | 轉變 |
|---|---|---|
| — 協定拆成三個主體 | 第 233–239 刀（**零行產品碼**）。協定原本住在三個地方而沒有一個是協定的形狀；拆出 [連接](concepts/連接.md) · [組裝](concepts/組裝.md) · [契約](concepts/契約.md) · [面板](concepts/面板.md)，`元件.md` 816 → 634。🔴 四條判決：**投影是三族** · **契約是精化不是第六槽** · **視圖的第一等公民是它的編輯動作** · **粒度換的是結構不是關係** | [282](history/282-協定從一個主體變成三個而拆的過程量到兩件看不見的.md) · [episode](episodes/2026-09-26-一個下午重新推導出路線圖上已有的東西.md) |
| 6.x 契約的第一刀 | 第 245 刀。`judge` 欄位落地，值是**種類不是工具名**（P9 逼出來的）。🔴 而護欄第一版問錯了問題，抓到它的是它自己寫在量測之前的自我否證聲明——棘輪因此從「有沒有判定者」改成「**判定者宣稱覆蓋而沒有人量過**的路數」＝ 5 / 6 | [283](history/283-一個結構上必為零的分子與做完了長得一樣.md) |
| 8 操作說明會過期就變紅 | 使用者問「加入 Semorphe 怎麼使用……**這樣使用者比較知道要拉哪裡**」，而答案的第一句是**不要抹在 66 堂課上**——會腐爛，且**一個需要被教的工具是在增加認知負荷**。交付：`withCursor()`（把游標畫進頁面裡——Playwright 錄的是頁面內容，作業系統的游標不在裡面）· 三支 webm 片段 · `interactions` 登錄表（課程只說「我會用到什麼」）· 護欄**兩個方向** ＋ 200KB 媒體上限。🔴 而腳本錄製的真正理由不是省時間：**一張手工截的圖是死的；一支腳本錄的片段是活的——按鈕搬家時它會紅**。⚠️ 這一筆是 **2026-09-07 補記的**——它 2026-09-04 就做完了而沒有轉變記錄 | [215](history/215-操作說明變成會過期就變紅的東西.md) |
| 8 課程的第五刀 | 「排回去」的題型（`kind: 'arrange'`）· 版面是**建議不是鎖**（「一個鎖，多數時候不是寫成鎖的——它是一段『每次都重新套用預設值』的程式碼」）· 語意波。🔴 它是**被外部研究換掉的**（三處），而驗收裡的「前 5 課」實際落在 3。⚠️ 同一輪還發現課文頁那 68 張對照圖**全部是舊引擎產的**——`codeHash` 錨在輸入，而過期的是輸出 | [207](history/207-第五刀被研究換掉而驗收裡的-5-落在-3.md) |
| 8 未清的債清了一輪 | 八筆逐筆「**先驗再決定**」，而查證讓五筆的紀錄與現實不符：一筆債**從來不存在**（範例寫錯）· 一筆早就完成（註解停在「124 顆」）· 一筆與 `experience` 打架 · 兩條驗收過期（活動列已退場／那個視圖還不存在）。實質交付：骨架上匯流排 · 函式指標曾**變成函式宣告** · 核心的 `localStorage` **7 → 0** · side-car 丟掉曾讓**整份存檔被拒絕** · SDK 公開面進基線。🔴 三筆判定「正確地在等」，動工條件寫死 | [208](history/208-骨架告示合成一條路而那個問題本身問錯了.md) · [209](history/209-五個形狀裡有一筆債從來不存在.md) · [210](history/210-把例外換成回傳值之後接例外的那一段安靜地變成永遠成功.md) · [211](history/211-兩份宣告對同一個欄位說了相反的話.md) |
| 6.x 符合性的清償 | 宣告了的接點在積木上表達不出來：**12 → 11 → 1 → 0**（11 → 1 是第四十六條護欄「投影遺失」順帶的；而**最後那 1 筆是護欄在說謊**——`cpp:var_declare` 的 `declarators` 宣告成 `'expressions'`，一個謊讓三個消費者各自錯一次） | 第四十六條護欄 |
| 6.x 左值必須被結構表達 | 使用者拍板判準後，左值解析器改成**扣除式**——加一種新左值形狀**不改任何既有執行器**；第七十三條護欄硬性零。🔴 收穫是護欄自己換了量測：從**讀宣告**改成**主動探測**，改完當天抓到四筆漏 | [157](history/157-左值的判準從必須是接點改成必須被結構表達.md) |
| 8 每個槽自己選視圖 | 使用者：「**為何復原按鈕只跟積木**？…希望每個面板可以去選擇要哪一種視圖」。↩↪ 搬到全域標頭（`doUndo` 早就走三條路——行為是全域的，而行動版早就這樣了）；每格一顆下拉，**同一份產生器**四格共用；使用者的覆寫是一張**層→層的置換表**——「同一視圖最多一格／選到別處的就對調／切走再切回來不變」三條都變成定義本身 | [200](history/200-復原早就不是積木的動作了，而錯的是它的位置.md) |
| 8 十字退場・主控台回到 panel 區 | 使用者在 Arduino IDE 按了三欄與十字：「**好像沒有比較好**」，接著給了解法：「捨棄十字…最底下水平**完全展開**是放主控台…把我們的主控台跟原生的綁在一起，就是**多塞幾個 tab**」。🔴 而十字是**唯一需要編輯區有第二列**的版面，而 Theia 沒有 `setEditorLayout`——我花了兩輪在問「怎麼讓這個宿主做到 X」，正確的問題是「**為什麼我們需要 X**」。答案在自己的概念裡：主控台不是投影，是**執行的輸出**（三維錨定）。三張版面全是純欄 ⟹ `reveal(ViewColumn)` 就排得出來，**三個宿主形狀一致**；VSCode 那側改成 panel 區的 `WebviewView`。第八十一條的 I4 從「`state` 不得缺席」反轉成「不得出現 ＋ 叫得回來 ＋ 有輸出自己回來」。🔴 而**同一天的第二刀**推翻了拍板點①：主控台與變數變成 panel 那一排上的**兩個原生分頁**，因為「不要被餵」是**投影**的規矩，而執行的輸出是**資料流**——它本來就只有一個源頭 | [202](history/202-十字退場而主控台回到它本來的位置.md) |
| 8 課文有了出口 | 使用者問的是「如何提升 SEO」，而查下去發現那不是 SEO 題：`lessons/` 有 6 軌 66 課、**129,368 字**課文，而 `load-lessons.ts:8` 寫著「課文不在這裡」——**產品裡零個讀者**，唯二讀它的是測試。交付：66 課 → **73 頁純 HTML 靜態頁**（零 JS），產生器走**同一份宣告**，雙向連結（靜態頁「在編輯器打開」↔ 章節選單「📖 看這一課的課文」），第一百零一條護欄 15 條。🔴 做的過程翻出**四個既有缺陷**，修了三個（骨架只生效三分之一／存檔蓋掉課程釘住的那幾格／行動版匯不出檔案），第四個記進已知問題 | [205](history/205-課文有了出口而它一直都在那裡.md) · [206](history/206-零外部資源有了一個具名的例外.md) |
| 8 槽的切換＝交換內容 | 使用者連續否決**五個**做法，最後定案於一句「**我不是要輪轉，我是要交換**」＋一個參照系「就像網頁版那樣」。🔴 前三個提案都在問「這個宿主做得到哪一種」，而答案 2026-09-01 就寫在自己家裡（指派是一張層→層的置換表）。程式碼那一格是 IDE 的編輯器，換不了內容只能換位子——開一份替身佔住槽、就位、收掉替身 | [203](history/203-切換的意思是交換內容而不是輪轉格子.md) · [場景](episodes/2026-09-02-五個被否決的切換.md) |
| 8 身分不准問，除了一個附病歷的檔 | [154](history/154-宿主的能力要用做得到嗎問不是用你是誰猜.md) 的**修訂**：撞到兩件**探測不出來**的事（分頁關不掉、`visible` 與 `document.hidden` 同時說謊）。規則從「不准知道」改成「只有 `host-quirks.ts` 可以知道，而它必須說明它怎麼知道的」——⚠️ 而護欄跟著**變嚴**：掃描範圍從一個檔擴大到整個 `src/vscode/` | [204](history/204-身分不准問除了一個附病歷的檔.md) |
| 8 版面：四張示意圖 | 使用者：「積木和流程用 tab 切換……**程式碼面板就變得比較特別了**」。佈局宣告從一維 `layers` 換成二維 `areas`；編輯區換成 CSS Grid（🪦 `split-pane.ts` 退場）；**示意圖從同一份宣告產生**，所以圖與畫面不可能不一致。十字＝四層各一格，而從「對照」切過去時**程式碼與積木一格都不動**。第八十一條的兩條一維硬性零升成**六條二維不變式** | [199](history/199-版面說了根公理的反話，而修它動到一條一維的硬性零.md) |
| 7 語言耦合的清償（階段 7 第 0 刀） | 中立性護欄從**一個數字**長成**三個維度**（元件身分字面 · 積木型別字面 · import），三維全部歸零；命令式積木定義歸零、組裝點的語言管線 **12 → 0**。🔴 三次都是「前一維看不見另一種拼法」。⚠️ 它 2026-09-07 才被收斂——在那之前掛著 `🟡` 而底下一個沒打勾的框都沒有，已經十二天，而第一百零三條護欄**掃不到它**（那條只掃〈🟡 進行中〉） | [104](history/104-語言耦合先變成看得見的再清它.md) · [106](history/106-護欄的維度是被發現的不是被設計的.md) · [160](history/160-最後一顆命令式積木退場而清單歸零不等於清乾淨.md) · [165](history/165-組裝點不再認得任何語言的名字.md) |
| 8 開放值域的欄位要寫得出來 | 2026-08-24 升格 → 2026-09-06 全部兌現：清單外的值不得被改掉 · 第五十九條護欄的棘輪 3 → 0 · **型別的下拉跟著程式長**（導出的，不是宣告的）。🔴 教訓：**一份清單如果是宣告的，它描述的是我們想到的** | [170](history/170-查證完要在同一次編輯裡決定那個框.md) · [213](history/213-一份清單如果是宣告的它描述的是我們想到的.md) |
| 7 Python 語言套件 | 第二個**能跑**的語言：文法／元件／課程／執行器／互動輸入全數落地，而它的價值是**第一件能證偽「語言無關」的東西**。🔴 目的中途被改寫——「通解是**算子**的性質，不是**解**的性質」 | [107](history/107-第一條跨語言的等價邊而契約沒有語言建構中這個狀態.md)–[160](history/160-最後一顆命令式積木退場而清單歸零不等於清乾淨.md)（34 筆） |
| 8 骨架在其餘視圖上的顯示 | 流程視圖認得 `ScaffoldMode`，判定搬進 `core/scaffold-nodes.ts` 走匯流排，第八十八條護欄釘住（**留債見「未清的債」**） | [191](history/191-骨架在流程視圖上是一句謊話.md) |
| 0–5b 地基與解耦 | Core 零 DOM、面板零 import、跨投影查詢可用 | [069](history/069-vscode原型退休而它的兩個教訓被撈出來.md) |
| 6.7 語法錯誤的辨識層 | 閘門 ＋ 訊息投影 ＋ 修復建議全數關閉；**而 ① 從「待實作」重判為「待論證」**（它拿不到 P6 的背書，論證移入 `draft/語義診斷系統`） | [092](history/092-把構想翻過來的是一次量測.md) |
| 8·控制項離開積木面板 | `HostFeatures` 布林 **5 → 4**；面板內控制項 **→ 0**（工具箱與畫布不變）；三個宿主讀**同一份 `ControlState`** | [153](history/153-控制項離開積木面板而做法是登錄表不是第六格布林.md) |
| 6.13 IDE 擴充（spec 140） | 驗收**七條逐條按過、一條刪除**；同步那一族結清（擴充 0.9.4） | [087](history/087-一段活過重畫的復原歷史.md)·[088](history/088-空行是排版不是真實.md)·[089](history/089-spec-140的驗收清單.md) |
| 6 Topic 系統 | 同一語言在不同 Topic 下有不同層級樹與積木覆蓋（2026-03-11） | — |
| 執行機構自己接上 | CI 真的跑測試；四項獨立性第一次會關；e2e 接上；識別字 618 → 0 | [050](history/050-識別字不再是中文-一條沒有機制的慣例漂了兩個月.md) |
| 目錄結構四件小整理 | `src/blocks/`／`src/views/` 消失，五個檔各歸其位（交付 2/4） | [052](history/052-同名不是重複的證據.md) |
| 6.5 元件膠囊重構 | **加一顆元件 ＝ 新增一個資料夾，零編輯**（177 顆全膠囊化） | [030](history/030-C1參數規格化-證據優先於名字.md)·[031](history/031-D命名空間遷移-身分不只以字串出現.md)·[049](history/049-F完成-把身分換成性狀.md) |
| 6.6 語義診斷系統 | 五條驗收全數達成 | [071](history/071-裁判有了而它看不懂的方言要先排掉.md) |
| 6.8 第一課 | 教材的第一個實例，四條驗收全數兌現（specs `124`／`125`） | [098](history/098-第一課補一筆轉變而它當初撞到的是產品的核心宣稱.md)·[過程](episodes/2026-08-19-第一課走過三遍.md) |
| 6.9 第二課 | 訊息不是代號；第四十四條護欄 | [066](history/066-護欄錨在供給端所以量不到消費端.md) |
| 6.10 目標（`target`） | 選一次而不是三次；C 的產出 6/10 → 10/10 | [072](history/072-target的兩個假設都倒了而它反而證明了target.md)·[074](history/074-機制有了沒人接上的第十一個而這次它有了執行機構.md) |
| 6.12 寬度 | 貼進來的 Arduino 程式碼轉得動；殘差與漂移各說得出數字 | [078](history/078-量完之後階段6-12的前提垮了一半.md)·[079](history/079-補量執行之後下一步又換了一次.md) |
| 6.13 擴充第一刀 | `.vsix` 產得出來，畫布在 VSCode 裡跑得順（16.7 ms） | [080](history/080-ArduinoIDE吃VSCode擴充而那不是一個平台是一個位置.md)·[081](history/081-第一刀畫得出積木而三個坑都不會拋錯.md) |
| 6.14 擴充長成能用的 | 雙向同步／高亮／執行／設定 | [082](history/082-擴充長成能用的而一個缺口只在大家都不那樣寫的地方.md) |
| 6.15 面板就是網頁版本身 | 程式碼那一格交給 IDE；`src/vscode/` 2170 → 1624 | [083](history/083-面板就是網頁版本身而四次修好都是我自己上一次修出來的.md) |
| 6.16 零件積木 | 四批 33 顆；貼上的 LLM 程式碼轉得成專屬積木**而形式一字不差**；四份盲測殘差 0.00% | [084](history/084-零件不是身分是參數而那份draft擋的是執行不是積木.md)·[過程](episodes/2026-08-18-三批零件積木.md) |

- 🟢 **課程的第五刀**（2026-09-05）：「排回去」的題型 · 版面是**建議不是鎖** ·
  語意波（宣告了看法的課要**先下到具體再回到抽象**）。⚠️ 它是**被外部研究換掉的**
  → [207](history/207-第五刀被研究換掉而驗收裡的-5-落在-3.md)

⚠️ **階段 6 沒有 `history/` 轉變**（做的時候還沒有這個習慣）
——那一行因此寫得比其他行完整一點，因為沒有指標可以贖回它。
🟢 **6.8 的那一筆已於 2026-08-19 補上**——它是 `draft/教材要如何呈現` 反流時
順帶還的債。**一個階段的 draft 退場，正是補回它缺的轉變的時機。**

## 關鍵延伸（主題觸發必讀）

| 觸發關鍵字 | MUST 讀 |
|---|---|
| 新增語言、語言套件、manifest、依賴解析 | `concepts/開放擴充.md` |
| Topic、認知層級、toolbox、scaffold | `concepts/漸進揭露.md` |
| 新視圖、viewType、可逆性、唯讀視圖 | `concepts/投影.md` |
| 元件、接點、命名、跨域、五槽 | `concepts/元件.md` |
| 🆕 邊、埠、關係律、接線、組合／資訊／守恆、樹圖網、粒度 | `concepts/連接.md` |
| 🆕 組裝、DRC、全網不變量、預算、唯一性、blame、跨元件的約束 | `concepts/組裝.md` |
| 🆕 契約、pre／post／invariant、判定者、證據階梯、超性質、形式化 | `concepts/契約.md` |
| 🆕 套件、manifest、貢獻點、依賴的種類、版本釘選 | `draft/2026-09-26-套件協定.md` |
| 殼、規範沒有檢查、護欄、量測工具 | `concepts/執行機構.md` |
| 舊註解裡的數字、一筆帶理由的舊決定、「等 X 再做」、已交付的驗收、路線圖上一筆看起來還沒做的事 | `concepts/紀錄的腐壞.md` |
| 語義診斷、錯誤訊息投影 | `draft/2026-08-05-語義診斷系統.md` |
| 圖解、記憶體視覺化、資料結構圖鑑 | `draft/2026-08-05-圖解形態與資料結構圖鑑.md` |
| 硬體、Arduino、接線視圖、2D/3D 組裝 | `draft/2026-08-05-硬體域併入計畫.md` |
| 待解工程問題、效能、版本控制、沙箱 | `draft/2026-03-11-已知工程待解問題.md` |
| 狀態遷移、形狀變了、hot-fix、`code_change`、配對、鑰匙、`strict=False`、存檔改版 | `concepts/狀態遷移.md` |
| 🆕 等價、觀察集、值不值得具名、結構性 vs 累積性、抽象的曲線 | `concepts/等價與觀察集.md` |
| 🆕 降級、認知邊界、能力協商、對方比較舊、不支援時要顯示什麼 | `concepts/降級與認知邊界.md` |
| 🆕 面板、視圖分類、驅動源、疊加層、後端可插拔 | `concepts/面板.md` |
| 分層、五路完備性、屬性升格成接點、元件之間的代數 | `concepts/元件代數.md` |
| 宣告要登記在哪、單一登記處、兩份宣告說相反的話 | `concepts/宣告登記處.md` |
| 左值、可賦值、位置 vs 值 | `concepts/左值.md` |
| 性狀、traits、脈絡標記 | `concepts/性狀.md` |
| 積木怎麼從宣告長出來、渲染管線、blockDef 推導 | `concepts/積木投影管線.md` |
| 鷹架、骨架、初學者的第一屏 | `concepts/認知鷹架.md` |
| 三軸、域／語言／layer、錨定 | `concepts/三維錨定.md` |
| 模擬到什麼程度算誠實、假的硬體、模擬與真實的落差 | `concepts/模擬的誠實.md` |
| 理解的層次、學習者在哪一層、Ln 的 n | `concepts/理解的層次.md` |
