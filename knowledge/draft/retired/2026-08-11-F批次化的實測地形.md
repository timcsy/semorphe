# F 批次化的實測地形——167 顆真正卡在哪

> ⚠️ **已退場（2026-08-11，F 完成）。** 這份是搬家過程中的即時地形圖，**內容停在「167 → 84」**——後面八批的教訓在下面這些檔裡：
> [history/049](../../history/049-F完成-把身分換成性狀.md)（轉變）、[concepts/性狀](../../concepts/性狀.md)（長出來的概念）、[episodes/四次整批回退](../../episodes/2026-08-11-四次整批回退.md)（場景）。
> **不要拿這裡的數字當現況**——現況看 `tests/baselines/component-locality.json`。

> 日期：2026-08-11
> 怎麼冒出來的：試著寫批次膠囊化工具把 F 一次推完。**工具做出來了，
> 而第一批四顆帶著迴歸，已回退**。回退的是程式碼，留下的是這份地形圖。
> 見 [[回退程式碼但不回退知識]]（`experience.md`）。

## 一、量到的地形

`allCppConcepts()` 167 顆未膠囊化。逐顆問「把概念／積木／標籤／產生／執行
都剪進膠囊之後，它的身分還會出現在哪些共用檔」：

```
零殘留（剪完就乾淨）                          0 顆   ← ⚠️ 沒有便宜的子集
殘留只落在「已有機制」的三個檔                83 顆
  lifters/io.ts                     54
  core/lifters/strategies.ts        26
  lift-patterns.json                14   （有重疊）
需要新機制                                    84 顆
  core/lifters/statements.ts        24
  templates/universal-templates.json 24
  core/lifters/expressions.ts       20
  core/concepts.json                 8
  core/executors/structs.ts          8
  std/iostream/generators.ts         7
  style-exceptions.ts                7
  core/lifters/declarations.ts       6
  cpp-scaffold-filter.ts             6
  pending-containers.ts              6
```

> **每一顆都還外洩到 lift 那一層——所以 F 不存在「先撿便宜的」這種走法。**

而 `id-migrations.ts`／`topics/*.json` 被護欄豁免（清單類），不算殘留。

## 二、切割器的四個坑（全部實際踩過）

| 坑 | 症狀 |
|---|---|
| **括號配對沒跳過字串／樣板／註解** | 在 `cout << "("` 提早收尾，切出的片段引用不存在的符號 |
| **搬走的段落用到原檔 import 的符號** | `Cannot find name`——要把原檔的 import 行改寫路徑帶過來 |
| **模組層級 helper 沒 export** | 同上，處置是補 `export` 再 import（它們是共用工具，不屬於那顆元件） |
| **標籤鍵的前綴規則猜錯** | 三顆膠囊搬完**一個標籤都沒有**，而護欄當場指名（「沒有標籤的元件在 UI 上是空白的」） |

## 三、⚠️ 第六種形狀：**產生器依 `style` 的閉包**

`registerStatementGenerators` 裡：

```ts
const openBrace = style.brace_style === 'Allman'
  ? (ctx) => `\n${indent(ctx)}{`
  : () => ' {'
```

**它是捕獲 `style` 的閉包**，`statements.ts` 那 24 顆全部依賴它。
剪出去的膠囊拿不到閉包，只能自己從 `style` 算——所以
`componentGenerateRegistrars()` 的呼叫要**把 `style` 一起傳進去**。

⚠️ **少傳不會紅**：排版差異多數測試不比。症狀是
「膠囊化之後大括號的位置變了」，而沒有任何東西會說話。

## 四、⚠️ 一個 NUL 位元組讓 grep 對整個檔失明

`core/generators/statements.ts:2629` 有一個**字面 NUL**（當複合鍵的分隔字元）：

```ts
const key = `${n.conceptId}\x00${normalizeHeader(...)}`
```

git 因此把整個檔判為 binary，`git grep` 只回「Binary file matches」、
shell `grep` 也不列行。**我因此連續得出三個錯誤結論**
（「`openBrace` 不存在」→「檔案被我寫壞了」→ 差點以為是資料損毀）。

✅ **護欄沒有被蒙蔽**：它們走 Node 的 `readFileSync(..., 'utf8')`，NUL 照樣讀得到
（`tests/helpers/component-scan.ts:269`）。瞎掉的只有 shell 工具。

> **一個位元組可以讓一整個檔案從命令列的世界消失，而測試完全正常。**

處置未定：改成 `\0` 逸出（等價、可 grep）看起來是免費的，
但那一行是**熱路徑上的鍵**，要確認沒有人依賴位元組長度。

## 四之二、⚠️ **`strategies.ts` 那 41 顆不是「分派表的一列」**——估計要修

`specs/104` 的形狀表把 `core/lifters/strategies.ts` 的 41 顆列為
「lift 是共用分派表裡的一列 → 登錄呼叫」，而那個結論來自**一個樣本**
（`cpp:vector_declare`，它確實是 container-template 的一列）。

2026-08-11 打開來看其餘的：

```ts
// cpp:cast 與 cpp:malloc 在**同一個函式**裡
if (…) return createNode('cpp:malloc', …)
return createNode('cpp:cast', …)            ← 同一個 strategy 的兩個出口

// cpp:constructor 與 cpp:destructor 同上
if (nameNode?.type === 'destructor_name') return createNode('cpp:destructor', …)
return createNode('cpp:constructor', …)
```

> **它們是真的手寫辨識邏輯，不是資料。**
> 而且**一個函式產出多個身分**——那不是「剪一段出來」的問題，
> 是**要先把函式拆開**，而拆的判準是語法而不是元件。

→ **第七種形狀：一個 lift 函式產出多個身分。**
  處置未定。最小的做法是讓那個函式留在共用檔、由多顆膠囊「共同擁有」
  ——但那違反就近性護欄（身分不得出現在自己資料夾外）。
  另一條是拆成 `if` 前置判斷 ＋ 各自的 `createNode`，各自登錄。

⚠️ **這一條把 83 顆「可立即批次」的估計打掉一大塊**——
`strategies.ts` 那 25 顆（15 ＋ 10）要先解第七種形狀。

## 五、回退的那一批證明了什麼

四顆（`builtin_constant`／`delete`／`loop_do_while`／`throw`）搬完之後：

```
✅ 未膠囊化 167 → 163
✅ tsc 乾淨
❌ 殘差率 0.22% → 0.3%     ← 真的迴歸
❌ 三顆膠囊零標籤
```

**帶著迴歸換 4 顆的數字下降，正是這個專案一直在防的「刷數字」。** 所以回退。

## 六、2026-08-11 稍晚：工具已驗證，第一批三顆進去了

坑一～四全部修掉，`cpp:delete`／`cpp:loop_do_while`／`cpp:throw` 搬完，
**未膠囊化 167 → 164**，全套綠。

順帶掀出**第九個組裝點**（`real-world-programs.test.ts` 自己列舉來源、
漏了元件膠囊）與**第六種形狀**（產生器依 `style` 的閉包，已在
`componentGenerateRegistrars()` 把 `style` 傳進去）。

### 下一批卡在哪（依規模排序）

| 顆數 | 殘留檔 | 需要什麼 |
|---|---|---|
| **53** | `lifters/io.ts` | `call-concepts`／`method-concepts` 已有機制，但要逐顆判斷是哪一種 |
| **25** | `core/lifters/strategies.ts` | ⚠️ **第七種形狀**（一個函式多個身分），見四之二 |
| 7 | `structs.ts` ＋ `strategies.ts` | 同上 |
| 6 | `core/lifters/expressions.ts` | 同 strategies，手寫辨識 |
| 6 | `pending-containers.ts` | 未看 |
| 1 | `lift-patterns.json` | `cpp:builtin_constant`——**卡在寫死的中文 tooltip**，沒有 i18n 鍵 |

**io.ts 那 53 顆是下一個最大且機制已備的批次。**

## 七、第二、三批的結果（2026-08-11 稍晚）

| 批 | 顆數 | 形狀 | 結果 |
|---|---|---|---|
| 二 | **9** | `io.ts` 的**純資料分支**（`if (funcName === 'strcat')` 只做引數 lift ＋ 單一 `createNode`） | ✅ 164 → 155 |
| 三 | 14 → 7 → **0** | `strategies.ts` 的具名策略 | ❌ **整批回退** |

### 第三批為什麼回退

`strategies.ts` 有 18 個具名策略，**14 個只產一種身分**（原本以為全部都是
「一個函式多個身分」，那是錯的——只有 4 個是）。而搬的時候：

```
7 顆  用到 registerCppLiftStrategies **內部的閉包 helper**
       （openBrace 捕獲 style、拆解成員、安裝方法執行器…）→ 搬不動
7 顆  搬得動，而搬完引入誤差迴歸 ＋ 殘留外洩       → 整批回退
```

> **一個「只產一種身分」的策略仍然可能搬不動**——因為它站在別人的閉包裡。
> 第七種形狀要再切一刀：**「產幾種身分」與「用到誰的閉包」是兩個獨立的阻礙**。

### 留下來的機制（獨立成立，不必再造一次）

- `componentLiftStrategyRegistrars()`／`lift-strategy.ts`——膠囊登錄具名策略的入口
- `componentGenerateRegistrars()` 現在把 `style` 傳進去（第六種形狀）
- 批次工具的四個坑全部修好（見第二節）

### ⚠️ 順帶：第三十二條護欄在全套平行時會假紅

它 shell out 到 `g++`，單獨跑 72 秒。全套平行時失敗過一次、單獨跑綠。
**不是迴歸，是時序**——而「偶爾紅一次」比永遠紅更難處理，因為它會訓練人忽略它。

## 相關

- [元件膠囊重構](2026-08-05-元件膠囊重構.md)——F 的設計理由
- `knowledge/skills/component-encapsulate/SKILL.md`——逐顆搬的步驟表（五種形狀）
- [history/047](../../history/047-模組是搬家的中途站不是終點.md)——第五種形狀是怎麼發現的

---

## 八、2026-08-11 全日結果：**167 → 97**（九批）

```
167 → 164  ①  3 顆  lift 是一整筆 pattern（glob 直讀）
164 → 155  ②  9 顆  io.ts 的自由函式**純資料分支**
155 → 147  ③  8 顆  只產一種身分的具名策略      前置：openBraceFor 提升
147 → 144  ④  3 顆  執行器側閉包提升之後才搬得動
144 → 138  ⑤  6 顆  方法路由器的純資料 case      前置：method-concepts 帶槽名
138 → 119  ⑥ 19 顆  **膠囊自己的辨識分支**（帶真邏輯）
119 → 113  ⑦  6 顆  pending-containers 過渡表退場（檔案已刪）
113 → 108  ⑧  5 顆  偵測器補兩種形狀
108 →  97  ⑨ 11 顆  容器方法資料表（**第二張表**，查詢點不同）
```

### 建起來的機制（都已測過，下次直接用）

| 機制 | 收什麼 |
|---|---|
| `call-concepts` | 自由函式：名字 → 身分 ＋ 引數槽名 |
| `method-concepts` | 方法：同上，多一個 `obj` |
| `method-concepts` 的**第二張表** | 容器方法——**查詢點在後段**（要先做型別分派與 `container_kind`） |
| `container-templates` | 宣告的型別名 |
| `lift-patterns` glob 直讀 | 一整筆 pattern |
| `lift-strategy.ts` | 具名策略（lift-pattern 以名字引用） |
| `lift-branches`：`registerCallBranch`／`registerMethodBranch` | **帶真邏輯**的分支 |
| `lift-branches`：`registerAstBranch` | **AST 節點型別**的分支 ⬅ 2026-08-11 建好、已接上 `assignment_expression`，**還沒有膠囊用它** |

### 批次工具踩過的六個坑（全部修好）

1. 括號配對沒跳過字串／樣板／註解
2. 搬走的段落用到原檔 import 的符號 → 要帶過來
3. 模組層級 helper 沒 export
4. **識別字正則不吃中日韓**——這個專案的 helper 是中文命名的
5. **座標兩段式**：先算座標、後改檔案，中間不能插入任何寫入
6. **族前綴比對貪婪**：`CPP_CONTAINER_PUSH` 吃掉 `CPP_CONTAINER_PUSH_BACK_*`

### ⚠️ 每一批都要重問的三件事

- **哪條護欄的掃描範圍沒跟著走？**（目錄要跟、**檔名也要跟**：`executors.ts` vs `execute.ts`）
- **owner 章蓋了嗎？**（核心層沒有 `requires`，要在 `component.json` 明寫）
- **有沒有新的組裝點？**（第九個是 `real-world-programs.test.ts` 列舉來源而不呼叫組裝函式）

## 九、剩下 97 顆卡在哪

```
21  core/lifters/strategies.ts     liftDeclaration 一人產五種
17  core/lifters/expressions.ts    一個 AST 型別多顆身分
 7  structs.ts + strategies.ts     OOP 方法族
 6  core/lifters/statements.ts
 6  core/lifters/declarations.ts   ⬅ **機制已就位**（registerAstBranch）
 …  universal-templates.json 一族
```

**下一步很明確**：`registerAstBranch` 已經接上 `assignment_expression`，
把 `declarations.ts` 那 6 顆的 if 分支剪進各自的膠囊即可——
那是這個機制的第一個實例，驗完就能複製到 `expressions.ts`／`statements.ts`。

⚠️ **`liftDeclaration` 那 21 顆要最後做**：它 220 行、五顆身分共用同一份解析，
不是「剪一段」而是「拆一個函式」。

## 十、第十一批**整批回退**——三顆都卡在同一件事的三種面貌

`expressions.ts`／`statements.ts` 裡有三個 AST 型別**只產一顆身分**
（`update_expression`→`increment`、`comma_expression`→`comma_expr`、
`switch_statement`→`switch`），看起來是最單純的「整段搬」。搬完全部紅：

| 顆 | 卡在 |
|---|---|
| `cpp:increment` | 身分還被 `core/storage-version.ts`（**改名表**）與 `statements.ts` 提到 |
| `cpp:comma_expr` | 外洩三處，而且**一個積木都沒有** → 沒有標籤 → 護欄指名「UI 上是空白的」 |
| `cpp:switch` | 它的 lift 裡提到 `cpp:default`（case 標籤族）→ 反向護欄：**膠囊裡混進別顆元件** |

> **「這個 AST 型別只產一顆身分」不等於「這顆元件只被這個 AST 型別提到」。**
> 前者是**產出**的單一性，後者是**引用**的單一性——搬得動要的是後者。

### 這一條把可搬性的判準補完整了

一顆元件搬得動，要同時滿足**四件事**：

1. 它的五路都切得出來（工具的事）
2. 它的 lift 不與別顆共用一個不可分割的函式（**產出**單一）
3. **沒有別的共用檔還提到它的身分**（**引用**單一）
4. 它有積木、因此有標籤（沒有積木的元件搬進膠囊會被判為「UI 空白」）

前三條這一天都踩過；第四條是 `comma_expr` 這次新增的
——**一顆沒有積木的元件，膠囊契約沒有為它設計過位置。**

---

## 十一、2026-08-11 收工：**167 → 84**（十五批成功、四批回退）

### 剩下 84 顆的**真正**分布——按「還被幾個共用檔提到」

```
0 個   1 顆    cpp:if_else  ⚠️ 而它仍搬不動，見下
1 個  26 顆    15 顆卡在 strategies.ts、3 顆 statements.ts、2 顆 expressions.ts…
2 個  32 顆
3 個   9 顆
4 個   9 顆
5+     7 顆
```

> **這張表比「卡在哪個檔」有用得多**：它說的是**還要清幾次**，
> 而不是「下一次要動哪個檔」。26 顆只差一次。

### 提到最多元件的共用檔（清一個就解鎖一批）

```
30  core/lifters/strategies.ts     ← liftDeclaration 一人產五種
27  lift-patterns.json
24  core/lifters/statements.ts
16  core/lifters/expressions.ts
14  lifters/io.ts
```

### ⚠️ `cpp:if_else` 零殘留卻仍搬不動——第五個條件

它的**產生器是一個共用函式的別名**：

```ts
g.set('cpp:if_else', ifGenerator)   // ifGenerator 是 cpp:if 的產生器
```

剪出去之後 `ifGenerator` 找不到——而它不是模組層級的 helper，
是**另一顆元件的產生器本體**。把它 export 出來等於讓 `if_else` 的膠囊
import `if` 的實作，反向護欄會指名「膠囊裡混進別顆元件」。

→ **可搬性的第五個條件：它的五路實作不得是另一顆元件實作的別名。**

那不是工具問題，是**兩顆元件共用一份實作**——要嘛它們是同一顆（形態），
要嘛實作要拆。`cpp:if_else` 本來就是「路徑滿的但到不了」的第一個實例，
這一條讓它的處置更明確：**它可能根本不該是獨立的一顆**。

---

## 十二、2026-08-11 最終：**167 → 66**（二十五批，六批部分／全部回退）

### ⚠️ OOP 方法族的終端阻礙：**共用消費者依身分分派**

`constructor`／`destructor`／`method_override`／`method_virtual`／
`method_virtual_pure`／`operator_overload` 六顆，lift 各只有**一個建立點**，
execute 也切得出來。而它們全部卡在同一處：

```ts
// core/executors/structs.ts 的 拆解成員
} else if (m.conceptId === 'cpp:constructor') {
  ctor = { … }
} else if (m.conceptId === 'cpp:destructor') {
  dtor = { … }
}
```

那**不是實作散落，是一個合法的消費者**——`class_def` 要知道自己的成員裡
哪一個是建構式，才知道實例化時要跑什麼。

> **可搬性的第六個條件：沒有共用的消費者依它的身分分派。**
>
> 而這一條與前五條不同：前五條是「實作住錯地方」，
> **這一條是「另一顆元件需要認得它」**——那是真的耦合，不是碎裂。

處置要反轉分派：讓每顆元件**宣告自己的角色**（`role: 'constructor'`），
消費者問角色而不問身分。那是 `component.json` 的契約要加一個欄位，
不是搬檔案——**它是 C2（資訊軸）那一類的問題，不是 F 的問題。**

### 全日成果

```
167 → 66   −101（60%）
膠囊 10 → 111
```

**建起來的機制（12 個）**：三張方法表（一般／容器／依型別）、兩張型別表
（容器樣板／型別名）、關鍵字表、`call-concepts`、`lift-patterns` glob 直讀、
`lift-strategy.ts`、`registerCallBranch`／`registerMethodBranch`、
`registerAstBranch`、`registerDeclaratorBranch`、**建構子**。

**可搬性的六個條件**（全部踩過）：

1. 五路都切得出來
2. lift 不與別顆共用一個不可分割的函式（**產出**單一）
3. 沒有別的共用檔還提到它（**引用**單一）
4. 它有積木、因此有標籤
5. 五路實作不是另一顆元件實作的別名
6. **沒有共用的消費者依它的身分分派** ⬅ 最後一條，而它是真的耦合

### ⚠️ 一個沒有被機械化的約定，今天代價六次

`block-input-names.ts`、`blockdef-consistency`、`unified-extractor-dynamic`、
`unified-extractor-migration`、`unified-extractor`、`level-switching`、
`code-to-blocks`、`real-world-programs`——**八處**自己列舉宣告來源。

它們的失敗訊息**全部與根因無關**（「must use CONDITION, not COND」、
「ELSEIF 子節點少一個」、「expected true to be false」、
「expected [cpp_raw_code] to include cpp_loop_for」）。

> **這不是八個 bug，是一個沒有被機械化的約定**：
> 「要一份完整的宣告，就呼叫組裝函式」——而沒有任何東西擋得住第九處。

→ **下一條該蓋的護欄**：量「有幾個地方在列舉宣告來源而不呼叫
  `allCppConcepts()`／`allCppProjections()`」。今天實測是 8。

### ⚠️ 條件 6 的處置試過了，**而它有時序問題**（同日回退）

設計是對的：`component.json` 加一個 `memberRole` 欄位，`structs.ts` 的
`拆解成員` 改成問角色不問身分。實作了、加了過渡表（`pending-member-roles.ts`，
照 `pending-containers` 的形狀），而**跑起來 16 支紅**。

根因是**登錄時序**：

```
過渡角色登錄在 registerCppLifters 裡
而 structs.ts 的執行器可能更早跑，或測試根本沒呼叫 registerCppLifters
```

那正是這個專案記過的病——**「把資料做成登錄呼叫，等於替它發明一個
會忘記呼叫的時序」**（第三顆膠囊的教訓②）。而我在同一天又犯了一次。

> **判準（那條教訓自己寫的）：這個東西有沒有人要「查」它？**
> 有 →登錄。**而登錄要問「誰保證登錄先於查詢」**。
>
> 這一次的答案是「沒有人保證」——`memberRole` 該走
> **glob 直讀**（與 `component.json` 同類），不是登錄呼叫。

→ **下一次的做法**：`memberRoleOf` 直接讀 `registeredComponents()`（那是 glob，
  沒有時序），過渡表改成一個 **JSON 檔**由同一個 glob 讀進來，
  而不是一個要人呼叫的 `register…()`。

### ✅ 條件 6 的機制做好了（glob 直讀版），**而那六顆仍搬不動**

第二版把時序拿掉：`memberRoleOf` 直接讀 `registeredComponents()`（glob eager），
過渡表是一個**靜態 import 的 JSON**（`pending-member-roles.json`）而不是登錄呼叫。
**一次過，全套綠**，已 commit。

而拿它去搬那六顆，仍然紅——因為條件 6 不是它們唯一的阻礙：

```
core/executors/structs.ts    ✅ 解開了（問角色不問身分）
core/generators/statements.ts ❌ 還在依身分產生類別成員
core/lifters/strategies.ts    ❌ 還在依身分辨識類別成員
```

> **一顆元件被三個共用消費者依身分分派時，解開一個不會讓它變得可搬。**
> 而每一個都要用同一招（宣告角色、消費者問角色）分別解開。

順帶第三次閉包提升：`formatParams`（`registerStatementGenerators` 內部）。
**同一個檔案裡的第二個閉包 helper**——`openBrace` 是第一個。

### 這一天最後的數字

```
F        167 → 66（−101，60%）
膠囊     10 → 111
批次     二十七批，八批部分／全部回退
機制     13 個（＋ memberRole 角色宣告）
```
