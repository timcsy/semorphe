# 已退場的 draft

路線圖項目完成之後，它的**在建設計理由**（draft）反流並退場，搬到這裡。

**為什麼不刪**：`knowie` 的規矩是「**記轉變，不刪舊的**」。這些檔案保存的是
**被推翻的過程與被否決的選項**——那是結論裡看不到的東西。

**為什麼不留在 `draft/`**：`draft/` 是**短期記憶**，裡面的東西應該是未決的。
一份已經完成的設計留在那裡，會讓人以為它還開著。
