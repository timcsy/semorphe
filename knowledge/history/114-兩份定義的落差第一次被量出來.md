# 114：兩份定義的落差第一次被量出來——而它是 19 筆

**日期**：2026-08-20 ｜ **spec**：163 ｜ **前一步**：[113](113-中立性第二維第一次下降.md)

## 轉變

| | 之前 | 之後 |
|---|---|---|
| 「宣告與命令式說得一不一樣」 | 🔴 **沒有人量過** | 🟢 **4 一樣、19 有差異，逐筆印出差在哪** |
| 中立性第二維 | 32 | 🎯 **28** |
| 雙重真相 | 33 | 🎯 **29** |

## 為什麼要先量

`CLAUDE.md` 逐字：

> **雙重真相來源**……修改任一方時**必須同步另一方**。
> **PatternRenderer fallback**：若 JSON 名稱錯誤，
> **只在 Block Style 切換（serialize→deserialize）時才暴露**。

而今天的真相是**命令式那份沒有守衛**（直接賦值、後跑）→ **使用者看到的是它**；
宣告式那份有守衛且先跑 → **它躺著**。

> **所以「刪掉命令式」＝「換成宣告式」，那是一次行為改動，不是清理。**

🎯 於是先蓋比對護欄——**第一支真的跑 `BlockRegistrar.registerAll()` 的測試**
（在此之前所有 `block-registrar` 的測試都只掃檔案文字）。

## 那 19 筆的性質

```
cpp_break        命令式【少了 nextStatement】——🔴 宣告才是對的
cpp_comment      「註解：」vs「備註：」——標籤字不同
cpp_func_call    output null vs Expression——兩顆不同的積木被比在一起
cpp_func_def     插槽 4 個 vs 1 個——形狀差很多，宣告表達不完
```

🔴 **不准用「把宣告改成跟命令式一樣」來還數字**——那是在假設命令式是對的。

## 三個把我自己騙過去的地方

三條都進了 [experience](../experience.md)：

1. **「207 顆一模一樣」是灌水的**——分母裡混進了沒有對照組的個案
2. **「8 顆宣告不完整」是測試環境少了一步**——`%{BKY_X}` 沒載入，`jsonInit` 展不開
3. 🔴 **一次錯誤的歸因**：實測看到全降級，我用 `git stash` 做「對照實驗」並下結論
   說是自己造成的迴歸——**而那次的目標停在 Python**。
   同時換了程式碼與頁面狀態，**證明不了任何事**，而我差一點把一個好的改動還原掉。
