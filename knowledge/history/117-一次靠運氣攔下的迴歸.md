# 117：一次靠運氣攔下的迴歸——而護欄現在自己看得見

**日期**：2026-08-20 ｜ **spec**：166 ｜ **前一步**：[116](116-比對器四次指控宣告而四次都是它自己缺東西.md)

## 轉變

| | 之前 | 之後 |
|---|---|---|
| 比對的維度 | 4 | **5（＋載入時的狀態）** |
| `cpp_raw_code` | 🔴 被判「一模一樣，**可刪**」 | 🟢 「載入時的狀態 `nodeType,unresolved` —— 宣告表達不出」 |
| 中立性第二維 | 20 | 19 |
| 雙重真相 | 20 | 19 |

## 起因

spec 165 裡比對護欄說 `cpp_raw_code` 可刪，**而它不能刪**——
`loadExtraState` 會依 `degradationCause` 換顏色與 tooltip。

⚠️ **我發現它的方式是 `tsc` 抱怨 `DEGRADATION_VISUALS` 變成未用的 import。**

> **一次靠運氣攔下的迴歸，下一次不會有那個運氣。**

## 而第一版的新維度**沒有讓任何東西變紅**

注射：拿掉宣告的 `extraStateFlags`、整條關掉那一維——**棘輪都是綠的**，
因為那些注射讓數字**下降**。

> **一條只擋「變差」的棘輪，擋不住「量得更少」。**

🟢 補了一支**指名**的斷言。四針注射裡最重要的是
「**假裝宣告表達得出來刷數字**」→ 當場紅。

## 兩次猜錯，兩次被護欄糾正

```
猜  cpp_var_assign_compound 有「載入時才長出來的東西」
錯  它的 hasIndex 由 extraStateFlags 表達得出——護欄【正確地沒報它】

猜  表達不出的是 var_declare／if／raw_code／doc_comment
錯  實際是 doc_comment／if／if_else／raw_code
```

> **一個猜測被護欄否證，比被護欄證實更有價值。**

三條教訓進了 [experience](../experience.md)，其中一條是關於**錨點自己會說謊**
（它寫 `> 10`，而清到剩 10 顆的那天紅了，訊息還說「registerAll 沒跑起來」）。
