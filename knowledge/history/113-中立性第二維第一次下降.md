# 113：中立性第二維第一次下降——而它靠的是「一次只還一種形狀」

**日期**：2026-08-20 ｜ **spec**：162 ｜ **前一步**：[112](112-加一個語言的代價第一次有數字.md)

## 轉變

| | 之前 | 之後 |
|---|---|---|
| **中立性第二維** | 🔴 33（從 spec 153 立起來就沒動過） | 🎯 **32** |
| 雙重真相 | 🔴 34 | 🟢 33 |
| `print(a, b)` | 🔴 走不完 | 🟢 兩條路徑都走得完（瀏覽器實測） |
| 那支「偶發」 | 🔴 一個 session 紅四次 | 🟢 負載下 4/4 |

## 兩半，而只有一半接上了

```
renderMapping.dynamicRules
  🟢 語義 → 積木狀態    PatternRenderer／PatternExtractor 讀它（16 顆膠囊已宣告）
  🔴 積木型別的【定義】  block-registrar 讀它 0 次
```

`python_print` 在 spec 160 就是死在這一半（`missing a(n) EXPR0 connection`）
——**宣告有了，沒有人照著它建那些 input**。

## 而判準從「推論」改成「顯式選入」

第一版：「有單一 `inputPattern` 且沒有 `message0`」就接管。
🔴 `cpp_vector_declare` 也符合，而它另有型別下拉與名稱欄位
——宣告式建構器會**把它們全部弄丟**，而**沒有任何測試在看標籤與欄位**。

> **「符合這個形狀」不等於「只有這個形狀」。**

🟢 改成 `builder: "variadic"`，**一顆一顆選入，每顆都開瀏覽器看**。
所以這一刀只還了 **2 顆**，不是 16 顆——其餘的形狀**還沒有被宣告表達出來**。

## 代價，要誠實記

- 只選入 2 顆。「16 顆全退場」**沒有做到**，而理由寫在 spec 裡
- 第 0 步花在一支「偶發」上——而**對照實驗**證明它不是偶發（見 [experience](../experience.md)）

四條教訓進了 experience，其中兩條是**注射逼出來的**（`loadExtraState` 沒人守、
測試繞過產品入口）。
