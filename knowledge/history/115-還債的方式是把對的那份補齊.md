# 115：還債的方式是「把對的那份補齊」，不是「刪掉」

**日期**：2026-08-20 ｜ **spec**：164 ｜ **前一步**：[114](114-兩份定義的落差第一次被量出來.md)

## 轉變

| | 之前 | 之後 |
|---|---|---|
| 落差 | 19 | **17** |
| 中立性第二維 | 28 | 🎯 **25** |
| 雙重真相 | 29 | 🎯 **26** |
| 「怎麼還」這件事 | 🔴 以為是「刪掉命令式」 | 🟢 **「把對的那一份補齊，另一份才變成多餘的」** |

## 三顆，三種不同的還法

```
cpp_break／cpp_return   修【宣告】     它多了 nextStatement，而那之後是不可達的程式碼
cpp_var_ref             補【機制】     它的下拉跟著工作區長，而宣告只表達得出死文字框
                                      → field_dynamic_dropdown（source 由組裝點注入）
```

⚠️ 而 `cpp_break` **我第一次判反了**——更正記在 [114](114-兩份定義的落差第一次被量出來.md) 的追記。

## 順手拆掉的地雷

`cpp_func_call` 與 `cpp_func_call_expression` 的宣告**完全對調**，
而**使用者看不到**（命令式蓋住了）。**而這一系列刀正在刪那些命令式定義。**

> **一份被覆蓋著的錯誤宣告，是一顆等著下一次清理踩上去的地雷。**

## 這一天的第二次「量測方法本身出錯」

```
上午  對照實驗同時換了程式碼與頁面狀態  → 誤判成迴歸，差點把好的改動還原掉
下午  切 Block Style 後拿著舊的 workspace 參照 → 誤報「積木不見了」兩次
```

> **搞錯的不是產品，是我量它的方式**——而那種錯的症狀長得跟真的缺陷一模一樣。

三條都進了 [experience](../experience.md)。

## 而這個操作被 skill 化了

`retire-imperative-block`（第三次重複時建的）——它把「先比對、判哪一邊對、
補齊對的那份、才刪、然後開瀏覽器看」寫成一條路，
並記下**量測本身最容易出錯的兩處**。
