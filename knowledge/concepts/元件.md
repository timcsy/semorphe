# 元件（Component）

**元件是語義真實在系統裡的單位**——有身分、有埠、有參數，並向外投影出形態與行為。「一個積木」「一顆零件」「一個 UI 按鈕」是同一個東西在三個域的名字。

這個概念的用途不是當口號（「一切都是元件」泛到能裝任何東西 = 沒有剪枝力的假母概念），而是當**一張五槽模板**：填得出就白拿整套機器（登錄／投影／分步播放／符合性／跨域組裝），填不出就代表那個域不是元件，剪掉。

**三視角投影：**

- **原則**：根公理直接推論——語義結構是唯一真實，元件是那個結構的節點；投影是開放集合
- **願景**：跨域合流（軟體／硬體／UI）的整合契約，讓工具蓋一次 N 域通用
- **經驗**：碎裂與雙重真相的根因，是「同一個元件的槽散在按機制組織的檔案裡」

## 五槽

一個元件由五個槽組成。前三槽是**真實**（不是投影），後兩槽是**投影**：

| 槽 | 內容 | 是真實還是投影 |
|---|---|---|
| **身分** | `componentId`——登錄表的鍵，跨域唯一 | 真實 |
| **埠** | 具名介面，帶種類與資源語義 | 真實 |
| **參數** | 型別側宣告（規格）＋ 實例側存值 | 真實 |
| **形態** | 積木／文字／2D／3D／URDF——「長什麼樣」 | 投影 |
| **行為** | 直譯執行／電氣傳播／物理模擬——「做什麼」 | 投影 |

**組裝**（哪些實例經哪些埠相連）也是真實，但它不屬於單一元件，屬於元件之間。

埠是語義的一部分、與檢視無關——這是判斷一個東西該進哪一槽的試金石：**換一個檢視它會變嗎？會變就是投影，不會就是真實。**

## 真實與投影：兩族並列

根公理已經列出三種投影：

```
「x++;」              ← 文字投影
[變數 x 加1（++）]     ← 視覺投影
 x: 0→1               ← 行為投影
```

前兩者是**形態**，第三者是**行為**。**這兩族並列，不互相包含。**

一個常見的誤推是「行為也是投影，所以行為屬於『表示』」——不成立。「投影」是兩族的**上位詞**（公理層），不是其中任一族的欄位名。若讓形態那一槽吃掉行為，它就退化成「所有非身分的東西」的雜物袋，失去剪枝力。

反過來，這也解釋了為何 `representations` 這個欄位名不好：它只涵蓋形態，卻佔著一個看起來像上位詞的位置。**上位詞是「投影」，形態那一槽就老實叫形態。**

## 否決：行為軌跡不是真實

一個誘人但錯誤的提案是：把「訊號／狀態隨時間的行為軌跡」當成共享的真實，而程式碼／硬體／模擬／執行是「產生它／承載它／算出它／實現它」的四個角色。

**否決，理由與 [墓碑](../history/014-墓碑目錄.md) 否決 λ-core 完全同構：**

以軌跡為真實，`count_loop` 和手寫 `while` ＋ 計數器會塌成同一個東西——它們產生**相同的軌跡**，卻是**不同的概念**。真實變成軌跡 → 區別消失 → 可逆性從 R0 掉到 R1 → 摧毀語義意圖。這就是 lowering，只是換上物理學的外衣。

**正解：行為軌跡是行為投影吐出來的東西，不是真實。** 那四個角色不是「四者對真實的角色」，而是**同一個元件的四個投影去向**。真實始終是元件。

## 名詞表（跨域唯一）

| 角色 | 中文 | 程式 | 取代 |
|---|---|---|---|
| 承載意義的單位（型別） | **元件** | `Component` / `ComponentDef` | `BlockSpec`、`ConceptDefJSON`、`PartDefinition` |
| 身分鍵 | — | **`componentId`** | `conceptId`、`typeId`、`SemanticNode.concept` |
| 具現進組裝的個體 | **實例** | `Instance` | `SemanticNode`、`Component`（ArduinoCAD 現用） |
| 語義介面 | **埠** | `ports`（帶 `kind`） | `children`、`pins`、`ports`（機械專用） |
| 埠的資源語義 | 資訊埠／守恆埠 | `resource: 'information' \| 'conserved'` | （原無欄位） |
| 埠之間的連接 | **連接** | `Link`（電氣特例＝`Net`） | — |
| 型別側參數宣告 | **參數規格** | `ParamSpec` | `properties: string[]`、`AttrSpec` |
| 實例側參數值 | **參數** | `params` | `properties: Record<…>`、`attrs` |
| 「長什麼樣」那族 | **形態** | `forms` | `representations` |
| 「做什麼」那族 | **行為** | `behavior.step` | `executors` |
| 兩族的上位詞（公理詞） | **投影** | — | — |
| 登錄表 | 元件登錄 | `ComponentRegistry` | `BlockSpecRegistry` |
| 投影忠實度不變式 | **符合性** | `conformance` | （原無名） |

三個刻意的取捨：

- **`params` 而非 `attrs` 或 `properties`**——`properties` 在現行碼中同名異義（型別側是 `string[]`、實例側是 `Record`），且被 HTML/OOP 污染；`attrs` 撞 HTML attribute。
- **`Instance` 而非 `Node`**——`node`／節點在硬體域已被 Net 佔用（電路學的 node 就是 net），不與電子學慣例打架。
- **`step` 保留**——對齊 FMI `doStep`，白拿工業接縫。

## 為何是「元件」不是「概念」

`concept` 曾是首選（它連著根公理的「概念節點」），四條理由推翻它：

1. **`concept` 是 C++20 的關鍵字**（`template<typename T> concept Integral = …`）。Semorphe 主要是 C++ 工具，同一畫面上會出現兩個 concept——這是教育 telos 上的實傷。
2. **「概念」聽起來在腦子裡**，但它是系統裡最真實的東西。
3. **它不蘊含埠與可組裝**，`component` 天生蘊含。
4. **三域通用性**——電子元件／軟體元件／UI component 是唯一一個三個域都通用的詞。學習遷移（一個心智模型教一次通吃）只有它兌現得了。

**代價，要誠實記**：`concept` 帶著一個 `component` 給不了的意思——**「發現而非發明」**。那句話是承重的，它是墓碑否決 λ-core 的核心論據。

**解法是分工，但只有一個進資料結構**：

- **元件** = 系統裡的單位。有 `componentId`、進登錄表、被實例化、有埠有參數有投影。**唯一的技術名詞。**
- **概念** = 一個元件**所承載的意義**。只活在原則、墓碑、教學語言裡，**不是欄位、不是 id、不是型別**。句子長這樣：「這顆元件承載 `count_loop` 這個概念」。

這樣墓碑的論據原封不動（真實是語義概念），資料結構拿到三域通用的名字。這個分工本身就是根公理的形狀——**概念是意義，元件是它在系統裡的身分**。

其餘死掉的候選與死因：`Entity`（軟體界一律指實例，撞「實例」）、`Unit`（無剪枝力，撞 unit test）、`Archetype`（已被參數化 CAD 佔用）、`Category`（撞範疇論＋toolbox category）、`Module`（撞 std 模組）、`Noumenon`（哲學正確但過重）、`Sememe`（語言學上是 morpheme 的對偶、正好對上 σῆμα＋μορφή，但它指「**最小**意義單位」而概念可分解，語義錯了——etymology 誘人但不是論證）。造新詞一律否。

## 判準：哪些名詞必須統一

整合契約必須**薄**——只標窄接縫，域內部當不透明酬載。厚協定要罩住硬體＋軟體＋UI，會薄弱到近乎空。

**這條的命名推論——一個名詞要不要跨域統一，看兩件事：**

1. **跨域機器需不需要「理解」它的語義？**（登錄表／圖鑑／分步播放／符合性／跨域接線）
2. **學習者會不會跨域碰到它？**（教育 telos——學習遷移靠的是同一個心智模型搬到所有域）

任一為真 → 統一。兩者皆否 → 域內俗名，統一反而是厚協定。

注意判準問的是「**需不需要理解**」，不是「讀不讀」。反例：`role: 'statement' | 'expression'` 被登錄表**讀**著，但硬體域根本沒有這個分野——登錄表可以原封不動搬運它而不理解它（不透明酬載）。用「讀不讀」當判準會產生偽陽性，用「理解不理解」才剪得對。

於是「零件 vs 積木 vs 按鈕」的爭論消失——那是域內俗名。真正要統一的只有上面那張表。

## 剪枝力：填不出就不是元件

母概念的價值在它**排除**什麼。對一個新的域，逐槽填：

**身分？埠＋埠的資源語義？形態＋符合性不變式？行為＋時鐘？組裝（樹或圖）？**

- 填得出 → 學到具體東西，且白拿整套機器
- 填不出 → **這個域不是元件**，別硬套

用 UI 域驗證（三域中業界最成熟的一域）：

| 槽 | UI 域 | |
|---|---|---|
| 身分 | `Button` 這個型別 | ✓ |
| 埠 | props（入）／events（出）／slots（結構） | ✓ |
| 埠的資源語義 | 全是**資訊埠**（可複製廣播、無反作用） | ✓ 而且它**解釋了**為何 UI 最好教 |
| 形態 | 渲染的 DOM ／ JSX 文字 | ✓ |
| 行為 | state ＋ 生命週期 | ✓ |
| 組裝 | 樹（slots 嵌套）＋ 圖（event→state→prop） | ✓ |
| 符合性 | props 名 == 埠名 | ✓ |

七格全填得出，且**填的過程學到了新東西**（UI 埠全是資訊埠，這解釋了它為何最乾淨好教）——這才是剪枝力兌現的樣子，不是口號。

## 組裝：圖是本體，樹是特例

根公理已經寫死了這件事：

> 程式是由「概念節點」和「關係邊」組成的語義結構：**在檔案內是樹，在檔案間是圖，在系統間是超圖**。

所以「組裝是圖」不是新決定，是**還原根公理**——現行 `SemanticNode` 只有 `children`、沒有邊，是實作欠公理的債。

`children: Record<string, Instance[]>` 的真身是**containment 型的埠**。統一後：實例有埠，埠之間有邊，邊帶種類（containment／dataflow／electrical／mechanical）。三個平行陣列（`children`／`pins`／機械 `ports`）收成一個 `ports` 帶判別聯集。

**檢查它不違反墓碑**：`count_loop` 還是 `count_loop`，condition／body 還是具名埠，**沒有任何語義意圖被降級**——只是把「巢狀所隱含的關係」寫明。Sc2／Sc3 不破，可逆性不變。通過。

> ⚠️ ArduinoCAD 的經驗有一條「**加法式併行通道 >> 加寬共享型別（blast radius）**」，字面上反對這個合併。但那條的理由明寫是 blast radius，是**成本壓力下的增量擴充啟發式**。它適用於未來的增量改動，**不適用於地基決定**。別拿它擋這一條，也別因為這一條就丟掉它。

## 現況落差（採用此表要改什麼）

| 名詞表 | 現行碼 |
|---|---|
| `componentId` | `SemanticNode.concept`（欄位名與型別 `ConceptId` 就已不一致） |
| `Instance` | `SemanticNode` |
| `params` / `ParamSpec` | `properties` 型別側 `string[]`（**只有名字，沒有型別、範圍、預設值**）／實例側 `Record` |
| `ports` | `children`（僅 containment，無邊、無 kind、無資源語義） |
| `forms` | `blockDef` ／ `codeTemplate` ／ `astPattern` 分屬三個機制 |
| `ComponentRegistry` | `BlockSpecRegistry`（**以投影命名登錄表**，且有 `byConceptId`／`byBlockType` 雙索引） |
| `conformance` | 無名，只有零星 guard test |

`params` 那一列不只是改名——型別側只有名字清單，**沒有型別、沒有範圍、沒有預設值**，正是「複合型別字串解析」那條技術債的根因，也是 [元件膠囊重構](../draft/2026-08-05-元件膠囊重構.md) 說的「結構化 property」增量。**改名時應連結構一起借過來。**

## 待人拍板的一件事

根公理現行措辭是「**概念節點**和關係邊」「節點的**概念類型**和屬性是語義」。採用這張表意味著根公理的措辭要跟著動（概念類型 → 元件身分）。

**根公理的 churn 應趨近零，此事 MUST 由人拍板，不得順手改。** 本檔先立詞彙，`principles.md` 不動。

## 相關

- [投影](投影.md)——投影管線、可逆性分級 R0-R4、「執行也是投影」
- [概念代數](概念代數.md)——分層、五路完備性、屬性的結構化邊界（`ConceptRegistry` 一詞出自此）
- [開放擴充](開放擴充.md)——P3 多層引擎是「宣告式優先＋就近 escape hatch」的依據
- [墓碑目錄](../history/014-墓碑目錄.md)——λ-core 被否決的論據，本檔「行為軌跡不是真實」與它同構
- [元件膠囊重構](../draft/2026-08-05-元件膠囊重構.md)——按元件聚合的重構提案；此表是它的詞彙地基
- ArduinoCAD `knowledge/`——五槽、符合性、行為 seam、埠的資源語義（#B）皆源自該專案的實證
