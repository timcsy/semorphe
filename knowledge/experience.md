# 經驗

從 Semorphe 開發中蒸餾出的教訓——理論與實作碰撞後留下的東西。完整的因果軌跡在 [history/](history/)，完整的除錯現場在 [episodes/](episodes/)。

## 教訓

### 型別分裂在「兩邊都能跑」時最危險

- **理論說**：根公理定義了 SemanticNode 的標準形式，實作照著寫就好。
- **實際發生**：同時存在兩套不相容的 SemanticNode 定義（`children` 一個是 `SemanticNode[]`、一個是 `SemanticNode | SemanticNode[]`）。TypeScript 的 structural typing 讓聯合型別在某些用法下相容，**靜態檢查不報錯**，問題直到 runtime 才爆炸。interpreter 因此塞滿 `as SemanticNode` 斷言和 `Array.isArray()` 判斷。
- **解決方式**：統一為單一定義（陣列版 children），刪除 `semantic-model.ts`，工具函式遷入 `semantic-tree.ts`，約 15 個檔案的 import 更新 + 所有測試重寫。
- **教訓**：型別分裂是漸進開發的自然結果，關鍵是**發現時立刻統一，不要因為「兩邊都能跑」就容忍共存**。structural typing 會隱藏不一致，必須靠 review 或定期掃描發現。
- **來源**：[history/001-兩套-semanticnode-型別統一.md](history/001-兩套-semanticnode-型別統一.md)

### R0 雙射是設計目標，不是工程現實

- **理論說**：P1 定義 blocks 投影為 R0（雙射），`lift(project(T)) ≡ T`，投影是純函數。
- **實際發生**：Blockly 有大量有狀態的副作用——動態積木的 FieldDropdown 選項來自即時掃描 workspace（不是 `project(tree)` 能覆蓋的）、mutator 的 `decompose()`/`compose()` 生命週期由 Blockly 控制、積木的視覺狀態和語義資訊被框架混在一起。
- **解決方式**：承認投影不純但確保語義層等價——用 `const self = this` 把 workspace 狀態注入投影過程；用 `extraState` 補語義樹與積木之間的間隙；把反向過程命名為 `extractBlockInner()` 而非 `lift()`，在命名上承認它不是理論中的純投影反函數。
- **教訓**：在第三方 UI 框架中，投影管線必須適應框架的狀態模型。**正確做法是確保 roundtrip 結果在語義層等價，即使中間經過了有狀態的中介。**
- **來源**：[concepts/積木投影管線.md](concepts/積木投影管線.md)

### 四路完備性需要自動化驗證，不能靠記憶

- **理論說**：P2 要求每個概念有四條路徑（lift → render → extract → generate）。
- **實際發生**：加入 `u_negate` 積木時完成了 BlockSpec、動態積木、generator、測試，卻忘記在 `UNIVERSAL_CONCEPTS` registry 註冊。結果 diagnostics 報「unknown concept」、code-to-blocks 轉換失敗，但**積木本身能正常使用**，容易讓人以為沒問題。根因是四條路徑分散在不同檔案，沒有統一檢查點。
- **解決方式**：補上 registry 條目，並建立認知——新增任何概念時第一步就是註冊。
- **教訓**：**靠開發者記憶力不可靠。** 應寫測試或 lint 規則：遍歷所有 BlockSpec 的 `conceptId`，檢查每個都在 registry 中存在。
- **來源**：[concepts/概念代數.md](concepts/概念代數.md)

### P4 在理論上是一維的，在實作中是多維的

- **理論說**：漸進揭露只要 `level <= currentLevel` 就能決定積木是否顯示。
- **實際發生**：IO 偏好（iostream vs cstdio）需要額外維度；某些積木只在特定 Code Style 下有意義；語言專屬積木和 universal 積木的 level 需要協調。而且切到 L0 時 workspace 上可能已有 L1 積木，使用者看到 toolbox 沒有卻在畫布上有，會困惑。
- **解決方式**：`buildToolbox()` 實作多維度過濾（level × category × ioPreference × language）；workspace 既有積木不受層級切換影響（只影響 toolbox 可用性）；level 值由教育判斷決定並記錄在 BlockSpec，工程只負責正確過濾。
- **教訓**：**不要試圖用單一 level 數字解決所有問題。** 接受過濾邏輯天然需要多個正交維度，但維度之間的職責必須分清（Topic 管層級樹結構、Level 管可見性、View Params 管呈現）。
- **來源**：[concepts/漸進揭露.md](concepts/漸進揭露.md)

### 「只加 JSON 不改程式碼」不等於「不影響既有行為」

- **理論說**：P3 開放擴充——新概念可加入而不破壞既有結構。
- **實際發生**：`c_pointer_op` 的 astPattern 宣告 `nodeType: "unary_expression"` 但 constraints 為空，結果 `++i`（tree-sitter 也分類為 unary_expression）被搶匹配成指標操作而非 increment。Pattern Engine 按登記順序嘗試，**先匹配到的就贏**。
- **解決方式**：禁止空 constraints（除非該 nodeType 全局唯一）；歧義改在**註冊時**偏序仲裁而非執行時碰運氣。這個經驗直接推動了第一性原理的修訂。
- **教訓**：**禁止歧義比仲裁歧義更安全。** 開放擴充的前提是新 pattern 不能改變既有 pattern 的匹配結果——這個保證必須由註冊時的檢查提供，不能靠開發者自律。
- **來源**：[history/005-pattern-歧義從禁止到偏序仲裁.md](history/005-pattern-歧義從禁止到偏序仲裁.md)

### 第一性原理提供方向，不提供具體設計

- **理論說**：CLT 說「最小化外在認知負荷」，Sc3 說「一個積木 = 一個語法結構」。
- **實際發生**：怎麼最小化需要反覆試錯。積木文字 `"宣告 %1 型別 變數 %2"` 讀起來生硬，改成 `"宣告 %1 變數 %2"` 又讓 `%1` 語義模糊；變數名用 FieldTextInput 會讓初學者輸入不合法名稱，改成 FieldDropdown 又在 workspace 沒有變數時讓 Blockly 拋錯；I/O 參數從純 ValueInput（太複雜）到純 Dropdown（無法輸入常數）走了三個版本才收斂到三模式。
- **解決方式**：message 拆成獨立 label key，每段都有明確語義；dropdown 加 fallback 選項；三模式（select/compose/custom）讓簡單情境用最低認知負荷、複雜情境保有完整表達力。
- **教訓**：**積木文字和互動模式的設計本質上是 UX 問題，不能純靠理論推導。** 原理負責否決錯誤方向，不負責生出正確設計。
- **來源**：[history/012-io-參數從單一模式到三模式.md](history/012-io-參數從單一模式到三模式.md)

### 需要 parse 回結構才能用的字串，就不該是字串

- **理論說**：P2 定義 properties 為 `Record<string, PropertyValue>`，但沒規定何時用字串、何時用結構化物件。
- **實際發生**：`func_def` 的 params 存成字串陣列 `["long long base", "int y"]`。同一份字串在四個環節被不同方式解讀——lifter 整串存入、generator 照原樣拼接（正確）、renderer 用 `split(/\s+/)[0]` 取型別（`long long` 被截斷成 `long`）、extractor 從 UI 欄位讀（正確）。
- **解決方式**：短期用已知複合型別清單做前綴匹配（從最長候選開始）；長期應結構化為 `[{ type: "long long", name: "base" }]`。
  → **✅ 長期那條已經做了**（2026-08-07 實測確認）：`func_def` 的參數現在是 `param_decl` 子節點，`{type: "long long", name: "base"}`／`{type: "const string&"}`／`{type: "int*"}` 全部完整。**這一欄曾經過期**——教訓成立，而狀態寫著一個已經付掉的債。
- **教訓**：**判定準則——如果一個屬性值在管線中需要被拆分或解析，它就不該是字串。** 壓扁的唯一合理場景是該值只會被完整傳遞、不會被拆分。
- **來源**：[concepts/概念代數.md](concepts/概念代數.md)

### 複製貼上積木定義是序列化契約分裂的溫床

- **理論說**：同一概念的 statement 版和 expression 版只是投影差異，語義相同。
- **實際發生**：`u_input_expr` 是從 `u_input` 複製貼上再改的。`u_input` 的 `saveExtraState` 讀 `getFieldValue('SEL_i')` 存成 `{ text }`，`u_input_expr` 卻直接複製內部狀態 `argSlots_`，而 dropdown validator 寫入的是 `selectedVar` 不是 `text`。`STATEMENT_TO_EXPRESSION` 映射直接搬移 extraState，欄位名不一致導致 `cin >> s` 變成 `cin >> x`——**所有變數 fallback 成預設值**。
- **解決方式**：兩版本的 `saveExtraState`/`loadExtraState` 格式統一，抽成共用函式而非各自實作。
- **教訓**：**序列化格式是隱式的 API 契約**，違反不會在編譯期報錯，只在特定語境（如 if 條件中的 cin）才暴露。新增雙版本積木時共用序列化函式，不要複製貼上。
- **來源**：[episodes/2026-03-12-cin-變數名靜默降級.md](episodes/2026-03-12-cin-變數名靜默降級.md)

### 功能完成 ≠ 功能接通

- **理論說**：auto-include 的元件都寫好了——ModuleRegistry、`computeAutoIncludes()`、工廠函式、`GeneratorContext.moduleRegistry` 欄位。
- **實際發生**：UI 層從來沒呼叫 `setModuleRegistry()`。`GeneratorContext` 建構時 `moduleRegistry` 永遠是 `undefined`，`if (ctx.moduleRegistry)` 永遠不進去。**所有單元測試都通過**（因為測試手動注入了 registry），但瀏覽器中功能完全無效。同一時期 `ModuleRegistry.register()` 還寫錯欄位名（`concept.id` 而非 `concept.conceptId`），structural typing 沒報錯，registry 靜默註冊了一堆 `undefined` 當 key。
- **解決方式**：加入全域 setter 並在初始化時呼叫；修正欄位名；認識到依賴解析架構上該抽離為獨立的 Scaffold 層。
- **教訓**：**端到端驗證不可省**——單元測試手動注入依賴會繞過真實的接線路徑。**全域 setter 模式的風險**：呼叫順序和遺漏都無法被型別系統捕獲。
- **來源**：[episodes/2026-03-10-auto-include-寫好了卻沒接上.md](episodes/2026-03-10-auto-include-寫好了卻沒接上.md)

### 四路完備性不等於可執行

- **理論說**：P2 的四路完備性（lift → render → extract → generate）保證概念正確。
- **實際發生**（現場：[episodes/2026-03-12-cmath-pow-回傳0](episodes/2026-03-12-cmath-pow-回傳0.md)）：`<cmath>` 概念完成四路、round-trip 測試全過，但使用者按「執行」時 `pow()` 算出 0，二次方程式解錯。原因是「執行」走的是 `SemanticInterpreter` 而非 C++ 編譯器，而這些概念沒註冊 interpreter executor——interpreter 原本的邏輯是 `if (concept.includes(':')) return`，**靜默跳過所有語言特定概念**，回傳 `undefined` 一路轉換成 `0`。
- **解決方式**：四路完備性擴充為五層（加執行層）；移除靜默跳過改查 executor registry；宣告性概念註冊 noop executor；未知概念透過 `unknownConceptHandler` 讓使用者選擇跳過或中止。
- **教訓**：**編輯管線的完備性不等於執行管線的完備性。** 宣告性概念也要註冊 noop executor，才能區分「已知不執行」和「未知」。
- **來源**：[history/011-四路完備性擴充為五層.md](history/011-四路完備性擴充為五層.md)

### 靜默降級是 bug 的藏身之處

- **理論說**：fallback 值提供 null safety，讓系統在資料缺失時不崩潰。
- **實際發生**：`cin >> s` 變成 `cin >> x` 的 bug 有**四層防線，每層都靜默降級為同一個預設值 `'x'`**——沒有 console.warn、沒有 annotation、沒有任何可觀察的信號。從使用者角度程式碼「就是錯了」，但系統毫無提示。多層 fallback 用同一預設值會互相掩蓋，讓真正的資料遺失點無法定位。
- **解決方式**：改為優先嘗試恢復（如檢查 `selectedVar`、讀正確的欄位名），只在確實沒有更好選擇時才用預設值。
- **教訓**：**「資料格式不符預期」不等於「沒有輸入」。** 前者應該嘗試恢復或發出信號，後者才用預設值。這也是「不做向後相容」的另一面——`??` 是 null safety，不是吞掉舊格式的工具。
- **來源**：[episodes/2026-03-12-cin-變數名靜默降級.md](episodes/2026-03-12-cin-變數名靜默降級.md)

### 而更難發現的一種：不是降級到錯的值，是那段東西壓根沒被看到

- **實際發生（2026-08-14）**：`int x = @@@;` 被 lift 成一顆**乾淨的
  `cpp:var_declare`，信心 `high`**——`ERROR` 節點是 `declaration` 的子節點，
  而 lift **只走它認得的那些**。那個 `@@@` 整個消失，零個訊號。

- **與上一條的差別，而這個差別決定了怎麼找它**：

  ```
  上一條   四層防線，每層都降級到同一個預設值   → 有東西在跑，只是跑錯
  這一條   一層防線都沒有                        → 那段東西不在任何一條路上
  ```

  ⚠️ 前者可以靠「加一個 warn」抓到；後者**加在哪裡都沒用**，
  因為沒有一行程式碼曾經碰過它。

- **判準**：`raw_code` 是「**我看到了而我不認得**」的誠實標記——
  **它擋不住「我根本沒看到」**。所以問題要換一個問法：
  **「這個節點的每一個子節點都被處理過了嗎？」**
  而不是「我處理的那些對不對」。

- ⚠️ **它的代價不只是一個缺陷**：殘差護欄的 `0%` 讀起來像「模型長全了」，
  而真相有一部分是**壞掉的那段被丟了**——**一個不會變的數字，
  不代表它涵蓋了它名字所說的東西。**

- **相關**：[concepts/降級與認知邊界](concepts/降級與認知邊界.md) 的 **Lift-0**

### Extractor 讀的欄位名必須與積木定義一致

- **實際發生**：`getFieldValue('VAR')` 是想像中的欄位名，真實積木用的是 `'SEL_0'`（動態 dropdown）或 `'NAME'`（JSON blockDef）。`getFieldValue` 對不存在的欄位**只回傳 null，不報錯**，所以這類不匹配在靜態分析中很難發現。
- **解決方式**：修正欄位名並建立 fallback 鏈（`'SEL_0'` → `'NAME'` → 預設值）。
- **教訓**：欄位名稱改動必須**全鏈路同步**：blockDef → extract → generate → lift → adapter。**Extractor 測試應覆蓋「欄位名正確性」**——用 mock block 驗證讀取的欄位名確實存在於積木定義中。
- **來源**：[concepts/積木投影管線.md](concepts/積木投影管線.md)

### 同一概念同時有 template 和 hand-written generator，測試會騙人

- **實際發生**：`input` 概念同時有 universal template 和 hand-written generator。真實 app 中 template 優先，但測試中（未載入 template）hand-written 優先。**測試通過但實際行為不同。** 更早之前也發生過開發者寫了 hand-written generator，卻因為 JSON 還留著 `codeTemplate` 而完全不會被執行，本人毫不知情。
- **解決方式**：兩者互斥——改用 hand-written 時必須從 JSON 刪除 `codeTemplate`；若必須並存，測試要明確覆蓋兩條路徑。
- **教訓**：**優先移除 hand-written generator，讓 template 成為唯一來源**（減少雙真相源）。雙真相源的代價不只是維護成本，還包括「測試環境與生產環境走不同路徑」這種最難察覺的失敗模式。
- **⚠️ 敘述已過期，警告仍成立（2026-08-06 實測更正）**：原文說「真實 app 中 template 優先」。**實際上 app 從來沒接上 TemplateGenerator**——`setTemplateGenerator()` 在 `src/` 內零呼叫、唯一會接它的 `initCppModule()` 是死碼。所以 app 恆走 hand-written，而**93 個概念宣告的 `codeTemplate` 一行都沒被用到**。
  兩組態比對（`audit-completeness`）進一步發現：其中 **35 個元件在「接上 template」與「不接」下產出不同的程式碼**，而且有些 template 是**壞的**——`cpp_auto_declare` 會吐出未取代的佔位符 `auto x = ${VALUE};`。**因為從來沒被執行過，所以沒人知道。**
  → 原文的**方向**（雙真相源會讓測試與生產走不同路徑）完全正確，只是這次是「宣告了一整套、實際上一條都沒走」——比走錯路徑更難察覺。
- **來源**：[concepts/積木投影管線.md](concepts/積木投影管線.md)、`specs/049-audit-guardrails/research.md` F1–F3

### 論證會壞在三種地方，只自查第一種會漏掉另外兩種

2026-08-05 跨專案協商（五封往返）裡，本側的論證壞了四次，**失敗模式有三種**，前兩種靠自查抓到、後兩種靠對方抓到：

| 模式 | 實例 | 為何難自查 |
|---|---|---|
| **搬別處的權威句子** | 「投影是超集」與五槽「並列不包含」自相矛盾；「發現而非發明」原本講語義概念 vs λ-core，被誤套到物理零件；「不計成本，故豁免」豁免錯了層 | 引用的句子本身是對的，錯在**適用範圍** |
| **類比推過頭** | 「Rust 的 aliasing XOR mutability 就是麵包板的一個 driver 多個 reader」——被 open-drain／I2C 砍掉（多 driver 合法） | **前 90% 都對**，只有最後一步不成立 |
| **推導沒走完** | 同時寫了「`balance` 永遠可滿足」和「數位模型把 DRC 慣例烘進模型」，**沒發現後者就是前者的例外來源**（理想化） | 兩半都在紙上，**看起來已經完整** |

**教訓**：自查時除了問「這個論據撐得住嗎」，還要問「**這個類比在哪裡開始不成立**」和「**我寫下的兩件事之間有沒有沒接起來的關係**」。

**附帶發現**：敵對式驗證的產出常常不是「你錯了」，而是「**你的東西比你以為的更站得住，只是理由不是你講的那個**」——那句被砍掉的麵包板等式，若成立反而會讓兩律變成同一條律、使分律過不了 over-justify。**對方的更正救了本側的提案。**

- **來源**：ArduinoCAD `draft/2026-08-05-回送semorphe-*.md`（五封）、[concepts/元件.md](concepts/元件.md)

### 豁免一條反對意見時，要指名豁免的是它的哪一層論據

- **實際發生**：跨專案詞彙統一時，使用者說「不計成本、不計代價」。我方用這個豁免推翻了 ArduinoCAD 的一條教訓（「加法式併行通道 >> 加寬共享型別」），理由是「那條的理由是 blast radius＝成本，故豁免」。**查證後那條教訓有兩層理由**：成本層（blast radius）**＋語義層**（「數位 HIGH/LOW 本是類比電壓的量化讀數，兩者本就分層」；判準是「先問這是**加法**還是**改寫**」）。語義層不是成本論，豁免不到它。
- **對方一句話點破**：**over-justify 不是成本測試——所以「不計成本」不能替一個抽象脫罪。** 抽象要嘛多解釋、多預測、要嘛壓縮；三個都不中就是裝飾，跟花多少力氣無關。
- **更普遍的模式**：這是同一輪裡第三次**先有結論、再從別處搬權威句子當理由**（另兩次：「投影是超集」與五槽「並列不包含」自相矛盾；「發現而非發明」原本講語義概念 vs λ-core 人造編碼，被誤套到物理零件）。前兩次自查出、第三次靠外部反駁抓到。
- **教訓**：豁免一條反對意見時，先把它的論據**逐層列出來**，再指名豁免哪一層、為什麼那層可豁免。整條豁免掉最省事，也最容易漏掉真正擋你的那層。**成本可以豁免，語義不行。**
- **來源**：ArduinoCAD `draft/2026-08-05-回送semorphe-埠統一沉到連接層.md`、[concepts/元件.md](concepts/元件.md)

### 寫下一條規則之後，要回頭掃「現有的資料違反它嗎」

- **實際發生（2026-08-09，G 項）**：我在 `concepts/元件.md` 寫下跨域命名規則③
  「主體在前，限定在後」，然後做了一條護欄。護欄報「裸的函式庫名 84 顆」，
  而我量測時只數到 ~35。差額裡有一批是 `count_loop`／`while_loop`／`for_loop`
  ——**它們違反的正是我剛寫下的規則③**，而我的護欄沒有檢查那一條。
- **為什麼會漏**：寫規則的時候是在想「未來的人該怎麼命名」。
  於是檢查也只朝未來看——**而規則同時也是對現況的一個宣稱**。
- **教訓**：**一條新規則落地時，第一件事是拿它掃一遍現有的資料。**
  掃出來的數量就是它的初始債務；掃不出東西才要懷疑規則沒接上。
  這與「第一次跑必須紅」是同一件事的另一半：那條講的是護欄要接上，
  這條講的是**規則的涵蓋面要完整**。
- **它同時是分類表誤報的成因**：84 → 36 的修正過程裡，三個原因有兩個是
  「詞彙表不全」，一個是「規則沒被檢查」。**前者是資料問題，後者是覆蓋問題**，
  而後者更難發現，因為它不會產生看起來奇怪的數字——它產生的是**別的類別**
  的誤報（那些迴圈被歸進了「裸的函式庫名」）。

### 身分不只以字串字面出現——而工具只看得到字串字面

- **實際發生**：命名空間遷移（`specs/103`，177 顆身分／3251 處引用）用 TypeScript AST
  掃字串字面來定位。做完之後測試紅了 128 檔，而根因**沒有一個是字串字面**：

  | 形狀 | 例 | 壞掉的方式 |
  |---|---|---|
  | 裸的物件鍵 | `cpp_isalpha: (c) => …` | 是 Identifier 不是 StringLiteral |
  | 正則裡的形狀 | `/^cpp_(\w+?)_declare$/` | 型別追蹤靜默失效 |
  | 模板字串 | `` createNode(`cpp_string_${m}`) `` | 概念建不出來——**而後果比這一格嚴重得多，見下** |
  | 型別聯集 | `type X = 'if' \| 'print' \| …` | 24 顆 |
  | 測試標題 | `[BLOCKED:cpp_xxx]` | 阻斷者指不到 |
  | JSON 的值位置 | `routes['+'] = 'arithmetic'` | 鍵是運算子、值才是身分 |
  | **拿形狀當判斷** | `if (concept.includes(':')) return` | 命名慣例一改就靜默失效 |

- **教訓**：**凡是「這個字串是什麼」需要靠人讀上下文才知道的地方，工具就看不到它。**
  大規模改名之前，先問「這個識別字有幾種不以字串字面出現的方式」，而不是先問「有幾處」。
- **最兇的是最後一列**：`if (concept.includes(':')) return` 原意是「語言專屬的概念不算
  除錯步驟」——它**把命名慣例當成了契約**。慣例一改，它安靜地變成「什麼都不記錄」。
  → **命名慣例不是契約。** 要判斷「這顆概念是不是 X」，就宣告一個 X 標註，
  不要看名字長什麼樣。
- **⚠️ 模板字串那一列的真正後果（2026-08-11 才量到）**：不只是「概念建不出來」
  ——**它讓那顆身分的缺口對所有護欄不可見**。

  `cpp:string_find_first_not_of` / `find_last_not_of` 的身分原本是
  `` createNode(`cpp:string_${method}`) ``。掃描器只匹配字面字串，於是：

  | 誰在量 | 量到什麼 | 真相 |
  |---|---|---|
  | 就近性護欄 | 這兩顆散在 **1 檔** | 2 檔（`io.ts` 那一檔看不到） |
  | 全部 35 條護欄（當時） | 沒有異常 | **產生器從未存在**——`generateCode` 回 `⟨unknown concept⟩` |

  也就是說 `s.find_first_not_of(" ")` 這段真實可寫的 C++，走
  「程式碼 → 積木 → 程式碼」一圈會**變成一句錯誤訊息**，而根公理是
  「唯一真實，各式投影」。**沒有任何機制看得見它。**

  > **一個看不見的身分，連它缺了哪一路都量不出來。**

  → 這是「盲區的大小量不出來」那條在**身分**這一維的實例。
  而它給膠囊化加了一項不在數字上的收益：**搬家把身分變成字面，
  那些洞當場現形**——「未膠囊化 173 → 167」這個數字不會告訴你這件事。

- **第三個實例（2026-08-09，G 項）**：第九條護欄用 `/_declare$/` 判「宣告族」。
  G 把修飾詞移到後面之後 `var_declare_const` 不再以 `_declare` 結尾——
  **整族從信號裡消失，五筆判定同時變孤兒**。
  ⚠️ **而這發生在同一輪重構裡**：修掉前兩個實例的人（我）沒有去掃同形的地方。
  這正是本檔記過的「一個教訓被記下來、處方也被記下來，而程式碼仍然帶著那個病」。
  → 抓到這一類時，**立刻 grep 全樹的同形寫法**（正則裡的名字、`startsWith` 前綴、
  `endsWith` 後綴、字串拼接出來的 id），而不是只修眼前這一處。
- **救它的不是掃描器，是既有的護欄**：deny-list 誤擋一筆時，抓到它的是 C3 的
  引用完備性護欄（`specs/101`）——它掃的是**執行期建出來的身分**，不是字串，
  所以它看得見掃描器看不見的東西。**兩種量測方式的價值在於它們的盲區不同。**
- **半套比兩個極端都糟**：裸名那段先試「只改分類器有把握的位置」，952 支測試紅
  ——約 800 處身分引用落在分類器涵蓋不到的形狀。要嘛全改（附一份量出來的
  例外清單），要嘛不動。

### 同一個陷阱會在不同的層重演，而在新層上它不像那個陷阱

- **實際發生（2026-08-09）**：討論命名時我先指出一個陷阱——
  `lang:sequence_declare` 帶一個參數 `impl: vector|deque|list`，那是
  **一個只有某個語言會填的槽，寄生在共用的東西上**，而中立性護欄量不到參數這一維。
  **然後我在同一場對話的下一個問題上又做了一遍**：提議「各語言共用一顆積木」。
  共用的積木必須是所有語言需求的聯集——C++ 要型別下拉、Python 不要，
  於是 Python 學生看到一個永遠不會動的欄位，而每加一個語言聯集就長大一次。
  **一模一樣的形狀，換到積木層，我沒認出來。**
- **為什麼認不出來**：陷阱的**特徵**（「只有某一方會填的槽」）在原層有具體的名字
  （`impl` 參數），換層之後那個名字不見了，剩下的是「共用一顆積木」——
  聽起來像優點（減少重複），而不像陷阱。
- **教訓**：抓到一個陷阱時，**把它抽象成一句與層無關的話**，否則你只認得它的那一件外衣。
  這一個的與層無關版本是：
  > **共用的東西不得包含只有部分成員會用到的槽。**
  這句話在參數層、積木層、介面層、資料格式層都成立，而 `impl` 那個講法只在參數層成立。
- **它同時解釋了為什麼「重複」有時是對的**：`py:if` 和 `cpp:if` 的積木宣告幾乎一樣，
  而它們遲早會分岔（Python 有 `elif`／`for...else`）。**提早抽象的代價是分岔那天要拆，
  而拆比重複貴**——因為重複只花空間，拆要動已經被依賴的東西。

### 只有一個樣本時，別問「這個通用嗎」——內省會產出跟查證一樣好看的數字

- **實際發生（2026-08-08）**：規劃元件命名重新設計時，我把 177 顆分成「通用／需要參數軸／
  語言特定／已經對了」四堆，並回報「38% 真的是 C++ 特有，不用動」。
  使用者只說了一句「用假設我們已經支援多種語言的處境再看一次」，那一堆當場垮掉：

  | 我判「C++ 特有」 | 我寫的理由 | 三語言在場時 |
  |---|---|---|
  | `try_catch`／`throw` | 「catch 型別是 C++ 的」 | Python `try/except`、JS `try/catch`。**catch 型別是參數不是身分** |
  | `lambda` | 「capture 是 C++ 的」 | Python lambda、JS arrow。**capture 是只有 C++ 會填的參數** |
  | `range_for` | 「介面是 C++ 的」 | Python 的 `for x in xs` 是**主要**迴圈，比 `count_loop` 更通用 |
  | `printf` | 「格式字串是 C 家族特有」 | Python 的 `%` 格式化**就是從 C 繼承的** |

  至少十幾顆，而錯的方向**一致**：只看得到 C++ 時，什麼都像 C++ 特有的。
- **與「量測工具第一版會量錯」的差別**：那幾次是**工具**錯（入口用錯、粒度太粗、詞義錯）。
  這次工具沒錯——**是我沒有第二個樣本可查，於是用內省填了答案**。
  而內省產出的分類表，看起來與查證產出的一模一樣：一樣有顆數、一樣分族、一樣附理由。
- **教訓**：**「這個東西通用嗎」是一個關於關係的問題，而關係需要兩端。**
  只有一端時它不是「難回答」，是**問不出來**——正確的提法會讓你當場發現手上沒有證據。
  → 判斷前先問：**我這個判斷的證據，來自幾個獨立來源？** 答案是一時，那不是判斷，是猜測。
- **它同時是一個設計判準**：如果一個 formulation 會讓人在資訊不足時**還是答得出來**，
  那個 formulation 本身有問題。好的提法會讓過早的問題**問不了**（見
  [等價與觀察集](concepts/等價與觀察集.md)）。

### 量測工具的第一版會安靜地量錯——救它的是「結果長這樣代表工具壞了」

- **實際發生**：實作四條審計護欄（`specs/049-audit-guardrails`）時，有兩支量測**產出了看起來合理、實際上錯誤的數字**：
  - **完備性護欄第一版**用 `renderToBlocklyState` 當 render 的入口，但它需要 `program` 外殼且只處理 statement——於是所有 expression 元件被判為缺，產出 **84 個假的 missing**。數字很大、很像真的。
  - **兩組態比對第一版**比的是 verdict（實作／殼／缺），得到「**無差異**」。而 verdict 粒度太粗：兩種組態都產得出有效輸出，差別在**文字**。真實差異是 35 個元件。
- **怎麼抓到的**：**不是靠檢查程式碼，是靠結果不合理**。extract 顯示「0 殼 ／ 84 缺」——`render` 全過卻 `extract` 全缺，這個組合不可能。而兩組態那次，救命的是我自己寫進報表的那句「**（無差異——若這裡是空的，多半是護欄沒接對）**」：工具吐出的空結果，被工具自己預先貼上的標籤判為可疑。
- **教訓**：**給每個量測工具寫一句自我否證提示**——「如果結果長成 ○○，代表工具壞了而不是世界長這樣」。量測工具是最容易變成殼的東西：它產出數字，而數字天生看起來像證據。沒有這句提示，一個安靜量錯的護欄比沒有護欄更糟——它會讓你**帶著假的安全感**去做重構。
- **第三個實例，而且觸發方式不同（2026-08-06）**：缺陷帳護欄把「被關掉的測試」（22 筆，`it.skip` 有本體）與「只有名字的測試」（64 筆，`it.todo` 無本體）**當成同一種東西數**。數字是對的、分類是對的，**只有「停用測試」這個詞的語義錯了**——它把兩種需要完全不同工作量的東西包在一起，讓優先序灌水 3–10 倍（「修 `print` 解鎖 21 個」實際是 5 個）。
  前兩個實例靠「結果不合理」抓到；**這個是拿它來規劃真實工作時才暴露的**。
  ⚠️ **而它有另一半，2026-08-13 才露出來**：那個差別不只在**估算**，也在**回饋**。

  | | 缺陷被修好時 |
  |---|---|
  | `it.skip`／`it.fails`（有本體） | **變紅**——逼人來拔釘子 |
  | `it.todo`（無本體） | **什麼都不會發生**——它繼續躺在那裡，而缺陷帳把它算成「還沒做」 |

  實例：`[BLOCKED:cpp:destructor] should preserve inner block scope for correct
  destructor ordering` 在 2026-08-13 被 `cpp:block` 修掉了，**而那筆 `it.todo`
  一聲都沒出**——是人去梳理「還有什麼沒做」時才發現它已經不是問題。
  → **`it.todo` 本身就是一種殼**：它宣告了一個缺陷，而沒有任何機構在看它還在不在。
  處方 `build-guardrail` 早就寫了（「要釘住已知缺陷用 `it.fails`」），
  缺的不是處方，是**知道 `it.todo` 也算一種殼**。

- **第四個實例，而這次是【正面】的（2026-08-14）**：一支寫著
  「⚠️ 能力邊界：少一個右大括號今天標不出來」的測試，**有本體**
  （斷言「這一格今天是空的」），而註解逐字：
  「**修好它的那天這支會變紅，那時把註解一起更新**」。

  補上解析器傳播旗標之後（spec `121`），它**紅了**——而那個缺口
  **不在那一輪的範圍裡**，是順帶被修好的。

  > **一個被記下來的能力邊界，會在根因被修掉的那天自己舉手。**

  ⚠️ 而換成 `it.todo` 的話它**一聲都不會出**——那個順帶的修復
  會靜靜地不被人知道，而註解會繼續說一件已經不成立的事。
- **第四個實例，觸發點與第三個相同但對象是自己（2026-08-06 同日）**：辨識歧義護欄報出「5 條規則的限定條件完全相同」，我據此在 vision 與 history 寫下「另外 4 條是**死規則**」並列為最嚴重的發現。**下一步要照它規劃工作時去實測，全部推翻**——`vector`／`map`／`stack`／`queue`／`set` 宣告都辨識正確，因為那 5 條指向**同一個 strategy**。它們不是 5 條規則，是同一條規則被登記 5 次。
  護欄報的**事實**是對的（那 4 條確實不會被試到），錯的是我從事實推出的**嚴重性**。而且護欄的自我驗證測試（FR-022）當時**通過了**——它只釘住「這一群必須被報出來」，沒有釘住「必須因為正確的理由被報出來」。
  → **自我否證聲明要釘的是理由，不只是結果。** 一個因為錯誤理由而給出正確結果的護欄，看起來與健康的護欄完全一樣。
- **第八個實例，而它是「綠」（2026-08-08，`specs/102`）**：**綠不等於檢查有效。** 參數規格化加了一條「`enum` 的 `values` 必須等於積木下拉的選項」，第一次跑就是綠——而那是因為那 29 筆 `values` **本來就是從下拉抄出來的**。同一份來源比對自己，永遠一致。
  前七個實例的症狀都是「數字錯」，這個的症狀是「**沒有數字**」——一條由建構保證的綠看起來與一條真的守住了的綠完全一樣。
  救它的是額外注入一顆**真實元件**（`arithmetic`）宣告錯的值域，證明檢查會叫。
  → **`build-guardrail` 第 6.5 步（第一次跑必須紅）在推導出來的資料上失效**，因為推導者與檢查者是同一個人、同一份來源。那時要問的不是「它紅了嗎」，是「**它能不能紅**」。

- **第七個實例，機制是新的（2026-08-06）**：**元件身分與普通英文單字撞名。** 中立性護欄的判定是純文字比對（「只匹配完整的引號字串字面」），於是 `'comment'` 這個字串不論它是概念身分、語法樹的節點型別、一個列舉的成員、還是使用者在積木上看到的提示文字，一律被計為違規。六筆裡三筆是誤報。
  前六個實例的機制是入口用錯／粒度太粗／詞義錯／從事實推錯嚴重性／判準對但自動化錯／測試沒測到東西。**這個是第七種，而且它對每一個與普通單字撞名的元件身分都同樣脆弱**——修的時候不知道還有幾個。
  → **修法是遮罩，而每個遮罩的判準必須是語言規則、不是統計傾向。** 三個候選只採用兩個：型別位置（編譯後不存在，不可能是執行期引用）與 Blockly 欄位建構式的第一引數（那是值不是識別碼）。第三個 `.type === 'x'` 被**實測否決**——它會遮掉 14 筆真違規，因為 `block.type === 'cpp_string_declare'` 與 `node.type === 'comment'` 在文字上完全相同。**判不出來一律留著算違規。**
  ⚠️ 同一次還撞見一個**更根本、不屬於這條教訓**的東西：護欄量的**維度**不是規範的全部（它找身分，看不見語法）。那不是工具實作錯，是規範被機械化時選了一維而那件事消失在數字裡——見 [concepts/執行機構.md](concepts/執行機構.md)「一條規範被機械化時，選了哪一維會消失在數字裡」與 [history/021](history/021-中立性的數字只量身分那一維.md)。
- **第十一個實例，而它不在量測工具裡，在註解裡（2026-08-06）**：三處註解把「還沒接上」寫成「做不到」——「為了避免型別消歧問題」「辨識器**沒有**型別資訊（`ARCHITECTURAL`）」「辨識器產出的是運算式版（`architectural`）」。三句都是假的：型別追蹤的機制一直都在（零呼叫者），位置判斷只要看語法樹的父節點。
  → **一句解釋為什麼「只能這樣」的註解，會讓那個限制看起來是本質的**，而大寫的 `ARCHITECTURAL` 讓它看起來像已經有人想過了。**這比沒有註解更難發現**——沒有註解的話下一個人會去查。判準：這類標籤要附**哪一條設計決定**，指不出來就只是一句沒有依據的斷言。
- **第十個實例，而它揭露前九個共有的一個機制（2026-08-06）**：以名字為基礎的比對**忘了詞界**，一天之內四次——`comment` 撞名普通英文字、`'u_endl'` 裡含 `endl`、剝註解的規則寫在正則字面裡（只掃字串就看不到）、`children.init` 是 `children.initializer` 的子字串。
  **四次的後果不一樣**：前兩次多報（數字虛高），**後兩次漏報（數字虛低，而 0 與健康的 0 長得一模一樣）**。漏報那一種更危險——它讓護欄在最需要出聲的時候安靜。第四次那條護欄的第一版回報「0 個問題」，而它要抓的那一筆正好被自己的子字串遮掉。
  → **任何以名字為基礎的比對，先寫一支專門釘詞界的注入測試**：「一個更長的名字包含了它，不得算它出現過」。那支測試比報表上的數字更能說明護欄有沒有壞。
- **第九個實例，而這次它被「已知答案的樣本」當場擋下（2026-08-06）**：語法耦合護欄的偵測器第一版只掃**字串字面**，於是漏掉核心層剝除註解符號的那一整段——那一處寫在**正則字面**裡（`/^\/\/\s?/`）。
  **抓到它的不是 code review，是 `build-guardrail` 第 6 步要求的已知答案樣本**：拿 git 裡那個功能動工之前的真實程式碼當輸入，斷言「這條護欄必須抓得到當時確實存在的耦合」。抓不到就代表它沒有存在的價值。
  同一次的第一輪真實量測，5 處裡有 4 處是誤報（子字串、概念身分、型別位置）——**與撞名那個實例同一個坑的第三次**。
  → **靜態判斷的驗證要有三層**：合成注入雙向、**加上一個真實的、已知有問題的樣本**。前兩層只證明邏輯自洽，第三層才證明它對真實世界有效。
- **第十二個實例，而它在 shell 裡，不在 TypeScript 裡（2026-08-07）**：知識庫的孤兒掃描寫成 `grep -rl --include=*.md`，**zsh 把 `--include=*.md` 當成 glob 去展開**，找不到符合的檔案 → 每個 grep 靜默回傳零 → **44 個檔全被報成孤兒**。修正引號之後實際只有 4 個。
  前十一個實例都在應用程式碼或測試裡。**這個在跑量測的那行指令裡**——工具邏輯完全正確，是餵給它的東西在到達它之前就壞了。而它的失敗方向最糟：**空集合在這個掃描裡代表最嚴重的結果**（全是孤兒），所以錯誤看起來像一個驚人的發現。
  → **一次性的掃描指令也是量測工具**，同樣適用「如果結果長成 ○○，代表工具壞了」。判準很便宜：**先問這個數字的合理範圍**——「知識庫 44 個檔全是孤兒」在任何健康的專案裡都不可能。
- **第八個實例，而它是「讀量測的人」出錯（2026-08-06）**：全套測試的失敗清單被 `head -N` 截斷，我沒讀完就報「全綠」——**而且連報兩次**（059、061）。紅的那支是概念數量的斷言，在 059 就壞了，兩輪之後才被下一個功能撞出來。
  前七個實例是**工具量錯**；這個是**工具量對了、報表也對了，讀的人只看了前幾行**。→ **截斷輸出等於沒有讀。** 檢查全套結果時，要嘛完整列出失敗清單、要嘛只看「Test Files: N passed」那一行的判定，**不要用 `head` 看 FAIL 列**——它會給你一個看起來完整的清單。
- **因此教訓有三個觸發點**：
  1. **結果不合理**（可自查，但取決於當下夠不夠清醒）
  2. **照它行動時**（不可預先自查，但必然會現形）——所以**每次照量測行動的第一步，都要準備好推翻它**
  3. **量測結果被寫進知識庫之後**——這時它已經不是數字，是**後續每一次決策引用的權威**。所以第 2 點的「準備好推翻它」包含推翻自己剛寫下的東西
- **第五、六個實例，觸發點又不同（2026-08-06）**：
  - **判準對、自動化錯**：「有子槽而沒人讀 ＝ 還沒實作」這個判準是對的，但把它實作成 grep 概念名之後，答案是**反的**——壞掉的 `cpp_static_cast` 被判成好的，好的 `cpp_case` 被判成壞的。抓到它的是**先做的實測與它矛盾**：兩個量測互相打架。→ **判準本身可以是對的，把它自動化的第一版仍然會量錯。** 靜態判斷要先在已知答案的樣本上驗過再拿來下結論。
  - **測試通過卻什麼都沒測到**：分類測試有五列的「通過」是假的——那些概念根本沒出現在語義樹裡（例如用 `static_cast` 代測 `dynamic_cast`）。加上「這個概念必須真的出現在樹裡」之後，通過數從 17 掉到 12。→ **一支斷言「輸出正確」的測試，要先證明它真的執行到了被測的東西。**
- **相關**：自我否證聲明本身也會過期——判準已於 2026-08-06 加強為「**根本別錨在真實狀態上**」（護欄修好了它要量的東西，就是它的錨點爛掉的時候）。見 [concepts/執行機構.md](concepts/執行機構.md) 與 [history/022](history/022-護欄的自我否證不得錨在真實狀態.md)
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)「執行機構自己也可能是殼」
- **來源**：[episodes/2026-08-06-護欄自己量錯.md](episodes/2026-08-06-護欄自己量錯.md)、`specs/050-repay-top-blockers/research.md` F4／F6

### 量一個**新刻度**時，最可能錯的是語料——而語料的錯看起來像世界的性質

- **與上一條的關係**：上一條那十一個實例**全部是工具錯**（入口用錯、粒度太粗、
  詞義錯、從事實推錯嚴重性、綠是建構保證的……）。這一條的三次**工具都是對的**，
  錯的是餵進去的東西。**修法不同，所以是兩條而不是第十二個實例。**

- **實際發生（2026-08-10，`specs/108`，一輪之內三次）**：

  | | 第一版 | 正確 | 語料錯在哪 |
  |---|---|---|---|
  | 形態的殘差 | **48.83%** | **0.23%** | 820 段裡 353 段是測試檔裡的**片段**，語法本來就不完整 |
  | 行為的誤差 | 64 筆 | 40 筆 | 61 段讀 `cin`／用 `rand`，無輸入時**兩邊都不可判定** |
  | 行為的誤差 | 40 筆 | **31 筆** | 抽取器把 `\n` 錯拆成「反斜線＋真換行」＝ C 的**續行符**，`printf` 的格式字串被吃掉——**這一個是我自己寫壞的** |

- **為什麼它比工具錯更難發現**：工具錯的症狀是「數字不合理」，
  而**語料錯的症狀是「數字看起來像一個發現」**。
  `48.83%` 讀起來就是「**模型只理解一半的程式碼**」——它不像壞掉，它像壞消息。
  順著它走的話，下一步會是「去補模型」，而模型根本沒有問題。

- **判準（比「結果不合理」更早生效）**：
  > **這個數字如果是真的，它與我已知的事實矛盾嗎？**

  已知事實是「這個系統天天在用，完整程式的殘差應該接近零」。
  48.83% 與那句話矛盾——**而矛盾的來源有兩個候選（工具、語料），
  上一條只教你查前者。**

- **處置：語料的分欄要進基線，不是靜默排除。** 兩條護欄各有一欄「不算數的語料」
  （殘差的「語法有錯片段 353」、誤差的「不可判定 61」）。
  理由是對稱的：**只記分子的話，「加一段讀 `cin` 的語料」就能縮分母讓比率下降
  ——而縮分母比修分子容易。**
  → 這是「用宣告刷數字」（`history/018`）在語料上的變形：
    **不是改宣告，是改進來的東西。**

- **推廣**：任何**新**刻度都適用，因為新刻度沒有歷史數字可以對照。
  已經跑過幾輪的護欄有棘輪替它擋（數字跳動會被看見），
  **第一次跑的那個數字沒有任何東西在守。**

- **相關**：[concepts/等價與觀察集.md](concepts/等價與觀察集.md) §七、
  [history/037](history/037-模型與實體的距離-兩個刻度.md)、
  `knowledge/skills/build-guardrail/SKILL.md` 第 6 步與 6.5 步

### 知識庫存得住「當時的觀察」，存不住「現在的事實」——而兩者長得一樣

- **實際發生（2026-08-10，`specs/109`）**：整份 spec 的第一版圍繞
  「新增一個『已知不支援』宣告值」設計，連防洗數字的機制都寫好了
  （分欄、總和不變、只能宣告已記錄過的缺口）。
  **依據是 `concepts/執行機構.md:168`（2026-08-06）**：
  「其餘 14 個跑起來結果是錯的（**十個是物件導向，直譯器不支援它**）」。

  而查證的結果是 `specs/071`／`072`／`073` **在那之後實作了它**，
  `unimplemented.ts` 的 `OOP_NOT_IMPLEMENTED` 現在是**空陣列**。

- **它與「凍結的讀數」是同一個形狀**：`specs/108` 記過「27 個檔的 `g++` 答案
  是人工量一次凍進註解的——**一個凍結的讀數是宣告，不是量測**」。
  這次凍結的不是外部工具的答案，是**我們自己對系統的判斷**。
  → **同一件事的兩個方向都撞過了**：對外部權威的讀數會凍結，
    對自己系統的判斷也會。

- **為什麼特別難防**：知識庫裡的句子**沒有時間戳的語感**。
  「直譯器不支援物件導向」讀起來像一條性質，不像一次觀察。
  而它旁邊就寫著日期——**日期在，語氣沒有跟著**。

- **判準（讀的時候問，不是寫的時候）**：
  > **這句話是「當時的觀察」還是「現在的事實」？**

  凡是描述**系統現況**的（支不支援、有沒有實作、剩幾筆、誰呼叫誰），
  一律當成前者，**引用前重新量一次**。
  凡是描述**推理或決定**的（為什麼這樣設計、否決了什麼、判準是什麼），
  才是知識庫真正存得住的東西。

- **而救它的是 `grep`，不是讀得更仔細。** 這一點與「量測工具第一版會安靜地
  量錯」同一個結論：**不要用更用力的閱讀去對抗一個機械問題。**

- **相關**：[history/038](history/038-過期的論斷被當成現況引用.md)、
  [concepts/執行機構.md](concepts/執行機構.md)（`:168` 已就地標註過期）

### 照抄已驗證的形狀，不要自己換一個判準

- **實際發生（2026-08-10，`specs/109`）**：修「`char c = 66` 印出 `6`」時，
  第一版寫成

  ```ts
  const n = this.toNumber(val)
  if (val.type !== 'char' && Number.isFinite(n)) return { …, value: String.fromCharCode(n) }
  ```

  而 **`toNumber({type:'string', value:'a'})` 回傳 `0`，不是 `NaN`**
  → `char s[8]="a"` 的每個字元都變成 NUL → `strcat` 的測試當場變紅。

- **而同一題早就被解過**：`std/cctype/executors.ts` 的 `charOf` 用的是
  `typeof v.value === 'number'`，並且**在它上方寫著**
  「⚠️ **不能一律 `String(value).charAt(0)`**」。
  **我讀過那段註解、在修法裡引用了它，然後換了一個自己的判準。**

- **為什麼會想換**：既有形狀看起來「不夠通用」——`typeof === 'number'`
  漏掉了「數字存成字串」的情況，而 `toNumber` 看起來涵蓋更多。
  **改動的動機是把它做得更好，而不是偷懶。** 這正是它危險的地方：
  一個看起來更周全的判準，沒有那份形狀背後的實測。

- **判準**：
  > **這個問題別的地方解過嗎？解過的話，先照抄，再問要不要改。**
  > 而**要改就得付出原本那份形狀付過的驗證代價**——它是誰在什麼情況下驗出來的。

- **它同時是另一條的實例**：「**修一條路時要問：同一個缺陷在別的路上長什麼樣**」
  ——`cctype` 修過 char 的取法，而**核心的轉型沒有**，所以缺陷在核心又活了一次。
  → 兩條合起來的操作：**先 grep 這個症狀在別處有沒有解，有就照抄；
    沒有就修完之後回頭找同族。**

- **相關**：[history/038](history/038-過期的論斷被當成現況引用.md)

### 探測腳本的**過濾器**也是語料的一部分——而它比語料更隱形

- **實際發生（2026-08-10，`specs/110` 規劃時）**：查 `s.size()` 為什麼回 0，
  寫了一支探測腳本印出語義樹，並用 `x.includes('string')` **過濾**要看的節點
  （理由：這是字串的方法）。輸出是

  ```
  cpp:program cpp:include cpp:string_declare cpp:literal_string cpp:var_declare
  ```

  沒有任何 `size` 相關的節點 → 我的結論是「**`s.size()` 完全沒有被 lift**」，
  並據此推薦蓋一條「量得到靜默丟棄」的護欄。

  **兩個都錯。** 拿掉過濾器再跑一次：`s.size()` lift 成 **`cpp:vector_size`**
  ——它有 lift，只是**身分錯**。而 `vector_size` 不含 `string`，
  **剛好被我的過濾器濾掉。**

- **它是「語料錯看起來像世界的性質」的近親，但更難防**：

  | | 錯在哪 | 會不會被看見 |
  |---|---|---|
  | 語料錯 | 餵進去的東西不對 | **語料通常會被印出來**（段數、字元數） |
  | **過濾器錯** | 看的窗開錯地方 | **過濾器不會被印出來**——它只留下一個乾淨的輸出 |

  > **一個過濾掉答案的探測，輸出看起來與「答案不存在」一模一樣。**
  > 而後者是一個「發現」，前者是一個 bug。

- **判準**：**探測腳本的過濾器，要在已知答案的樣本上驗過。**
  這次只要加一句「`s.length()` 必須出現在輸出裡」就會當場戳破——
  因為 `.length()` 是已知會被正確辨識的那一支。
  → 具體做法：**任何過濾器旁邊放一個「這個一定要留下來」的對照**。

- **它也是「靜態判斷只能排順序」的一個新面向**：那條講的是判準錯，
  這條是**判準對但視野被裁掉**——而裁掉的動作看起來像「聚焦」。

- **相關**：[history/039](history/039-靜默回退掩蓋辨識歧義.md)、
  本檔「量一個新刻度時，最可能錯的是語料」

### 想用註解說明一個結構性的空缺時，先問「有沒有一個欄位可以裝它」

- **實際發生（2026-08-11，第四顆膠囊）**：`cpp:queue_back` 沒有 lift 那一路
  （`.back()` 在辨識層被判成 `cpp:vector_back`，這顆身分從程式碼永遠得不到）。
  我打算**在檔頭註解裡說明**，而契約直接擋下：

  > 「膠囊 cpp/queue_back：paths 少了「lift」。**沒有那一路也要寫**
  > （`null` ＋ `_lift_why`）——不然**「顯式的空」與「遺漏的空」分不出來，
  > 而後者就是殼**。」

- **機制比我先想到**，而那正是它存在的意義。兩者的差別是**誰會檢查**：

  | | 說明放哪 | 誰會讀 | 漏了會怎樣 |
  |---|---|---|---|
  | 註解 | 檔頭 | 下一個打開這個檔的人 | **沒有人知道它漏了** |
  | 欄位 | `paths.lift = null` ＋ `_lift_why` | **契約，每次載入** | **當場擲錯** |

- **判準**：**當你想用註解說明一個結構性的空缺時，問「這個空缺有沒有一個欄位
  可以裝它」。** 有 → 用欄位，註解寫理由的細節。
  沒有 → 那可能是**該加一個欄位**的訊號，而不是該寫得更詳細的訊號。

- **它是本檔「沉默的正確和沉默的缺失撞在一起時，讓正確的那個說話」的操作版**：
  那條講的是**要宣告**，這條講的是**宣告要有地方放**。
  ⚠️ 而「地方」不能是註解——**註解不會被檢查**。

- **推論：一個好的契約會拒絕含糊，即使含糊看起來比較快。**
  我當時想用註解是因為那比較快，而快的那條路**會讓下一顆膠囊沒有先例可循**。

- **相關**：[history/046](history/046-第四顆膠囊與一個從程式碼到不了的身分.md)、
  [concepts/執行機構.md](concepts/執行機構.md)

### 列舉已知的，等於保證下一個會被漏掉

- **實際發生（2026-08-10，第三顆膠囊）**：兩個地方各自列舉「積木的來源」：

  ```ts
  ...allStdModules.flatMap((m) => componentBlocks(m.header))   // std 模組的 header
  ...componentBlocks(null)                                     // 沒有 owner 的
  ```

  而新搬進來的膠囊 owner 是 **`(core)`**——**兩類都不是**。
  症狀：它的積木**從工具箱整個消失**，而讀起來像
  「一顆剛搬好的元件被刪掉了」。

- **這是第三次**，而前兩次的處置都是「**再加一個分支**」
  （`specs/104` 的卡點 6 收斂了八份各自組裝、`specs/113` 補了 lift 那一維）。
  → **第三次才看見通則**：問題不在漏了哪一類，在**分派是列舉式的**。

- **判準**：**這個分派是列舉式的還是扣除式的？**

  | | 寫法 | 新類別出現時 |
  |---|---|---|
  | 列舉式 | 「A 的、B 的、沒有的」 | **漏掉**，而且是靜默的 |
  | 扣除式 | 「A 的、B 的、**其餘全要**」 | **自動涵蓋** |

- **而它為什麼難發現**：列舉式在**寫的當下是完整的**——它涵蓋了所有已知的類別。
  它的缺陷不在現在，在**下一個類別出現的那一刻**，而那通常是別人、別的時候。
  → 與本檔「一件『該做但不急』的事」同一族：**代價在未來的東西，
    在現在看起來沒有問題。**

- **⚠️ 而扣除式有它自己的代價**：「其餘全要」會把**你沒想到的東西也收進來**。
  所以它適合**同質的集合**（都是膠囊的積木），不適合異質的。
  判準：**「其餘」是不是一個定義得出來的集合？** 是 → 扣除式；否 → 列舉並加一支
  「有沒有漏」的檢查。

- **相關**：[history/045](history/045-第三顆膠囊三種形狀全驗過.md)、
  `knowledge/skills/component-encapsulate/SKILL.md` 步驟 4

### 回退程式碼，但不回退知識

- **實際發生（2026-08-10，同一顆膠囊搬兩次）**：第一次嘗試撞上三個坑後**被回退**
  ——約 400 行程式碼刪掉，而三條「為什麼失敗」寫進 skill。
  第二次重做：**六支自證測一次過**，省下的正是其中兩個坑。

  ```
  上次：lift 寫成登錄呼叫 → 壞兩次     這次：glob 直讀，一次就對
  上次：owner 章沒蓋（核心無 requires） 這次：component.json 顯式宣告
  ```

- **關鍵是回退時的分開處置**：`git checkout` 掉程式碼，
  但**先把「這次嘗試證明了什麼」寫下來**。
  兩者是**不同的東西**，而回退指令只認得前者。

- **判準（回退之前問）**：**這次嘗試證明了什麼？**
  - 「證明這條路走不通」→ 那個**理由**要留下，否則下一個人會再走一次
  - 「證明我的假設錯了」→ 那個**假設**要留下並標明被推翻
  - 答不出來 → 那才是真正白做的一次

- **⚠️ 而它不是「失敗也有價值」那種安慰**：判準是**可驗證的**——
  下一次做同一件事時，**有沒有少踩那幾個坑**。
  這次的答案是「少踩兩個，冒出一個新的」，而那個新的也寫進去了。

- **它同時解釋了為什麼「回退」常常是對的**：一次嘗試的產出有兩部分，
  而**知識那一半不會被回退拖累**。所以「做到一半發現方向不對」時，
  真正的成本不是那些程式碼，是**沒有把學到的東西寫下來就砍掉**。

- **相關**：[history/044](history/044-第三顆膠囊被回退而知識留下.md)、
  [history/045](history/045-第三顆膠囊三種形狀全驗過.md)

### 一件「該做但不急」的事，會被合理地推遲到永遠——而每一次推遲都有好理由

- **實際發生（2026-08-10）**：`specs/104` 的切片紀錄開頭就寫著

  > 「哪些步驟會**重複 176 次**。沒有它，剩下的 176 顆仍然是憑感覺開票。」

  而它寫完之後**過了五輪才被 skill 化**（`specs/108`–`112` ＋ judge 的連鎖）。
  那五輪不是浪費——它們抓到 `p->x` 壞掉、`char c=66` 印成 `6`、建構子不執行。
  **每一輪的選擇單獨看都是對的。**

- **它與「忘了做」不同**：忘了做的東西**沒有人記得它存在**；
  這一種**每一輪都被看見、被評估、然後被合理地排在後面**。
  → 而清單上它們**長得一模一樣**：都是「還沒做」。

- **兩個症狀**：
  - **一直被合理地推遲**與**其實不重要**，在待辦清單上分不出來
  - 而推遲的成本**不是線性的**：skill 化的價值是「省下的重複」，
    第 176 次之前 skill 它省 175 次，第 175 次之後省 1 次

- **判準**：**這件事的成本會隨推遲而增加嗎？**
  - 不會（修一個 bug、還一筆債）→ 排在後面是對的
  - **會**（skill 化、識別碼的設計、格式定案）→ **它不該與其他項目一起排序**，
    因為排序比的是「現在多重要」，而它的代價在**未來**

  > 判定落點的鍵是同一個形狀：它出過三次問題，而每一次都只修那一條護欄。
  > **第 175 次膠囊搬家之前修完它，比第 175 次之後便宜 175 倍。**

- **可操作的形式**：待辦清單上把這一類**另立一區**，而不是給它一個順位。
  它們的排序判準與其他項目**不可比**。

- **相關**：[history/043](history/043-第二顆膠囊把109顆的估計合併了.md)、
  `knowledge/draft/2026-08-10-掀出來但還沒做的.md`（「還開著的」第 2 項）

### 注入釘住的是**機制**，不是**輸入**——而一條沒吃到輸入的護欄，注入照樣全綠

- **實際發生（2026-08-10，judge 收 `specs/112` 時查到）**：第七條護欄
  「執行器重複註冊」自 2026-08-06 起**量的是一個空的註冊表**——
  它在量測前**沒有呼叫 `registerCppLanguage()`**，所以 C++ 的執行器一個都沒註冊。

  ```
  基線寫的      duplicateConcepts: 0        （四天以來一直是這個）
  真實的數字     4                          （method_call／struct_at_member／
                                             struct_at_ptr／template_function 各兩次）
  ```

- **而它有注入測試，而且一直是綠的。** 注入手動 `reg.register()` 同一個名字兩次，
  再確認被數到——它證明的是「**計數器會數**」。
  > **計數器會數 ≠ 註冊表裡有東西。**

- **它的自我否證聲明也寫了，而且問錯了問題**：
  「⚠️ 這個數字若是 0，先確認計數真的有接上……判斷依據是『★ 注入』那支測試」
  ——那句話把讀者**明確地指向注入**，而注入正好是看不到這個缺口的那一支。
  → **一句錯誤的自我否證提示，比沒有提示更糟**：它讓人停止懷疑。

- **教訓**：**把量測的入口條件寫成斷言**——
  「掃了幾個檔／載入了幾條規則／註冊表裡有幾個概念／語料幾段」。
  這條在 `experience.md` 早就寫過（「盲區的大小量不出來」那條的具體做法），
  **而它被理解成「注入的補充」，其實是「注入涵蓋不到的另一半」**：

  | | 釘住什麼 | 漏掉什麼 |
  |---|---|---|
  | 注入 | 判定邏輯**會不會報** | 真實輸入**有沒有進來** |
  | 入口條件 | 真實輸入**有沒有進來** | 判定邏輯對不對 |

  **兩者不能互相取代，而「有注入」很容易讓人以為已經夠了。**

- **順帶**：這條護欄修好之後第一件事，就是抓到四筆真的重複註冊——
  其中三筆的註解寫著「OOP concepts — noop in interpreter (no OOP runtime)」，
  **而那句話在 `specs/071`／`072`／`073` 之後就過期了**
  （見本檔「知識庫存得住當時的觀察，存不住現在的事實」）。
  它們沒出事是因為真實作載入在後、蓋掉了空操作——**靠註冊順序，不是靠設計**。

- **相關**：[history/042](history/042-護欄自己量了四天的空註冊表.md)、
  `knowledge/skills/build-guardrail/SKILL.md` 第 9 步

### 一個分類機制長出或收掉類別時，人的判定詞彙要跟著改

- **實際發生（2026-08-10，兩個方向各一次）**：

  | | 機器 | 人的詞彙 | 後果 |
  |---|---|---|---|
  | `#33` | **長出**新類別（型別不符／缺子節點） | 沒跟上 | 8 筆被硬套成「靜默回退」，**判定檔說 8 個缺陷、護欄說 0 個** |
  | `#32` | **接管**了一個類別（`不可判定()` 自動濾掉讀 `cin` 的語料） | 沒縮回 | 值域裡兩個值**永遠不可能被用到** |

- **兩個方向的症狀不同，而報表都正常**：
  長出的那邊是**兩份紀錄各說各話**（讀哪一份決定你以為有幾個缺陷）；
  收掉的那邊是**死值留著**（看起來還有人在判那一類）。

- **教訓**：**每次改動機器的分類規則，去看一眼判定的值域還對不對。**
  → 機械化的形式是一條斷言：**人的判定與機器的分類必須相容**
    （`#33` 已有；相容表要允許「合法」出現在任何一欄，
     因為 `strcmp` 相等回 0 那種語義可能落在任何形狀底下）。

- **為什麼容易漏**：改分類規則時人在想「機器怎麼分」，
  而判定檔是**另一個檔案、另一個時間寫的**。
  兩邊都合理，合起來才矛盾——**而沒有任何一次執行會同時讀到兩邊**。

- **相關**：[history/042](history/042-護欄自己量了四天的空註冊表.md)

### 數到了東西，不代表那些東西**會發生**——而計數天生看起來像證據

- **實際發生（2026-08-10，連續兩輪，同一個形狀）**：

  | 我的推薦 | 依據 | 實測 |
  |---|---|---|
  | 「把靜默回退護欄擴到產生器」 | **26 處，比執行器多三倍** | 415 段語料觸發 **0 次** |
  | 「第三十三條護欄抓到 9 筆」 | **9 筆，第一次跑就紅** | **8 筆是死分支** |

  兩次的推薦都建立在**計數**上。而計數回答的是「**有幾個長這樣的東西**」，
  不是「**有幾個會發生的東西**」。

- **它與「量測工具第一版會安靜地量錯」不同**：那條講的是量測錯。
  **這兩次量測都是對的**——26 處確實存在，9 筆確實符合判準。
  錯的是**從「量到了」推出「值得做」**這一步。
  → 前者的修法是修工具；這條的修法是**多問一個問題**。

- **為什麼特別容易中**：一個數字自帶「已經查證過」的氣味。
  「26 處」聽起來像調查的結論，而它其實只是**一次 grep**。
  而下一步的規劃會直接接在那個數字上，**沒有人會回頭問那 26 處會不會發生**。

- **判準**：
  > **任何以「有 N 筆」為依據的推薦，先問「這 N 筆在真實輸入上發生過幾次」。**

  ⚠️ 而**量活性要有對照**——否則「0 次」有兩種解釋：
  它不會發生，或者**輸入根本沒碰到那個概念**。
  這次的對照是「那些概念有沒有出現過」：5/10 出現、共 14 次 → 量測有效。

- **推論：不會發生 ≠ 不該存在。** 那 8 個防禦分支永遠不執行，
  但它們防的是**未來的壞樹**。處置是**留著，記成一個可見的數字，不進棘輪**
  ——而不是刪掉，也不是讓它假裝是缺陷。

- **相關**：[history/040](history/040-護欄抓到的九筆有八筆是死分支.md)、
  `knowledge/skills/build-guardrail/SKILL.md` 6.5 之後那條

### 識別碼必須識別得出那個東西——截斷的鍵會讓「已判定」變成謊話

- **實際發生（2026-08-10，`specs/110` 收尾）**：`specs/108` 建的判定落點
  （`build-guardrail` 第 11 步）用「語料正規化後的前 80 字元」當鍵。實測：

  ```
  17 筆明細  →  16 個不同鍵
  19 筆判定  →  16 個不同鍵
  ```

  碰撞的是三段開頭相同的 `#include <iostream> #include <cctype> …`。

- **後果比「數字不準」嚴重**：判定落點有兩道防線，**兩道都靠鍵**——
  - **孤兒檢查**（訊號消失了、判定還在）→ 別名讓過期判定繼續生效
  - **「要看」清單**（未判定的命中）→ **一筆新缺陷會被同鍵的舊判定吸收，
    而報表上顯示「已判定 N，要看 0」**

  > **一個被誤認為已經看過的缺陷，比一個沒被報出來的更糟**
  > ——後者至少不會讓人以為看完了。

- **它與「以名字為基礎的比對忘了詞界」是同一族，而機制不同**：
  那次是**子字串**（`children.init` 落在 `children.initializer` 裡），
  這次是**截斷**（兩個不同的東西前 80 字元相同）。
  → 合起來的判準：**任何從內容導出的識別碼，先問「兩個不同的東西會不會拿到同一個」。**
    答不出來就加雜湊——它便宜，而碰撞的代價是靜默的。

- **為什麼當初會這樣寫**：鍵要**人看得懂**（報表上要顯示是哪一段程式）。
  那個需求是對的，錯在**用同一個字串同時當顯示與識別**。
  → **顯示與識別要分開**：`前 60 字元 + '#' + 全文雜湊` 兩者都滿足。

- **相關**：[history/039](history/039-靜默回退掩蓋辨識歧義.md)、
  `knowledge/skills/build-guardrail/SKILL.md` 第 11 步

### 行為對、身分對，標籤仍然可以說謊——而只有使用者會發現

- **實際發生**：一名學生反應「stack 和 queue 的 `push` 意思不一樣，所以積木寫錯了」。查證後**兩邊都對，但對在不同的層**：執行器完全不分支（`arr.value.push(val)`，stack 與 queue 走同一行）→ **身分是對的**；而標籤 `"Push %2 onto %1"` 的 **“onto” 字面就是「放到…上面」——那是堆疊語義**，佇列的元素是排到後面去。中文的「推入」中性一點，但一樣沒說放到哪裡。
- **同一個檔案裡有反證**：vector 那顆的標籤反而寫對了——`"在 %1 尾端加入 %2"`／`"Append %2 to %1"`，**它說了「尾端」**。所以這個專案知道怎麼寫好標籤，只是共用的那顆沒寫。
- **它落在所有既有檢查的外面**：執行測試綠（行為對）、來回轉換綠（產出的 C++ 對）、五路完備綠（五路都在）、就近性／中立性／語法耦合無關。**十八條護欄一條都不會叫，而使用者第一眼就看出來。**
- **教訓**：**「換一個檢視它會變嗎」那條試金石判得出身分，判不出形態有沒有說謊。** 形態的正確性沒有內部 oracle——產出的程式碼可以完全正確，而描述它的那句話完全錯誤。
  → 可操作的判準：**一個操作的標籤要說出「作用在哪裡」，不只說「做什麼」。**「推入容器」是做什麼，「推到頂端」才是作用在哪裡。同一顆元件在不同上下文的標籤**應該不同**——那不是重複，那是形態的職責。
- **而這揭露了一個更大的洞**：`knowledge/` 裡**沒有任何一則來自實際學習者的回報**，而這個專案的 telos 是教育。所有內部論證都自洽，**而沒有東西能證偽「這樣好教」**。協定、五槽、命名空間、膠囊全部來自第一原理推導與跨專案協商——那些是好論證，但它們都沒有被真人打過臉。
  → **使用者回報是唯一能證偽「這樣好教」的證據，它的權重與實測相同**，不該被當成軼事。
- **來源**：[episodes/2026-08-07-學生說積木寫錯了](episodes/2026-08-07-學生說積木寫錯了.md)

### 共用一個實作，可能是「行為一樣」，也可能是「差別沒被模型化」

- **實際發生**：身分健檢護欄報出六顆宣告概念**宣告完全相同**，並問「型別是身分還是參數？」。量執行器：五顆共用 `execVarDeclare`，只有 pointer 有自己的——**看起來像「行為一樣，可以合併」**。
- **跟 `g++` 對答案之後，答案是第三種**：

  | | 直譯器 | g++ |
  |---|---|---|
  | `static int n` 跨呼叫保存 | **111** | 123 |
  | `int& r = a; r = 9;` | **59** | 99 |

  **兩顆的身分是對的，缺的是行為。** 而它們之所以三個月沒被發現，正是因為共用執行器讓「沒實作」長得像「一樣」。
- **這是一個新的偽裝**：既有的教訓講的是「量測工具量錯」與「標籤說謊」。這個不同——**程式碼本身是誠實的**（它確實共用一個函式），誤導的是**從「共用」推出「相同」**這一步。
- **教訓**：**看到兩個概念共用一個實作，先問「它們**應該**一樣嗎」，而不是「它們一樣，所以可以合併」。** 判準是外部真實（編譯器、規格），不是自家程式碼的形狀——**自家程式碼正是可能漏掉那個差別的地方。**
  → 操作化：合併任何身分之前，**先為每一顆寫一支對照外部真實的行為測試**。測試綠了才談合併；紅了就是找到缺陷，而那比合併有價值。
- **來源**：`specs/099-*`、`tests/integration/declare-family-semantics.test.ts`

### 量測錯了，會逼出一個假的宣告——因果方向是新的

- **實際發生**：完備性 harness 靠正則剝掉「看起來像鷹架」的行（`#include`／`using `／`int main`…）來算「這個概念自己產出了什麼」。而 `cpp_include`／`cpp_using_namespace` 這幾個概念的**產出就是那個形狀**——輸出被自己的過濾器剝光 → 判成殼。
- **然後有人把殼消掉了**：那四顆被加上 `skipPaths: ['generate']`、理由 `consumed-by-parent`。**實測推翻**（樹裡有 include 節點 → 出現；拿掉 → 不出現）：是節點自己產生的，父概念沒有消費它。
- **`history/018`「用宣告刷數字」的因果方向是新的**：那條講的是「有人為了讓數字好看而宣告」。這裡是**量測工具先報了一個假的殼，宣告是為了消掉那個假殼**——動機甚至是正當的，而結果一樣是把一個實作說成不存在。
- **教訓**：**一個宣告出現時，先問「它在回應什麼」。** 如果它在回應一條護欄的紅燈，那要先確認**那個紅燈是真的**——否則護欄與宣告會互相背書：護欄說有殼、宣告說那是設計，而兩邊都是錯的。
  → 修法不是刪宣告，是**修量測**：「這個概念自己產出了什麼」改用**差分**算（有節點 vs 只有鷹架），不用形狀猜。**沒有這個節點時也會出現的，就不是它產的**——這條判準不需要知道哪些行是鷹架。
- **來源**：`tests/integration/audit-completeness.test.ts` 的 generate 那一段

### 機制做對了，而使用者拿不到它——工具箱／入口從來沒有人量過

- **實際發生**：097 把「一個身分、多個積木形態」做出來了，測試全綠、十七條護欄零上升、積木確實顯示「推 1 到 stk 的頂端」。**使用者拿去跑，第一句話是：「這樣會不會讓學生困擾找不到積木？」**
- **會，而且比找不到更糟**：工具箱裡只有中性版，學生拖出來、填入變數、**同步之後那顆積木被換成另一個型別，文字自己變了**。他沒有要求任何改變。而「推到頂端」那顆根本不在工具箱裡。
- **為什麼十七條護欄一條都沒叫**：**工具箱的內容從來沒有人量過**。五路完備管的是「這個元件的五條路都在」，不管「使用者找不找得到它」。**「做得出來」與「拿得到」是兩件事，而所有既有的檢查都只量前者。**
- **修法出乎意料地小**：工具箱是一份手寫清單，換兩行就好——而且**教得更好**（學生在工具箱就看到堆疊與佇列的差別，不必拖出來才知道）。原本打算建一條「動態標籤」通道，查了之後否決：多一整條推／讀通道去解一個兩行的問題，違反憲章 I。
- **教訓**：**一個功能做完之後，去看它的入口。** 元件加好了、路徑通了、測試綠了——然後問「使用者要從哪裡拿到它？」那個入口（工具箱、選單、搜尋）通常是一份**手寫清單**，而手寫清單不在任何護欄的視野裡。
- **這是第二次由使用者發現、而不是由測試發現**（前一次是同一輪的標籤說謊）。兩次都落在「機器驗得到的正確性」與「人拿得到的價值」之間那道縫裡。
- ⚠️ **原本的判準不夠，而證明它不夠的是我自己（2026-08-07，E 項）**：這一條原本寫著
  「新增一個使用者選得到的東西時，**同時問**『它在哪個清單裡』」。寫下它之後，
  **同一個 session** 我又漏了兩顆——098 的 `c_map_assign`、095 的 `cpp_istringstream_declare`。
  真的蓋了護欄之後第一次跑，指名 **7 顆**（另含 `<fstream>` **整個模組**使用者拿不到）。
  → **修正後的判準：問清單不夠，要蓋機械檢查。**
  「新增使用者選得到的東西時同時問一句」是一條**靠記得**的規範，而這一條教訓
  自己就是它失效的證據。**一條教訓寫進知識庫，不會讓下一次不發生——只有機械檢查會。**
  （第十九條護欄「可拿性」：`tests/integration/audit-toolbox-reachability.test.ts`）
- **這與「處方只留在發現它的那一處」是同族的第二個實例，而且更乾淨**：那次是
  處方沒被推廣到同形的檔案；這次是**教訓寫下之後，同一個作者在同一個 session 裡又犯**。
  中間沒有時間、沒有換人、沒有換檔案——只有「靠記得」這個機制本身失效。
- **來源**：`specs/097-multi-form-projection/`（使用者實測那一節）、`specs/100-registry-derived-lists/`、[episodes/2026-08-07-學生說積木寫錯了](episodes/2026-08-07-學生說積木寫錯了.md)、[episodes/2026-08-07-使用者拿不到那七顆](episodes/2026-08-07-使用者拿不到那七顆.md)

### 症狀是「忘了登記」，而宣告其實是對的——呈現層把它吃掉了

- **實際發生（2026-08-07，E 項）**：可拿性護欄報出七顆使用者拿不到的積木，我準備把七顆都加進工具箱清單。查 `cpp_getline` 的宣告時發現它的 `category` **已經是 `'io'`**——`cpp_ifstream_declare`／`cpp_ofstream_declare` 也是。三顆宣告完全正確。
- **吃掉它們的是一個分堆邏輯**：

  ```ts
  const universalIo = ioTypes.filter(t => t.startsWith('u_'))
  const cppIo       = ioTypes.filter(t => t.startsWith('c_'))   // ← cpp_ 兩邊都不屬於
  ```

  `'cpp_getline'.startsWith('c_')` 是 **false**。兩堆都不收，於是靜靜地丟掉。
- **這是第三種偽裝**，前兩種是「量測工具量錯」與「共用執行器讓沒實作看起來像一樣」。這一種的特徵：**症狀與「忘了登記」一模一樣（使用者拿不到），而修法完全相反**——加進清單只會多一份手寫真相，真正要修的是分堆邏輯。
- **一般化的形狀**：**把集合分成兩堆時，第二堆要是「其餘」，不是另一個述詞。**
  `filter(p)` ＋ `filter(q)` 只在 `q ≡ ¬p` 時才是**劃分**；只要有一個元素兩者皆非，它就消失了，而**沒有任何地方會出聲**。
  → 判準：看到兩個並列的 `filter`／`if`，問一句「**有沒有東西兩邊都不屬於？**」寫成 `filter(p)` ＋ `filter(!p)` 就不可能漏（讓錯誤無法被表達）。
- **診斷順序也要換**：報表說「拿不到」時，**先讀那顆東西的宣告，再去看清單**。反過來做的話，第一直覺（忘了登記）會給出一個能讓測試轉綠、而根因還在的修法——下一顆 `cpp_*` 的 I/O 積木還是會消失。
- **來源**：`src/ui/toolbox-builder.ts`（`buildIoContents` 的註解）、[episodes/2026-08-07-使用者拿不到那七顆](episodes/2026-08-07-使用者拿不到那七顆.md)

### 一支測著死程式碼的測試，綠燈與測著活程式碼的長得一模一樣

- **實際發生（2026-08-07，E 項）**：`toolbox-categories.ts` 裡有一份 `buildIoCategoryContents`，與 `toolbox-builder.ts` 的 `buildIoContents` 是**同一段邏輯的第二份拷貝**。`isIoCategory` 走的是 builder 的預設路徑，所以**沒有任何產品程式碼在用它**——只有它自己的單元測試在叫。
- **那支測試三個月來一直是綠的。而那份拷貝有一模一樣的缺陷**（同樣把 `cpp_*` 的 I/O 積木丟掉）。它的綠燈**不但沒有保護任何東西，還讓那段邏輯看起來被覆蓋過了**。
- **與「機制有了，沒人接上」的差別**：那條講的是**機制**沒有呼叫者——症狀是「功能沒發生」。這條講的是**測試**沒有被測對象——症狀是「信心是假的」。後者更難發現，因為前者至少會有人抱怨功能不對。
- **它落在所有十九條護欄的外面**：`audit-orphan-implementations` 量的是「已註冊執行器的概念」減「概念定義檔宣告的概念」（基線 `_meta.rule` 逐字），看不到「匯出了但只有測試在叫的函式」。
- **量了之後發現它不是孤例**：`src/` 匯出 178 個函式，**26 個產品程式碼零引用**。而最嚴重的一筆是 `generateCode`——它與 `generateCodeWithMapping` 是**同一段 setup 的兩份拷貝**，production 走後者，而**384 次測試引用走前者**。兩份一旦分歧，整套測試會在驗證一條沒有人執行的路徑，**而全套仍然是綠的**。
- ⚠️ **量它的時候我差點丟掉正確的結果**：掃描器報 `generateCode` 是「只有測試在叫」，我憑印象判定掃描器壞了、對使用者說了那個結論，去查才發現**錯的是我**。
  → **「在已知答案的樣本上驗過」只有在那個答案是查過的時候才有效。** 用**記得的**答案驗工具，驗的是記憶不是工具。這是那條判準（本檔「靜態判斷不能下結論，只能排順序」）沒說出來的前提。
- ⚠️ **寫完這一條之後三個 commit，我又做了一次——而且更嚴重（2026-08-07）**：
  導出改用 `owner` 比對時，我建了 `src/blocks/universal.ts`⟨曾經⟩當蓋章的唯一入口，
  **卻只把測試路徑改過去**。`app.ts` 與 `module.ts` 仍 import 原始 JSON，
  於是每個 `(universal)` 段落在正式路徑上回傳零筆——**通用積木整批從工具箱消失，
  學生的起始關卡只剩兩個分類、沒有任何 statement 積木**。全套 222 檔全綠。
  **是使用者截圖發現的。**
  → 症狀比死程式碼更難察覺：死程式碼至少是**多**一份沒人跑的；這次是
  **同一份資料有兩條載入路徑**，測試走 A、production 走 B，而 A 是對的。
- **教訓**：**一段程式碼唯一的呼叫者是它自己的測試時，那不是「被測過」，是「只有測試需要它」。**
  → 判準：寫測試時問一句「**產品程式碼從哪裡進到這個函式？**」答不出路徑就先答這個，再寫斷言。
  → 而**重複的兩份實作最容易掉進這裡**：其中一份被接上，另一份留著測試——兩份都綠，而只有一份在跑。
  → **建了「唯一入口」之後，第一件事是搜尋舊入口還有誰在用**，不是改測試。
    留一個舊入口在那裡，等於宣告「唯一」的那句話是假的。
- **修法不是把 import 改對——那只是再賭一次沒有人會忘記**。真正的修法是讓
  **組裝只剩一份**（`all-declarations.ts`），測試與 production 用同一個函式，
  於是分歧**無法被表達**。＋ 一條護欄看「每顆積木都蓋了章」——因為漏蓋不會產生違規，
  它產生的是**空段落**，而空段落與「這個分類就是這麼小」長得一模一樣。
- **而它不是一次手滑，是系統性的**：事後量，**還有 21 個測試檔**握著同一扇舊門。
- ⚠️ **一年後（2026-08-10）還有八扇沒關，而是新功能撞出來的**：
  搬第一顆元件進膠囊（`specs/104`）時，`all-declarations.ts` 那句
  「**所有宣告的唯一組裝點**」被證實是假的——測試 helper（4 份）、
  整合測試（3 份）、`strategies.ts` 的一段推導（1 份），共八處各自組一份。
  症狀**各不相同且都指向錯的方向**：孤兒實作、幽靈積木、render 缺、初始值被丟掉。
  → **它們全部在同一天現形**，因為元件膠囊是第一個「**只存在於正式路徑**」的
    宣告來源。在此之前八份的內容完全一樣，所以沒有任何訊號。
  → **一份重複的組裝不會自己現形，要等到兩份的內容真的不一樣那天**
    ——而那天通常是重構日，也就是最不想同時處理八個無關紅燈的那一天。
  → 上面那條處方（「建了唯一入口之後，第一件事是搜尋舊入口還有誰在用」）
    **當時就寫下來了，而它沒有被執行**。這是「教訓記下來、處方記下來，
    而程式碼仍然帶著那個病」的又一個實例——**處方需要的是一條護欄，不是一句話**。
  → 所以判準要再往前推一步：**建了「唯一真相」之後，第一個動作是讓舊路徑無法通行**
  （搬走／改名，讓 `tsc` 當場爆），**不是改測試**。舊路徑還走得通 ＝「唯一」那句話
  是假的，而它會安靜地假下去。已蓋成第二十條護欄（唯一入口，**硬性零不用棘輪**——
  棘輪是給「大量既有違規慢慢還」用的，這一條只要開一個口就整條失效）。
- **不對稱是關鍵**：**多餘是吵鬧的，缺失是靜默的。** 多一顆積木看得到；少一整包
  只會讓某個分類「看起來就是這麼小」。→ 斷言要寫成**存在性**（「這些必須在」），
  不能只寫成一致性（「和上次一樣」）——一致性比對的是我選的那個狀態，而我選錯了狀態。
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)「機制有了，沒人接上」；本檔「與其偵測錯誤，不如換一個讓錯誤無法被表達的形式」（**只有一份實作**就沒有這個問題）
- **快照為什麼也沒抓到**：工具箱快照用「**全部概念可見**」，而使用者看到的是課程的**起始關卡**。全部可見時少掉的那批被其他分類的數量蓋過去，結構上看不出來；只有起始關卡會裸露（那一關幾乎只有通用積木）。
  → **快照要釘在使用者實際會看到的狀態上，不是最大的那個狀態。**
- ⚠️ **第三次，而這次「最大狀態」換了一個維度（2026-08-07）**：`priority_queue` 補齊五路、
  進了工具箱、專屬測試全綠——**而它沒有被任何課程收錄**，`getVisibleConcepts` 把它擋掉，
  學生永遠看不到。測試用的是「全部概念可見」，使用者看到的是**課程的可見集合**。
  使用者的話是「priority_queue 我沒看到呀」。
  → 前一次的判準寫「不是最大的狀態」還不夠精確——**可見度有兩層**（課程收錄 × 關卡解鎖），
    而測試預設把兩層都開到最大。判準要問的是：**這顆東西要通過幾道閘門才會到學生眼前？
    每一道都測了嗎？**
- **而「忘了收錄」是完全靜默的**：課程護欄刻意不把「未收錄」算成違規（做成違規會逼出
  「為了讓護欄綠而亂塞課程」）。修法不是改成違規，是**不強迫收錄、但強迫做決定**——
  沒收錄就要寫下為什麼不教。同一次清出另外 **8 顆**同樣靜默的（093／095／098 加的），
  它們全都是「加了元件而忘了課程那一格」。
- **五次都是使用者發現的**（標籤說謊、變體不在工具箱、通用積木消失、
  `priority_queue` 學生看不到、飛出選單一團亂），而共同點不是不夠小心，
  是**我量的東西不是使用者看到的東西**——所有量測都停在瀏覽器之前。
  → **而五次裡有四次現在有機械檢查了**（可拿性／唯一入口／課程可見度／訊息參數一致性）。
    只剩「標籤說謊」那次沒有，因為它是**語義正確性**——`push` 在堆疊與佇列上意思不同，
    那本來就是人的領域。**這個比例是這一輪真正的成果**：不是「更小心」，是把四種
    失敗方式各自變成一條會紅的檢查。
  這一格**目前無解且明確未補**：Blockly 12 的 FocusManager 在 happy-dom 下 inject
  失敗；退而求其次用 headless Workspace 的 spike 報了 13 筆**全是假的**違規
  （未註冊的是命令式動態積木、建不出來的是沒載 i18n）。
  → **半調子的冒煙測試比沒有更糟**：第一天 13 筆誤報，只會教人忽略它。
    要補這一格該走真瀏覽器，不是 happy-dom。在那之前，「人工看一眼」是流程的一步，
    不是可省的收尾。
- ⚠️ **而它當天就咬了（2026-08-07）**：使用者回報工具箱「怪怪的」，我第一時間從
  螢幕截圖的像素去推，推了兩輪都沒指到根因。**改用真瀏覽器 ＋ DOM 量測**之後，
  一次就看到：飛出選單裡每一顆積木的 `transform` 都是 `translate(0, 0)`、高度 0、
  `textContent` 空的——`updateToolbox()` 換掉工具箱時，開著的飛出選單不會跟著重排。
  → **看得到的東西不要用猜的。** 從像素推 UI bug 的錯誤率高到不值得——
    真瀏覽器 ＋ 量 DOM 是幾分鐘的事，而它給的是**數字不是印象**。
  → 同一次也證明了**修法要驗，不能只推理**：第一版修法（換完再關）看起來合理，
    而**把它退掉之後數字一模一樣**——它既沒修好也不是原因。
    ⚠️ 真正的根因是**少一個 i18n 鍵**：`%{BKY_…}` 展不開 → 訊息沒有 `%1` →
    Blockly `jsonInit` 拋例外 → `clearOldBlocks()` 中斷 → 舊積木永遠清不掉。
    **「重疊」與「少一個翻譯鍵」之間沒有任何直覺連結**——照著堆疊追，不要照著症狀猜。
  → 這條已 skill 化：[[diagnose-in-browser]]（先讀 console → 量 DOM → 做對照實驗）。
- **來源**：commit `ab516ed`（刪除 `buildIoCategoryContents`）、commit `88682a6`（通用積木消失的修復與三道新檢查）、[episodes/2026-08-07-使用者拿不到那七顆](episodes/2026-08-07-使用者拿不到那七顆.md)

### 同一件事推導兩次，兩份就會分歧——而分歧的症狀在很遠的地方

- **實際發生（2026-08-08）**：沒有顯式 `renderMapping.fields` 的積木投影
  （實測 **107 / 131**）靠推導決定「積木欄位 ↔ 語義參數」的對應。
  而推導有**兩份**：`pattern-renderer.ts:365` 與 `pattern-extractor.ts:225`。
- **兩份不一樣**：渲染那份認 `field_multilinetext`，抽取那份不認。於是
  `c_comment_block` 的內容**渲染得出去、抽取不回來**——
  使用者在積木編輯器裡寫的區塊註解**會消失**。
- **症狀離根因很遠**：唯一的表現是「切換積木風格之後東西不見了」，
  而那時使用者已經不記得自己寫過什麼。**沒有任何錯誤訊息。**
- **而它躲得住是因為隔壁那顆活著**：`c_comment_doc` 幾乎一模一樣，
  只因為它剛好有**顯式**的 `fields` 就沒中。
  → **一顆會掉、一顆不會，兩顆看起來一樣**——這比兩顆都掉更難發現。
- **與「一支測著死程式碼的測試」同族，但更糟**：那次是**同一段邏輯的兩份拷貝**
  （`buildIoCategoryContents`），有同樣的缺陷；這次是**兩份不同的拷貝**——
  它們不只重複，還分歧，而分歧只在某些輸入上顯現。
- **收場（同一天）**：兩份合併成一份，再把 **186 筆對應固化成顯式宣告**，
  然後把推導**刪掉**。做法是 E 項那一招：**先拍合併後的照片 → 固化 → 驗一字不差 → 才刪**。
  → 而固化的真正目的不是消滅重複，是**解耦**：推導拿 `concept.properties` 去比對
    積木欄位名，於是**參數宣告驅動了抽取行為**。刪掉之後，把 `var_declare` 的參數列
    整個換成不相干的名字，抽取結果一模一樣——那正是 C1 需要的自由。
  → **代價要有收據**：新積木必須自己宣告對應，忘了不會靜默推導，
    由第二十五條護欄當場指名。**顯式 ＋ 護欄，不是隱式魔法。**
- **教訓**：**同一件事在兩個地方各推導一次，就是在等它們分歧。**
  → 判準：看到「推導／衍生／fallback」出現在兩個模組裡，先問
    **「這兩份會不會不一樣？」**——問法不是讀程式碼比對，是**餵同一個輸入給兩邊，比對輸出**。
  → 而 MEMORY.md 早就記著這個坑的形狀（「只在 Block Style 切換時才暴露」）。
    **記著形狀不會讓它不發生**；把它變成「餵同一個輸入比對兩邊」的護欄才會。
- **來源**：`tests/integration/audit-derive-agreement.test.ts`（第二十四條護欄）、
  `tests/integration/comment-block-roundtrip.test.ts`

### 正則不適合對有巢狀結構的程式碼動刀——同一天栽三次

- **實際發生（2026-08-08）**：把重複的推導合併、把測試改成新契約，我三次用正則
  切／插 TypeScript 區塊，**三次都壞**：
  1. 刪方法時多刪了一堆匯入與私有欄位
  2. 再刪時把 `PatternRenderer.render` 整個刪掉
  3. 插入 `renderMapping` 時插到物件的閉合括號**之後**，語法直接炸
- **三次都是 `tsc` 或測試當場叫住**，所以沒有流出去——但每一次都要還原重來。
- **成功的做法是兩種**：**精確字串取代**（`Edit` 的 old/new 必須完全相符），
  或**先算出行號範圍再逐行操作**。兩者的共同點是**不靠正則去理解巢狀**。
- **這與掃描器那幾次是同一個根**：本檔「量測工具的第一版會安靜地量錯」裡，
  區塊切割、註解誤判、子節點混淆——**全部是正則在假裝它懂結構**。
  差別只在：掃描器錯了會給出假數字（靜默），改程式碼錯了會語法炸（大聲）。
- **教訓**：**要按結構操作程式碼，就用能理解結構的工具。**
  → 讀：TypeScript 編譯器 API（`tests/helpers/param-reads.ts` 就是這樣做對的）
  → 寫：精確字串取代，或行號範圍
  → **正則只適合「找出候選」，不適合「決定邊界」。**

### 一份「宣告」可能正在驅動行為——改它不是文件工作

- **實際發生（2026-08-08，C1）**：`concepts.json` 的 `properties: string[]` 看起來是
  **描述**（這顆元件有哪些參數）。要把它結構化成規格，第一步是讓宣告符合實際。
  改下去，來回轉換兩次變紅。
- **它其實是資料**：`PatternExtractor.deriveRenderMapping`（`pattern-extractor.ts:243`）
  拿 `concept.properties` 去比對積木欄位名（`findMatchingProperty('NAME', […])`），
  推導出抽取用的欄位對應。**改參數列 ＝ 改抽取行為。**
- **兩次撞牆**：把 `input` 的參數列改成「實際被讀的那三個」→ 身分守恆紅
  （`name` 是抽取器經推導對應讀的）；刪掉 `cpp_increment` 看似死掉的大寫退路
  → 來回轉換紅（那個退路是抽取器餵的）。兩次都還原。
- **教訓**：**動一份宣告之前，先問「有沒有人拿它當資料用」。**
  → 判準：grep 那個欄位名在 `src/` 的**讀取**（不是宣告處），
    看有沒有出現在 `if`／查表／比對裡。出現了就不是描述。
  → 而**掃描器看不到這種讀取**：它是「拿整份清單去比對」，不是「讀某一個欄位」。
    我的參數讀取掃描器有三條路徑（TS 產生器／執行器、`codeTemplate`、
    **推導出的欄位對應**），而它只看得到前兩條。
- **連帶**：這也改了護欄的判定方式——見 [[build-guardrail]] 第 6.8 步補的第二個問題
  （「留一筆規範還成立嗎」決定規範值不值得；「**修一筆要付多少**」決定硬性零還是棘輪）。
- **來源**：`specs/102-param-spec/research.md` 決定 6

### 計畫裡的機制斷言，要嘛附行號，要嘛標「未驗」

- **實際發生**：097 的計畫排了一條**「不可調換的硬邊」**——存檔轉換必須先於積木型別改名，理由寫得斬釘截鐵：「反過來做的話，中間會有一段時間任何人重開專案都會壞，那是不能賭的」。實作時去讀程式碼，前提整個垮掉：`app.ts:241` 載入時 `blocklyState` **原樣還原**、`sync-controller.ts:202` **任何編輯都從語義樹重新渲染** → 正確做法是**加法式**（舊型別保留當中性形態），**根本不需要改名，也不需要轉換**。整條硬邊消失。
- **同一輪的第二次**：規格指名阻斷點是 `BlockSpecRegistry.byConceptId`。研究階段一查，它**零呼叫者**——真正的活路徑是 `PatternRenderer.renderSpecs`。
- **兩次的共同形狀**：一個**讀程式碼就會知道**的事實，被當成已知寫進計畫，而且**寫得比任何有證據的段落都更有信心**（「不可調換」「必須」）。
- **這不是「量測工具第一版會安靜地量錯」**：那條講的是工具實作錯。這裡沒有工具，只有**沒去查**。差別在修法：那條要修工具，這條要**改寫計畫的方式**。
- **而流程救了其中一次**：`research.md` 抓到了阻斷點那個，因為它被寫成一個要回答的問題。硬邊那個沒被抓到，因為它**被寫成結論，沒有被寫成問題**。
- **教訓**：**計畫裡任何一句「X 會導致 Y」的機制斷言，要嘛附行號，要嘛標成未驗。** 標了未驗它就會進研究階段；沒標它就會被當成地基，而蓋在上面的東西（排程、硬邊、風險表）全部要重做。
  → 而**語氣愈斬釘截鐵，愈該要求行號**——那正好是最不會有人回頭去查的那一句。
- **同族的第二種：不是假的，是空的（2026-08-07，E 項）**。研究階段的決定 3 寫著
  「讓 `registryCategories` 支援**更細的比對**——而**細分規則本身是宣告的資料**」。
  它讀起來像一個決定，有理由、有否決的替代方案。實作時才發現**那句話沒有指涉物**：
  `containers` 這個 `category` 底下 49 顆積木要散進四個工具箱分類，而它們身上
  **除了 conceptId 之外沒有任何欄位分得開**——而「用 conceptId 比對」正是決定 3
  自己否決掉的做法。
  > **一個沒有指涉物的設計，讀起來與一個有指涉物的完全一樣。只有去實作它才會發現。**
  → 判準：**設計句子裡的每個名詞，指得出一個欄位、函式或檔案嗎？** 指不出來就
  標未驗，和機制斷言同一條紀律。「更細的比對」「適當的抽象」「合適的機制」
  這類**看起來已經決定、其實只描述了願望**的句子，是這個坑的入口。
- **兩者的差別在修法**：假的斷言修完之後計畫還在（換一條路）；空的設計**沒有東西可修**——
  它要作廢重來，而且愈晚發現重做的範圍愈大（決定 3 是在 tasks 都寫完之後才垮的）。
- **來源**：`specs/097-multi-form-projection/`（plan.md 的硬邊、research.md 的更正）、`specs/100-registry-derived-lists/research.md`（決定 3 作廢那一節）、[history/029](history/029-E項登錄表導出-清單不是碎裂是量測算錯了.md)

### 修好一個 bug 之後，去掃同形的地方——否則處方只留在發現它的那一處

- **實際發生**：097 新增的 `form` 欄位在 `BlockSpecRegistry.loadFromSplit` 被**逐欄位列舉**的建構丟掉，症狀看起來像形態選擇函式壞掉，而其實資料在進到它之前就沒了。
- **而處方早就寫在本檔**：「與其偵測錯誤，不如換一個讓錯誤無法被表達的形式」那一則的第一個實例逐字寫著「**展開合併 → 漏欄位不可能**」。**它只被套用在發現它的那一處（存檔）。** `loadFromSplit` 是一模一樣的形狀，沒有人去掃。
- **所以這不是「第 N 個實例」，是更難看的一種**：教訓記了、處方也記了、**而程式碼仍然帶著那個病**，只是換一個檔案。記錄本身讓人以為處理過了——與 [history/025](history/025-那個-flake-從真實變數改為診斷過但沒人接上.md) 同族，只是那次是沒建機制，這次是機制建了沒推廣。
- **教訓**：**一個 bug 的處方寫進知識庫時，同時做一次「還有哪裡是同一個形狀」的掃描**，而且那個掃描要留在原地（一支測試、一條護欄、或至少一行「已掃過 X、Y、Z」）。
  → 判準：**這個處方如果只有一個使用者，它就還沒完成。**
- **而同一條判準也適用於「機制」**（2026-08-07，B 項一輪四次）：`Scope.declareRef`（別名）、`Scope.parent`（走到根作用域）、`ctx.isExpression`（產生的位置）、`SAMPLE_CONTEXT`（合成樣本的脈絡）——**四個機制都早就在，而各自只有一個使用者**，於是需要它的第二處各自繞路或直接壞掉。
  這與 [concepts/執行機構.md](concepts/執行機構.md)「機制有了，沒人接上」是同一件事的兩半：那裡講的是「零個使用者」，這裡講的是「**只有一個**」——而只有一個的更難發現，因為它看起來是在用的。
  → **新增一個機制時，最後一步是找出它的第二個使用者。** 找不到就先不要抽象化（那是 YAGNI）；找得到卻沒接上，那才是這裡說的病。
- **來源**：`src/core/block-spec-registry.ts` 的展開合併（2026-08-07 才補上）

### 契約要能執行，才發現得了契約自己不自洽

- **實際發生**：097 的 `data-model.md` 寫了不變式 **FS-2「fallback 必須在 forms 的值域裡」**。第一版把中性形態**只寫在 `fallback` 而沒放進 `forms`**——於是 FS-2 **永遠不成立**。契約測試第一次跑就紅在「合法的形態集合必須通過」那支。
- **修的是設計，不是測試**：中性形態是一顆**真實存在的積木**（統一的 `c_container_push`），它本來就該被宣告出來。因此引入保留鍵 `_`。
- **關鍵**：這個矛盾**用讀的看不出來**——`data-model.md` 每一條分開看都合理，是把它們一起編碼成可執行的斷言時才撞在一起。
- **教訓**：**契約文件寫完不算數，把它變成會跑的斷言才算。** 而且第一次跑要**同時餵合法與非法輸入**——只餵非法的話，一個「什麼都拒絕」的驗證器會全部通過，而契約自己不自洽這件事永遠不會現形。
- **來源**：`specs/097-multi-form-projection/`（data-model.md FS-2、`tests/unit/core/form-selection.test.ts`）

### 「不在本次範圍」要留下一個會叫的機制，不是留一句話

- **實際發生**：`fuzz-cpp-*` 幾個呼叫編譯器的測試在高負載下隨機變紅。成因**四天前就診斷對了**，逐字寫在 draft 裡：「沒有自訂 timeout，吃 vitest 預設的 5000ms」「**不在 P0 範圍**（只量不修），記在此供後續期別處理」。
  範圍切得對——P0 只量不修是紀律。**壞的是「供後續期別處理」沒有任何機制**：沒有測試、沒有護欄、沒有路線圖項目，只有一句話。
- **那句話產生了反效果**：它讓「**已知的載入敏感 flake**」變成一個**看起來已經處理過的說法**。後續開發中它被用來帶過紅燈至少四次，每次都附「單獨跑就綠」當證據——那個證據是真的，而它剛好是**逾時不足的症狀**。修好之後全套連兩次 211 檔全綠、零 FAIL。
- **順帶推翻了它自己的分類**：當時歸成「真實變數（機器負載消除不了）」。負載確實消除不了，**但逾時是我們設的**——把一個可調的參數說成環境條件，等於把問題移出可修的範圍。→ 判準：**這裡面有沒有一個是我們設的參數？** 有的話它就不是環境。
- **教訓**：**寫下「之後再處理」的那一刻，它就從一個問題變成了一則記錄**——而記錄看起來像處理過了。這與「記錄代替修復」是同一種便宜，只是延後的形式讓它更難察覺：那時你**確實**做了正確的事（診斷、切範圍），只是停在最後一步。
  → **延後要留機制**：一支 `it.fails`、一條護欄的基線、或一個帶驗收標準的路線圖項目。三者都會在該叫的時候叫。**一句話不會。**
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)「機制有了，沒人接上」——這一則更極端，機制連建都沒建；下一條「宣稱『與我無關』之前先去量」的歸因做**對**了，本則揭露的是歸因做對之後仍然可以停在半路
- **來源**：[history/025](history/025-那個-flake-從真實變數改為診斷過但沒人接上.md)、`specs/096-vector-source-init/`

### 停用測試的標籤有四層可信度，而只有最底層是證據

- **實際發生**：查證停用測試的第八批，三支全部標著 `[UNSUPPORTED:vector 的初始化列表語法尚無對應概念]`。實測後：**兩支標籤過期**（初始化列表 094 早就做了，直接開回來就綠，測試本體一個字沒改），**一支標籤方向錯**（列表支援了，掉的是**函式呼叫**當初始值）。
- **它打在既有判準的縫裡**：既有的判準是「逐筆註解是一手記錄，檔案層級的概括是二手概括，衝突時信前者」。這一次衝突發生在**同一支測試內部**——**標題裡的標籤**錯了，而**它下面的註解本體**（「function call initializer is not lifted」）是對的。
- **階梯其實有四層**：檔頭概括 ＜ 逐筆標題標籤 ＜ 逐筆註解本體 ＜ **實測**。愈上面愈是為了「歸檔」而寫，愈下面愈是為了「記住剛才看到什麼」而寫。**標籤是給護欄數的，註解是給人讀的**——前者被寫的時候，寫的人在意的是分類欄位填得對不對，不是它指的方向對不對。
- **八批累計的規律**：~50/64 的標籤是過期的或指錯方向的，而查證過程本身翻出 20+ 個真實缺陷。命中率最高的一批是標著 `[UNVERIFIED]`（「不知道為什麼停用」）的——**因為沒有人假裝知道，所以也沒有人去看**。
- **教訓**：**只有最底層那一層是證據，上面三層都是關於證據的說法。** 照標籤規劃工作之前先量一次；成本通常是跑一支測試，而收益是不去改一段已經正確的程式碼。
- **相關**：本檔「量測工具的第一版會安靜地量錯」第三個實例——**護欄把兩種停用測試當成同一種數**。那條講的是護欄量錯，這條講的是**被量的東西上面貼的標籤本身不可信**：即使護欄數對了，它數的是一堆說法。
- **來源**：`specs/096-vector-source-init/`、`tests/baselines/defect-ledger.json`

### 宣稱「與我無關」之前先去量——歸因不做就只是推託

- **實際發生**：P0 完成後全套測試在高負載下失敗 2–8 個。直覺是「這是既有 flake，跟我的護欄無關」——聽起來很合理，因為失敗的是呼叫編譯器的 fuzz 測試。
- **做法**：**切回功能前的 commit 跑同一套**，結果失敗 **8 個**（比加了護欄之後的 2–3 個更多）；再做 A／B 量測（含護欄 27.21s vs 不含 27.84s）確認護欄本身幾乎不增加耗時。**這時才敢寫「與本功能無關」。**
- **教訓**：「這不是我造成的」是一個**可驗證的宣稱**，而驗證通常很便宜（切一個 commit、跑兩次）。不驗就說，和「記錄代替修復」是同一種便宜——**聽起來像處理過了，實際上只是講過了**。
- **⚠️ 第二個實例，而這次錯的是「是我造成的」那一邊（2026-08-14）**：CI 紅而本機綠，
  我判定成因是「護欄從自己所在的目錄撈語料，把**自己的注入樣本**吃了進去」，
  **並照著改了程式碼、寫了一段很篤定的註解**。而那個注入樣本用的是**單引號**，
  撈語料的 regex 只抓反引號——那條因果從一開始就不成立。
  （真正的成因是參照編譯器在兩台機器上是兩個實體，見
  [history/059](history/059-棘輪假設同樣的輸入給同樣的數字.md)。）
- **而那個錯誤診斷的危險之處不是它錯，是它解釋得通而且有教育意義**：
  「護欄吃自己的注入樣本」聽起來就像這個專案會犯的錯，與這份檔案裡既有的
  十幾條教訓同一個口吻。**一個像本檔會收錄的診斷，比一個明顯錯的更難被懷疑。**
- **判準**：一個成因**改動程式碼之前**要能指出**它為什麼會發生**的機械證據
  （哪一行 regex、哪一個引號）。說得出故事不算——故事是最便宜的那一半。
- **來源**：`knowledge/draft/retired/2026-08-05-元件膠囊重構.md` 坑 ⑥；`d4389e0`

### 守「未來」的規範，症狀恆為零——所以它最容易空著沒人發現

- **實際發生**：`history/005`（2026-03-09）決定 pattern 歧義用偏序仲裁，三種情況各有處置，狀態欄寫「✅ 已採用」。四個月後第一次為它蓋執行機構才發現：**情況二（交叉歧義報註冊錯誤）從來沒有實作過**，實測 18 對規則落在那裡卻無人報錯；情況一用的是 constraint **數量**這個代理指標而非偏序本身，導致 5 條 constraints 完全相同的規則同分，其中 4 條成為永不被試到的死規則。
- **為什麼能空四個月**：因為它守的不是「現在是對的」，而是「**未來的變更不會破壞現在**」。靠登記順序決勝負的規則**現在幾乎都是對的**（否則專案早就不能用了）——它不會出現在任何失敗測試、任何 bug 報告裡。**零症狀不是健康，是這類規範沒實作時的正常表現。**
- **兩種護欄要分開講**：多數護欄量的是「**有多少東西壞了**」，辨識歧義那條量的是「**有多少東西靠運氣**」。後者的數字下降**不代表修好了 bug**，而代表移除了一個未來會咬人的機會。把兩者混在同一個「品質分數」裡，會讓人以為靠運氣的那部分是安全的。
- **教訓**：一條規範如果的形式是「新增 X 不得改變既有 Y」，**它不做也不會痛，直到很痛**——因此它比任何其他規範都更需要註冊時的機械化檢查。而且**知識庫自己的狀態欄也會存殼**：`history/` 標「已採用」的是當時的**意圖**，不是實作的證據。回頭引用一條舊決定時，先問「它的哪一半真的落地了」。
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)、[history/016](history/016-偏序仲裁的情況二從未實作.md)、[history/005](history/005-pattern-歧義從禁止到偏序仲裁.md)
- **來源**：`specs/051-lift-claim-arbitration`、`tests/baselines/lift-ambiguity.json`
- ⚠️ **同一個病的第二個實例，而它在同一條護欄上連續發生兩次（2026-08-10）**：
  蓋符合性護欄時，第一版比對 `blockDef` 的 input 名與接點名（報 9 筆），
  第二版改讀 `block-registrar.ts` 的 `appendValueInput('X')`（還是 9 筆）。
  **兩版都在量名字對不對——而那是代理。** 符合性問的是「接點回不回得來」。
  第三版改成**合成節點走一次 render → extract 再比對回來的子節點**，報 7 筆，
  而且**完全不比名字**——`extract` 自己會套用 `renderMapping`，走完來回名字問題就不存在了。
  → 判準：**「我量的這個東西，與我想知道的那件事，中間隔了幾層推論？」**
    隔了一層以上就是代理，而代理會在你沒想到的那一層失效。
  → 救它的是「先在**已知答案的樣本**上驗過」（`build-guardrail` 第 6 步）——
    沒有那個樣本，9 筆誤報會被當成真相寫進基線。


### 一個取捨若沒有退出條件，它會永遠留著

- **實際發生**：修函式族的參數時（`specs/105`）要在兩條路之間選——
  照 `u_func_def` 複製 82 行 Blockly 動態積木 × 6，或讓那六顆沿用既有的
  單一文字欄位、加一個宣告式映射。選了後者，理由是前者**正是這階段在治的碎裂**。
- **而那個選擇與專案自己的一條教訓有張力**：
  「需要 parse 回結構才能用的字串，就不該是字串」。
- **處置不是忽略張力，也不是為它辯護，是給它一個出口**：
  寫下**三個升級訊號**，任一出現就該改成結構化插槽——
  ① 使用者在那個欄位打錯字造成的缺陷**出現第二次**
  ② 需要對它做**逐項操作**（重排、單獨刪一個、下拉選單）
  ③ 那個邊界情形從「測試裡的案例」變成**課程裡真的會教的東西**
- **教訓**：**一個取捨若沒有寫下退出條件，它就不是取捨，是預設值。**
  → 而退出條件要是**可觀察的事件**，不是「等我們有空」或「等它變嚴重」。
    「出現第二次」數得出來，「變嚴重」數不出來。
  → 這與護欄的「本護欄不檢測什麼」是同一條紀律：**劃出邊界，
    而不是假裝沒有邊界**。差別是那個講能力，這個講時間。


### 「沒有值」與「沒有人讀」是兩個不同的問題

- **實際發生**：要把六顆元件的 `args`／`params` 從**屬性**移到**接點**（`specs/106`），
  動手前立了一道閘門——**先量那個屬性有沒有值**。量完六顆都是「出現 0 次」，
  於是判定它是死宣告，安全可移。
- **而移完之後有一條護欄變紅**：`audit-param-spec` 報「程式碼讀了規格裡沒有的參數」。
  去查，兩處 legacy fallback 正在讀 `node.properties.args`。
- **兩者都是對的**：那個屬性**真的沒有值**，而**真的有人在讀它**——讀到 `undefined`，
  走進一條永遠走不到的分支。
- **教訓**：**「這個欄位有沒有值」與「有沒有人讀這個欄位」是兩個問題，
  而只量前者會漏掉後者。**
  → 移除一個宣告之前，兩個都要量：**寫的人**（有沒有值）與**讀的人**（有沒有引用）。
  → 而這一次的答案讓修法更好——那兩處是**死程式碼**，該刪掉而不是保留；
    只量了「有沒有值」的話，會把它們留著，於是宣告收不乾淨。
- **順帶**：一條護欄替另一條護欄的盲區補了位。
  `audit-param-spec` 量的是「讀的人」，而我立的閘門量的是「寫的人」——
  **兩條合起來才是完整的問題**。這也是為什麼護欄要多而不是要大。


### 量測低報到**零**是最糟的方向——看不見的缺陷與不存在的缺陷長得一樣

- **實際發生**：缺陷帳（追蹤停用測試的護欄）在同一個月裡被發現**三個**盲區，
  每一個都讓某一整類缺陷從帳上完全消失：

  | | 盲區 | 怎麼發現的 |
  |---|---|---|
  | 1 | 掃描範圍只有 `tests/`，而元件膠囊的自證測住在 `src/components/` | 寫第一顆膠囊的自證測時 |
  | 2 | 掃描是**逐行**的——把標題換到下一行那筆就消失 | 同上，而全庫只有我那一筆是跨行的，**所以從來沒被觸發過** |
  | 3 | 完全不認 `it.fails` | 釘住一個已知缺陷時 |

- **第 3 個最難看**：`build-guardrail` **明確推薦**用 `it.fails` 釘住已知缺陷
  （它會跑：缺陷還在時綠、修好時紅並提醒拔釘子）。而缺陷帳看不到它。
  → **「用推薦的方式釘住缺陷」＝「那筆缺陷從帳上消失」。**
    那是一個**獎勵錯誤行為**的量測。補上之後多出來的兩筆裡，
    有一筆已經在那裡很久了，而且沒有分類標記。
- **教訓**：**量測出錯的方向要分開看。低報到「少一點」還有得救——
  數字不對但形狀還在；低報到「零」沒有：一筆看不見的缺陷，與一筆不存在的缺陷，
  在報表上長得一模一樣。**
  → 判準：**「這個量測有沒有某一整類輸入會回報零？」**
    有 → 那一類就是它的盲區，而**盲區的大小量不出來**（它在盲區裡）。
  → 具體做法：把量測的**入口條件**寫成斷言（掃了幾個目錄、認得哪幾種寫法、
    語料幾段），而不是只斷言結果。三個盲區裡有兩個是靠「換一種寫法試試」
    發現的，不是靠讀程式碼。
- **而護欄要多而不是要大**：三個盲區有兩個是**別的護欄**替它補位發現的
  （`audit-param-spec` 量「讀的人」、符合性量「回得來嗎」）。
  一條大而全的護欄沒有第二雙眼睛。


### 一支斷言「檔案裡有這個字串」的測試，全綠不代表行為是對的

- **實際發生**：要把兩顆積木重複的參數列抽成工廠（`specs/107`）。動手前查覆蓋，
  發現那個檔的 12 對動態插槽行為**零自動化覆蓋**：
  - `happy-dom` 跑不動 Blockly 12 的 FocusManager
  - 生產的投影入口 `renderToBlocklyState` 產的是純 JSON，**完全不經過 `Blockly.Blocks`**
  - 而 `block-registrar.test.ts` 的四支測試是 **`readFileSync` ＋ `toContain('saveExtraState')`**
- **那四支測試會通過任何一次重構**，只要字串還在檔案裡——包括把功能改壞的那些。
- **而它們同時會擋住對的重構**：把行為搬到另一個檔，字串就不在了，
  測試變紅——**為了一個錯誤的理由**（能力沒有消失，只是被共用了）。
- **教訓**：**斷言「原始碼長什麼樣」與斷言「系統做什麼」是兩件事，
  而前者會同時放過真的退步、擋住真的改善。**
  → 判準：**「這支測試會因為什麼而紅？」** 答案若是「檔案內容變了」而不是
    「行為變了」，它就不是行為測試，報表上不該算成覆蓋。
  → 而它**仍然可以有用**——「`app.ts` 裡不得有 `Blockly.Blocks[`」是一條真的架構約束，
    文字就是它要管的東西。**問題不在它存在，在它被讀成行為覆蓋。**
- **這一次它直接改變了一個設計決定**：工廠因此**放在同一個檔**，
  而不是抽成新模組——不是因為那樣比較好，是因為搬走會讓那四支測試
  因為錯誤的理由變紅，而**這次沒有第二個 oracle 能分辨對錯**。
  → 沒有行為覆蓋的地方，**重構的形狀會被測試的形狀綁架**。


### 與其偵測錯誤，不如換一個讓錯誤無法被表達的形式

- **實際發生**：存檔層把 `blockStyleId` 與 `locale` 每次都丟掉——呼叫端傳了、介面宣告了、組合新狀態時**沒有列舉到**。兩個欄位在型別上是選填的，編譯器不會抱怨。使用者的積木外觀與介面語言**從來沒有被保存過**。
- **兩條修法**：
  - **偵測**——保留逐欄位列舉，加一支測試抓漏。下次還會漏，只是會被抓到。
  - **消除**——改用展開合併 `{...預設, ...既有, ...傳入}`。**漏欄位在結構上不可能發生。**
- **同一個功能裡出現三次**：
  1. 展開合併 → 漏欄位不可能
  2. `refused` 分支型別上必帶 `backedUpTo` → 「拒絕了但沒備份」**編不出來**
  3. `satisfies Record<keyof Required<T>, 1>` → 欄位清單與型別漂移**編不過**
- **關鍵的但書**：**消除之後，偵測的測試仍然保留**。它守的不是同一件事——修法消除的是今天的 bug，測試守的是「有人日後把展開改回列舉」。以為消除之後就不用測，是把兩種保護當成同一種。
- **判準**：拿到一個 bug 先問「有沒有一種寫法讓它連寫出來都不行」。答得出來就選那個，然後**仍然把測試留著**。答不出來才退回偵測。
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)「把推斷改成宣告」是同一個家族——那條講的是讓**缺失**可偵測，這條講的是讓**錯誤**不可表達
- **來源**：`specs/052-storage-integrity-gate/research.md` F4／F5

### 檔案怎麼分，和東西屬於誰，是兩回事

- **實際發生**：把執行器搬進各自的模組時，spec 寫的是「`strings.ts` → `std/string/executors.ts`」。**錯的**——那個檔跨兩個標準函式庫模組（`<string>` 17 個、`<cstring>` 10 個）。同樣地 `containers.ts` 跨六個。
- **為什麼會寫錯**：檔名長得像歸屬。但**執行器檔案是按「誰跟誰寫在一起方便」分的，歸屬則寫在概念註冊表裡**，兩者從來沒有義務一致。
- **怎麼查對**：不看檔名，去問註冊表——每個概念宣告在哪個模組的定義檔裡，它就屬於誰。查完才發現還有 7 個跨容器的泛用操作**不屬於任何標準函式庫**，而它們的產生器早就在語言核心——**它們的家本來就在核心，只有執行那一路走丟了**。
- **教訓**：**重構前先問「這個東西屬於誰」，答案要從宣告來，不從檔案位置來。** 檔案位置是上一次重構留下的痕跡，不是事實。
- **來源**：`specs/054-execute-into-capsules/research.md` F1／F2

### 每條防線都有它抓不到的形狀，而那個形狀要說出來

- **實際發生**：搬移的主防線選了「搬移前後執行引擎認得的概念集合必須完全相同」——因為逐一比對輸出**漏一個不會現形**（測試沒覆蓋到就過了），集合比對會。這個選擇是對的，它在第一次搬移就抓到「推送函式寫了卻沒人呼叫」。
- **但它抓不到另一種錯**：拆分工具第一版用括號深度找區塊邊界，字串裡的括號把計數弄歪，**把兩筆註冊併成一塊**。併起來之後概念**還在**，只是跑進了錯的模組——**集合完全相同，防線全綠**。
- **抓到它的是別的東西**：27 vs 25 這個對不上的數字。而 research 的自我否證正好預先寫過「若某份執行器用批次註冊，正規表示式會漏掉」。
- **教訓**：選一條防線的時候，同時寫下**它抓不到什麼**。「漏失」與「錯置」是兩種形狀，集合比對只抓得到前者。**沒說出來的話，全綠會被讀成「都對了」。** 這與護欄要聲明「本護欄不檢測什麼」是同一條紀律，只是套用在臨時的驗證手段上。
- **來源**：`specs/054-execute-into-capsules/`、[concepts/執行機構.md](concepts/執行機構.md)

### 護欄常常只問了一個方向

- **實際發生**：完備性護欄迭代**概念註冊表**，所以它問的一直是「已宣告的概念，五條路都有實作嗎」。**它從來沒問過反方向**——「有實作的東西，有被宣告嗎」。於是 4 個有執行器卻沒有定義的概念**完全不在任何報表上**。補上宣告後，它們身上藏了很久的 8 個缺口才第一次現形。
- **同一天的第二個實例**：P9 的原文是「拔掉 C++ 後核心**無 `languages/cpp/` import**」，而中立性護欄從第一天起數的是**概念身分字串**。一個核心檔可以一個 C++ 概念名都不提、卻直接 import 整個語言套件——身分掃描全綠，P9 已經破了。**那條原則自己寫下的檢查，兩個月來沒被做過。**
- **為什麼會這樣**：護欄是照著「我們怕什麼」寫的，而怕的通常是**已經被咬過**的那一種。沒被咬過的方向就沒人想到。
- **教訓**：蓋一條護欄時多問一句——**「這條規範還有沒有另一個方向？」** 具體做法是把規範的主詞與受詞對調再讀一次：「宣告的有沒有實作」→「實作的有沒有宣告」；「核心提到語言概念」→「核心引用語言套件」。對調之後讀起來成立的，就是漏掉的那一半。
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)、`specs/055-finish-executor-move`

### 「指不到」與「沒有」回傳同一個值時，破 75% 也沒人知道

- **實際發生**：131 個概念宣告了語言中立的父概念，**98 個（75%）指向根本不存在的概念**。查詢的函式長這樣：

  ```
  findAbstract(id) {
    if (!concrete?.abstractConcept) return undefined   // 沒有宣告
    return this.concepts.get(concrete.abstractConcept) // 指不到 → 也是 undefined
  }
  ```

- **所以它壞掉的樣子，與「這個概念沒有宣告父概念」完全一樣**——而後者是完全可以接受的狀態。於是一個四分之三是破的抽象層，看起來像「大部分概念還沒填父概念」。
- **連帶的後果**：介面層需要「這個概念退回哪個通用概念」時，抽象層指不到東西，只好**自己寫一份 16 行的對照表**。同一個事實因此有兩處記載，而正解那一處是壞的。
- **教訓**：一個查詢若把**「沒有設定」與「設定了但指不到」回傳同一個值**，那個欄位的完整性就永遠沒有人會發現。**判準**：對每個「指向別的東西」的欄位問一句——「填錯的時候，回傳值跟沒填一樣嗎？」一樣 → 需要一條檢查存在性的護欄。
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)「護欄常常只問了一個方向」
- **來源**：`specs/056-abstract-concept-integrity`

### 掃描要分得出「這裡實作了它」與「這裡提到它」

- **實際發生**：把 30 個概念的父概念改指向 `var_declare` 之後，就近性護欄把 `var_declare` 的擴散度從 12 檔算成 **19 檔**——它把 `"abstractConcept": "var_declare"` 這個**值**當成 `var_declare` 的實作足跡。
- **那是反的**：越多概念認它當父概念，它就越「碎裂」。而事實正好相反——那代表抽象層更完整。
- **同一個形狀先前出現過兩次**：中立性護欄要剝離註解（提到 ≠ 耦合）；P9 的 import 檢查第一版忘了剝離，把一句解釋用的註解報成違規。
- **教訓**：任何以「字串出現次數」為基礎的掃描，都要先問**「這一次出現，是實作、是引用、還是只是提到？」** 三者混算的話，數字會在**做對事情的時候變糟**——那比數字錯更危險，因為它會懲罰正確的改動。
- **來源**：`specs/056-abstract-concept-integrity`

### 半條路修好，比沒修更難察覺

- **實際發生**：`specs/050` 修好了陣列初始值的**辨識**——`int a[3] = {1,2,3}` 的值會進到語義樹的 `values` 子槽，測試也釘住了。但**執行那半從來沒接上**：`array_declare` 的執行器只按大小配置預設值，從頭到尾沒讀過 `values`。
- **症狀**：`int a[3]={3,1,2}; cout << a[0];` **輸出 0**。而在語義樹上、在積木上、在產生的程式碼上，那三個值**都看得見**。
- **為什麼特別難察覺**：修好的那半會產生「已經修好了」的證據——round-trip 測試綠、語義樹裡有值、缺陷帳上那條被劃掉。**證據是真的，只是不完整。**
- **教訓**：修一條跨越多個路徑的缺口時，**逐路確認，而不是確認「那個 bug 的測試綠了」**。這個專案有五條路（辨識／產生／渲染／取回／執行），一條路的測試綠不代表另外四條接上了。
- **相關**：[concepts/執行機構.md](concepts/執行機構.md)「功能完成 ≠ 功能接通」是同一族——那條講的是「元件都寫好了但沒人呼叫」，這條講的是「一條路修好了，另一條沒有」
- **來源**：`specs/053-declare-noop-execute/classification.md` 追加分類

### 殼的數字要按**原因**分組，才看得出它是幾個問題

- **實際發生**：完備性報表說有 78 個殼。按原因分組之後：

  ```
  38  execute｜空操作且未宣告      ← 真工作
  34  lift｜找不到原本的身分        ← **一個根因**
   4  generate｜輸出為空或佔位
   2  lift｜降級成 raw_code
  ```

- **那 34 個不是 34 個問題**：查下去發現多數是合成樣本產生了錯的程式碼（`compare` 產生 `1 + 1`）或語法上孤立的片段（`case 1:` 沒有 switch）。**20 個是量測造成的。**
- **教訓**：一個護欄報「N 個違規」時，先問**「這 N 個是幾個原因？」** 不分組的話，一個根因會被讀成 N 個待辦，而清償優先序會照著那個假的 N 排。**分組的成本是十行程式碼，不分組的成本是把力氣花在錯的地方。**
- **來源**：完備性護欄的原因分佈區塊

### 機制跨不過分層時，複製一份到界線另一邊，而不是把機制變大

- **實際發生**：積木插槽名的「唯一真相」機制住在核心側，只涵蓋通用積木。語言專屬積木的插槽名因此仍然寫死。第一個結論是「**遷不了**——要涵蓋語言積木，那個模組就得引用語言套件，與『核心不認識語言』相衝」。
- **那個推論漏了一步**：正確的形狀不是讓核心那份變大，是**讓語言套件自己提供一份**。語言套件引用自己的宣告檔完全沒有問題，而消費者（介面層）從兩邊各取所需。
- **一般化**：當一個機制「涵蓋不到某類東西」而理由是**分層限制**時，先問——**這個機制能不能在界線的另一邊各存在一份？** 能的話，兩份同形的小機制勝過一份跨界的大機制。跨界的那份會把分層打破，而分層本來就是為了讓拔掉一邊還能運作。
- **代價要記**：多一份模組，就多一個檔提到那些概念。這次是 `cpp_string_at` 從 9 檔變 10 檔——**殺掉雙重真相換一個元件多碰一個檔**，與先前拆分執行器時的交易同型。
- **來源**：`specs/057-single-source-input-names`

### 量錯的方向通常是「把不該算的算進來」

- **實際發生**：同一個問題的量法換了三次，每次數字都更小、更誠實：

  | 版本 | 量什麼 | 數字 | 錯在哪 |
  |---|---|---|---|
  | 一 | 比對兩邊的插槽名集合 | 29 | 動態側只算輸入不算欄位，JSON 側兩者都算——**兩邊定義沒對齊** |
  | 二 | 對齊之後再比 | 18 | 用常數寫的插槽名被判成「動態側沒有」——**那些恰恰是正例** |
  | 三 | **有 JSON 對應卻仍寫死** | **13** | — |

- **第三版還排除了 57 處**：mutator 容器、執行期生成的插槽、純排版用的虛擬輸入——它們沒有 JSON 對應物**是設計如此**。
- **模式**：三次錯都是**把不該算的算進來**，沒有一次是漏算。原因很直白——**寬鬆的量法比較容易寫**，而數字大看起來又像「發現了很多問題」。
- **判準**：一個護欄報出 N 之後，先問**「這 N 裡面有哪些本來就該長這樣？」** 而不是先問「怎麼把 N 降下來」。
- **來源**：`specs/057-single-source-input-names`

### 單向的棘輪不會自己收緊——改善也要紅

- **實際發生**：一條護欄的基線寫著 4，實際量到 1。前一個功能修好了三個，**沒有重新產生基線**。而棘輪只擋上升，所以它**默許退回 4** 而不會有人發現。
- **改善訊息一直都印著**（「有改善，可下調基線」），印在 stdout 上。**沒有人會為了一行輸出去改檔案。**
- **教訓**：棘輪要真的「棘」，**兩個方向都要出聲**。改善時擲錯，訊息寫「請下調基線並與這次改善一起 commit」——下調是十秒鐘的事，而且本來就該在同一個 commit 裡。
- **與「不准紅著過夜」（[vision](vision.md#開發約束跨階段不隨階段退場)）不衝突**：那條講的是不要**放著**，不是不要紅。一個十秒能修的紅，比一個沒人看的 stdout 有用得多。
- **推廣**：任何「只在變壞時出聲」的機制，都要問一句——**變好的時候誰負責把門檻拉上來？** 答案是「靠人記得」的話，那個門檻遲早會鬆掉，而且鬆掉的樣子與從來沒緊過完全一樣。
- **來源**：`tests/helpers/guardrail.ts` 的 `assertRatchet`

### 合成測試的**環境**也要真實，不只輸入要真實

- **實際發生**：完備性護欄與身分測試都寫著 `const STYLE = { id: 'default' } as StylePreset`——一個**不存在的風格**。於是 `io_style` 是 undefined，產生器走了非預期的分支：

  ```
  假風格  print → 「printf("%d", 1);」→ 辨識回 cpp_printf  🔴
  真風格  print → 「cout << 1;」      → 辨識回 print       ✅
  ```

- **後果不只是一個數字錯**：那個假象被 `specs/050` **當成已知缺陷釘住**，還附上「兩條修法都會動跨風格既有行為，因此擱置」的完整分析。**那整段分析建立在一個假的測試環境上。**
- **釘住一個量測假象，比不釘更糟**——它讓後續每一個讀到的人相信系統壞了，而且釘子本身看起來像盡責。
- **`it.fails` 這個機制沒錯**：假象一被修掉，它立刻變紅提醒拔釘子，完全按設計運作。**問題不在機制，在釘之前沒有先確認缺陷是真的。**
- **教訓**：合成測試裡凡是「隨手造一個物件塞進去」的地方都要問——**真實流程裡這裡是什麼？** 輸入的真實性通常有人管，**環境（風格、設定、註冊表狀態）的真實性沒有**。
- ⚠️ **「輸入的真實性通常有人管」在 2026-08-10 被推翻了一半**：那是對**單筆合成輸入**說的（有人會盯著看），而對**大批撈來的語料**不成立——`specs/108` 一輪之內語料錯了三次，其中一次差 200 倍。
  → 分界：**輸入是一顆一顆寫的，就有人管；是一批撈的，就沒有。** 見本檔「量一個新刻度時，最可能錯的是語料」。
- **來源**：`specs/057`、`specs/050/research.md` D3

### 「後註冊的贏」的註冊表，順序就是優先權——而順序沒有人在設計

- **同一件事咬了三次**：

  | 何時 | 什麼被蓋掉 | 症狀 |
  |---|---|---|
  | 052 之後 | 四個轉型概念的真實作被「無執行行為」清單覆蓋 | `static_cast<int>(3.9)` 輸出 `void` |
  | 054 之後 | 模組的執行器被同一份清單覆蓋 | 概念從清冊裡消失 |
  | 057 | 語言套件的條件編譯實作被核心的缺口清單覆蓋 | `#ifdef` 的 body 不跑 |

- **三次都不是「寫錯」**，是**註冊順序**：`Map.set` 後寫的贏，而順序取決於建構式裡幾行呼叫的先後——**那個先後從來沒有人刻意設計過**。
- **修法不是每次都去調那一行**，是把優先權變成結構：**語言套件對自己的概念有最終發言權，所以它的註冊放在最後**，並在那段程式碼旁邊寫明「這一段不能往前搬」。
- **判準**：任何「後寫的贏」的註冊表，都要問一句——**誰應該贏？** 答得出來就把順序固定下來並寫明理由；答不出來的話，那個註冊表需要的是**衝突偵測**而不是順序約定。
- **相關**：`specs/051` 為辨識規則建了歧義護欄，執行器註冊表的重複計數是 055 才有的（第七條護欄）
- **來源**：`specs/057`

### 同一個欄位名長在三個不同型別上時，腳本改名一定會錯——而型別檢查看不到

> **母條**：「型別檢查的沉默有三種來源」——本條是**身分**那一格（一名多物）。

- **實際發生**：要把語義樹的 `concept` 改名成 `componentId`（跨專案協商定案的詞彙）。做法是「先改介面，讓編譯器指出每一處」——那一步很乾淨，94 處一次改完。
- **然後就翻車了**，因為 `concept` 這個名字**同時長在三個不同的型別上**：

  | 型別 | 欄位 | 意思 |
  |---|---|---|
  | `SemanticNode` | `concept: string` | 這是哪個元件的實例 ← **要改的** |
  | `BlockSpec` | `concept: { conceptId }` | 這顆積木對應的概念物件 ← **不能改** |
  | `ConceptMapping` | `concept` | 又是另一回事 ← **不能改** |

- **編譯器只守得住第一個**。測試檔裡的 `concept: { conceptId: 'number_literal' }` 被改成 `componentId: {...}` 之後**照樣編得過**（測試不在型別檢查範圍內），只有跑起來才錯。
- **腳本還做了兩件更糟的事**：刪掉一整段執行器、把一個**刻意擺在建構式最後**的區塊移到中間（那個位置有註解寫著「放在前面的話核心的空操作會蓋掉語言的真實作，這個專案已經被同一件事咬過三次」）。
- **最後回退了。** 121 個檔改動、10 個檔紅著、根因不明，而**這次改名沒有任何功能價值**（純詞彙對齊）。無法在合理時間內逐一驗證的修改不該留著。
- **教訓**：
  1. 大規模改名前，先問**「這個名字在幾個型別上出現？」** 只有一個才適合腳本。
  2. **型別檢查涵蓋不到的地方（測試、JSON、字串鍵）就是腳本會錯的地方**——那裡沒有安全網。
  3. 腳本改名要**每次只動一種語法形式**（屬性存取／物件鍵／型別標註各一輪），而不是一次全部。混在一起的話，出錯時分不出是哪一種形式錯了。
- **來源**：`specs/058-core-type-vocabulary`（已回退，方法要換）

### 「另一顆元件需要認得它」是真的耦合，不是碎裂

- **實際發生（2026-08-11，F 膠囊搬家 167 → 0）**：搬不動的元件裡最大一類
  **不是實作散落**——是**別的程式碼認得它的身分**：

  ```
  cpp:switch 的執行器要知道哪一支是 default:   → 它認得 cpp:default
  cout << … 要知道哪些東西需要括號            → 它認得 cpp:ternary / cpp:comma_expr
  核心的 createEmptyProgram() 要建一棵空樹     → 它認得 cpp:program
  ```

- **兩者在護欄的數字上長得一模一樣**（就近性都會指名），而處置完全不同：

  | | 處置 |
  |---|---|
  | 實作散落 | 把實作剪回膠囊 |
  | 被別人認得 | **把「我是什麼」變成元件自己的宣告**（消費者有它自己的家，搬走是錯的）|

- **判準**：**消費者在問這顆元件的「性質」還是「實作」？**
  性質 → 一條性狀；實作 → 那是五路，該搬進膠囊。

- **⚠️ 而性狀只講得出「我是什麼」，講不出「我在哪裡時算什麼」**：
  `#include` 在哪裡都是鷹架，而 `return` **只有在 main 裡**才是。
  共用一個旗標的話 `return` 在任何函式裡都會消失。
  → 條件切兩半：`scaffoldInMain` 是元件的宣告，「在不在 main 裡」留在消費者手上。

- **⚠️ 換的過程可能換出一條反向依賴**：核心的執行器改問性狀之後，
  **核心就 import 了語言套件**——中立性護欄當場抓到（P9 的字面違反）。
  處置是讀取器分兩層，核心只讀膠囊宣告。

- **相關**：[concepts/性狀](concepts/性狀.md)、[history/049](history/049-F完成-把身分換成性狀.md)

### 順序是一個沒有被宣告的判別——而修法是讓順序不再有影響

- **實際發生（2026-08-11，同一天三次）**：

  | 哪裡 | 兩者為什麼平手 | 誰決定勝負 |
  |---|---|---|
  | doc_comment vs block_comment 的 lift pattern | `/**` 也 startsWith `/*`，優先權都是 15 | 陣列順序 |
  | pointer_assign vs var_assign_compound 的 astPattern | **兩者都宣告 `constraints: []`**，而前者其實有條件 | 陣列順序 |
  | `BlockSpecRegistry.loadFromSplit` 的「第一個勝出」 | 註解擋住了**覆寫** | 「中性的那一顆排第幾」 |

- **三次的共通點**：程式碼裡**寫著一句看起來像規則的話**
  （「第一個勝出——後者覆寫的話，改變 JSON 順序就會改變行為」），
  而那句話只擋住了它想到的那一種順序依賴。

  > **「第一個勝出」只在「該贏的那個排第一」時才是規則，
  > 否則它只是把順序偽裝成規則。**

- **⚠️ 它為什麼在膠囊化時才爆**：共用檔的陣列順序是人寫的、穩定的；
  搬進膠囊之後順序由 **`import.meta.glob` 的字母序**決定。
  **同一份資料換一種載入方式，就失去了那個沒有被宣告的判別。**

- **兩種修法，而第三次才選對**：
  1. 補上顯式宣告（`priority`、`constraints`）——修那一筆
  2. **讓順序不再有影響**（登錄時先把中性排到前面）——修那個機制

  第三次選了 2，並且**故意把 JSON 排成亂序驗證過**。

- **症狀有多遠**：`int x; int y; int z;` 的類別成員來回轉換後變成三個 `int x;`
  ——而 lift 的產出是正確的，錯在投影那一側。

- **相關**：[history/049](history/049-F完成-把身分換成性狀.md)、
  本檔「後註冊的贏的註冊表，順序就是優先權」

### 一個迴圈註冊出來的 N 個實作，在按位置記帳的護欄裡只算一筆

- **實際發生（2026-08-11，一天三次）**：

  ```ts
  for (const c of ['cpp:cast_static', 'cpp:cast_dynamic', …]) register(c, async … )
  ```

  四顆元件的執行器**來源位置是同一行**。於是：
  - 第二十三條護欄（參數規格一致性）分不出是哪一顆 → 四顆的退路都寫 `'int'`
    而宣告是 `int*`／`Derived*`，**一筆都沒報**
  - 第三十三條護欄（靜默回退）把三顆 cctype 元件算成兩筆帳

- **膠囊化把迴圈拆成 N 個檔，護欄立刻指名**——這一天三次都是這樣現形的。

- **判準**：**這條護欄是按「位置」記帳還是按「身分」記帳？**
  按位置 → 任何一對多的註冊（迴圈、共用閉包、分派表）都會讓它少報。

- **推論**：這與本檔「一個函式長得像實作，不代表它是實作」是同一族——
  **把 N 個東西寫成一個的寫法，會讓所有按「一個」計數的工具少數 N-1 個。**

- **相關**：[history/049](history/049-F完成-把身分換成性狀.md)

### 一份少一半的組裝，錯誤訊息會指向被害者，不是兇手

- **實際發生（2026-08-11，一天四次）**：測試各自列宣告來源

  ```ts
  const allConcepts = [...universalConcepts, ...coreConcepts, ...allStdModules.flatMap(m => m.concepts)]
  //                                                                        ↑ 膠囊不在這裡
  ```

  四個錯誤訊息**都不提「組裝」**：

  | 檔 | 訊息 |
  |---|---|
  | `code-to-blocks` | 「`x += 5` 辨識不出 `cpp:var_assign_compound`」 |
  | `identity-merge-expr-pairs` | 「`cpp_increment_expression` 不見了」 |
  | `p3-json-only` | 「lift 回傳 null」 |
  | `scaffold-codegen` | 「`⟨unknown concept: cpp:using_namespace⟩`」 |

  每一個看起來都像「那顆元件壞了」。

- **⚠️ 最典型的一筆**：`code-to-blocks` 的**積木那邊記得加膠囊、概念那邊忘了**
  ——同一支測試裡一半對一半錯。

- **這是卡點 6「八份各自組裝」的第 N 次重演**，而前幾次的處方是
  「收斂成一個組裝點」——**收斂之後沒有東西擋住第九份出現**。
  → 這一次蓋了第三十七條護欄，第一次跑抓到 11 份，修完歸零。

- **判準**：一個「唯一入口」如果沒有護欄盯著，它就只是**今天唯一**。

- **相關**：`tests/integration/audit-declaration-assembly.test.ts`、
  [history/049](history/049-F完成-把身分換成性狀.md)

### 一條只在過渡期有輸入的護欄，會在過渡完成的那天全部變紅

- **實際發生（2026-08-11，F 歸零那一刻）**：第十八條護欄的標題寫著
  「元件身分健檢（**膠囊化之前**）」——它審的是還住在共用宣告檔裡的元件。
  全部搬完之後它**沒有輸入了**，於是三個 `expect(...).toBeGreaterThan(0)` 同時變紅。

- **而那與它壞掉長得一模一樣**：報表全空、健康檢查全紅。

- **這是「錨在會下降的數字上」的極端形式**——不只錨點會爛，
  **整條護欄的輸入都會歸零**。而那正是它成功的定義。

- **處置**：真實資料那幾支改成「**有輸入才驗**」，
  偵測是否活著全部交給**合成注入**（`build-guardrail` 第 2 步一直是這樣要求的）。

- **判準**：蓋一條「清償期」的護欄時先問：**清完之後它量什麼？**
  答不出來 → 它的健康檢查必須從第一天就只錨在合成輸入上。

- **⚠️ 順帶記一筆**：這一輪「錨在會下降的數字上」總共出現 **7 次**，
  其中一次就在**前一批才改過的註解正下方**。散文規則擋不住，語法簽名才擋得住
  （見本檔 build-guardrail 第 2 步那條）。

- **相關**：[history/049](history/049-F完成-把身分換成性狀.md)

### 一個宣告要指涉另一顆元件時，指它的性質而不是它的名字

- **實際發生（2026-08-11）**：第三種「寫死身分」的地方不是程式碼、也不是登錄表，
  是**投影宣告本身**：

  ```json
  "dynamicRules": [{ "modes": { "select": { "wrap": "cpp:var_ref" } } }]
  ```

  「把選到的文字包成一顆變數參照」——寫在 `print_formatted` 的
  `forms/blocks.json` 裡，於是**那顆膠囊提到了另一顆的身分**。

- **三種寫死身分的地方**（處置各不相同）：

  | | 例 | 處置 |
  |---|---|---|
  | 程式碼 | `node.conceptId === 'cpp:default'` | 問性狀 |
  | 登錄表 | `{ push: 'cpp:container_push' }` | 元件自己登錄 |
  | **宣告** | `"wrap": "cpp:var_ref"` | **指性質**（`wrapTrait`）|

- **而第三種最難發現**：它不在任何 `.ts` 檔裡，掃程式碼的工具看不到它。
  抓到它的是就近性護欄的**反向檢查**（「膠囊資料夾裡不得出現別顆元件的身分」）。

- **相關**：[concepts/性狀](concepts/性狀.md)

### 一個名字取得夠好，會讓人以為自己已經讀過它了

- **實際發生（2026-08-11）**：我連續三輪對話說「核心把 `else { if … }`
  **摺成** `else if …`」，並據此推出三層結論、寫進兩份知識檔、
  開了四個候選處置。

- **而我從頭到尾沒有讀那八行程式碼。** 我看到的是：

  ```
  函式名     isElseIfChainable
  呼叫位置   core/lift/pattern-lifter.ts
  ```

  然後推出了一個行為。真正的程式碼做的是**標記**，目的**正好相反**
  ——沒有那個標記，兩種寫法會 lift 成一模一樣的樹，
  來回轉換就把使用者寫的形式改掉了。**它是保住區別的東西。**

- **四個候選處置全部作廢**——它們解的是一個不存在的問題。

- **判準**：**我對這段程式碼的敘述，是讀出來的還是推出來的？**
  推出來的就標成推測，或者去讀。⚠️ 而危險的是**名字取得好的時候**
  ——名字愈準確，推測愈像事實。`isElseIfChainable` 這個名字沒有任何問題，
  問題是它好到讓我不覺得需要打開那個檔。

- **這與本檔「一個函式長得像實作，不代表它是實作」同族，而方向相反**：
  那次是把 40 行的分支表當成沒驗過的新形狀（**高估**）；
  這次是把 8 行的標記當成一個機制（**低估**，而且方向也錯）。
  兩次的共通點：**沒有打開檔案。**

- **相關**：[draft/核心硬編了別人的文法詞彙](draft/2026-08-11-核心硬編了別人的文法詞彙.md)

### 一個在宣告處才知道、而在使用處才需要的事實，必須跟著值走

- **實際發生（2026-08-13）**：`priority_queue<int, vector<int>, greater<int>>`
  的 `top()` **回傳最大值**。比較器寫在**宣告**上，而 `top()` 的執行器
  手上只有變數名——那個資訊跟著呼叫端走，於是它在呼叫點消失了。
  症狀是**程式跑完、印出一個數字、而它是錯的**。

- **同一輪的第二個**：`v.push_back({2,1})` 的 `{2,1}` 要變成 pair 還是一串值，
  **取決於容器裝的是什麼**——而那也只寫在宣告上。

- **處置**：兩個都改成跟著**值**走（`RuntimeValue.heapOrder`／`elemType`）。

- **判準**：問「這個事實在**哪裡**被寫下來，在**哪裡**被需要？」
  兩個地方不同，而中間只傳一個名字 → **它必須搭上值那班車**。
  ⚠️ 登記處（查名字）解不了這一類：名字查得到的是「這個**種類**是什麼」，
  查不到「**這一個**是什麼」。

- **相關**：[concepts/性狀](concepts/性狀.md)「資訊必須跟著值走」

### 「上次那件事很貴」不等於「這件事很貴」——先問它們是不是同一類

- **實際發生（2026-08-13）**：三項「屬性升格成接點」被判為「該走 spec」，
  理由是本檔記過的翻車——改名腳本誤傷凍結明表，**真實使用者的舊存檔升不上來**。
  於是它們被寫進 draft 等排期。

- **而那個類比是錯的**：翻車那次動的是**凍結的歷史明表**（語義詞彙），
  這三項動的是**積木的欄位形狀**（投影）。P8 的範圍
  [history/026](history/026-P8-的範圍不含語義詞彙本身.md) 早就釐清過：
  **管投影與程式碼，不管語義詞彙。** 三項各花不到一輪。

- **判準**：引用一次過去的代價之前，問「**它們動的是同一類東西嗎**」。
  說得出類別（語義詞彙／投影／程式碼）才算引用得成立。

- ⚠️ **這個誤判的方向是保守的，所以它不會出事**——它只會讓東西一直不做，
  而**那種延遲沒有任何訊號會響**：沒有測試會紅，沒有護欄會叫，
  只有一份 draft 靜靜地躺著。

- **相關**：[history/058](history/058-問角色不問身分從一條處方變成一個機制族.md)

### 「改動前後症狀相同」只有在你確定哪一刻是「前」的時候才成立

- **實際發生（2026-08-14）**：使用者回報工具箱壞掉，我比對他**兩張截圖**
  ——第一張在我改積木之前、第二張在之後，**症狀完全相同**。
  據此寫了一份 draft 宣告「**不是這一輪的迴歸**」，並轉去查別的方向。

- **而那張「之前」的截圖不是改動之前**：它是**同一輪的更早一步**，
  而缺陷（移除 `args0`）在那之前就已經進去了。**兩張都在缺陷之後**，
  所以症狀相同什麼都不證明。

- **判準**：說「改動前後」之前，**指出那個「前」是哪一個 commit**。
  說不出 commit 的「之前」是一個感覺，不是一個參考點。
  ⚠️ 而 `git stash`／worktree 只要一分鐘——**這個專案的 feedback 早就寫過
  「發現錯誤先用 worktree 確認是不是迴歸」，而我沒有做**。

- **與本檔「宣稱『與我無關』之前先去量」的差別**：那條講的是**不驗就說**，
  這條講的是**驗了、而參考點取錯**。後者更危險，因為它**看起來像做過功課**。

- **來源**：`6cddbfa`（那份錯的 draft）→ `040eb01`（真正的根因）

### 一個被吞掉的錯誤，要用「把那件事單獨做一次」逼出來

- **實際發生（2026-08-14）**：Blockly 的 flyout 建積木時 `init` 拋錯，
  而**它被吞在 inflater 裡**——console 全乾淨，
  `window.onerror`／`unhandledrejection`／`console.error` 三個 handler 都攔了，**零筆**。
  症狀只剩「那個分類後面的積木都不見了」。

- **做法**：把 flyout 一次做的事**拆成一顆一顆做**——
  `for (const t of types) { try { ws.newBlock(t) } catch (e) { … } }`。
  一行就指名了：`cpp_array_declare → Value inputs must have a non-empty name`。

- **判準**：**症狀是「一批東西少了」而沒有錯誤訊息時，
  不要去找那個訊息——去把那批東西一個一個做一次。**
  框架吞錯誤的地方通常是「批次處理」的邊界（渲染、載入、序列化）。

- ⚠️ **而在那之前我花了四輪讀 console、加 handler、量 layout**——
  那些都是在**找一個不存在的訊息**。

- **相關**：[history/060](history/060-護欄的盲點在它自己的分類裡.md)

### 一個「查不到」與一個「沒有」長得一樣——包括在你的除錯程式碼裡

- **實際發生（2026-08-14）**：換完診斷錨點之後，我在瀏覽器裡驗它有沒有生效，
  用的是 `block.getWarningText()`。**Blockly 12 沒有那個 API**——回 `undefined`，
  而我把它讀成「**警告沒設上去**」，開始往回查一段**本來就對**的程式碼。

- **救它的是換一種查法**：改用 DOM（`.blocklyIconGroup` 的數量），
  一次就看到它一直是好的。同一輪還有第二次：`elementFromPoint` 那次
  我用合成 `.click()` 開 flyout，開不起來，而那也不是產品的問題。

- **判準**：**驗證一個東西「沒發生」之前，先證明你的查法在它發生時查得到。**
  那正是 `component-encapsulate` 步驟 3 的「每條負向前面先釘一個正向錨點」
  ——只是這次用在**除錯**而不是測試上，而除錯沒有人幫你把關。

- **與本檔「量測工具的第一版會安靜地量錯」的差別**：那條講的是
  **工具產出錯的數字**；這條講的是**查詢方法根本不存在，而它回空**。
  ⚠️ 共通的根更深，而它就是這個專案追了一整年的病：
  **「查不到」與「沒有」在畫面上完全相同**——只是這次發生在我的驗證程式碼裡。

- **來源**：`f3ba1b6`（診斷錨點），驗證繞路的過程

## 關鍵延伸（主題觸發必讀）

| 觸發關鍵字 | MUST 讀 |
|---|---|
| 積木定義、generator、extractor、動態積木、extraState、欄位同步 | `concepts/積木投影管線.md` |
| 殼、做一半、看起來完成、規範沒有檢查、護欄、audit 測試、量測工具自己量錯、死程式碼上的綠燈 | `concepts/執行機構.md` |
| 工具箱、入口、使用者拿不拿得到、形態的軸、中性形態／退路 | `concepts/積木投影管線.md` |
| 元件、接點、埠、關係律、命名、跨域、合流 | `concepts/元件.md` |
| 性狀、traits、消費者依身分分派、膠囊搬不動、「另一顆元件認得它」 | `concepts/性狀.md` |
| 概念註冊、四路/五層完備性、屬性結構化 | `concepts/概念代數.md` |
| pattern 搶匹配、歧義、constraint、lifter | `concepts/開放擴充.md` |
| 靜默失敗、降級、confidence | `concepts/降級與認知邊界.md` |
| 這兩個算不算同一個、合併、外延等價、觀察集、容差、**殘差 vs 誤差**／回饋、數位孿生 | `concepts/等價與觀察集.md` |
| 命名、scope、對內對外、詞彙表、套件與版本 | `concepts/元件.md`（命名章節） |
| 決策為什麼變成現在這樣 | `history/` |
| 某個 bug 當初的完整現場 | `episodes/` |

### 一個函式長得像實作，不代表它是實作

- **實際發生（2026-08-11，第八～十顆膠囊）**：`<cmath>` 三顆元件的 lift 那一路住在
  `std/cmath/lifters.ts` 的 `tryCmathLift()`——**40 行、三個分支、自己組
  `createNode`**。前四顆驗過的三種形狀都是「表裡的一列」或「一整筆 pattern」，
  而這個東西看起來完全不像它們，於是被歸成「第四種、沒驗過的形狀」。

  拆開之後只剩三列：

  ```
  pow      → cpp:math_pow    引數槽 base, exponent
  18 個名字 → cpp:math_unary  引數槽 value      ＋ func 屬性
   5 個名字 → cpp:math_binary 引數槽 arg1, arg2 ＋ func 屬性
  ```

- **判準**：**把它的分支排成表，每一列還剩下什麼？**
  - 只剩資料（名字、槽名、屬性名）→ **它是分派表**，資料回膠囊、判別留共用檔
  - 還剩控制流或計算 → 它才是實作

- **處置是擴表，不是開新表**：已被第二顆驗證的 `single-arg-functions` 改名為
  `call-concepts`，多帶「引數槽名」與「函式名記進哪個屬性」，舊入口留著當特例。
  **換掉一個已驗證的形狀需要理由**——而「多帶兩個欄位」不需要換掉它。

- **⚠️ 而三張登錄表刻意不合併**（`container-templates`／`call-concepts`／
  `method-concepts`）：它們各對**一個語法位置**（宣告的型別／自由函式呼叫／
  方法呼叫）。**位置決定形狀**——方法多一個 `obj`，宣告根本不是呼叫。
  合併成一張要嘛塞一堆 optional 欄位，要嘛在查詢端再分一次流。

- **同一輪的姊妹錯誤**：批准這批時我報的前提是「六顆同形狀」，而那是按
  **檔案位置**（都在 `std/<mod>/`）判的，實際橫跨三種 lift 形狀。
  → **形狀要看 lift 住在哪，不是看檔案位置**。檔案位置是看得見的，
  lift 的住處**要打開才知道**——所以這個錯誤會重演。

- **相關**：[history/047](history/047-模組是搬家的中途站不是終點.md)、
  `knowledge/skills/component-encapsulate/SKILL.md` 的五種形狀表

### 一次改名要問兩件事：哪些要改，以及**哪些因為描述過去而不能改**

- **實際發生（2026-08-11，153 顆積木型別改名）**：腳本用
  `(?<![A-Za-z0-9_])OLD(?![A-Za-z0-9_])` 掃遍整個 repo。**四類誤傷，
  每一次都是同一個形狀**：

  | 誤傷 | 後果 |
  |---|---|
  | **另一張凍結明表**（身分改名表） | ⚠️ 最危險。鍵是舊時代的**身分**，字面與積木型別一模一樣而意思完全不同——改掉 = **真實使用者的舊存檔升級不了** |
  | 知識庫 | 湮滅「化石長什麼樣」的證據 |
  | 基線／有日期的稽核報表 | 基線要**顯式重產並註明原因**，不是被腳本靜靜改掉 |
  | **護欄自己的注入錨點** | 「壞名字」被改成對的名字——**壞的輸入變成對的，注入測試靜靜失去意義** |
  | **描述舊資產的斷言** | 「這份 v9 存檔含 X」被改成新名——斷言還在，但**它問錯了問題** |

- **判準**：**這個字串是在描述現在的世界，還是在描述一個已經過去的世界？**
  描述過去的 → 排除在腳本之外。
- **⚠️ 而它們往往長得跟要改的東西一模一樣**——那正是危險的地方。
  凍結明表的鍵與待改的名字同形，是因為它們本來就是同一個命名慣例的兩個時代。
- **相關**：[history/048](history/048-積木型別導出與四類描述過去的東西.md)、
  `knowledge/skills/component-rename/SKILL.md` 第六步

### 一個被存起來、而且載入時當作還原來源的投影，行為上就是真實

- **實際發生**：積木型別按定義是**投影**，而 P8 說投影可以不相容
  （「投影可重建」）——所以改名本來不需要存檔遷移。
  而 `app.ts` 載入時第一件事是 `setState(state.blocklyState)`：
  **積木狀態才是還原的主要來源**，語義樹是之後才從它重建的。
- **教訓**：判斷「這個東西要不要遷移」，不看它在架構圖上被畫成哪一層，
  **看它在還原路徑上排第幾**。排第一的就是真實，不管它叫什麼。
- **推論**：`P8` 的例外條款（「語義詞彙的變更 MUST 附一次性轉換」）
  因此適用於它。這是 v9 → v10，**專案第一次改寫積木狀態**。
- **⚠️ 同一天的第二個實例，代價更大**：課程降級的結果也被寫進了存檔
  （`tree` 裡是 `cpp:var_declare` 而不是 `cpp:vector_declare`）。
  2026-08-09 修過「降級只作用在顯示用的深拷貝上」，**而那個修法沒有涵蓋存檔這條路**
  ——**修一條路時沒有問「同一個缺陷在別的路上長什麼樣」**。
  性質從「使用者看到一個壞掉的東西」變成「**使用者的資料被改壞了**」。
- **相關**：[history/048](history/048-積木型別導出與四類描述過去的東西.md)


### 兩份實作裡有一份是死的時，活的那份會長出只有它才有的條件

- **實際發生（2026-08-11）**：四個面板各有一個 `connectBus()` 訂閱 `semantic:update`，
  而 `app.ts` 裡另有一段硬編做同一件事。查下去：
  **`grep '\.connectBus('` 在 `src` 與 `tests` 裡零命中**——那一層整個沒有人呼叫過。
- **而兩份不一樣**：`monaco-panel` 的自訂閱只處理 `source === 'blocks'`，
  漏掉 `resync`；`app.ts` 那條兩個都處理。**漏掉的那份從來沒跑過，所以沒有人發現。**
- **教訓**：與「同一件事推導兩次，兩份就會分歧」同族，而**分歧的偵測難度不同**。
  那條是兩份都活 → 症狀會出現在某個輸入上。這條是**一份死一份活** →
  **在你刪掉活的那份之前，永遠沒有症狀**。
  → 收攏兩份實作時，**不要假設它們等價**——先逐條對出差異，
  因為死的那份的差異就是它沒被維護的證據。
- **同一輪的第三層**：`onExecutionState` 四個面板全是空樁，
  註解寫「Handled via execution:state bus event」——**在那段沒人呼叫的程式碼裡**。
  而 `emit('execution:state')` 整個 `src/` **零次**。
  宣告了事件、宣告了處理方法、零發送者：**三層都沒接上，而型別檢查全綠。**
  → **只接收端不夠。一條沒有人發的線，與一條沒有接的線，在執行時完全相同。**
- **第四個實例（2026-08-13）——而這次連「活的那份」都不是我們以為的那份**：
  `cpp:struct_at_member` 同時有 `forms/blocks.json` 的 `astPattern` 與
  `lift.ts` 的 `registerAstBranch`。**astPattern 優先，於是膠囊那一份
  從來沒有被呼叫過**——實測方式：在分支裡加一行 `console.log`，跑一次，**零輸出**。

  > **兩份宣告同時存在時，輸的那一份不會報錯——它只是安靜地沒有作用。**

  代價很具體：astPattern 的 `extract: "text"` 把 `v[0].first` 的 obj 抽成
  字串 `"v[0]"`，執行器拿去查 scope 查不到。移除那份之後
  `audit-lift-ambiguity` 的「同優先權群組 2 → 1」。
  → **偵測法**：懷疑一份實作是死的時，**在它裡面印一行**——
  比讀程式碼推導快，而且它回答的是「有沒有跑」而不是「應該會跑嗎」。
- **相關**：[執行機構](concepts/執行機構.md)「機制有了，沒人接上」、
  `src/core/view-registry.ts`、
  [draft/執行器直接持有五個面板](draft/2026-08-11-執行器直接持有五個面板.md)、
  [history/055](history/055-全修一輪-缺陷的形狀比缺陷本身重複.md) §四

### 兩份真相之中有一份長得對的時候，沒有人會去看另一份

- **實際發生（2026-08-11）**：同一個「等待輸入」狀態有兩份文案，
  **而它們連語言都不一樣**：

  ```
  console-panel.ts:313   this.setStatus('等待輸入...', 'running')      ← 硬編中文
  execution-controller   Blockly.Msg['EXEC_STATUS_WAITING'] || '…'    ← 落到英文退路
  ```

  `EXEC_STATUS_WAITING` 這個 i18n 鍵**從來不存在**（`ABORTED` 與
  `PAUSED_BREAKPOINT` 也是），所以執行器那份永遠是英文。
- **而畫面一直是對的**——因為面板那句硬編中文**後執行**，蓋掉了前者。
- **教訓**：一個缺陷可以被另一個缺陷遮住，而**遮住它的那個看起來像功能正常**。
  → 發現兩份真相時，不要因為「畫面是對的」就降低優先級——
  **那正是它活得久的原因**。要問的是「如果順序反過來，使用者會看到什麼」。
- **它是怎麼被發現的**：不是有人回報，是**把文案從執行器搬進視圖**時，
  兩份被迫放在同一個視野裡。→ 收攏耦合的副作用之一，是把重複逼到看得見的地方。
- **相關**：本檔「同一件事推導兩次，兩份就會分歧」（那次兩份都活，這次一份被另一份遮住）、
  `src/i18n/*/blocks.json`

### 估一個重構的成本時，數的是呼叫處，不是方法種類

- **實際發生（2026-08-11）**：把執行器對面板的呼叫分成「廣播／命令／詢問」三類時，
  我估廣播那類 **~20 筆**，**實測 38**。
- **低估的機制**：我在心裡數的是**方法**（`setStatus` 一種、`write` 一種、
  `error` 一種、變數快照一種 → 四種，看起來很小），
  而 `setStatus` **一個方法就佔 24 個呼叫處**。
- **教訓**：分類的單位是「種類」，而工作量的單位是「處」——
  **兩者差一個乘法，而那個乘數是不均勻的**（24 / 3 / 5 / 1）。
  → 寫下一個估計時，問一句「這個數字是種類數還是處數」。
- **附帶**：同一次預測還錯了第二件事——我寫「做完 console 與 variable
  就完全不必被執行器持有」，實際上它們仍然被持有，因為
  `clear()` 與 `promptInput()` 不是廣播。
  → **清掉一類呼叫不會讓一個依賴消失，只會讓剩下的那類變得顯眼。**
- **相關**：[draft/執行器直接持有五個面板](draft/2026-08-11-執行器直接持有五個面板.md)

### 改名把一個名字換成另一個既有的名字時，遮蔽是靜默的——而型別檢查看不到

> **母條**：「型別檢查的沉默有三種來源」——本條是**語意**那一格（多名一物）。

- **實際發生（2026-08-12，中文識別字清理）**：把 618 個中文識別字改成英文，
  用的是 TS LanguageService 的 `findRenameLocations`（正確的工具，
  它不碰字串與註解）。而其中一個測試檔裡：

  ```ts
  async function run(tree) { … }              // 外層
  it('…', async () => {
    const 跑 = async (c, k) => 'OUT:' + (await run(branches(c, k)))   // 內層
  })
  ```

  `跑` → `run` 之後，**內層遮蔽了外層**，`await run(...)` 變成呼叫自己
  ——`Maximum call stack size exceeded`。**兩處**，同一個檔。
- **`tsc` 完全不報**：兩者的簽名相容，遮蔽本來就是合法的 JS/TS。
- **與「同一個欄位名長在三個不同型別上」同族，而機制是反的**：那次是
  **一個名字對應多個東西**，改名工具分不出；這次是**多個名字被改成同一個**，
  而工具**沒有理由**認為那有問題——它只做被要求的事。
  → **判準**：改名的目標名，在它要生效的每一個作用域裡，**已經有人叫那個名字嗎**？
    LanguageService 不會替你問這一句。
- **抓到它的是測試**，不是 review 也不是型別檢查。而它的症狀（無限遞迴）
  離根因（一個內層變數改名）很遠。
- **相關**：本檔「同一個欄位名長在三個不同型別上時，腳本改名一定會錯」

### 同一個詞在型別的值與物件的鍵上，只有寫法決定它是不是識別字

- **實際發生（2026-08-12）**：一份分類表寫成

  ```ts
  type shape = '型別不符' | '缺子節點' | …          // 值：領域詞彙
  const compatible: Record<shape, …> = {
    型別不符: [...],                                // ← identifier 形式的鍵
  }
  ```

  改名工具把**鍵**當識別字改成英文，而 `shape` 那一側是**字串字面值**、
  不會被改。於是 `compatible[h.shape]` 查表回 `undefined`。
- **一個字元的差別**：寫成 `'型別不符': [...]` 就不是識別字，工具不碰它。
- **教訓**：一個詞是不是識別字，**不看它是什麼詞，看它寫在哪個語法位置**。
  → 領域詞彙（狀態名、分類名）當物件鍵時**寫成字串字面值**，
    那同時是一個宣告：**這是資料，不是程式的骨架**。
- **推論（更貴的那一半）**：**一個型別的欄位名同時是一份磁碟資料的鍵時，
  改名有兩個現場**。`理由` → `reason` 之後三支護欄變紅，
  因為 `tests/assets/*.json` 裡還是 `理由`——而症狀是「讀到 undefined」，
  不是型別錯誤。→ 改 interface 欄位名時，先問「這個型別有沒有序列化的形式」。
- **相關**：[第四十條護欄](../tests/integration/audit-identifier-language.test.ts)

### 「執行到哪裡」不能從輸出推——那是兩個時間軸

- **實際發生（2026-08-12，第一支 e2e）**：驗「斷點會停」時，我寫的斷言是
  「停住 ＝ 主控台還沒印出 6」。**它紅了**，而斷點其實完全正常
  ——變數面板顯示 `total=0`，停在第一次迭代之前。
- **根因是我對執行模型的假設錯了**：除錯模式是**先跑完再回放**
  （`executeWithSteps` 預錄步驟），輸出在錄製時就全部產生了。
- **教訓**：驗「執行進度」要看**執行狀態**（變數快照、高亮位置），
  不要看**副作用的累積**（輸出、檔案、計數器）——後者可能屬於另一個時間軸。
- **⚠️ 而這個假設在單元測試裡永遠不會被否證**，因為那裡沒有「主控台」。
  它是 e2e 接上的**第一天**抓到的第一件事，也正好說明了那條缺口的價錢。
- **相關**：`e2e/debug.spec.ts`、`knowledge/vision.md`「沒有任何自動化在看渲染出來的結果」

### 一個沒有機械檢查的慣例，不會被違反一次，它會被違反到變成慣例

- **實際發生（2026-08-12）**：使用者指出「程式碼裡出現很多中文變數名，
  應該用英文」。量出來是 **618 個不同的中文識別字、3918 次出現、223 個檔案**
  ——涵蓋兩個月、幾十次提交。
- **沒有任何一次是「決定要用中文」**。第一次可能只是順手，
  而**後來的每一次都有前例可循**。
- **與「機制有了，沒人接上」的差別**：那條講的是**建好的機制沒人用**；
  這條講的是**沒有機制**，於是慣例往阻力最小的方向漂。
  → 兩者的處置相同：**把規範變成一條會變紅的檢查**。
  而時機不同：前者是補接線，後者要**先清償再蓋**（618 → 0，然後硬性零）。
- **⚠️ 我自己在被指出的當天還加了 33 個**——**知道一條規範存在，
  與有東西擋著你違反它，是兩件事。**
- **相關**：[concepts/執行機構](concepts/執行機構.md)、
  [第四十條護欄](../tests/integration/audit-identifier-language.test.ts)


### 邊界是可以否證的，介面不行——所以邊界現在定，介面等第一個跨過去的東西

- **實際發生（2026-08-12）**：使用者要「先訂好模組邊界、互動方式、資料夾結構，
  免得到時候一團混亂還要花很多時間重構」。而前一輪才剛說過相反的話
  ——「等第二個實作才看得見正確形狀」。
- **兩句都對，而它們適用的東西不同**：

  | | 現在定 | 等 |
  |---|---|---|
  | **邊界的位置**（誰不准 import 誰） | ✅ 定，而且蓋護欄 | |
  | **介面的形狀**（那條線上傳什麼） | | ⚠️ 等第一個真的跨過去的東西 |

- **理由是可否證性**：邊界畫錯了**護欄會告訴你**——同一週發生兩次
  （中立性抓到 `universal` 放錯層、P9 護欄抓到視圖 import 語言）。
  而**介面猜錯了要等有人用才知道**，那時已經有一堆程式碼依賴它。
- **推論**：一個「先卡位」的欄位或介面，與一個沒接上的機制，
  **在程式碼裡長得一樣**。→ 加介面前先問「今天有沒有第二個實作」；
  加邊界前只要問「這條線該不該存在」。
- **相關**：[draft/完整願景與它的邊界](draft/2026-08-12-完整願景與它的邊界.md)、
  [第三十九條護欄](../tests/integration/audit-four-independences.test.ts)

### 一個對稱的資料遺失不會讓任何比對變紅——它只會讓執行結果變成別的程式

- **實際發生（2026-08-13，全修一輪）**：同一種形狀在一天內出現**三次**。

  | 語法 | 掉了什麼 |
  |---|---|
  | `pair<int,string> p = make_pair(…)` | 初始值 |
  | `new int[n]` | 大小（於是它與 `new int` 在語義樹上是同一棵） |
  | `vector<int> v(5)` | 大小（於是 `iota(v.begin(), v.end(), 1)` 立刻越界） |

- **三者的來回轉換比對全都一直是綠的**——因為**辨識掉、產生也掉**，兩邊對稱。
- ⚠️ **只有把執行結果拿去跟真編譯器比才照得出來**。這是第三十二條護欄
  （行為的誤差）存在的全部理由，而它 2026-08-13 才第一次被大規模使用。
- **判準**：一個「來回轉換是不動點」的測試**證明不了資料沒掉**——它只證明
  兩邊掉得一樣多。要證明沒掉，得拿**第三方**（真編譯器、參照實作）來比。
- **相關**：[history/055](history/055-全修一輪-缺陷的形狀比缺陷本身重複.md)、
  `tests/integration/audit-behavior-error.test.ts`

### 一個回退值若剛好是某些情況的正確答案，它會偽裝成正確很久

- **實際發生（2026-08-13）**：三個獨立的實例，而每一個的偽裝方式不同。

  | 回退 | 為什麼看起來對 |
  |---|---|
  | `container_empty` 對非 array 回 `true` | `if (from.empty()) return s;` 於是 `replaceAll` 一次都沒替換——**而輸出是原字串，看起來像「沒有東西需要替換」** |
  | `sizeof` 查不到時回 `4` | `sizeof(3.14f)` **剛好對**（float 是 4），`sizeof(3.14)` 才露餡 |
  | `numericCast` 認不得的型別原封回傳 | `static_cast<unsigned char>(~0)` 回 -1，而 -1 **看起來像一個成功的轉型結果** |

- ⚠️ **這比「回一個明顯錯的值」危險**：明顯錯的值會被第一個測試抓到；
  而一個「在某些情況下正確」的回退，會讓那些情況的測試**全部通過**，
  於是它累積出一份看起來很健康的紀錄。
- **判準**：寫一個退路時問——**這個值會不會剛好是某些輸入的正確答案？**
  會 → 那條路必須出聲（丟錯），不能回值。
- **相關**：[history/055](history/055-全修一輪-缺陷的形狀比缺陷本身重複.md) §八⑩

### 路徑活著不代表做法還對——一份指著存在檔案的過時說明書最難發現

- **實際發生（2026-08-13）**：使用者問「component 相關的 skill 會不會過時」。
  我先用「skill 裡提到的檔案路徑還在不在」掃——**九支全綠，零筆死路徑**，
  而其中**四份是壞的**（教「把五路加進共用檔」，而 177 顆元件三天前就全膠囊化了）。
- **兩個原因**：那些共用檔**大多還在**（只是身分不該再寫進去）；
  而帶佔位符的路徑（`src/languages/{lang}/core/concepts.json`）**regex 抓不到**
  ——那一條正是死的。
- **有效的判準**：不是「路徑在不在」，是**「它對今天的結構特徵提及幾次」**。
  `component-generate` 對膠囊／`component.json`／form 軸／登錄表的提及是 **0 次**。
- ⚠️ **而判準本身也會過時**：我第一版的特徵表裡列了「ParamSpec」，
  而真實的欄位是 `properties[].kind/values/required`——那一欄的 0 是誤報。
- **相關**：[history/054](history/054-元件skill的世界觀落後了一個階段.md)

### 一個假的「顯式的空」不會隨時間過期——它從第一天就是假的

- **實際發生（2026-08-13）**：`cpp:pair_declare` 宣告
  `skipPaths: ["execute"]`、理由 `"declarative"`，`_execute_why` 逐字
  「這顆沒有獨立的執行語義。**顯式的空，不是遺漏。**」
  而 `pair<int,int> p;` **當然有執行語義**——它要宣告一個變數。
- 代價：`p.first` 丟 `UNDECLARED_VAR`，佔第三十二條護欄 18 段缺口裡的 5 段，
  **而完備性護欄一路是綠的**——那個宣告替它背書。
- ⚠️ **要與「真的過期」分開**：`cpp:define` 曾經也在「刻意不執行」清單裡，
  而它是**實作條件編譯之後才有可觀察效果**——那是宣告隨系統長出新能力而過期。

  > **過期的宣告會被新能力推翻；假的宣告不會——因為它長得像一個決定，
  > 沒有人會回頭質疑它。**

- ⚠️ **而診斷這一類要小心**：同一輪我把 `cpp:destructor` 也判成假的，
  查證後**推翻了自己一半**——它的 `skipPaths` 是對的（執行語義由 `class_def`
  承載），真正缺的是「誰呼叫 dtor」。
  **「這一路是空的」與「這一路的實作住在別人身上」在 `skipPaths` 上長得一樣。**
- **相關**：[history/055](history/055-全修一輪-缺陷的形狀比缺陷本身重複.md) §三

### 一個判準如果是照著手上那個實例寫的，它只會涵蓋那個實例

- **實際發生（2026-08-13，同一輪內兩次）**：修 `writeBaseline` 的
  `_meta` 保存時，第一版判準寫「新 note **等於** `RATCHET_NOTE` 才保留舊的」。
  而 14 條護欄裡只有一部分傳純樣板，**其餘各自組一段更長的固定說明**
  ——不等於樣板，於是照樣覆蓋，三個基線裡剛加上去的理由**在同一輪內又被吃掉一次**。
- **修法是找出真正的關係**：不是「等於」，是**追加關係**
  （舊 note 以新 note 開頭 ⇒ 舊的是「新的 ＋ 追加」）——這同時涵蓋純樣板那一種。
- ⚠️ **這與同一天的判準誤報是同一族**：judge 那輪我的三個「發現」全是假陽性，
  因為機械掃描的入口條件太寬。

  > **判準太窄會漏，太寬會產出看起來很嚴重的假陽性——而後者更浪費，
  > 因為它會被當真。**

- **判準**：寫完一個判準後，**找第二個實例試它**。第一個實例永遠會通過。
- **相關**：[history/055](history/055-全修一輪-缺陷的形狀比缺陷本身重複.md) §六

### 一個作用域少了，症狀不是「變數不見」而是「變數活太久」

- **實際發生（2026-08-13）**：辨識器把**所有** `compound_statement` 展平。
  那對 `if (c) { a; b; }` 是對的（body 就是那個結構的作用域），
  **而對一個獨立的 `{ … }` 是錯的**。

  ```cpp
  int main() { { int x = 1; } cout << x; }   // 真編譯器：編譯錯誤
                                             // 展平之後：印出 1
  ```

- **症狀從一個完全不同的地方露出來**：`{ S s; }` 的解構子印在 `main end`
  **之後**——追那個時機才發現作用域根本不存在。
- ⚠️ **活太久不會報錯**：少一個作用域不會讓任何東西「找不到」，
  它只是讓東西**留得比該留的久**。而那與「變數不見」相反——後者第一次讀就爆。
- **判準**：改動作用域邊界時，問的不是「變數還讀得到嗎」（那永遠是 yes），
  是「**它什麼時候該消失**」。
- **相關**：[history/055](history/055-全修一輪-缺陷的形狀比缺陷本身重複.md) §八⑨

### 一個沒有被宣告的意圖，會在承載它的機制改變時安靜消失

- **實際發生（兩次，2026-08-10 與 2026-08-13）**：
  - `vector_declare` 排在「陣列與列表」第一**不是偶然**，是有人這樣放的
    ——而那個意圖**只存在於陣列位置裡**。膠囊化把陣列換掉，意圖就沒了。
  - 四份元件 skill 的「世界觀」（元件住哪、五路怎麼接）**只存在於
    「寫的時候大家都知道」**——F 步把世界改了，而它們不會出聲。
- **兩者的共通點**：意圖是真的、正確的、有人刻意設計的，
  **而它沒有被寫成任何一個機器讀得到的東西**。
- ⚠️ **它與「機制有了沒人接上」相反**：那一類是**東西在而沒有人用**；
  這一類是**沒有東西在，只有一個習慣**。前者 grep 得到，後者要靠
  「承載它的機制改變」那一刻才會被發現——而那時已經消失了。
- **判準**：改一個機制之前問——**有沒有什麼東西是靠它的現狀成立的，
  而那件事沒有被寫下來？**
- **相關**：[history/054](history/054-元件skill的世界觀落後了一個階段.md)、
  `knowledge/skills/component-encapsulate`（已退休，那個意圖記在它的卡點 4）

### 排技術債的優先序時，問的不是「有幾筆」或「多難修」，是「使用者多早會撞到」

- **實際發生（2026-08-13）**：C++ 的行為缺口與形態殘差歸零之後，剩下八項
  進階語言功能（樣板特化、可變參數樣板、`mutable` lambda、多 catch ＋ rethrow、
  `ptr->method` 派發…）。三個判準給出**不同的順序**：

  | 判準 | 它會排出什麼 |
  |---|---|
  | 筆數 | 樣板那一族 2 筆 > 成員預設值 1 筆 |
  | 修的成本 | 成員預設值可能比多 catch 難修 |
  | **使用者多早會撞到** | ✅ `class C { int x = 5; };` 是**第一週**的語法 |

- **而只有第三個判準給出對的答案**：其餘七項在教學場景裡**可能永遠不需要**，
  而成員預設值是初學者第一週就會寫的東西。

  > **一份「還沒做」的清單如果不按「誰會撞到」排序，
  > 它會讓最進階的那幾項看起來和最基礎的那一項一樣重要。**

- ⚠️ **而這個專案已經有那個排序，只是沒有拿來用**：`levelTree` 的關卡順序。
  `cpp:class_def` 在 `L2c`，而樣板那些在 `L3b` 或**不在任何關卡裡**。

  > **課程順序不只是給學生的，它也是一份缺陷的嚴重性排序。**

- **與 P4 的關係**：同一個座標系的兩個用途——P4 用關卡決定學生**看得到什麼**，
  這條用同一個關卡順序決定我們**先修什麼**。⚠️ 而那讓 `levelTree` 的正確性
  多了一個消費者：它排錯了，缺陷的優先序也會跟著錯
  （見 [draft/Ln 的 n 只是排序](draft/2026-08-13-Ln的n只是排序.md)）。
- **不適用的時候**：這條的前提是「有一份可信的使用者接觸順序」。沒有課程樹的
  專案要換一個代理量（用量統計、入門教學的涵蓋範圍），**而不是退回筆數**。
- **相關**：[history/056](history/056-C++的債換了性質.md)、`principles.md` P4

### 一個座標可以描述節點，不代表它描述得了「走到這裡的人」

- **怎麼撞到的**：語義診斷的訊息設計寫成 `(level, panel)` 的函數，
  而 `level` 打算表示「這段訊息適合多熟的學生」。查證 `levelTree` 之後
  發現**節點的 level 定義得很好**——`L1a/L1b/L1c` 同為 L1 是因為距離 `L0`
  都是一步（[`Ln` 的 n](draft/2026-08-13-Ln的n只是排序.md)`:256`
  「**n 是步數，字母是方向**」）。

- 🔴 **而讀者不在那個座標系裡**：

  ```
  節點    「L2a 距離 L0 兩步」            ✅ 一個座標
  讀的人  「走完 L2a 但沒碰 L1b 的那個」  🔴 不在任何一個座標上
  ```

  > **樹上的位置是給節點的。一個走過樹的人手上是一個【集合】，
  > 而集合壓不進一條數線——除非那棵樹剛好是一條鏈。**

- **怎麼用**：看到一個欄位用等級描述「人／請求／狀態走到哪」時，先問
  **「兩個走過不同分支的人，會不會被指派同一個數字」**。會 → 那個數字
  正在丟掉分支資訊，欄位應該是集合（或集合的摘要），不是等級。

- ⚠️ **不是「等級一律有害」**：節點那一側的等級**照樣好用**，而它昨天才被
  驗證過（40 顆概念的前置邊實驗）。**壞的是拿它去描述另一種東西。**

- **代價**：改成集合之後，「今天走到哪」變成一個要維護的狀態。
  ⚠️ 而診斷這個案例**因此變便宜**：今天那個狀態不存在，所以集合恆等於
  「全部」，資料形狀定得下來而不必先做進度追蹤。

- **不適用的時候**：如果那棵樹真的是一條鏈（沒有分支），等級與集合等價，
  這條不成立——**所以先看樹的形狀，不要先看欄位名字**。

- **相關**：[`Ln` 的 n 只是排序](draft/2026-08-13-Ln的n只是排序.md)`:36`
  「兩個座標軸如果其中一個是另一個的函數，那不是二維，是一維寫成兩個欄位」
  ——⚠️ **那條講的是軸與軸的關係，本條講的是座標與使用者的關係**，
  兩條不重疊；[語義診斷系統](draft/2026-08-05-語義診斷系統.md)文末——本條的來源

### 一條只描述「差異」的驗收，一個壞掉的那一側照樣通得過

- **怎麼撞到的**：驗收④要證明「同一則診斷在兩個面板說不同的話」。
  而查證後發現程式碼面板**一直把規則代號當訊息顯示**
  （`DIAG_MISSING_CONDITION`）——它查的是 `window.Blockly?.Msg`，
  那個全域不存在，而 `?? key` 讓失敗看起來像成功。

  ```
  積木側    「缺少條件」                  ← 對的
  程式碼側  「DIAG_MISSING_CONDITION」    ← 壞的
  ────────────────────────────────────
  「兩邊不同」？   ✅ 通過。而它是靠一個缺陷通過的。
  ```

  > **一個壞掉的那一側，本身就是差異的來源。**

- **怎麼用**：寫「A 與 B 必須不同」這類驗收時，**先各自釘一條「A 是有效的」
  「B 是有效的」**，而且把它們排在差異那條**前面**。順序有意義——
  先跑的那條紅了，你就知道問題不在差異上。
  ⚠️ 這與入口條件是同一個形狀，只是方向相反：入口條件證明「量得到東西」，
  這裡證明「量到的東西是合法的」。

- **不適用的時候**：兩側都由同一段程式產生（例如同一個函式的兩次呼叫）時，
  「有效性」是共用的，分開釘只是重複。**這條針對的是兩條獨立實作的路徑。**

- **相關**：[history/061](history/061-兩條軸的第二條一直沒接上.md)；
  同族的「一支斷言『檔案裡有這個字串』的測試，全綠不代表行為是對的」

### e2e 測的是 build 產物——還原原始碼不等於還原產物

- **怎麼撞到的**：做完「把兩個面板的組法改成同一個 → e2e 必須紅」的反向驗證，
  `git checkout` 還原了原始碼，接著直接跑下一個瀏覽器檢查
  ——而 preview 服務的還是**注入版的 bundle**。

  症狀是「兩個面板說同一句話」，**而它看起來就像修正根本沒生效**。

- **怎麼用**：任何跑在 build 產物上的檢查（e2e、preview、瀏覽器實測），
  **還原原始碼之後必須重 build 才算還原**。判準很簡單：
  **問「這個檢查讀的是原始碼還是產物」**——是產物，就有一個獨立的還原步驟。

- ⚠️ **它為什麼難發現**：注入版的症狀與「功能沒做好」的症狀**一模一樣**，
  而後者是當下最合理的解釋。當時救回來的是**單元測試說文案是不同的**
  ——兩個層級的結論打架，才逼出「跑的不是同一份東西」。

- 🔴 **第二次（2026-08-17，spec 136），而這次連【重 build】都不夠**：
  `playwright.config.ts` 的 `reuseExistingServer: !process.env.CI`
  ——**preview server 還開著，服的是上一次的 build**，
  所以「還原原始碼 → 重跑 → 綠」證明的是舊產物，不是新的。
  ⚠️ 上面那句「還原原始碼之後必須重 build」**寫得對而不夠**：
  **重 build 了，而請求沒有打到新的那一份。**
  → 判準補一句：**注入證明之前先把 server 殺掉**，
  或者確認建置真的跑了（看得到 build 的輸出）。

  > **兩個檢查給出相反的答案時，先問它們讀的是不是同一份東西。**

- **不適用的時候**：dev server 有 HMR 的情況下原始碼即產物，這條不成立。
  ⚠️ 而這個專案的 e2e 用的是 `npm run build && npm run preview`，**不是 dev server**。

- **相關**：[history/061](history/061-兩條軸的第二條一直沒接上.md)；
  同族的「改動前後症狀相同」——那條講的是時間點，這條講的是**讀的東西**

### 「刪掉欄位讓型別檢查去找」只涵蓋型別檢查看得到的地方

- **怎麼撞到的**：改診斷的資料形狀時，刻意**刪掉**舊欄位而不留相容欄位
  ——理由是讓 `tsc` 把每一個引用處都指出來，而不是讓漏改的地方在執行期靜默。
  它報了 3 處，全部修掉，`npx tsc --noEmit` 綠。

  **而全套測試紅了兩支**：`tests/` 裡還有兩處在讀舊欄位。

  ```
  tsconfig.json   include: ['src']     ← 測試不在裡面
  ```

- **怎麼用**：把「編譯器會找出來」當成完備性論證之前，
  **先去 `tsconfig`／`Cargo.toml`／`go.mod` 看那個工具的涵蓋範圍**。
  典型的範圍外：測試、腳本、設定檔裡的字面值、動態存取（`obj[key]`）。
  ⚠️ 而**測試是最常見的那一格**——很多專案刻意把它排除在型別檢查外以求快。

- **為什麼這個錯誤特別安靜**：`tsc --noEmit` 綠**看起來就是完備性的證明**，
  而它其實是「在我看得到的範圍內完備」。
  兩句話的差別要去讀設定檔才知道。

  > **一個機械檢查的範圍寫在設定檔裡，不寫在你對它的期待裡。**

- **不適用的時候**：型別檢查涵蓋全部原始碼（含測試）的專案裡，這條不成立
  ——**而那正是要去確認的那一件事**。

- **相關**：[history/061](history/061-兩條軸的第二條一直沒接上.md)；
  同族的「一個查不到與一個沒有長得一樣」——這裡是「一個沒看與一個沒有長得一樣」。
  **母條**：「型別檢查的沉默有三種來源」（見下）——本條是其中的**範圍**那一格。

### 型別檢查的沉默有三種來源——而「編譯器綠了」只證明其中一種不成立

> **母條**。三個實例各自的完整場景在下列子條裡，本條只給**分類與判準**。

- **怎麼長出來的**：2026-08-14 掃描 `experience` 時發現三條各自獨立寫下的教訓
  都以「而型別檢查看不到」結尾，**而它們的根因互不相同**。

  | 沉默的來源 | 長什麼樣 | 實例 |
  |---|---|---|
  | **範圍** | 它**根本沒看**那個檔 | `tsconfig` 的 `include: ['src']`，測試不在裡面 |
  | **語意** | 它看了，而那段程式**合法** | 內層遮蔽外層，簽名相容 |
  | **身分** | 它看了、也懂，而它**分不出**兩個同名的東西 | 一個欄位名長在三個不同型別上 |

- **怎麼用**：把「編譯器會找出來」「`tsc --noEmit` 綠了」當成完備性論證之前，
  **依序問三個問題**：

  ```
  ① 它看得到嗎     → 讀設定檔的 include／exclude，不要憑印象
  ② 它看得懂嗎     → 這個改動有沒有可能產出【合法但不是我要的】程式
  ③ 它分得出嗎     → 這個名字／型別在幾個地方出現，它們是不是同一件事
  ```

  **三個都過，「綠了」才等於「完備」。** 過不了的那一格，就是要補測試的地方。

- ⚠️ **為什麼要一條母條而不是三條並列**：三條的**症狀完全相同**
  （一個綠燈 ＋ 一個沒被發現的缺陷），所以撞到時分不出是哪一種
  ——而**處置完全不同**（改設定 / 補行為測試 / 先數名字）。

  > **症狀相同而處置不同的一組教訓，需要的不是更多實例，是一個分類問題。**

- **不適用的時候**：動態語言沒有這一層，這條整條不成立。
  ⚠️ 而**有型別檢查卻不信任它**也是錯的——本條要治的是**過度信任**，不是信任。

- **子條**：「刪掉欄位讓型別檢查去找」只涵蓋型別檢查看得到的地方（範圍）、
  改名把一個名字換成另一個既有的名字時遮蔽是靜默的（語意）、
  同一個欄位名長在三個不同型別上時腳本改名一定會錯（身分）

### 發現兩件東西性質不同時，只改說法不改通道，等於沒改

- **怎麼撞到的**：`degradationCause` 有三個值，全部走「殘差」那個顯示通道
  （`MarkerSeverity.Info` ＋ 專屬 owner，刻意**不長得像錯誤**）。
  而其中 `syntax_error` 是**使用者打錯字**，不是「我們還沒長到」。

  🔴 **而那個差別當時就被看見了**——同一個檔的註解逐字寫著：

  > 「主詞是「我」，不是「你」。⚠️ 那不是修辭：`syntax_error` 是**使用者寫壞了**，
  > 而 `unsupported` 是我們沒長到。」

  **看到了，換了措辭，沒換通道。** 結果：第一週最常見的錯誤顯示成
  最低等級的灰色提示，而且不走診斷那一整套機器。

- **怎麼用**：發現一組東西裡有一個「其實不一樣」時，問**三件事而不是一件**：

  ```
  ① 它的【說法】要不要改       ← 最容易做，也最容易停在這裡
  ② 它的【通道】要不要換       ← severity／owner／走哪個 handler
  ③ 它的【歸屬】要不要搬       ← 它還該不該待在那個型別／那個列舉裡
  ```

  **只做①的話，註解會讓它看起來已經處理過了**——而那比沒發現更糟，
  因為下一個讀到的人會以為有人想過了。

- ⚠️ **不適用的時候**：如果那個差別**只影響人怎麼讀**（例如同一類錯誤的兩種語氣），
  改說法就是全部。判準是**問「有沒有下游程式碼會因為這個差別而做不同的事」**
  ——有，就不只是措辭。

- **相關**：[history/062](history/062-把使用者的錯歸進了我們的殘差.md)；
  同族的「一個沒有被宣告的意圖，會在承載它的機制改變時安靜消失」
  ——那條是意圖沒寫下來，這條是**寫下來了而沒有被兌現**

### 決定投入打磨之前，先問這個領域有沒有證據說打磨會兌現

- **怎麼撞到的**：排了一個「錯誤回饋」的階段，而使用者問「是不是要一直打磨才會好用」。
  當時的回答是分析成本曲線（哪一段會停、哪一段停不下來）——**而沒有去問
  「別人打磨過嗎，結果如何」**。

  查了之後：加強程式錯誤訊息這件事**做了六十年**，
  2014 的對照實驗說沒有顯著效果、2019 回顧 107 篇說那些指引大多沒有嚴謹證據、
  2024 年連 LLM 版本在 6 個任務裡也只贏了 1 個。

  > **我們準備投入的，是一個已經被試了六十年而證不出來的方向。**

- **怎麼用**：一件事看起來「只是需要慢慢打磨」的時候，**在排期之前問三個問題**：

  ```
  ① 有沒有人打磨過        找一次文獻／找一次同類產品，比自己推便宜得多
  ② 他們量到了什麼        「有效」還是「沒有顯著差異」
  ③ 有沒有【反例存活】    有人做成功過嗎，他做對了什麼
  ```

  ⚠️ **③最重要**：全否定的領域不代表不能做，代表**要先講清楚你憑什麼不一樣**。
  這次查到的那個反例是「手寫的解釋仍然贏過 LLM 與原生訊息」
  ——所以有價值的是**人寫的那份工**，而不是任何機制。

- **它改變了什麼**：驗收的寫法。原本會寫成「訊息變好了」，
  而那**正是六十年來證不出來的宣稱**。改成「兩個數字都要記，而假警報只准下降」
  ——一個**就算打磨無效也量得出來**的判準。

  > **一個領域如果沒有共識，你的驗收就不能建立在「我做得比較好」上，
  > 只能建立在「這個數字有沒有變」。**

- **不適用的時候**：純工程問題（效能、正確性）不需要這一步——那裡的「更好」有定義。
  ⚠️ **這條針對的是效果取決於人的東西**：訊息、教材、介面、說明文件。

- **相關**：[draft/語義診斷系統](draft/2026-08-05-語義診斷系統.md)§「文獻怎麼說」——出處；
  [concepts/認知鷹架](concepts/認知鷹架.md)——同一次查證推翻了那裡的一句話

### 一份沒有人引用的教訓，等於沒有寫過

- **怎麼撞到的**：一輪功能把「語法錯誤」的顯示從資訊級**加嚴**成錯誤級。
  規格、計畫、任務、四個注入驗證都做了，而**沒有一個環節問過**
  「加嚴會不會讓事情變糟」。

  收尾的 `judge` 掃出一份**零引用的孤兒**——`history/017`，逐字：

  > 「**加嚴一個檢查，可能讓事情比不檢查更糟。**
  > 一道檢查一旦會『拒絕』，就必須同時回答『被拒絕的東西去哪了』。」

  **那份教訓早就存在、寫得很清楚、而且正好描述那一輪的形狀。**
  它沒有被讀到，因為**沒有任何檔案指向它**。

- **怎麼用**：知識庫的檢索靠的是**連結**，不是靠人記得。所以：

  ```
  寫的時候   一份新的 history／experience 至少要被【一個】既有檔案指向
             ——「相關」那一節不是裝飾，它是這份東西未來被讀到的唯一路徑
  掃的時候   孤兒不只是整潔問題。⚠️ 一份孤兒【正好在講你現在做的事】
             的機率，比它是廢物的機率高——因為它當初是為了某個真問題寫的
  ```

- ⚠️ **而它改變了孤兒掃描的意義**：原本讀成「這份沒人連，可能該刪」，
  正確的讀法是「**這份沒人連，所以沒有人在需要它的時候讀到它**」
  ——處置是**接上**，不是刪掉。

- **不適用的時候**：真的過期的東西（描述已刪除的機制）該退場而不是接上。
  判準是**問「它描述的形狀今天還存在嗎」**——形狀還在就接上，只有實例過期。

- **相關**：[history/063](history/063-通道接好了而辨識那一層只認得某些形狀.md)
  ——那次的第三個缺口是靠這條路徑找到的；
  `knowie-judge` §3 的孤兒掃描——⚠️ **機制早就在跑，缺的是把結果讀成這個意思**

### 一個指錯地方的錯誤訊息，比沒有訊息更糟

- **怎麼撞到的**：量「寫錯的程式今天跑不跑得起來」時，四種寫錯的程式裡
  有一種**丟了錯**——看起來是好消息。而那則錯誤是：

  ```
  學生寫的      int x = 1        ← 少了分號
                cout << x;
  系統說的      RUNTIME_ERR_UNDECLARED_VAR: x
                「變數 x 未宣告」
  ```

  🔴 **技術上正確**（分號沒了，宣告沒生效，所以 `x` 真的未宣告），
  **而它把學生帶去錯的地方**——他會盯著 `int x = 1` 想「我明明宣告了」，
  而真正的錯是那一行**結尾**。

- **怎麼用**：一則錯誤訊息要問的不是「它對不對」，是
  **「照著它去看，會走到出問題的那一行嗎」**。

  ```
  技術上正確   x 確實未宣告                     ✅
  指得對地方   指向 cout << x 那一行             ❌ 錯在上一行
  ```

  ⚠️ 而**兩者都對才算對**。只有前者的話，訊息會消耗學生的時間**而且消耗信任**
  ——被誤導一次之後，下一則正確的訊息也會被懷疑。

- **它為什麼特別容易發生**：這種訊息來自**下游**。上游的問題（少分號）
  沒有被攔住，於是它以一個**合法但錯誤的狀態**流下去，
  在下游觸發一個**關於下游的**正確診斷。

  > **上游沒攔住的錯誤，會在下游變成一則指著下游的正確訊息。**

- **不適用的時候**：真的就是那一行的錯（型別不符、除以零），
  這條不成立——它針對的是**錯誤傳播**。

- **相關**：[history/063](history/063-通道接好了而辨識那一層只認得某些形狀.md)
  ——同一輪的三個缺口；`draft/2026-08-05-語義診斷系統` §八——完整的四種樣本

### 一份按「症狀」分類的缺陷清單，會把同一個根因寫成好幾筆

- **怎麼撞到的**：`history/063` 記了辨識層的三個缺口，其中兩個是
  「**只有某些形狀**的漏分號會被標記」與「抄回來的原文只有片段」。
  它們讀起來是兩個獨立的問題，而排期時也是這樣被算的。

  查根因之後：**兩個都是同一件事**——解析器有**兩種**標記錯誤的方式
  （實體錯誤節點／傳播旗標），而我們只認得前者。

  ```
  症狀   形狀 A 不會被標記      形狀 C 不會被標記      原文只有片段
  根因   ────────────  只認得一種表示方式  ────────────
  ```

  補上第二種之後**三個症狀一起消失**，而且**順帶關掉了第四個**
  （「少一個右大括號標不出來」——那不在範圍裡）。

- **怎麼用**：一份清單上有多筆時，**在排期之前問一次
  「這幾筆會不會是同一個東西的不同角度」**。判準：

  ```
  症狀分類   我觀察到什麼不對          容易寫，而它會把一個根因拆成 N 筆
  根因分類   哪一個機制沒有做到什麼      難寫，而它會讓 N 筆塌成一筆
  ```

  ⚠️ 而**症狀分類本身沒有錯**——第一次發現的時候你只有症狀。
  錯的是**停在那裡就去排期**：一份三筆的清單看起來像三份工作，
  而它可能是一份。

- ⚠️ **反過來也要小心**：`build-guardrail` 記過「一叢違規看起來像一個根因，
  而那是假設不是結論」——那次五筆共用症狀而根因有三個。
  **兩條合起來才是完整的**：

  > **共用症狀既不證明同根因，也不證明不同根因
  > ——它只證明「還沒有人去查」。**

- **不適用的時候**：症狀只有一筆時無所謂。這條針對的是**清單**。

- **相關**：[history/063](history/063-通道接好了而辨識那一層只認得某些形狀.md)
  ——三個症狀一個根因；`knowledge/skills/build-guardrail`「一叢違規看起來像一個根因」

### 一句限定寫在四個地方，拿掉它的成本也是四個地方——而那正是它值得的原因

- **怎麼撞到的**：一輪交付附了一句限定
  （「不得宣稱『寫錯的程式不能跑了』，只能說『**被辨識出來的**語法錯誤不能跑了』」），
  而它被刻意寫進**四個地方**：規格、核心函式的檔頭、閘門的註解、
  端對端測試的失敗訊息。

  當時的理由是「一句限定條件只寫在一個地方，下一個讀到的人只會讀到其中一個」。

  **而下一輪就把那個限定拿掉了**——一次 `grep` 找齊四處，全部更新。

- **怎麼用**：一句**會過期的限定**（「今天還做不到 X」「暫時只支援 Y」）
  該寫在**每一個會讓人誤以為做到了的地方**，而不是只寫在規格裡。

  ⚠️ 判準不是「重複幾次」，是問
  **「有沒有人可能只讀到其中一處就下結論」**：

  ```
  只有規格有        看程式碼的人不會讀到        → 不夠
  只有註解有        看測試的人不會讀到          → 不夠
  四處都有          任何一條路徑都撞得到        → 夠了
  ```

- ⚠️ **而重複的成本是真的**——它在**拿掉**的時候付：四個地方都要改。
  而那次拿掉花的時間比寫的時間短，**因為那句話是可 grep 的**。

  > **一句會過期的限定，要用同一段文字寫在每一處
  > ——同義改寫會讓它在該被拿掉的那天漏掉一個。**

- **不適用的時候**：**不會過期**的事實（設計理由、根公理的推導）
  重複寫是雜訊——它們該有一個家再被連過去。
  **這條只針對「有到期日」的話。**

- **相關**：[history/064](history/064-量出來的東西不是我們要找的.md)
  ——那句限定的出處與它被拿掉的那一次

### 一個用了不存在身分的假樣本，與一個真的壞掉的功能長得一模一樣

- **怎麼撞到的**：寫一支來回測試驗「字串宣告的初始值會不會掉」，
  合成的子節點寫成 `cpp:string_literal`——**而真的身分是 `cpp:literal_string`**。

  結果：render 出來的積木 `inputs: {}`，測試紅，訊息是「初始值走完來回不見了」。
  **而那個訊息是真的**——只是原因不是功能壞了，是**樣本裡的那顆節點根本不存在**。

  🔴 我因此去查了 renderer 的合併邏輯、查了 spec 有沒有吃到新的對映
  ——**兩處都是好的**，而那兩次查證各花了一輪。

- **怎麼用**：合成一棵樹來測投影時，**先證明那棵樹是真的**：

  ```
  ★ 正向錨點   同一個合成器造一棵【已知會通過】的樹 → 必須綠
  或           斷言合成出來的節點數 / 子節點身分【真的存在於登錄表】
  ```

  ⚠️ 沒有這一步的話，**「樣本錯了」與「功能壞了」給出完全相同的訊號**
  ——而前者會讓你去改一段本來就對的程式碼。

- ⚠️ **而它與「入口條件」是同一族**：入口條件證明「量得到東西」，
  這條證明「**量的那個東西是真的**」。
  ```
  入口條件   工具有沒有吃到輸入
  這一條     餵進去的輸入是不是合法的
  ```

- **不適用的時候**：樣本來自真實語料（而不是手寫合成）時不成立
  ——那時錯的是語料的**範圍**，不是它的**合法性**。

- **相關**：`specs/122-declared-slots-unreachable`；
  同族的「一個查不到與一個沒有長得一樣」

### 一個量測工具若「照宣告造樣本」，一個假的宣告就會變成一筆永遠還不掉的債

- **怎麼撞到的**：符合性護欄的做法是**照著元件的宣告合成一棵樹**，
  再看它走不走得完投影來回。而兩筆宣告與現實不符：

  ```
  struct_at_member.obj      產生器讀的是 properties.obj——它是【參數】不是接點
  var_declare.declarators   多變數 lift 成別的形狀——這個接點【從來不會被產生】
  ```

  於是合成器造出 `5.field` 與 `int a, 5;`——**真實世界不存在的樹**
  ——再回報它們走不通。

  🔴 **而那兩筆進了棘輪**，被當成技術債排期了好幾輪。

- **怎麼用**：一條「照宣告造輸入」的護欄，它的數字**同時**受兩件事影響：

  ```
  被量的東西壞了       → 真違規，要修實作
  宣告與現實不符       → 🔴 假違規，要修【宣告】
  ```

  **而報表上它們長得一模一樣。** 所以這種護欄要問一次
  **「這一筆對得上一段真實會發生的程式碼嗎」**——對不上就先查宣告。

- 🔴 **而修法有一條紅線**：刪宣告讓數字下降，與 `build-guardrail` 警告的
  「**用宣告刷數字看起來會像進步**」**從外面看一模一樣**。

  > **唯一的差別是出處**：刷數字沒有出處；
  > 修假宣告要拿得出**產生端或消費端**的位置，證明那條路本來就沒有人走。

  ⚠️ 而兩種下降**要在基線裡分開記**——否則未來的人讀不出差別。

- 🔴 **而證據的門檻比我第一次想的高**（2026-08-14 同日翻車，[history/065](history/065-兩條護欄從相反方向量同一件事.md)）：

  我拿出了「消費端讀的是參數不是接點」的出處，刪了宣告，
  `conformance` 11 → 9，對照組**逐字相同**——**看起來完全成立**。
  而另一條護欄說 **lift 真的會產生那些接點**。

  ```
  audit-conformance     宣告了的，形態表達得出來嗎    ← 刪宣告讓它【變好】
  audit-declared-slots  lift 產出的，宣告裡有嗎       ← 刪宣告讓它【變壞】
  ```

  > **兩條護欄從相反方向量同一件事，而那對關係本身
  > 就是「用宣告刷數字」的機械偵測器。**

  → **判準**：宣稱一個宣告「沒有人走」之前，**產生端與消費端都要查**，
  而且**要跑那條反方向的護欄**。⚠️ 只跑一條的話，兩種下降還是分不出來。

- ⚠️ **而「行為沒變」騙得過人**：對照組逐字相同是真的
  ——**因為那批語料裡沒有一段會走到那條路**。
  > **一個對照組只證明「它涵蓋的那些情況沒變」。**

- **不適用的時候**：語料來自真實程式（而非合成）的護欄不受這條影響
  ——那時錯的是語料的**範圍**不是它的**合法性**。

- **相關**：`specs/123-tighten-slot-declarations`（**被否決的路線**）；
  同族的「一個用了不存在身分的假樣本，與一個真的壞掉的功能長得一模一樣」
  ——⚠️ 那條是**手寫**的假樣本，這條是**宣告產生**的假樣本


### 一張手工截圖，是一個沒有執行機構的宣告

- **怎麼撞到的**：寫完第一課，使用者說「有圖片會更好」，接著問
  「**怕圖片在版本更新之後很容易過時，要如何處理比較好？**」

  而那個顧慮命中一個已經記過的形狀——「**路徑活著不代表做法還對
  ——一份指著存在檔案的過時說明書最難發現**」。
  🔴 **而截圖比那更糟**：連「指著的東西還在不在」都沒有人查。

- **怎麼用**：先分清**兩種圖，它們的過時風險完全不同**：

  ```
  UI 截圖      「按這個按鈕」「積木長這樣」   🔴 隨介面改版過時
  概念示意圖   「文字在記憶體裡長什麼樣」     🟢 不隨介面過時（畫的是領域不是工具）
  ```

  **只有第一種需要處理**，而處理的方式不是「記得更新」，是**讓它自己產生**：

  ```
  人開瀏覽器截圖 → 貼進文件    介面一改就過時，而沒有人會發現
  端對端走查順手截圖           每次跑測試都重新產生
  ```

- 🔴 **而副作用比圖片本身值錢**：

  > **一份會自己重新截圖的教材，同時是一支「這一課還走得通嗎」的測試。**

  按鈕改名、積木不出現、輸出變了——**截圖產生會失敗，而那就是課壞掉的訊號**。
  ⚠️ **就算永遠不加圖片，那支測試都該在**——圖片只是它的副產品。

- ⚠️ **而它有一個誠實的代價**：

  ```
  自動截圖   永遠正確，而【不好看】（固定視窗、測試環境）
  手工截圖   好看，而【會說謊】
  ```

  **那是取捨不是最佳解。** 真的需要構圖的那一張，
  單獨手畫**並標明「示意圖，不是實際畫面」**——讓讀者知道哪張是保證同步的。

- **不適用的時候**：一次性的文件（簡報、公告）不需要——它們沒有「之後」。
  **這條針對的是會被長期依賴的說明。**

- **相關**：`e2e/lesson-01.spec.ts`；`specs/124-first-lesson/findings.md`；
  同族的「路徑活著不代表做法還對」與 [concepts/執行機構](concepts/執行機構.md)

### 一條護欄如果只涵蓋「上次出事的那一族」，它防的是上一次，不是這一類

**現象**：2026-08-14 修好「程式碼面板顯示 `DIAG_MISSING_CONDITION` 這串代號」，
並蓋了第四十二條護欄。**隔天**同一個形狀在**執行期錯誤**上復發：學生看到
`RUNTIME_ERR_UNDECLARED_VAR: {"%1":"Cout"}`。

**根因**：不是「護欄漏了 `RUNTIME_ERR_` 這一族」。就算把它加進去也接不住——

```
護欄量的   文案【在不在】       ✅ 文案就在 blocks.json:134，護欄是綠的
而缺陷是   顯示端【有沒有查】    🔴 從來不查
```

> **文案齊全，而沒有人查它。**

⚠️ 那與「機制有了沒人接上」是同一個病的**第四次**（`mappings` 零接收者、
23 筆 `min` 零消費者、`children.allowed` 41 筆）。

**教訓**：修完一個缺陷要問的不是「同一族還有誰」，是
**「這個缺陷的判準錨在哪一端」**——錨在**供給端**（文案表、機制、宣告）
的護欄，量不到**消費端**（有沒有人用）的缺失。

**如何應用**：蓋護欄時問一句「**如果供給那一側全部齊全而沒有人消費它，
這條護欄會是綠的嗎？**」會 → 錨錯端了。

### 誤報的風險不是靠判準寫嚴來消除的，是靠量測單位選對來消除的

**現象**：第四十四條護欄要量「推給使用者看的字串不得長得像代號」。
第一個設想是掃 `src/ui/` 找顯示呼叫——而 `src/` 裡到處都是開發期日誌，
判準寬一格就會報一整片，然後被人加進忽略清單（`build-guardrail` 第 11 步）。

**改法**：把單位從**「一行原始碼」**換成**「一個 (錯誤身分, 拋出點傳的參數)
組合走過顯示路徑的結果」**。開發期日誌**永遠進不了那個集合**——
它們不經過顯示路徑。

**教訓**：**一個判準要排除的東西，如果能靠「它根本不在集合裡」排除，
就不要靠「判準寫得夠嚴」排除。** 後者每加一個例外就多一個會漂移的地方。

⚠️ **而換單位順便多抓了兩種缺陷**：同一個判準同時抓到「不查表」（出現代號）、
「沒文案」（沒退路就出現代號）、「缺參數」（出現未替換的 `%2`）。
**一個錨對地方的判準，會抓到你沒預期的同族。**

### 一個什麼都不做的按鈕，與一個壞掉的按鈕，在使用者眼裡是同一件事

**現象**：走第二課時發現課文走不通——只按「執行」，**輸出區完全空白**，
沒有錯誤、沒有提示。必須先按「程式碼→積木」。

**而課文從頭到尾沒說要按那個鈕**——因為寫課文的人（我）心裡跑的是 e2e 的
步驟，那裡每次都按了。**這是「因為知道答案而跳過」的直接後果。**

**教訓**：**靜默的無操作是最貴的錯誤回饋**——它不給使用者任何可以查的東西，
而且它看起來像「我按錯了」而不是「系統沒反應」。

**如何應用**：任何「條件不滿足就不做事」的分支，問一句
**「使用者怎麼知道剛才那一下沒有生效？」** 答不出來就是一個靜默降級。

### 當工具的行為與課文教的規則相反時，課文只能繞路——而繞路的地方就是下一個規格

**現象**：第二課花兩段講「`int` 不能省」，而實測 `score = 90;`（沒有 `int`）
**在我們的直譯器上跑得完並輸出 90**——C++ 拒絕它。
於是課文**避開了這個寫法**。

**教訓**：**教材是一個會說出「工具哪裡不對」的量測工具**，
而它說的方式是**繞路**，不是報錯。⚠️ 所以繞路要被記下來，否則它會被讀成
「課文就是這樣寫的」，而底下那個缺陷永遠沒有人排期。

**如何應用**：寫教材時每繞一次路，在 findings 記一筆
「**為什麼不能直接講**」——那份清單就是下一輪的候選規格。

### 一支沒有寫理由的測試，記的是「當時的實作」而不是「當時的意圖」

**現象**：`Scope.set` 找不到名字就默默建立變數，於是 `score = 90;`（忘了 `int`）
跑得完並印出 90，而 C++ 拒絕它。改成拒絕之後，**全套 4168 支只有 1 支失敗**
——而那一支斷言的**正是這個缺陷**（`should set variable in current scope if not
found anywhere`，來自最早的 TDD 地基階段，**沒有寫任何理由**）。

**教訓**：一支只斷言行為而不寫理由的測試，**後來的人分不出它記的是
「我們決定這樣」還是「當時剛好這樣」**——於是它變成一道**看起來像決定的擋牆**，
而拆它的人要自己承擔「會不會拆錯」。

**如何應用**：反轉一支測試時，**新的斷言要附兩件事**：
**為什麼**，以及**推翻它需要什麼**。只改斷言等於原地換一道同樣的牆。
⚠️ 而 TDD 產出的測試特別容易犯——**它們是照著剛寫好的實作寫的**。

### 一個為了「看看有沒有」而存在的 try/catch，會在被查的那件事開始拋錯時吃掉新行為

**現象**：`Scope.set` 往上找時寫的是
`try { parent.get(name) } catch { /* 沒有，往下走 */ }`。
把「找不到就建立」改成「找不到就拋」之後，`parent.set()` 也會拋
——而**那個 catch 把它吃掉**，控制流照樣掉到最後一行，**行為完全沒變**。

🔴 **而它看起來像修好了**：程式碼裡有一行 `throw`，而它永遠傳不出去。

**教訓**：用例外當「存在性查詢」的地方，**是一個對未來的假設**
——假設被查的那件事永遠不會因為別的原因拋錯。而那個假設沒有寫在任何地方。

**如何應用**：改一個會拋錯的函式之前，**grep 誰 catch 它**。
⚠️ 而驗證要從**行為端**（跑一段真的程式）——單元測試直接呼叫那個函式，
**看不見這條路徑**。

### 把自己知道的事，當成系統會做的事——寫教材時這個錯犯兩次

**現象**（同一輪，兩次）：

```
126  課文沒說要按「程式碼→積木」——因為我心裡跑的是 e2e 的步驟
127  課文寫「不加 int 就會看到那句話」——而實測它【默默成功】
```

**第二次比第一次嚴重**：漏講是「不講」，而這是**講錯**。
一個學生照著做會發現「老師說會出錯，而它沒有」
——**那會讓他不再相信課文其餘的部分**。

**教訓**：寫教材的人知道**領域的規則**（C++ 要宣告），
而教材描述的是**這個工具的行為**。兩者不同的那些地方，
**正是教材最容易說謊的地方**——而作者最不可能發現。

**如何應用**：課文裡每一句「你會看到 ○○」，**都要有一支走查測試逐字釘住它**。
⚠️ 而那不是為了防止系統退步，是為了**當場抓出作者的假設**。

### 教材是一個會說出「工具哪裡不對」的量測工具，而它說的方式是繞路

（與上一條互補：上一條講作者說謊，這一條講**課文的沉默**。）

**現象**：第二課花兩段講「`int` 不能省」，而系統允許省，
所以課文原本**避開了那個寫法**。修好之後，那一段從「繞路」變成
**第五步（讓學生真的試一次）**，而錯誤訊息成了教學主軸。

**教訓**：教材繞路的地方，是**工具與領域不一致**的地方
——而它不會報錯，只會安靜地不講。

**如何應用**：寫教材時每繞一次路，在 findings 記一筆「**為什麼不能直接講**」。
那份清單就是下一輪的候選規格。⚠️ **而繞路修好之後，那一段通常不必刪掉
——它會變成一個更好的教學步驟**，因為錯誤訊息本身就是解釋。

### 一個探針最有價值的產出，常常不是它要驗的那件事

**現象**：為了驗「委派編譯器需要的 COOP/COEP 能不能在 GitHub Pages 上成立」，
把 semorphe 放進 `crossOriginIsolated` 環境跑。前置驗過了（可行），
**而 COEP 順手擋掉一張圖**——`blockly-demo.appspot.com/static/media/sprites.png`。

🔴 **那張圖今天就在跑**：每一個使用者都在向 Google 的 demo 伺服器要它，
而**離線時圖示會壞掉**。「離線可用」這句話因此不完全成立。

**教訓**：探針把系統放進一個**比平常嚴格**的環境，
而嚴格環境會把**平常安靜的缺陷變吵**。那些缺陷與探針要驗的事無關
——**而它們正是平常抓不到的那些**。

**如何應用**：跑探針時，**把「順手壞掉的東西」逐項記下來**，
不要只記你要的那個答案。⚠️ 而記之前要跑對照組——
本輪就跑了（同一份 build 不加 COEP，圖示正常），否則歸因會錯。

### 一條只斷言「沒有向外要」的護欄，對「根本沒要到」是瞎的

**現象**：第四十五條護欄斷言「執行期零外部請求」。
🔴 而一個把資源路徑設成**不存在的位置**的實作**也會通過**
——它一個外部請求都不發，而圖示全部消失。

**教訓**：「不做某件事」的護欄，天然對「什麼都不做」是瞎的。
**它需要一個配對的、斷言「該做的有做」的檢查。**

**如何應用**：每寫一條「MUST NOT」的護欄，問一句
**「一個什麼都不做的實作會通過嗎？」** 會 → 補一條 MUST。
⚠️ 本輪的配對是「自託管的檔案真的要到了幾個」，
而它的入口條件錨在**要到幾個**（合成量，不隨修復下降）。

### 一份簽進版控的複製品，會在上游升級時默默停在舊版

**現象**：修 Blockly 的外部相依時，第一個念頭是把 `media/` 那 16 個檔簽進 `public/`。
⚠️ 而那會讓升級 Blockly 時圖示**停在舊版**——**與原本那個缺陷同一族（安靜地不對）**。

**教訓**：**複製品的正確性靠人記得同步，而人不會記得。**
從來源產生的複製品**在構造上不可能失同步**。

**如何應用**：要複製一份上游的東西時，問
**「上游改了，這份會自己跟著改嗎？」** 不會 → 改成建置時產生，並列入 `.gitignore`
（它是產物不是原始碼）。⚠️ 而這與「自動截圖 vs 手工截圖」是**同一條**。

### 一個只有護欄的專案，會非常穩定地維持一個沒有人檢查過的現況

**現象**：2026-08-14～16 三輪，最有價值的發現一次都不是護欄給的——
學生看到原始代號、`score = 90;`（忘了 `int`）跑得完、每個使用者都在向
Google 的 demo 伺服器要一張圖。🔴 **而在這三個缺陷存在的整段期間，
43～44 條護欄全部是綠的。**

**根因**：護欄量的是**我們宣告過的規範**，而缺陷住在**我們沒說過的地方**。
要求護欄抓到它們，等於要求一份規範涵蓋它自己沒想到的事。

**教訓**：**「全綠」只證明沒有退步，不證明現況是對的。**
而一個團隊愈擅長蓋護欄，愈容易把「全綠」讀成「健康」。

**如何應用**：定期問一句**「我上一次量【現況】而不是量【規範】是什麼時候？」**
答不出來 → 該跑探針了。⚠️ 而探針要有對照組，
且**沒有目標值的量不要做成護欄**（會逼人去湊一個目標）。
見 [執行機構](concepts/執行機構.md)「量測工具有三種」。

### 我寫下的分類，比我跑出來的數字更常出錯

**現象**（連續兩輪，都是我前一天親手寫進 `vision` 的）：

```
「score = 90 需要型別資訊」          → 實測只需要已存在的符號表，爆炸半徑 1 支測試
「它屬於 6.7 第三個缺口那一族」      → 那一族卡在委派編譯器，於是它被一起延後
```

**根因**：分類是在**沒有量測**的情況下做的，而它一旦寫進路線圖，
就**繼承了那個類別的所有阻斷**——「需要型別資訊」讓一個一行的修法
跟著一個被卡住的前置一起排到後面。

> **一個被歸進「需要某個被卡住的前置」的缺陷，會跟著那個前置一起被延後
> ——而歸類錯了沒有任何東西會發現。**

**教訓**：**成本的分類是一個經驗判斷，而它可以被便宜地量。**
「量爆炸半徑」（注入嚴格版 → 跑全套 → 數失敗 → 還原）只要一次全套測試。

**如何應用**：在把一筆缺陷寫成「大／需要某某前置」之前，**先量一次**。
⚠️ 而量完要**寫進驗收而不是寫成前提**——
「失敗數 = 1，大於 1 就停下來重新評估」擋得住，「我量過是 1」擋不住。

### 一句「像 X 一樣」的要求，有兩種實作，而便宜的那種會先被做

**現象**：使用者 2026-08-12 說「**越像實際編譯器吐出的訊息越好**」。
我實作成**措辭偏編譯器**（「條件運算式缺漏（COND）」）。
2026-08-16 使用者再說一次，才清楚他要的是**編譯器實際吐出的那一句**
（含檔名、行號、插字符、修法建議）。

```
模仿 X 的形式   便宜、自己就能做、看起來像做到了
接上 X 本身     貴、要一個外部相依          🔴 而價值全在這裡
```

**教訓**：使用者說「像 X」的時候，價值可能來自**它的樣子**，
也可能來自**它的來源**。而**只有後者能讓學習者在離開這個工具之後受益**
——一個我寫的「像編譯器的句子」，他明天在別處不會再看到。

**如何應用**：聽到「像 X 一樣」時問一句
**「這個要求的價值，來自它的樣子還是它的來源？」**
來自來源 → 模仿是一個**看起來完成的殼**，而它會讓真正的需求晚幾個月才被發現。
⚠️ 而這一次晚了四天，代價很小——**因為使用者又說了一次**。

### 一個「只有一個 X」的假設，不會出現在任何介面上——它出現在誰持有狀態上

**現象**：VSCode 原型需要 `Map<uri, {semanticTree, blocklyState, lastSyncSource}>`，
因為編輯器同時開 N 個文件。而瀏覽器版把「只有一個文件」寫進了 **app 層的六個單例**
（`blocklyPanel`／`monacoPanel`／`syncController`／`currentTree`／`currentTopic`／
`enabledBranches`）。

⚠️ **而階段 0-5b 的解耦沒有解掉它**：`SemanticBus`／`ViewHost` 解的是
**視圖之間**的耦合，**不是「有幾個文件」**。

**教訓**：容量假設（只有一個 X、最多 N 個 Y）**不會出現在型別或介面上**
——它出現在**誰持有那個狀態**上。所以型別檢查、介面審查、解耦重構**都抓不到它**，
而它在換宿主的那天一次全部現形。

**如何應用**：看到一個 `private currentSomething` 在應用層，問一句
**「如果同時有兩個，這裡會怎樣？」** 答案是「要變成 Map」→ 那就是一個沒說出口的容量假設，
**寫下來**（不必現在改）。

### 同一個防迴圈的問題，在同步宿主用布林旗標就夠，在非同步宿主要加時間

**現象**：雙向同步（改 A → 更新 B → B 的變更事件 → 又改 A）要防迴圈。

```
瀏覽器   if (this.syncing) return                    布林，夠了
VSCode   setTimeout(() => flag = false, 50)          🔴 要延遲 50ms
```

**因為 `workspace.applyEdit()` 回來之後，變更事件還沒到。**
布林旗標在 `await` 結束當下就清掉，於是自己寫的內容會被當成使用者的編輯**繞回來**。

**教訓**：「我正在寫，別理我」這個旗標的**生命週期，取決於宿主的事件何時送達**
——而那不在我們控制之內。同步宿主可以用作用域，非同步宿主只能用時間。

**如何應用**：⚠️ 用時間的那個版本，**那個常數是猜的**（50ms 沒有人驗過夠不夠）。
把它標成猜測，並在它出問題時**先懷疑它**——一個偶發的「同步了兩次」長得像競態，
而根因可能只是這個數字太小。

### 一個「找不到」的錯誤，分不出是「東西不在」還是「路徑沒說」

**現象**：clangd 在瀏覽器裡跑起來之後，第一次檢查報

```
[pp_file_not_found] 'iostream' file not found
[undeclared_var_use] use of undeclared identifier 'cout'
```

**看起來像「sysroot 沒有一起打包」**——而我差點去找一份要另外下載的標頭檔。
實測 `FS.readdir('/usr/include/wasm32-wasi/c++/v1')` → **`iostream` 就在裡面**。
缺的是**編譯旗標**（`-isystem`），不是檔案。

**教訓**：「找不到 X」這種錯誤有**兩個根因**而症狀相同：
**X 不存在** vs **沒有人告訴它去哪裡找**。
🔴 而前者的處置（去弄一份）比後者（給一行設定）貴一個數量級
——**所以猜錯的方向是有系統性的：會往貴的那邊猜。**

**如何應用**：看到「找不到」，**先列出它實際找過哪些地方**（或直接問檔案系統），
再決定要不要去弄一份。⚠️ 一分鐘的 `readdir` 省掉一次沒有必要的下載與整合。

### 一個數字要標明它量的是哪一條路徑，否則它會被當成那件事的成本

**現象**：探針量到 clangd 的 `--check` 要 **26.1 秒**。
⚠️ 而 `--check` 是**一次性全量檢查**——它建索引、跑 inlay hints、跑語意高亮、
「Testing features at each token」。**而我們只要診斷。**

**教訓**：**26 秒不是「診斷要 26 秒」，是「`--check` 這個模式要 26 秒」。**
一個沒有標明路徑的數字，會被下一個讀到的人當成**那件事本身**的成本
——而它可能差一個數量級。

**如何應用**：探針報數字時，**把「量的是哪一條路徑」寫在數字旁邊**，
並明說**還沒量的那一條**。⚠️ 本輪的例子：LSP 的
`didOpen → publishDiagnostics` 才是「打字時診斷」的實際數字，**而它沒有量**。

### 一個裁判的能力邊界，會偽裝成被判者的缺陷

**現象**：用 wasm 版 clangd 當裁判量「我們擋得住幾成不合法的程式」，
第一次算出 **35%**。而 13 個缺口裡有 **10 個是 Arduino**——它們報
`undeclared_var_use`，是因為 **wasi 的 sysroot 沒有 Arduino 的核心標頭**。

**不是程式寫錯，是裁判看不懂那個方言。** 排掉之後是 **78%**。

**教訓**：⚠️ 而 35% **看起來像一個發現**——
「我們只擋得住三分之一」是一句很有說服力的話。
🔴 **裁判的盲區與被判者的缺陷，在報表上長得一模一樣**，
而前者會讓你去修一個不存在的問題。

**如何應用**：引入一個外部裁判時，**先列出它看不懂什麼**，
並把那些歸「無法確定」、**逐筆寫出理由**。
判準：**這一筆的錯誤訊息，換一個裁判還會出現嗎？** 不會 → 那是裁判的邊界。

### 一個量測最好的結果之一，是它證明你已知的清單沒有漏

**現象**：涵蓋率 78%，而那兩個缺口（`Cout` 大小寫、`<<` 寫成 `<`）
**正是三天前就記在 `history/064` 的那兩筆**。量測**沒有揭露新的東西**。

**教訓**：一個量測的價值不只在「發現新缺口」——
**「已知清單是完整的」本身就是一個結果**，而它讓後續的優先序不必再猜。

⚠️ 而它與「量了沒發現東西＝白做」的直覺相反：
**沒發現新東西，與沒有量過，是兩種完全不同的狀態。**

**如何應用**：報告量測結果時，**明說「有沒有新東西」**。
如果沒有，那句話要寫出來——否則下一個人會以為那個範圍還沒被看過。

### 遞迴到最外層才拋錯，錯誤訊息就只看得到最外層的世界

**現象**：加「你是不是要打 `score`？」的近似名建議。單元測試 13 支**全綠**，
而跑真的程式**一句建議都沒有**。

**根因**：`Scope.get()` 寫成 `return this.parent.get(name)`
——一路遞迴到**最外層**才拋錯。而 `getAll()` 只**往上**攤平，
所以**最外層看不到子層的名字**。`int score` 在 main 裡，
而例外在 global 拋出 → 候選集合是空的。

```
單元測試   直接對一個 Scope 呼叫 → 那一層就是最外層 → 🟢 過
真的程式   main 是子層           → 拋錯的是 global    → 🔴 永遠找不到
```

**教訓**：**「誰有資格說『找不到』」是一個設計決定，而它藏在遞迴的形狀裡。**
往上遞迴的查找，天然把「錯誤發生在哪個脈絡」丟掉了——
而錯誤訊息要的正是那個脈絡。

**如何應用**：查找失敗要報告**脈絡**時，把「找」與「拋」分開：
往上找用迴圈，而**拋錯留在發起查找的那一層**。
⚠️ 而驗證必須在**行為端**——這是同一族的第二次（`spec 127` 的
`try/catch` 吞掉新行為是第一次），**兩次都是單元測試看不見**。

### 一個測試裡的假設出錯，可能是設計問題不是測試問題

**現象**：寫近似名建議時，我在測試裡假設 `scoer` 與 `score` 是「編輯距離 1」。
實作之後那一支紅了——**`scoer` 在 Levenshtein 下是距離 2**（兩次取代），
而它其實是**一次相鄰對調**。

🔴 **而正確的處置不是改測試，是改實作**：
**打字打顛倒是最常見的錯法之一，而純 Levenshtein 看不見它**
（Damerau-Levenshtein 才看得見）。

**教訓**：測試紅的時候有兩種可能——**測試的期望錯了**，
或**測試不小心表達了一個比實作更好的設計**。
⚠️ 而第二種很容易被當成第一種處理掉，因為改測試比較快。

**如何應用**：測試的期望與實作不合時，問一句
**「如果使用者真的這樣做，我希望發生什麼？」**
——答案站在測試那邊 → 改實作。

### 一個量測把「偵測到了」與「預先擋下了」算成一件事，於是刻意的設計看起來像缺口

**現象**：用 clangd 當裁判量涵蓋率，得到 **78%**，缺口兩筆。
而追下去發現那兩筆**我們都偵測得到**——它們是**語義**錯誤，
`spec 120` 的設計**刻意**只把**語法**錯誤擋在執行前，語義錯誤
**跑到那一行才停**。

```
偵測率      9/9 = 100%   擋下 or 跑到那一行才停
執行前擋下  7/9 =  78%   只有語法錯誤——【那是刻意的】
🔴 真的漏掉  0 筆
```

**教訓**：**「我們發現了嗎」與「我們在什麼時候發現」是兩個問題**，
而把它們壓成一個數字，會讓一個**刻意的設計位置**看起來像一個缺陷
——然後有人去「修」它。

⚠️ 而 `history/017` 正是那個設計的理由：一道會拒絕的檢查必須回答
「被拒絕的東西去哪了」，而**跑到那一行才停**的答案是
「已經印出來的還在，錯誤指在那一行」——**對初學者那比整段拒絕更好**。

**如何應用**：一個比率的分子若混了**兩種不同的成功**，拆開它。
判準：**如果把 A 改成 B，這個數字會變好，而使用者會變糟嗎？**
會 → 那個數字在鼓勵錯的事。

### 一個使用者自己取的名字，長得像函式庫的名字，是常態不是打錯

**現象**：把 C++ 的串流名（`cout`／`cin`）加進近似名的候選之後，
測試當場抓到 **`couty` 被誤認成 `cout`**（距離 1、兩邊都 ≥4）。

**教訓**：近似名建議的**候選從哪裡來，決定了門檻該多高**：

```
候選是【他自己宣告過的】   編輯距離可以用——他確實打算指其中一個
候選是【我們塞進去的】     🔴 門檻要更高，因為他可能根本沒想指它
```

**如何應用**：擴充「你是不是要打 X」的候選集合時，**每一種來源各自設門檻**。
本輪：變數用 Damerau-Levenshtein 距離 1，函式庫名字**只認大小寫**。

### 一個靠靜態掃描判定「沒有人用」的清理，會同時漏掉死的、殺掉活的

**現象**：查 `tree-sitter-c` 那條懸案（「裝了，而完全沒有被接上」）時，
`grep public/*.wasm` 給出：

```
tree-sitter-cpp.wasm    141 處引用
web-tree-sitter.wasm      0 處   ← 🔴 而它【是活的】
tree-sitter-c.wasm        0 處   ← 死的
tree-sitter.wasm          0 處   ← 死的
```

`web-tree-sitter.wasm` 的檔名是 `Parser.init({ locateFile })` **在執行期組出來的**
——**原始碼裡根本沒有那個字串**。照 grep 清，會刪掉一個系統跑不起來就少它不可的東西。

**教訓**：「有沒有人用」對**動態組出來的名字**是靜態掃不到的。
而這一族的危險不對稱：**漏掉死的只是浪費，殺掉活的是壞掉**。

**如何應用**：清理資產前，**用執行期的證據**（瀏覽器真的請求了哪些）而不是 grep。
⚠️ 而清完要留一條**會變紅的線**——本輪是第四十六條護欄，
它量的同樣是「發出去的請求」，與第四十五條同一個判準。

### 一個從第一個 commit 就躺著的東西，不會被任何「最近改了什麼」的檢查看到

**現象**：那兩個死 wasm（822 KB）是**專案第一個 commit** 放進來的
（`2584980` 初始化），**躺了整個專案的生命週期**——而 45 條護欄、
4195 支測試、無數次 review 都沒有碰到它。

**教訓**：**回顧型的檢查（diff、review、棘輪）看的都是「變動」**，
而一個從來沒有變動過的錯誤**永遠不在它們的視野裡**。

**如何應用**：定期問一次**「這個目錄裡的每一個檔，今天還有人要嗎？」**
——那是一個**全量**的問題，而它與所有增量檢查互補。
⚠️ 而它與「量現況 vs 量規範」是同一族
（見 [執行機構](concepts/執行機構.md)「量測工具有三種」）。

### 一份只走得到其中一條路徑的測試，會讓另一條路徑的缺陷全綠通過

**現象**：把 C 的產出從 6/10 修到 10/10，`c-style-parity` 全綠。
**而開瀏覽器一看**——`#include <iostream>`、`using namespace std;` 都還在。

根因：`cpp:program` 有**兩條產出路徑**——**有鷹架的（UI 走）**與
**legacy 的（測試走）**。第一版只改了 legacy。

**教訓**：一個函式裡的 `if/else` 分岔，如果**測試只走得到其中一邊**，
那另一邊的缺陷**不會被任何數字反映**——而那個數字看起來很健康（10/10）。

**如何應用**：改一個有分岔的產出點時，先問**「測試走的是哪一邊？」**
⚠️ 而答案要用**執行**確認（開瀏覽器、跑 e2e），不是讀程式碼推論。
本輪是 `experience`「重構後開瀏覽器實測」抓到的——**那條規則第一次真的救了人**。

### 一顆元件宣告它需要什麼，而那個宣告假設了一種產出風格

**現象**：C 產出缺 `<stdio.h>`。追下去發現 `cpp:print` 的 `requires` 宣告的是
**`<iostream>`**——**而它用 `printf` 產出時要的是 `<stdio.h>`**。

```
宣告   概念 → 標頭          今天的形狀
實際   (概念, 風格) → 標頭   而風格那一維【沒有被表達】
```

⚠️ **而系統的另一半已經知道這件事**：`style-exceptions` 的
`IO_PREF_TO_HEADER` 就是在**分析**同一個不一致，**而產出這一側沒有人接**。

**教訓**：一個「需求宣告」如果只有一維，它就**隱含地假設了其餘各維的預設值**
——而那個假設不會出現在任何型別上，直到有人換掉其中一維。

**如何應用**：看到 `A → B` 形式的宣告，問一句
**「B 會不會隨著別的東西改變？」** 會 → 那個宣告缺一維，
**而它今天正靠一個沒說出口的預設值活著**。

### 一個從測試檔撈出來的語料庫，會撈到那些「刻意壞掉的樣本」

**現象**：比對兩種投影的行為時，10 段語料裡有 1 段**兩邊都編不過**。
查下去——那段是 **`int main` 三個字**，某支測試在驗解析器對**殘缺輸入**的行為。

**教訓**：從測試檔的反引號字面撈語料，會連**刻意壞掉的樣本**一起撈進來
——而它們在報表上長得像缺陷。

🔴 **而正確的處置是留在「不可判定」欄，不是把它篩掉**：
篩掉的話分母會**安靜地變小**，而**縮分母比修分子容易**。

**如何應用**：語料裡出現「兩邊都失敗」的樣本時，**先看它是不是本來就壞的**。
是 → 歸「不可判定」並**寫出理由**；⚠️ **不要加一條篩選規則把它藏起來**。

### 一個開著沒關的 dev server，會讓「開瀏覽器實測」測到一個不存在的世界

- **現象**（2026-08-17，spec 136）：把目標接上選單之後開瀏覽器驗，
  選「C 語言教學」→ 風格切了（產出是 `printf`）、課程清單切了，
  **而第一行仍是 `#include <iostream>`**，積木區也顯示「引入函式庫 iostream」。

- **我的判定**：顯式的 `cpp:include` 節點走元件自己的 `generate`，
  **而那裡沒有人問過風格**——與 [history/072](history/072-target的兩個假設都倒了而它反而證明了target.md)§三
  「兩條產出路徑」同族，聽起來完全成立。我照這個判定改了程式碼。

- 🔴 **而它重現不出來**：build 產物、以及**全新啟動的 dev server**，
  在**沒有**那個修正的情況下產出就已經是 `<stdio.h>`
  ——`header` 屬性本身早就被改寫了，那顆元件的 `generate` **從來沒看到過 `iostream`**。
  真正的原因：那個 dev server 是我在**好幾個編輯之前**啟動的，HMR 沒跟上。

- **教訓**：**開瀏覽器實測只有在 server 是【改動之後】啟動的時候才算數。**

- ⚠️ **而它反過來限縮了本檔「重構後開瀏覽器實測」那一條**：
  上一輪（spec 134）它救了人，這一輪它**製造了一個假缺陷**
  ——而**假缺陷比沒抓到更貴，因為我為它寫了程式碼**（那段修正是死碼，已還原）。

- **如何應用**：手動開瀏覽器之前問一句「**這個 server 是什麼時候起的**」。
  在改動之前 → **重啟它**。⚠️ 判準不是「HMR 通常會生效」，
  是「**我沒辦法從畫面上分辨它有沒有生效**」。

- **相關**：本檔「e2e 測的是 build 產物」——同一個根（跑的不是現在的程式碼），
  而那條講的是**產物**，這條講的是**開發伺服器的模組狀態**；
  [執行機構](concepts/執行機構.md)「機制有了，沒人接上」

### 一個「靠宣告推導」的判準，看不見那些不需要宣告任何東西的成員

- **現象**（2026-08-17，spec 136）：要推導「哪些概念在 C 裡不存在」，
  用元件自己的 `requires`（標頭宣告）對照 C/C++ 標頭對映表——
  **59 顆**，判準乾淨、可重跑、不會與課程清單漂移。

  🔴 **而 `class_def`／`method_virtual`／`template_function`／`try_catch`／`lambda`
  一顆都沒被抓到**——它們**不需要任何標頭**。

- **教訓**：一個靠「X 宣告了什麼」推導出來的分類，
  對**那些不需要宣告 X 的成員完全失效**——而它們在報表上看起來像**通過**。

  > **判準的涵蓋範圍等於宣告的涵蓋範圍，而那兩件事很容易被混為一談。**

- **如何應用**：用宣告當判準時，先問「**有沒有一種成員根本不需要這個宣告**」。
  有 → 那一族**不在判準的視野裡**，要嘛補宣告、要嘛另立一段，
  ⚠️ **而不要以為「沒被抓到」等於「不屬於這一類」**。

- 🟢 **而具名清單不是失敗，只要它有執行機構**：本輪那 35 顆用一份具名清單，
  並配**兩道獨立的交叉驗證**（幽靈檢查 ＋ 掃產出裡的 C++ 專屬語法）。
  注入實測：把 `cpp:class_def` 放回去 → **兩支都紅、都指名它**。

- ⚠️ **而其中 9 顆是「該由推導抓到而漏宣告」的**（`container_*` 七顆、
  `string_at`、`istringstream_declare` 沒有 `requires`）——
  **正確的修法是補宣告，不是擴充清單**。本輪沒修，理由是
  `requires` 不是描述而是**驅動自動 include 的資料**，補一筆要驗行為。

- **相關**：本檔「一顆元件宣告它需要什麼，而那個宣告假設了一種產出風格」
  ——那條講宣告**少一維**，這條講宣告**少一族**

### 一個靠「識別字的名字」認人的樣式，會把使用者自己宣告的名字搶走

- **現象**（2026-08-17，spec 137）：`HIGH`／`LOW`／`OUTPUT` 做成一個 lift 樣式
  （AST 是 `identifier`、文字等於那些名字 → 一顆腳位常數概念）。
  第三十二條護欄（行為的誤差）當場抓到語料裡的：

  ```c
  enum Level { LOW = -1, MEDIUM = 0, HIGH = 1 };
  cout << LOW << endl;      // 參照：-1     我們：0  🔴
  ```

- **教訓**：**一個名字的意思由【誰宣告它】決定，而樣式比對看不到宣告。**

- ⚠️ **而既有的 `cpp:builtin_constant` 用【同一個做法】**（`EOF`／`NULL` 靠名字 lift）
  ——🔴 **差別不在做法，在那些名字幾乎沒有人會重新宣告**。
  > **同一個做法在兩個名字上，一個安全一個不安全
  > ——而安全的那個不會告訴你為什麼。**

- **如何應用**：要把一個名字綁成概念之前，問一句
  **「使用者會不會拿這個名字宣告自己的東西？」**
  會 → 那不是 lift 的事，是**執行期查不到宣告之後**才問的事。
  ⚠️ 判準不是「這個名字常不常見」，是「**它出現在別人的宣告裡合不合理**」。

- 🔴 **而代價要說出來**：改到執行期之後，積木 → 程式碼 → 積木時那顆會**變回變數參照**。
  **correctness 贏 round-trip**——一個印錯數字的程式，比一顆換了形狀的積木糟。

- **相關**：[執行機構](concepts/執行機構.md)；本檔「一個『靠宣告推導』的判準，
  看不見那些不需要宣告任何東西的成員」——那條講**判準的盲點**，這條講**判準的越界**

### 我把「這個功能需要什麼資訊」錯認成「這個資訊要從哪裡傳進來」

- **現象**（2026-08-17，spec 137）：要讓 Arduino 的 `setup`／`loop` 跑得起來，
  我在規劃時指認的「本輪真正的閘門」是
  「執行路不知道目標——`ExecutionContext` 沒有 `style` 也沒有 `target`」。

  🔴 **而修法是六行，核心一個字都沒動**：進入點由「**樹裡有哪些函式**」決定。

- **而那顆元件的檔頭早就寫著答案**（逐字）：

  > 「哪一個函式是進入點是**這顆（整個程式）的知識**，不是那顆函式的性質。」

  ⚠️ **我讀過那句話，還把它抄進了規格——而我沒有把它當成答案。**

- **教訓**：問「這個功能需要什麼資訊」時，**先問那個資訊在不在手上的資料裡**，
  再問「要從哪裡傳進來」。⚠️ 後者是一個**設計問題**，會長出欄位、傳遞、生命週期；
  前者常常是**一次查詢**。

  > **一個被誤判成「缺資訊」的問題，會被答成一條新的傳遞路徑
  > ——而那條路徑之後要有人維護。**

- **如何應用**：規劃階段寫下「閘門」時，**逐條問「我怎麼知道它是閘門」**。
  如果理由是「某個結構裡沒有那個欄位」，⚠️ **那只證明了它不在【那裡】**。

- **相關**：本檔「一顆元件宣告它需要什麼，而那個宣告假設了一種產出風格」

### 我寫的測試繞開了我自己知道的實作邊界——而我看不見那個繞開

- **現象**（2026-08-17）：為 11 顆新膠囊寫了 **38 支自證測、全綠**。
  隔一步用**資訊隔離的盲測**（`component-fuzz`，出題者跑在看不到原始碼的
  worktree 裡）跑 20 段，抓到 **3 個真缺陷，其中兩個就在那 11 顆裡**：

  ```
  5L / 7U / 1.5f / 4294967295UL   → 全部【NaN 且不出聲】
  Serial.print(v, 3) 的第二引數    → 被【靜默丟掉】
  ```

- **為什麼我的測試看不到**：我知道字面值的實作是 `Number(raw)`，
  **所以我不會想到去寫 `5L`**。同理，我知道 `serial_print` 只接一個引數，
  **所以我測的每一段都只給一個引數**。

  > **一個知道實作的人寫的測試，會沿著實作的形狀長
  > ——而實作的形狀正是缺口的形狀。**

- ⚠️ **而這不是「測得不夠多」**：38 支涵蓋 lift／generate／round-trip／execute
  四路，每條負向前都有正向錨點。**數量與結構都對，而視野是歪的。**

- **如何應用**：交付一批新概念之後，**跑一次不知道實作的盲測**。
  判準不是「我測得夠不夠」，是「**出題的人知不知道我做了什麼**」。
  🟢 而它便宜：一次 skill 呼叫。

- 🔴 **而報表要能承受「發現＝退步」**：這一輪停用測試 32 → 45。
  那 13 筆**不是新造的缺陷，是新發現的**——而棘輪只看得到數字上升。
  **上調要指名、要寫理由**，否則下一個人會以為有人放水。

- **相關**：[執行機構](concepts/執行機構.md)；本檔「一個過濾掉答案的探測，
  輸出看起來與『答案不存在』一模一樣」——那條講**探測的濾網**，這條講**出題者的視野**

### 一個從「刻意找邊界」的語料得出的優先序，會把邊界誤認成中心

- **現象**（2026-08-17）：盲測 20 段**難度 hard ＋ 刻意陷阱**的 Arduino 程式，
  抓到 13 筆缺口，其中 **6 筆是 `const`／`static` ＋ 陣列**。
  我據此寫進轉向記錄：「`const byte PINS[3] = {9,10,11};` 是 Arduino 教材的招牌寫法」。

  🔴 **而換一批【典型的】語料（20 段，AI 會寫的那種）實測：出現 0 次。**

- **為什麼**：出題的提示要的是「什麼會讓工具壞掉」。
  **那批語料的分佈是【缺口的分佈】，不是【使用的分佈】** ——
  而我把前者當成後者去排優先序。

  > **同一個系統，兩個母體，兩個數字——而它們沒有矛盾，因為問的是兩件事。**

- **如何應用**：**排優先序要用「使用的分佈」，找缺口要用「邊界的分佈」。**
  ⚠️ 判準：問一句「**這批語料是怎麼被生出來的**」。
  出題者被要求「找邊界」→ 它答得了「哪裡會壞」，**答不了「哪裡常壞」**。

- 🟢 **而兩批都要留**：這一輪之後兩份探測並存，⚠️ **而它們的數字不可互相解讀**
  （一份執行通過 7/20，一份殘差 0.07%）。

- **相關**：本檔「我寫的測試繞開了我自己知道的實作邊界」——那條講**出題者的視野**，
  這條講**出題者的目標**；[執行機構](concepts/執行機構.md)「量測工具有三種」

### 一次誤判本來只是「身分不對」，而少一層防護它會變成「產出無效程式碼」

- **現象**（2026-08-17）：`dht.begin()` 被 `cpp:container_iter`（迭代器取得）認走
  ——那是一顆**運算式**概念，而產生器對運算式**不補分號也不換行**：

  ```
  原始     dht.begin();  lcd.init();
  一次產出  "dht.begin()    lcd.init();"      🔴 兩句黏在一起
  二次產出  "    dht.begin().init();"         🔴 而第二次把它們合併成方法鏈
  ```

- **教訓**：**誤判的代價不是固定的，它取決於下游有沒有一層「即使判錯也產得出合法輸出」的防護。**
  少了那一層，一個身分錯誤會**升級成語義錯誤**——而且是在**下一次讀進來的時候**才發作。

- **如何應用**：問一句「**如果這裡判錯了，產出還合法嗎**」。
  🟢 合法 → 誤判只是誤判，看得見也修得動。
  🔴 不合法 → **誤判會被下一輪 lift 讀成另一個意思**，而那時根因已經看不見了。

- ⚠️ **而修那一層試了三版，前兩版都動到了量測工具**：
  「產出沒有換行結尾 ⟹ 它是運算式」是**猜的而且錯**（`do-while` 是語句而它也沒換行）；
  改成問宣告之後，完備性護欄的 9 筆判定翻掉——它的合成環境是
  **一個沒有 `int main` 的 program**，而在那裡 `-5` 與 `-5;` **都不合法**。

  > **兩種都是壞的合成環境；改動只是換了它壞的方式，而那讓它現形。**

  最後的判準是**位置**：函式體裡的裸運算式是合法的運算式語句，編譯單元層級的不是。

- **相關**：[投影](concepts/投影.md)「可逆性分級」；本檔「靜默降級反模式」


### 一個名字對得剛剛好的設定，會讓人以為那一格已經填完了

- **現象**（2026-08-17，spec 138）：Webview 的 CSP 事前想到了五條，
  而**漏了 `connect-src`**——Blockly 用 `fetch` 載那三個 `.mp3` 音效：

  ```
  Fetch API cannot load …/media/click.mp3.
  Refused to connect because it violates the document's CSP.
  PAGEERROR Failed to fetch
  ```

  ⚠️ **而 `media-src` 已經設了**。那條管的是 `<audio>`／`<video>` **元素**，
  而 Blockly 走的是 **`fetch` ＋ `AudioContext`**。

- **教訓**：**兩條看起來管同一件事的指令，管的其實是兩條不同的路。**
  而 `media-src` 這個名字**對得剛剛好**——它讓人**停止追問**「音效是怎麼載進來的」。

  > **一個名字對得剛剛好的設定，會讓人以為那一格已經填完了。**

- **如何應用**：白名單型的設定（CSP、`localResourceRoots`、防火牆規則）
  不要照**資源類型**填，要照**取得資源的機制**填。
  判準：問「**這個東西是誰去拿的**」，不是「這個東西是什麼」。

- 🟢 **而它是被一支「先在 Chromium 裡撞一遍」的預檢抓到的**，不是在 IDE 裡。
  ⚠️ 那支預檢第一版**自己寫了一條寬鬆的 CSP**，於是它預檢到的不是要上線的東西
  ——改成呼叫同一個 `csp()` 函式才對。

  > **一個「只是拿來預檢」的東西，如果自己有一份設定，
  > 那它預檢到的就不是那個要上線的東西。**

- **相關**：本檔「靜默降級反模式」；[執行機構](concepts/執行機構.md)「機制有了，沒人接上」

### 一個把「沒有」與「零」編碼成同一個值的量，遲早會拿零去回答沒有

- **現象**（2026-08-17，spec 138）：要從 209 筆積木規格裡挑「最簡單的一顆」，
  判準寫成「`blockDef.args0` 長度最小」。它挑到 `cpp_array_2d_declare`
  ——**而那顆的 `args0` 不存在**，因為它的真正定義是**命令式**的
  （動態插槽要 `+`／`-` 按鈕，宣告式的 `args0` 描述不了）。

  ```
  我以為   args0 長度 0 = 最簡單
  實際     args0 長度 0 = 這份 JSON 根本沒在描述這顆積木
  ```

- **教訓**：我用一個**數量**去代理一個**性質**（「欄位少」代理「簡單」），
  而那個數量在「**這一格沒填**」與「**這一格填了 0**」之間**分不出來**。

  > **一個把「沒有」與「零」編碼成同一個值的量，遲早會拿零去回答沒有。**

- **如何應用**：拿一個計數當判準之前，先問「**這個計數是 0 的時候，
  有幾種不同的原因**」。多於一種 → 加一個**存在性**的前置條件
  （這一例是「`message0` 必須存在」）。

- ⚠️ 而這一次的代價本來會很大：它要證的正是「積木**來自登錄表**」，
  而**一顆定義住在別處的積木證明不了那件事**——判準錯了，結論會是假的。

- **相關**：本檔「靜默降級反模式」；[執行機構](concepts/執行機構.md)「沉默的正確和沉默的缺失撞在一起時，讓正確的那個說話」
  （⚠️ 這個指標 2026-08-18 修正過：原本指向 `concepts/元件.md`，而那個檔裡**一個字都沒有**
  ——連結解析得開，所以死連結掃描抓不到。**指錯檔的引用比斷掉的引用難發現。**）

### 三個檢查器對同一份程式碼有三種嚴格度，而最寬的那個會先跑

- **現象**（2026-08-17，spec 138）：`src/vscode/panel.ts` 用了建構子參數屬性
  （`constructor(private readonly x: T) {}`），而專案開了 `erasableSyntaxOnly`。

  ```
  npm test（vitest → esbuild）    🟢 通過
  npm run build:vscode（Vite）    🟢 通過
  npx tsc --noEmit                🔴 error TS1294
  ```

- **教訓**：**打包器不是型別檢查器。** esbuild／Vite 只做語法轉換，
  `tsconfig` 的規則對它們**大部分不存在**——而它們跑得比 `tsc` 快、也更常被跑。

  > **一份程式碼有幾個檢查器，就有幾種「通過」的意思。**

- **如何應用**：把新程式碼放進 `tsconfig` 的 `include` 裡，**而不是排除它來讓建置變綠**。
  🔴 判準：如果「讓它過」的方法是**縮小檢查範圍**，那就是把尺改短。

- 🟢 **而這一則正是「放 `src/vscode/` 而不是根目錄 `vscode/`」買到的東西**
  ——放在護欄與 `tsc` 看不到的地方，等於替它辦一張免檢證。

- **相關**：[執行機構](concepts/執行機構.md)；本檔「一個名字對得剛剛好的設定…」

### 一個缺口如果只在「大家都不那樣寫」的地方，它會一直在

- **現象**（2026-08-17，spec 139）：新增一個測試檔，裡面的 fixture 寫了
  `std::cout << x + y;`。第三十一條護欄（形態的殘差）立刻 **0 → 0.02%**。

  ```
  cout << 1;        🟢 clean
  std::cout << 1;   🔴 unresolved ＋ raw_code
  std::string s;    🟢 clean      ← 所以不是「限定名稱」全壞
  ```

  `cpp_cout_chain` 的 `rootMatch.text` **一直**是精確比對 `"cout"`
  ——⚠️ 而既有語料幾乎都寫 `using namespace std;`，所以它**從來沒有現形**。

- **教訓**：覆蓋率不是「量了多少」，是「量的東西**長得多像**」。
  一批寫法一致的語料，會讓所有「只在另一種寫法下才錯」的缺口**集體隱形**。

  > **一個缺口如果只在「大家都不那樣寫」的地方，
  > 它會一直在，而且沒有人會發現。**

- **如何應用**：新增語料時**故意換一種等價的寫法**（限定 vs 不限定、
  `printf` vs `cout`、`i++` vs `++i`）。🟢 而護欄做對了它該做的事
  ——**一段新的真實程式碼進來，數字就動了**。

- 🔴 **而處置有三條路，兩條是錯的**：

  ```
  調基線                        🔴 把尺改短
  把 fixture 換成不會踩到的寫法   🔴 也是——那是 curating the corpus
  修它                          🟢
  ```

  ⚠️ 我一度走錯：先去改護欄的語料抓取（以為污染來自散文），
  而那**沒有修好任何東西**。
  > **一個「改了護欄而沒修好任何東西」的變更，就是把尺改短的前一步。**

- **相關**：本檔「排優先序要用使用的分佈，找缺口要用邊界的分佈」；
  [執行機構](concepts/執行機構.md)

### 一個講得通的解釋，與一個正確的解釋，在數字上長得一樣

- **現象**（2026-08-17，spec 139）：量到「一次積木編輯重寫 5 行」，
  而我解釋成「第一次編輯會把使用者的排版換成我們的，所以跨距大」
  ——那個解釋**有出處、有病歷、講得通**（`history/080`§三 量過排版差異）。

  🔴 **而它是錯的。** 真正的原因是 `registerCppLanguage()` 沒被呼叫，
  產生器沒註冊。補上之後跨距變成 **1 行**。

- **教訓**：一個講得通的解釋會**停止追問**——而它與正確的解釋
  在數字上長得一樣。⚠️ 尤其當那個解釋**引用得出病歷**時，
  它看起來比「我不知道」可信得多。

- **如何應用**：一個數字若與預期不符而你「解釋得通」，
  問一句「**這個解釋預測得出別的什麼**」。
  - 解釋預測「第二次編輯會變小」→ 那就去量第二次。
  - 預測不出任何別的東西 → 它可能只是一個**故事**。

- ⚠️ 而這一次的缺失還有另一個特徵：**它只在第三個功能上現形**。
  積木→程式碼、程式碼→積木**兩個方向看起來都正常**，
  直到按下單步才噴 `RUNTIME_ERR_UNKNOWN_CONCEPT`。

- **相關**：本檔「一個名字對得剛剛好的設定，會讓人以為那一格已經填完了」

### 一個「per-document 的欄位」，如果從文件本身重算得出來，那它就不是狀態

- **現象**（2026-08-17，spec 139）：VSCode 擴充被認為必須先把 `App` 的
  18 個 per-document 欄位拆乾淨才能做——那被寫進 draft 當成**前置工程**。

  🟢 而量完「切分頁重建要多久」（**13 ms**）之後，它拆成兩堆：

  ```
  從文件重算得出來   tree／target／topic／branches／style／ioPref／cache
                    → 切分頁時【丟掉重建】
  導不出來的視圖狀態  捲動／縮放／undo／dirty
                    → 只有這一堆要真的保存，而它小得多
  ```

- **教訓**：**「狀態」與「快取」的差別不在它住在哪，在它導不導得出來。**
  一個導得出來的東西被當成狀態管理，會讓「保存它」變成一個假的必要工作。

- **如何應用**：看到一份「要拆的狀態清單」時，先逐項問
  「**這一格能不能從真相重算**」——⚠️ 而「重算太慢」要**量**，不要假設。
  🔴 這一次量完，那個被視為前置的大工程**整個不必做**。

- **相關**：[投影](concepts/投影.md)；本檔「一個被當成架構限制的東西…」

### 一個被吞掉的例外，會把「失敗了」變成「成功了，只是內容比較少」

- **現象**（2026-08-18，spec 140）：使用者連續回報「`setup()`／`loop()` 不見了」
  「整份 sketch 變成 `int x;`」。而元凶是一行寫著「安全」的程式碼：

  ```ts
  try { this.setState(event.blockState) }
  catch { /* safe to ignore */ }
  ```

  載到一半拋錯 → **工作區是殘的** → 下一次積木變動把殘的**寫回使用者的檔案**。

- **教訓**：**一個被吞掉的例外，會把「失敗了」變成「成功了，只是內容比較少」。**
  而「內容比較少」在一個雙向同步的系統裡，等於**刪掉使用者的資料**。

- **如何應用**：處置有兩半，缺一不可——**說出來**（記錄／提示），
  以及**記住這份結果不可信**（旗標 ＋ 讓每一條下游都問它）。
  🔴 只做前一半的話，錯誤有人看見而資料照樣被毀。
  ⚠️ 判準：**這個 catch 之後，有沒有人會拿這份半成品去覆蓋別的東西？**

- **相關**：[執行機構](concepts/執行機構.md)；本檔「靜默降級反模式」

### 兩邊各自把同一份規格翻譯一次，就會有兩份規格

- **現象**（2026-08-18，spec 140）：範圍編輯的模型是「行陣列的 splice」。
  積木那側用它算鏡像，而宿主那側**自己把它翻譯成 `vscode.Range`**：

  ```ts
  new vscode.Range(new vscode.Position(span.startLine, 0), doc.lineAt(lineCount - 1).range.end)
  ```

  檔尾追加時 `startLine === lineCount`，而那是一個**不存在的位置**——被夾到檔尾，
  於是新文字接在最後一行**後面**而不是**下面**：`}Serial.println();`。
  🔴 而它**自我延續**：檔案一旦沒了結尾換行，之後每次追加都再合併一次。

- **教訓**：**兩邊各自把同一份規格翻譯一次，就會有兩份規格；
  而它們的分歧只在資料被寫壞的時候才看得見。**

- **如何應用**：讓兩側**呼叫同一個函式**算出結果，再由結果反推最小的編輯
  （共同前後綴）。🟢 這樣仍然是最小編輯，而「第二份翻譯」不存在。
  ⚠️ 判準：**同一個語義有沒有第二處實作？** 有 → 那不是最佳化，是第二份真相。

- **相關**：[投影](concepts/投影.md)；本檔「一份只走得到其中一條路徑的測試…」

### 模擬環境有三層天花板，而每一層都讓一個真的缺陷變綠

- **現象**（2026-08-18，spec 140）：擴充的預檢在 Chromium 裡跑一個假宿主。
  使用者連續七次回報缺陷，而預檢每一次都是綠的：

  ```
  ① 只記訊息的假宿主   驗得到「有沒有送」，驗不到「收不收得下」   → 版本永遠差一
  ② 會回話的假宿主     驗得到協定，驗不到宿主【怎麼實作】那個協定  → Range 翻譯錯
  ③ Chromium ≠ Theia   同一份檔案內容，一邊 9 顆積木一邊 TypeError
  ```

- **教訓**：**一個模擬環境驗得到「我送了什麼」，驗不到「對面怎麼處理」。**
  升級模擬（①→②）只是把天花板抬高一層，不會讓它消失。

- **如何應用**：把「對面怎麼處理」**變成同一份實作**（見上一條），
  剩下真正環境相依的部分**明說驗不到**，不要讓綠燈代表它。
  ⚠️ 而模擬環境「重現不出來」本身是線索：**它取代掉的那一塊，正是出事的那一塊。**

- **相關**：[執行機構](concepts/執行機構.md)「為什麼『看一眼』永遠抓不到」

### 一個沒有紀錄的系統只能被推理，而推理出來的修法要靠下一次災難驗證

- **現象**（2026-08-18，spec 140）：使用者七次回報「檔案被寫壞了」，
  我七次「修好了」——而**其中三次的症狀是上一次的修法造成的**：

  ```
  修「能力旗標沒人讀」  → 只關了 JS 那一半，CSS 照舊 → 積木看不見
  修「版本差一」        → ready 時清掉綁定再重找 → 點進面板就斷線
  ```

  🟢 換成「讓後果變小 ＋ 讓證據留下 ＋ 讓錯誤出聲」之後才收斂。
  真正的根因是**推**出來的——但那是在有了「檔案怎麼一步步變壞」的完整形狀之後。

- **教訓**：**一個沒有紀錄的系統，只能被推理；
  而推理出來的修法，要靠下一次災難來驗證對不對。**

- **如何應用**：連續兩次「修好了又壞」時**停止推理**，先交付三樣東西：
  ① 安全網（讓錯誤的後果小於一次重試）
  ② 流水帳（哪一次操作造成的，而不只是結果長怎樣）
  ③ 出聲（把靜默的失敗變成看得見的失敗）
  ⚠️ 而安全網要**明說它不是修好根因**——擋下來的次數非 0 就代表上游還有 bug。

- **相關**：本檔「一個被吞掉的例外…」；[history/083](history/083-面板就是網頁版本身而四次修好都是我自己上一次修出來的.md)

### 要讓一個通用的層知道特例，辦法是讓特例自己帶著宣告來

- **現象**（2026-08-18，spec 140）：Arduino 的 `.ino` 沒有 `main()`，
  而鷹架把 `setup()`／`loop()` **包進了 `int main()`**——那寫進了使用者的檔案。
  最短的修法是讓 `src/ui` 認得 `'arduino'` 這個名字，⚠️ **而中立性護欄擋著**。

  🟢 處置是給目標加一格宣告（`Target.entryShell: 'main' | 'none'`），
  由**兩個**下游各自問它（鷹架與程式碼補丁器）。

- **教訓**：**要讓一個通用的層知道特例，辦法是讓特例自己帶著宣告來，
  不是讓通用的層去記住特例的名字。**

- **如何應用**：新增這種宣告時要能回答「**它今天住在哪裡**」
  ——`entryShell` 的答案是「寫死在鷹架的 `'int main() {'` 與補丁器的第 3 步」。
  🔴 答得出來＝替既有決定命名；答不出來＝新的抽象層，那要另外討論。
  ⚠️ 而**同一個決定的每一個入口都要問同一份宣告**：只改一處時症狀只少一半，
  而少一半看起來很像修好了。

- **相關**：[元件](concepts/元件.md)；[執行機構](concepts/執行機構.md)

### 一個症狀在上游被修好之後消失，是證據，不是證明

- **現象**（2026-08-18，spec 140）：`TypeError: … reading 'indexOf'` 只在 Arduino IDE
  出現，Chromium 用**相同的檔案內容**重現不到。為它排除了三個假設（tree-sitter 的
  過期樹／自家的 `idToDir`／組態比文件晚到），三個都附了證據。
  🟢 而修好**另一個** bug（宿主自己把行範圍翻譯成 `Range`）之後，它就不再出現了。

- **教訓**：**一個症狀在上游被修好之後消失，是證據，不是證明——
  而兩者的差別在於：下一次它回來時，有沒有東西會先出聲。**

- **如何應用**：這種項目**不要記成「已修復」**，記成「停止復現 ＋ 留了哪些防線」。
  🔴 判準：**有沒有一個會紅的檢查在守它？** 沒有 → 它只是暫時安靜。
  ⚠️ 而防線要選「再發生時能把問題縮到最小」的那種
  （這一次是：失敗時逐顆隔離重載，指出是哪一顆積木 ＋ 它的 extraState），
  不是「再發生時記一筆」。

- **相關**：本檔「一個沒有紀錄的系統只能被推理…」；
  [history/083](history/083-面板就是網頁版本身而四次修好都是我自己上一次修出來的.md)

### 一個「每次都多一點」的錯誤，單看一次轉換是對的

- **現象**（2026-08-18，第 0 批的 fuzz）：20 段 AI 生成的 Arduino sketch 丟進
  lift → generate → lift，抓到**兩個既有的 bug**，而它們有同一個形狀：

  ```
  區塊註解      /* * A  →  /* * * A  →  /* * * * A      每轉一次多一顆星號
  陣列宣告      int a[] = {8個}  →  int a[10] = {8個}   大小被【編】出來
  ```

  🔴 第二個不是排版：`sizeof(a)/sizeof(a[0])` 從 8 變 10
  ——學生的旋律迴圈**多播兩個垃圾音**。

- **教訓**：**一個「每次都多一點」的錯誤，單看一次轉換是對的
  ——它只有在【轉第二次】的時候才看得見。**

- **如何應用**：round-trip 測試要跑**兩次**產生（`g1 = gen(lift(src))`、
  `g2 = gen(lift(g1))`，斷言 `g2 === g1`）。⚠️ 只比對 `src` 與 `g1` 的話，
  「產生器把它正規化了」與「產生器每次都加一點」**看起來一樣**。
  🔴 而判準是**冪等**：`gen ∘ lift` 套第二次必須是恆等。

- **⚠️ 而它們為什麼是被 fuzz 抓到，不是被既有測試**：既有語料用 `//` 行註解、
  陣列都寫了大小；而 **AI 生成的 sketch 幾乎都以 `/* … */` 開頭、旋律陣列幾乎都不寫大小**。

  > **一批「我們自己寫的」語料，量到的是我們自己想得到的用法。**

- **相關**：本檔「靜默降級反模式」（那個 `?? '10'` 是它的又一個實例）；
  [投影](concepts/投影.md)「可逆性分級」

### 一個警告完卻做出相反行為的分支，比沒有警告更糟

**情境**：`PatternLifter` 遇到「樣式宣告了一支具名策略，而登錄表裡找不到它」時，
印一行 `console.warn` 然後**往下走**，落到無條件的樣式比對。

**問題**：策略的存在本來就是因為「要跑真邏輯才判得出來」。找不到它而改用無條件比對，
是做出**與策略意圖相反**的事。2026-08-18 被一顆掛在 `translation_unit` 上的策略引爆
——某支測試沒有登錄膠囊的策略表，於是**每一支程式的根節點**都被判成那顆概念，
31 支測試一起紅。

⚠️ 而它在此之前已經**無聲地錯了很久**：既有的 11 顆膠囊策略在同一支測試裡也全部
落到無條件比對，只是它們的節點型別（`enum_specifier`／`lambda_expression`…）
剛好沒出現在那些程式裡。**沒有人看那行 warn。**

**教訓**：一個「發現異常 → 記一筆 → 繼續用不對的路徑」的分支，把「壞了」偽裝成
「注意一下」。而 `console.warn` 在測試輸出裡與雜訊無法區分。

**怎麼做**：異常時**停在那一步**（回 `null`／丟錯），不要退到一條語義不同的路。
🔴 而判斷的方法是問：**退路做的事，與原本要做的事，是不是同一件事？**
不是的話，它就不是退路，是另一個功能。

### 一顆預組積木把它摺掉的概念，從別人的覆蓋清單裡一起帶走

**情境**：第 0 批的整合測試宣稱「九顆全部在這四段場景裡被覆蓋到」，
而 `delay_microseconds` 的唯一覆蓋來源是那段超音波程式。

第 1 批把那五句觸發序列摺成一顆積木之後，那段程式**不再產出** `delay_microseconds`
——⚠️ **而那句宣稱會變成假的**。

**問題**：預組（一顆積木 = N 句）是一個**會回頭改變既有測試語義**的操作。
它不改任何既有程式碼，卻讓既有測試的**覆蓋主張**失效。
🟢 而這一次那條護欄當場叫了（它逐顆數九顆的出現），所以損失是**看得見的**。

**教訓**：加一顆預組概念時，要問**「它摺掉的那些概念，誰在靠這段程式覆蓋它們」**。

**怎麼做**：把覆蓋主張寫成**會失敗的斷言**（逐顆數，而不是「大致上有測到」）。
🔴 一句寫在註解裡的覆蓋主張，被摺走時不會有任何人知道。

### 一個解析器少了一個階段，會在那個階段本來會消掉的地方看到歧義

> 處置的通則在 [concepts/降級與認知邊界](concepts/降級與認知邊界.md)「辨識的保守性」。

**情境**：`DHT dht(DHTPIN, DHT11);` 被 tree-sitter 解析成**函式宣告**，
而 `DHT dht(2, DHT11);` 解析成變數定義。同一個東西，兩種結果。

**根因**：C++ 的「最令人困惑的解析」（most vexing parse）——`T x(A, B);` 在
語法上既可以是變數定義也可以是函式宣告。⚠️ **而真編譯器沒有這個問題**：
前置處理器先把 `DHTPIN` 換成 `2`，它看到的一直是後者。

**tree-sitter 不做前置處理。**

> **一個解析器少了一個階段，會在那個階段本來會消掉的地方看到歧義
> ——而那個歧義在原始語言裡並不存在。**

🔴 而它的影響不是邊緣的：盲測語料裡 **30% 用這個寫法**（`#define` 定腳位再傳給
建構子是 Arduino 教學的標準寫法）。不修的話那一族程式全部認不出來。

**怎麼做**：在缺掉的那個階段本來會提供的資訊裡找替代——這裡是「型別是一個
**登錄過的具體型別**」（`DHT`／`Servo`／`LiquidCrystal`），而那種型別在 sketch 裡
不會被當成函式的回傳型別。**歧義由我們自己已經知道的事情解掉。**

### 一個「誠實的失敗值」如果沒有配套的檢查函式，它與一個當機沒有差別

> 通則在 [concepts/模擬的誠實](concepts/模擬的誠實.md)——**模擬器什麼時候可以編一個數字出來**。

**情境**：溫濕度感測器讀不到時回 `NaN`——那是查證過的真板子行為，而理由是
「學生的程式**本來就會檢查它**」（`if (isnan(h))` 是所有教材的標準寫法）。

**問題**：測試當場證明那句話原本是空的——**`isnan` 這個函式不存在**，
所以程式在**檢查那一行**就中止了。學生看到的不是「感測器讀不到」，是「程式壞了」。

> **一個誠實的失敗值，它的誠實建立在「有人接得住它」上。
> 接不住的話，它與一個當機沒有差別。**

**怎麼做**：設計一個失敗值時，**同一輪就把接住它的東西做出來**。
🔴 判準是：**寫一段真實的使用者程式碼，讓它走到那條失敗路徑，看它走不走得完。**
（那條測試現在釘在 `servo_write/spec.test.ts`：斷言 `"no sensor"` 真的被印出來。）

### 一個只在投影那一側丟資料的 bug，lift 與 generate 各自的測試都看不到它

> 通則在 [concepts/投影](concepts/投影.md)「雙重真相來源」——同一顆積木兩份定義。

**情境**：使用者在 Arduino IDE 看到 `Serial.write(cmd)` 的積木是
「對 Serial 執行 write（ ▯ ）」——**括號裡是空的**。

**而兩側都是對的**：

```
語義樹   children.args = [var_ref cmd]     ✅
產生器   Serial.write(cmd);                ✅
積木     inputs: {}                        🔴 引數不在上面
```

於是 lift 的測試綠、generate 的測試綠、round-trip（文字→文字）也綠
——⚠️ **因為那條路徑根本沒有經過積木**。而使用者按一次「積木→程式碼」，
`cmd` 就永遠不見了。

> **一個資料只在【某一條投影】上丟失時，
> 任何不經過那條投影的測試都會是綠的。**

🔴 **而根因有兩層，只修一層等於沒修**：

```
① renderMapping 把 `ARGS` 當成【欄位】對到 `args` 這個【接點】
   —— 接點是節點陣列，一個文字欄位裝不下
② 修完①之後產出的 `ARG_0`【沒有插槽可接】
   —— `block-registrar.ts` 只替 `cpp_func_call` 寫了動態引數
```

②是專案記過的**雙重真相來源**：同一顆積木，JSON 一份、UI 一份，而它們不一致。

**怎麼做**：涉及積木形狀的東西，測試要**渲染成積木狀態再斷言**，
不能只驗「語義樹對」與「產出的字串對」。🔴 而同一輪要問：
**這顆積木在 UI 那側有沒有第二份定義？**

⚠️ 同一天出現三次（液晶的建構參數、方法呼叫的引數、而第三次是液晶那顆
在瀏覽器裡才看見）——**三次都是眼睛看到的，沒有一次是測試先報的**。

### 一個大得像發現的數字，比一個小得像雜訊的數字更該先被懷疑

**情境**：新蓋的「投影遺失」護欄第一次跑，報 **261/357 = 73% 的接點在積木上找不到落點**。

那個數字讀起來像一個發現（「原來投影層爛成這樣」）。⚠️ **而它與一個已知事實矛盾**：
`cpp:arithmetic` 的 left／right 顯然在畫布上是好的——整個系統天天在用它。

根因是我沒有把節點包進 `cpp:program` 就渲染。包了之後：**15 處**。

> **一個大得像發現的數字，比一個小得像雜訊的數字更該先被懷疑
> ——因為它會讓人停止查證，直接開始解釋。**

**怎麼做**：新護欄第一次跑出來的數字，先問「**這個數字如果是真的，
它與我已知的事實矛盾嗎**」。矛盾 → 先查工具，不要先寫結論。

⚠️ 而同一輪它犯了第二次：**基線產生器**跑在 `beforeAll` 之前，量到一個沒載入
渲染器的世界，產出 **257** 筆——而測試裡量到的是 13。

> **入口條件的斷言只保護 `it()` 裡的量測，
> 而基線產生器跑在它們之前——它需要自己的那一份。**

### 一個說謊的宣告，會讓信任它的護欄一起說謊

**情境**：投影遺失護欄靠**宣告**決定要填幾個子節點（複數的填 3 個、單數的填 1 個）
——因為「這個接點放得下幾個」該由宣告說了算。

而三顆硬體宣告寫著 `initializer: 'expression'`（**單數**），**實際裝三個**
（液晶的建構參數）。於是護欄只填一個去驗，**兩邊都是 1，看起來完全健康**
——而那正是它要抓的那個 bug。

🔴 **它是靠「把已知答案退回去」才被發現的**：把液晶的修法退回，護欄**沒有報**。

> **一條抓不到已知答案的護欄，不知道自己在量什麼。**

**怎麼做**：護欄的判定如果依賴某份宣告，那份宣告本身就成了它的**盲點**。
🔴 而唯一測得出這種盲點的方法是**把已知的缺陷退回去，看它報不報**
——code review 看不到它（兩邊的程式碼都很合理）。

### 要判斷一個接點是不是死的，兩端都要查

**情境**：清償「投影遺失」的 13 筆時，我看到三顆元件的產生器**完全沒有讀**某個接點，
於是判定它們是死宣告，**把它們刪掉**。

🔴 **而宣告完整性護欄當場糾正**：「產出 [obj] 而宣告裡沒有」——**lift 真的會產出它們**。

> **只看產生器的話，「lift 產出而 generate 丟掉」會被誤判成「沒有人用」
> ——而那兩件事的處置完全相反：一個是刪宣告，一個是修產生器。**

而查完之後，那三個「死接點」其實是**兩個真的資料遺失**：

```
s.push_back('Z')   →   s.push_back('a')    🔴 字元被換成產生器的預設值
s.find("b", 5)     →   s.find("b")         🔴 起始位置不見（從第 0 個字元開始找）
```

⚠️ **兩個都不會報錯**：語法對、型別對、程式跑得完——只是結果不是他寫的那個。

**根因是同一個寫法**：

```ts
const ch = charNodes.length > 0 ? generateExpression(charNodes[0], ctx) : "'a'"
```

而 lift 那一側把它放進 `value`（共用的型別方法表用的槽名），不是 `char`。

> **一個「找不到就用預設值」的讀法，在槽名改變時不會報錯
> ——它會安靜地產出一個看起來合理的東西。**

**怎麼做**：讀接點時**找不到就丟錯**，不要用預設值。⚠️ 而預設值不是全錯——
`tone` 的第三個引數本來就可選；差別在**「可選」是宣告出來的，還是碰巧沒有**。

### 一個會在缺陷被修好時變紅的釘子，是唯一一種不會被遺忘的待辦

**情境**：`slot-roundtrip-first-week.test.ts` 有一根 `it.fails` 釘子
（`vector<int> v(10)` 的大小會消失），註解寫著「修好的那天它會紅，逼人來拔釘子」。

🟢 **2026-08-18 它真的紅了**——新蓋的投影遺失護欄掃出那三個接點沒有落點，
補上插槽之後，那根一年前埋的釘子自己跳出來。

而同一輪 `audit-defect-ledger` 的總數 45 → 44，也是它帶動的。

> **`it.todo` 宣告一個缺陷而沒有任何機構在看它還在不在；
> `it.fails` 有本體，所以缺陷消失的那一刻它會出聲。**

⚠️ 而它證明的不只是「釘子有用」——**是「護欄之間會互相觸發」**：
一條新護欄逼出的修法，讓另一條舊釘子到期。**那是知識庫在自己收斂。**

### 查一個設計「為什麼這樣做」，要看它的來源真相，不要看關於它的描述

**情境**：查四家競品（MakeCode／Open Roberta／Mind+／WebDuino）的積木分類。

前三次 `WebSearch` 問「設計理由」「分類清單」，拿回來的全是二手概括
——「MakeCode 把積木分成類別並用顏色編碼」這種**讀起來像答案而沒有資訊**的句子。

改成抓**它們自己的東西**之後，一次就到位：

```
makecode.microbit.org/reference     11 個分類 ＋ 每格的完整積木清單
gh api …/ext-oled12864/main.ts      六個積木、零個宣告、addObject 5 次
gh api …/webduino.js                196 顆積木身分，一眼看出每族都有 _new
```

🔴 **而最關鍵的那個發現只在原始碼裡**：Mind+ **沒有**宣告積木。
**任何一篇介紹 Mind+ 的文章都不會寫這件事**——它是一個「沒有的東西」，
而二手描述只描述有的東西。

> **二手描述描述「有什麼」；
> 而設計最重要的資訊常常是「沒有什麼」。**

**怎麼做**：查競品的設計時，優先序是
**原始碼 > 官方 reference 頁 > 官方文件散文 > 搜尋結果摘要**。

⚠️ 判準是「**這份資料能不能讓我數出數字**」——數不出來的就是二手的。
（「196 顆積木、addObject 5 次、11 個分類」數得出來；「用顏色編碼」數不出來。）

⚠️ 而它有一個**誠實的邊界**：拿不到一手資料時要說出來，不要用二手的補。
這一輪 mBlock 5 的擴充就拿不到（不在 GitHub 開源），而報告裡標明了那一欄是空的
——見 [draft/競品的積木與分類](draft/2026-08-18-競品的積木與分類.md)。

### 一個量測的效力，上限是它的樣本來自誰

**情境**：一個提案（腳位欄位改成跟著板子的下拉）要決定做不做，
而 draft 的出口條件寫著「**先量『腳位打錯』在語料裡發生過幾次**」。

量出來：**140 段、597 處腳位使用、四類錯誤 0 筆**。

⚠️ 而偵測器**驗過**（四類注入都會報、兩個對照組不亂報）——**0 是量到的，不是漏掉的**。

🔴 **而 0 答的不是那個問題**：那 140 段是**AI 生成的**。
LLM 寫 Arduino 會用教科書腳位（LED 接 13、PWM 接 9），**因為訓練資料就是那些**。

> **一份沒有學生寫過的語料，量不出學生的錯誤。**

**怎麼做**：量測跑出結果之後，除了問「這個數字可信嗎」（工具對不對），
還要問「**這份樣本回答得了我的問題嗎**」（來源對不對）。
🔴 而後者**不會**因為前者做得好而變好——一個完美的偵測器跑在錯的樣本上，
給出的是一個**看起來很乾淨的錯誤答案**。

⚠️ **判準**：寫下結論之前，把那句話補完整——
「**在【某某樣本】上，X 發生了 N 次**」。補不出前半句就是還不知道自己量了什麼。

**而處置不是丟掉量測**：那支探針留著當**回歸線**——真實樣本進來的那天數字會變，
**那才是訊號**。（見 [history/085](history/085-腳位下拉被自己的量測否決而語料答不了學生的問題.md)）

### 一個「可以有很多個」的接點，用一個去驗它，驗的是它退化成單數時的行為

**情境**：兩條護欄對 `cpp:var_declare` 的 `declarators` 給出相反答案。
查下去發現**兩條都在量一個不存在的東西**，而其中一半的原因是**只填了一個**。

填一個會從**兩個方向**出錯，而它們的症狀相反：

```
假陰性   積木上只放得下第一個 → 兩邊都是 1，看起來健康
         （液晶的建構參數：語義樹 3 個、積木 1 個插槽）
假陽性   一個宣告子本來就塌回普通宣告 → 報「declarators 不見了」
         （`int d;` 不是多變數宣告——而那是【對的】行為）
```

**怎麼做**：可變數量的接點，合成時填 **≥2**。
⚠️ 而「可變數量」要由**宣告**說（複數形／`max > 1`／型別指名一顆概念），不是猜。

### 一個餵錯型別而碰巧通過的測試，與一個真的在守著那件事的測試長得一模一樣

**情境**：`US2：int a, b, c 的其餘變數不得消失` 綠了很久。
而它餵的是 `cpp:var_ref`——**型別是錯的**（lift 真的產出的是 `cpp:var_declare` 自己），
只是它**碰巧有 `.name`**，於是渲染策略讀得到、測試就過了。

🔴 **那讓它守不住它宣稱要守的事**：真正的形狀壞掉時，這支測試不會紅。

> **合成節點永遠有「餵錯形狀而碰巧通過」的風險
> ——因為形狀是我決定的，不是系統決定的。**

**怎麼做**：驗投影／往返這類「形狀敏感」的東西時，**至少要有一條從真實程式碼出發**
——那一條的形狀由 `lift` 決定，餵不錯。
⚠️ 合成節點仍然有用（涵蓋得廣、跑得快），但**不能只有它**。

（根因與處置見 [history/086](history/086-一個說謊的宣告讓三個消費者各自錯一次.md)
——同一個說謊的宣告讓**三個**消費者各自錯一次，而方向都不同。）


### 一份只記「發生了什麼」的日誌，能否證假設，不能指出成因

2026-08-19 查「Cmd+Z 之後 `int x;` 跑到檔案最後」時，寫入紀錄證明了那一行是
積木面板寫回去的（鏡像 6 個非空白行 → 寫成 7 行），**然後就斷了**：
三個假設全部被實測否決，而症狀還在。

補一行「最近被判成使用者編輯的積木事件」之後，答案自己出現
（`create｜cpp_var_declare｜頂層 2 顆`）。

**要點**：儀器記錄**結果**時只能做排除法；記錄**觸發源**才能做歸因。
兩者的成本差不多，而診斷力差一個量級。

**如何應用**：任何「某個動作把資料寫壞了」的儀器，除了記「寫了什麼」，
一定要記「**是什麼觸發了這次寫入**」。⚠️ 而「一則都沒有」本身也是答案。

→ 同族的四條收斂在 [[執行機構]]「第四種工具：**儀器**」——**一個儀器答得出的問題，等於它記了哪些維度**。


### 一個拿不到的儀器，與一個不存在的儀器，在出事那天是同一件事

`semorphe.showDiagnostics` 在 `extension.ts` 裡 `registerCommand` 了，
**而沒有宣告在 manifest 的 `commands`**——所以它不在指令面板上。
於是使用者回報同步異常時，唯一的現場紀錄沒有人拿得到。

**要點**：這是「可拿性」護欄量的那件事，換到另一個宿主上又發生一次
——**護欄只看它掃的那個範圍**。

**如何應用**：新增任何**給人用**的入口（指令、按鈕、選單）時，
問一句「使用者實際上要按哪裡才會到它」，而不是「它註冊了沒有」。

→ 同族的四條收斂在 [[執行機構]]「第四種工具：**儀器**」——**一個儀器答得出的問題，等於它記了哪些維度**。


### 查一個 bug 時抓到的副產品，不要當成成因

同一輪查出 `busUpdateInProgress` 從來沒有為真過（Blockly 非同步發事件，
旗標同步開關）。那是真的缺陷，**而它不是那個 bug 的成因**
——真正在擋的是 `setState` 裡的 `Events.disable()`，所以它死得無害。

**要點**：一個在查案途中撿到的真缺陷，會讓人很想收工
——因為它**看起來就是**那個解釋。

**如何應用**：宣告成因之前，**把修法退回去，看護欄會不會紅**。
這一輪就是靠這一步發現第一版護欄對舊程式碼是綠的，才沒有結錯案。

### 一份看不出版本的診斷，答不出「你測的是哪一版」

2026-08-19 連續三張診斷截圖長得幾乎一樣，而**「修法沒載到」與「修法沒用」
在畫面上完全同形**——於是無法判斷下一步該修程式還是該叫使用者重開視窗。

⚠️ 同一族的坑 `tools/install-ide.mjs` 的檔頭已經記過**兩次**（Theia 的
解壓快取以名字為鍵、VSCode 的 `contributes` 以 (id, version) 為鍵）
——**而那兩次治的是「裝進去的是舊的」，這一次是「看不出裝的是哪一個」。**

**要點**：一個儀器如果不報告**它自己是哪一版**，它的每一次讀數都少了一個維度。

**如何應用**：任何跨處理程序／需要重載才生效的診斷輸出，**第一行印版本**。

→ 同族的四條收斂在 [[執行機構]]「第四種工具：**儀器**」——**一個儀器答得出的問題，等於它記了哪些維度**。


### 兩份各自正確的日誌，沒有共同的順序就講不出因果

2026-08-19 的同步調查卡在這裡：積木事件與檔案寫入各有一份環形紀錄，
**而它們沒有共同的時間軸**——那個 `create` 是在那次寫入之前還是之後？
兩份表都答不出來，而那正是判斷因果的**唯一**問題。

🔴 而最關鍵的那一則本來**誰都沒有記**：「**重畫**發生在哪裡」。
一則積木事件屬於重畫前還是重畫後的世界，決定了它是不是合法的使用者動作。

**要點**：分散的日誌各自完整，合起來仍然可能**比單獨一份講的還少**
——因為讀的人會自己編一個順序，而那個順序是猜的。

**如何應用**：一個系統有兩個以上的地方會「做事」時，日誌要有**一條共同的軸**
（序號 ＋ 距上一則的間隔），而且**狀態轉換本身要是軸上的一則**，不能只記動作。

→ 同族的四條收斂在 [[執行機構]]「第四種工具：**儀器**」——**一個儀器答得出的問題，等於它記了哪些維度**。


### 一個「還沒被載入過」的視圖，與一個「內容真的是空的」視圖，在讀取端同形

2026-08-19 的時間軸抓到：文件在 +0ms 到達，積木的第一次重畫在 +941ms，
**而自動同步在 +125ms 就拿那個空的工作區去寫檔案**——算出「整份檔案變成 0 行」。
是安全網擋下來的，⚠️ **而安全網只擋「少一半以上」，少一點的擋不住**。

**要點**：兩種空要分得出來，而**只有視圖自己知道差別**
——讀取端拿到的都是「零顆積木」。

**如何應用**：任何「從視圖讀出狀態再寫回真實」的路徑，
視圖要能回答「**我被填過了嗎**」，而不是只回答「我現在有什麼」。
這與「顯式的空與遺漏的空要分得出來」（`skipPaths` 那條）是同一條性質。

### 用【時間間隔】分辨「人做的」與「程式做的」

同一條時間軸上：

```
 5｜ +816ms｜🔄 重畫
 6｜+2893ms｜▫️ create cpp_var_declare     ← 2.9 秒後
 7｜   +3ms｜✏️ 寫入
 8｜   +1ms｜▫️ move
 9｜ +731ms｜▫️ move                        ← 0.7 秒後
```

程式造成的連鎖是 **1–3 ms**；人的動作之間是 **幾百到幾千 ms**。
**那個間隔本身就是證據**，而它不需要任何額外的儀器。

**如何應用**：診斷日誌記「距上一則幾毫秒」，成本近乎零，
而它能回答「這是連鎖反應還是兩件獨立的事」——那常常是唯一要問的問題。

### 要擋「稍後才發生」的事，蓋章要蓋在它被造出來的當下——事後清掃分不出對象

2026-08-19 修「Cmd+Z 讓積木憑空長回來」時，第一版寫的是**延後清空**復原堆疊
（`setTimeout` 之後再 `clearUndo()`）。**護欄當場紅了**：那一刻使用者若剛好
動了積木，他的動作會被一起清掉——**清空分不出那一項是誰放的**。

改成把「不記復原」的窗口**延到下一幀之後才關**：`recordUndo` 是事件**創建時**
蓋的章，所以延後關窗只影響那一幀內被造出來的事件，而人在 16 ms 內
完成不了一個手勢。

**要點**：同一個目的有兩種寫法——**在來源蓋章**與**在事後篩選**。
後者只有在「篩得出來」時才成立，而多數情況篩不出來。

**如何應用**：想寫「稍後清掉」時先問一句
「**清的時候我還分得出哪一項是我要清的嗎**」。分不出 → 回到來源。

⚠️ 而這條有一個必要的配對護欄：**窗口關掉之後使用者的動作必須重新可復原**
——沒有它，「永遠不記復原」也能讓正向那支通過，而那等於把功能整個關掉。

### 一個「什麼都沒收到」的紀錄，答不出「有沒有人送」

2026-08-19 第五輪：面板那側的時間軸顯示「重畫之後到下一次文件之間，
一份文件都沒收到」。⚠️ 而那有**三種可能，在面板那側完全同形**：

```
① 使用者根本沒改文件
② 改了，而回音守衛判成【回音】吞掉了
③ 改了、送了，而訊息沒送到
```

**三種的修法完全不同**，而缺的那一半日誌在**另一個處理程序**裡。

**要點**：儀器裝在接收端時，「沒有紀錄」是**歧義**不是資訊
——它同時代表「沒發生」與「發生了而我不知道」。

**如何應用**：跨處理程序的同步問題，兩端都要有時間軸，
而**判定要記在判定之前**（被吞掉的那些否則一則都不會出現）。

→ 同族的四條收斂在 [[執行機構]]「第四種工具：**儀器**」——**一個儀器答得出的問題，等於它記了哪些維度**。


### 一個復原步驟的邊界，要落在「使用者認為自己做完一件事」的地方

2026-08-19，兩份時間軸拼起來才看到：**一次拖曳產生兩次檔案寫入**
（放下之前一次、接上之後一次），而它們各自是編輯器裡的一個復原項。

於是使用者按 Cmd+Z 還原到的是「拖到一半」的中間狀態——同一個宣告
同時出現在 `loop` 裡和最外層，**而他從來沒看過那個狀態**。

⚠️ **同步本身完全正確**：回音正確擋掉、復原正確送出、積木正確重畫。
壞的是**寫入的時機**——而那讓前面四輪都在錯的地方找。

> **把過程中的每一幀都存成一步，等於逼使用者倒著走過自己沒有走過的路。**

**如何應用**：任何「視圖變動 → 寫進共享文件」的自動同步，
先問「**使用者認為這一步做完了嗎**」。拖曳中、輸入到一半、多鍵組合的中途
——那些都不是一步。

🔴 而配對的護欄不可省：**放下之後必須補寫**。
沒有它，「永遠不寫」也能讓「拖曳中不得寫」通過，而那是更糟的 bug。

### 一個「重寫成一樣的內容」的寫入，仍然改變了歷史

2026-08-19：把空行還原之後，沒有語義變動時產生的文字與原檔**逐字相同**，
於是最小編輯算出「沒有差異」，**整個寫入不會發生**。

⚠️ 在此之前每一次同步都在重寫檔案，而**沒有人把它當成缺陷**
——「內容一樣」看起來無害。而它是那一整串同步災難的燃料：
每一次重寫都是使用者編輯器裡的一個復原項。

**要點**：寫入的成本不只是「內容變了沒有」。它還會動到**歷史、游標、
外部工具的檔案監看**——那些在「內容一樣」的時候仍然全部發生。

**如何應用**：任何自動寫回，**先問「這一次真的有差異嗎」並在沒有時徹底不寫**，
而不是「寫了也沒差」。

### 「使用者做完一件事了嗎」不是一個布林，是一串**互不相干**的狀態

2026-08-19 同一個病修了**兩次**：

```
拖曳中     一次拖曳兩次寫入 → ⌘Z 回到「拖到一半」（積木在最外層）
改欄位中   x 改成 y 兩次寫入 → ⌘Z 第一次得到 `int ;`
```

第一次修完以為結案了，⚠️ **而「中途」有好幾種，它們在程式裡沒有共同的旗標**：
`isDragging()`、`WidgetDiv.isVisible()`（文字欄位）、`DropDownDiv.isVisible()`
（下拉）——**三個互不相干的容器**。

**要點**：治「中間狀態外洩」時，先把**所有**的「中途」列出來再動手。
只修撞到的那一個，剩下的會以**同一個症狀、不同的入口**回來。

🔴 而第二次還逼出一個第一次沒有的需求：**拖曳結束後通常還會來一則事件**
（順便把累積的寫掉），**而欄位編輯沒有這個保證**——最後一次值變更可能在
編輯器關閉之前就發出。於是需要有人去**看「忙完了沒有」**，
否則使用者的修改**永遠不進檔案**（比多寫一次糟得多）。

### 一個沒有對照組的驗收條文，量不出「是誰造成的」

驗收清單第 1 條原本寫：「按 `⌘Z` → 🟢 一次還原整個改動；🔴 壞的是要按好幾次」。

2026-08-19 實測「要按兩次」，於是被判成缺陷。⚠️ **而使用者自己去試才發現
Arduino IDE 手打同樣的改動也要按兩次**——那是**宿主自己的粒度**，不是我們寫的。

> **一條沒有對照組的驗收，量到的是「宿主 ＋ 我們」的總和，
> 而它的失敗訊息會指著我們。**

**如何應用**：任何「在別人的宿主裡按了之後看到什麼」的驗收，
條文要先要求**量一次基準線**（同樣的事情用宿主原生的方式做一遍），
判準寫成「**不差於基準線**」，不要寫成絕對值。

🟢 而這一次途中仍然抓到一個真缺陷（欄位清空的瞬間被寫進檔案 → `int ;`）
——**它與宿主粒度無關，獨立成立**。⚠️ 判準錯了不代表期間的發現都是假的，
**兩件事要分開結案**。

### 一條沒有人看得懂的驗收，與一條沒有人按的驗收，結果完全一樣

2026-08-19，spec 140 的八條驗收裡七條被使用者按過並回報，
**剩下的那一條沒被按的原因不是麻煩，是看不懂**——使用者逐字：
「5b 我不知道是什麼意思」。

原條文只寫了**怎麼按**（改哪個設定），沒寫**這在測什麼、為什麼要有它**。

**要點**：驗收條文的讀者是**未來的人**（含使用者自己），
而他要決定「值不值得花這三分鐘」——那個決定需要「為什麼」，不只是「怎麼做」。

**如何應用**：每一條人工驗收至少要有三段——**這在測什麼**（一句話的動機）、
**怎麼按**、**好與壞長什麼樣**。⚠️ 只有後兩段的話，它會安靜地留在清單上不被按。

### 一個「依語言」的設定，能不能分辨兩種檔案，是**宿主**決定的

2026-08-19：驗收條文寫著「`.ino` 與 `.cpp` 可以用不同的工具箱」，
而使用者一句話拆掉了它的前提——「`.ino` 在 VSCode 也會顯示 C++」。

```
Arduino IDE (Theia)   .ino → arduino     🟢 覆寫分得出來
純 VSCode             .ino → cpp         🔴 兩者同一個 id，分不出
```

我們**沒有註冊自己的語言**，所以 `.ino` 拿到什麼 id 完全看宿主。
機制（`language-overridable` ＋ `{uri, languageId}` 範圍）是對的，
**而它能不能達成那個目的，不在我們手上。**

**要點**：接上宿主的機制 ≠ 得到那個機制承諾的效果
——中間還隔著「宿主怎麼分類使用者的檔案」，而那**因宿主而異**。

**如何應用**：任何「依 X 給不同設定」的功能，驗收條文要分開寫
**機制有沒有接上**（我們的責任）與**X 分得出來嗎**（宿主的），
否則一個宿主上的合理結果會在另一個宿主上被判成缺陷。

### 一份要跟著另一份走的清單，遲早會停在它被寫下的那一天

2026-08-19 蓋能力供給完備性護欄時，我在它裡面**手寫了四個目標的 import**
（產品的註冊在 `app.ts`，測試拿不到）。加完三塊板子之後，
護欄仍然回報「有限縮的目標：**0/4**」——**它看不到新目標，
於是它守的那件事悄悄變成空的**，而它是綠的。

**要點**：一條護欄如果自己維護一份「應該與別處一致」的清單，
那份清單就是它的**盲點**——而盲點的症狀是**綠**。

**如何應用**：判準與 `component/registry.ts` 同一條——
**這個東西有沒有人要「查」它？** 沒有（一整批資料）→ `import.meta.glob` 直讀，
不要列清單。⚠️ 而列清單時如果非列不可，要問「新增一筆的人**憑什麼**會想到來改我」。

### 一條護欄的判準有時不必改——要改的是我們對它在量什麼的理解

spec 142 的規格寫著「可拿性護欄的判準要放寬成『在至少一個目標下拿得到』」。
Phase 0 實測：它算「拿得到」是看**分類定義**，**與目標無關**
——原封不動就是綠的。它今天量的**其實已經是**那句話，
只是當時只有一個目標，所以那個差別看不出來。

🔴 而真正的缺口在別的地方：宣告一個**沒有任何目標提供**的能力，
那顆元件在所有板子上都拿不到，**而可拿性全綠**。

**要點**：新增一個維度時，先問既有護欄**從哪裡取資料**
——它可能早就涵蓋了新情況，也可能在新維度的**視野外**多出一種同樣的缺陷。
兩者都不是「放寬判準」。

**如何應用**：改一條既有護欄之前，**先讀它 import 什麼**。
⚠️ 而規格裡寫「某條護欄要改」的那種需求，**要當成假設而不是結論**——
spec 142 的 FR-008 就是這樣被推翻的。

### 一個不認得慣例的格式稽核，會把風格報成腐爛

2026-08-19 的健檢說 `experience.md`「**四種格式並存**」，並提議把 71 條
「沒有 `**要點**`」的補齊。⚠️ **查了才發現那個分類是我的分類器造出來的**：

```
標題                      ← 教訓本身（實測：185/191 條的標題就是一句完整的教訓）
- **實際發生（日期）**：   ← 證據
```

那 53 條用的是**同一個形狀的舊世代**，不是散文。機械補一行 `**要點**：`
只會把標題那句話**再講一次**——而憲法第一條擋這個。

**要點**：一個稽核如果只認得**最新的**慣例，它會把**舊的慣例**報成缺陷
——而那份報告讀起來與真的腐爛完全一樣。

**如何應用**：宣稱「格式不一致」之前，**打開幾條看它們實際在做什麼**。
🔴 判準是 knowie 自己的那條：**這一欄如果拿掉，會有人漏掉什麼嗎？**
標題已經講完的東西，多一個標籤不是結構，是重複。

⚠️ 而量出來的**真缺口**是另一個：127 條沒有「如何應用」。
**那一條沒有補**——替一段自己沒有親歷的教訓編造「如何應用」，
會把記憶庫變成一份看起來完整而不可信的東西。
