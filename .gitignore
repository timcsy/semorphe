node_modules/
dist/
build/
*.log
.DS_Store
Thumbs.db
*.tmp
*.swp
*.vsix
# ⚠️ `vscode-ext/` 的兩行在 2026-08-17 移除——那個目錄是 2026-03 原型退休
# （`3d6768b`）之後留在硬碟上的產物殘骸，零追蹤檔、26 MB。
# 留著只有誤導價值：`history/069`§四 逐字「一個原型……**它會偽裝成
# 『已經有基礎』**」。新的擴充在 `src/vscode/`，產物在 `build/`。

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Blockly 的圖示與音效——由 vite.config.ts 從 node_modules 複製（產物，不是原始碼）
public/blockly-media/

# 出貨產物與範例的建置輸出——由 `npm run build:sdk` 與範例自己的 build.mjs 產生
dist-sdk/
examples/**/dist/
