{
  "_meta": {
    "guard": "dropdown-domain",
    "measuredAt": "2026-08-24",
    "rule": "值域【在現實中開放】的欄位（型別、標頭檔、函式名、裝置…），使用者要造得出清單外的值。值域封閉的（運算子、public/private、begin/end）不在此列——那些下拉是限制，正確。",
    "note": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。\n🔴 **這 3 筆都不是宣告錯，是【命令式定義蓋過宣告】**（2026-08-24 實測）：`cpp_increment`／`cpp_increment_expression`／`cpp_var_assign_compound_expression` 的宣告已經改成跟著工作區長的下拉，而**使用者看到的仍是命令式那份**。它會在那三顆積木退場的那天下降（vision 階段 7「那 6 筆命令式積木」）。\n⚠️ 判定住在 `tests/assets/dropdown-domain-decisions.json`——**每一筆都要有理由**，而沒有判定的下拉是硬性零。\n⚠️ **第一版的量測是壞的**：它印出 46（＝全部），因為真積木沒被定義進 Blockly。修法是走產品那條註冊路，而入口條件多了一句「`Blockly.Blocks['cpp_var_declare']` 要在」。\n🟢 **2026-08-25：3 → 2**——`cpp_increment` 的命令式定義退場，它那顆寫死選項的 `OP`／`POSITION` 下拉換成宣告式的。**這是往好的方向。**\n🎯 2026-08-25：下拉總數 77 → 76——**語料真的變小了，而它是這條護欄樂見的**：\n   `python_var_assign_compound.NAME`（一顆變數下拉）換成了 `TARGET` 接點。\n   🔴 那顆下拉列的是變數清單，而它裝的是左值（`nums[0]`／`o.x`／`a.b.c`）\n   ——學生點一下就把成員存取毀掉。見路線圖「左值是接點，不是字串」。\n   ⚠️ 一顆欄位**變成接點**與一顆欄位**被刪掉**在這個數字上長得一樣，\n     所以下調時要問「它去哪了」——這一次的答案是 `TARGET`。\n🎯 2026-08-25：76 → 74——`cpp_var_assign_compound`（含運算式形態）的 `NAME` 變數下拉\n   換成 `TARGET` 接點。⚠️ 同一個判準：欄位**變成接點**與欄位**被刪掉**在這個數字上\n   長得一樣，所以下調時要答「它去哪了」——這一次是 `TARGET`。\n🎯 2026-08-25：開放而不保值 2 → 1。⚠️ **而它不是「開放了一顆下拉」**——\n   是那顆下拉整個**變成接點**（`cpp_var_assign_compound` 的左值）。\n   一個「值域開放」的問題，最好的答案有時候是「這一格根本不該是欄位」。\n🎯 2026-08-25：74 → 72。`cpp_increment`（含運算式形態）的 `NAME` 變數下拉換成 `TARGET` 接點（2026-08-25「左值是接點，不是字串」第三筆：`cpp:increment`）。\n🎯 2026-08-25：開放而不保值 1 → **0**。⚠️ 而它一樣不是「開放了一顆下拉」——\n   `cpp_increment` 的 `NAME` 整個變成 `TARGET` 接點。\n   🔴 **這個 0 現在該被問一次：這條護欄還有東西可量嗎？**\n     路線圖那一項寫著「那 3 筆都是命令式定義蓋過宣告」——三筆都清了。\n🎯 2026-08-25：72 → 71。`cpp_var_assign` 的 `NAME` 變數下拉換成 `TARGET` 接點（第四筆：`cpp:var_assign`，2026-08-25）。\n🎯 2026-08-26：71 → 70。`cpp_array_at` 的 `NAME` 變數下拉換成 `OBJ` 接點（2026-08-26「左值必須被結構表達」，見 history/157）。\n🎯 2026-08-26：70 → 72。`cpp_forward_decl` 的兩顆型別下拉**第一次進得了這個母體**\n   ——它們本來活在命令式定義裡（`createOpenDropdown`），而這條護欄只看得見宣告。\n   🔴 **所以「總數變大」這一次代表【看得見的變多了】，不是【下拉變多了】**：\n     那兩顆一直都在畫面上，只是沒有人在數它們。\n🎯 2026-08-26：72 → 73。`cpp_array_declare.TYPE` **第一次進得了這個母體**\n   ——它本來活在命令式定義裡（靜態五選一），而這條護欄只看得見宣告。\n   🔴 **總數變大代表【看得見的變多了】，不是下拉變多了**；而它同時從\n     「靜態五選一」變成開放下拉（連 `string`／`bool` 本來都選不到）。\n🎯 2026-08-26：73 → 74。`cpp_array_2d_declare.TYPE` 同上（本來活在命令式定義裡）。\n🎯 2026-08-26：74 → 75。`cpp_vector_declare.TYPE` 同上。\n   🟢 **五顆宣告類積木從此共用同一份型別清單**（`cpp_var_types`）\n     ——在此之前它們是 7／5／3／6 個各寫各的，而那不是設計。"
  },
  "下拉總數": 75,
  "開放而不保值": 0
}
