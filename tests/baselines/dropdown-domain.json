{
  "_meta": {
    "guard": "dropdown-domain",
    "measuredAt": "2026-08-24",
    "rule": "值域【在現實中開放】的欄位（型別、標頭檔、函式名、裝置…），使用者要造得出清單外的值。值域封閉的（運算子、public/private、begin/end）不在此列——那些下拉是限制，正確。",
    "note": "📈 **2026-09-19（第 197 刀：位置的相鄰一格 `cpp:pointer_step`）：新增一顆身分。**\n  `prev(it)`／`next(it)`——語料 `prev(` 10 處 / 8 支，每一處都是 `prev(X.end())`。\n  方向是**屬性**不是身分（元件代數 250）。\n  方向那一格判為 `closed`：一個位置只有兩個相鄰方向，而 C++ 的 `<iterator>`\n  也只給了 `prev`／`next` 這兩支。🟢 「開放而不保值」仍然是 0。\n\n數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。\n🔴 **這 3 筆都不是宣告錯，是【命令式定義蓋過宣告】**（2026-08-24 實測）：`cpp_increment`／`cpp_increment_expression`／`cpp_var_assign_compound_expression` 的宣告已經改成跟著工作區長的下拉，而**使用者看到的仍是命令式那份**。它會在那三顆積木退場的那天下降（vision 階段 7「那 6 筆命令式積木」）。\n⚠️ 判定住在 `tests/assets/dropdown-domain-decisions.json`——**每一筆都要有理由**，而沒有判定的下拉是硬性零。\n⚠️ **第一版的量測是壞的**：它印出 46（＝全部），因為真積木沒被定義進 Blockly。修法是走產品那條註冊路，而入口條件多了一句「`Blockly.Blocks['cpp_var_declare']` 要在」。\n🟢 **2026-08-25：3 → 2**——`cpp_increment` 的命令式定義退場，它那顆寫死選項的 `OP`／`POSITION` 下拉換成宣告式的。**這是往好的方向。**\n🎯 2026-08-25：下拉總數 77 → 76——**語料真的變小了，而它是這條護欄樂見的**：\n   `python_var_assign_compound.NAME`（一顆變數下拉）換成了 `TARGET` 接點。\n   🔴 那顆下拉列的是變數清單，而它裝的是左值（`nums[0]`／`o.x`／`a.b.c`）\n   ——學生點一下就把成員存取毀掉。見路線圖「左值是接點，不是字串」。\n   ⚠️ 一顆欄位**變成接點**與一顆欄位**被刪掉**在這個數字上長得一樣，\n     所以下調時要問「它去哪了」——這一次的答案是 `TARGET`。\n🎯 2026-08-25：76 → 74——`cpp_var_assign_compound`（含運算式形態）的 `NAME` 變數下拉\n   換成 `TARGET` 接點。⚠️ 同一個判準：欄位**變成接點**與欄位**被刪掉**在這個數字上\n   長得一樣，所以下調時要答「它去哪了」——這一次是 `TARGET`。\n🎯 2026-08-25：開放而不保值 2 → 1。⚠️ **而它不是「開放了一顆下拉」**——\n   是那顆下拉整個**變成接點**（`cpp_var_assign_compound` 的左值）。\n   一個「值域開放」的問題，最好的答案有時候是「這一格根本不該是欄位」。\n🎯 2026-08-25：74 → 72。`cpp_increment`（含運算式形態）的 `NAME` 變數下拉換成 `TARGET` 接點（2026-08-25「左值是接點，不是字串」第三筆：`cpp:increment`）。\n🎯 2026-08-25：開放而不保值 1 → **0**。⚠️ 而它一樣不是「開放了一顆下拉」——\n   `cpp_increment` 的 `NAME` 整個變成 `TARGET` 接點。\n   🔴 **這個 0 現在該被問一次：這條護欄還有東西可量嗎？**\n     路線圖那一項寫著「那 3 筆都是命令式定義蓋過宣告」——三筆都清了。\n🎯 2026-08-25：72 → 71。`cpp_var_assign` 的 `NAME` 變數下拉換成 `TARGET` 接點（第四筆：`cpp:var_assign`，2026-08-25）。\n🎯 2026-08-26：71 → 70。`cpp_array_at` 的 `NAME` 變數下拉換成 `OBJ` 接點（2026-08-26「左值必須被結構表達」，見 history/157）。\n🎯 2026-08-26：70 → 72。`cpp_forward_decl` 的兩顆型別下拉**第一次進得了這個母體**\n   ——它們本來活在命令式定義裡（`createOpenDropdown`），而這條護欄只看得見宣告。\n   🔴 **所以「總數變大」這一次代表【看得見的變多了】，不是【下拉變多了】**：\n     那兩顆一直都在畫面上，只是沒有人在數它們。\n🎯 2026-08-26：72 → 73。`cpp_array_declare.TYPE` **第一次進得了這個母體**\n   ——它本來活在命令式定義裡（靜態五選一），而這條護欄只看得見宣告。\n   🔴 **總數變大代表【看得見的變多了】，不是下拉變多了**；而它同時從\n     「靜態五選一」變成開放下拉（連 `string`／`bool` 本來都選不到）。\n🎯 2026-08-26：73 → 74。`cpp_array_2d_declare.TYPE` 同上（本來活在命令式定義裡）。\n🎯 2026-08-26：74 → 75。`cpp_vector_declare.TYPE` 同上。\n   🟢 **五顆宣告類積木從此共用同一份型別清單**（`cpp_var_types`）\n     ——在此之前它們是 7／5／3／6 個各寫各的，而那不是設計。\n🎯 2026-08-26：75 → 76。`cpp_var_declare.TYPE`——**命令式積木退場計畫結案**——最後一顆 `cpp_var_declare` 連同三顆齒輪子積木\n  （`u_var_declare_*`）退場，`block-registrar.ts` 的 `Blockly.Blocks[...] = {` **歸零**。\n🎯 2026-09-17：76 → 78。`cpp_set_declare.UNIQUE` 與 `cpp_map_declare.ORDERED`——**兩顆新下拉，而它們各自把一個既有的缺陷變成一個可選的值**：在此之前 `multiset` 與 `unordered_map` 根本沒有被登錄成容器樣板，於是前者被去重成一半、後者被當成陣列索引而丟 `INDEX_OUT_OF_RANGE`。\n   🔴 **兩筆都判 `closed`，而理由是同一句**：那兩個值各自對應一個 C++ 型別名（set／multiset、map／unordered_map），清單外沒有第三個容器。\n\n📈 **2026-09-21（第 217 刀）：下拉總數 90 → 91——輸入量上調。**\n  第一百二十六條護欄那八顆補了**七個運算式形態**（`form: {axis:'role', value:'expression'}`）\n  ——宣告早就說得出那個位置，而形態沒做 ⟹ 渲染退成灰色逃生艙（實測過）。\n"
  },
  "下拉總數": 91,
  "開放而不保值": 0,
  "note": "📈 **2026-09-17（迭代器一族，管線 180）：語料變大，缺陷那幾格未動。**\n  新增 `cpp:container_find`（三個方法名對同一顆身分，判準進屬性）\n  ＋ 既有那顆取端點的元件多認反向的兩端。\n\n  🔴 判定 `closed`：三個判準各自對應一個 C++ 的方法名，清單外沒有第四種找法。\n  ⚠️ 而既有那顆取端點的元件**多了兩個值而不是多一顆下拉**——同一個軸。\n\n\n\n📈 **2026-09-18（`cpp:deque_declare`，探索報告的 P1）：＋1 顆元件。**\n  `deque` 在此之前**沒有主人**，於是 `deque<pair<int,int>> q;` 掉進一般的變數宣告，\n  `type` 裝著一整串 `deque<pair<int,int>>`——那個容器因此沒有 `elemType`，\n  而 `q[0].first` 說「（不是一個結構）」。語料 **22 支**用 deque。\n  🔴 這件事早就寫在 `cpp:vector_push` 的執行器註解裡等人來修。\n\n\n📈 **2026-09-18（`cpp:var_declare_sequence` ＋ 走訪的容器換成接點）**：\n  新增一顆元件（結構化繫結，語料 16 處／13 支），並把範圍 for 的容器\n  從欄位換成接點（`d2[pt]`／`m[k]` 是運算式，語料 3 支）。\n\n\n📈 **2026-09-18（模糊測試第二輪，容器）：新增一個 17 支的回歸網。**\n  資訊隔離的盲測 10 支，第一輪 **1/10**——而那是在前一天同一批元件走完\n  一整條管線、全套 7117 綠之後量的。\n  > **資訊隔離量到的不是「我做得對不對」，是「我想不想得到」。**\n\n📈 **2026-09-18（範圍那一族：兩端從字串屬性換成接點，管線 188）：語料變大，缺陷那幾格未動。**\n  10 顆範圍演算法的 `begin`／`end`（＋前綴和的 `dest`）從**字串屬性**換成**接點**，\n  ＋ 三顆新元件（`cpp:range_find`／`cpp:range_unique`／`cpp:range_remove`）\n  ＋ `cpp:container_iter` 多一個 `call` 屬性（`begin(a)` 這個自由函式形式）\n  ＋ 27 顆宣告式元件補上 `forLoopPart`（同一個病的第三次，這次整族一起補並加了一條護欄）。\n  🔴 而拆掉那個 regex 之後發現**結構早就在了**：容器與位置在這個直譯器裡是同一個形狀\n  （`interpreter/pointer.ts` 的 `isCellPointer`），`A+n` 的位置算術也早就有人做。\n  > **那條 regex 解析的東西，執行期早就會算了——它不是在補一個缺口，它是在繞過一個已經存在的機制。**\n\n📈 **2026-09-19（I/O 操縱子三顆，管線 189）：語料變大，缺陷那幾格未動。**\n  `cpp:io_flush`（`cout << flush`）· `cpp:io_mode`（`setw`／`setprecision`／`setfill`）·\n  `cpp:io_mode_number`（`fixed`／`scientific`），\n  ＋ 串流狀態（`WeakMap` 以 `ctx.io` 為鍵，**一次執行一份**）\n  ＋ `interpreter/decimal.ts`（十進位的四捨五入，收尾照 C 的**逢五取偶**）。\n  🔴 而命名是**詞彙表逼出來的**：第一版叫 `stream_*`，三個字都不在表裡；\n  換成既有詞（`io` ＋ `mode` ＋ `number`）之後名字反而對得上同族\n  （`cpp:io_sync`／`cpp:io_tie` 早就在那裡）。**只補了一個新詞：`flush`。**\n\n📈 **2026-09-19（第 199 刀：一排位元 `bitset`）：新增兩顆身分。**\n  `cpp:bits_declare`（一排固定長度的位元）與 `cpp:bits_fill`（整排歸零／設為 1／反轉），\n  ＋ 既有那顆「數 1 的個數」多一個**寫法**欄位（內建函式／一排位元）。\n  🔴 語料 `bitset<` **4 處 / 3 支**，而三支**全部**只缺這一族——\n  逐支讀過全文，其餘用到的東西今天都支援。\n  🟢 而索引的讀與寫**沒有新增任何機制**：一排位元在執行期就是 `type: array`，\n  於是既有那顆「取第幾格」連左值都免費沿用。\n\n📈 **2026-09-20（第 199 刀階段四：資訊隔離盲測）：再新增兩顆身分。**\n  `cpp:bits_is`（整排的三個問句）與 `cpp:bits_as`（整排換成整數／字串），\n  ＋ 既有那顆「整排改值」多一個**常態留空的位置**接點。\n  🔴 **三顆都是探索報告以「語料 0 處」拒絕過的**，而盲測十支裡\n  `to_ulong` 用了 4 支、帶位置的 `set(i)` 多支、`cout << bs` 1 支。\n  > **「語料 0 處」量到的是【這批語料的人怎麼寫】，\n  > 不是【這個寫法有多常見】——而它在兩刀內錯了四次。**"
}
