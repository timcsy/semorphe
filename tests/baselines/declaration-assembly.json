{
  "_meta": {
    "note": "📈 **2026-09-19（轉型的三種寫法 ＋ 除以零不是錯誤 ＋ 八進位 ＋ 二維扁平初值）：\n  輸入量上調，缺陷格未動。**\n  新增 `tests/probes/studycpp-has-blocks.test.ts`（語料用到的元件都有積木嗎）\n  ＋ `interpreter-matches-compiler` 的 15 條最小重現。\n  🔴 這一刀讓語料的**「內容真的不同」第一次下降**（7 → 5）——\n  那一欄從 2026-09-16 補上第五個面向以來，八刀都沒有動過。\n\n📈 **2026-09-19（解析器解錯的兩種形狀）：輸入量上調，缺陷格未動。**\n  新增 `core/lift/ast-repairs.ts`（「換一棵樹」的登記處，與辨識後處理同形狀）\n  ＋ `languages/cpp/lang/misparse.ts`（`!K--` 與 `a<b && c>-d` 兩條重寫）\n  ＋ `tests/unit/languages/cpp/misparse.test.ts`。\n  🔴 它們補的是**唯一一批「往下追的每一層都是對的」的缺陷**——\n  tree-sitter-cpp 自己解錯，而 lift 只問「這棵樹是什麼意思」。\n  🟢 跨層相依未動（5）：新檔住在 `core/lift/`，語言套件推、核心讀。\n\n📈 **2026-09-19（把東西移出缺陷帳要外部權威）：輸入量上調一個檔，缺陷格未動。**\n  新增第一百二十二條護欄 `audit-sanitizer-verdict`——語料的「解譯器出錯」那一欄裡，\n  有一類是**那份測資讓那支程式走進未定義行為**（`vector<S> A[n]` 之後用輸入來的索引），\n  而把它移出缺陷帳的資格不在我，在 UBSan／ASan。那支判定自己要有錨點。\n  🔴 而它第一版漏掉一半：`execFileSync` 只在「它失敗了」那一路讀 stderr，\n  而 UBSan 預設**讓程式繼續跑並正常退出**——純 UBSan 那半完全看不見。\n  > **一個只在失敗那一路讀 stderr 的偵測器，對「它成功了而且順便印出違規」保持沉默。**\n\n各自列宣告來源、而沒把膠囊算進去的測試檔數。\n處方：改用 `allCppComponents()` / `allCppProjections()`（它們含膠囊）。\n⚠️ 這個數字只准下降，而**下降必須是因為改用組裝函式**，不是因為那支測試被刪掉了。\n\n📈 **2026-09-20（第 202 刀：參照綁到一個算出來的位置）：輸入量上調，缺陷格未動。**\n  新增 `tests/integration/interpreter-cross-run-leak.test.ts`（上一支程式不得影響下一支）\n  ＋ `interpreter-matches-compiler` 的 16 條最小重現 ＋ `tests/unit/scope.test.ts` 的六條。\n  🔴 修的是**兩個綁定點都只認得裸的變數名**，而「否則」那一條是安靜地改用傳值：\n  `g(a[1])`／`g(v[1])`／`g(A[0])`／`g(s.a)`／`int &r = a[1]` **五種形狀全中**，\n  五種都是「跑得完的錯答案」。語料 `AP325/2/2_5` 整支輸出全是 0。\n  🔴 順帶抓到 `resetAliases()` **零個呼叫者**——別名表跨程式殘留，\n  而它只在「跑過別的東西之後」才錯（251 條一起跑紅三條、每一條單獨跑都綠）。\n\n📈 **2026-09-20（第 206 刀：課說了算）：輸入量調整，判定格未動。**\n  新增第 125 條護欄 `audit-lesson-blocks-reachable`\n  ＋ `ui/app.ts` 的「課宣告的直接進來，不再與主題取交集」。\n  🔴 它補的是第 205 刀留下的一半——`主題 ∩ 課` 那個交集還在，天花板換了個名字。\n\n📈 **2026-09-20（第 208 刀：產出不得比使用者寫的少）：輸入量上調一個檔，各自組裝仍是 0。**\n  新增 `tests/integration/codegen-honest-syntax-error.test.ts`（七種「解不乾淨」的形狀）。\n  🔴 它補的是**只有程式碼那一側在說謊**的那個病：執行那一側早就被\n  `canExecute` 擋住（`degradationCause`），而產生器把 `int x = 1 ⏎ cout << x;`\n  併成 `int x = cout << x;`——**一支別的程式**，另外兩種是整段蒸發。\n  🟢 判準不是「有沒有語法錯誤」（補一個分號是幫忙），是\n  **產出的非空白字元不得少於原文**。\n  ⚠️ 它用 `createTestLifter()`，所以「各自組裝」那一格沒有動。\n\n📈 **2026-09-21（第 210 刀：宣告說得出的位置形態沒做）：輸入量上調兩個檔，各自組裝仍是 0。**\n  新增 `tests/integration/audit-expression-form.test.ts`（第 126 條護欄）\n  ＋ `tests/integration/roundtrip-cpp-read-as-condition.test.ts`（讀取當條件的四個面向）。\n  🔴 修的是 `while (getline(cin, s))` 的積木是一顆**灰色逃生艙**——\n  `component.json` 早就宣告了 `positions: [\"statement\", \"expression\"]`，\n  而形態只做了語句那一個。**五路完備性看不到它**（它問「render 那一路有沒有檔案」）。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "scanned": {
    "testFileCount": 517
  },
  "adHocAssembly": 0,
  "details": [],
  "note": "📈 **2026-09-17（迭代器的 round-trip 那一關）：語料變大，缺陷那幾格未動。**\n  新增 `tests/integration/roundtrip-cpp-iterators.test.ts`（10 支）\n  ＋ `interpreter-matches-compiler` 加 14 條最小重現（總計 60/60）\n  ＋ `runtime/map.ts` 的 `compareValues`（有序容器的比較規則，排序與查找共用）。\n  🔴 那一輪抓到三個【疊在同一支語料上】的缺陷：容器不記元素型別、\n  比較規則不認一對值（於是有序容器根本沒有排序）、查找把每一格拆開只比第一個。\n\n  🟢 而【各自組裝】仍然是 0。\n\n📈 **2026-09-17（量測工具自己的孤兒）：語料變大，缺陷那幾格未動。**\n  新增 `tests/integration/run-cpp-no-orphans.test.ts`——它證明「逾時之後不得留下孤兒」。\n  🔴 那個缺陷讓**三次全套測試被系統以記憶體不足砍掉**，而它一支測試都沒有弄紅：\n  一輪正常結束的語料探針留下 3 支孤兒，各燒 90% CPU。見 history/244。\n\n  🟢 而【各自組裝】仍然是 0。\n\n📈 **2026-09-17（模糊測試那一輪）：測試檔 488 → 490，而【各自組裝】仍然是 0。**\n  新增容器兩軸的 round-trip 與模糊測試回歸，以及兩顆膠囊的自證測。\n  🟢 新檔都走 `createTestLifter`（共用組裝點）——這條護欄要的正是這個。\n  ⚠️ **這個數字我第一次寫成 491，而那是錯的**：量的當下有一支用完即丟的探針\n  還在磁碟上。護欄下一輪就抓到了（`491 → 490`）。\n  > **一個從「暫時的狀態」量出來的基線，與一個從負載下量出來的基線是同一類錯誤\n  > ——它把一個當下的偶然，寫成了往後每一次比對的標準。**\n\n📈 **2026-09-16（模糊測試那一輪）**：新增 `cpp:container_push_front`、\n  `interpreter/clone.ts`、`interpreter/aliases.ts` 與兩支回歸測試檔。\n  **輸入量上調，缺陷格未動。**\n\n📉 **2026-09-16：489 → 487（下調）。** 這一輪用完即丟的四支探針\n  （模糊測試的驗證者、單支追蹤、樹的傾印）刪掉了。\n  ⚠️ 棘輪連【改善】也要下調——基線停在舊值的話，往後退回去不會有人發現。\n\n📈 **2026-09-16（第一百七十九刀，第二顆）**：`cpp:container_push_front`\n  ＋ 它的自證測。**輸入量上調，缺陷格未動。**\n\n📈 **2026-09-16（第一百七十九刀，第二顆）**：新增 `cpp:container_push_front`。\n  🔴 它是**模糊測試**升級上來的：探索報告原本把它排在最後（真語料只有 1 支用到），\n  而隔離出題的 10 支程式有 5 支卡在它——**一個沒有 push_front 的 deque 只有一半**。\n  **新增一顆元件，輸入量本來就會長。**\n\n📈 **2026-09-16（第一百七十九刀，round-trip）**：新增\n  `tests/integration/roundtrip-cpp-container-pop-front.test.ts`（5 條，含行為與帶下標的接收者）。\n  **輸入量上調，缺陷格未動。**\n\n📈 **2026-09-16（第一百七十九刀）**：新增 `cpp:container_pop_front`。\n  **新增一顆元件，輸入量本來就會長**——缺陷那幾格一筆未動。\n\n📈 **2026-09-16：485 → 486**——新增 `tests/integration/interpreter-matches-compiler.test.ts`\n  （解譯器與參照編譯器的最小重現）。輸入量變大，不是缺陷。\n\n📈 **2026-09-16：484 → 485**——新增 `tests/probes/studycpp-behaves.test.ts`\n  （學生的程式，解譯器與參照編譯器跑出來一不一樣）。輸入量變大，不是缺陷。\n\n🟢 **2026-09-15（整班回饋的第二刀）：輸入量上調，耦合數字未動。**\n  ① 第一百三十一條護欄 `audit-lesson-tells-what-to-do`（棘輪，205 / 240）\n  ② `LessonNudgeBar` 多一種 `kind: 'start'`——冷開（沒選課、畫布空的）指一條路\n  🔴 兩個學生說「找不到方塊」「最難的是找積木，眼睛快花了」，而他們**沒有從課程\n  點進來**——於是拿到完整工具箱（實測 12 個分類、196 顆）。\n  > **問題不是那 196 顆太多——自由練習本來就該有全部。\n  > 問題是他不知道有一條帶路的。**\n  ⚠️ 而那條線的機制從 2026-09-10 就在，只是**冷開那條路從來不呼叫它**。\n\n🟢 **2026-09-15（highlight 差一行）：輸入量上調，耦合數字未動。**\n  新增 `core/projection/patch-shift.ts` ＋ `tests/unit/core/patch-shift.test.ts`。\n  🔴 使用者：「積木跟程式碼好像 highlight 的地方對不上，會差一行」——\n  對照表描述的是【補之前】那份文字，而補丁器在最上面塞了一行 `#include`。\n  ⚠️ 而組裝點那個 wrapper **早就算過同一個位移**（`linesDelta`，給游標保位用）。\n  > **同一個位移被算出來給 A 用，而 B 也需要它——那是「已經知道答案而沒有交出去」。**\n\n🟢 **2026-09-15（highlight 差一行）：輸入量上調，耦合數字未動。**\n  新增 `core/projection/patch-shift.ts`（補丁器插了幾行，對照表就挪幾行）\n  ＋ `tests/unit/core/patch-shift.test.ts`。\n  🔴 使用者：「積木跟程式碼好像 highlight 的地方對不上，會差一行」——\n  對照表描述的是【補之前】那份文字，而補丁器在最上面塞了一行 `#include`。\n  ⚠️ 而組裝點那個 wrapper **早就算過同一個位移**（`linesDelta`，給游標保位用）。\n  > **同一個位移被算出來給 A 用，而 B 也需要它\n  > ——那是「已經知道答案而沒有交出去」，不是「還不知道」。**\n\n🟢 **2026-09-15（第一百三十條：課文的強調要留得出喘息）：輸入量上調一個檔。**\n  新增 `tests/integration/audit-lesson-emphasis.test.ts`。\n  學生：「雖然中間的文字寫得有點難理解，但整體講得很詳細」——量出來\n  全庫 69 課的「沒有被標記過的文字」中位只有 14 字，而一句中文是 20–25 字。\n  > **當每三個詞就有一個被標成重點，讀者就建不出層次。**\n\n🟢 **2026-09-14（名字也要驗）：輸入量上調，缺陷格未動。**\n  新增 `core/identifier-syntax.ts`（語言套件宣告、核心讀）\n  ＋ `core/component/param-check.ts`（`ParamSpec.kind` 的唯一判定處）。\n  🔴 使用者轉述學生：「變數名稱會寫成數字，Semorphe 竟然還可以接受」\n  ——實測產出 `int 123 = 16;`，主控台零錯誤、畫面零標記。\n  而 97 個參數宣告了 `kind: 'identifier'`，**沒有任何一處讀 `kind`**。\n  ⚠️ 同一週第三個同族缺陷（slots 的 min/max、形態軸 role、然後是這個）。\n\n🟢 **2026-09-14（參考解答的用字）：輸入量上調，缺陷格未動。**\n  新增第一百二十八條護欄 `audit-solution-vocabulary`：參考解答不得用到這一課還沒教過的元件。\n  🔴 它從一則學生回報來：第 3 課〈做一個〉要他宣告「有沒有及格（真或假）」，\n  而那一課的 `components` 裡沒有 `cpp:builtin_constant`——**那顆積木真的不在工具箱裡**。\n  ⚠️ 第一百一十四條看不到它：那一條掃的是課文裡的 ```cpp 區塊，而這一題只有散文。\n  > **一條護欄看得到的，是這一課【展示】了什麼；\n  > 而學生撞到的，是這一課【要求】他做什麼。**\n  🟢 順帶抓到另外 7 課同族的漏宣告（break／array_assign／string_size／…），\n  以及兩條入門軌 13 道沒有參考解答的題目（補齊了——**那才是這一條生效的前提**）。\n\n🟢 **2026-09-14（快車道 `/next/` ＋ e2e 分片）：輸入量上調，缺陷那幾格一筆未動。**\n  新增 `core/base-path.ts`（站台前綴的唯一來源）＋ `tools/build-site.sh`（兩份站一包）\n  ＋ 第一百二十七條護欄 `audit-shell-var-braces`。\n  🔴 快車道與主站**刻意同一個 origin**——`localStorage` 是 per-origin 的，\n  換子網域會讓學生在快車道上的存檔在主站全部不存在，而他不會知道。\n  ⚠️ 代價就是每一條絕對路徑都要帶前綴；少帶一條的症狀不是報錯，是只有 `/next/` 那邊 404。\n\n🟢 **2026-09-13（入門軌課程修訂）：測試檔數 475 → 476**——**輸入量上調**。\n  新增探針 `tests/probes/lesson-declared-components.test.ts`（課文用到的元件，這一課宣告了嗎）。\n  🔴 它與 e2e 那一支驗同一件事，而那一支一輪 17 分鐘——這次它在**第十二分鐘**\n  才說出「第 16 課漏宣告 `cpp:arithmetic`」，一行 JSON 的漏字。\n  > **一條驗證迴路如果比人的耐性長，它就會在最需要的時候被跳過。**\n\n🟢 **2026-09-13（面板登錄表接上了）：測試檔數 474 → 475**——**輸入量上調**。\n\n  spec 170 的 T001–T006 蓋好登錄表就停住了，而**那個停住看不見**：\n  `src/panels/` 零份宣告、`loadPanels()` 零個產品呼叫者、\n  README 引用的那支測試不存在。登錄表自己的測試檔頭寫著：\n  「**一個沒有人宣告的登記處【就是殼】，而它綠得跟真的一樣。**」\n\n  這一輪：五份 `panel.ts`（code／flow／blocks／console／variables）\n  ＋ `loadPanels()` 接上組裝點（健檢結果印出來）\n  ＋ 讓「狀態」那一層的分頁**真的去問登錄表**（名字與順序）\n  ＋ 第一百二十五條護欄 `panel-mount-migration`（棘輪，`mount` 還在組裝點的只准變少）。\n\n  ⚠️ **`mount` 沒搬是有理由的**，而那是 `app-shell.ts` 自己留下的：\n  「一次抽象如果同時搬走「容器」與「內容」，它壞掉時你分不出是哪一半。」\n  🔴 所以由 `mountedByShell` 說出來、由棘輪數著——\n  **一個分階段的搬遷，如果沒有東西數著還剩幾個，它會停在第一階段。**\n\n🟢 **2026-09-13（套件契約分層）：測試檔數 473 → 474**——**輸入量上調**。\n  新增第一百二十四條護欄 `pack-contract`：**`Pack` 不得長出只有一個域填得出的欄位**。\n\n  🔴 它守的是「域」那一軸。`concepts/元件.md` 定過「域／語言／layer 三軸正交」，\n  而查證量到**那一軸在程式碼裡不存在**（`component.json` 有 `domain` 的：0 個），\n  唯一的擴充點 `LanguagePack` 有一整族只有軟體域填得出的欄位\n  （`grammar`／`createParser`／`programRoot`／`fileExtension`／`createCodeShaping`…）。\n\n  > **一個擴充點如果它的必填欄位只有一個域填得出來，\n  > 那它不是擴充點，是那個域的建構式。**\n\n  拆成 `Pack`（身分／順序／宣告／課程／目標／分類）＋ `LanguagePack extends Pack`。\n  ⚠️ 而護欄擋的是**拆完之後慢慢漂回去**：下一個人在 `Pack` 加一格語言專屬的欄位，\n  在硬體套件進來之前沒有任何東西會出聲。注入驗證過（塞一個 `grammar` 進 `Pack` → 紅）。\n\n🟢 **2026-09-12（秒級的解答探針）：測試檔數 472 → 473**——**輸入量上調**。\n  新增 `tests/probes/lesson-solutions-run.test.ts`：把每一份參考解答走一次\n  lift → 直譯器，比對課程宣告的 `check.stdout`。\n\n  🔴 它為什麼存在：`e2e/lessons.spec.ts` 在驗同一件事，而一輪 **5.1 分鐘**。\n  寫題目的時候改一行等五分鐘，那條迴路太長——而迴路太長的後果不是慢，是**改完不驗**。\n\n  > **一條驗證迴路如果比人的耐性長，它就會在最需要的時候被跳過。**\n\n  🟢 它是**必要條件不是充分條件**（驗不到課程的 pins／鷹架／工具箱下學生做不做得到），\n  所以不取代 e2e 那一支。⚠️ 它住在 `tests/probes/`，不進 `npm test`。\n\n  🔴 **它上線兩秒就抓到兩個真缺陷**（都用參照編譯器確認過是課程宣告錯了）：\n  `猜數字` 的 `ex1` 宣告 7 而實際 6、`二分搜` 的 `ex1` 宣告 `3\\n2` 而實際 `3\\n4`。\n  那兩題的裁判一直在對**做對的學生**說他錯。\n🟢 **2026-09-10（中鍵推畫面）：測試檔數 471 → 472**——**輸入量上調**。\n  學生回饋（附截圖）：「就是有時候遇到這種其實蠻難滑的（積木），要不要設定\n  按滑鼠中鍵可以移動我看的地方然後不會動到積木」。那張截圖是一段 DFS，\n  `陣列 p 的第 [ u ] 格 = v` 橫著長到出畫面。\n\n  🔴 **Blockly 早就能拖背景平移——而那張畫面上背景不存在**：每一個像素都是積木，\n  於是「先找一塊空白」這一步本身就失敗了。\n\n  > **一個要求使用者先找到空白處的手勢，在畫面最滿的時候最沒有用\n  > ——而那正是他最需要平移的時候。**\n\n  新增 `ui/panels/middle-drag-pan.ts`（capture 階段攔中鍵，Blockly 的手勢收不到它）\n  ＋ 第一百二十二條護欄 `middle-drag-pan`。\n  ⚠️ 中鍵的三個瀏覽器預設行為都要擋（Windows 自動捲動／X11 貼上主選取區／auxclick 開新分頁）。\n\n🟢 **2026-09-10（網頁版的擴充主機）：測試檔數 +1**——**輸入量上調**。\n  使用者在 Codespaces 撞到 `command 'semorphe.openBlocks' not found`\n  ——指令沒被註冊，因為**擴充根本沒有啟動**：manifest 只宣告了 `main`，\n  而網頁版的 VS Code 用的是 web worker 擴充主機，它只讀 `browser`。\n\n  > **一個只宣告了 `main` 的擴充，在網頁版裡不是「壞掉」——\n  > 它是【不存在】，而畫面上只看得到一句「找不到那個指令」。**\n\n  🟢 而 `src/vscode/` 底下**一個 node 內建都沒用到**，所以那是建置的事。\n  新增第三個 vite 目標 `extension-web` ＋ 第一百二十一條護欄\n  `web-extension-host`（含一條硬性零：擴充那一側不得 import node 內建）。\n\n🟢 **2026-09-10（打暗要說得出換哪一課）：測試檔數 468 → 470**——**輸入量上調**。\n  使用者轉述學生的畫面：他沒有換課，一路往下寫。第 1 課只開了三顆元件，\n  而他在宣告變數——**他自己寫的每一塊積木都被打暗**，而畫面上沒有一個字\n  說為什麼、也沒說怎麼辦（出路藏在最下面那條狀態列的一格下拉裡）。\n\n  ⚠️ 同一個問題被問過兩次：2026-08-28 使用者自己看著畫面問過\n  「為何積木變這麼暗？」。那一次修的是「不該暗的別暗」，\n  這一次是「該暗的**沒說為什麼**」。\n\n  > **一個純視覺的訊號，如果它在使用者【正當地往前走】的時候觸發，\n  > 那它讀起來不是提示，是故障。**\n\n  新增 `core/lesson-suggest.ts`（哪一課涵蓋得了這些元件——**算出來的**，\n  每一課都宣告了自己的 `components`）＋ `ui/lesson-nudge-bar.ts`（一條指路的線）\n  ＋ 第一百二十條護欄 `lesson-nudge`。\n\n🟢 **2026-09-10（積木改不動程式碼要說出來）：測試檔數 467 → 468**——**輸入量上調**。\n  學生回報：「我執行一次之後改右邊，左邊不會改，下方的答案也不會改。\n  我不知道是我的問題還是他的。反正我還要自己改左邊。」\n\n  🔴 積木載入失敗之後 `isStateStale` 會擋掉「積木→程式碼」——**那是對的**。\n  錯的是它**不說**：狀態列照樣寫「⇄ 同步中」，改一顆積木什麼都沒發生，\n  而那個旗標只在下一次成功重畫時才清——被擋住就不會有下一次重畫。\n\n  > **一個「我暫時不聽你的」的狀態，如果沒有一直說出來，\n  > 使用者會以為是自己不會用。**\n\n  新增狀態列第四態「⛔ 積木改不動程式碼」＋ 每次被丟掉的編輯出一次聲（節流）\n  ＋ 恢復時通知狀態列 ＋ 第一百一十九條護欄 `sync-stuck-speaks`。\n\n🟢 **2026-09-10（第一百一十七條護欄：載得進工作區）：測試檔數 466 → 467**——**輸入量上調**。\n  使用者回報 `vector<int> C(n+1), V(n+1);` 會錯，接著問「你到底有沒有整個仔細測過一遍？」\n  ——**沒有**。前一輪拿那份語料量的是【語義不動點】，而那一層對\n  「Blockly 建不建得起來」一個字都沒說（第五十一條護欄的檔頭寫過這句）。\n\n  補上之後量到 218 支裡 **2 支載不進去**，而那不是少一行，是**一片空白**：\n  ```\n  cc[A[i]];          離散化的慣用寫法——一個沒有敘述形態的運算式當語句用\n  return c;\n  //保底              return 在此之前沒有下接點\n  ```\n  再用參照編譯器做一次 15 種型別 × 20 種宣告形狀的全掃（236 個合法組合），\n  找出**六族**第二個宣告子蒸發，全部修完。\n\n  > **一個你剛寫下來的教訓，如果沒有立刻套回手上這份輸入，\n  > 它記的是「我知道有這回事」，不是「我檢查過了」。**\n\n🟢 **2026-09-09（逗號的積木）：測試檔數 465 → 466**——**輸入量上調**。\n  新增第一百一十六條護欄 `comma-block`。逗號運算式在真實語料裡出現 140 次而\n  **沒有積木形態**（整行變一顆黑盒），使用者拍板做它，理由是**兩根指標的迴圈**\n  （`for (int a = 0, b = n; a < b; a++, b--)`，AP325 的常見技巧）。\n  ⚠️ 語料變大是那個護欄檔裡的 C++ 片段——設計如此（「測試長，語料就跟著長」）。\n  🔴 而它宣告的 `skipReasons.render: consumed-by-parent` 是**錯的**：\n    140 次裡 138 次的父節點是 body，沒有人吃它。\n  > **一個「由父節點吃掉」的宣告，要說得出【是哪一個父節點】。**\n\n🟢 **2026-09-09（拿學生的競賽練習當語料——七族靜默的資料遺失）：測試檔數 462 → 465**——**輸入量上調**。\n  使用者：「這是我的學生的練習的 repo，我希望他所有的有用到的東西在 semorphe 都是\n  有被處理好的，你可以把這當作是語料庫來看看我們有沒有什麼缺乏，例如有什麼是\n  **轉成積木轉不回來**的之類的」。218 個 `.cpp`、7327 行（AP325／TIOJ／zeroJudge／APCS）。\n\n  🔴 **第一個數字很好看，而它量錯了東西**：解析失敗 0、lift 失敗 0、殘差 **0.0%**\n  ——而**218 支裡 144 支轉一圈回來不是同一棵樹**。\n\n  ```\n  string A[100];            ->  string x;         🔴 陣列、大小、名字一起蒸發\n  string s, t, u;           ->  string s;         🔴 第二、三個變數蒸發\n  string* p;                ->  string x;         🔴 指標與名字蒸發\n  int a[10], b[20];         ->  int a, b;         🔴 兩個大小蒸發\n  vector<int> v(n);         ->  vector<int> x;    🔴 32 個檔這樣寫\n  ios::sync_with_stdio(0),  ->  ...(0);/, cin.tie(0)  🔴 編不過（136/218 支的第一行）\n  max 的大括號多載          ->  多補一個 0        🔴 編不過\n  lambda 裡的 count++;      ->  count++（沒有分號） 🔴 編不過\n  ```\n\n  > **一份自己造的語料，量的是「我們想到的那些輸入」。**\n\n  🟢 **144 -> 2**（剩下的兩支是形狀差異，程式碼正確）。\n  修的七處裡有**四處是同一個反模式**：`?? 'x'` 那一族\n  ——「一個 `?? 預設值` 讓『我沒認出來』與『它本來就長這樣』長得一模一樣」。\n\n  ⚠️ 而**兩處修在共用層，不修在 152 顆膠囊裡**：\n  ① `asExpression`（`asStatement` 的反面）——152 個產生器無條件產語句形，只有 7 個\n     看 `ctx.isExpression`；② `generateBody` 清掉 `isExpression`——lambda 的主體\n     是語句的地方，而那個旗標會穿過它不再成立的邊界。\n  > **一個「要記得先檢查」的契約，就是一個等著被忘記的契約。**\n\n  新增第一百一十五條護欄 `declaration-shapes-roundtrip`（53 條，形狀出自那份語料）\n  ＋ 兩支探針 `tests/probes/studycpp-*`（**沒有 `STUDYCPP_DIR` 就跳過**——語料是別人的 repo）。\n\n🪦 **2026-09-08：語料【縮水】而它是正當的——`edit-tally` 那一條線整條退場。**\n  測試檔數 464 → 462\n\n  使用者：「我後來覺得弄這條好像沒必要，而是可以在**設計課程的時候提醒\n  可以開始用程式碼寫**」。\n\n  🔴 而那讓「這一課改了哪一邊」的計數**回到零消費者**——所以它跟著走：\n  > **一個沒有消費者的機制，是最差的那一個選項**（我自己在 history/222 寫的）。\n\n  刪掉的：`src/core/edit-tally.ts` · `previewBar`（四個檔）·\n  三個測試檔（unit ×2、e2e ×1）。\n  🟢 **已逐項核對縮水的數字與刪掉的東西對得上**（1 個 src 檔、3 個測試檔、\n  7 個 export）——是「東西真的被刪了」，不是「掃描器不再吃到它」。\n\n🟢 **2026-09-08（拆輪子的橫條——一面鏡子）：測試檔數 463 → 464**——**輸入量上調**。\n  `ControlOption` ＋ `QuickPickItem` 多一格 `previewBar`（兩色橫條，**上面沒有字**）；\n  `edit-tally.ts` 多 `tallyCurve`／`barShare`；`semantic-wave.ts` 多 `viewLabel`；\n  新測試 `tests/unit/ui/quick-pick-bar.test.ts`（happy-dom）。\n  🔴 它是 `edit-tally` 的**第一個消費者**——那份計數在此之前零人讀。\n  > **一面鏡子不說話**：橫條裡不得有任何文字節點，`title` 只准是數字\n  > ——「還」「才」「只」「已經」「你」一個都不准（兩者都有測試）。\n  ⚠️ 橫條放在**章節清單**而不是題目格：16 條排成一列**就是曲線**，\n    而學生要**主動打開**才看得到——一面你自己走過去照的鏡子，與掛在你面前的不同。\n\n🟢 **2026-09-07（關卡的目標 ＋ 規格頁）：測試檔數 462 → 463**——**輸入量上調**。\n  `LessonTask` 多一格 `stepBudget`（⚠️ **一句話不是一道門**：超過了照樣算通過）\n  ＋ `describeBudget`；`tools/build-lessons` 多一頁**規格頁**\n  （`targets/*.json` 的投影，8 塊板子的腳位與常數，**每一筆附上游來源**）。\n  🔴 那個目標數字是**量出來的**（`tests/probes/step-budget.test.ts`）：\n    排序版 57 步、兩兩比 154 步——訂 80。\n  🟢 而規格頁**不可能與宣告不一致**，因為它是投影\n    ——`audit-lesson-pages` 多兩條驗它（注入「不印上游來源」→ 紅）。\n\n🟢 **2026-09-07（效率的回饋）：測試檔數 461 → 462**——**輸入量上調**。\n  新增 `core/steps.ts`（跑了幾步 ＋ 與上一次比）＋ `tests/unit/core/steps.test.ts`；\n  `LessonTask` 多一格 `compareSteps`。\n  🔴 它補的是一個**教材已經在教而沒有回饋**的落差：`cpp-advanced` 第 1 課\n    逐字「n ≤ 100000 這句話**直接決定你可以用什麼做法**」，\n    而在此之前 O(n²) 與 O(n log n) 的兩份解答**在畫面上長得一模一樣**。\n  ⚠️ 「一步」＝直譯器走過一顆語義節點一次——**那不是課本的定義**，\n    而選它的理由是誠實：它是執行器真的知道的東西。\n  ⚠️ 而它**只在課程宣告的題目上說話**，且**說的是任務不是這個人**。\n\n🟢 **2026-09-07（spec 178：課文的用字與還沒教過的東西）：測試檔數 458 → 461**——**輸入量上調**。\n  使用者回報「**沒有先說明註解是什麼就使用註解**」＋「還有**課文本身的用字**」，\n  而查證量到**兩族共 171 筆**：\n  ```\n  ① 課文的程式碼用了還沒教過的元件      62\n  ② 一顆積木【第一次】出現而課文沒提它在畫面上叫什麼   109\n  ```\n  🔴 **同一天清到 0 ＋ 0**，第一百一十四條護欄 `audit-lesson-vocabulary`（**硬性零**）。\n  ⚠️ 語料變大是因為**新護欄檔裡的 C++ 片段**（`takeCorpus` 掃 `tests/integration/`\n    的反引號——設計如此：「測試長，語料就跟著長」）。\n\n🟢 **2026-09-07（拆輪子的曲線）：測試檔數 455 → 458**——**輸入量上調**。\n  新增 `core/edit-tally.ts`（「這一課你改了哪一邊」，**只留在本機**）\n  ＋ `semantic-wave.ts` 的 `curveOf`（跨課的曲線）\n  ＋ `load-lessons.ts` 的 `viewForLesson`（**階梯**語意）\n  ＋ 第一百一十三條護欄 `audit-training-wheels-curve` ＋ 兩份單元測試。\n  🔴 `Track.view` ＋ `LessonPins.view`：軌道給預設、課在**轉折點**覆寫\n    ——形狀與 `scaffold` 那一軸一樣，**刻意不發明區間語法**。\n  ⚠️ **護欄當場抓到第一版的設計缺陷**：`pins.view ?? track.view` 會讓\n    轉折點的**下一課退回預設**（`blocks blocks blocks compare blocks…`）。\n  > **一條曲線的宣告，寫的是【轉折點】而不是【每一格的值】\n  > ——而「沒宣告就退回預設」會把轉折變成一根刺。**\n  🟢 而它抓得到，是因為**護欄與產品共用同一支** `viewForLesson`。\n\n🟢 **2026-09-06（spec 177：匯出是一份帶得走的作品）：測試檔數 453 → 455**——**輸入量上調**。\n  新增 `core/portable.ts`（拆檔／組回來／預設名）＋ 第一百一十二條護欄\n  `audit-portable-export` ＋ `tests/unit/core/portable.test.ts`\n  ＋ `e2e/portable-export.spec.ts`（**匯出／匯入的第一批 e2e**——在此之前是 0 支）；\n  `LanguagePack` 多一格 `fileExtension`，兩個語言套件各宣告一次。\n  🔴 匯出從「一份只有我們打得開的 `.json`」變成一個容器：\n    `<作品>.cpp`（原始碼，任何編輯器打得開）＋ `.semorphe/<同名>.json`（側檔）。\n  > **一份匯出如果程式碼不是第一級的檔案，那份匯出把投影當成了真相。**\n  ⚠️ 新增 npm 依賴 `fflate`（8KB、無相依）——**它跟著產品一起打包**，\n    而 `e2e/offline.spec.ts` 仍然綠（零外部請求）。\n\n🟢 **2026-09-06（spec 176：型別的下拉跟著程式長）：測試檔數 451 → 453**——**輸入量上調**。\n  新增 `core/types-in-use.ts`（`typesInUse` ＋ `withTypesInUse` ＋ 餵樹的登記處）\n  ＋ 第一百一十一條護欄 `audit-types-grow-with-code` ＋ 端對端 `types-in-use-e2e`。\n  🔴 它兌現 vision「開放值域的欄位要寫得出來」**最後一條**未做的驗收：\n    「選項跟著程式長——用過的型別會出現在其他積木的下拉裡（**導出的，不是宣告的**）」。\n  ⚠️ **它修的不是正確性，是可發現性**：學生宣告過 `struct Point` 之後，\n    那個型別**打字寫得出來**（認不得的值不會被換掉），而他**選不到**。\n  > **一份「這個欄位可以填什麼」的清單，如果它是宣告的而不是導出的，\n  > 那它描述的是【我們想到的】，不是【這支程式裡有的】。**\n  ⚠️ 而 `vector<int>` **掃不到，那是對的**：容器那一層的資訊住在**身分**裡\n    （`cpp:vector_declare`），所以掃 `type` 拿到的是**元素型別**。界線已釘在測試裡。\n\n🟢 **2026-09-06（spec 175：身分階層 vs 實例路徑）：測試檔數 450 → 451**——**輸入量上調**。\n  新增 `core/component/path-syntax.ts`（判別器 ＋ 兩個投影）\n  ＋ 第一百一十條護欄 `audit-identity-path-syntax`。\n  🔴 它把 `concepts/元件.md` §`name` 的階層那三條規則變成**機械檢查**\n    ——那一節自己標著「**狀態：規則已定，尚未實作**」。\n  ⚠️ 而它**現在守的是未來**：帶 `.` 的身分今天 0 顆，\n    它會在第一個人寫下 `cpp:containers.vector` 的那一刻紅。\n  > **一條規範沒有機械化的檢查，它本身就是殼——而殼看起來像完成。**\n\n🟢 **2026-09-06（spec 174：lift 樣式先問一句）：測試檔數 448 → 450**——**輸入量上調**。\n  `cpp:pin_constant` 的 lift 樣式**重開了**（5 條），而它先問\n  「這個名字被宣告過嗎」——判準與那份病歷裡的一樣：\n  **沒有人宣告它，它才是環境提供的**。\n  🔴 收益：學生從工具箱拖 HIGH 積木，同步一趟回來不再變成「變數參照」積木。\n  ⚠️ 語料段數上升是因為**新增的測試檔本身**（那個檔在 `tests/integration/`，\n    而殘差護欄掃那個目錄的反引號片段）。\n\n⚠️ 2026-09-06（`pin_constant` 的重開條件量過了）：測試檔數 447 → 448——新增探針 `tests/probes/lift-scope-knows.test.ts`。**輸入量上調。**\n🔴 量出來的答案是**重開條件不成立**：enum 成員在 lift 期查不到（`recordDeclaration` 只認 `<scope>:<x>_declare`）。\n> **一個「重開條件」如果沒有人去量它有沒有成立，它與「永遠不重開」是同一件事。**\n⚠️ 2026-09-06（第三方 scope 驗過了）：測試檔數 446 → 447——新增 `tests/integration/third-party-scope.test.ts`。**輸入量上調。**\n🟢 而它**一行產品程式碼都沒改**：`@someone:*` 在身分、登錄、抽象元件三層本來就成立。\n> **一個設計如果沒有人用它的極端值跑過一次，那它成立的範圍是【設計者想像的那些輸入】。**\n🟢 **2026-09-06（兩個宿主都要記得住）：測試檔數 445 → 446**——**輸入量上調**。\n  新增第一百零九條護欄 `audit-both-hosts-persist`（棘輪）：\n  只在一個宿主記得住的東西只准變少——今天是 **1**（流程佈局在 IDE 那側零筆）。\n  🔴 它是查證 vision 一條**已經打勾**的驗收時翻出來的：\n    「重開後在原位（持久化）」只在網頁版成立。\n  > **一個「已交付」的驗收如果只在一個宿主上驗過，\n  > 它記的是「那個宿主可以」，不是「這件事做完了」。**\n  ⚠️ 它**刻意不是硬性零**：今天真的缺一項，而一條每天都紅的護欄\n    與一條沒有的護欄在第二天之後是同一個東西。降到 0 的那天它會紅（自我否證）。\n\n🟢 **2026-09-06（同步的入口兩個宿主都要有）：測試檔數 444 → 445**——**輸入量上調**。\n  新增第一百零八條護欄 `audit-sync-entry-both-hosts`：\n  網頁版與 IDE 那側的入口一個都不得少（注入「拿掉 IDE 那側的註冊」→ 紅並指名宿主）。\n  🔴 它守的是 2026-08-25 使用者反問後留下的一句更正——**而那句話一直沒有機械檢查**。\n  ⚠️ 它**驗不到**「在 Arduino IDE 裡按下去有反應」（`preflight` 跑的是 Chromium）\n    ——那條收工條件仍然要人做。這條守的是它的**必要條件**。\n  > **一個要換一個環境才看得到的東西，在另一個環境裡等於不存在\n  > ——而寫它的人是在那另一個環境裡工作的。**\n\n🟢 **2026-09-06（把核心從 `localStorage` 上拔下來，spec 173）：測試檔數 442 → 444。**\n  新增第一百零七條護欄 `audit-core-no-storage`（核心不得直接用宿主的儲存 API）\n  ＋ `core/host/key-value-store.ts`（埠 ＋ 記憶體實作）\n  ＋ `ui/browser-store.ts`（瀏覽器實作——⚠️ **它住在 UI 層**：\n    知道宿主的那一層是 UI，核心只宣告它要什麼）。\n  🔴 **核心裡直接叫 `localStorage` 的次數 7 → 0。**\n  > **一個「核心」如果它的存檔只在一個宿主上跑得起來，\n  > 那它不是核心，是那個宿主的一部分。**\n  ⚠️ `ui 檔數` 與 `import 行數` 上調是**輸入量**：多了一個實作檔，\n    而那正是這一刀要的形狀（實作在 UI，宣告在核心）。\n\n⚠️ 2026-09-06（SDK 的公開面）：測試檔數 441 → 442——**輸入量上調**。新增第一百零六條護欄 `tests/integration/audit-sdk-surface.test.ts`（公開面的每一個匯出名記在基線裡；移除／改名 ＝ 破壞性改版，要顯式下調）。\n🔴 它守的是一句本來只寫在註解裡的規範（`src/sdk/index.ts` 檔頭：「加一個匯出很便宜，**移掉一個很貴**」）。\n> **一個公開介面的移除，在自己的 repo 裡與一次正常的重構長得一模一樣——差別在別人的 CI 上。**\n⚠️ 2026-09-06（那五個語法形狀驗過了）：測試檔數 440 → 441——新增探針 `tests/probes/five-shapes.test.ts`。**輸入量上調。**\n🟢 而它讓 `corpus-shapes` 的「C++ 語法形狀該補」5 → 3（顯式下調）。\n⚠️ 2026-09-06（骨架上匯流排，spec 172）：測試檔數 439 → 440——**輸入量上調**。新增第一百零五條護欄 `tests/integration/audit-scaffold-on-bus.test.ts`（組裝點不得直接呼叫視圖的骨架標記）。\n🟢 而**耦合那一欄是下降的**：`compositionRootViewCalls` 72 → 71——組裝點少替視圖做了一個決定。\n⚠️ 而語料的「方法呼叫數」2969 → 2971 是**上升**的，那正常：加了一支 `republishScaffold` 與一條護欄。\n> **一個驗收標準如果錨在「總量」上，它會在【做對事情】的時候也動。**\n⚠️ 2026-09-05（版面建議與語意波）：測試檔數 437 → 439——**輸入量上調，不是耦合變差**。新增 `core/semantic-wave.ts`（`LessonView` ＋ `abstraction()` ＋ `waveOf()`）＋ `tests/unit/core/semantic-wave.test.ts` ＋ 第一百零四條護欄 `tests/integration/audit-lesson-wave.test.ts`；`LessonTask` 多一格 `view`；`app.ts` 多一支 `applySuggestedView`。\n🔴 這一格是**建議，不是鎖**：`applySuggestedView` 只從【換題】那一條路呼叫，不從任何重畫路徑——因為「搶回去」在程式碼裡不是一個 `if (locked)`，它是一段「每次都重新套用預設值」的程式碼。外部證據是學習者控制的整合分析：「sequence control is the only type that generally does not harm」（`元件身分s/認知鷹架.md`）。\n🔴 而 `applySuggestedView` 除了呼叫 `applyLayout` 還要寫 `this.currentLayout`——少了後者，版面真的換了而狀態列的標籤還寫著舊的：**畫面上兩個地方互相打臉，比沒換更糟**。\n\n\n📈 **2026-09-17（管線 180，第二輪盲測）：測試檔數 492 → 493。**\n  多的是 `tests/integration/fuzz-cpp-iterators.test.ts`（迭代器一族的常駐模糊測試網）。\n  **輸入量長了，`adHocAssembly` 仍然是 0。**\n\n\n📈 **2026-09-18（round-trip 那一關）：新增一個五面向的回歸網**\n  （`roundtrip-cpp-structured-binding.test.ts`，34 支），而它順手量到兩個既有缺陷：\n  `unordered_map` 的鍵型別（同一顆元件的第二個樣板名）與\n  `int x, y;` 產不回一行（只在「再走一趟」時才看得出來）。\n  🟢 **語料的語義不動點因此從「走樣 6」變成「走樣 0／218」**。\n\n\n📈 **2026-09-18（模糊測試第二輪，容器）：新增一個 17 支的回歸網。**\n  資訊隔離的盲測 10 支，第一輪 **1/10**——而那是在前一天同一批元件走完\n  一整條管線、全套 7117 綠之後量的。\n  > **資訊隔離量到的不是「我做得對不對」，是「我想不想得到」。**\n\n\n📈 **2026-09-18：新增第 119 條護欄 `audit-silent-downgrade`。**\n  它從**瀏覽器驗收**來：一顆宣告了抽象父、又不在某一軌課程清單裡的元件，\n  會被降級成那個父——而它比父多的那幾格**沒有地方放**。\n  `auto[pt, d] = BFS.front();` 在那一軌變成 `int x;`，**兩個名字沒了**。\n  > **降級只咬得到「宣告了自己屬於誰」的那些元件\n  > ——而那正是最該被完整保留的那些（它們有更多格）。**\n\n\n📈 **2026-09-18（第 120 條護欄 `audit-refcc-headers`）**：CI 紅了七支，全部是\n  「參照編譯器收不下」——**而本機全綠**。本機是 Apple clang（libc++）、\n  CI 是 GNU g++（libstdc++），兩者對「哪個標頭遞移帶進哪個」的答案不同。\n\n📈 **2026-09-18（範圍那一族：兩端從字串屬性換成接點，管線 188）：語料變大，缺陷那幾格未動。**\n  10 顆範圍演算法的 `begin`／`end`（＋前綴和的 `dest`）從**字串屬性**換成**接點**，\n  ＋ 三顆新元件（`cpp:range_find`／`cpp:range_unique`／`cpp:range_remove`）\n  ＋ `cpp:container_iter` 多一個 `call` 屬性（`begin(a)` 這個自由函式形式）\n  ＋ 27 顆宣告式元件補上 `forLoopPart`（同一個病的第三次，這次整族一起補並加了一條護欄）。\n  🔴 而拆掉那個 regex 之後發現**結構早就在了**：容器與位置在這個直譯器裡是同一個形狀\n  （`interpreter/pointer.ts` 的 `isCellPointer`），`A+n` 的位置算術也早就有人做。\n  > **那條 regex 解析的東西，執行期早就會算了——它不是在補一個缺口，它是在繞過一個已經存在的機制。**\n\n📈 **2026-09-18（round-trip 那一關，管線 188）：語料再變大一點，缺陷那幾格未動。**\n  新增 `tests/integration/roundtrip-cpp-range-slots.test.ts`（四個面向 × 18 個形狀）。\n  🔴 而那一關抓到一個**真的缺陷**：`v.erase(a, b)` 走一趟積木回來變成 `v.erase(a)`\n  ——刪一整段變成刪一格，**而它編得過**。修法是讓第二個位置有自己的接點。\n  🔴 順帶抓到第二個：**多宣告子的型別從來沒被記下來**（`vector<int> v, w;` 的\n  `v` 查不到型別），因為宣告子是由具名策略直接組出來的，不經過那個收集器的掛鉤。\n\n📈 **2026-09-18（盲測第一輪：範圍演算法，管線 188 階段四）：語料變大，缺陷那幾格未動。**\n  新增 `tests/integration/fuzz-cpp-ranges.test.ts`（10 支，2 綠 ＋ 8 根釘子）\n  ＋ `interpreter-matches-compiler` 一條最小重現（`&a[n]` 當結尾）。\n  🔴 盲測 **2/10**，而那是好消息：八支**沒有一支是語義錯**，全部是缺元件，\n  加上一個真缺陷（`&a[n]` 被 `INDEX_OUT_OF_RANGE` 擋下來——**取位址不讀那一格**）。\n\n📈 **2026-09-18（`cout` 的浮點格式對上 g++）：測試檔 ＋1。**\n  新增 `tests/unit/interpreter/float-format.test.ts`（21 格，**每一個期望值都是問 g++ 來的**）。\n  🔴 修的是 `formatDefaultPrecision` 疊在一起的**四個錯**：科學記號的尾數沒去尾零、\n  指數沒補兩位、切換門檻用 JS 的不是 C 的 `%g`、指數在四捨五入之前算。\n  ⚠️ 母體**不是一支語料**——是**每一支印大數或小數的程式**。\n\n📈 **2026-09-19（管線 197・階段三：位置的相鄰一格）：上調的是【輸入量】，缺陷那幾格未動。**\n  新增一顆膠囊 `cpp:pointer_step`（`prev`／`next`，方向是一個屬性）\n  ＋ 兩支常駐測試檔：`roundtrip-cpp-pointer-step`（四個面向 44 條）與\n  `roundtrip-cpp-cast`（15 條）。\n  🔴 後者是**從語料蒸餾**的，而它蒸餾的是我自己第 194 刀留下的兩個迴歸：\n  `int()` 產成 `(int)`（不是合法的 C++）與 `(ll)(x+1)*z` 產成 `(ll)x+1*z`\n  （合法而算另一件事）。抓到它們的是**語料的探針**，而那支探針\n  沒設 `STUDYCPP_DIR` 就會跳過。\n  > **跳過的護欄與不存在的護欄長得一樣。**\n\n📈 **2026-09-19（管線 197 階段六：瀏覽器驗收）：新增第一百二十三條護欄。**\n  `audit-label-markdown`——**積木上的字不得留著沒人渲染的標記**。\n  🔴 它是**用眼睛看到的**：滑鼠停在新積木上，tooltip 印著\n  「而\\*\\*原本那個位置不動\\*\\*」——Blockly 的 tooltip 是純文字，不渲染 markdown。\n  量了之後才知道不是一顆的問題：**349 句裡有 10 句**，而多數是同一批新元件留下的。\n  > **一個在註解裡正確的寫法，搬到隔壁那個檔就變成使用者看得到的雜訊。**\n\n📈 **2026-09-19（197 的收尾）：新增第一百二十四條護欄 `audit-node-version-drift`。**\n  🔴 它是**第 123 條自己的第一版在 CI 上炸掉**逼出來的：\n  `globSync is not a function`——本機 Node 24 有、**CI 的 Node 20 沒有**。\n  ⚠️ 而 `tests/helpers/find-files.ts` 早就為了同一件事存在（2026-08-27 咬過一次），\n  檔頭逐字寫著「不依賴 `fs.globSync`」。\n  > **知識在，而寫新程式的人不會去找它——一條只寫在註解裡的規矩，\n  > 擋得住讀到那個註解的人，擋不住沒讀到的人。**\n\n📈 **2026-09-19（第 199 刀階段三：round-trip）：輸入量上調。**\n  新增 `roundtrip-cpp-bits.test.ts`（四個面向 × 15 個形狀 ＋ 舊存檔遷移 ＋ 兩段正規化，共 67 支）。\n  🔴 而它抓到兩個**既有**的東西：\n  ① `staleShapeIn` **看不見接點改名**（只讀 `fields`／`extraState`）——機制加一格 `retiredInputs`\n  ② `cout << (n << 1)` 走一趟積木回來變成 `cout << n << 1`——**印 51 而不是 10**\n     （`LOW_PREC_OPS` 少了 `<<`／`>>`，而程式碼那一路靠 `layoutHints` 撐著）\n\n📈 **2026-09-20（第 199 刀階段四：資訊隔離盲測）：再新增兩顆身分。**\n  `cpp:bits_is`（整排的三個問句）與 `cpp:bits_as`（整排換成整數／字串），\n  ＋ 既有那顆「整排改值」多一個**常態留空的位置**接點。\n  🔴 **三顆都是探索報告以「語料 0 處」拒絕過的**，而盲測十支裡\n  `to_ulong` 用了 4 支、帶位置的 `set(i)` 多支、`cout << bs` 1 支。\n  > **「語料 0 處」量到的是【這批語料的人怎麼寫】，\n  > 不是【這個寫法有多常見】——而它在兩刀內錯了四次。**\n📈 **2026-09-20（帶參數的巨集）：測試檔數 510 → 511。**\n  **母體變大，缺陷那幾格未動**——新增一顆身分（`cpp:define_func`）＋ 一支樹修復（`languages/cpp/lang/macro-expand.ts`）＋ 一支回歸網（`roundtrip-cpp-func-macro.test.ts`）。\n\n📈 **2026-09-20（帶參數的巨集，第四關：資訊隔離盲測）：測試檔數 511 → 512。**\n  **輸入量變大，缺陷那幾格未動**——新增 `fuzz-cpp-macros.test.ts`（10 支盲測程式，\n  出題者看不到原始碼）＋ `interpreter-matches-compiler` 多 6 條最小重現\n（括號裡的逗號運算式，盲測抓到的）。\n  ⚠️ 那 10 支的標頭**一律換成 `bits/stdc++.h`**——出題者手列了標準標頭，\n  而第 120 條護欄當場擋下（手列 2 → 3）。換完輸出逐字不變。\n"
}
