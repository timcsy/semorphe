{
  "_meta": {
    "note": "各自列宣告來源、而沒把膠囊算進去的測試檔數。\n處方：改用 `allCppComponents()` / `allCppProjections()`（它們含膠囊）。\n⚠️ 這個數字只准下降，而**下降必須是因為改用組裝函式**，不是因為那支測試被刪掉了。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "scanned": {
    "testFileCount": 432
  },
  "adHocAssembly": 0,
  "details": [],
  "note": "⚠️ 2026-09-04（迴圈跑了幾次）：測試檔數 431 → 432——**輸入量上調，不是耦合變差**。新增 `core/iterations.ts`（把「每顆節點走過幾次」換算成「這顆迴圈重複了幾輪」）＋ `tests/unit/core/iterations.test.ts`；直譯器的 `visited: Set` 換成 `visitCounts: Map`（同一個漏斗，一次 `Map.set`）；匯流排的 `execution:coverage` 多帶 `counts`（🔴 **一個事件不是兩個**——分開的話收的人要自己保證它們來自同一次執行）；`BlocklyPanel.markIterations` 只畫。🔴 「哪一塊是迴圈」讀 `control_flow` 標註，「哪一格是身體」讀 `bodySlotsOf`——**兩份判斷都已經有家了，這一刀一份新的都沒寫**。\n"
}