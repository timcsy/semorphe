{
  "_meta": {
    "note": "各自列宣告來源、而沒把膠囊算進去的測試檔數。\n處方：改用 `allCppComponents()` / `allCppProjections()`（它們含膠囊）。\n⚠️ 這個數字只准下降，而**下降必須是因為改用組裝函式**，不是因為那支測試被刪掉了。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "scanned": {
    "testFileCount": 431
  },
  "adHocAssembly": 0,
  "details": [],
  "note": "⚠️ 2026-09-04（一課多題）：測試檔數 430 → 431——**輸入量上調，不是耦合變差**。新增 `core/progress.ts`（通過紀錄，自己一格 localStorage：匯出一份作品給同學，不該把「你第 3 題還沒過」一起送出去）＋ `tests/unit/core/progress.test.ts`；`core/lesson.ts` 的 `check` 升格成 `tasks`（`LessonTask`／`parseTasks`／`taskById`／`FREE_PRACTICE`）；登錄表多一顆 `task` picker；`ConsolePanel.offerNextTask`。🔴 它修的是一個**會在學生做對事情時說他錯**的裁判。\n"
}