{
  "_meta": {
    "note": "各自列宣告來源、而沒把膠囊算進去的測試檔數。\n處方：改用 `allCppComponents()` / `allCppProjections()`（它們含膠囊）。\n⚠️ 這個數字只准下降，而**下降必須是因為改用組裝函式**，不是因為那支測試被刪掉了。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "scanned": {
    "testFileCount": 433
  },
  "adHocAssembly": 0,
  "details": [],
  "note": "⚠️ 2026-09-04（跑之前先猜一下）：測試檔數 432 → 433——**輸入量上調，不是耦合變差**。新增 `core/predict.ts`（要問什麼 ＋ 這支程式是不是還是剛才那一支）＋ `tests/unit/core/predict.test.ts`；`LessonTask` 多一格 `predict`；`ConsolePanel.askPrediction`／`showPrediction`；`iterations.ts` 多三支（`isLoop`／`loopNodes`／`loopNodeById`／`loopRatio`——後者**不過濾**，因為揭曉一個預測時 `1` 與 `0` 都是正當的答案）。🔴 執行器多一個**可省略的** `beforeRun` async 鉤子——形狀抄 `pauseForUnrecognized`：**要停下來問人的事，用鉤子，不是把那件事搬進執行器**。沒注入時行為與從前逐字相同。\n"
}