{
  "_meta": {
    "note": "各自列宣告來源、而沒把膠囊算進去的測試檔數。\n處方：改用 `allCppComponents()` / `allCppProjections()`（它們含膠囊）。\n⚠️ 這個數字只准下降，而**下降必須是因為改用組裝函式**，不是因為那支測試被刪掉了。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "scanned": {
    "testFileCount": 429
  },
  "adHocAssembly": 0,
  "details": [],
  "note": "⚠️ 2026-09-04（裁判 ＋ 輸出 diff）：測試檔數 428 → 429——`check` 終於進了 `Lesson` 型別（在此之前 `parseLesson` 讀完就丟，**應用根本不知道它存在**），新增 `compareOutput`（逐行比對）· `ConsolePanel.showVerdict`（並排的 diff）· 組裝點的 `wireLessonCheck`。**輸入量上調，不是耦合變差**（directViewCalls 未動）。\n"
}