{
  "_meta": {
    "guard": "lesson-vocabulary",
    "measuredAt": "2026-09-07",
    "rule": "① 課文的程式碼裡的元件，要在這一課或之前被宣告過（骨架除外）。② 一顆元件【第一次】出現的那一課，課文要提到它在畫面上的名字。 🟢 2026-09-07 兩者都清到 0，於是從棘輪升成【硬性零】——這個檔只剩語料量。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "課數": 69,
  "畫面名字": 348,
  "note": "📈 **2026-09-19（第 197 刀：位置的相鄰一格 `cpp:pointer_step`）：新增一顆身分。**\n  `prev(it)`／`next(it)`——語料 `prev(` 10 處 / 8 支，每一處都是 `prev(X.end())`。\n  方向是**屬性**不是身分（元件代數 250）。\n  ⚠️ 它是「無名的積木」——句式是 `%1 的 %2`，**名字在下拉裡**\n  （與同族取端點那顆一模一樣）。判定寫在 `blocks-without-name-decisions.json`。\n\n📈 **2026-09-17（迭代器一族，管線 180）：語料變大，缺陷那幾格未動。**\n  新增 `cpp:container_find`（`find`／`lower_bound`／`upper_bound` 三個方法名\n  對同一顆身分，判準進 `how` 屬性）＋ 既有的取端點那顆多認反向的兩端。\n  🔴 而最大的一個修法只有**一個宣告**：自動型別的變數宣告沒有宣告\n  `forLoopPart`，於是 `for (auto it = v.begin(); …)` 整段掉進 raw code。\n\n  🟢 而**還沒教過的那一格沒有變**——新積木進的是 `cpp-advanced` 的 L2，\n  而那一軌的課文本來就在教有序容器。\n\n📈 **2026-09-16（第一百七十九刀，第二顆）**：`cpp:container_push_front`\n  ＋ 它的自證測。**輸入量上調，缺陷格未動。**\n\n📈 **2026-09-16（第一百七十九刀）**：新增 `cpp:container_pop_front`。\n  **新增一顆元件，輸入量本來就會長**——缺陷那幾格一筆未動。\n\n🔴 **2026-09-07 建檔**——使用者回報（附截圖，第 2 課）：\n  「我發現你似乎沒有先說明註解是什麼就使用註解，你能不能**有系統地找出類似的錯誤**\n  定**保證之後不會出現**這錯？」＋「我要說的**還有課文本身的用字**。」\n\n  那句「保證之後不會出現」就是這個檔。\n\n## ⚠️ 三個排除，而每一個都是量出來的\n\n```\n骨架的元件        227 → 63    程式的殼由骨架提供，課程刻意不教（前三課藏著它）\n提示文字不算      66 課全紅 → 判準太寬（學生認積木靠【上面印的字】）\n沒有中文名的積木  36 顆        那不是缺陷，是它本來就沒有名字可比對\n```\n\n> **一條護欄漏掉東西的方式，多半是它認得的範圍太窄；\n> 而它誤報的方式相反：範圍太寬。**\n\n## 🔴 為什麼是棘輪而不是硬性零\n\n今天有 171 筆。而**一條每天都紅的護欄，與一條沒有的護欄，\n在第二天之後是同一個東西**（`audit-both-hosts-persist` 的檔頭記過）。\n\n## 起點（2026-09-07 建檔時）\n\n```\n① 用了沒教過的        62   （建檔當下 63，而第 2 課的註解已經先修掉了）\n② 首次出現而沒說名字  109  （建檔當下 111，而 print 那一族改了兩筆）\n```\n\n⚠️ 同一天的第一刀：`print` 的積木字 **輸出 → 印出**（使用者拍板），\n課文 103 處「印出」不動——那一筆讓 43 課的用字對上了。\n\n🟢 **同一天就清完了，於是升成硬性零**（2026-09-07）：\n\n```\n補上「這一軌接在哪一軌後面」   62 → 24 · 109 → 17   ← 不是修，是把【誤報】拿掉\n逐課補課文與宣告               24 → 0  ·  17 → 0\n```\n\n🔴 **那個 `after` 是缺的宣告**：進階軌與兩條銜接軌都接在入門軌後面，\n而在此之前**沒有地方說得出這件事**（`order` 是選單順序，不是前置）。\n少了它，進階軌的第 1 課會被要求重新介紹「換行」與「建立變數」。\n\n> **一條護欄該用棘輪還是硬性零，答案在「今天量到幾筆」\n> ——而那個數字要在【扣掉每一個誤報之後】才算數。**\n\n🟢 **2026-09-13（入門軌課程修訂）：課數 66 → 69。**\n  使用者交來 28 條修正，其中三條各生出一課：\n  `02-記住資料` 拆成 **02-型別 ＋ 03-變數**、`15-多層迴圈` 拆出 **17-二維陣列**、\n  `16-函式與遞迴` 拆成 **18-函式 ＋ 19-遞迴**。\n\n  🔴 而拆課動到的不只是資料夾名字：**課文裡每一句「第 N 課」「上一課」都是一個引用**，\n  而它們在改號那一刻全部靜默過期——`grep` 出 24 筆逐筆對帳，\n  其中兩筆因為字串課從第 7 移到第 14 而變成**前向引用**（指向還沒教的東西）。\n\n  > **課程的順序是一份沒有型別的相依圖；改動它的時候，編譯器不會幫你。**\n\n\n📈 **2026-09-18（`cpp:deque_declare`，探索報告的 P1）：＋1 顆元件。**\n  `deque` 在此之前**沒有主人**，於是 `deque<pair<int,int>> q;` 掉進一般的變數宣告，\n  `type` 裝著一整串 `deque<pair<int,int>>`——那個容器因此沒有 `elemType`，\n  而 `q[0].first` 說「（不是一個結構）」。語料 **22 支**用 deque。\n  🔴 這件事早就寫在 `cpp:vector_push` 的執行器註解裡等人來修。\n\n\n📈 **2026-09-18（`cpp:var_declare_sequence` ＋ 走訪的容器換成接點）**：\n  新增一顆元件（結構化繫結，語料 16 處／13 支），並把範圍 for 的容器\n  從欄位換成接點（`d2[pt]`／`m[k]` 是運算式，語料 3 支）。\n\n📈 **2026-09-18（範圍那一族：兩端從字串屬性換成接點，管線 188）：語料變大，缺陷那幾格未動。**\n  10 顆範圍演算法的 `begin`／`end`（＋前綴和的 `dest`）從**字串屬性**換成**接點**，\n  ＋ 三顆新元件（`cpp:range_find`／`cpp:range_unique`／`cpp:range_remove`）\n  ＋ `cpp:container_iter` 多一個 `call` 屬性（`begin(a)` 這個自由函式形式）\n  ＋ 27 顆宣告式元件補上 `forLoopPart`（同一個病的第三次，這次整族一起補並加了一條護欄）。\n  🔴 而拆掉那個 regex 之後發現**結構早就在了**：容器與位置在這個直譯器裡是同一個形狀\n  （`interpreter/pointer.ts` 的 `isCellPointer`），`A+n` 的位置算術也早就有人做。\n  > **那條 regex 解析的東西，執行期早就會算了——它不是在補一個缺口，它是在繞過一個已經存在的機制。**\n\n📈 **2026-09-19（I/O 操縱子三顆，管線 189）：語料變大，缺陷那幾格未動。**\n  `cpp:io_flush`（`cout << flush`）· `cpp:io_mode`（`setw`／`setprecision`／`setfill`）·\n  `cpp:io_mode_number`（`fixed`／`scientific`），\n  ＋ 串流狀態（`WeakMap` 以 `ctx.io` 為鍵，**一次執行一份**）\n  ＋ `interpreter/decimal.ts`（十進位的四捨五入，收尾照 C 的**逢五取偶**）。\n  🔴 而命名是**詞彙表逼出來的**：第一版叫 `stream_*`，三個字都不在表裡；\n  換成既有詞（`io` ＋ `mode` ＋ `number`）之後名字反而對得上同族\n  （`cpp:io_sync`／`cpp:io_tie` 早就在那裡）。**只補了一個新詞：`flush`。**\n\n📈 **2026-09-19（第 199 刀：一排位元 `bitset`）：新增兩顆身分。**\n  `cpp:bits_declare`（一排固定長度的位元）與 `cpp:bits_fill`（整排歸零／設為 1／反轉），\n  ＋ 既有那顆「數 1 的個數」多一個**寫法**欄位（內建函式／一排位元）。\n  🔴 語料 `bitset<` **4 處 / 3 支**，而三支**全部**只缺這一族——\n  逐支讀過全文，其餘用到的東西今天都支援。\n  🟢 而索引的讀與寫**沒有新增任何機制**：一排位元在執行期就是 `type: array`，\n  於是既有那顆「取第幾格」連左值都免費沿用。\n\n📈 **2026-09-20（第 199 刀階段四：資訊隔離盲測）：再新增兩顆身分。**\n  `cpp:bits_is`（整排的三個問句）與 `cpp:bits_as`（整排換成整數／字串），\n  ＋ 既有那顆「整排改值」多一個**常態留空的位置**接點。\n  🔴 **三顆都是探索報告以「語料 0 處」拒絕過的**，而盲測十支裡\n  `to_ulong` 用了 4 支、帶位置的 `set(i)` 多支、`cout << bs` 1 支。\n  > **「語料 0 處」量到的是【這批語料的人怎麼寫】，\n  > 不是【這個寫法有多常見】——而它在兩刀內錯了四次。**\n📈 **2026-09-20（帶參數的巨集）：畫面名字 347 → 348。**\n  **母體變大，缺陷那幾格未動**——新增一顆身分（`cpp:define_func`）＋ 一支樹修復（`languages/cpp/lang/macro-expand.ts`）＋ 一支回歸網（`roundtrip-cpp-func-macro.test.ts`）。\n"
}
