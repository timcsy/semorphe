{
  "_meta": {
    "note": "⚠️ 2026-09-02（spec 171 · 主控台的表面）：export 數 674 → 675——**輸入量上調**。新增 `core/host/console-surface.ts`（`ConsoleSurface` ＋ `revealForOutput`）＋ `tests/unit/core/console-comes-back.test.ts`（4 條，**注入驗過會紅**）。🔴 那條規則寫在 `ConsolePanel` 的**寫入路徑**上，不是各宿主各寫一份——兩個宿主各寫一次的話，其中一個遲早會漏，而漏掉的症狀是**使用者看不到程式在說什麼**。\n⚠️ 2026-09-02（spec 171 · 主控台的表面）：共用檔數 260 → 261——**輸入量上調**。新增 `core/host/console-surface.ts`（`ConsoleSurface` ＋ `revealForOutput`）＋ `tests/unit/core/console-comes-back.test.ts`（4 條，**注入驗過會紅**）。🔴 那條規則寫在 `ConsolePanel` 的**寫入路徑**上，不是各宿主各寫一份——兩個宿主各寫一次的話，其中一個遲早會漏，而漏掉的症狀是**使用者看不到程式在說什麼**。\n⚠️ 2026-09-02（積木側的改名）：export 數 673 → 674——新增 `detectRename`（比對前後兩棵樹）。**輸入量上調。**\n🔴 積木那一側是**整棵樹重抽**，所以「舊名字是什麼」只有比對前後才知道——與流程那一側（就地改屬性）是**兩種修法**。\n⚠️ 而它**多於一格就不猜**：\n> **一個靠差分推測意圖的機制，在證據不只一種解釋時，應該什麼都不做——而不是挑一個看起來對的。**\n⚠️ 2026-09-02（改名跟得上參照 · 缺陷③）：export 數 668 → 673——**輸入量上調**。新增 `core/rename-variable.ts`（`isIdentifierProperty`／`isVariableReference`／`scopeOf`／`renameReferences`／`isRenamingADefinition`）＋ `tests/integration/rename-follows-references.test.ts`（5 條，**兩個注入驗過會紅**）。🔴 判準來自**宣告**：`kind: 'identifier'`（屬性自己說它裝名字）＋ `variableRef`（元件自己說它是參照）——不是一份「哪些欄位是名字」的清單。\n> **改名是一次【重構】，而設一個屬性只是設一個屬性——兩者在畫面上看起來一樣，直到你去跑它。**\n⚠️ 2026-09-02（改名跟得上參照 · 缺陷③）：共用檔數 259 → 260——**輸入量上調**。新增 `core/rename-variable.ts`（`isIdentifierProperty`／`isVariableReference`／`scopeOf`／`renameReferences`／`isRenamingADefinition`）＋ `tests/integration/rename-follows-references.test.ts`（5 條，**兩個注入驗過會紅**）。🔴 判準來自**宣告**：`kind: 'identifier'`（屬性自己說它裝名字）＋ `variableRef`（元件自己說它是參照）——不是一份「哪些欄位是名字」的清單。\n> **改名是一次【重構】，而設一個屬性只是設一個屬性——兩者在畫面上看起來一樣，直到你去跑它。**\n⚠️ 2026-09-01（spec 170 · US2 · 一格的頭只有一支產生器）：export 數 667 → 668——**輸入量上調**。新增 `ui/layout/cell-head.ts` ＋ `tests/unit/ui/cell-head.test.ts`；四條頭的產生器 5 → 1（`flow-toolbar`／`monaco-clipboard-bar`／`bottom-panel-tabs`／`quick-access-bar` 從「帶樣式的 class」變成純粹的**掛鉤**）。\n⚠️ 2026-09-01（spec 170 · US2 · 一格的頭只有一支產生器）：共用檔數 258 → 259——**輸入量上調**。新增 `ui/layout/cell-head.ts` ＋ `tests/unit/ui/cell-head.test.ts`；四條頭的產生器 5 → 1（`flow-toolbar`／`monaco-clipboard-bar`／`bottom-panel-tabs`／`quick-access-bar` 從「帶樣式的 class」變成純粹的**掛鉤**）。\n⚠️ 2026-09-01（spec 170 · 面板宣告的骨架）：export 數 658 → 667——**輸入量上調，不是判準放寬**。新增 `core/host/panel-spec.ts`（PanelSpec／PanelAction／PanelInstance／PanelContext）、`panel-registry.ts`（allPanels／panelsOfLayer／panelsFor／layersOf／assertPanelsSane）、`load-panels.ts`（`import.meta.glob`，與課程／範例／語言套件同一招）＋ `tests/unit/core/panel-registry.test.ts`（四條「要出聲」，**兩個注入驗過會紅**）。🔴 使用者：「面板可以模組化就更好了……我的重點是要好維護管理」——量到加一種投影要動 8 個既有檔。\n⚠️ 2026-09-01（spec 170 · 面板宣告的骨架）：共用檔數 255 → 258——**輸入量上調，不是判準放寬**。新增 `core/host/panel-spec.ts`（PanelSpec／PanelAction／PanelInstance／PanelContext）、`panel-registry.ts`（allPanels／panelsOfLayer／panelsFor／layersOf／assertPanelsSane）、`load-panels.ts`（`import.meta.glob`，與課程／範例／語言套件同一招）＋ `tests/unit/core/panel-registry.test.ts`（四條「要出聲」，**兩個注入驗過會紅**）。🔴 使用者：「面板可以模組化就更好了……我的重點是要好維護管理」——量到加一種投影要動 8 個既有檔。\n⚠️ 2026-09-01（偏好屬於這份文件）：export 數 656 → 658——**輸入量上調，不是判準放寬**。使用者：「**C++ 一開始要預設 C++ 吧**」——一份 C++ 的暫存檔而狀態列寫著 Arduino。🔴 前一刀讓每顆 picker 選完就寫回 **workspace 設定**，於是一次「我這個檔要用 Arduino」變成整個專案的設定，把「依副檔名自動判斷」蓋掉。> 一份偏好寫回去的範圍，不得大於它描述的東西。新增 `sync/doc-prefs.ts`（per-uri，住在宿主的 workspaceState，所有面板共用；形狀與 `view-state.ts` 一致，含存檔那一刻的身分搬遷）＋ 它的測試。\n⚠️ 2026-09-01（偏好屬於這份文件）：共用檔數 254 → 255——**輸入量上調，不是判準放寬**。使用者：「**C++ 一開始要預設 C++ 吧**」——一份 C++ 的暫存檔而狀態列寫著 Arduino。🔴 前一刀讓每顆 picker 選完就寫回 **workspace 設定**，於是一次「我這個檔要用 Arduino」變成整個專案的設定，把「依副檔名自動判斷」蓋掉。> 一份偏好寫回去的範圍，不得大於它描述的東西。新增 `sync/doc-prefs.ts`（per-uri，住在宿主的 workspaceState，所有面板共用；形狀與 `view-state.ts` 一致，含存檔那一刻的身分搬遷）＋ 它的測試。\n⚠️ 2026-09-01（版面交還給 IDE 執行）：共用檔數 253 → 254——**輸入量上調，不是判準放寬**。新增 `vscode/editor-layout.ts` 與 `tests/unit/vscode/editor-layout.test.ts`。\n⚠️ 2026-09-01（版面交還給 IDE 執行）：export 數 655 → 656——**輸入量上調，不是判準放寬**。使用者：「我現在要如何切換佈局？」→「我要的就是這個，你怎麼現在才聽懂？」🪦 上一筆把版面選單整個拿掉是**錯的**：把【繪製】交出去不等於把【談論它】也交出去。新增 `vscode/editor-layout.ts`（版面宣告 → VSCode 編輯器分組的純推導）＋ 它的測試，`panel.ts` 的 `applyEditorLayout`（攔下 layout 就地執行，不轉回 webview）。\n⚠️ 2026-09-01（面板獨立出來・第①②刀）：export 數 652 → 655（`vscodeProfileFor`／`openPanel`／`OPEN_FLOW_COMMAND`）——**輸入量上調，不是判準放寬**。使用者：「我原本的期待是能不能把面板都獨立出來？」`HostProfile.layers`（這個視窗畫哪幾層）、`vscodeProfileFor`、`openPanel`（一種投影一個 VSCode 面板，單例退場）、`consoleTarget`（終端機回給【正在等輸入的】那個面板，不是畫面上的那個）。\n⚠️ 2026-09-01（VSCode 四缺陷）：export 數 648 → 652（`boundaryAt`／`reduceAreas`／`normalizeShape`／`hostLayoutOptions`）——**輸入量上調，不是判準放寬**。使用者在 VSCode 回報「面板不是真的面板」「這拉不動」「說是四格其實根本不是」「沒有辦法區分骨架和顯示」。四刀：`grid-dividers` 的把手改用【位置】對映軌道（新增純函式 `boundaryAt`）、`applyLayout` 把宿主沒有的軌道【整條拿掉】而不是收成 0px、`layout-presets` 新增 `reduceAreas`／`normalizeShape`／`hostLayoutOptions`（版面清單問宿主）、VSCode 的 QuickPick 畫出分組與說明。另加三份測試檔與預檢的第五問。\n共用檔（`src/` 扣掉膠囊）裡的**空掉的註冊函式**與**同名的 export**。\n⚠️ 兩個數字的意義不同：空殼是**殼**（規範說有、實際是空的），重複是**兩份真相**（會漂移）。\n⚠️ 下降必須是「刪掉了殼／合併了兩份」，不是「把檔案排除在掃描外」。\n⚠️ 2026-08-25：「控制項離開積木面板」那一刀新增了控制項登錄表（`core/host/controls.ts`）與宿主那側的接手，**語料因此變大——不是退步**。\n⚠️ 2026-08-25：網頁版狀態列改成「文字項目 ＋ QuickPick」（`ui/toolbar/quick-pick.ts`、`ui/layout/status-bar-controls.ts`），語料因此變大——**不是退步**。\n⚠️ 2026-08-25：新增 `core/projection/diagnostic-projection.ts`（4 個 export）——語料變大。\n⚠️ 2026-08-25「行動版 ＝ 第三個渲染器」：刪除 `ui/toolbar/{topic,style,locale}-selector.ts` 與 `setupSelectors`／`#mobile-selector-parking`／「把選擇器搬進漢堡選單」那整段——那四顆下拉變成 `ControlState` 的渲染器。🔴 **語料縮水而正當**：違規數未動（跨層 5 筆、直接呼叫視圖 70、組裝點豁免 10），已確認是「東西真的被刪了」，不是「掃描器不再吃到它」。\n⚠️ 2026-08-25「行動版的選單改成往下鑽的 QuickPick」：刪除 `ui/toolbar/mobile-menu.ts` 與其測試、`renderSheetControls`、`mobile-sync-btn`。🔴 **語料縮水而正當**——已確認是「東西真的被刪了」。\n⚠️ 2026-08-25：576 → 577（`vscode/panel.ts` 多一個 `showConsole` 匯出）。語料變大。\n⚠️ 2026-08-25：新增 `ui/alt-layout-block.ts`（依 extraState 換佈局的建構子）。語料變大。\n⚠️ 2026-08-25：共用檔 237 → 238、export 578 → 580，新增 src/migrations/block-shape-changes.ts\n   （`ShapeChange`／`SHAPE_CHANGES_V12`／`staleShapeIn`）。語料變大。\n⚠️ 2026-08-25：export 580 → 581（`ShapeChange` 的 v13 表）。語料變大。\n⚠️ 2026-08-25：581 → 579。`C_COMPOUND_ASSIGN_INPUTS` 與 `_EXPR_INPUTS` 兩個匯出刪除\n   ——唯一消費者是那兩顆積木的命令式定義（`retire-imperative-block` §4 的孤兒注入欄位）。\n⚠️ 2026-08-25：579 → 580。`SHAPE_CHANGES_V14`（第四筆：`cpp:var_assign`，2026-08-25）。語料變大。\n⚠️ 2026-08-26：580 → 581（`SHAPE_CHANGES_V15`）。語料變大。\n⚠️ 2026-08-26：581 → 583（`SHAPE_CHANGES_V16` 與 `mapInsertSorted`）。語料變大。\n⚠️ 2026-08-26：583 → 584（`isAddressable` 性狀查詢）。語料變大。\n⚠️ **語料欄** export 584 → 583（2026-08-26）：`cpp_var_declare_expression` 的命令式定義退場，**連帶整個 `setLanguageInputNames`\n  注入契約刪除**——它從十二個插槽名縮到一個，而最後一個的消費者就是那顆積木。\n  ⚠️ 那道「沒注入就拋錯」的守衛也一起走：**它守的是一份空契約**。\n- ⚠️ **語料欄**（2026-08-26）：slot 詞彙落地——`UnderstandingLayer`／`LAYER_ORDER`／\n  `viewsByLayer()`／`TAB_LAYERS` 加進來，五個面板各宣告一格 `layer`。**語料變大而正當。**\n⚠️ 2026-08-26：export 數 586 → 589，新增 `describeUnknownPause`／`describeUnknownContinued`／`describeSetVariableRefused`（暫停那條路的三句使用者文案）。**語料變大，不是退步**。\n⚠️ 2026-08-26：第七十六條護欄（audit-execution-replay）。export 數 589 → 590，新增 `describeInterventions`（這次執行有人插手過的那一句）。**語料變大，不是退步**。\n⚠️ 2026-08-26：新增 `core/flow/vocabulary.ts`（流程視圖的詞彙：標題／位置名／值的顯示文字）＋ 第七十八條護欄。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：第八十條護欄（audit-flow-connect：流程視圖上的線只有父子關係）＋ `core/flow/connect.ts` ＋ 流程面板的拉線。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：(d) palette ＋ (e) 重排。**輸入量變大**。\n⚠️ 2026-08-26：第八十一條護欄（audit-layout-presets：佈局預設列的是層不是面板）＋ `core/host/layout-presets.ts` ＋ `applyLayout`／`setDividerVisible`／`layout` picker。**輸入量變大，不是退步**。\n⚠️ 2026-08-27：語料變大——新增 `core/flow/presets.ts`（把工具箱的 `extraState` 翻譯成流程視圖的骨架）與它的單元測試。**輸入量，不是耦合。**\n⚠️ 2026-08-27：語料變大——新增 `core/flow/layout-key.ts`（手拖佈局的鑰匙：內容／行號／路徑三把一起）＋ 它的單元測試與第八十二條護欄。**輸入量，不是耦合。**\n⚠️ 2026-08-27：語料變大——存檔 v17 新增 `flowLayout`（流程節點手放過的位置），以及 `layout-key.ts` 的 `matchByKeys`／`keysOfNodes`／`PlacedEntry`（編輯時的配對與**存檔還原**共用同一份實作，不另寫一份）。**輸入量，不是耦合。**\n🔴 **2026-08-27：語料縮水而正當**——`src/ui/layout/status-bar.ts` 刪除（63 行，一個 class）。\n   它畫的是**四格**（language｜風格｜課程｜語系｜Sync），而畫面上是**七格**（那七格由 `status-bar-controls.ts` 畫，2026-08-25「控制項離開積木面板」的產物）。\n   查證：**零產品呼叫者、零測試引用**。\n   > 一個渲染器，畫的東西與畫面上的不一樣，而沒有人發現——因為沒有人在看它。\n   ⚠️ 已逐項核對數字對得上「刪掉一個檔、一個 class」，**是東西真的被刪了，不是掃描器不再吃到它**。\n⚠️ 同一輪語料變大的部分：新增 `tests/unit/ui/status-bar-language-cell.test.ts`。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。",
    "measuredAt": "2026-08-25"
  },
  "scanned": {
    "fileCount": 260,
    "exportCount": 675
  },
  "husks": 0,
  "duplicates": 0,
  "details": {
    "husks": [],
    "duplicates": []
  },
  "note": "⚠️ 2026-09-02（spec 171 第三刀 · hello 一直閃）：export 數 674 → 675——**輸入量上調**。新增 `isDocumentWriter`（`vscode-profile.ts`）＋ `tests/unit/vscode/stream-views-are-not-writers.test.ts`（4 條，兩個方向的注入都驗過會紅）。🔴 主控台與變數那兩個視圖不再是文件的寫入者——它們的樹是空的，而它們曾經把裸骨架寫回檔案。\n⚠️ 2026-09-02（spec 171 · US3）：export 數 673 → 674——`panel.ts` 新增 `CONSOLE_VIEW_ID` 與 `registerConsoleView`（panel 區的 WebviewView），而 `editor-layout.ts` 整支已在同一刀退場。**輸入量上調。**\n⚠️ 2026-09-02（spec 171 · 十字退場）：共用檔數 261 → 260、export 數 675 → 673——**語料下修，因為東西真的被刪了**：`src/vscode/editor-layout.ts`（142 行的「版面 → 編輯器分組」推導）與 `tests/unit/vscode/editor-layout.test.ts` 整支退場。三張版面全是純欄之後，`reveal(ViewColumn)` 就排得出來——不需要那份推導，也不需要 Theia 沒有的 `setEditorLayout`。🔴 已確認不是「掃描器不再吃到它」：那兩個檔在 `git status` 裡是 `D`。\n⚠️ 2026-09-01（spec 169：每個槽自己選視圖）：新增 `core/host/slot-assignment.ts`、第九十九條護欄與兩支測試檔。**輸入量上調，不是缺陷上調。**\n⚠️ 2026-09-01（spec 168：版面四張示意圖）：編輯區從 `SplitPane` ＋ 巢狀 flex 換成 CSS Grid，新增 `layout/grid-dividers.ts` 與 `tests/unit/core/layout-presets.test.ts`，`layout-presets.ts` 多了三個純函數（gridTemplateAreas／thumbnailCells／occupiedLayers）。**全部是輸入量上調，不是耦合或殼變多**——`split-pane.ts` 同時退場。\n⚠️ 2026-08-31：export 數 640 → 641——`core/scaffold-nodes.ts` 新增 `unwrapSkeletonFrame`（換骨架時把舊骨架的框拆掉）。**輸入量上調，不是殼變多**。\n⚠️ 2026-08-28：語料變大——新增 `core/lesson.ts`／`core/load-lessons.ts`／`e2e/lesson-pins.spec.ts`／兩條護欄（第八十四、八十五條）＋ 6 份 `track.json`。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28：語料變大——骨架宣告化（`core/shell.ts` ＋ `languages/cpp/skeletons/{main,none,index}`）。四段骨架從 `cpp-scaffold.ts` 的寫死字串搬進 JSON，而 `skeleton` 的值域從 `main|none` 打開成鷹架 id。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28：語料變大——新增第八十七條護欄（audit-templates：範例）＋ `core/template.ts`／`core/load-templates.ts`／`e2e/templates.spec.ts`＋ 6 份 `templates/*`。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28：語料變大——鷹架控制項（兩個軸：骨架／顯示）＋ Python 自己的骨架宣告 ＋ `e2e/scaffold-code-complete.spec.ts`。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28 上調的是【入口條件】（合成量），不是缺陷數：新增 src/ui/panels/ghost-drag-strategy.ts（ghost 鷹架的拖曳策略）。缺陷欄一筆未動。\n⚠️ 2026-08-28 上調的是【入口條件】（合成量），不是缺陷數：Arduino 的骨架宣告（`shells/arduino.json`、`entryFunctions`、`skeletonPresent`、`canHideScaffold`）。「哪一塊是骨架」原本寫死在六處 `name === 'main'`，現在問宣告。缺陷欄一筆未動。\n⚠️ 2026-08-30：共用檔 251 → 252、export 637 → 640——`core/scaffold-nodes.ts`（`scaffoldNodeIds`／`scaffoldComponentIds`）＋ `core/lesson.ts` 的 `scaffoldModeOfDepth`。輸入量變大。"
}
