{
  "_meta": {
    "note": "📈 **2026-09-19（64 位元整數）：輸入量上調，缺陷格未動。**\n  新增 `interpreter/int64.ts`（「|v| ≤ 2^53 是 number，超過是 bigint」的不變式）\n  ＋ `core/scalar-types.ts`（拼法 → 執行期種類的登記處，核心給機制、語言給資料）\n  ＋ `languages/cpp/scalar-types.ts`（C++ 那份資料，**在模組頂層**）。\n  🔴 修的是「`long long` 在這個直譯器裡是一個 double」——\n  `ans*x%P`（乘積 1e18）失真、`nt>>=1` 被截成 32 位元、\n  `ll n = 2e9` 印成 `2e+09`、函式回傳值完全沒有依宣告型別轉換。\n  🟢 跨層相依未動（5）：`scalar-types.ts` 住在核心，資料由語言套件推進來。\n  讀數：內容真的不同 **5 → 3**（累計 7 → 3）、一致 147 → 151。\n\n共用檔（`src/` 扣掉膠囊）裡的**空掉的註冊函式**與**同名的 export**。\n⚠️ 兩個數字的意義不同：空殼是**殼**（規範說有、實際是空的），重複是**兩份真相**（會漂移）。\n⚠️ 下降必須是「刪掉了殼／合併了兩份」，不是「把檔案排除在掃描外」。\n\n📉 **2026-09-20（第 205 刀：層級樹退場）：輸入量【下調】，而這是刪掉東西的正常結果。**\n  使用者：「我們現在已經有課程了，應該就沒有需要再用 levelTree 了吧」。\n  刪掉 `core/lesson/level-tree.ts`（六支匯出）與 `ui/toolbar/level-tree-selector.ts`，\n  並把 `enabledBranches` 從 app 狀態、存檔（v24）、VSCode 設定裡拿掉。\n  ⚠️ **下調不是縮水**：這幾格量的是「掃到多少東西」，而東西真的變少了。\n  🟢 判定格（耦合數、空殼數、跨層相依）**一格未動**。\n\n📈 **2026-09-21（第 211 刀：脫下原文還站得住嗎）：輸入量上調，判定格未動。**\n  新增 `src/core/lift/honest-degradation.ts`（一個 `syntax_error` 的節點，\n  脫下 `rawCode` 之後還原不出使用者寫的字，就整段換成 `raw_code`）\n  ＋ `comma-block.test.ts` 的 `TIE_CORPUS`（一段帶 main 的真語料）。\n\n📈 **2026-09-21（第 211 刀）：共用檔 301 → 302、export 798 → 800。空殼與重複仍是 0。**\n  新增 `src/core/lift/honest-degradation.ts`（匯出 `Reproducer`／`provideReproducer`\n  ／`degradeWhatCannotStand`）——「脫下原文還站得住嗎」那一問。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "scanned": {
    "fileCount": 302,
    "exportCount": 800
  },
  "husks": 0,
  "duplicates": 0,
  "details": {
    "husks": [],
    "duplicates": []
  },
  "note": "📈 **2026-09-19（解析器解錯的兩種形狀）：輸入量上調，缺陷格未動。**\n  新增 `core/lift/ast-repairs.ts`（「換一棵樹」的登記處）\n  ＋ `languages/cpp/lang/misparse.ts`（`!K--` 與 `a<b && c>-d` 兩條重寫）。\n  六個新 export 全部有呼叫者（空殼 0、重複 0 都未動）。\n\n📈 **2026-09-17（迭代器的 round-trip 那一關）：語料變大，缺陷那幾格未動。**\n  新增 `tests/integration/roundtrip-cpp-iterators.test.ts`（10 支）\n  ＋ `interpreter-matches-compiler` 加 14 條最小重現（總計 60/60）\n  ＋ `runtime/map.ts` 的 `compareValues`（有序容器的比較規則，排序與查找共用）。\n  🔴 那一輪抓到三個【疊在同一支語料上】的缺陷：容器不記元素型別、\n  比較規則不認一對值（於是有序容器根本沒有排序）、查找把每一格拆開只比第一個。\n\n  🟢 空殼與重複都還是 0。新增的那一個 export 是 `compareValues`——\n  **有序容器的比較規則只有一份**，否則「放進去的順序」與「找出來的位置」會對不上。\n\n📈 **2026-09-17（位置＝實體式指標，管線 180 的前兩顆）：語料變大，缺陷那幾格未動。**\n  新增 `src/interpreter/pointer.ts`＋三個註冊點各補一條位置分支。\n  🔴 修的是兩個**靜默的**缺陷：`++p` 把指標寫成一個 double、`p != q` 恆等於「相等」。\n\n  🟢 空殼與重複都還是 0。\n\n📈 **2026-09-17（重複性與有序性那兩軸）：export 數 756 → 757，缺陷格未動。**\n  新增 `core/component/container-templates.ts` 的 `propsForContainerTemplate`\n  ——**樣板名自己附帶的屬性**（`multiset` → 不去重、`unordered_map` → 不保證走訪順序）。\n  🔴 它不是一個新機制，是把一個**本來要寫成第三個 `if` 的東西**放回資料那一側：\n  共用的 lift 檔裡 `templateName === 'map' ? … : templateName === 'pair' ? …` 旁邊\n  逐字寫著「第三個出現時該收斂成『從宣告推導』」。\n\n📈 **2026-09-16（模糊測試那一輪）**：新增 `cpp:container_push_front`、\n  `interpreter/clone.ts`、`interpreter/aliases.ts` 與兩支回歸測試檔。\n  **輸入量上調，缺陷格未動。**\n\n📈 **2026-09-16（第四輪）**：新增 `src/interpreter/receiver.ts`\n  （把被壓成文字的接收者 `d2[3]` 解開，34 個元件共用一處）。\n  **輸入量上調，缺陷格未動。**\n\n📈 **2026-09-16（清那 90 支，第二輪）**：新增 `src/interpreter/aliases.ts`\n  （名字的別名表，`#define x first` 那一族）。**輸入量上調，缺陷格未動。**\n\n🟢 **2026-09-15（只差一個空白也要說得出來）：export 數 748 → 749，缺陷格未動。**\n  新增 `core/lesson/lesson.ts` 的 `describeLineDiff`。\n  🔴 整班回饋裡兩個學生分別逐字說：「第四題卡最久，因為一直卡在中間的空格」\n  「就算只是一個空格沒打到都不行」——而在此之前裁判遇到「這一行內容不同」\n  **什麼都不說**：畫面上是兩行看起來一模一樣的東西並排。\n\n  > **一個「你錯了」而看不出錯在哪的回饋，教的是「這個工具有脾氣」。**\n\n  ⚠️ **只在【一行】不同時說**：兩行以上就指不出「差在哪」，\n  而一個講錯位置的診斷比沒有診斷糟。\n\n🟢 **2026-09-15（highlight 差一行）：輸入量上調，耦合數字未動。**\n  新增 `core/projection/patch-shift.ts` ＋ `tests/unit/core/patch-shift.test.ts`。\n  🔴 使用者：「積木跟程式碼好像 highlight 的地方對不上，會差一行」——\n  對照表描述的是【補之前】那份文字，而補丁器在最上面塞了一行 `#include`。\n  ⚠️ 而組裝點那個 wrapper **早就算過同一個位移**（`linesDelta`，給游標保位用）。\n  > **同一個位移被算出來給 A 用，而 B 也需要它——那是「已經知道答案而沒有交出去」。**\n\n🟢 **2026-09-15（把「差在哪」講成一句話）：export 數 745 → 746，缺陷格未動。**\n  新增 `core/lesson/lesson.ts` 的 `summarizeComparison`。\n  🔴 使用者轉述老師自己的日常：「因為之前是輸出四行，所以到這題很常會被\n  判斷多一行，**我都要跟學生說把最後一行拿掉**才會通過」。\n  而 `showVerdict` 的檔頭早就寫著「回饋要說的是『你少了第 3 行』」\n  ——它自己沒做到：畫面上只有一張並排的表，多的那一行寫著 `—`。\n\n  > **一句每天都要由老師口頭翻譯的回饋，就是那個回饋還沒有寫完。**\n\n  ⚠️ 而它**只在「行數對不上而其餘每一行都一樣」時說話**：中間有一行不同的\n  時候「多一行」是錯的診斷，而學生會照著它刪掉不該刪的東西。\n\n🟢 **2026-09-14（名字也要驗）：輸入量上調，缺陷格未動。**\n  新增 `core/identifier-syntax.ts`（語言套件宣告、核心讀）\n  ＋ `core/component/param-check.ts`（`ParamSpec.kind` 的唯一判定處）。\n  🔴 使用者轉述學生：「變數名稱會寫成數字，Semorphe 竟然還可以接受」\n  ——實測產出 `int 123 = 16;`，主控台零錯誤、畫面零標記。\n  而 97 個參數宣告了 `kind: 'identifier'`，**沒有任何一處讀 `kind`**。\n  ⚠️ 同一週第三個同族缺陷（slots 的 min/max、形態軸 role、然後是這個）。\n\n🟢 **2026-09-14（快車道 `/next/`）：輸入量上調，空殼與重複都還是 0。**\n  新增 `core/base-path.ts`（`BASE` ＋ `withBase`）——站台前綴的唯一來源。\n\n🟢 **2026-09-14（槽的宣告要真的有人讀）：輸入量上調，缺陷那幾格一筆未動。**\n  新增 `core/component/slot-check.ts`（`allowed`／`min`／`max` 的唯一判定處）\n  ＋ `ComponentDefJSON.positions`（文法那一軸，與 `role` 分家）\n  ＋ 第一百二十六條護欄；`flow/connect.ts` 的重複判定刪掉 20 行。\n\n🟢 **2026-09-13（面板登錄表接上了）：共用檔數 276 → 281（五份 panel.ts）**——**輸入量上調**。\n\n  spec 170 的 T001–T006 蓋好登錄表就停住了，而**那個停住看不見**：\n  `src/panels/` 零份宣告、`loadPanels()` 零個產品呼叫者、\n  README 引用的那支測試不存在。登錄表自己的測試檔頭寫著：\n  「**一個沒有人宣告的登記處【就是殼】，而它綠得跟真的一樣。**」\n\n  這一輪：五份 `panel.ts`（code／flow／blocks／console／variables）\n  ＋ `loadPanels()` 接上組裝點（健檢結果印出來）\n  ＋ 讓「狀態」那一層的分頁**真的去問登錄表**（名字與順序）\n  ＋ 第一百二十五條護欄 `panel-mount-migration`（棘輪，`mount` 還在組裝點的只准變少）。\n\n  ⚠️ **`mount` 沒搬是有理由的**，而那是 `app-shell.ts` 自己留下的：\n  「一次抽象如果同時搬走「容器」與「內容」，它壞掉時你分不出是哪一半。」\n  🔴 所以由 `mountedByShell` 說出來、由棘輪數著——\n  **一個分階段的搬遷，如果沒有東西數著還剩幾個，它會停在第一階段。**\n\n🟢 **2026-09-13（解掉 core ↔ interpreter 的環）：共用檔數 275 → 276**——**輸入量上調**。\n  新增 `src/core/execution.ts`（`ExecutionStatus`／`ExecutionSpeed`／`RuntimeType`／`StepInfo`\n  從 `interpreter/types.ts` 搬過來）。⚠️ 它 0 個 export 算進 exportCount\n  ——那四個都是 `type`，而護欄的正則只數 `function|const|class`。\n\n🪦 **2026-09-13（資料夾整理）：語料【縮水】而它是正當的。**\n  使用者：「我想要把這專案資料夾結構重新整理的好看一些……準則是要說得出\n  這資料夾或檔案與實際內容結構的關係，否則可能就是放錯地方。」\n\n  🔴 **`src/scripts/` 整個退場**——那兩支建置工具搬到 `tools/`。\n  而「它不屬於 src」這件事 **`tsconfig.json` 早就自己招了**：\n  它寫著 `exclude: [\"src/scripts\"]`。\n\n  > **一個必須被排除在建置之外的目錄，它的位置就是錯的。**\n\n  ＋ 刪掉 `src/style.css`（8.8KB、零引用——`index.html` 載的是 `src/ui/style.css`）。\n\n  🟢 **已逐項核對**（這條護欄的規矩是「下降必須是刪掉了，不是排除在掃描外」）：\n\n  ```\n  共用檔數 277 → 275   離開 src/ 的那兩個 .ts，正好 2\n  export 數 741 → 730  verify-component-paths 的 12 個 export 減 1\n                       ——護欄的正則是 /export (?:async )?(?:function|const|class)/，\n                       **不數 `interface`**，而它有一個 `export interface ComponentPathReport`\n  ```\n\n  ⚠️ 那個「12 減 1」不是誤差，是判準：**核對到對不上為止，才知道縮水是刪掉還是漏掃**。\n\n🟢 **2026-09-12（題目在課文裡要有入口）：export 數 739 → 741**——**輸入量上調**。\n  使用者：「我在想要不要每個題目都有一個在編輯器練習的按鈕跟著？這樣大家比較能 follow 到」。\n\n  🔴 而查證之後它比那句話說的更需要：`tools/build-lessons/` 裡 **`tasks` 一次都沒出現過**\n  ——課文的靜態頁從頭到尾沒有提過這一課有幾題，整頁只有底下一顆「在編輯器打開這一課」。\n\n  > **一份教材如果它的練習題只存在於另一個畫面裡，\n  > 那些練習題對讀教材的人來說不存在。**\n\n  `core/lesson.ts` 多兩支：`taskIdFromQuery`（讀 `?task=`）與 `editorHref`（寫網址）\n  ——它們與 `lessonIdFromQuery`／`lessonDocHref` 是同一組，住在一起的理由也一樣：\n  **兩邊分開住的話，中文課名的 encode 遲早會不一樣。**\n  ⚠️ 而那個網址在此之前就是**在 `render.ts` 裡手寫的**，正是「兩邊分開住」。\n\n  ＋ 第一百二十三條護欄 `lesson-task-entry`（棘輪，今天 135 / 137）。\n\n🟢 **2026-09-10（中鍵推畫面）：共用檔數 276 → 277、export 數 737 → 739**——**輸入量上調**。\n  學生回饋（附截圖）：「就是有時候遇到這種其實蠻難滑的（積木），要不要設定\n  按滑鼠中鍵可以移動我看的地方然後不會動到積木」。那張截圖是一段 DFS，\n  `陣列 p 的第 [ u ] 格 = v` 橫著長到出畫面。\n\n  🔴 **Blockly 早就能拖背景平移——而那張畫面上背景不存在**：每一個像素都是積木，\n  於是「先找一塊空白」這一步本身就失敗了。\n\n  > **一個要求使用者先找到空白處的手勢，在畫面最滿的時候最沒有用\n  > ——而那正是他最需要平移的時候。**\n\n  新增 `ui/panels/middle-drag-pan.ts`（capture 階段攔中鍵，Blockly 的手勢收不到它）\n  ＋ 第一百二十二條護欄 `middle-drag-pan`。\n  ⚠️ 中鍵的三個瀏覽器預設行為都要擋（Windows 自動捲動／X11 貼上主選取區／auxclick 開新分頁）。\n\n🟢 **2026-09-10（打暗要說得出換哪一課）：export 數 735 → 737**——**輸入量上調**。\n  使用者轉述學生的畫面：他沒有換課，一路往下寫。第 1 課只開了三顆元件，\n  而他在宣告變數——**他自己寫的每一塊積木都被打暗**，而畫面上沒有一個字\n  說為什麼、也沒說怎麼辦（出路藏在最下面那條狀態列的一格下拉裡）。\n\n  ⚠️ 同一個問題被問過兩次：2026-08-28 使用者自己看著畫面問過\n  「為何積木變這麼暗？」。那一次修的是「不該暗的別暗」，\n  這一次是「該暗的**沒說為什麼**」。\n\n  > **一個純視覺的訊號，如果它在使用者【正當地往前走】的時候觸發，\n  > 那它讀起來不是提示，是故障。**\n\n  新增 `core/lesson-suggest.ts`（哪一課涵蓋得了這些元件——**算出來的**，\n  每一課都宣告了自己的 `components`）＋ `ui/lesson-nudge-bar.ts`（一條指路的線）\n  ＋ 第一百二十條護欄 `lesson-nudge`。\n\n🪦 **2026-09-08：語料【縮水】而它是正當的——`edit-tally` 那一條線整條退場。**\n  共用檔數 275 → 274、export 數 742 → 735\n\n  使用者：「我後來覺得弄這條好像沒必要，而是可以在**設計課程的時候提醒\n  可以開始用程式碼寫**」。\n\n  🔴 而那讓「這一課改了哪一邊」的計數**回到零消費者**——所以它跟著走：\n  > **一個沒有消費者的機制，是最差的那一個選項**（我自己在 history/222 寫的）。\n\n  刪掉的：`src/core/edit-tally.ts` · `previewBar`（四個檔）·\n  三個測試檔（unit ×2、e2e ×1）。\n  🟢 **已逐項核對縮水的數字與刪掉的東西對得上**（1 個 src 檔、3 個測試檔、\n  7 個 export）——是「東西真的被刪了」，不是「掃描器不再吃到它」。\n\n🟢 **2026-09-08（拆輪子的橫條——一面鏡子）：共用檔數與 export 數**——**輸入量上調**。\n  `ControlOption` ＋ `QuickPickItem` 多一格 `previewBar`（兩色橫條，**上面沒有字**）；\n  `edit-tally.ts` 多 `tallyCurve`／`barShare`；`semantic-wave.ts` 多 `viewLabel`；\n  新測試 `tests/unit/ui/quick-pick-bar.test.ts`（happy-dom）。\n  🔴 它是 `edit-tally` 的**第一個消費者**——那份計數在此之前零人讀。\n  > **一面鏡子不說話**：橫條裡不得有任何文字節點，`title` 只准是數字\n  > ——「還」「才」「只」「已經」「你」一個都不准（兩者都有測試）。\n  ⚠️ 橫條放在**章節清單**而不是題目格：16 條排成一列**就是曲線**，\n    而學生要**主動打開**才看得到——一面你自己走過去照的鏡子，與掛在你面前的不同。\n\n🟢 **2026-09-07（關卡的目標 ＋ 規格頁）：export 數 738 → 739**——**輸入量上調**。\n  `LessonTask` 多一格 `stepBudget`（⚠️ **一句話不是一道門**：超過了照樣算通過）\n  ＋ `describeBudget`；`tools/build-lessons` 多一頁**規格頁**\n  （`targets/*.json` 的投影，8 塊板子的腳位與常數，**每一筆附上游來源**）。\n  🔴 那個目標數字是**量出來的**（`tests/probes/step-budget.test.ts`）：\n    排序版 57 步、兩兩比 154 步——訂 80。\n  🟢 而規格頁**不可能與宣告不一致**，因為它是投影\n    ——`audit-lesson-pages` 多兩條驗它（注入「不印上游來源」→ 紅）。\n\n🟢 **2026-09-07（效率的回饋）：共用檔數與 export 數**——**輸入量上調**。\n  新增 `core/steps.ts`（跑了幾步 ＋ 與上一次比）＋ `tests/unit/core/steps.test.ts`；\n  `LessonTask` 多一格 `compareSteps`。\n  🔴 它補的是一個**教材已經在教而沒有回饋**的落差：`cpp-advanced` 第 1 課\n    逐字「n ≤ 100000 這句話**直接決定你可以用什麼做法**」，\n    而在此之前 O(n²) 與 O(n log n) 的兩份解答**在畫面上長得一模一樣**。\n  ⚠️ 「一步」＝直譯器走過一顆語義節點一次——**那不是課本的定義**，\n    而選它的理由是誠實：它是執行器真的知道的東西。\n  ⚠️ 而它**只在課程宣告的題目上說話**，且**說的是任務不是這個人**。\n\n🟢 **2026-09-07（除錯題 `kind: 'debug'`）：export 數 733 → 735**——**輸入量上調**。\n  新增 `starterFor`（`starters/<題目 id>.<ext>`）＋ `TaskKind`／`TASK_KINDS`\n  ＋ `app.ts` 的 `seedDebug`；`audit-lessons` 多三條規則（起點要有 ·\n  起點與解答不得同一題 · 課文不得洩漏起點）＋ 四支注入。\n  🔴 它補的是研究點名的那一半：blocks→text 的工具「往往**沒有處理**\n    從拖拉到打字與**除錯**的概念轉變」——而診斷系統早就在了，\n    缺的只是**一段壞掉的程式**。\n  ⚠️ 起點與正解**分開放**：\n  > **一個資料夾如果同時裝著「該對的」與「該壞的」，\n  > 那麼任何一條對它的檢查都必須先問「這一份是哪一種」\n  > ——而那個問題的答案不在檔案裡。**\n  🟢 而 `e2e/lessons.spec.ts` 多一支**驗相反方向**的：起點必須**跑不出**答案\n    （注入「把起點改成正解」→ 當場紅）。\n\n🟢 **2026-09-07（拆輪子的曲線）：共用檔數與 export 數（`edit-tally` ＋ 三支新導出）**——**輸入量上調**。\n  新增 `core/edit-tally.ts`（「這一課你改了哪一邊」，**只留在本機**）\n  ＋ `semantic-wave.ts` 的 `curveOf`（跨課的曲線）\n  ＋ `load-lessons.ts` 的 `viewForLesson`（**階梯**語意）\n  ＋ 第一百一十三條護欄 `audit-training-wheels-curve` ＋ 兩份單元測試。\n  🔴 `Track.view` ＋ `LessonPins.view`：軌道給預設、課在**轉折點**覆寫\n    ——形狀與 `scaffold` 那一軸一樣，**刻意不發明區間語法**。\n  ⚠️ **護欄當場抓到第一版的設計缺陷**：`pins.view ?? track.view` 會讓\n    轉折點的**下一課退回預設**（`blocks blocks blocks compare blocks…`）。\n  > **一條曲線的宣告，寫的是【轉折點】而不是【每一格的值】\n  > ——而「沒宣告就退回預設」會把轉折變成一根刺。**\n  🟢 而它抓得到，是因為**護欄與產品共用同一支** `viewForLesson`。\n\n🟢 **2026-09-06（spec 177：匯出是一份帶得走的作品）：共用檔數 272 → 273、export 數 721 → 726**——**輸入量上調**。\n  新增 `core/portable.ts`（拆檔／組回來／預設名）＋ 第一百一十二條護欄\n  `audit-portable-export` ＋ `tests/unit/core/portable.test.ts`\n  ＋ `e2e/portable-export.spec.ts`（**匯出／匯入的第一批 e2e**——在此之前是 0 支）；\n  `LanguagePack` 多一格 `fileExtension`，兩個語言套件各宣告一次。\n  🔴 匯出從「一份只有我們打得開的 `.json`」變成一個容器：\n    `<作品>.cpp`（原始碼，任何編輯器打得開）＋ `.semorphe/<同名>.json`（側檔）。\n  > **一份匯出如果程式碼不是第一級的檔案，那份匯出把投影當成了真相。**\n  ⚠️ 新增 npm 依賴 `fflate`（8KB、無相依）——**它跟著產品一起打包**，\n    而 `e2e/offline.spec.ts` 仍然綠（零外部請求）。\n\n🟢 **2026-09-06（spec 176：型別的下拉跟著程式長）：共用檔數 271 → 272、export 數 717 → 721**——**輸入量上調**。\n  新增 `core/types-in-use.ts`（`typesInUse` ＋ `withTypesInUse` ＋ 餵樹的登記處）\n  ＋ 第一百一十一條護欄 `audit-types-grow-with-code` ＋ 端對端 `types-in-use-e2e`。\n  🔴 它兌現 vision「開放值域的欄位要寫得出來」**最後一條**未做的驗收：\n    「選項跟著程式長——用過的型別會出現在其他積木的下拉裡（**導出的，不是宣告的**）」。\n  ⚠️ **它修的不是正確性，是可發現性**：學生宣告過 `struct Point` 之後，\n    那個型別**打字寫得出來**（認不得的值不會被換掉），而他**選不到**。\n  > **一份「這個欄位可以填什麼」的清單，如果它是宣告的而不是導出的，\n  > 那它描述的是【我們想到的】，不是【這支程式裡有的】。**\n  ⚠️ 而 `vector<int>` **掃不到，那是對的**：容器那一層的資訊住在**身分**裡\n    （`cpp:vector_declare`），所以掃 `type` 拿到的是**元素型別**。界線已釘在測試裡。\n\n🟢 **2026-09-06（spec 175）：共用檔數 270 → 271、export 數 714 → 717**——**輸入量上調**。新增 `core/component/path-syntax.ts`（`classifyPath` ＋ `identityToDir` ＋ `identityToBlockType`）。\n  🔴 它把一條「規則已定，尚未實作」變成會出聲的——而它**現在守的是未來**。\n🟢 **2026-09-06（把核心從 `localStorage` 上拔下來，spec 173）：共用檔數 268 → 270、export 數 711 → 714**——**輸入量上調**。新增 `core/host/key-value-store.ts`（埠 ＋ 記憶體實作）、`ui/browser-store.ts`（瀏覽器實作）、第一百零七條護欄。\n  🔴 核心裡直接叫 `localStorage` 的次數 **7 → 0**。\n  > **一個「核心」如果它的存檔只在一個宿主上跑得起來，那它不是核心，是那個宿主的一部分。**\n\n⚠️ 2026-09-05（版面建議與語意波）：共用檔數 267 → 268、export 數 708 → 711——**輸入量上調，不是耦合變差**。新增 `core/semantic-wave.ts`（`LessonView` ＋ `abstraction()` ＋ `waveOf()`）＋ `tests/unit/core/semantic-wave.test.ts` ＋ 第一百零四條護欄 `tests/integration/audit-lesson-wave.test.ts`；`LessonTask` 多一格 `view`；`app.ts` 多一支 `applySuggestedView`。\n🔴 這一格是**建議，不是鎖**：`applySuggestedView` 只從【換題】那一條路呼叫，不從任何重畫路徑——因為「搶回去」在程式碼裡不是一個 `if (locked)`，它是一段「每次都重新套用預設值」的程式碼。外部證據是學習者控制的整合分析：「sequence control is the only type that generally does not harm」（`concepts/認知鷹架.md`）。\n🔴 而 `applySuggestedView` 除了呼叫 `applyLayout` 還要寫 `this.currentLayout`——少了後者，版面真的換了而狀態列的標籤還寫著舊的：**畫面上兩個地方互相打臉，比沒換更糟**。\n\n\n📈 **2026-09-18（接收者從文字屬性換成接點，34 顆膠囊）。**\n  export 數 762 → 763：多的是 `varRefName`（理由同上）。**空殼與重複不變。**\n\n\n📈 **2026-09-18（容器語義那一批）：語料再長。**\n  新增：可重複集合的 `count`、範圍刪除 `erase(a, b)`、巢狀容器的種類、\n  自訂結構的排序／去重／查找。而 `runtime/container-defaults` 是一個新的共用檔。\n\n\n📈 **2026-09-18（位置要收得到刪除通知）：export 數 766 → 769。**\n  多的三個都在 `src/interpreter/pointer.ts`：`noteErasure`（刪一格時說一聲）、\n  `erasureCount`（蓋章用的當前次數）、`positionIn`（造一個蓋過章的位置）。\n  **殼與重複仍然是 0**——它們三個各有真的呼叫者。\n\n\n📈 **2026-09-18（`cpp:deque_declare`，探索報告的 P1）：＋1 顆元件。**\n  `deque` 在此之前**沒有主人**，於是 `deque<pair<int,int>> q;` 掉進一般的變數宣告，\n  `type` 裝著一整串 `deque<pair<int,int>>`——那個容器因此沒有 `elemType`，\n  而 `q[0].first` 說「（不是一個結構）」。語料 **22 支**用 deque。\n  🔴 這件事早就寫在 `cpp:vector_push` 的執行器註解裡等人來修。\n\n\n📈 **2026-09-18（結構化繫結 ＋ 走訪的容器換成接點）**：新增的常駐測試與一顆元件。\n\n📈 **2026-09-18（指定值保住宣告的型別 ＋ 參照編譯器的 stdin 走檔案）：語料變大，缺陷那幾格未動。**\n  新增 `src/interpreter/assign-type.ts` 的 `keepDeclaredType`——**一個變數的型別不會因為被指定而改變**。\n  🔴 它先寫在 `languages/cpp/lang/runtime/`，而複合指定的執行器住在核心，\n  於是核心 import 了語言套件、**中立性護欄當場指名**。搬進核心之後護欄綠\n  ——而理由不只是分層：那一段從頭到尾只講 `RuntimeValue` 的純量型別，那是核心的詞彙。\n  🟢 空殼與重複都還是 0。\n\n📈 **2026-09-18（範圍那一族：兩端從字串屬性換成接點，管線 188）：語料變大，缺陷那幾格未動。**\n  10 顆範圍演算法的 `begin`／`end`（＋前綴和的 `dest`）從**字串屬性**換成**接點**，\n  ＋ 三顆新元件（`cpp:range_find`／`cpp:range_unique`／`cpp:range_remove`）\n  ＋ `cpp:container_iter` 多一個 `call` 屬性（`begin(a)` 這個自由函式形式）\n  ＋ 27 顆宣告式元件補上 `forLoopPart`（同一個病的第三次，這次整族一起補並加了一條護欄）。\n  🔴 而拆掉那個 regex 之後發現**結構早就在了**：容器與位置在這個直譯器裡是同一個形狀\n  （`interpreter/pointer.ts` 的 `isCellPointer`），`A+n` 的位置算術也早就有人做。\n  > **那條 regex 解析的東西，執行期早就會算了——它不是在補一個缺口，它是在繞過一個已經存在的機制。**\n\n📈 **2026-09-18（瀏覽器驗收，管線 188 階段六）：語料變大，缺陷那幾格未動。**\n  新增 `slotCode`（空插槽的預設）＋ 九條「空插槽產出的碼要編得過」的測試。\n  🔴 這一關抓到的：剛拖出來的積木**在畫面上是對的**（可讀的圓洞 ＋ ⚠️），\n  而產出的程式碼是 `find(v.begin(), v.end(), );`——**編不過**。\n  > **一個半完成的程式該長什麼樣，是一個設計決定；\n  > 而「有的格子有答案、有的格子留一個語法錯誤」不是決定，是沒有決定。**\n\n📈 **2026-09-19（I/O 操縱子三顆，管線 189）：語料變大，缺陷那幾格未動。**\n  `cpp:io_flush`（`cout << flush`）· `cpp:io_mode`（`setw`／`setprecision`／`setfill`）·\n  `cpp:io_mode_number`（`fixed`／`scientific`），\n  ＋ 串流狀態（`WeakMap` 以 `ctx.io` 為鍵，**一次執行一份**）\n  ＋ `interpreter/decimal.ts`（十進位的四捨五入，收尾照 C 的**逢五取偶**）。\n  🔴 而命名是**詞彙表逼出來的**：第一版叫 `stream_*`，三個字都不在表裡；\n  換成既有詞（`io` ＋ `mode` ＋ `number`）之後名字反而對得上同族\n  （`cpp:io_sync`／`cpp:io_tie` 早就在那裡）。**只補了一個新詞：`flush`。**\n\n📈 **2026-09-19（二維陣列的維度換成接點，第 190 刀）：語料變大，缺陷那幾格未動。**\n  `cpp:array_2d_declare` 的 `rows`／`cols` 從屬性換成接點——**同一個病的第四次**\n  （`V19` 接收者 39 顆 · `V20` 走訪對象 1 顆 · `V21` 範圍的兩端 10 顆 · 這裡 1 顆）。\n  🔴 而這一顆是一個**不對稱**：同族那顆一維陣列的 `size` 早就是接點。\n  ⚠️ 第七十二條看不到它——測試語料裡的維度永遠是數字字面值。\n  > **語料乾淨不代表模型對：它代表語料是照著模型長的。**\n\n📈 **2026-09-19（第 199 刀：一排位元 `bitset`）：新增兩顆身分。**\n  `cpp:bits_declare`（一排固定長度的位元）與 `cpp:bits_fill`（整排歸零／設為 1／反轉），\n  ＋ 既有那顆「數 1 的個數」多一個**寫法**欄位（內建函式／一排位元）。\n  🔴 語料 `bitset<` **4 處 / 3 支**，而三支**全部**只缺這一族——\n  逐支讀過全文，其餘用到的東西今天都支援。\n  🟢 而索引的讀與寫**沒有新增任何機制**：一排位元在執行期就是 `type: array`，\n  於是既有那顆「取第幾格」連左值都免費沿用。\n📈 **2026-09-20（帶參數的巨集）：共用檔數 301 → 302、export 數 802 → 803。**\n  **母體變大，缺陷那幾格未動**——新增一顆身分（`cpp:define_func`）＋ 一支樹修復（`languages/cpp/lang/macro-expand.ts`）＋ 一支回歸網（`roundtrip-cpp-func-macro.test.ts`）。\n"
}
