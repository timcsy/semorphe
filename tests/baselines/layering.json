{
  "_meta": {
    "guard": "layering",
    "measuredAt": "2026-08-25",
    "rule": "相依只准往下：core 不得 import ui／languages／components／vscode；languages 與 components 不得 import ui／vscode；ui 不得 import languages／vscode。組裝點（src/ui/app.ts）具名豁免，而豁免附上數字。",
    "note": "📈 **2026-09-19（第 197 刀：位置的相鄰一格 `cpp:pointer_step`）：新增一顆身分，輸入量上調。**\n  `prev(it)`／`next(it)`——語料 `prev(` 10 處 / 8 支，而**每一處都是 `prev(X.end())`**\n  （那是「取最後一個」在有序容器上唯一的寫法，`set` 沒有 `back()`）。\n  方向是**屬性**不是身分（元件代數 250），前例是同族取端點那顆的四值 enum。\n  🟢 跨層相依未動（5）：膠囊只用 `interpreter/pointer.ts` 的 `movePointer`。\n\n📈 **2026-09-19（64 位元整數）：輸入量上調，缺陷格未動。**\n  新增 `interpreter/int64.ts`（「|v| ≤ 2^53 是 number，超過是 bigint」的不變式）\n  ＋ `core/scalar-types.ts`（拼法 → 執行期種類的登記處，核心給機制、語言給資料）\n  ＋ `languages/cpp/scalar-types.ts`（C++ 那份資料，**在模組頂層**）。\n  🔴 修的是「`long long` 在這個直譯器裡是一個 double」——\n  `ans*x%P`（乘積 1e18）失真、`nt>>=1` 被截成 32 位元、\n  `ll n = 2e9` 印成 `2e+09`、函式回傳值完全沒有依宣告型別轉換。\n  🟢 跨層相依未動（5）：`scalar-types.ts` 住在核心，資料由語言套件推進來。\n  讀數：內容真的不同 **5 → 3**（累計 7 → 3）、一致 147 → 151。\n\n📈 **2026-09-19（解析器解錯的兩種形狀）：輸入量上調，缺陷格未動。**\n  新增 `core/lift/ast-repairs.ts`（「換一棵樹」的登記處，與辨識後處理同形狀）\n  ＋ `languages/cpp/lang/misparse.ts`（`!K--` 與 `a<b && c>-d` 兩條重寫）\n  ＋ `tests/unit/languages/cpp/misparse.test.ts`。\n  🔴 它們補的是**唯一一批「往下追的每一層都是對的」的缺陷**——\n  tree-sitter-cpp 自己解錯，而 lift 只問「這棵樹是什麼意思」。\n  🟢 跨層相依未動（5）：新檔住在 `core/lift/`，語言套件推、核心讀。\n\n數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。\n🔴 **剩下的 5 筆是同一團**：`src/languages/style.ts` 的 `CodingStyle` 與 `core/types.ts` 的 `StylePreset` 是**兩份描述同一件事的型別**（`'iostream'|'cstdio'` vs `'cout'|'printf'`），中間靠一個轉換函式。\n2026-08-24 已經把轉換從 `sync-controller` 搬進語言那側（`detectStyleExceptionsForPreset`），於是引擎不再認識 C++ 的詞彙；**剩下四個 UI 檔仍直接 import 那個型別**，而根治要先答「這兩份型別哪一份是真的」。\n第五筆 `core/language-executors.ts → languages/cpp/builtins` 是既有的 P9 債。\n⚠️ **組裝點豁免今天是 10**——它印出來是為了讓「它是組裝點所以沒關係」有一個上限。\n⚠️ 2026-08-25：「控制項離開積木面板」那一刀新增了控制項登錄表（`core/host/controls.ts`）與宿主那側的接手，**語料因此變大——不是退步**。\n⚠️ 2026-08-25：網頁版狀態列改成「文字項目 ＋ QuickPick」（`ui/toolbar/quick-pick.ts`、`ui/layout/status-bar-controls.ts`），語料因此變大——**不是退步**。\n⚠️ 2026-08-25：1167 → 1168，新增 `core/projection/diagnostic-projection.ts`。\n⚠️ 2026-08-25「行動版 ＝ 第三個渲染器」：刪除 `ui/toolbar/{topic,style,locale}-selector.ts` 與 `setupSelectors`／`#mobile-selector-parking`／「把選擇器搬進漢堡選單」那整段——那四顆下拉變成 `ControlState` 的渲染器。🔴 **語料縮水而正當**：違規數未動（跨層 5 筆、直接呼叫視圖 70、組裝點豁免 10），已確認是「東西真的被刪了」，不是「掃描器不再吃到它」。\n⚠️ 2026-08-25「行動版的選單改成往下鑽的 QuickPick」：刪除 `ui/toolbar/mobile-menu.ts` 與其測試、`renderSheetControls`、`mobile-sync-btn`。🔴 **語料縮水而正當**——已確認是「東西真的被刪了」。\n⚠️ 2026-08-25：新增 `ui/alt-layout-block.ts`。\n⚠️ 2026-08-25：1165 → 1166，新增 src/migrations/block-shape-changes.ts。語料變大。\n🎯 **2026-08-26：組裝點豁免 10 → 9**——`cpp_var_declare_expression` 的命令式定義退場，**連帶整個 `setLanguageInputNames`\n  注入契約刪除**——它從十二個插槽名縮到一個，而最後一個的消費者就是那顆積木。\n  ⚠️ 那道「沒注入就拋錯」的守衛也一起走：**它守的是一份空契約**。\n  組裝點少了一個「認得語言插槽名」的理由。\n🎯 **2026-08-26：組裝點豁免 9 → 0。**`app.ts` 的語言管線 9 → 0：那九處寫死的 `cpp/*` import 換成**語言套件宣告的能力**\n  （`installLifters`／`installExtractStrategies`／`declarations`／`diagnosticRules`／\n   `styleExceptions`／`createCodeShaping`）。\n⚠️ 2026-08-26：新增 `core/flow/vocabulary.ts`（流程視圖的詞彙：標題／位置名／值的顯示文字）＋ 第七十八條護欄。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：第八十條護欄（audit-flow-connect：流程視圖上的線只有父子關係）＋ `core/flow/connect.ts` ＋ 流程面板的拉線。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：(d) palette（`core/flow/palette.ts`：讀工具箱的【輸出】）＋ (e) 語句重排（`tryReorder`）。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：第八十一條護欄（audit-layout-presets：佈局預設列的是層不是面板）＋ `core/host/layout-presets.ts` ＋ `applyLayout`／`setDividerVisible`／`layout` picker。**輸入量變大，不是退步**。\n⚠️ 2026-08-27：語料變大——新增 `core/flow/presets.ts`（把工具箱的 `extraState` 翻譯成流程視圖的骨架）與它的單元測試。**輸入量，不是耦合。**\n⚠️ 2026-08-27：語料變大——新增 `core/flow/layout-key.ts`（手拖佈局的鑰匙：內容／行號／路徑三把一起）＋ 它的單元測試與第八十二條護欄。**輸入量，不是耦合。**\n🔴 **2026-08-27：語料縮水而正當**——`src/ui/layout/status-bar.ts` 刪除（63 行，一個 class）。\n   它畫的是**四格**（language｜風格｜課程｜語系｜Sync），而畫面上是**七格**（那七格由 `status-bar-controls.ts` 畫，2026-08-25「控制項離開積木面板」的產物）。\n   查證：**零產品呼叫者、零測試引用**。\n   > 一個渲染器，畫的東西與畫面上的不一樣，而沒有人發現——因為沒有人在看它。\n   ⚠️ 已逐項核對數字對得上「刪掉一個檔、一個 class」，**是東西真的被刪了，不是掃描器不再吃到它**。\n⚠️ 同一輪語料變大的部分：新增 `tests/unit/ui/status-bar-language-cell.test.ts`。\n⚠️ **2026-09-06（spec 176：型別的下拉跟著程式長）：1199 → 1200**——新增 `core/types-in-use.ts`。**輸入量，不是耦合。**\n  🟢 而**相依的方向是對的**：語言那一側（`cpp/pack.ts`）import 核心的列舉，\n  而**核心不知道任何語言的型別名**（第一百一十一條護欄驗了那一點）。\n  🔴 「C++ 有哪些內建型別」不是核心的知識——那份清單留在語言那一側。\n⚠️ **2026-09-06（spec 177：帶得走的作品）：1200 → 1201**——新增 `core/portable.ts`。**輸入量，不是耦合。**\n  🟢 相依的方向是對的：**核心說「這份作品拆成哪幾個檔」，宿主說「怎麼變成一個下載」**\n  ——壓縮與 `<a download>` 都在 UI 那一側，核心連 `.cpp` 這三個字都沒有。\n  🔴 那是**宿主獨立性的第四個實例**（前三個：儲存、程式碼視圖、控制項）。\n⚠️ **2026-09-07（拆輪子的曲線）：1201 → 1202**——新增 `core/edit-tally.ts`。**輸入量，不是耦合。**\n  ⚠️ 它與 `progress.ts` 共用的是**那個埠**（`KeyValueStore`），**不是一個介面**\n  ——`progress.ts:23` 早就決定過：把三件事一次抽象成「學習訊號」會長出一個假的父概念。\n⚠️ **2026-09-07（效率的回饋）：1202 → 1203**——新增 `core/steps.ts`。**輸入量，不是耦合。**\n  🟢 而它是純函式：執行器只廣播 `counts`，**「跑了幾步」由組裝點自己算**——執行器不必知道「哪一題」。\n\n🪦 **2026-09-08：語料【縮水】而它是正當的——`edit-tally` 那一條線整條退場。**\n  掃描檔數 1203 → 1202\n\n  使用者：「我後來覺得弄這條好像沒必要，而是可以在**設計課程的時候提醒\n  可以開始用程式碼寫**」。\n\n  🔴 而那讓「這一課改了哪一邊」的計數**回到零消費者**——所以它跟著走：\n  > **一個沒有消費者的機制，是最差的那一個選項**（我自己在 history/222 寫的）。\n\n  刪掉的：`src/core/edit-tally.ts` · `previewBar`（四個檔）·\n  三個測試檔（unit ×2、e2e ×1）。\n  🟢 **已逐項核對縮水的數字與刪掉的東西對得上**（1 個 src 檔、3 個測試檔、\n  7 個 export）——是「東西真的被刪了」，不是「掃描器不再吃到它」。\n\n\n\n📉 **2026-09-20（第 205 刀：層級樹退場）：輸入量【下調】，而這是刪掉東西的正常結果。**\n  使用者：「我們現在已經有課程了，應該就沒有需要再用 levelTree 了吧」。\n  刪掉 `core/lesson/level-tree.ts`（六支匯出）與 `ui/toolbar/level-tree-selector.ts`，\n  並把 `enabledBranches` 從 app 狀態、存檔（v24）、VSCode 設定裡拿掉。\n  ⚠️ **下調不是縮水**：這幾格量的是「掃到多少東西」，而東西真的變少了。\n  🟢 判定格（耦合數、空殼數、跨層相依）**一格未動**。\n\n📈 **2026-09-21（第 211 刀：脫下原文還站得住嗎）：掃描檔數 1279 → 1280，違規仍是 5 筆。**\n  新增 `src/core/lift/honest-degradation.ts`——一個 `syntax_error` 的節點，\n  脫下 `rawCode` 之後如果還原不出使用者寫的字，就整段換成 `raw_code`\n （**「脫下原文」正是走一趟積木回來的處境**）。\n  🟢 而它要的產碼能力**接在語言套件**（`languages/cpp/generators/index.ts`），\n  不是讓 `core/lift` 去 import `core/projection`——**跨層相依因此一筆未動**。\n\n📈 **2026-09-25（形態的因子化）：掃描檔數 1280 → 1281。**\n  `deriveFactoredSpecs`——軸之間**條件獨立**時，組合形態是**導出的**不是手寫的。\n  🟢 `cpp:container_push` 從**四個宣告**組出**六顆積木**，而多出來的兩顆\n  （`_stack_expression`／`_queue_expression`）正是先前**標籤說謊**的那兩顆：\n  一顆堆疊的 push 出現在運算式位置時，標籤是中性的「放進」而不是「推入堆疊」。\n  🔴 判準來自 `Transformers learn factored representations`（arXiv 2602.02385）：\n  因子化在因子條件獨立時**無損**；而在形態上的讀法是\n  **每條軸動的 `blockDef` 欄位彼此不相交**（實測：文字 vs 形狀，交集 ∅）。\n  ⚠️ **機制檢查獨立性，不假設它**——有交集就不產生並說出交集。\n  **導出兩顆積木型別 ＋ 一個新檔 ＋ 一支單元測試，輸入量本來就會長。**\n"
  },
  "掃描檔數": 1282,
  "跨層相依": 5,
  "組裝點豁免": 0,
  "note": "📈 **2026-09-26（第 245 刀：判定者覆蓋率）：母體變大，缺陷那幾格未動。**\n  新增第 128 條護欄 `audit-judge-coverage` ＋ `src/core/component/judges.ts`。\n  🔴 它是契約化的第一刀，而那一刀的判準是「把**已經在跑**的判定者接上去，不是加一個新欄位」。\n  🟢 已確認是「多了檔案」不是「掃描器變寬」：增量逐字等於這一刀新增的檔數。\n📈 **2026-09-17（迭代器一族，管線 180）：語料變大，缺陷那幾格未動。**\n  新增 `cpp:container_find`（`find`／`lower_bound`／`upper_bound` 三個方法名\n  對同一顆身分，判準進 `how` 屬性）＋ 既有的取端點那顆多認反向的兩端。\n  🔴 而最大的一個修法只有**一個宣告**：自動型別的變數宣告沒有宣告\n  `forLoopPart`，於是 `for (auto it = v.begin(); …)` 整段掉進 raw code。\n\n  🟢 跨層相依沒有變（5）。\n\n📈 **2026-09-17（位置＝實體式指標，管線 180 的前兩顆）：語料變大，缺陷那幾格未動。**\n  新增 `src/interpreter/pointer.ts`（「這是一個位置嗎」「往後幾格」「同一串格子嗎」\n  的唯一判定處，三個註冊點共用）＋ `cpp:compare`／`execIncrement`／`execCompoundAssign`\n  各補一條位置分支。\n  🔴 修的是兩個**靜默的**缺陷：`++p` 把指標寫成一個 double（錯誤訊息出現在\n  下一次解參考）、`p != q` 恆等於「相等」（於是每一個走訪迴圈一次都不跑）。\n\n  🟢 而**跨層相依沒有變**（5）：新檔住在 `interpreter/`，而膠囊本來就\n  引用得到它（同 `receiver.ts`／`clone.ts`／`aliases.ts` 的位置）。\n\n📈 **2026-09-16（模糊測試那一輪）**：新增 `cpp:container_push_front`、\n  `interpreter/clone.ts`、`interpreter/aliases.ts` 與兩支回歸測試檔。\n  **輸入量上調，缺陷格未動。**\n\n📈 **2026-09-16（第一百七十九刀，第二顆）**：新增 `cpp:container_push_front`。\n  🔴 它是**模糊測試**升級上來的：探索報告原本把它排在最後（真語料只有 1 支用到），\n  而隔離出題的 10 支程式有 5 支卡在它——**一個沒有 push_front 的 deque 只有一半**。\n  **新增一顆元件，輸入量本來就會長。**\n\n📈 **2026-09-16（第一百七十九刀）**：新增 `cpp:container_pop_front`。\n  **新增一顆元件，輸入量本來就會長**——缺陷那幾格一筆未動。\n\n📈 **2026-09-16（第四輪）**：新增 `src/interpreter/receiver.ts`\n  （把被壓成文字的接收者 `d2[3]` 解開，34 個元件共用一處）。\n  **輸入量上調，缺陷格未動。**\n\n📈 **2026-09-16（清那 90 支，第二輪）**：新增 `src/interpreter/aliases.ts`\n  （名字的別名表，`#define x first` 那一族）。**輸入量上調，缺陷格未動。**\n\n🟢 **2026-09-15（highlight 差一行）：輸入量上調，耦合數字未動。**\n  新增 `core/projection/patch-shift.ts` ＋ `tests/unit/core/patch-shift.test.ts`。\n  🔴 使用者：「積木跟程式碼好像 highlight 的地方對不上，會差一行」——\n  對照表描述的是【補之前】那份文字，而補丁器在最上面塞了一行 `#include`。\n  ⚠️ 而組裝點那個 wrapper **早就算過同一個位移**（`linesDelta`，給游標保位用）。\n  > **同一個位移被算出來給 A 用，而 B 也需要它——那是「已經知道答案而沒有交出去」。**\n\n🟢 **2026-09-14（名字也要驗）：輸入量上調，缺陷格未動。**\n  新增 `core/identifier-syntax.ts`（語言套件宣告、核心讀）\n  ＋ `core/component/param-check.ts`（`ParamSpec.kind` 的唯一判定處）。\n  🔴 使用者轉述學生：「變數名稱會寫成數字，Semorphe 竟然還可以接受」\n  ——實測產出 `int 123 = 16;`，主控台零錯誤、畫面零標記。\n  而 97 個參數宣告了 `kind: 'identifier'`，**沒有任何一處讀 `kind`**。\n  ⚠️ 同一週第三個同族缺陷（slots 的 min/max、形態軸 role、然後是這個）。\n\n🟢 **2026-09-14（快車道 `/next/` ＋ e2e 分片）：輸入量上調，缺陷那幾格一筆未動。**\n  新增 `core/base-path.ts`（站台前綴的唯一來源）＋ `tools/build-site.sh`（兩份站一包）\n  ＋ 第一百二十七條護欄 `audit-shell-var-braces`。\n  🔴 快車道與主站**刻意同一個 origin**——`localStorage` 是 per-origin 的，\n  換子網域會讓學生在快車道上的存檔在主站全部不存在，而他不會知道。\n  ⚠️ 代價就是每一條絕對路徑都要帶前綴；少帶一條的症狀不是報錯，是只有 `/next/` 那邊 404。\n\n🟢 **2026-09-14（槽的宣告要真的有人讀）：輸入量上調，缺陷那幾格一筆未動。**\n  新增 `core/component/slot-check.ts`（`allowed`／`min`／`max` 的唯一判定處）\n  ＋ `ComponentDefJSON.positions`（文法那一軸，與 `role` 分家）\n  ＋ 第一百二十六條護欄；`flow/connect.ts` 的重複判定刪掉 20 行。\n\n🟢 **2026-09-13（面板登錄表接上了）：掃描檔數 1204 → 1209（五份 panel.ts）**——**輸入量上調**。\n\n  spec 170 的 T001–T006 蓋好登錄表就停住了，而**那個停住看不見**：\n  `src/panels/` 零份宣告、`loadPanels()` 零個產品呼叫者、\n  README 引用的那支測試不存在。登錄表自己的測試檔頭寫著：\n  「**一個沒有人宣告的登記處【就是殼】，而它綠得跟真的一樣。**」\n\n  這一輪：五份 `panel.ts`（code／flow／blocks／console／variables）\n  ＋ `loadPanels()` 接上組裝點（健檢結果印出來）\n  ＋ 讓「狀態」那一層的分頁**真的去問登錄表**（名字與順序）\n  ＋ 第一百二十五條護欄 `panel-mount-migration`（棘輪，`mount` 還在組裝點的只准變少）。\n\n  ⚠️ **`mount` 沒搬是有理由的**，而那是 `app-shell.ts` 自己留下的：\n  「一次抽象如果同時搬走「容器」與「內容」，它壞掉時你分不出是哪一半。」\n  🔴 所以由 `mountedByShell` 說出來、由棘輪數著——\n  **一個分階段的搬遷，如果沒有東西數著還剩幾個，它會停在第一階段。**\n\n🟢 **2026-09-13（解掉 core ↔ interpreter 的環）：掃描檔數 1203 → 1204**——**輸入量上調**（新增 `core/execution.ts`）。\n\n  🔴 **而這張表漏了一格。** `core/` 有四個檔 import `interpreter/types`，\n  而 `interpreter/` 也 import `core/`——**兩個平級目錄互相依賴**，\n  而 `FORBIDDEN.core` 只列了 `ui／languages／components／vscode`。\n\n  > **一張「誰不准 import 誰」的表，漏掉的那一格就是環長出來的地方。**\n\n  修法不是加一條禁令就算了：`ExecutionStatus`／`ExecutionSpeed`／`RuntimeType`／\n  `StepInfo` 搬進 `core/execution.ts`，理由是**講它們的是匯流排與視圖，\n  而直譯器只是它們的第一個實作**（與 `core/host/` 那十個埠同一個形狀）。\n  ⚠️ `interpreter/types.ts` 原樣再匯出，500 多處既有 import 一行都沒動。\n\n  🟢 `interpreter` 加進 `FORBIDDEN.core`，同一個環不會再長出來。\n  這也是「外部執行器」進得來的前提——它要說得出同一組狀態，而不必 import 內建直譯器。\n\n🪦 **2026-09-13（資料夾整理）：語料【縮水】而它是正當的。**\n  使用者：「我想要把這專案資料夾結構重新整理的好看一些……準則是要說得出\n  這資料夾或檔案與實際內容結構的關係，否則可能就是放錯地方。」\n\n  🔴 **`src/scripts/` 整個退場**——那兩支建置工具搬到 `tools/`。\n  而「它不屬於 src」這件事 **`tsconfig.json` 早就自己招了**：\n  它寫著 `exclude: [\"src/scripts\"]`。\n\n  > **一個必須被排除在建置之外的目錄，它的位置就是錯的。**\n\n  ＋ 刪掉 `src/style.css`（8.8KB、零引用——`index.html` 載的是 `src/ui/style.css`）。\n\n  🟢 **已逐項核對**：掃描檔數 1205 → 1203，正好是離開 `src/` 的那兩個 `.ts`\n  （`build-vscode.ts`、`verify-component-paths.ts`）。`src/style.css` 不是 `.ts`，不在這個分母裡。\n\n🟢 **2026-09-10（中鍵推畫面）：掃描檔數 1204 → 1205**——**輸入量上調**。\n  學生回饋（附截圖）：「就是有時候遇到這種其實蠻難滑的（積木），要不要設定\n  按滑鼠中鍵可以移動我看的地方然後不會動到積木」。那張截圖是一段 DFS，\n  `陣列 p 的第 [ u ] 格 = v` 橫著長到出畫面。\n\n  🔴 **Blockly 早就能拖背景平移——而那張畫面上背景不存在**：每一個像素都是積木，\n  於是「先找一塊空白」這一步本身就失敗了。\n\n  > **一個要求使用者先找到空白處的手勢，在畫面最滿的時候最沒有用\n  > ——而那正是他最需要平移的時候。**\n\n  新增 `ui/panels/middle-drag-pan.ts`（capture 階段攔中鍵，Blockly 的手勢收不到它）\n  ＋ 第一百二十二條護欄 `middle-drag-pan`。\n  ⚠️ 中鍵的三個瀏覽器預設行為都要擋（Windows 自動捲動／X11 貼上主選取區／auxclick 開新分頁）。\n\n🟢 **2026-09-10（打暗要說得出換哪一課）：**——**輸入量上調**。\n  使用者轉述學生的畫面：他沒有換課，一路往下寫。第 1 課只開了三顆元件，\n  而他在宣告變數——**他自己寫的每一塊積木都被打暗**，而畫面上沒有一個字\n  說為什麼、也沒說怎麼辦（出路藏在最下面那條狀態列的一格下拉裡）。\n\n  ⚠️ 同一個問題被問過兩次：2026-08-28 使用者自己看著畫面問過\n  「為何積木變這麼暗？」。那一次修的是「不該暗的別暗」，\n  這一次是「該暗的**沒說為什麼**」。\n\n  > **一個純視覺的訊號，如果它在使用者【正當地往前走】的時候觸發，\n  > 那它讀起來不是提示，是故障。**\n\n  新增 `core/lesson-suggest.ts`（哪一課涵蓋得了這些元件——**算出來的**，\n  每一課都宣告了自己的 `components`）＋ `ui/lesson-nudge-bar.ts`（一條指路的線）\n  ＋ 第一百二十條護欄 `lesson-nudge`。\n\n🟢 **2026-09-06（spec 175：身分階層 vs 實例路徑）：掃描檔數 1198 → 1199**——**輸入量上調**。\n  新增 `core/component/path-syntax.ts`（判別器 ＋ 兩個投影）\n  ＋ 第一百一十條護欄 `audit-identity-path-syntax`。\n  🔴 它把 `concepts/元件.md` §`name` 的階層那三條規則變成**機械檢查**\n    ——那一節自己標著「**狀態：規則已定，尚未實作**」。\n  ⚠️ 而它**現在守的是未來**：帶 `.` 的身分今天 0 顆，\n    它會在第一個人寫下 `cpp:containers.vector` 的那一刻紅。\n  > **一條規範沒有機械化的檢查，它本身就是殼——而殼看起來像完成。**\n\n🟢 **2026-09-06（把核心從 `localStorage` 上拔下來，spec 173）：掃描檔數 1196 → 1198**——**輸入量上調**。新增 `core/host/key-value-store.ts`（埠 ＋ 記憶體實作）、`ui/browser-store.ts`（瀏覽器實作）、第一百零七條護欄。\n  🔴 核心裡直接叫 `localStorage` 的次數 **7 → 0**。\n  > **一個「核心」如果它的存檔只在一個宿主上跑得起來，那它不是核心，是那個宿主的一部分。**\n\n⚠️ 2026-09-05（版面建議與語意波）：掃描檔數 1195 → 1196——**輸入量上調，不是耦合變差**。新增 `core/semantic-wave.ts`（`LessonView` ＋ `abstraction()` ＋ `waveOf()`）＋ `tests/unit/core/semantic-wave.test.ts` ＋ 第一百零四條護欄 `tests/integration/audit-lesson-wave.test.ts`；`LessonTask` 多一格 `view`；`app.ts` 多一支 `applySuggestedView`。\n🔴 這一格是**建議，不是鎖**：`applySuggestedView` 只從【換題】那一條路呼叫，不從任何重畫路徑——因為「搶回去」在程式碼裡不是一個 `if (locked)`，它是一段「每次都重新套用預設值」的程式碼。外部證據是學習者控制的整合分析：「sequence control is the only type that generally does not harm」（`concepts/認知鷹架.md`）。\n🔴 而 `applySuggestedView` 除了呼叫 `applyLayout` 還要寫 `this.currentLayout`——少了後者，版面真的換了而狀態列的標籤還寫著舊的：**畫面上兩個地方互相打臉，比沒換更糟**。\n⚠️ 2026-09-05（「排回去」那種題）：掃描檔數 1194 → 1195——**輸入量上調，不是耦合變差**。新增 `core/arrange.ts`（確定性的打散——🔴 一顆亂數都不產：一個每次都不一樣的東西，測試只能驗「它有動」，驗不了「它對」）＋ `tests/unit/core/arrange.test.ts`；`LessonTask` 多一格 `kind: 'arrange'`（**沒有 check 的「排回去」要丟錯**——排完之後沒有人會說話）；`load-lessons` 多一個 `solutionFor`（那 42 份參考解答本來只有護欄在讀，現在產品這一側也讀）；`BlocklyPanel.scatterTopStatements` 只負責畫。\n🔴 外部證據：文獻裡叫 Parsons problem：「與自己寫程式同樣的學習成效，而只花 70% 的時間」（`concepts/認知鷹架.md`）。\n（跨層相依停在 5，一筆未動）\n⚠️ 2026-09-04（跑之前先猜一下）：掃描檔數 1193 → 1194——**輸入量上調，不是耦合變差**。新增 `core/predict.ts`（要問什麼 ＋ 這支程式是不是還是剛才那一支）＋ `tests/unit/core/predict.test.ts`；`LessonTask` 多一格 `predict`；`ConsolePanel.askPrediction`／`showPrediction`；`iterations.ts` 多三支（`isLoop`／`loopNodes`／`loopNodeById`／`loopRatio`——後者**不過濾**，因為揭曉一個預測時 `1` 與 `0` 都是正當的答案）。🔴 執行器多一個**可省略的** `beforeRun` async 鉤子——形狀抄 `pauseForUnrecognized`：**要停下來問人的事，用鉤子，不是把那件事搬進執行器**。沒注入時行為與從前逐字相同。\n（跨層相依停在 5，一筆未動）\n⚠️ 2026-09-04（迴圈跑了幾次）：掃描檔數 1192 → 1193——**輸入量上調，不是耦合變差**。新增 `core/iterations.ts`（把「每顆節點走過幾次」換算成「這顆迴圈重複了幾輪」）＋ `tests/unit/core/iterations.test.ts`；直譯器的 `visited: Set` 換成 `visitCounts: Map`（同一個漏斗，一次 `Map.set`）；匯流排的 `execution:coverage` 多帶 `counts`（🔴 **一個事件不是兩個**——分開的話收的人要自己保證它們來自同一次執行）；`BlocklyPanel.markIterations` 只畫。🔴 「哪一塊是迴圈」讀 `control_flow` 標註，「哪一格是身體」讀 `bodySlotsOf`——**兩份判斷都已經有家了，這一刀一份新的都沒寫**。\n（跨層相依停在 5，一筆未動）\n⚠️ 2026-09-04（一課多題）：掃描檔數 1191 → 1192——**輸入量上調，不是耦合變差**。新增 `core/progress.ts`（通過紀錄，自己一格 localStorage：匯出一份作品給同學，不該把「你第 3 題還沒過」一起送出去）＋ `tests/unit/core/progress.test.ts`；`core/lesson.ts` 的 `check` 升格成 `tasks`（`LessonTask`／`parseTasks`／`taskById`／`FREE_PRACTICE`）；登錄表多一顆 `task` picker；`ConsolePanel.offerNextTask`。🔴 它修的是一個**會在學生做對事情時說他錯**的裁判。\n（跨層相依停在 5，一筆未動）\n⚠️ 2026-09-04（操作片段）：掃描檔數 1190 → 1191——新增 `core/interactions.ts`（「這一課會用到哪些操作」的登錄表，課文頁與之後的 app 提示**共用同一份**）。**輸入量上調**，跨層相依一筆未動。\n⚠️ 2026-09-03（課文的出口・第四輪 · 抽屜與示範）：掃描檔數 1189 → 1190——新增 `ui/layout/drawer.ts`（☰ 的動作清單）。**輸入量上調**，跨層相依一筆未動。\n⚠️ 順帶記一條**流程**：`GENERATE_BASELINE=1` 會整份重寫，而有幾條的產生器**不保留 `note`**（實測：本檔保留、`four-independences` 與 `shared-file-husks` 被吃掉）。**所以順序是「先重生、再寫註記」**——反過來寫的那一次，註記當場消失。\n⚠️ 2026-09-03（課文的出口・第三輪 · 入口與抽屜）：掃描檔數 1189 → 1190——標頭多一顆「課程」（`fileButtons` 閘門，走 `<a>` 不走 `<button>`）、行動版的 ☰ 抽屜（新檔 `ui/layout/drawer.ts`：課程／匯出／匯入／上傳自訂積木／GitHub）與 ⚙ 設定分開、`GITHUB_MARK` 改成具名匯出（一份兩個消費者）。**輸入量上調，不是耦合變差。**\n⚠️ 2026-09-02（這一整輪 UI 的修）：掃描檔數 1188 → 1189——**輸入量上調**。新增：`src/vscode/host-quirks.ts`（宿主怪癖的唯一一處，附病歷）、`tests/unit/core/always-in-scope.test.ts`、`tests/unit/core/one-layer-window-swap.test.ts`、以及底條／窄欄／面板開關那幾支的實作與匯出。\n⚠️ 2026-09-02（spec 171 · 十字退場）：掃描檔數 1189 → 1188——**語料下修，因為東西真的被刪了**：`src/vscode/editor-layout.ts`（142 行的「版面 → 編輯器分組」推導）與 `tests/unit/vscode/editor-layout.test.ts` 整支退場。三張版面全是純欄之後，`reveal(ViewColumn)` 就排得出來——不需要那份推導，也不需要 Theia 沒有的 `setEditorLayout`。🔴 已確認不是「掃描器不再吃到它」：那兩個檔在 `git status` 裡是 `D`。\n⚠️ 2026-09-02（spec 171 · 主控台的表面）：掃描檔數 1188 → 1189——**輸入量上調**。新增 `core/host/console-surface.ts`（`ConsoleSurface` ＋ `revealForOutput`）＋ `tests/unit/core/console-comes-back.test.ts`（4 條，**注入驗過會紅**）。🔴 那條規則寫在 `ConsolePanel` 的**寫入路徑**上，不是各宿主各寫一份——兩個宿主各寫一次的話，其中一個遲早會漏，而漏掉的症狀是**使用者看不到程式在說什麼**。\n⚠️ 2026-09-02（改名跟得上參照 · 缺陷③）：掃描檔數 1187 → 1188——**輸入量上調**。新增 `core/rename-variable.ts`（`isIdentifierProperty`／`isVariableReference`／`scopeOf`／`renameReferences`／`isRenamingADefinition`）＋ `tests/integration/rename-follows-references.test.ts`（5 條，**兩個注入驗過會紅**）。🔴 判準來自**宣告**：`kind: 'identifier'`（屬性自己說它裝名字）＋ `variableRef`（元件自己說它是參照）——不是一份「哪些欄位是名字」的清單。\n> **改名是一次【重構】，而設一個屬性只是設一個屬性——兩者在畫面上看起來一樣，直到你去跑它。**\n（跨層相依停在 5，一筆未動）\n⚠️ 2026-09-01（spec 170 · US2 · 一格的頭只有一支產生器）：掃描檔數 1186 → 1187——**輸入量上調**。新增 `ui/layout/cell-head.ts` ＋ `tests/unit/ui/cell-head.test.ts`；四條頭的產生器 5 → 1（`flow-toolbar`／`monaco-clipboard-bar`／`bottom-panel-tabs`／`quick-access-bar` 從「帶樣式的 class」變成純粹的**掛鉤**）。\n⚠️ 2026-09-01（spec 170 · 面板宣告的骨架）：掃描檔數 1183 → 1186——**輸入量上調，不是判準放寬**。新增 `core/host/panel-spec.ts`（PanelSpec／PanelAction／PanelInstance／PanelContext）、`panel-registry.ts`（allPanels／panelsOfLayer／panelsFor／layersOf／assertPanelsSane）、`load-panels.ts`（`import.meta.glob`，與課程／範例／語言套件同一招）＋ `tests/unit/core/panel-registry.test.ts`（四條「要出聲」，**兩個注入驗過會紅**）。🔴 使用者：「面板可以模組化就更好了……我的重點是要好維護管理」——量到加一種投影要動 8 個既有檔。\n（跨層相依停在 5，一筆未動）\n⚠️ 2026-09-01（偏好屬於這份文件）：掃描檔數 1182 → 1183——**輸入量上調，不是判準放寬**。使用者：「**C++ 一開始要預設 C++ 吧**」——一份 C++ 的暫存檔而狀態列寫著 Arduino。🔴 前一刀讓每顆 picker 選完就寫回 **workspace 設定**，於是一次「我這個檔要用 Arduino」變成整個專案的設定，把「依副檔名自動判斷」蓋掉。> 一份偏好寫回去的範圍，不得大於它描述的東西。新增 `sync/doc-prefs.ts`（per-uri，住在宿主的 workspaceState，所有面板共用；形狀與 `view-state.ts` 一致，含存檔那一刻的身分搬遷）＋ 它的測試。（跨層相依停在 5，一筆未動）\n⚠️ 2026-09-01（版面交還給 IDE 執行）：掃描檔數 1181 → 1182——**輸入量上調，不是跨層相依變差**（跨層相依停在 5，一筆未動）。新增 `vscode/editor-layout.ts`：版面宣告 → VSCode 編輯器分組的純推導。⚠️ 它**刻意不 import `vscode`**——那個模組在測試環境不存在，而這段推導必須測得到（與 `sync/view-state.ts` 同一條理由）。\n⚠️ 2026-09-01（spec 169：每個槽自己選視圖）：新增 `core/host/slot-assignment.ts`、第九十九條護欄與兩支測試檔。**輸入量上調，不是缺陷上調。**\n⚠️ 2026-08-28：語料變大——新增 `core/lesson.ts`／`core/load-lessons.ts`／`e2e/lesson-pins.spec.ts`／兩條護欄（第八十四、八十五條）＋ 6 份 `track.json`。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28：語料變大——骨架宣告化（`core/shell.ts` ＋ `languages/cpp/skeletons/{main,none,index}`）。四段骨架從 `cpp-scaffold.ts` 的寫死字串搬進 JSON，而 `skeleton` 的值域從 `main|none` 打開成鷹架 id。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28：語料變大——新增第八十七條護欄（audit-templates：範例）＋ `core/template.ts`／`core/load-templates.ts`／`e2e/templates.spec.ts`＋ 6 份 `templates/*`。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28：語料變大——鷹架控制項（兩個軸：骨架／顯示）＋ Python 自己的骨架宣告 ＋ `e2e/scaffold-code-complete.spec.ts`。**輸入量變大，不是判準放寬。**\n⚠️ 2026-08-28 上調的是【入口條件】（合成量），不是缺陷數：新增 src/ui/panels/ghost-drag-strategy.ts（ghost 鷹架的拖曳策略）。缺陷欄一筆未動。\n⚠️ 2026-08-30：掃描檔數 1179 → 1180，新增 `core/scaffold-nodes.ts`。輸入量變大。\n\n📈 **2026-09-18（容器語義那一批）：語料再長。**\n  新增：可重複集合的 `count`、範圍刪除 `erase(a, b)`、巢狀容器的種類、\n  自訂結構的排序／去重／查找。而 `runtime/container-defaults` 是一個新的共用檔。\n\n\n📈 **2026-09-18：掃描檔數 1228 → 1229。** 新增的是\n  `src/components/cpp/ternary/build.ts`（三元式的建構子）。\n  🟢 **跨層相依仍然是 5**——它是膠囊內的檔，被共用檔往下呼叫，方向沒有反過來。\n\n\n📈 **2026-09-18（`cpp:deque_declare`，探索報告的 P1）：＋1 顆元件。**\n  `deque` 在此之前**沒有主人**，於是 `deque<pair<int,int>> q;` 掉進一般的變數宣告，\n  `type` 裝著一整串 `deque<pair<int,int>>`——那個容器因此沒有 `elemType`，\n  而 `q[0].first` 說「（不是一個結構）」。語料 **22 支**用 deque。\n  🔴 這件事早就寫在 `cpp:vector_push` 的執行器註解裡等人來修。\n\n\n📈 **2026-09-18（`cpp:var_declare_sequence` ＋ 走訪的容器換成接點）**：\n  新增一顆元件（結構化繫結，語料 16 處／13 支），並把範圍 for 的容器\n  從欄位換成接點（`d2[pt]`／`m[k]` 是運算式，語料 3 支）。\n\n📈 **2026-09-18（指定值保住宣告的型別）：語料變大，缺陷那幾格未動。**\n  新增 `src/interpreter/assign-type.ts`（1237 → 1238）。\n  🔴 它第一版住在 `languages/cpp/lang/runtime/`，而複合指定的執行器住在核心\n  ——**中立性護欄當場指名**「src/interpreter/executors/mutations.ts → languages/cpp」。\n  搬進核心之後綠，而判準不是分層本身：那一段只講 `RuntimeValue` 的純量型別，\n  沒有提到任何 C++ 的身分。\n  > **一段程式該住在哪裡，問它講的是誰的詞彙，不是問誰第一個需要它。**\n\n  🟢 跨層相依沒有變（5）。\n\n📈 **2026-09-18（範圍那一族：兩端從字串屬性換成接點，管線 188）：語料變大，缺陷那幾格未動。**\n  10 顆範圍演算法的 `begin`／`end`（＋前綴和的 `dest`）從**字串屬性**換成**接點**，\n  ＋ 三顆新元件（`cpp:range_find`／`cpp:range_unique`／`cpp:range_remove`）\n  ＋ `cpp:container_iter` 多一個 `call` 屬性（`begin(a)` 這個自由函式形式）\n  ＋ 27 顆宣告式元件補上 `forLoopPart`（同一個病的第三次，這次整族一起補並加了一條護欄）。\n  🔴 而拆掉那個 regex 之後發現**結構早就在了**：容器與位置在這個直譯器裡是同一個形狀\n  （`interpreter/pointer.ts` 的 `isCellPointer`），`A+n` 的位置算術也早就有人做。\n  > **那條 regex 解析的東西，執行期早就會算了——它不是在補一個缺口，它是在繞過一個已經存在的機制。**\n\n📈 **2026-09-19（I/O 操縱子三顆，管線 189）：語料變大，缺陷那幾格未動。**\n  `cpp:io_flush`（`cout << flush`）· `cpp:io_mode`（`setw`／`setprecision`／`setfill`）·\n  `cpp:io_mode_number`（`fixed`／`scientific`），\n  ＋ 串流狀態（`WeakMap` 以 `ctx.io` 為鍵，**一次執行一份**）\n  ＋ `interpreter/decimal.ts`（十進位的四捨五入，收尾照 C 的**逢五取偶**）。\n  🔴 而命名是**詞彙表逼出來的**：第一版叫 `stream_*`，三個字都不在表裡；\n  換成既有詞（`io` ＋ `mode` ＋ `number`）之後名字反而對得上同族\n  （`cpp:io_sync`／`cpp:io_tie` 早就在那裡）。**只補了一個新詞：`flush`。**\n\n📈 **2026-09-19（第 199 刀：一排位元 `bitset`）：新增兩顆身分。**\n  `cpp:bits_declare`（一排固定長度的位元）與 `cpp:bits_fill`（整排歸零／設為 1／反轉），\n  ＋ 既有那顆「數 1 的個數」多一個**寫法**欄位（內建函式／一排位元）。\n  🔴 語料 `bitset<` **4 處 / 3 支**，而三支**全部**只缺這一族——\n  逐支讀過全文，其餘用到的東西今天都支援。\n  🟢 而索引的讀與寫**沒有新增任何機制**：一排位元在執行期就是 `type: array`，\n  於是既有那顆「取第幾格」連左值都免費沿用。\n\n📈 **2026-09-20（第 199 刀階段四：資訊隔離盲測）：再新增兩顆身分。**\n  `cpp:bits_is`（整排的三個問句）與 `cpp:bits_as`（整排換成整數／字串），\n  ＋ 既有那顆「整排改值」多一個**常態留空的位置**接點。\n  🔴 **三顆都是探索報告以「語料 0 處」拒絕過的**，而盲測十支裡\n  `to_ulong` 用了 4 支、帶位置的 `set(i)` 多支、`cout << bs` 1 支。\n  > **「語料 0 處」量到的是【這批語料的人怎麼寫】，\n  > 不是【這個寫法有多常見】——而它在兩刀內錯了四次。**\n📈 **2026-09-20（帶參數的巨集）：掃描檔數 1277 → 1280。**\n  **母體變大，缺陷那幾格未動**——新增一顆身分（`cpp:define_func`）＋ 一支樹修復（`languages/cpp/lang/macro-expand.ts`）＋ 一支回歸網（`roundtrip-cpp-func-macro.test.ts`）。\n"
}
