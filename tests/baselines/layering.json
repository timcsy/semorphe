{
  "_meta": {
    "guard": "layering",
    "measuredAt": "2026-08-25",
    "rule": "相依只准往下：core 不得 import ui／languages／components／vscode；languages 與 components 不得 import ui／vscode；ui 不得 import languages／vscode。組裝點（src/ui/app.ts）具名豁免，而豁免附上數字。",
    "note": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。\n🔴 **剩下的 5 筆是同一團**：`src/languages/style.ts` 的 `CodingStyle` 與 `core/types.ts` 的 `StylePreset` 是**兩份描述同一件事的型別**（`'iostream'|'cstdio'` vs `'cout'|'printf'`），中間靠一個轉換函式。\n2026-08-24 已經把轉換從 `sync-controller` 搬進語言那側（`detectStyleExceptionsForPreset`），於是引擎不再認識 C++ 的詞彙；**剩下四個 UI 檔仍直接 import 那個型別**，而根治要先答「這兩份型別哪一份是真的」。\n第五筆 `core/language-executors.ts → languages/cpp/builtins` 是既有的 P9 債。\n⚠️ **組裝點豁免今天是 10**——它印出來是為了讓「它是組裝點所以沒關係」有一個上限。\n⚠️ 2026-08-25：「控制項離開積木面板」那一刀新增了控制項登錄表（`core/host/controls.ts`）與宿主那側的接手，**語料因此變大——不是退步**。\n⚠️ 2026-08-25：網頁版狀態列改成「文字項目 ＋ QuickPick」（`ui/toolbar/quick-pick.ts`、`ui/layout/status-bar-controls.ts`），語料因此變大——**不是退步**。\n⚠️ 2026-08-25：1167 → 1168，新增 `core/projection/diagnostic-projection.ts`。\n⚠️ 2026-08-25「行動版 ＝ 第三個渲染器」：刪除 `ui/toolbar/{topic,style,locale}-selector.ts` 與 `setupSelectors`／`#mobile-selector-parking`／「把選擇器搬進漢堡選單」那整段——那四顆下拉變成 `ControlState` 的渲染器。🔴 **語料縮水而正當**：違規數未動（跨層 5 筆、直接呼叫視圖 70、組裝點豁免 10），已確認是「東西真的被刪了」，不是「掃描器不再吃到它」。\n⚠️ 2026-08-25「行動版的選單改成往下鑽的 QuickPick」：刪除 `ui/toolbar/mobile-menu.ts` 與其測試、`renderSheetControls`、`mobile-sync-btn`。🔴 **語料縮水而正當**——已確認是「東西真的被刪了」。\n⚠️ 2026-08-25：新增 `ui/alt-layout-block.ts`。\n⚠️ 2026-08-25：1165 → 1166，新增 src/migrations/block-shape-changes.ts。語料變大。\n🎯 **2026-08-26：組裝點豁免 10 → 9**——`cpp_var_declare_expression` 的命令式定義退場，**連帶整個 `setLanguageInputNames`\n  注入契約刪除**——它從十二個插槽名縮到一個，而最後一個的消費者就是那顆積木。\n  ⚠️ 那道「沒注入就拋錯」的守衛也一起走：**它守的是一份空契約**。\n  組裝點少了一個「認得語言插槽名」的理由。\n🎯 **2026-08-26：組裝點豁免 9 → 0。**`app.ts` 的語言管線 9 → 0：那九處寫死的 `cpp/*` import 換成**語言套件宣告的能力**\n  （`installLifters`／`installExtractStrategies`／`declarations`／`diagnosticRules`／\n   `styleExceptions`／`createCodeShaping`）。\n⚠️ 2026-08-26：新增 `core/flow/vocabulary.ts`（流程視圖的詞彙：標題／位置名／值的顯示文字）＋ 第七十八條護欄。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：第八十條護欄（audit-flow-connect：流程視圖上的線只有父子關係）＋ `core/flow/connect.ts` ＋ 流程面板的拉線。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：(d) palette（`core/flow/palette.ts`：讀工具箱的【輸出】）＋ (e) 語句重排（`tryReorder`）。**輸入量變大，不是退步**。\n⚠️ 2026-08-26：第八十一條護欄（audit-layout-presets：佈局預設列的是層不是面板）＋ `core/host/layout-presets.ts` ＋ `applyLayout`／`setDividerVisible`／`layout` picker。**輸入量變大，不是退步**。\n⚠️ 2026-08-27：語料變大——新增 `core/flow/presets.ts`（把工具箱的 `extraState` 翻譯成流程視圖的骨架）與它的單元測試。**輸入量，不是耦合。**"
  },
  "掃描檔數": 1171,
  "跨層相依": 5,
  "組裝點豁免": 0
}
