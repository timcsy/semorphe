{
  "_meta": {
    "guard": "declared-slots",
    "measuredAt": "2026-08-10",
    "rule": "跑 lift 在真實語料上（掃測試檔裡的 C++ 片段），比對「實際產出的非空接點」與「concepts.json 宣告的 children 鍵」。⚠️ **是實測不是靜態掃描**——靜態掃 `createNode(` 看得到產生了什麼概念，看不到它被掛在**哪個接點**下，而後者才是這條護欄要問的。語料沒碰到的元件歸「無法確定」，**不計入安全**——那個數字是這條護欄的誠實度指標。",
    "note": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。 ⚠️ 2026-08-10 確定違規 16 → 1（spec 106）：**下降的原因是「因為宣告了」，不是「因為實作了」**——系統一行行為都沒變（`declaration-change-parity` 的產生碼／來回／執行輸出逐字相同），只是我們終於把 lift 一直在產出的接點寫進宣告。剩下的一筆是 `cpp:if` 的 `else_body`，它牽涉 `cpp:if_else` 是不是死概念，而**身分決定不可撤回**，要人拍板。⚠️ **同一次提交裡 `conformance.json` 的數字上升 1 → 10**——那是這次改動的直接後果：補上接點宣告之後，符合性護欄才開始檢查那些接點。**那是揭露不是退步。**"
  },
  "確定違規": 1,
  "無法確定": 8,
  "違規清單": [
    "cpp:if: else_body"
  ]
}