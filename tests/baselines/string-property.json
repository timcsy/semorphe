{
  "_meta": {
    "note": "📈 **2026-09-19（64 位元整數）：輸入量上調，缺陷格未動。**\n  新增 `interpreter/int64.ts`（「|v| ≤ 2^53 是 number，超過是 bigint」的不變式）\n  ＋ `core/scalar-types.ts`（拼法 → 執行期種類的登記處，核心給機制、語言給資料）\n  ＋ `languages/cpp/scalar-types.ts`（C++ 那份資料，**在模組頂層**）。\n  🔴 修的是「`long long` 在這個直譯器裡是一個 double」——\n  `ans*x%P`（乘積 1e18）失真、`nt>>=1` 被截成 32 位元、\n  `ll n = 2e9` 印成 `2e+09`、函式回傳值完全沒有依宣告型別轉換。\n  🟢 跨層相依未動（5）：`scalar-types.ts` 住在核心，資料由語言套件推進來。\n  讀數：內容真的不同 **5 → 3**（累計 7 → 3）、一致 147 → 151。\n\n📈 **2026-09-19（轉型的三種寫法 ＋ 除以零不是錯誤 ＋ 八進位 ＋ 二維扁平初值）：\n  輸入量上調，缺陷格未動。**\n  新增 `tests/probes/studycpp-has-blocks.test.ts`（語料用到的元件都有積木嗎）\n  ＋ `interpreter-matches-compiler` 的 15 條最小重現。\n  🔴 這一刀讓語料的**「內容真的不同」第一次下降**（7 → 5）——\n  那一欄從 2026-09-16 補上第五個面向以來，八刀都沒有動過。\n\n📈 **2026-09-19（`#define` 取的小名與方法名的拼法）：輸入量上調，缺陷格未動。**\n  新增 `lifter.ts` 的 `recordMacroAlias`（`#define A B` 與 `typedef` 同一張表）、\n  `lifters/io.ts` 的 `resolveMethodAlias`（**原名查不到、別名查得到**時才換）、\n  `container_append`／`set_insert` 的 `method` 屬性（記住使用者寫的那個方法名），\n  以及 `interpreter-matches-compiler` 的 14 條最小重現。\n  🔴 修的是三個**從第一天就沒有work過**的東西：`#define pb push_back`、\n  `#define z '0'` 這一類字面量、以及二維原生陣列上的結構成員。\n\n字串屬性不得裝結構。「非原子」不等於錯——判定在 tests/assets/string-property-decisions.json。\n🔴 而語料乾淨不代表模型對：它代表語料是照著模型長的。",
    "ratchet": "數字只准下降。調整此檔即為顯式下調，須在 commit 訊息說明原因。"
  },
  "語料段數": 1336,
  "要看": 26,
  "孤兒判定": 0,
  "details": [
    "cpp:var_declare.type",
    "cpp:array_declare.type",
    "param_decl.type",
    "cpp:func_call.name",
    "cpp:enum.values",
    "cpp:try_catch.catch_type",
    "cpp:func_def.return_type",
    "cpp:pointer_declare.type",
    "cpp:var_declare_const.type",
    "cpp:vector_declare.type",
    "cpp:using_alias.orig_type",
    "cpp:include.header",
    "cpp:var_declare.name",
    "cpp:loop_range.var_type",
    "cpp:malloc.type",
    "cpp:sizeof.target",
    "cpp:typedef.orig_type",
    "cpp:array_2d_declare.type",
    "cpp:operator_overload.param_type",
    "cpp:lambda.capture",
    "cpp:cast_dynamic.target_type",
    "cpp:cast_const.target_type",
    "cpp:map_declare.value_type",
    "cpp:set_declare.type",
    "cpp:priority_queue_declare.type",
    "cpp:deque_declare.type"
  ],
  "note": "📈 **2026-09-17（迭代器的 round-trip 那一關）：語料變大，而「要看」跟著 +2。**\n  新增的兩筆是 `cpp:set_declare.type`（`pair<int,int>`）與 `cpp:container_append.obj`\n  ——**不是新缺陷，是既有設計問題的新暴露**：我的新測試第一次寫出\n  `multiset<pair<int,int>>` 這種形狀，而它本來就會落進「字串屬性裝了結構」。\n\n  🔴 而這條護欄自己的註解逐字說過這件事：\n  > **語料乾淨不代表模型對：它代表語料是照著模型長的。**\n\n  ⚠️ 所以這個 +2 是**好的**：它證明那條護欄在新形狀出現時真的會說話。\n  🟠 該修的是那個設計（屬性升格成接點），而它住在 `concepts/概念代數.md`\n  的「屬性的結構化邊界」——不是這一刀。\n\n\n\n📈 **2026-09-17（管線 180，第二輪盲測）：語料段數 1052 → 1089。**\n  新增的是常駐測試：迭代器一族的模糊測試網（＋6 支）、`const` ＋ 陣列（＋2 支）、\n  `static` ＋ 陣列的誠實降級（＋1 支）、成員初始化列與 g++ 的最小重現（＋3 支）。\n  **輸入量長了，判準沒有變。**\n\n🟢 **而「要看」停在 37 沒有上升**——量到的新面孔有兩個，兩個都寫了判定：\n  `cpp:raw_code.code`（降級目標，那一格裝的就是「還沒被結構化的那一段」）\n  `cpp:array_2d_declare.type`（與已判定的一維那顆同一族）\n  ⚠️ `raw_code` 這一格**在此之前不在語料裡**：測試庫裡唯一會降級的片段是\n  `const ＋ 陣列`，而那個缺陷修好之後它的覆蓋一度消失。\n  > **一個降級目標如果只被「還沒修好的缺陷」覆蓋到，\n  > 那它的覆蓋會在缺陷修好的那天消失。**\n\n\n🔴 **2026-09-17（第五關的覆核）：「要看」37 → 38，而這是一個【上調】。**\n  多的那一格是 `cpp:array_2d_declare.type`——**不是一種新的違規，是同一個\n  早就在列的開放問題（`.type` 那一族，37 個裡有 11 個）多了一個看得見的成員**，\n  因為語料裡第一次出現了帶 `const` 的二維陣列宣告。\n\n  ⚠️ **我一度替它寫了一筆判定，而那筆判定的理由是假的**：它寫著\n  「與已在列的一維那顆同一個判定」，而一維那顆（`cpp:array_declare.type`）\n  **根本不在判定裡——它就在「要看」那一列**。判定已撤回。\n\n  > **一筆判定如果理由是「另一筆已經判過了」，那要先確認另一筆真的判過。\n  > 否則兩筆一起變成「看過了」，而其實一筆都沒看。**\n\n  🟢 同一輪加的另一筆判定（`cpp:raw_code.code`）留著：那一格裝的**就是**\n  「還沒被結構化的那一段」，替它發明結構等於宣稱我們看懂了它。\n\n\n📈 **2026-09-18（第六關）：語料段數 1091 → 1093。** 同上，兩支新的形態測試。\n\n\n🎯 **2026-09-18（接收者重構）：「要看」38 → 37（下調）。**\n  離開的那一筆是 `cpp:container_append.obj`——**接收者不再是一個字串屬性**，\n  它是一個接點。34 顆膠囊一起搬，而這條護欄只看得到其中一顆，\n  因為**它列的是「語料裡真的裝過結構的那些」**：其餘 33 顆的 `obj` 在語料裡\n  一直只裝著裸的名字（`v`／`s`），所以它們從來沒有被這條護欄指名。\n\n  > **一條「只報真的犯過的」護欄，量不到「還沒犯但遲早會犯」的那些\n  > ——而那正是它誠實的地方：它報的每一筆都是事實。**\n\n\n📈 **2026-09-18：語料段數 1093 → 1101。** 同上，接收者那批常駐測試。\n\n\n🔴 **2026-09-18（接收者重構的尾巴）：「要看」37 → 38，誠實上調。**\n  新面孔是 `cpp:map_declare.value_type`——它裝到了 `vector<int>`。\n  **不是一種新的違規**，是**同一個早就在列的開放問題**（`.type` 那一族）\n  多了一個看得見的成員：語料裡第一次出現 `map<int, vector<int>>`\n  （相鄰串列的標準寫法，而它是這一刀新加的常駐測試帶進來的）。\n\n  > **一條「只報真的犯過的」護欄，它的數字會隨語料長\n  > ——而那不是退步，是它終於看到了。**\n\n\n📈 **2026-09-18（`->` 的接收者也換成接點）：語料再長一點。**\n  新增的是兩支常駐測試（`m.begin()->second`、`it->second` 當引數）。\n\n\n📈 **2026-09-18（容器語義那一批）：語料段數 1104 → 1112。** 同上，新的常駐測試。\n\n\n📈 **2026-09-18（型別別名指向容器）：語料再長。**\n  新增的是一支常駐測試（`typedef map<…> Graph;` 的三種別名 ＋ 產出要保住短名字）。\n\n\n📈 **2026-09-18（型別別名指向容器）：語料段數 1112 → 1118。**\n  🟢 **而「要看」停在 38**：一度多了一筆 `cpp:container_empty.container_kind`\n  （值是 `deque<int>`）——那是修「`var` 不是一個型別」的副作用：一般變數\n  現在記得住自己真正的型別了。\n\n  ⚠️ 而**「種類」不該帶著樣板參數**：問那一支的人問的都是「這是哪一種容器」，\n  而容器宣告那幾顆元件記下來的本來就是基底名（`cpp:map_declare` → `map`）。\n  所以一般變數那一條**對齊了它**（`deque<int>` → `deque`），那一筆因此離開。\n  > **同一個問題有兩種答案的形狀時，先讓它們變成一種。**\n\n\n📈 **2026-09-18（邊走邊刪）：語料段數 1118 → 1126。**\n  新增的是常駐測試：`erase(it++)` 的三種容器、`it = c.erase(it)` 的正向錨點、\n  兩個位置存在變數裡、迭代器傳值進函式、字元相減之後的整數除法。\n  🟢 **而「要看」停在 38**——新形狀沒有帶進新的「字串屬性裝了結構」。\n\n\n📈 **2026-09-18（別名 ＋ 修飾詞的參數型別）：再長一段。**\n  新增的是一支常駐測試：`typedef map<int,set<int>> Graph;` 當 `const Graph &` 參數傳進去，\n  而它**同時斷言身分**（`cpp:container_find`，不得是 `cpp:string_find`）。\n\n\n📈 **2026-09-18（三元式的第三個運算元是指定式）：語料再長。**\n  新增的是五支與 g++ 對照的常駐測試：併查集的 `p[x] = find(p[x])`、\n  沒被選到那一支不得有副作用（正向錨點）、三元式裡的成員式左值與複合指定、\n  四種左值的鏈式指定都要求值成被指派的值。\n\n\n🔴 **2026-09-18（`cpp:deque_declare`）：「要看」38 → 39，誠實上調。**\n  新面孔是 `cpp:deque_declare.type`——它裝到了 `pair<int,int>`。\n  **不是一種新的違規**，是**同一個早就在列的開放問題**（`.type` 那一族）\n  多了一個成員：同族的 `cpp:vector_declare.type`／`cpp:set_declare.type`／\n  `cpp:priority_queue_declare.type` **也都在「要看」那一欄**（我去看過了，\n  它們不在判定檔裡——判定檔只有十筆，全部是「文字本來就是字串」那一類）。\n\n  ⚠️ 上一次有人替這一族寫判定時，理由是「與已判過的同族一致」而**那一筆其實沒判過**，\n  判定因此被撤回。這次不重蹈：**它就是還沒判，所以它留在「要看」。**\n\n  > **一條「只報真的犯過的」護欄，它的數字會隨語料長——而那不是退步，是它終於看到了。**\n\n\n📈 **2026-09-18（`cpp:deque_declare` ＋ `push` 照元素型別長）：語料再長。**\n  新增六支與 g++ 對照的常駐測試：雙端佇列記得住元素型別、建構子引數、\n  三種容器的 `push`、優先佇列裝一對值時的堆頂，以及兩支正向錨點\n  （純量的雙端佇列與優先佇列不得被弄壞）。\n\n\n📈 **2026-09-18（結構化繫結 ＋ 走訪的容器換成接點）**：新增的常駐測試與一顆元件。\n\n\n🎯 **2026-09-18（走訪的容器換成接點）：「要看」40 → 39（下調）。**\n  離開的是 `cpp:loop_range.container`——它裝著 `d2[pt]`／`ar[P]`（4 種非原子），\n  而那正是「一個欄位只裝得下一串文字」的症狀：執行期拿那串文字去查變數，\n  說「沒有宣告過 `d2[pt]`」。語料 3 支。\n\n  ⚠️ **屬性是整個退場的，不是「留給舊存檔」**：留著它等於留下第二份真相，\n  而那一格仍然會裝著同一個結構。舊存檔由 `SHAPE_CHANGES_V20` 丟快取重 lift\n  ——**程式碼那一份從來沒有掉過**。\n\n  > **一個「留給舊存檔」的欄位，如果它裝的是同一個結構，\n  > 那它不是相容性，是第二份真相。**\n\n\n📈 **2026-09-18（round-trip 那一關）：新增一個五面向的回歸網**\n  （`roundtrip-cpp-structured-binding.test.ts`，34 支），而它順手量到兩個既有缺陷：\n  `unordered_map` 的鍵型別（同一顆元件的第二個樣板名）與\n  `int x, y;` 產不回一行（只在「再走一趟」時才看得出來）。\n  🟢 **語料的語義不動點因此從「走樣 6」變成「走樣 0／218」**。\n\n\n📈 **2026-09-18（模糊測試第二輪，容器）：新增一個 17 支的回歸網。**\n  資訊隔離的盲測 10 支，第一輪 **1/10**——而那是在前一天同一批元件走完\n  一整條管線、全套 7117 綠之後量的。\n  > **資訊隔離量到的不是「我做得對不對」，是「我想不想得到」。**\n\n\n📈 **2026-09-18（整合那一關）：新增四支跨概念的組合測試**\n  ——新元件放進條件的主體／巢狀的迴圈／函式／與既有的走訪並列。\n  ⚠️ 理由是完備性護欄自己的檔頭：「本護欄只抓『殼』……**不檢測條件性正確**\n  ——單獨測通過、組合起來才壞的問題」。\n\n\n📈 **2026-09-18：新增第 119 條護欄 `audit-silent-downgrade`。**\n  它從**瀏覽器驗收**來：一顆宣告了抽象父、又不在某一軌課程清單裡的元件，\n  會被降級成那個父——而它比父多的那幾格**沒有地方放**。\n  `auto[pt, d] = BFS.front();` 在那一軌變成 `int x;`，**兩個名字沒了**。\n  > **降級只咬得到「宣告了自己屬於誰」的那些元件\n  > ——而那正是最該被完整保留的那些（它們有更多格）。**\n\n\n🎯 **2026-09-18：語料縮水 4 段（下調），而縮的是【標頭前言】不是程式。**\n  六個測試檔的手列標頭（4–6 行）換成一行 `#include <bits/stdc++.h>`\n  ——那些被收掉的段落**本來就是語法不完整的片段**（一疊 `#include`）。\n\n  🟢 **判準是「殘差本身有沒有變」**：387 字元／16 個節點**一個字都沒變**，\n  而完整語料段還多了 1。**被量的程式一支都沒少，少的是前言。**\n\n  > **一次「語料縮水」要先問它縮在哪一欄：\n  > 縮在分母而分子不動，那是輸入變乾淨了；兩邊一起縮，才是覆蓋掉了。**\n\n📈 **2026-09-18（指定值保住宣告的型別）：語料變大，「要看」未動。**\n  `interpreter-matches-compiler` 加三條最小重現（char 走碼位那一路 · 整數不得變成小數 ·\n  ★ 正向錨點：小數與字串不得被截斷），語料段 1214 → 1226。\n  🔴 前兩條在改動前是紅的、錨點兩邊都綠——**那個錨點是這一刀最大的風險**：\n  一個「照宣告的型別轉」的實作很容易把 `double` 也一起截斷。\n\n📈 **2026-09-18（範圍那一族：兩端從字串屬性換成接點，管線 188）：語料變大，缺陷那幾格未動。**\n  10 顆範圍演算法的 `begin`／`end`（＋前綴和的 `dest`）從**字串屬性**換成**接點**，\n  ＋ 三顆新元件（`cpp:range_find`／`cpp:range_unique`／`cpp:range_remove`）\n  ＋ `cpp:container_iter` 多一個 `call` 屬性（`begin(a)` 這個自由函式形式）\n  ＋ 27 顆宣告式元件補上 `forLoopPart`（同一個病的第三次，這次整族一起補並加了一條護欄）。\n  🔴 而拆掉那個 regex 之後發現**結構早就在了**：容器與位置在這個直譯器裡是同一個形狀\n  （`interpreter/pointer.ts` 的 `isCellPointer`），`A+n` 的位置算術也早就有人做。\n  > **那條 regex 解析的東西，執行期早就會算了——它不是在補一個缺口，它是在繞過一個已經存在的機制。**\n\n📉 **2026-09-18：39 → 26 —— 這是【下調】，而它是這一刀的正題。**\n  範圍那一族的 13 個字串屬性整批退場（`range_sort`／`range_reverse`／`range_fill`／\n  `range_fill_sequence`／`range_sum`／`range_sum_partial` 的 `begin`／`end`／`dest`），\n  換成 expression 接點。\n  🔴 而它們裝的正是這條護欄在講的東西：**原始碼的一段文字，用完要 parse 回結構**。\n  執行期那條 regex 只認得「一個裸識別字」開頭的東西，於是學生真的寫的\n  `begin(a)`、`d2[i].begin()`、`d2[0]` 三種形狀全部斷在那裡（語料 4 支）。\n  > **需要 parse 回結構才能用的字串，就不該是字串。**（同一個病的第三次：\n  > `SHAPE_CHANGES_V19` 的接收者 39 顆 · `V20` 的走訪對象 1 顆 · 這裡 10 顆）\n\n📈 **2026-09-18（round-trip 那一關，管線 188）：語料再變大一點，缺陷那幾格未動。**\n  新增 `tests/integration/roundtrip-cpp-range-slots.test.ts`（四個面向 × 18 個形狀）。\n  🔴 而那一關抓到一個**真的缺陷**：`v.erase(a, b)` 走一趟積木回來變成 `v.erase(a)`\n  ——刪一整段變成刪一格，**而它編得過**。修法是讓第二個位置有自己的接點。\n  🔴 順帶抓到第二個：**多宣告子的型別從來沒被記下來**（`vector<int> v, w;` 的\n  `v` 查不到型別），因為宣告子是由具名策略直接組出來的，不經過那個收集器的掛鉤。\n\n📈 **2026-09-18（盲測第一輪：範圍演算法，管線 188 階段四）：語料變大，缺陷那幾格未動。**\n  新增 `tests/integration/fuzz-cpp-ranges.test.ts`（10 支，2 綠 ＋ 8 根釘子）\n  ＋ `interpreter-matches-compiler` 一條最小重現（`&a[n]` 當結尾）。\n  🔴 盲測 **2/10**，而那是好消息：八支**沒有一支是語義錯**，全部是缺元件，\n  加上一個真缺陷（`&a[n]` 被 `INDEX_OUT_OF_RANGE` 擋下來——**取位址不讀那一格**）。\n\n📈 **2026-09-18（整合關卡，管線 188 階段五）：語料再變大一點。**\n  `block-shape-changes` 加 v21 的遷移測試 ＋ 三顆新元件的自證各自成一支\n  ＋ 一根新釘子（聚合宣告的括號形式）。\n\n📈 **2026-09-18（瀏覽器驗收，管線 188 階段六）：語料變大，缺陷那幾格未動。**\n  新增 `slotCode`（空插槽的預設）＋ 九條「空插槽產出的碼要編得過」的測試。\n  🔴 這一關抓到的：剛拖出來的積木**在畫面上是對的**（可讀的圓洞 ＋ ⚠️），\n  而產出的程式碼是 `find(v.begin(), v.end(), );`——**編不過**。\n  > **一個半完成的程式該長什麼樣，是一個設計決定；\n  > 而「有的格子有答案、有的格子留一個語法錯誤」不是決定，是沒有決定。**\n\n📈 **2026-09-19（I/O 操縱子三顆，管線 189）：語料變大，缺陷那幾格未動。**\n  `cpp:io_flush`（`cout << flush`）· `cpp:io_mode`（`setw`／`setprecision`／`setfill`）·\n  `cpp:io_mode_number`（`fixed`／`scientific`），\n  ＋ 串流狀態（`WeakMap` 以 `ctx.io` 為鍵，**一次執行一份**）\n  ＋ `interpreter/decimal.ts`（十進位的四捨五入，收尾照 C 的**逢五取偶**）。\n  🔴 而命名是**詞彙表逼出來的**：第一版叫 `stream_*`，三個字都不在表裡；\n  換成既有詞（`io` ＋ `mode` ＋ `number`）之後名字反而對得上同族\n  （`cpp:io_sync`／`cpp:io_tie` 早就在那裡）。**只補了一個新詞：`flush`。**\n\n📈 **2026-09-19（二維陣列的維度換成接點，第 190 刀）：語料變大，缺陷那幾格未動。**\n  `cpp:array_2d_declare` 的 `rows`／`cols` 從屬性換成接點——**同一個病的第四次**\n  （`V19` 接收者 39 顆 · `V20` 走訪對象 1 顆 · `V21` 範圍的兩端 10 顆 · 這裡 1 顆）。\n  🔴 而這一顆是一個**不對稱**：同族那顆一維陣列的 `size` 早就是接點。\n  ⚠️ 第七十二條看不到它——測試語料裡的維度永遠是數字字面值。\n  > **語料乾淨不代表模型對：它代表語料是照著模型長的。**\n\n📈 **2026-09-19（管線 197・階段三：位置的相鄰一格）：上調的是【輸入量】，缺陷那幾格未動。**\n  新增一顆膠囊 `cpp:pointer_step`（`prev`／`next`，方向是一個屬性）\n  ＋ 兩支常駐測試檔：`roundtrip-cpp-pointer-step`（四個面向 44 條）與\n  `roundtrip-cpp-cast`（15 條）。\n  🔴 後者是**從語料蒸餾**的，而它蒸餾的是我自己第 194 刀留下的兩個迴歸：\n  `int()` 產成 `(int)`（不是合法的 C++）與 `(ll)(x+1)*z` 產成 `(ll)x+1*z`\n  （合法而算另一件事）。抓到它們的是**語料的探針**，而那支探針\n  沒設 `STUDYCPP_DIR` 就會跳過。\n  > **跳過的護欄與不存在的護欄長得一樣。**\n\n📈 **2026-09-19（管線 197 階段四：盲測第二輪）：語料段數 1291 → 1297。**\n  22 支常駐測試（十支盲測程式的整支與蒸餾）。🟢 **而「要看」沒有上升**\n  ——`cpp:pointer_step` 新增的 `count` 是**接點**不是字串屬性。\n\n📈 **2026-09-19（管線 197 階段五：三根死釘子換成真的測試）：輸入量再長一點。**\n  五支新測試（轉型印字元 · `sizeof` 算長度 · 列舉常數當值 ·\n  前置遞增當初值的不動點 · 列舉的明寫值）。\n  🎯 **而缺陷帳同時下調 61 → 58**——那三根躺了半年的 `describe.skip`，\n  底下每一件事都早就修好了，而**它們只有標題、沒有本體，所以不會變紅**。\n  ⚠️ 第一版把那五支寫成**單行帶字面 `\\n`** 的樣板字串，於是語料掃描器\n  拿到的是編不過的一行，「兩邊都不成」+5——**那是我自己造的雜訊**。\n  改成真的多行之後「兩邊都跑得動」+5，那才是真的覆蓋。\n  > **一份「語料變大了」的讀數，要先問長在哪一欄。**\n\n📈 **2026-09-19（第 199 刀階段三：round-trip）：輸入量上調。**\n  新增 `roundtrip-cpp-bits.test.ts`（67 支），而它抓到兩個**既有**的東西：\n  ① `staleShapeIn` 看不見接點改名 ② `cout << (n << 1)` 走一趟積木回來印 51 而不是 10。\n\n📈 **2026-09-20（第 199 刀階段四：盲測）：再新增兩顆身分 ＋ 19 支常駐測試。**\n📈 **2026-09-20（語料的三個缺陷）：語料段數 1317 → 1319。**\n  同一批 15 條最小重現讓母體變大，而「要看」那一格（26）與「孤兒判定」（0）都一筆未動。\n\n📈 **2026-09-20（帶參數的巨集）：語料段數 1319 → 1324。**\n  **輸入量變大，缺陷那幾格未動**——新增一支回歸網（`roundtrip-cpp-func-macro.test.ts`，35 支）\n  ＋ `interpreter-matches-compiler` 多 9 條最小重現（拿 g++ 當權威）。\n\n📈 **2026-09-20（帶參數的巨集，第三關）：語料段數 1324 → 1328。**\n  **輸入量再變大，缺陷那幾格未動**——第三關補了七支：\n  三層巢狀 · `cin >>` 那一種樹形狀 · 巨集體裡又呼叫巨集 ·\n  引數個數對不上 · 後面沒有語句 · 舊存檔的布林排版註記 · 字串與布林同住一格。\n\n📈 **2026-09-20（帶參數的巨集，第四關：資訊隔離盲測）：語料段數 1328 → 1334。**\n  **輸入量變大，缺陷那幾格未動**——新增 `fuzz-cpp-macros.test.ts`（10 支盲測程式，\n  出題者看不到原始碼）＋ `interpreter-matches-compiler` 多 6 條最小重現\n（括號裡的逗號運算式，盲測抓到的）。\n  ⚠️ 那 10 支的標頭**一律換成 `bits/stdc++.h`**——出題者手列了標準標頭，\n  而第 120 條護欄當場擋下（手列 2 → 3）。換完輸出逐字不變。\n\n📈 **2026-09-20（第 201 刀第五關：整合關卡）：語料段數 1334 → 1336。**\n  新增的是**一根釘子 ＋ 一支 ★ 錨點**（運算式位置的語法錯誤要原文照抄／\n  語句位置那一路不得退步），以及兩根死釘子拔掉後**開回來的兩支測試**。\n  🟢 缺陷那幾格未動。\n"
}
