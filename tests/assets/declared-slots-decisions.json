[
  {
    "componentId": "cpp:analog_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:analog_resolution",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言）。"
  },
  {
    "componentId": "cpp:analog_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:bits_count",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`__builtin_popcount` 這個名字是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:delay_microseconds",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/micros/spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言，含「同一個時鐘」那條）。"
  },
  {
    "componentId": "cpp:dht_declare",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerPlainTypeComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。⚠️ 而 `declaresVariableType` 那一格才是關鍵：它讓作用域記住型別，之後那個變數上的方法才查得到主人。接點由同批的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "cpp:dht_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:dht_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:digital_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:digital_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:eeprom_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:eeprom_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:if_else",
    "cause": "宣告過的降級目標",
    "reason": "components.json 明文 skipPaths:['lift']、skipReasons.lift='degradation-target'，由 cpp:ternary 降級抵達。**它本來就不該從語料 lift 出來**——不是缺口。"
  },
  {
    "componentId": "cpp:include_local",
    "cause": "語料沒覆蓋",
    "reason": "`#include \"my.h\"`（雙引號的本地標頭）——測試檔裡的片段幾乎都用角括號的系統標頭。加一段語料就會動。"
  },
  {
    "componentId": "cpp:io_sync",
    "cause": "lift 是登錄表裡的資料，靜態掃不到",
    "reason": "這一路搬回膠囊的是**一筆資料**（`ios::sync_with_stdio` 這個名字屬於我），判別邏輯留在共用路由器。掃描器讀不到執行期才組起來的登錄表，所以歸「無法確定」——**而宣告與實際是一致的**：`children.value` 對上 `argSlots: ['value']`，膠囊自證測從真語料 lift 出這顆並斷言接點在原地。⚠️ 這與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:io_tie",
    "cause": "lift 是登錄表裡的資料，靜態掃不到",
    "reason": "這一路搬回膠囊的是**一筆資料**（`tie` 這個方法名屬於我），判別邏輯留在共用路由器。掃描器讀不到執行期才組起來的登錄表，所以歸「無法確定」——**而宣告與實際是一致的**：`children.value` 對上 `argSlots: ['value']`，膠囊自證測從真語料 lift 出這顆並斷言接點在原地。⚠️ 這與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:istringstream_declare",
    "cause": "語料沒覆蓋",
    "reason": "istringstream 宣告。sstream 的語料只有幾段而且都在做別的事。加一段就會動。"
  },
  {
    "componentId": "cpp:lcd_at",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:lcd_clear",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:lcd_declare",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerPlainTypeComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。⚠️ 而 `declaresVariableType` 那一格才是關鍵：它讓作用域記住型別，之後那個變數上的方法才查得到主人。接點由同批的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "cpp:lcd_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:lcd_print",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:math_constrain",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言）。"
  },
  {
    "componentId": "cpp:math_is_nan",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。"
  },
  {
    "componentId": "cpp:micros",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/micros/spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言，含「同一個時鐘」那條）。"
  },
  {
    "componentId": "cpp:millis",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:pin_attach",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是一支**具名策略**（`lift-strategy.ts` ＋ `lift-pattern.json`）——判別要跑真邏輯（結構比對 ＋ 走整棵樹查「這個名字有沒有被當腳位用」），所以靜態掃描看不到接點。⚠️ 而這顆**沒有子接點**（`children` 是空的），三個參數全是積木上的欄位。接點的正確性由這顆自己的 `spec.test.ts` 釘住（含「沒被當腳位用的常數不准被搶走」那條負向）。"
  },
  {
    "componentId": "cpp:pin_constant",
    "cause": "元概念",
    "reason": "它是 `lift-pattern.json` 的純資料樣式（六個識別字 → 一顆身分），**沒有接點**——`children` 是空的。與 `builtin_constant` 同一個形狀。"
  },
  {
    "componentId": "cpp:priority_queue_peek",
    "cause": "語料沒覆蓋",
    "reason": "priority_queue 的 top()。語料裡有 priority_queue 但沒有取頂端的片段。加一段就會動。"
  },
  {
    "componentId": "cpp:pulse_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pulse_read/spec.test.ts` 釘住（含「可選的第三格不得產出空逗號」與「它是運算式，回傳值進得了算術」兩條）。"
  },
  {
    "componentId": "cpp:pwm_attach",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:pwm_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:pwm_tie",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:pwm_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:queue_back",
    "cause": "辨識到不了",
    "reason": "**真的缺口**：`.back()` 在辨識層不看容器型別，六種容器一律判成 cpp:vector_back。這顆身分從程式碼永遠得不到，只能從積木拖。⚠️ **補再多語料也碰不到。** 見 draft/2026-08-11-容器方法的辨識不看型別.md"
  },
  {
    "componentId": "cpp:range_find_lower",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`lower_bound` 帶三個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_find_upper",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`upper_bound` 帶三個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_max",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`max_element` 帶兩個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_min",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`min_element` 帶兩個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_remap",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:raw_expression",
    "cause": "元概念",
    "reason": "同 cpp:raw_code，運算式位置的版本。"
  },
  {
    "componentId": "cpp:serial_count",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（`Serial` 上的方法，第 0 批／階段 6.16）——判別邏輯要看 `obj`，而分支住在執行期組起來的登錄表裡，靜態掃描看不到接點。⚠️ 這兩顆的接點是空的（零引數），而正確性由 `src/components/cpp/serial_count/spec.test.ts` 釘住（含「別的物件的 read 不得被搶走」與「沒有資料時回 -1 而 -1 是真」兩條）。"
  },
  {
    "componentId": "cpp:serial_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（`Serial` 上的方法，第 0 批／階段 6.16）——判別邏輯要看 `obj`，而分支住在執行期組起來的登錄表裡，靜態掃描看不到接點。⚠️ 這兩顆的接點是空的（零引數），而正確性由 `src/components/cpp/serial_count/spec.test.ts` 釘住（含「別的物件的 read 不得被搶走」與「沒有資料時回 -1 而 -1 是真」兩條）。"
  },
  {
    "componentId": "cpp:servo_attach",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:servo_declare",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerPlainTypeComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。⚠️ 而 `declaresVariableType` 那一格才是關鍵：它讓作用域記住型別，之後那個變數上的方法才查得到主人。接點由同批的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "cpp:servo_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:servo_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:string_empty",
    "cause": "辨識到不了",
    "reason": "**真的缺口**：`.empty()` 一律判成中性的 cpp:container_empty。同上，補語料無效。"
  },
  {
    "componentId": "cpp:ultrasonic_trigger",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是一支**具名策略**，而它比別顆更特別：它認的是**一串連續語句**，掛在 `compound_statement` 與 `translation_unit` 上、自己走訪子節點。靜態掃描看不到它產出的 `pin` 接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住，而那支測試的重點是**四條「不該摺時不摺」**（中間插句／微秒數不對／不同腳／換算不摺）——那才是這顆的主要風險。"
  },
  {
    "componentId": "cpp:vector_make",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`vector<T>(…)` 這個呼叫形狀是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：`children.size`／`children.fill` 對上分支產出的兩個接點，膠囊自證測從真語料 lift 出這顆。"
  },
  {
    "componentId": "cpp:wifi_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:wifi_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "python:arithmetic",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🔴 而這一顆的接點（`left`／`right`）**確實需要被驗**，所以膠囊自證測有一支專門斷言「lift 出來的 left/right 兩個接點都在原地」——**不靠這條護欄**。"
  },
  {
    "componentId": "python:array_make",
    "cause": "語料沒碰到這個接點的產出",
    "reason": "串列字面的元素接點（`items`）**是空的也合法**（`a = []`），而護欄的語料裡沒有一段同時證明「元素進得去」與「宣告裡有」。\n⚠️ 正確性由這顆的 `spec.test.ts` 釘住：`a = [1, 2]` 的來回一字不差——**那一支曾經是紅的**（`$namedChildren` 這個欄位參照當時被靜默忽略，產回去是 `a = []`）。"
  },
  {
    "componentId": "python:array_make_for",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "接點（要算什麼／從哪裡走／篩選）是策略從 `list_comprehension` 的**兄弟子節點**組出來的（`for_in_clause`／`if_clause` 不是欄位）。\n⚠️ 由 `spec.test.ts` 釘住：有無篩選的來回、巢狀整顆降級、以及「那個名字不得污染外面」。"
  },
  {
    "componentId": "python:break",
    "cause": "語料沒覆蓋",
    "reason": "spec 170。與其餘 Python 元件同一條理由：這條護欄的語料是 C++ 的，碰不到 Python。\n🔴 **它是盲測抓到的缺口**（2026-08-21，`fuzz_05`／`fuzz_07`）：在它存在之前，`break` 降級成灰色積木，而**執行到那顆積木時整支程式中止**。\n> 一個誠實的降級，在【執行】那一路上仍然是一個中止。\n🟢 它沒有接點，正確性由自證測釘（含「在 for 迴圈裡也接得住」）。"
  },
  {
    "componentId": "python:cast",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "`int`／`str`／`float`／`bool` 四個名字共用一個 `call` 節點型別，判別要跑真邏輯。⚠️ 接點（`value`）的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言）。"
  },
  {
    "componentId": "python:class_def",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "方法是策略從類別的 `block` 裡逐一 lift 的，而**有一個認不出來就整顆降級**（不產出少了方法的類別）。\n⚠️ 由 `spec.test.ts` 釘住：繼承與類別層級屬性都整顆降級、來回、以及兩個實例各有各的欄位。"
  },
  {
    "componentId": "python:comment",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。\n🟢 **判準沒有被收窄**——只是誠實地說「還沒量到」。\n🟢 沒有接點。而它的 round-trip 有一支專測「不得每次多一個 `#`」。"
  },
  {
    "componentId": "python:compare",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🔴 這一顆的接點取法與同族**不一樣**（`comparison_operator` 沒有 left/right 欄位，用位置式子節點），所以自證測特別釘了那一條——**那正是「量不到的地方，宣告錯了與宣告對了長得一樣」最容易出事的形狀**。"
  },
  {
    "componentId": "python:container_append",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:container_at",
    "cause": "語料沒碰到這個接點的產出",
    "reason": "`target`／`key` 兩個接點都必填，而護欄的語料沒有一段走到靜態掃描認得的形狀。由 `spec.test.ts` 的來回（`a[0]`／`d[\"k\"]`）與執行（含負數索引）釘住。"
  },
  {
    "componentId": "python:container_enumerate",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "同一個 `call` 節點型別，判別在被呼叫的名字。⚠️ 接點（`value`）由這顆自己的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "python:container_find",
    "cause": "lift 走 operatorDispatch 的路由，靜態掃不到",
    "reason": "它的接點是在 `operatorDispatch` 的 `fieldMappings` 裡宣告的（`$namedChildren[0]`／`[1]`），而那一層靜態掃描不進去。\n⚠️ 而它有一支**對照組**測試釘住「`<` 不得被搶走」——兩筆樣式的路由不重疊。"
  },
  {
    "componentId": "python:container_size",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:container_sort",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:container_substr",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "接點是策略從 `slice` 節點拆出來的，而**兩端都可以沒有**（`xs[:2]`／`xs[-2:]`）——那件事要看冒號的位置，不能只數子節點。\n⚠️ 由 `spec.test.ts` 釘住：四種寫法、取一格不得被搶走、帶步長的整顆降級、**沒有的那一端不得被補上**、以及「切片不會超界」。"
  },
  {
    "componentId": "python:container_sum",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:container_zip",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "同上。⚠️ 接點（`first`／`second`）由這顆自己的 `spec.test.ts` 釘住，含「三串以上讓一般呼叫接手」。"
  },
  {
    "componentId": "python:continue",
    "cause": "語料沒覆蓋",
    "reason": "spec 170。與其餘 Python 元件同一條理由：這條護欄的語料是 C++ 的，碰不到 Python。\n同 `break`——盲測抓到的同一族缺口，沒有接點，由自證測釘。"
  },
  {
    "componentId": "python:exception_case",
    "cause": "沒有自己的 lift 路，接點由父概念的策略建出來",
    "reason": "`except:` 單獨寫是語法錯誤——它只在「嘗試」那顆的策略裡被拆出來（`paths.lift` 是顯式的 `null`）。由父概念的 `spec.test.ts` 一起釘住。"
  },
  {
    "componentId": "python:func_call",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。🟢 判準沒有被收窄——只是誠實地說「還沒量到」。\n🔴 而它的 `args` 接點是**可變的**（mutation），自證測釘了零引數與多引數兩端。"
  },
  {
    "componentId": "python:func_call_named",
    "cause": "語料沒碰到這個接點的產出",
    "reason": "`value` 接點必填，而護欄的語料裡那一段（`sort(key=lambda …)`）走的形狀靜態掃描認不到。\n⚠️ 由 `lambda/spec.test.ts` 釘住，而其中一支是這一輪**最危險的缺陷**：接上 `key=` 之前，`xs.sort(key=lambda x: x[1])` **排序仍然發生、仍然有輸出，而順序是錯的**。"
  },
  {
    "componentId": "python:func_def",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。🟢 判準沒有被收窄——只是誠實地說「還沒量到」。\n⚠️ 它的參數列**是一個字串**（已知的權宜，理由寫在 component.json 的 `_why`），所以只有 `body` 一個接點。"
  },
  {
    "componentId": "python:if",
    "cause": "語料沒覆蓋",
    "reason": "spec 169：一顆元件帶 elif 鏈 ＋ 可有可無的 else（`if_else` 已退場）。這條護欄的語料是 C++ 的，碰不到 Python。\n🔴 而它的接點**最容易假通過**：`elif_condition` 與 `elif_body` **靠索引配對**，而兩個清單長度不同時每一格都還在，只是接錯了人——自證測有一支專釘長度相同。"
  },
  {
    "componentId": "python:import",
    "cause": "這顆沒有接點",
    "reason": "`import math` 沒有子節點——名字是一個屬性。**沒有接點的元件不可能有『產出了宣告裡沒有的接點』**，所以這一筆恆為「無法確定」。\n⚠️ 它的正確性由這顆的 `spec.test.ts` 的來回釘住。"
  },
  {
    "componentId": "python:input",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。🟢 判準沒有被收窄——只是誠實地說「還沒量到」。\n🔴 `prompt` 接點**可以是空的**，而自證測釘了「不得被補成 `input(\"\")`」。"
  },
  {
    "componentId": "python:lambda",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "參數在 `lambda_parameters` 裡，而**沒有參數時那個節點整個不存在**（`lambda: 1`）——要找而不是取欄位。\n⚠️ 由 `spec.test.ts` 釘住：三種簽名的來回、帶預設值的整顆降級、以及照 key 排序。"
  },
  {
    "componentId": "python:literal_bool",
    "cause": "語料沒覆蓋",
    "reason": "spec 168 的 `True`／`False`／`None`。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🟢 沒有接點。⚠️ 而它有一件別的元件沒有的事要驗：**三個節點型別對應同一顆元件**（tree-sitter-python 把 `true`／`false`／`none` 切成三種節點），那由膠囊自證測釘。"
  },
  {
    "componentId": "python:literal_number",
    "cause": "語料沒覆蓋",
    "reason": "spec 167。理由與其餘 Python 元件同一條：這條護欄的語料是 C++ 的。\n🟢 它沒有接點，正確性由膠囊自證測釘住（4 支，含一條專測【浮點數是另一種 AST 節點】）。"
  },
  {
    "componentId": "python:literal_string",
    "cause": "語料沒覆蓋",
    "reason": "spec 167 的 Python 字面常數。理由與前兩顆 Python 元件同一條：這條護欄的語料是 C++ 的。\n🟢 而它【沒有接點】（children: {}），所以「宣告的接點對不對」對它是空的——它的正確性由膠囊自證測釘住（4 支，含一條反向：C++ 的字串不得被認成 Python 那顆）。"
  },
  {
    "componentId": "python:logic",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🟢 接點由膠囊自證測釘（含短路求值那一條）。"
  },
  {
    "componentId": "python:logic_not",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🟢 單一接點 `value`，由膠囊自證測釘。"
  },
  {
    "componentId": "python:loop_for",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。🟢 判準沒有被收窄——只是誠實地說「還沒量到」。\n🔴 `iterable` 是一個**插槽**不是次數欄位——Python 的 for 是 for-each。自證測釘了「走訪的不是 range 時也產得回去」。"
  },
  {
    "componentId": "python:loop_while",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。\n🟢 **判準沒有被收窄**——只是誠實地說「還沒量到」。\n🔴 接點 `body` 由自證測釘（迴圈裡的指派要接得進來）。"
  },
  {
    "componentId": "python:map_at_default",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "同上，而且引數格數是 1 或 2。⚠️ 接點（`obj`／`key`／`fallback`）由這顆自己的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "python:map_iter",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "`items`／`keys`／`values` 三個名字共用一個 `call` 節點型別。⚠️ 接點（`obj`）與屬性（`kind`）由這顆自己的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "python:map_make",
    "cause": "語料沒碰到這個接點的產出",
    "reason": "與同批串列字面同一個理由：`pairs` 接點空著也合法（`d = {}`）。來回與執行由 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "python:map_make_for",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "接點是策略從 `dictionary_comprehension` 的兄弟子節點組出來的（`for_in_clause`／`if_clause` 不是欄位），而走訪目標可能是 `pattern_list`（`for k, v in …`——字典生成式**幾乎都是這個形狀**）。\n⚠️ 由 `lambda/spec.test.ts` 釘住：有無篩選的來回、篩選有作用、以及「那些名字不得污染外面」。"
  },
  {
    "componentId": "python:math_abs",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:math_max",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:math_min",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:math_round",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "`round(x)` 與 `round(x, 2)` 的引數格數不同，判別要跑真邏輯。⚠️ 接點（`value`／`digits`）由這顆自己的 `spec.test.ts` 釘住，含「位數留空時不得產出一個逗號」。"
  },
  {
    "componentId": "python:member_at",
    "cause": "語料沒碰到這個接點的產出",
    "reason": "`obj` 接點必填，而護欄的語料裡取成員的那幾段（`math.pi`）走的是模組那條路。由這一族的 `spec.test.ts` 釘住（來回 ＋ 執行 `math.pi > 3`）。"
  },
  {
    "componentId": "python:method_call",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "接收者是 `attribute` 節點的 `object` 欄位——**一整個運算式**，而不是名字的一部分。這顆存在的理由就是把它從字串變回接點。\n⚠️ 由 `spec.test.ts` 與第五十條護欄的語料（鏈式呼叫、字面接收者、索引接收者）釘住。"
  },
  {
    "componentId": "python:negate",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🟢 單一接點 `value`，由膠囊自證測釘。"
  },
  {
    "componentId": "python:pair_make",
    "cause": "接點由父概念的字面值一起產出",
    "reason": "它只出現在字典字面裡，靜態掃描看到的是父概念那一層。鍵與值兩個接點由 `spec.test.ts` 的來回（`d = {\"k\": 1, \"j\": 2}`）釘住。"
  },
  {
    "componentId": "python:print",
    "cause": "語料沒覆蓋",
    "reason": "spec 156 的第一顆 Python 元件。⚠️ 語料**沒有任何 Python 程式碼**——這條護欄的語料是 C++ 的，所以它碰不到這顆。🔴 而那不是「補語料就好」：Python 的 lift 這一刀只做一個樣式，**完整的 Python 語料是階段 7 後續的事**。🟢 這顆此刻的正確性由 `spec 156` 的等價量測釘住，不是由這條護欄。"
  },
  {
    "componentId": "python:program",
    "cause": "語料沒覆蓋",
    "reason": "spec 160 的程式根。理由與 `python:print` 同一條：**這條護欄的語料是 C++ 的**，它碰不到任何 Python 元件。\n🟢 而這一顆的接點（`body`）由 `python-block-both-paths.test.ts` 的「`children` 宣告 ↔ lift 實際產出」那一支釘住——**那支是注射逼出來的**：改壞宣告之後其他五支一支都沒紅，代表那個宣告原本沒有任何東西在驗。"
  },
  {
    "componentId": "python:range_make",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:raw_code",
    "cause": "語料沒覆蓋",
    "reason": "spec 168 的降級落點。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🟢 而它**本來就碰不到**：它不從 AST 認出來（沒有 `astPattern`），是核心判定「這段我看不懂」時的落點。它的接點是空的（`children: {}`），所以「宣告的接點對不對」對它是空的。"
  },
  {
    "componentId": "python:raw_expression",
    "cause": "語料沒覆蓋",
    "reason": "spec 168 的降級落點（運算式位置）。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🟢 與 `python:raw_code` 同一個理由：不從 AST 認出來，且沒有接點。"
  },
  {
    "componentId": "python:return",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。🟢 判準沒有被收窄——只是誠實地說「還沒量到」。\n🔴 `value` 接點**可以是空的**（裸的 `return`），而自證測有一支專測「不得被補成 `return None`」——**空接點與「忘了接」在這裡是兩件事**。"
  },
  {
    "componentId": "python:string_insert",
    "cause": "沒有自己的 lift 路，接點由父概念的策略建出來",
    "reason": "這一格**沒有對應的 AST 節點型別**（`paths.lift` 是顯式的 `null`）——它只在格式化文字的策略拆解插值時被建出來，所以任何靜態掃描都不會看到它產出接點。\n⚠️ 它的接點與 `format` 參數由父概念的 `spec.test.ts` 一起釘住（「冒號是語法不是格式，不得留在值裡」那一支）。"
  },
  {
    "componentId": "python:string_join",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "同上。⚠️ 接點（`obj`／`value`）由這顆自己的 `spec.test.ts` 釘住——**接收者是分隔用的那一段文字**。"
  },
  {
    "componentId": "python:string_lower",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:string_make",
    "cause": "lift 是具名策略，靜態掃不到",
    "reason": "lift 那一路是 `lift-strategy.ts` 的一支具名策略——**接點是在策略裡跑迴圈組出來的**（把 `string_content` 與 `interpolation` 交錯放進 `parts`），所以靜態掃描看不到它產出哪些接點。\n⚠️ 而它的正確性由這顆自己的 `spec.test.ts` 釘住：lift（含**對照組**——普通字串必須仍走字面值那顆）、四種寫法的來回、以及執行的實際輸出。"
  },
  {
    "componentId": "python:string_replace",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "同上。⚠️ 接點（`obj`／`old`／`new`）由這顆自己的 `spec.test.ts` 釘住，含「三個引數的讓一般方法呼叫接手」。"
  },
  {
    "componentId": "python:string_split",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:string_strip",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "方法名的判別要跑真邏輯（同一個 `call` 節點型別）。⚠️ 接點（`obj`）由這顆自己的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "python:string_upper",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "這一族（內建的專屬積木）的接點都是策略從 `call` 節點組出來的——判別「被呼叫的名字是不是它」要跑真邏輯。\n⚠️ 由 `container_size/spec.test.ts` 的四組斷言釘住：每一顆認得自己的名字、**別的名字不得被搶走**、來回一字不差、執行走內建表那一份。"
  },
  {
    "componentId": "python:ternary",
    "cause": "語料沒碰到這個接點的產出",
    "reason": "三個接點都必填，而護欄的語料裡那一段（`\"成年\" if age >= 18 else \"未成年\"`）走的形狀靜態掃描認不到。\n🔴 由 `spec.test.ts` 釘住最重要的那一條：**索引 0 是「真的時候的值」不是條件**——照 C++ 的直覺對映的話，那個字串會被當成條件而**永遠為真**，而積木畫得出來、來回一字不差、執行永遠走同一邊。"
  },
  {
    "componentId": "python:try_catch",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "`except_clause` 是 `try_statement` 的兄弟子節點，樣式的 `fieldMappings` 走不到那一層。分支是可變數量的。\n⚠️ 由 `spec.test.ts` 釘住：兩個分支要是兩顆、有無名字的來回、以及「控制流訊號不得被抓走」。"
  },
  {
    "componentId": "python:tuple_make",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "接點是在策略裡組出來的（兩個節點型別共用一顆元件：有括號的 `tuple` 與裸的 `expression_list`）。\n⚠️ 而策略還把「有沒有括號」記進 `metadata.layoutHints`——**投影記住它，積木看不到它**。由 `spec.test.ts` 的三支來回釘住（含一格的 `(3,)`）。"
  },
  {
    "componentId": "python:var_assign",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。與其餘 Python 元件同一條理由：**這條護欄的語料是 C++ 的**，它碰不到 Python。\n🟢 **判準沒有被收窄**——只是誠實地說「還沒量到」。\n🔴 而它的接點（`value`）**確實需要被驗**——膠囊自證測有一支專門斷言「有了指派，運算式才碰得到」（貼上的程式碼每一行都是 `x = ...`，整句降級的話裡面的算術一顆都出不來）。"
  },
  {
    "componentId": "python:var_assign_compound",
    "cause": "語料沒碰到這個接點的產出",
    "reason": "`value` 接點必填，而樣式用 `constraints` 把左邊限定成單純變數名，語料裡的那幾段剛好都在迴圈裡。由 `spec.test.ts` 的六個運算子來回與三條 Python 除法規則釘住。"
  },
  {
    "componentId": "python:var_assign_sequence",
    "cause": "lift 走具名策略，靜態掃不到",
    "reason": "與同族固定組合同一個理由：目標名單是策略從 `pattern_list` 拆出來的。由 `spec.test.ts` 的來回與「格數對不上要出聲」釘住。"
  },
  {
    "componentId": "python:var_ref",
    "cause": "語料沒覆蓋",
    "reason": "spec 168。理由與其餘 Python 元件同一條：**這條護欄的語料是 C++ 的**（它掃 `tests/integration/*.test.ts` 裡的 C++ 片段），所以它碰不到任何 Python 元件。\n🔴 而那不是「補語料就好」——一份完整的 Python 語料是後續的事，**在它到位之前，誠實地說「還沒量到」比把判準收窄成「只掃 C++」好**：後者會讓這條護欄永遠答不出 Python 的問題。\n🟢 它沒有接點（只有 `name` 這個屬性），正確性由膠囊自證測釘住。"
  }
]
