[
  {
    "componentId": "cpp:analog_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:analog_resolution",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言）。"
  },
  {
    "componentId": "cpp:analog_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:bits_count",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`__builtin_popcount` 這個名字是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:delay_microseconds",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/micros/spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言，含「同一個時鐘」那條）。"
  },
  {
    "componentId": "cpp:dht_declare",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerPlainTypeComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。⚠️ 而 `declaresVariableType` 那一格才是關鍵：它讓作用域記住型別，之後那個變數上的方法才查得到主人。接點由同批的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "cpp:dht_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:dht_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:digital_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:digital_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:eeprom_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:eeprom_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:if_else",
    "cause": "宣告過的降級目標",
    "reason": "components.json 明文 skipPaths:['lift']、skipReasons.lift='degradation-target'，由 cpp:ternary 降級抵達。**它本來就不該從語料 lift 出來**——不是缺口。"
  },
  {
    "componentId": "cpp:include_local",
    "cause": "語料沒覆蓋",
    "reason": "`#include \"my.h\"`（雙引號的本地標頭）——測試檔裡的片段幾乎都用角括號的系統標頭。加一段語料就會動。"
  },
  {
    "componentId": "cpp:io_sync",
    "cause": "lift 是登錄表裡的資料，靜態掃不到",
    "reason": "這一路搬回膠囊的是**一筆資料**（`ios::sync_with_stdio` 這個名字屬於我），判別邏輯留在共用路由器。掃描器讀不到執行期才組起來的登錄表，所以歸「無法確定」——**而宣告與實際是一致的**：`children.value` 對上 `argSlots: ['value']`，膠囊自證測從真語料 lift 出這顆並斷言接點在原地。⚠️ 這與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:io_tie",
    "cause": "lift 是登錄表裡的資料，靜態掃不到",
    "reason": "這一路搬回膠囊的是**一筆資料**（`tie` 這個方法名屬於我），判別邏輯留在共用路由器。掃描器讀不到執行期才組起來的登錄表，所以歸「無法確定」——**而宣告與實際是一致的**：`children.value` 對上 `argSlots: ['value']`，膠囊自證測從真語料 lift 出這顆並斷言接點在原地。⚠️ 這與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:istringstream_declare",
    "cause": "語料沒覆蓋",
    "reason": "istringstream 宣告。sstream 的語料只有幾段而且都在做別的事。加一段就會動。"
  },
  {
    "componentId": "cpp:lcd_at",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:lcd_clear",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:lcd_declare",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerPlainTypeComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。⚠️ 而 `declaresVariableType` 那一格才是關鍵：它讓作用域記住型別，之後那個變數上的方法才查得到主人。接點由同批的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "cpp:lcd_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:lcd_print",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:math_constrain",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言）。"
  },
  {
    "componentId": "cpp:math_is_nan",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。"
  },
  {
    "componentId": "cpp:micros",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/micros/spec.test.ts` 釘住（lift／generate／round-trip／執行都有實際斷言，含「同一個時鐘」那條）。"
  },
  {
    "componentId": "cpp:millis",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:pin_attach",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是一支**具名策略**（`lift-strategy.ts` ＋ `lift-pattern.json`）——判別要跑真邏輯（結構比對 ＋ 走整棵樹查「這個名字有沒有被當腳位用」），所以靜態掃描看不到接點。⚠️ 而這顆**沒有子接點**（`children` 是空的），三個參數全是積木上的欄位。接點的正確性由這顆自己的 `spec.test.ts` 釘住（含「沒被當腳位用的常數不准被搶走」那條負向）。"
  },
  {
    "componentId": "cpp:pin_constant",
    "cause": "元概念",
    "reason": "它是 `lift-pattern.json` 的純資料樣式（六個識別字 → 一顆身分），**沒有接點**——`children` 是空的。與 `builtin_constant` 同一個形狀。"
  },
  {
    "componentId": "cpp:priority_queue_peek",
    "cause": "語料沒覆蓋",
    "reason": "priority_queue 的 top()。語料裡有 priority_queue 但沒有取頂端的片段。加一段就會動。"
  },
  {
    "componentId": "cpp:pulse_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 內建函式的覆蓋缺口，第 0 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pulse_read/spec.test.ts` 釘住（含「可選的第三格不得產出空逗號」與「它是運算式，回傳值進得了算術」兩條）。"
  },
  {
    "componentId": "cpp:pwm_attach",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:pwm_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:pwm_tie",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:pwm_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（ESP32 的 LEDC 與觸摸感應，第 3 批／階段 6.16）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/pwm_write/spec.test.ts` 釘住（五顆寫在一起，因為**它們共用同一份通道狀態**——分開測的話「舊版三件套湊起來會不會通」沒有任何一支測得到）。"
  },
  {
    "componentId": "cpp:queue_back",
    "cause": "辨識到不了",
    "reason": "**真的缺口**：`.back()` 在辨識層不看容器型別，六種容器一律判成 cpp:vector_back。這顆身分從程式碼永遠得不到，只能從積木拖。⚠️ **補再多語料也碰不到。** 見 draft/2026-08-11-容器方法的辨識不看型別.md"
  },
  {
    "componentId": "cpp:range_find_lower",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`lower_bound` 帶三個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_find_upper",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`upper_bound` 帶三個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_max",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`max_element` 帶兩個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_min",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`min_element` 帶兩個引數時是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：膠囊自證測從真語料 lift 出這顆，並斷言接點在原地、產出的碼與原文相同。⚠️ 與 `cpp:queue_back` 那種「辨識到不了」不同：那是真缺口，這是量測方法的邊界。"
  },
  {
    "componentId": "cpp:range_remap",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerCallComponent` 的一筆登錄（Arduino 執行期表面，spec 137）——判別邏輯留在共用檔，膠囊裡只有一筆資料，所以靜態掃描看不到接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住（lift／generate／round-trip／execute 都有實際斷言）。"
  },
  {
    "componentId": "cpp:raw_expression",
    "cause": "元概念",
    "reason": "同 cpp:raw_code，運算式位置的版本。"
  },
  {
    "componentId": "cpp:serial_count",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（`Serial` 上的方法，第 0 批／階段 6.16）——判別邏輯要看 `obj`，而分支住在執行期組起來的登錄表裡，靜態掃描看不到接點。⚠️ 這兩顆的接點是空的（零引數），而正確性由 `src/components/cpp/serial_count/spec.test.ts` 釘住（含「別的物件的 read 不得被搶走」與「沒有資料時回 -1 而 -1 是真」兩條）。"
  },
  {
    "componentId": "cpp:serial_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（`Serial` 上的方法，第 0 批／階段 6.16）——判別邏輯要看 `obj`，而分支住在執行期組起來的登錄表裡，靜態掃描看不到接點。⚠️ 這兩顆的接點是空的（零引數），而正確性由 `src/components/cpp/serial_count/spec.test.ts` 釘住（含「別的物件的 read 不得被搶走」與「沒有資料時回 -1 而 -1 是真」兩條）。"
  },
  {
    "componentId": "cpp:servo_attach",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:servo_declare",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerPlainTypeComponent` 的一筆登錄——判別邏輯留在共用檔，膠囊裡只有一筆資料。⚠️ 而 `declaresVariableType` 那一格才是關鍵：它讓作用域記住型別，之後那個變數上的方法才查得到主人。接點由同批的 `spec.test.ts` 釘住。"
  },
  {
    "componentId": "cpp:servo_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:servo_write",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:string_empty",
    "cause": "辨識到不了",
    "reason": "**真的缺口**：`.empty()` 一律判成中性的 cpp:container_empty。同上，補語料無效。"
  },
  {
    "componentId": "cpp:ultrasonic_trigger",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是一支**具名策略**，而它比別顆更特別：它認的是**一串連續語句**，掛在 `compound_statement` 與 `translation_unit` 上、自己走訪子節點。靜態掃描看不到它產出的 `pin` 接點。⚠️ 接點的正確性由這顆自己的 `spec.test.ts` 釘住，而那支測試的重點是**四條「不該摺時不摺」**（中間插句／微秒數不對／不同腳／換算不摺）——那才是這顆的主要風險。"
  },
  {
    "componentId": "cpp:vector_make",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "這一路回家的是**判別**（`vector<T>(…)` 這個呼叫形狀是我），而掃描器讀不到執行期才組起來的登錄表。歸「無法確定」——**而宣告與實際是一致的**：`children.size`／`children.fill` 對上分支產出的兩個接點，膠囊自證測從真語料 lift 出這顆。"
  },
  {
    "componentId": "cpp:wifi_open",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "cpp:wifi_read",
    "cause": "lift 是登錄表裡的資料或分支，靜態掃不到",
    "reason": "lift 那一路是 `registerMethodBranch` 的一個分支（第 2 批套件物件／階段 6.16）——判別要跑真邏輯（查接收者的**宣告型別**），所以靜態掃描看不到接點。⚠️ 接點的正確性由 `src/components/cpp/servo_write/spec.test.ts` 釘住（十九顆寫在一起，因為**它們共用同一個辨識機制**——而那支測試的重點是「型別查不到就不猜」那條負向）。"
  },
  {
    "componentId": "python:print",
    "cause": "語料沒覆蓋",
    "reason": "spec 156 的第一顆 Python 元件。⚠️ 語料**沒有任何 Python 程式碼**——這條護欄的語料是 C++ 的，所以它碰不到這顆。🔴 而那不是「補語料就好」：Python 的 lift 這一刀只做一個樣式，**完整的 Python 語料是階段 7 後續的事**。🟢 這顆此刻的正確性由 `spec 156` 的等價量測釘住，不是由這條護欄。"
  },
  {
    "componentId": "python:program",
    "cause": "語料沒覆蓋",
    "reason": "spec 160 的程式根。理由與 `python:print` 同一條：**這條護欄的語料是 C++ 的**，它碰不到任何 Python 元件。\n🟢 而這一顆的接點（`body`）由 `python-block-both-paths.test.ts` 的「`children` 宣告 ↔ lift 實際產出」那一支釘住——**那支是注射逼出來的**：改壞宣告之後其他五支一支都沒紅，代表那個宣告原本沒有任何東西在驗。"
  },
  {
    "componentId": "python:literal_string",
    "cause": "語料沒覆蓋",
    "reason": "spec 167 的 Python 字面常數。理由與前兩顆 Python 元件同一條：這條護欄的語料是 C++ 的。\n🟢 而它【沒有接點】（children: {}），所以「宣告的接點對不對」對它是空的——它的正確性由膠囊自證測釘住（4 支，含一條反向：C++ 的字串不得被認成 Python 那顆）。"
  },
  {
    "componentId": "python:literal_number",
    "cause": "語料沒覆蓋",
    "reason": "spec 167。理由與其餘 Python 元件同一條：這條護欄的語料是 C++ 的。\n🟢 它沒有接點，正確性由膠囊自證測釘住（4 支，含一條專測【浮點數是另一種 AST 節點】）。"
  }
]
